---
title: newrelic_get_linking_metadata
type: apiDoc
shortDescription: データをトレースやエンティティにリンクするために必要なメタデータのコレクションを返します。
tags:
  - Agents
  - PHP agent
  - PHP agent API
translationType: machine
---

## 構文

```
newrelic_get_linking_metadata()
```

データをトレースやエンティティにリンクするために必要なメタデータのコレクションを返します。

## 要件

Requires PHP agent [version 9.3 or higher](/docs/release-notes/agent-release-notes/php-release-notes).

[トランザクションの中で呼び出す必要があります](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#transaction) 。

## 説明

このコールは、このアプリケーションとNew Relicバックエンドの他のデータとの関連付けに使用できる、キーと値のペアの不透明なマップを返します。

## 例

### リンク用ペイロードの作成

リンクデータを取得し、ツールにエクスポートするためにJSONとしてエンコードします。

```
if (extension_loaded('newrelic')) {
  $linking_md = newrelic_get_linking_metadata();
  $payload = json_encode($linking_md);
}
```