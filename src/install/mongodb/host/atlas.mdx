---
componentType: default
optionType: deployment
headingText: Prepare Atlas
descriptionText: "" 
---

Using the Atlas CLI, run the following command to create a new user, and assign `clusterMonitor` and `readAnyDatabase` roles to the user (adjust the `username` and `password` as required):

```shell
atlas dbuser create -u username -p password --role clusterMonitor,readAnyDatabase
```

You can also use the MongoDB Atlas UI to create your user and assign the same specific privileges. For more, information check the [MongoDB Atlas](https://www.mongodb.com/docs/atlas/security-add-mongodb-users/#add-database-users) documentation.
