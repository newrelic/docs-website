---
componentType: default
optionType: deployment
headingText: Download the MongoDB integration using APT
descriptionText: "" 
---

Run these commands:

    ```shell
    sudo apt-get update
    ```

    ```shell
    sudo apt-get install nri-mongodb3
    ```
