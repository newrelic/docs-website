---
componentType: default
headingText: Download the RabbitMQ integration using APT
---

In your terminal, run these commands:

```shell
sudo apt-get update
```

```shell
sudo apt-get install nri-rabbitmq
```
