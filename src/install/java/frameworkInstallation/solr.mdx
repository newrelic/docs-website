---
componentType: appInfo
optionType: framework 
headingText: Finish your installation
descriptionText: To complete your installation, follow these framework-specific steps. If you need a different framework, select it at the top of this page.
---

### Pass your argument

To pass the `-javaagent` argument on Solr:

<Tabs>
	<TabsBar>
		<TabsBarItem id="solr-standalone-5">Standalone Solr 5.x+</TabsBarItem>
		<TabsBarItem id="solr-standalone-other">Standalone Solr 4.x or lower</TabsBarItem>
		<TabsBarItem id="solr-app-server">App server Solr</TabsBarItem>
	</TabsBar>

	<TabsPages>
		<TabsPageItem id="solr-standalone-5">
            Add the `-javaagent` property to `bin/solr.in.sh`:

            ```java
            SOLR_OPTS="$SOLR_OPTS -javaagent:/full/path/to/newrelic.jar"
            ```
		</TabsPageItem>
		<TabsPageItem id="solr-standalone-other">
            Add the `-javaagent` before the `start.jar`:

            ```java
            java -javaagent:/full/path/to/newrelic.jar -jar start.jar
            ```

		</TabsPageItem>
		<TabsPageItem id="solr-app-server">
            When running Solr in an **application server**, follow the instructions of that application server to add the `-javaagent` flag. Also ensure that JMX is enabled for the application server. If you don't see data in the APM UI's Solr page, follow the [troubleshooting procedures for Solr data](/docs/agents/java-agent/troubleshooting/solr-data-not-appearing-apm-solr-tab-java).
		</TabsPageItem>
	</TabsPages>
</Tabs>