---
componentType: appInfo
optionType: additional-environments
headingText: Before you install the integration with Azure Kubernetes Service (AKS)
---

As you get ready to install the integration, note that the Kubernetes integration only monitors worker nodes in the Azure Kubernetes Service because Azure abstracts the management of master nodes away from the Kubernetes platform.
