---
componentType: default
headingText: Install the MySQL integration
---

1. [Download the .MSI installer image for New Relic's MySQL integration](https://download.newrelic.com/infrastructure_agent/windows/integrations/nri-mysql/nri-mysql-amd64.msi).

2. Install New Relic's MySQL integration by openning command prompt and running:
    ```shell
    msiexec.exe /qn /i $PATH_TO\nri-mysql-amd64.msi
    ```

3. In the `integrations` directory, `C:\Program Files\New Relic\newrelic-infra\integrations.d\`, create a copy of the sample configuration file by running:
    ```shell
    copy mysql-config.yml.sample mysql-config.yml
    ```

4. Edit the `mysql-config.yml` file by using one of the [`mysql-config.yml` sample files](#examples).

5. To enable automatic MySQL error log parsing and forwarding, copy or rename the `mysql-log-win.yml.example` file to `mysql-log-win.yml`:
    ```shell
    ren "C:\Program Files\New Relic\newrelic-infra\logging.d\mysql-log-win.yml.example" mysql-log-win.yml
    ```

6. Restart the infrastructure agent. See how to [restart the infrastructure agent in different Linux environments](/docs/infrastructure/install-infrastructure-agent/manage-your-agent/start-stop-restart-infrastructure-agent/#linux).
