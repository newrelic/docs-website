---
componentType: default
headingText: Download using Yum
---

From the command line, run these commands:

```shell
sudo yum -q makecache -y --disablerepo='*' --enablerepo='newrelic-infra'
```

```shell
sudo yum install nri-memcached
```
