---
componentType: default
headingText: Download the Hashicorp integration using APT
---

In your terminal, run these commands:

    ```shell
    sudo apt-get update
    ```

    ```shell
    sudo apt-get install nri-Hashicorp
    ```