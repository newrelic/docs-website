---
componentType: default
headingText: Download the Kafka integration using APT
---

Run these commands:

    ```shell
    sudo apt-get update
    ```

    ```shell
    sudo apt-get install nri-kafka
    ```
