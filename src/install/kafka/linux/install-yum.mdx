---
componentType: default
optionType: deployment
headingText: Download the Kafka integration using Yum
descriptionText: "" 
---

Run these commands:

    ```shell
    sudo yum -q makecache -y --disablerepo='*' --enablerepo='newrelic-infra'
    ```

    ```shell
    sudo yum install nri-kafka
    ```
