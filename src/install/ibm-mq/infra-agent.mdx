---
componentType: default
headingText: Install the infrastructure agent
---

To use the IBM MQ integration, you need to also [install the infrastructure agent](/docs/infrastructure/install-infrastructure-agent/get-started/install-infrastructure-agent-new-relic/) on the same host and replace the `INTEGRATION_FILE_NAME` variable with `nri-ibmmq`. 
