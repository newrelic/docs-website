---
headingText: Check the compatibility and requirements
componentType: default
---

To enable the integration, ensure you meet these requirements:

* TLS authentication version TLS 1.2 and above.
* SQL Server user or domain user with user privileges for both `CONNECT` and `VIEW SERVER STATE`, and `READ` access permissions.
