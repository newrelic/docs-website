---
componentType: default
headingText: Download using Zypper
---

In your terminal, run these commands:

```shell
sudo zypper -n ref -r newrelic-infra
```

```shell
sudo zypper -n install nri-mssql
```
