---
headingText: Download and install the agent
componentType: default
---

How do I skip this if users select Docker?

Run this command on your server to download and install the Python APM agent:

```
pip install newrelic
```