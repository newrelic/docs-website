---
componentType: default
headingText: Download the JMX integration using Yum
---

In your terminal, run these commands:

```shell
sudo yum -q makecache -y --disablerepo='*' --enablerepo='newrelic-infra'
```

```shell
sudo yum install nri-jmx
```
