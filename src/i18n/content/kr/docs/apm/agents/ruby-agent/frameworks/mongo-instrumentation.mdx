---
title: 몽고 계측
tags:
  - Agents
  - Ruby agent
  - Instrumented gems
metaDescription: New Relic Ruby agent support for MongoDB
freshnessValidatedDate: never
translationType: machine
---

Ruby 에이전트에는 [`mongo`](https://rubygems.org/gems/mongo "링크가 새 창에서 열립니다.") gem을 통해 생성된 MongoDB 쿼리를 위한 내장 도구가 있습니다.

## 지원되는 보석 [#supported_gems]

이것은 버전별 gem에 대한 Ruby 에이전트의 지원을 요약합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        <DNT>
          **Gem version**
        </DNT>
      </th>

      <th>
        <DNT>
          **Comments**
        </DNT>
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `mongo` 1.8 및 1.9
      </td>

      <td>
        New Relic의 Ruby 에이전트 3.7.1 이상에서 지원
      </td>
    </tr>

    <tr>
      <td>
        `mongo` 2.0
      </td>

      <td>
        지원되지 않음
      </td>
    </tr>

    <tr>
      <td>
        `mongo` 2.1 이상
      </td>

      <td>
        New Relic의 Ruby 에이전트 3.13.1 이상이 필요합니다.
      </td>
    </tr>

    <tr>
      <td>
        기타 보석
      </td>

      <td>
        Mongoid 2/3/4 및 Moped에 대한 지원은 현재 타사 gem을 통해서만 제공됩니다. 관련 프로젝트에 대한 링크는 [extends_newrelic_rpm](https://github.com/newrelic/extends_newrelic_rpm) 을 참조하십시오.
      </td>
    </tr>
  </tbody>
</table>

## 타사 및 extends_newrelic_rpm 계측 [#third_party_instrumentation]

`extends_newrelic_rpm` 및 `newrelic_mongo` gem은 모두 `mongo` 에 대한 기본 계측을 제공합니다. New Relic은 Ruby 에이전트의 내장 Mongo 지원과 함께 이러한 gem을 통해 Mongo 계측을 실행하는 것을 권장하지 않습니다. 이는 전체 데이터베이스 시간과 같은 메트릭을 두 배로 계산할 수 있습니다.

또한 `extends_newrelic_rpm` 및 `newrelic_mongo` gem은 모두 사용자 환경에서 해당 gem을 완전히 제거할 수 없는 경우 비활성화하는 데 사용할 수 있는 고유한 설정을 사용합니다. 해당 타사 보석의 경우 <DNT>**newrelic.yml**</DNT> 에 `disable_mongodb: false` 설정하세요.

## 계측 비활성화 [#Disabling_instrumentation]

에이전트에서 Mongo 계측을 비활성화하려면 다음을 <DNT>**newrelic.yml**</DNT> 에 추가하세요.

```
disable_mongo: true
```
