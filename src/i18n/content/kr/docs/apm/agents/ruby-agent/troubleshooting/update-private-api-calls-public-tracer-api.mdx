---
title: 공개 추적자 API에 대한 비공개 API 호출 업데이트
type: troubleshooting
tags:
  - Agents
  - Ruby agent
  - Troubleshooting
metaDescription: Update deprecated API calls for the New Relic Ruby agent.
translationType: machine
---

## 문제

New Relic Ruby 에이전트 버전 [6.0.0](/docs/releases/ruby) 에서는 공개적으로 지원되지 않는 많은 API가 더 이상 사용되지 않거나 삭제되었습니다. 삭제된 방법을 사용하는 경우 Ruby 에이전트 버전 6.0.0 이상으로 업그레이드하기 전에 권장되는 대체 방법으로 API를 업데이트해야 합니다. 더 이상 사용되지 않는 방법을 사용하는 경우 6.0.0 이상의 에이전트 버전과의 호환성을 보장하기 위해 최대한 빨리 API를 업데이트하는 것이 좋습니다.

## 해결책

다음 권장 대체품으로 API를 업데이트하십시오.

<CollapserGroup>
  <Collapser
    id="external-start-segment"
    title={<InlineCode>External.start_segment</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.start_external_request_segment`
  </Collapser>

  <Collapser
    id="transaction-create-segment"
    title={<InlineCode>Transaction.create_segment</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.start_segment`
  </Collapser>

  <Collapser
    id="transaction-start"
    title={<InlineCode>트랜잭션 시작</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.start_transaction_or_segment`
  </Collapser>

  <Collapser
    id="transaction-start-datastore-segment"
    title={<InlineCode>Transaction.start_datastore_segment</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.start_datastore_segment`
  </Collapser>

  <Collapser
    id="transaction-start-segment"
    title={<InlineCode>Transaction.start_segment</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.start_segment`
  </Collapser>

  <Collapser
    id="transaction-wrap"
    title={<InlineCode>트랜잭션.랩</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.in_transaction`
  </Collapser>

  <Collapser
    id="transaction-state-current-transaction"
    title={<InlineCode>TransactionState.current_transaction</InlineCode>}
  >
    다음으로 대체 `NewRelic::Agent::Tracer.current_transaction`
  </Collapser>
</CollapserGroup>