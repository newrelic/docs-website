---
title: 'Java 에이전트 구성: 구성 파일'
tags:
  - Agents
  - Java agent
  - Configuration
metaDescription: 'New Relic''s Java agent config settings for APM, including transaction tracer, errors, custom instrumentation, distributed tracing, system properties, etc.'
translationType: human
---

import apmJavaConfigPrecedence from 'images/apm_diagram_Java-config-precedence.png'

뉴렐릭 Java 에이전트는 `newrelic.yml` 파일에서 구성을 읽습니다. 기본적으로 에이전트는 `newrelic.jar`가 포함된 [디렉토리](/docs/agents/manage-apm-agents/troubleshooting/find-agent-root-directory#java-agent)에서 이 파일을 찾습니다. [`newrelic.config.file`](#newrelic-config-file) 시스템 속성을 정규화된 파일 이름으로 설정하여 구성 파일의 위치를 재정의할 수 있습니다.

[뉴렐릭 계정](https://newrelic.com/signup)(영구 무료)을 만들고 Java 에이전트를 [설치](/docs/apm/agents/java-agent/installation/install-java-agent/)한 후 환경에 맞게 Java 에이전트를 구성할 수 있습니다.

## 구성 파일 구조 [#Structure]

`newrelic.yml` 파일은 다양한 환경에 해당하는 스탠자로 분할됩니다.

* 테스트
* 개발
* 스테이징
* 프로덕션(기본값)

New Relic은 공통 스탠자의 설정을 이러한 환경 각각에 적용합니다. [`newrelic.environment`](#newrelic-environment) 시스템 속성을 환경 이름으로 설정하여 다른 환경을 기본값으로 선택할 수 있습니다.

<Callout variant="tip">
  [`newrelic.yml` 템플릿](/docs/java/java-agent-config-file-template)을 사용할 수 있습니다.
</Callout>

`newrelic.yml`을 편집하는 경우, [YAML 형식](https://en.wikipedia.org/wiki/YAML)을 준수하도록 주의하십시오. 뉴렐릭의 Java 에이전트와 함께 파일을 사용하기 전에 YAML 유효성 검사기를 사용하여 구문이 정확한지 확인하고 다음 규칙을 따릅니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Java agent newrelic.yml**
      </th>

      <th>
        **요구 사항**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        포맷
      </td>

      <td>
        YML 파일은 대소문자를 구분합니다.
      </td>
    </tr>

    <tr>
      <td>
        들여쓰기
      </td>

      <td>
        모든 들여쓰기는 2자씩 증가해야 합니다. 다른 들여쓰기는 에이전트 시작 시 `Unable to parse configuration file` 오류를 발생시킵니다.

        * 파일의 동일한 스탠자에 있는 데이터에 대해 동일한 수준의 들여쓰기를 사용합니다.
        * 하위 조항을 추가로 2칸 들여씁니다.
      </td>
    </tr>

    <tr>
      <td>
        파일 변경 사항
      </td>

      <td>
        변경 사항을 적용하려면 JVM 호스트 프로세스를 다시 시작해야 합니다.

        **예외:** `log_level` 및 `audit_mode`에 대한 속성 변경은 다시 시작할 필요가 없습니다. 서킷 브레이커에서의 속성 변경은 재시작이 필요하지 않습니다.
      </td>
    </tr>
  </tbody>
</table>

## 구성 설정 우선순위 [#config-options-precedence]

구성 파일의 설정을 재정의하려면 [시스템 속성 재정의](#System_Properties)를 사용합니다. 특정 환경에서는 [환경 변수](#Environment_Variables)를 사용하여 구성 파일과 시스템 속성을 모두 재정의할 수도 있습니다. 환경 변수는 주로 Heroku를 지원하기 위해 존재합니다. 사용하면 [서버 측 구성](/docs/agents/manage-apm-agents/configuration/server-side-agent-configuration)이 다른 모든 구성 설정보다 우선 적용됩니다.

<img
  title="java-config-order.png"
  alt="java-config-order.png"
  src={apmJavaConfigPrecedence}
/>

<figcaption>
  Java 에이전트를 사용하면 서버 측 구성이 다른 모든 설정을 재정의합니다. 환경 변수는 Java 시스템 속성을 재정의합니다. 자바 속성은 `newrelic.yml` 파일의 사용자 구성 설정을 재정의합니다. 사용자 설정은 `newrelic.yml` 기본 설정보다 우선합니다.
</figcaption>

## Java 확장 디렉터리 구성 [#extensions-directory]

Java 에이전트는 프로세스 시작 시 구성 파일을 읽습니다. 파일이 있는 디렉터리를 식별하려면 새 디렉터리를 생성하거나 기존 확장 디렉터리를 지정하십시오.

<CollapserGroup>
  <Collapser
    id="create-extensions-directory"
    title="확장 디렉터리 생성"
  >
    확장 디렉터리를 생성하려면:

    1. `newrelic.jar` 및 `newrelic.yml`이 있는 디렉터리로 이동합니다. 이름이 `extensions`인 디렉터리를 만듭니다.
    2. `newrelic.yml`에서 [`extensions.dir`](#cfg-extensions-dir) 속성이 설정되지 않았는지 확인합니다.
  </Collapser>

  <Collapser
    id="specify-extensions-directory"
    title="기존 확장 디렉터리 지정"
  >
    기존 Java 확장 디렉터리를 사용하려면:

    1. `newrelic.yml` 파일에서 `common` 섹션을 찾습니다.
    2. [`extensions.dir`](#cfg-extensions-dir) 속성을 사용하여 파일 위치를 지정합니다.
  </Collapser>
</CollapserGroup>

## 일반 구성 설정 [#General]

`common` 스탠자에서 이러한 옵션을 설정합니다. 이러한 옵션을 [재정의](#System_Properties)하려면 `newrelic.config` 접두사가 붙은 시스템 속성을 사용합니다.

<CollapserGroup>
  <Collapser
    id="cfg-license_key"
    title="license_key(필수)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 **필수**입니다. 뉴렐릭 계정과 연결된 [라이선스 키](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)를 지정해야 합니다. 이 키는 에이전트의 데이터를 뉴렐릭 서비스의 계정에 연결합니다.
  </Collapser>

  <Collapser
    id="cfg-app_name"
    title="app_name(필수)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 **필수** 입니다. 뉴렐릭에 데이터를 보고하는 데 사용되는 [애플리케이션 이름](/docs/site/naming-your-application)을 정의합니다.

    [`enable_auto_app_naming`](#cfg-enable_auto_app_naming)이 false인 경우, 에이전트는 이 애플리케이션에 모든 데이터를 보고합니다. 그렇지 않으면 에이전트는 이 애플리케이션에 백그라운드 작업(웹이 아닌 애플리케이션에 대한 트랜잭션)만 보고합니다.

    [둘 이상의 애플리케이션](/docs/java/multiple-application-names)에 데이터를 보고하려면 애플리케이션 이름을 세미콜론으로 구분합니다. 예를 들어, **My Application** 및 **My Application 2**에 데이터를 보고하려면 다음을 사용합니다.

    ```yml
    app_name: My Application;My Application 2
    ```

    애플리케이션 이름을 지정하는 더 많은 방법은 [Java 애플리케이션 이름 지정](/docs/agents/java-agent/configuration/name-your-java-application)을 참조하세요.
  </Collapser>

  <Collapser
    id="cfg-agent_enabled"
    title="agent_enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트를 활성화하는 플래그입니다. 에이전트를 강제 실행하거나 실행하지 않으려면 이 설정을 사용하십시오.
  </Collapser>

  <Collapser
    id="cfg-apdex_t"
    title="apdex_t(더 이상 사용되지 않음)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Float
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `1.0`
          </td>
        </tr>
      </tbody>
    </table>

    애플리케이션의 [Apdex](/docs/site/apdex-measuring-user-satisfaction) 점수에 대한 `apdex_t` 임계값(초)입니다. Java 에이전트 버전 1.2.008 이상의 경우 `apdex_t` 값이 UI에서 설정되고 `newrelic.yml`의 값은 무시됩니다.
  </Collapser>

  <Collapser
    id="cfg-appserver_port"
    title="appserver_port"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    동일한 시스템의 동일한 앱에 대한 JVM을 구별하기 위한 번호입니다. 뉴렐릭[은 고유성을 위해 호스트/포트를 사용](/docs/agents/java-agent/configuration/hostname-logic-java#unique-identifier)하므로 각 JVM의 시작 인수에 다음과 같은 스위치를 넣어 JVM을 구별할 수 있습니다.

    ```ini
    -Dnewrelic.config.appserver_port=<var>8081</var>
    ```

    `appserver_port`를 사용하여 JVM의 이름을 지정하고 다시 시작하면 드롭다운 및 프로파일링 인터페이스에서 개별적으로 볼 수 있어야 합니다.

    <Callout variant="important">
      이것은 뉴렐릭을 위한 변경 사항일 뿐입니다. 호스트가 어떤 식으로든 통신하는 포트에는 실제로 영향을 미치지 않습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-audit_mode"
    title="audit_mode"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트 로그 파일에 New Relic으로 전송된 모든 데이터의 일반 텍스트 로깅을 활성화합니다. 이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.
  </Collapser>

  <Collapser
    id="ca_config_bundle"
    title="ca_bundle_path"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            값 형식
          </th>

          <td>
            `/path/to/ca/cert/bundle.pem`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 뉴렐릭에 대한 보안 연결을 설정하는 데 사용할 커스텀 SSL 인증서 번들의 경로를 지정합니다. 커스텀 SSL 인증서 번들에 뉴렐릭에 연결하기에 충분한 인증서가 포함되어 있지 않으면 [필요한 인증서를 커스텀 인증서 번들에 병합](/docs/agents/java-agent/troubleshooting/ssl-or-connection-errors-java)해야 합니다.
  </Collapser>

  <Collapser
    id="use_private_ssl"
    title="use_private_ssl"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    다음 SSL 인증서가 에이전트 jar에 번들로 제공됩니다.

    ```
    META-INF/certs/eu-newrelic-com.pem
    META-INF/certs/eu01-nr-data-net.pem
    META-INF/certs/newrelic-com.pem
    ```

    기본적으로( `use_private_ssl: false` ) 에이전트는 JDK에 번들된 SSL 인증서를 사용하여 `ca_bundle_path`에서 지정한 뉴렐릭 또는 사용자 정의 SSL 인증서 번들에 대한 보안 연결을 설정합니다. 에이전트와 함께 번들로 제공되는 SSL 인증서를 사용하려면 `use_private_ssl: true`를 설정합니다.

    참고: &#x7B; `ca_bundle_path`가 설정된 경우 `use_private_ssl`은 무시됩니다.
  </Collapser>

  <Collapser
    id="cfg-enable_auto_app_naming"
    title="enable_auto_app_naming"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    각 웹 앱에 대해 개별적으로 데이터 보고를 활성화합니다. 자동 앱 이름 지정 지원을 활성화하려면 `true`로 설정합니다. 각 웹 앱의 이름은 자동으로 감지되고 에이전트는 각각에 대해 별도로 데이터를 보고합니다. 이는 뉴렐릭의 웹 앱에 대한 더욱 세분화된 성능 분석을 제공합니다.

    자세한 내용은 [자동 애플리케이션 이름 지정](/docs/agents/java-agent/configuration/automatic-application-naming)을 참조하십시오.

    애플리케이션 이름을 지정하는 더 많은 방법은 [Java 애플리케이션 이름 지정](/docs/agents/java-agent/configuration/name-your-java-application)을 참조하세요.
  </Collapser>

  <Collapser
    id="cfg-enable_auto_transaction_naming"
    title="enable_auto_transaction_naming"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    구성 요소 기반 트랜잭션 이름 지정을 활성화합니다. 구성 요소 기반 트랜잭션 이름 지정을 활성화하려면 `true`로 설정합니다. 웹 요청의 URI를 트랜잭션 이름으로 사용하려면 `false`로 설정합니다. 자세한 내용은 [웹 트랜잭션 이름 지정](/docs/agents/java-agent/instrumentation/naming-web-transactions)을 참조하십시오.

    <Callout variant="caution">
      API 호출을 구현하여 트랜잭션 이름을 지정하지 않는 한 자동 트랜잭션 이름 지정을 비활성화하면 [측정 항목 그룹화 문제](/docs/features/metric-grouping-issues)가 발생할 가능성이 매우 높습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-enable_custom_tracing"
    title="enable_custom_tracing"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [`@Trace` 주석을 사용하여](/docs/agents/java-agent/custom-instrumentation/java-instrumentation-annotation) 모든 계측을 활성화합니다. 이것을 비활성화하면 `@Trace` 주석이 무시됩니다.
  </Collapser>

  <Collapser
    id="event_ingest_uri"
    title="event_ingest_uri"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            [`https://insights-collector.newrelic.com/v1/accounts/events`](https://insights-collector.newrelic.com/v1/accounts/events)
          </td>
        </tr>
      </tbody>
    </table>

    일부 에이전트 기능에서 사용하는 이벤트 수집 URI입니다. 기본값은 미국 프로덕션 수집 엔드포인트입니다. 이벤트를 다른 수집 엔드포인트로 보내도록 수동으로 구성할 수 있습니다. (예: EU 프로덕션: [`https://insights-collector.eu01.nr-data.net/v1/accounts/events`](https://insights-collector.eu01.nr-data.net/v1/accounts/events))
  </Collapser>

  <Collapser
    id="cfg-extensions-dir"
    title="extension.dir"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    선택적 [확장 디렉터리](#extensions-directory)의 위치를 정의합니다. 이 속성이 설정되지 않은 경우 에이전트는 `newrelic.jar` 및 `newrelic.yml`과 동일한 디렉터리에서 `extensions`라는 하위 디렉터리를 찾습니다.
  </Collapser>

  <Collapser
    id="cfg-enable_high_security"
    title="high_security"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    [높은 수준의 보안](/docs/subscriptions/high-security)을 활성화하려면 이 속성을 `true`로 설정하고 뉴렐릭 사용자 인터페이스의 높은 보안 속성을 활성화해야 합니다. 높은 수준의 보안을 활성화하면 SSL이 켜져 있고 요청 및 메시지 대기열 매개변수가 수집되지 않으며 쿼리를 원시 형식으로 뉴렐릭에 보낼 수 없습니다.

    <Callout variant="important">
      [Java 에이전트 3.48.0](/docs/release-notes/agent-release-notes/java-release-notes/java-agent-3480)부터 SSL은 기본적으로 활성화되어 있으며 비활성화하는 구성 옵션은 더 이상 사용되지 않습니다. [Java 에이전트 4.0.0](/docs/release-notes/agent-release-notes/java-release-notes/java-agent-400)부터 SSL을 비활성화하는 기능이 제거되었습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-insert_api_key"
    title="insert_api_key"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    계정에 대한 유효한 [API 키 삽입](/docs/apis/get-started/intro-apis/types-new-relic-api-keys#event-insert-key). 현재 에이전트에서 사용되고 있지 않습니다.
  </Collapser>

  <Collapser
    id="labels"
    title="labels"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    이 앱에 [태그](/docs/apm/new-relic-apm/maintenance/labels-categories-organize-your-apps-servers)를 부착합니다.

    이제 이 옵션은 태그를 활성화하며, 라벨 기능을 대체합니다. 기존의 [라벨](/docs/new-relic-one/use-new-relic-one/core-concepts/tagging-use-tags-organize-group-what-you-monitor#labels)은 계속 쿼리할 수 있습니다. 다음과 같이 세미콜론(;)을 구분 기호로 사용하여 여러 태그를 첨부할 수 있습니다.

    ```ini
    -Dnewrelic.config.labels="tagName1:tagValue1;tagName2:tagValue2;tagName3:tagValue3"
    ```
  </Collapser>

  <Collapser
    id="max-stack-trace-lines"
    title="max_stack_trace_lines"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `30`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 각 스택 추적에서 수집하는 라인 수를 제한합니다. 이 값을 높이면 에이전트가 사용하는 메모리 양과 뉴렐릭으로 전송되는 데이터 양이 증가하므로 성능에 영향을 미칠 수 있습니다.
  </Collapser>

  <Collapser
    id="metric_ingest_uri"
    title="metric_ingest_uri"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            [`https://metric-api.newrelic.com/metric/v1`](https://metric-api.newrelic.com/metric/v1)
          </td>
        </tr>
      </tbody>
    </table>

    일부 에이전트 기능에서 사용하는 메트릭 인제스트 URI입니다. 기본값은 미국 프로덕션 수집 엔드포인트입니다. 다른 수집 엔드포인트로 메트릭을 보내도록 수동으로 구성할 수 있습니다. (예: EU 프로덕션 URI: [`https://metric-api.eu.newrelic.com/metric/v1`](https://metric-api.eu.newrelic.com/metric/v1))
  </Collapser>

  <Collapser
    id="cfg-proxy_host"
    title="proxy_host"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    [뉴렐릭 수집기](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#collector)에 연결할 프록시 호스트입니다. 프록시를 사용하는 경우 호스트 설정이 필요합니다. 다른 프록시 설정은 선택 사항입니다.
  </Collapser>

  <Collapser
    id="cfg-proxy_password"
    title="proxy_password"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    프록시 인증을 위한 비밀번호입니다. 프록시를 사용하는 경우 호스트 설정이 필요합니다. 다른 프록시 설정은 선택 사항입니다. 사용자 이름 및 비밀번호 설정은 프록시 서버에서 기본 인증 챌린지를 인증하는 데 사용됩니다.

    <Callout variant="important">
      Java 에이전트는 기본(일반 텍스트) 인증을 지원합니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-proxy_port"
    title="proxy_port"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `8080`
          </td>
        </tr>
      </tbody>
    </table>

    프록시 호스트 포트 번호입니다. 프록시를 사용하는 경우 호스트 설정이 필요합니다. 다른 프록시 설정은 선택 사항입니다.
  </Collapser>

  <Collapser
    id="cfg-proxy_user"
    title="proxy_user"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    기본(일반 텍스트) 인증과 같은 프록시 인증을 위한 사용자 이름입니다. 프록시를 사용하는 경우 호스트 설정이 필요합니다. 다른 프록시 설정은 선택 사항입니다. 사용자 이름 및 비밀번호 설정은 프록시 서버에서 기본 인증 챌린지를 인증하는 데 사용됩니다.
  </Collapser>

  <Collapser
    id="cfg-proxy_scheme"
    title="proxy_scheme"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    사용된 프록시 체계입니다. `proxy_scheme: "https"`를 설정하면 에이전트가 HTTPS 체계를 사용하여 프록시를 통해 연결할 수 있습니다.
  </Collapser>

  <Collapser
    id="cfg-reactor-netty-errors-enabled"
    title="Reactor-netty.errors.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    Reactor Netty에 대해 오류가 보고되는지 여부입니다. 기본값은 false입니다. true로 설정하면 Reactor Netty 오류가 보고됩니다.

    <Callout variant="important">
      [Java 에이전트 6.3.0](/docs/release-notes/agent-release-notes/java-release-notes/java-agent-630) 이상에서만 사용할 수 있습니다.

      참고로 Java 에이전트 6.5.0부터 기본값이 false로 변경되었습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-send_data_on_exit"
    title="send_data_on_exit"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    JVM 종료 전에 에이전트가 최신 메트릭 데이터를 뉴렐릭에 보낼 수 있도록 지연된 JVM 종료를 활성화합니다.
  </Collapser>

  <Collapser
    id="send_data_on_exit_threshold"
    title="send_data_on_exit_threshold"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `60`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 [`send_data_on_exit`](#cfg-send_data_on_exit) 설정을 사용할 때까지의 시간(초)입니다.
  </Collapser>

  <Collapser
    id="cfg-send_environment_info"
    title="send_environment_info"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    New Relic에 JVM 설정 보고를 활성화합니다.
  </Collapser>

  <Collapser
    id="cfg-send_jvm_props"
    title="send_jvm_props"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    `true`로 설정하면 JVM 속성이 뉴렐릭으로 전송됩니다.
  </Collapser>

  <Collapser
    id="cfg-ssl"
    title="ssl(더 이상 사용되지 않음)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    SSL을 통해 이동하려면 뉴렐릭 수집기에 연결해야 합니다.

    에이전트는 기본적으로 HTTPS를 통해 뉴렐릭과 통신하며 뉴렐릭은 APM 및 뉴렐릭 REST API에 대한 모든 트래픽에 대해 [HTTPS가 필요](/docs/apis/rest-api-v2/troubleshooting/301-response-rest-api-calls)합니다.

    이 작업은 애플리케이션 코드를 처리하는 threads에 대해 비동기적으로 수행되므로, 응답 시간은 이 변경으로 인해 직접적인 영향을 받지 않습니다.
  </Collapser>

  <dd>
    <Callout variant="important">
      [Java 에이전트 3.48.0](/docs/release-notes/agent-release-notes/java-release-notes/java-agent-3480)부터 SSL은 기본적으로 활성화되어 있으며 비활성화하는 구성 옵션은 더 이상 사용되지 않습니다. [Java 에이전트 4.0.0](/docs/release-notes/agent-release-notes/java-release-notes/java-agent-400)부터 SSL을 비활성화하는 기능이 제거되었습니다.
    </Callout>
  </dd>

  <Collapser
    id="cfg-sync_startup"
    title="sync_startup"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 앱 시작과 동시에, 즉시 뉴렐릭 수집기를 연결할 수 있도록 합니다.
  </Collapser>

  <Collapser
    id="scala-futures-segment"
    title="scala_futures_as_segments"
  >
    <Callout variant="important">
      이는 Java 에이전트 [버전 3.44.0 이상](/docs/release-notes/agent-release-notes/java-release-notes)에 적용됩니다.
    </Callout>

    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    보다 간결한 트랜잭션 추적 세부 정보를 위해 에이전트는 Scala Futures를 트랜잭션 세그먼트로 보고하지 않으며 해당 Futures는 [트랜잭션의 총 시간](/docs/data-analysis/user-interface-functions/view-your-data/response-time#response-time-total-time)에 기여하지 않습니다.

    `scala_futures_as_segments`를 활성화하면 오버헤드가 증가합니다. Scala Futures가 트랜잭션 세그먼트로 보고하여 트랜잭션 트레이스에서 볼 수 있도록 하려면, 활성화하는 것이 가능합니다.

    ```yml
    scala_futures_as_segments:
      enabled: true
    ```
  </Collapser>
</CollapserGroup>

## 환경 변수 [#Environment_Variables]

환경 변수가 가장 높은 우선순위를 가지며 시스템 속성 및 yml 구성 설정을 재정의합니다.

* 환경 변수를 설정하려면 `export VARNAME=value` 명령을 사용합니다.
* 환경 변수를 영구적으로 설정하려면 내보내기 행을 `~/.bashrc` 또는 `~/.bash_profile` 같은 파일에 추가합니다.

환경 변수를 설정하여 시스템 속성 또는 `newrelic.yml`의 모든 설정을 재정의할 수 있습니다. 구성 파일의 지정된 설정에 해당하는 환경 변수는 모든 점( `.` ) 및 대시( `-` )가 밑줄( `_` )로 대체된 `NEW_RELIC` 접두사가 붙은 설정 이름입니다. 예를 들어, `log_level` 설정에 대한 환경 변수는 `NEW_RELIC_LOG_LEVEL`입니다.

스탠자에 중첩된 설정의 경우 스탠자 이름을 설정 이름 앞에 추가합니다. 예를 들어, `transaction_tracer` 스탠자의 `enabled` 설정에 대한 환경 변수는 `NEW_RELIC_TRANSACTION_TRACER_ENABLED`입니다.

<Callout variant="important">
  환경 변수를 통한 에이전트 구성에는 [Java 에이전트 버전 4.10.0 이상](/docs/agents/java-agent/installation/upgrade-java-agent)이 필요합니다.
</Callout>

에이전트 버전 4.10.0 이상의 경우 다음 환경 변수를 사용할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="ev-NEW_RELIC_APP_NAME"
    title="NEW_RELIC_APP_NAME(필수)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 **필수 항목**입니다. 뉴렐릭에 데이터를 보고할 애플리케이션 이름을 포함합니다. 뉴렐릭에 표시할 애플리케이션 이름을 설정합니다.

    `enable_auto_app_naming`이 `false`인 경우 에이전트는 이 애플리케이션에 모든 데이터를 보고합니다. 그렇지 않으면, 에이전트가 이 애플리케이션에 백그라운드 작업(비웹 애플리케이션의 트랜잭션)만 보고합니다.

    둘 이상의 애플리케이션에 데이터를 보고하려면 애플리케이션 이름을 세미콜론(`;`)으로 구분합니다. 예를 들어 **내 애플리케이션** 및 **내 애플리케이션 2** 에 데이터를 보고하려면 다음을 수행합니다.

    ```yml
    app_name: My Application;My Application 2
    ```

    애플리케이션 이름을 지정하는 더 많은 방법은 [Java 애플리케이션 이름 지정](/docs/agents/java-agent/configuration/name-your-java-application)을 참조하세요.
  </Collapser>

  <Collapser
    id="ev-NEW_RELIC_DISTRIBUTED_TRACING_ENABLED"
    title="NEW_RELIC_DISTRIBUTED_TRACING_ENABLED"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    분산 추적을 활성화합니다. 대소문자 구분: `true` 또는 `false` 사용. 보다 자세한 내용은 [분산 추적 섹션](#dt-main)을 참조하십시오.
  </Collapser>

  <Collapser
    id="NEW_RELIC_PROCESS_HOST_DISPLAY_NAME"
    title="NEW_RELIC_PROCESS_HOST_DISPLAY_NAME"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    뉴렐릭 UI에서 "host:port" 레이블을 장식할 [표시 이름](/docs/agents/java-agent/configuration/hostname-logic-java#display-name)을 설정합니다.
  </Collapser>

  <Collapser
    id="ev-NEW_RELIC_LICENSE_KEY"
    title="NEW_RELIC_LICENSE_KEY(필수)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 **필수 항목**입니다. 뉴렐릭 계정 라이선스가 포함되어 있습니다.

    뉴렐릭 계정과 연결된 [라이선스 키](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)를 지정해야 합니다. 이 키는 에이전트의 데이터를 뉴렐릭 서비스의 계정에 연결합니다.
  </Collapser>

  <Collapser
    id="ev-NEW_RELIC_LOG"
    title="NEW_RELIC_LOG"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `newrelic_agent.log`
          </td>
        </tr>
      </tbody>
    </table>

    정규화되지 않은 로그 파일 이름 또는 표준 출력에 로그할 문자열 `STDOUT`입니다.
  </Collapser>
</CollapserGroup>

뉴렐릭 CodeStream을 사용하여 IDE에서 성능을 모니터링하는 경우 [저장소를 서비스](/docs/codestream/how-use-codestream/performance-monitoring#repo-association)와 [연결하고, 빌드 SHA 또는 릴리스 태그를 오류](/docs/codestream/how-use-codestream/performance-monitoring#buildsha)와 연결할 수도 있습니다.

## 시스템 속성 [#System_Properties]

시스템 속성을 설정하여 `newrelic.yml` 파일의 모든 설정을 재정의할 수 있습니다. 구성 파일의 지정된 설정에 해당하는 시스템 속성은 `newrelic.config` 접두사가 붙은 설정 이름입니다. 예를 들어, `log_level` 설정의 시스템 속성은 `newrelic.config.log_level`입니다.

스탠자에 중첩된 설정의 경우, 스탠자 이름을 설정 이름 앞에 추가합니다. 예를 들어, transaction_tracer 스탠자의 `enabled` 설정에 대한 시스템 속성은 `newrelic.config.transaction_tracer.enabled` 입니다.

구성 설정을 재정의하는 것 외에도 에이전트는 다음 시스템 속성을 인식합니다.

<CollapserGroup>
  <Collapser
    id="newrelic-bootstrap_classpath"
    title="newrelic.config.process_host.display_name"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    New Relic UI에서 "host:port" 라벨을 장식할 [표시 이름](/docs/agents/java-agent/configuration/hostname-logic-java#display-name)을 설정합니다. Java 에이전트 3.17 이상이 필요합니다.
  </Collapser>

  <Collapser
    id="newrelic-config-file"
    title="newrelic.config.file"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    newrelic 구성 파일에 대한 완전한 경로를 포함하는 문자열입니다. 비어 있으면 에이전트는 `newrelic.yml`이 `newrelic.jar`와 동일한 디렉터리에 있다고 가정합니다.
  </Collapser>

  <Collapser
    id="newrelic-debug"
    title="newrelic.debug"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    디버그 로깅을 활성화합니다.
  </Collapser>

  <Collapser
    id="newrelic-environment"
    title="newrelic.environment"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 사용할 환경 구성이 포함된 문자열입니다.
  </Collapser>

  <Collapser
    id="newrelic-home"
    title="newrelic.home"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    에이전트의 홈 디렉터리를 포함하는 문자열입니다. 기본값은 에이전트 jarfile과 동일한 디렉터리입니다.
  </Collapser>

  <Collapser
    id="newrelic-logfile"
    title="newrelic.logfile"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `newrelic_agent.log`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트 로그 파일의 이름이 포함된 문자열입니다.
  </Collapser>
</CollapserGroup>

## 속성 [#attributes]

이러한 옵션을 설정하려면 `attributes` 스탠자를 사용합니다. 이를 [재정의](#System_Properties)하려면 `newrelic.config.attributes` 접두사가 붙은 시스템 속성을 사용합니다.

속성은 트랜잭션 추적, 추적된 오류, 브라우저 모니터링 및 트랜잭션 이벤트에 대한 정보를 제공하는 키-값 쌍입니다. 또한 각 대상 아래에 속성 스탠자가 있습니다. 자세한 내용은 [Java 에이전트 속성](/docs/java/java-agent-attributes), 속성 [활성화 및 비활성화](/docs/java/enabling-and-disabling-attributes) 및 [속성 예제](/docs/java/attribute-examples)를 참조하십시오.

<CollapserGroup>
  <Collapser
    id="cfg-attributes-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 모든 속성을 켜거나 끌 수 있습니다.

    <Callout variant="important">
      보안상의 이유로 [Custom Instrumentation Editor](/docs/agents/java-agent/custom-instrumentation/custom-instrumentation-editor-instrument-ui#options)를 사용하여 사용자 정의 속성을 캡처하는 것은 기본적으로 `false`로 설정됩니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-attributes-include"
    title="include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    속성이 활성화되면 이 목록에 있는 속성 키가 뉴렐릭으로 전송됩니다. 목록의 키를 쉼표로 구분하십시오. 예를 들어:

    ```
    key1, key2, key3
    ```

    [에이전트 속성 규칙](/docs/subscriptions/agent-attributes)도 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-attributes-exclude"
    title="exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 모든 속성 키는 뉴렐릭으로 전송되지 않습니다. 목록의 키를 쉼표로 구분합니다. 예:

    ```
    key1, key2, key3
    ```

    [에이전트 속성 규칙](/docs/subscriptions/agent-attributes)도 참조하십시오.
  </Collapser>
</CollapserGroup>

## 비동기 계측 [#async_config]

이러한 옵션은 `common` 스탠자에서 직접 설정되며 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="cfg-token_timeout"
    title="token_timeout"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            Default (초)
          </th>

          <td>
            `180`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 `token.expire()`로 명시적으로 만료되지 않은 비동기 토큰을 자동으로 만료하는 시간(초)입니다. 사용 지침은 [토큰: 비동기 스레드 연결](/docs/agents/java-agent/java-agent-api/java-agent-api-asynchronous-applications#tokens)을 참조하십시오.

    <Callout variant="important">
      이 값을 늘리면 에이전트가 사용하는 메모리 양이 증가하고, 만료되지 않은 토큰으로 인해 트랜잭션이 보고되지 않기 때문에 성능에 영향을 미칠 수 있습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-segment_timeout"
    title="segment_timeout"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            Default (초)
          </th>

          <td>
            `600`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 `segment.end()` 또는 `segment.ignore()`로 명시적으로 종료되지 않은 세그먼트를 자동으로 종료하는 시간(초)입니다. 사용 지침은 [세그먼트: 시간 임의 비동기 활동](/docs/agents/java-agent/java-agent-api/java-agent-api-asynchronous-applications#segments)을 참조하십시오.

    <Callout variant="important">
      이 값을 늘리면, 에이전트가 사용하는 메모리 양이 증가하고 끝나지 않은 세그먼트로 인해 트랜잭션이 보고되지 않기 때문에 성능에 영향을 줄 수 있습니다.
    </Callout>
  </Collapser>
</CollapserGroup>

## 브라우저 모니터링 [#Browser_Monitoring]

이러한 옵션은 `browser_monitoring` 스탠자에 설정되며 `newrelic.config.browser_monitoring` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

브라우저 모니터링은 실제 사용자가 웹사이트에서 경험하고 있는 성능에 대한 통찰력을 제공합니다. 이는 각 페이지의 머리글과 바닥글에 소량의 JavaScript 코드를 삽입하여 사용자의 브라우저가 웹 페이지를 다운로드하고 렌더링하는 데 걸리는 시간을 측정하여 수행됩니다.

<CollapserGroup>
  <Collapser
    id="bm-auto_instrument"
    title="auto_instrument"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    기본적으로 에이전트는 모니터링 JavaScript를 웹 페이지에 삽입하기 위해 컴파일된 JSP에 API 호출을 자동으로 삽입합니다. 이 동작을 끄려면 이 속성을 `false`로 설정합니다.
  </Collapser>

  <Collapser
    id="bm-disabled_auto_pages"
    title="disabled_auto_pages"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            쉼표로 구분된 문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    [`auto_instrument`](#bm-auto_instrument)가 `true`이면 기본적으로 모든 페이지가 계측됩니다. 자동 계측에서 건너뛸 모든 페이지를 여기에 나열합니다. 이 페이지에서 수동 계측을 계속 사용할 수 있습니다.

    예:

    ```yml
    browser_monitoring:
      disabled_auto_pages: /WEB-INF/jsp/testpage_1.jsp, /WEB-INF/jsp/testpage_2.jsp
    ```
  </Collapser>

  <Collapser
    id="cfg-browser-attributes-enabled"
    title="attributes.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 브라우저 모니터링에 대한 모든 속성을 켜거나 끌 수 있습니다. [조회할 수 있는 데이터](/docs/query-your-data/explore-query-data/explore-data/introduction-querying-new-relic-data)입니다. 루트 수준에서 `attributes.enabled`가 false이면 `browser_monitoring`에서 이 속성이 설정되는 방식에 관계없이 브라우저 모니터링에서 속성이 전송되지 않습니다.
  </Collapser>

  <Collapser
    id="cfg-bm-attributes-include"
    title="attributes.include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    속성이 `browser_monitoring`에 대해 활성화된 경우, 이 목록에 있는 모든 속성 키는 페이지 보기에서 뉴렐릭으로 전송됩니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-bm-attributes-exclude"
    title="attributes.exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 모든 속성 키는 페이지 보기에서 뉴렐릭으로 전송되지 않습니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>
</CollapserGroup>

## Circuit breaker

이러한 설정은 [Java 서킷 브레이커](/docs/agents/java-agent/custom-instrumentation/circuit-breaker-java-custom-instrumentation)의 동작을 사용자 정의합니다. 이러한 설정은 기본적으로 `newrelic.yml`에 포함되지 않습니다. JVM을 변경한 후 JVM을 다시 시작할 필요가 없습니다.

서킷 브레이커를 맞춤화하려면 `common` 스탠자 아래에 스탠자를 추가합니다.

```yml
common: &default_settings​
  <var>OTHER_CONFIG_SETTINGS</var>
  circuitbreaker:
    enabled: <var>true</var>
    memory_threshold: <var>20</var>
    gc_cpu_threshold: <var>10</var>
```

<CollapserGroup>
  <Collapser
    id="cfg-circuitbreaker_enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    애플리케이션이 예상대로 작동하는 경우 서킷 브레이커를 비활성화할 수 있습니다.
  </Collapser>

  <Collapser
    id="cfg-circuitbreaker_memory_threshold"
    title="memory_threshold"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수(0-100)
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `20`
          </td>
        </tr>
      </tbody>
    </table>

    서킷 브레이커가 트립되어야 하는 여유 힙 메모리 비율을 맞춤화합니다. 사용 가능한 힙 메모리의 백분율이 `memory_threshold` 미만이고 가비지 수집에 소요된 CPU 시간이 `gc_cpu_threshold` 보다 크면 서킷 브레이커가 작동합니다. 서킷 브레이커가 트립될 가능성을 줄이려면 `memory_threshold`를 줄이거나 `gc_cpu_threshold`를 늘립니다. 애플리케이션의 작동 성능 및 동작에 따라 필요에 따라 이러한 값을 조정합니다.
  </Collapser>

  <Collapser
    id="cfg-circuitbreaker_gc_cpu_threshold"
    title="gc_cpu_threshold"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수(0-100)
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `10`
          </td>
        </tr>
      </tbody>
    </table>

    서킷 브레이커가 트립되어야 하는 가비지 수집 CPU 시간의 백분율을 사용자 지정합니다. 사용 가능한 힙 메모리의 백분율이 `memory_threshold` 미만이고 가비지 수집에 소요된 CPU 시간이 `gc_cpu_threshold`보다 크면 서킷 브레이커가 작동합니다. 서킷 브레이커가 트립될 가능성을 줄이려면 `memory_threshold`를 줄이거나 `gc_cpu_threshold`를 늘립니다. 애플리케이션의 작동 성능 및 동작에 따라 필요에 따라 이러한 값을 조정합니다.
  </Collapser>
</CollapserGroup>

## 클라우드 플랫폼 활용 [#utilization]

이러한 옵션은 `utilization` 스탠자에 설정되며 `newrelic.config.utilization` 접두사가 붙은 시스템 속성을 사용하여 재정의할 수 있습니다.

에이전트는 활용 정보를 수집하여 뉴렐릭 서비스로 전송합니다. 에이전트는 Amazon Web Services(AWS), Azure, Google Cloud Platform 및 Pivotal Cloud Foundry 인스턴스에서 정보를 수집할 수 있습니다. 또한 도커 컨테이너와 쿠버네티스 서비스와 관련된 정보도 수집합니다.

<CollapserGroup>
  <Collapser
    id="aws-enabled"
    title="detect_aws"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 AWS 메타데이터 API를 폴링할지 여부를 결정합니다.
  </Collapser>

  <Collapser
    id="azure-enabled"
    title="detect_azure"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 Azure 메타데이터 API를 폴링하는지 여부를 결정합니다.
  </Collapser>

  <Collapser
    id="gcp-enabled"
    title="detect_gcp"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 Google Cloud Platform 메타데이터 API를 폴링하는지 여부를 결정합니다.
  </Collapser>

  <Collapser
    id="kubernetes-enabled"
    title="detect_kubernetes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 `KUBERNETES_SERVICE_HOST` 환경 변수를 보고하는지 여부를 결정합니다.
  </Collapser>

  <Collapser
    id="pcf-enabled"
    title="detect_pcf"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 Pivotal Cloud Foundry 환경 변수를 읽는지 여부를 결정합니다.
  </Collapser>

  <Collapser
    id="docker-enabled"
    title="detect_docker"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 파일 시스템에서 Docker 정보를 읽을지 여부를 결정합니다.
  </Collapser>
</CollapserGroup>

## 코드 레벨 메트릭

코드 레벨 메트릭은 코드가 메서드 수준에서 수행되는 방식에 대한 세부적인 인사이트를 제공합니다. 자동으로 계측되거나 @Trace 주석으로 계측된 각 메서드에 대한 메트릭이 표시됩니다. 코드 레벨 메트릭에 대한 보다 자세한 내용은 [CodeStream을 사용한 성능 모니터링](/docs/codestream/how-use-codestream/performance-monitoring)을 참조하십시오.

코드 레벨 메트릭 옵션은 `code_level_metrics` 스탠자에 설정되며 `newrelic.config.code_level_metrics` 접두사가 있는 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="clm-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [코드 수준 지표는](/docs/codestream/how-use-codestream/performance-monitoring/#code-level) 에이전트 버전 7.10.0에서 기본적으로 비활성화되어 있습니다. 활성화하려면 true로 설정하십시오. 에이전트 버전 7.11.0부터 기본값은 위에서 언급한 대로 `true`입니다.
  </Collapser>
</CollapserGroup>

## 애플리케이션 간 추적기 [#Cross_Application_Tracer]

<Callout variant="important">
  애플리케이션 간 추적은 에이전트 버전 7.4.0부터 더 이상 사용되지 않으며 향후 에이전트 버전에서 삭제될 예정입니다.

  애플리케이션 간 추적을 사용하는 대신 [분산 추적](#distributed-tracing) 기능을 사용하시길 권합니다. 분산 추적은 애플리케이션 간 추적 기능을 개선한 것으로 대규모 분산 시스템에 권장됩니다.
</Callout>

교차 애플리케이션 추적 옵션은 `cross_application_tracer` 스탠자에 설정되며 `newrelic.config.cross_application_tracer` 접두사가 있는 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

[애플리케이션 간 추적(Cross application tracing)](/docs/apm/transactions/cross-application-traces/cross-application-tracing)은 Apache HttpClient 라이브러리를 사용해 외부 호출에 요청 및 응답 헤더를 추가합니다. 이는 다른 뉴렐릭 에이전트가 모니터링하는 애플리케이션을 호출할 때 더 나은 성능 데이터를 제공합니다.

<CollapserGroup>
  <Collapser
    id="cat-enabled"
    title="enabled (더 이상 사용되지 않음)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [애플리케이션 간 추적](/docs/apm/transactions/cross-application-traces/cross-application-tracing)은 기본적으로 활성화되어 있습니다. 이것을 끄려면 false로 설정합니다.
  </Collapser>
</CollapserGroup>

## 커스텀 이벤트 [#Custom_Events]

커스텀 이벤트는 `custom_insights_events` 스탠자에서 설정되며 `newrelic.config.custom_insights_events` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

APM을 사용하면 뉴렐릭 언어 에이전트 API를 통해 [커스텀 이벤트 데이터를 기록](/docs/insights/insights-data-sources/custom-data/insert-custom-events-new-relic-apm-agents)[할 수 있습니다](/docs/query-your-data/explore-query-data/explore-data/introduction-querying-new-relic-data).

<Callout variant="important">
  4.1.0 이전 Java 에이전트 버전의 경우 다음 YAML 구성이 인식됩니다.

  ```yml
  custom_insights_events.enabled: true
    custom_insights_events.max_samples_stored: 5000
  ```

  에이전트 버전 4.1.0 이상의 경우 YAML 구성은 중첩 스탠자 형식을 사용합니다.

  ```yml
  custom_insights_events:
    enabled: false
    max_samples_stored: 5000
  ```
</Callout>

<CollapserGroup>
  <Collapser
    id="cie-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이렇게 하면 사용자 지정 이벤트 서비스가 활성화됩니다.
  </Collapser>

  <Collapser
    id="cie-max_samples_stored"
    title="max_samples_stored"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `30000`
          </td>
        </tr>
      </tbody>
    </table>

    60초마다 보고되는 샘플링된 사용자 지정 이벤트의 최대 수입니다.
  </Collapser>
</CollapserGroup>

## 맞춤형 계측 [#Custom_Instrumentation]

이러한 옵션은 `class_transformer` 스탠자에 설정되며 `newrelic.config.class_transformer` 접두사가 있는 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="ci-trace_annotation_class_name"
    title="trace_annotation_class_name"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 계측할 사용자 지정 메서드를 결정하는 데 사용하는 주석 클래스의 전체 클래스 이름을 포함하는 문자열입니다. 사용자 정의 주석에 대한 자세한 정보는 [Java 커스텀 메트릭 콜렉션](/docs/java/java-custom-metric-collection)을 참조하십시오.
  </Collapser>

  <Collapser
    id="servlet-user"
    title="com.newrelic.instrumentation.servlet-user"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    `userPrincipal` 이름을 캡처하려면 이 옵션을 활성화합니다. 이 이름은 트랜잭션 추적 속성으로 포함되며 [쿼리할 수 있습니다](/docs/query-your-data/explore-query-data/explore-data/introduction-querying-new-relic-data).
  </Collapser>
</CollapserGroup>

## 분산 추적 [#dt-main]

<Callout variant="important">
  분산 추적을 활성화하면 [교차 애플리케이션 추적](#Cross_Application_Tracer)이 비활성화되고 APM 기능에 다른 영향을 미칩니다. 활성화하기 전에 [전환 가이드](/docs/transition-guide-distributed-tracing)를 확인하시기 바랍니다.

  [Java 에이전트 버전 4.3.0 이상](/docs/agents/java-agent/installation/upgrade-java-agent)이 필요합니다.
</Callout>

[분산 추적](/docs/understand-dependencies/distributed-tracing/get-started/introduction-distributed-tracing)은 분산 시스템을 통해 이동하는 요청의 경로를 볼 수 있도록 해줍니다. 이 기능은 Java 에이전트 버전 7.4.0 이상에서 기본적으로 활성화되어 있습니다.

구성 파일의 `distributed_tracing` 스탠자에서 이를 수동으로 재정의할 수 있습니다. 접두사 시스템 속성(`newrelic.config.distributed_tracing`) 또는 환경 변수(`NEW_RELIC_DISTRIBUTED_TRACING_ENABLED`)를 사용하여 이를 재정의할 수도 있습니다. 아래의 예를 참조하십시오.

분산 추적 설정에 대한 보다 자세한 정보는 [Java 애플리케이션에 대한 분산 추적 사용](/docs/apm/agents/go-agent/instrumentation/distributed-tracing-go-agent)을 참조하십시오.

<CollapserGroup>
  <Collapser
    id="dt-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    분산 추적을 비활성화하려면 이 옵션을 `false`로 설정합니다. 예를 들어 구성 파일에서 다음을 사용할 수 있습니다.

    ```yml
    distributed_tracing:
      enabled: false
    ```

    분산 추적을 비활성화하는 다른 옵션은 다음과 같습니다.

    [시스템 속성](/docs/agents/java-agent/configuration/java-agent-configuration-config-file#System_Properties):

    ```ini
    -Dnewrelic.config.distributed_tracing.enabled=false
    ```

    환경 변수:

    ```ini
    NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=false
    ```
  </Collapser>

  <Collapser
    id="dt-exclude_newrelic_header"
    title="exclude_newrelic_header"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    기본적으로 지원되는 에이전트 버전은 분산 추적을 위해 `newrelic` 헤더와 W3C 추적 컨텍스트 헤더를 모두 사용합니다. `newrelic` 분산 추적 헤더는 W3C 추적 컨텍스트 헤더를 지원하지 않는 이전 에이전트와의 상호 운용성을 허용합니다. W3C 추적 컨텍스트 헤더를 지원하는 에이전트 버전은 분산 추적을 위해 `newrelic` 헤더보다 우선합니다.

    `newrelic` 헤더를 사용하지 않으려는 경우, 이를 `true` 로 설정하면 에이전트가 `newrelic` 헤더를 제외하고 분산 추적에 W3C Trace Context 헤더만 사용하게 됩니다.

    예를 들어 구성 파일에서 `newrelic` 헤더를 제외하려면 다음을 사용합니다.

    ```yml
    distributed_tracing:
      exclude_newrelic_header: true
    ```

    [시스템 속성](/docs/agents/java-agent/configuration/java-agent-configuration-config-file#System_Properties)을 사용하여 `newrelic` 헤더를 제외하려면 다음을 사용합니다.

    ```ini
    -Dnewrelic.config.distributed_tracing.exclude_newrelic_header=true
    ```
  </Collapser>
</CollapserGroup>

## 오류 수집기 [#Error_Collector]

이러한 옵션은 `error_collector` 스탠자에 설정되며 달리 명시되지 않는 한 `newrelic.config.error_collector` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다. 오류 수집기는 포착되지 않은 예외에 대한 정보를 캡처하고 볼 수 있도록 이를 뉴렐릭으로 보냅니다.

<Callout variant="tip">
  UI를 통해 오류를 구성하는 방법을 포함하여 Java 에이전트에 대한 오류를 구성하는 방법은 [Java 에이전트 오류 구성](/docs/agents/java-agent/configuration/java-agent-error-configuration)을 참조하십시오.
</Callout>

<CollapserGroup>
  <Collapser
    id="ec-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    오류 수집을 활성화합니다.
  </Collapser>

  <Collapser
    id="ec-ignore_classes"
    title="ignore_classes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정규화된 `class_name` 문자열 목록이 포함된 스탠자
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    지정된 예외 클래스 이름은 무시되며 오류율이나 Apdex 점수에 영향을 미치지 않거나 APM에 보고됩니다. **시스템 속성으로 지정할 수 없습니다.**

    이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.

    예:

    ```yml
    error_collector:
      ignore_classes:
        - "com.example.MyException"
        - "com.example.DifferentException"
    ```

    환경 변수를 사용하여 무시하려는 예외 클래스 이름을 나열할 수 있습니다.

    ```ini
    NEW_RELIC_ERROR_COLLECTOR_IGNORE_CLASSES="[\"com.example.MyException\", \"com.example.DifferentException\"]"
    ```
  </Collapser>

  <Collapser
    id="ec-ignore_messages"
    title="ignore_messages"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            오류 클래스당 정규화된 `class_name` 및 `messages` 목록을 포함하는 스탠자
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 무시해야 하는 예외 클래스 이름을 지정합니다. 무시된 메시지는 오류율이나 Apdex 점수에 영향을 미치지 않거나 APM에 보고되지 않습니다. 다음으로 구성된 `yaml` 쌍을 포함합니다.

    * APM에 보고되지 않아야 하는 완전한 예외 클래스 이름

      AND

    * 비교할 예외 `message` 목록(최소 하나는 필수)

      예외 클래스 이름이 오류와 일치하지만 메시지가 일치하지 않는 경우 해당 오류는 **무시되지 않습니다**. 메시지 문자열은 일치에 `contains`를 사용합니다. 메시지는 자체적으로 제공될 수 없으며 항상 정규화된 클래스 이름과 쌍을 이루어야 합니다. **시스템 속성으로 지정할 수 없습니다.**

      이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.

      예:

      ```yml
      error_collector:
        ignore_messages:
          com.example.MyException:
            - "Some error message to ignore"
            - "Some other error message to ignore"
          com.example.DifferentException:
            - "Some different error message to ignore"
      ```

      환경 변수를 사용하여 무시하려는 예외 클래스 이름과 메시지를 나열할 수 있습니다.

      ```ini
      NEW_RELIC_ERROR_COLLECTOR_IGNORE_MESSAGES="{\"com.example.MyException\": [\"Some error message to ignore\", \"Some other error message to ignore\"], \"com.example.DifferentException\": [\"Some different error message to ignore\"]}"
      ```
  </Collapser>

  <Collapser
    id="ec-ignore_status_codes"
    title="ignore_status_codes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            쉼표로 구분된 문자열 및 범위 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `404`
          </td>
        </tr>
      </tbody>
    </table>

    오류로 처리되어서는 안 되는 HTTP 상태 코드의 개별 및 파선 범위로 구성된 쉼표로 구분된 목록입니다.

    이 속성이 `newrelic.yml` 구성 파일에서 주석 처리되면 404 상태 코드가 자동으로 무시됩니다. [서버 측 구성](/docs/agents/manage-apm-agents/configuration/server-side-agent-configuration)을 사용할 때 무시하려면 상태 코드 404를 지정해야 합니다.

    이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.

    예:

    ```yml
    error_collector:
      ignore_status_codes: 404,507-511
    ```
  </Collapser>

  <Collapser
    id="ec-expected_classes"
    title="expected_classes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정규화된 `class_name` 문자열 목록이 포함된 스탠자
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    APM에 오류를 보고하는 동안 지정된 예외 클래스가 오류율 또는 Apdex 점수에 영향을 미치지 않도록 합니다. **시스템 속성으로 지정될 수 없습니다.**

    이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.

    예:

    ```yml
    error_collector:
      expected_classes:
        - "com.example.MyException"
        - "com.example.DifferentException"
    ```

    환경 변수를 사용하여 예상되는 예외 클래스 이름을 나열할 수 있습니다.

    ```ini
    NEW_RELIC_ERROR_COLLECTOR_EXPECTED_CLASSES="[\"com.example.MyException\", \"com.example.DifferentException\"]"
    ```
  </Collapser>

  <Collapser
    id="ec-expected_messages"
    title="expected_messages"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            오류 클래스당 정규화된 `class_name` 및 `messages` 목록을 포함하는 스탠자
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    예상대로 표시되어야 하는 정규화된 예외 클래스 이름으로 구성되어 오류율이나 Apdex 점수에 영향을 미치지 않도록 하는 yaml 쌍과 일치시킬 예외 `message` 목록(후자 중 하나 이상은 필수)을 포함합니다. 예외 클래스 이름이 오류와 일치하지만 메시지가 일치하지 않는 경우 해당 오류는 예상대로 표시**되지 않으므로** 오류율 및 Apdex 점수에 영향을 미칩니다.

    메시지 문자열은 일치에 `contains`를 사용합니다. 메시지는 자체적으로 제공될 수 없으며 항상 정규화된 클래스 이름과 쌍을 이루어야 합니다. **시스템 속성으로 지정할 수 없습니다.**

    이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.

    예:

    ```yml
    error_collector:
      expected_messages:
        com.example.MyException:
          - "Some expected error message"
          - "Some other expected error message"
        com.example.DifferentException:
          - "Some different expected error message"
    ```

    환경 변수를 사용하여 예상되는 예외 클래스 이름 및 메시지를 나열할 수 있습니다.

    ```ini
    NEW_RELIC_ERROR_COLLECTOR_EXPECTED_MESSAGES="{\"com.example.MyException\": [\"Some error message to ignore\", \"Some other error message to ignore\"], \"com.example.DifferentException\": [\"Some different error message to ignore\"]}"
    ```
  </Collapser>

  <Collapser
    id="ec-expected_status_codes"
    title="expected_status_codes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            쉼표로 구분된 문자열 및 범위 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    예상대로 표시되어 오류율 또는 Apdex 점수에 영향을 미치지 않도록 방지할 HTTP 상태 코드의 개별 및 파선 범위로 구성된 쉼표로 구분된 목록입니다.

    이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.

    예:

    ```yml
    error_collector:
      expected_status_codes: 415,500-506
    ```

    예상되는 상태 코드는 `NEW_RELIC_ERROR_COLLECTOR_EXPECTED_STATUS_CODES` 환경 변수와 함께 사용할 수도 있습니다.
  </Collapser>

  <Collapser
    id="cfg-error-attributes-enabled"
    title="attributes.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 추적된 오류에 대한 모든 속성을 켜거나 끌 수 있습니다. 루트 수준에서 `attributes.enabled`가 `false`이면 이 속성이 `error_collector`에서 설정되는 방식에 관계없이 추적된 오류에 속성이 전송되지 않습니다.
  </Collapser>

  <Collapser
    id="cfg-ec-attributes-include"
    title="attributes.include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    추적된 오류에 대해 속성이 활성화된 경우 이 목록에 있는 모든 속성 키는 추적된 오류의 뉴렐릭으로 전송됩니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-ec-attributes-exclude"
    title="attributes.exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 속성 키는 추적된 오류에서 뉴렐릭으로 전송되지 않습니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="ec-ignoreErrorPriority"
    title="무시 오류 우선순위"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션에서 여러 오류가 발견되면 기본적으로 마지막 오류만 보고됩니다. 대신 이 속성을 `false`로 설정하면 발견된 첫 번째 오류만 보고합니다. 자세한 정보는 [noticeError API](/docs/agents/java-agent/configuration/java-agent-error-configuration/)를 참조하십시오. 예를 들어:

    ```yml
    error_collector:
      ignoreErrorPriority: false
    ```
  </Collapser>

  <Collapser
    id="ec-ignore_errors"
    title="ignore_errors(더 이상 사용되지 않음)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            쉼표로 구분된 문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    지정된 모든 예외 클래스 이름은 오류로 처리되지 않습니다. Java 에이전트 3.40.0부터 더 이상 사용되지 않으며 [`ignore_classes`](#ec-ignore_classes)로 대체되었습니다.

    예:

    ```yml
    error_collector:
      ignore_errors: some.other.MyException
    ```
  </Collapser>
</CollapserGroup>

## 외부 추적기 [#External_Tracer]

외부 추적 옵션은 `external_tracer` 스탠자에 설정되며 `newrelic.config.external_tracer` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="cat-enabled"
    title="exclude_request_uri"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 오류 및 트랜잭션 트레이스에 대한 나가는 요청 URI 컬렉션을 제어할 수 있습니다. 이 정보를 수집하지 않으려면 true로 설정합니다.
  </Collapser>
</CollapserGroup>

## 호스트 이름 구성 [#hostname_configuration]

이러한 옵션은 `process_host` 스탠자에 설정되며 `newrelic.config.process_host` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

다음 속성은 UI에 표시되는 호스트 이름을 구성하는 데 사용됩니다.

<CollapserGroup>
  <Collapser
    id="display_name"
    title="display_name"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    뉴렐릭 UI에서 "host:port" 레이블을 장식할 [표시 이름](/docs/agents/java-agent/configuration/hostname-logic-java#display-name)을 설정합니다.
  </Collapser>

  <Collapser
    id="ipv_preference"
    title="ipv_preference"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `4`
          </td>
        </tr>
      </tbody>
    </table>

    호스트 이름을 결정할 수 없으면 호스트의 IP 주소가 사용됩니다. 이 속성은 IPv4 또는 IPv6 주소를 사용해야 하는지 여부를 결정합니다. 기본값은 IPv4입니다.
  </Collapser>
</CollapserGroup>

## Infinite Tracing

<Callout variant="important">
  요구 사항:

  * [Java 에이전트 5.12.1 이상](/docs/agents/java-agent/installation/update-java-agent)
  * `enable_auto_app_naming` 이 활성화된 경우 Infinite Tracing이 작동하지 않습니다.
</Callout>

Infinite Tracing(무한 추적)을 켜려면, 분산 추적을 활성화하고 아래에 추가 설정을 추가합니다. 예시는 [언어 에이전트: 분산 추적 구성](/docs/understand-dependencies/distributed-tracing/enable-configure/language-agents-enable-distributed-tracing#java-config)을 참조하십시오.

<CollapserGroup>
  <Collapser
    id="infinite-tracing-trace-observer-host"
    title="trace_observer.host"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    유효한 Infinite Tracing 트레이스 옵저버 호스트 항목을 가져오는 데 도움이 필요하면 [트레이스 옵저버 찾기 또는 생성하기](/docs/understand-dependencies/distributed-tracing/enable-configure/language-agents-enable-distributed-tracing#provision-trace-observer)를 참조하십시오.

    YAML을 통해 이를 구성할 수 있습니다.

    ```yml
    infinite_tracing:
      trace_observer:
        host: <var>YOUR_TRACE_OBSERVER_HOST</var>
    ```

    시스템 속성 `newrelic.config.infinite_tracing.trace_observer.host` 또는 환경 변수 `NEW_RELIC_INFINITE_TRACING_TRACE_OBSERVER_HOST`를 사용할 수도 있습니다.
  </Collapser>
</CollapserGroup>

## Jar 수집기

Java 에이전트는 애플리케이션 클래스 경로에서 jar 및 해당 버전에 대한 정보를 수집합니다.

Jar 컬렉션 구성은 `jar_collector` 스탠자에 설정되며 `newrelic.config.jar_collector` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다. 옵션에는 다음이 포함됩니다.

<CollapserGroup>
  <Collapser
    id="cfg-jar-collector-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    jar 수집 및 보고를 활성화/비활성화하는 데 사용됩니다.
  </Collapser>

  <Collapser
    id="cfg-jar-collector-skip-temp-jars"
    title="skip_temp_jars"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    임시 jar 수집을 활성화/비활성화하는 데 사용됩니다. 임시 jar는 시스템 속성 `java.io.tmpdir`에 의해 지정된 디렉터리에 있습니다.
  </Collapser>

  <Collapser
    id="cfg-jar-collector-jars-per-second"
    title="jars_per_second"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `10`
          </td>
        </tr>
      </tbody>
    </table>

    초당 처리할 최대 jar 수입니다. 양수여야 합니다.
  </Collapser>
</CollapserGroup>

## JFR(실시간 프로파일링)

Java 에이전트는 JFR(Java Flight Recorder)을 사용하여 [실시간 프로파일링](/docs/agents/java-agent/features/real-time-profiling-java-using-jfr-metrics/)을 위해 충실도가 높은 JVM 데이터를 수집합니다.

에이전트 yaml의 `jfr` 스탠자에서 실시간 프로파일링을 구성할 수 있으며, [시스템 속성](#System_Properties)은 접두사 `newrelic.config.jfr.` 또는 [환경 변수](#Environment_Variables)는 접두사 `NEW_RELIC_JFR_`입니다.

<CollapserGroup>
  <Collapser
    id="jfr-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    [JFR을 사용한 실시간 프로파일링](/docs/agents/java-agent/features/real-time-profiling-java-using-jfr-metrics/)을 활성화하려면 `true`로 설정합니다.

    <Callout variant="important">
      JFR을 사용한 실시간 프로파일링은 Java 에이전트 [버전 7.1.0](/docs/release-notes/agent-release-notes/java-release-notes)에서 기본적으로 켜져 있습니다. 다른 에이전트 버전 7.0.0 이상을 사용 중인 경우 값을 `true`로 변경하여 JFR을 켤 수 있습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="jfr-audit_logging"
    title="audit_logging"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    각 수확 배치에서 모든 JFR 측정항목 및 이벤트를 표시하는 감사 로깅을 활성화하려면 `true`로 설정합니다. 감사 로깅은 매우 장황하며 문제 해결 목적으로만 사용해야 합니다.

    <Callout variant="important">
      이는 Java 에이전트 [버전 7.0.0 이상](/docs/release-notes/agent-release-notes/java-release-notes)에 적용됩니다.
    </Callout>
  </Collapser>
</CollapserGroup>

## JMX

이러한 옵션을 설정하려면 `jmx` 스탠자를 사용합니다. 이를 [재정의](#System_Properties)하려면 `newrelic.config.jmx` 접두사가 붙은 시스템 속성을 사용합니다.

Java 에이전트는 JMX를 사용하여 JVM 데이터를 수집합니다. 또한 에이전트는 다른 추적 시스템에서 사용할 수 있는 JMX를 통해 연결 메타데이터를 노출할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="jmx-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 모든 JMX 기능을 켜거나 끌 수 있습니다.
  </Collapser>

  <Collapser
    id="jmx-linkingMetadataMBean"
    title="linkingMetadataMBean"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 활성화하면 Java 에이전트가 원격 JMX를 통해 연결 메타데이터를 노출할 수 있습니다.

    <Callout variant="important">
      이는 Java 에이전트 [버전 6.1.0 이상](/docs/release-notes/agent-release-notes/java-release-notes)에 적용됩니다.
    </Callout>
  </Collapser>
</CollapserGroup>

## 문맥적 로그(Logs in context) [#logs-in-context]

Java 에이전트 버전 `7.6.0`부터 [문맥적 로그](/docs/logs/logs-context/java-configure-logs-context-all) 지원이 에이전트에 추가되어 지원되는 로깅 프레임워크에 쉽게 사용할 수 있습니다. 문맥적 로그에 Java 에이전트를 사용하는 방법에 대한 팁은 [문맥적 Java 로그](/docs/logs/logs-context/java-configure-logs-context-all)를 참조하십시오.

로컬 에이전트 구성 파일에서의 설정 변경은 동적으로 이루어지며, 변경 사항을 적용하기 위해 에이전트를 다시 시작할 필요가 없습니다. 구성 예시:

```yml
application_logging:
  enabled: true
  forwarding:
    enabled: true
    max_samples_stored: 10000
    context_data:
      enabled: false
      include:
      exclude:
  metrics:
    enabled: true
  local_decorating:
      enabled: false
```

문맥적 로그 설정은 `application_logging` 스탠자에 설정되며 `newrelic.config.application_logging` 접두사가 있는 시스템 속성을 사용하여 [재정의](/docs/apm/agents/java-agent/configuration/java-agent-configuration-config-file#System_Properties)할 수 있습니다. 사용 가능한 유일한 옵션은 다음과 같습니다.

<CollapserGroup>
  <Collapser
    id="cfg-application_logging-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    핵심 문맥적 로그 기능을 활성화하려면 `true`로 설정합니다. 활성화되면 로그가 데코레이팅되거나 뉴렐릭으로 전송되는지 여부에 관계없이 추가 로깅 프레임워크 계측이 활성화됩니다.

    <Callout variant="important">
      에이전트 릴리스 7.7.0 이상에는 기본적으로 에이전트 구성 파일에서 이 기능이 활성화되어 있습니다.
    </Callout>

    로그 메트릭 수집을 포함하여 이 기능을 완전히 비활성화하려면 `false`로 설정합니다.
  </Collapser>
</CollapserGroup>

### 로그 전달 [#log-forwarding]

지원되는 로깅 프레임워크를 사용 중이고 에이전트를 사용하여 애플리케이션 로그를 뉴렐릭으로 보내려는 경우, `newrelic.config.application_logging.forwarding` 접두사가 붙은 시스템 속성으로 재정의할 수 있는 `forwarding` 스탠자 아래의 설정을 통해 이를 제어할 수 있습니다. 사용 가능한 옵션은 다음과 같습니다.

<CollapserGroup>
  <Collapser
    id="cfg-application_logging_forwarding-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    뉴렐릭에 애플리케이션 로그 전송을 활성화합니다.

    <Callout variant="important">
      에이전트 릴리스 7.7.0 이상에는 기본적으로 에이전트 구성 파일에서 이 기능이 활성화되어 있습니다.

      로그 전달 기능을 사용하면 데이터 인제스트가 증가하여 요금에 영향을 미칠 수 있습니다. 자세한 내용은 [데이터 인제스트 추적](/docs/apm/new-relic-apm/getting-started/get-started-logs-context#ingest)에 대한 문서를 참조하십시오.

      기존 로그 전달 솔루션이 있고 자동 문맥적 로그를 사용하도록 에이전트를 업데이트하는 경우, **이전 로그 전달자를 비활성화해야 합니다**. 그렇지 않으면 앱에서 이중으로 로그 라인을 전송합니다. 계정에 따라 이중 청구가 발생할 수 있습니다. 자세한 내용은 절차에 따라 [특정 로그 전달자](/docs/logs/forward-logs/enable-log-management-new-relic#log-forwarding)를 비활성화하십시오.
    </Callout>

    데코레이팅된 로그를 뉴렐릭으로 보내려면 이를 `true`로 설정합니다.

    데코레이팅된 로그를 뉴렐릭으로 전송하지 않으려면 이를 `false`로 설정합니다.
  </Collapser>

  <Collapser
    id="cfg-application_logging_forwarding-max_samples_stored"
    title="max_samples_stored"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `10000`
          </td>
        </tr>
      </tbody>
    </table>

    전송할 분당 최대 로그 줄 수를 지정하는 양의 정수입니다.

    로그 전송을 효과적으로 비활성화하려면 이 값을 `0`으로 설정합니다.

    <Callout variant="important">
      이는 _분당_ 전송된 로그 수를 제어합니다.`max_samples_stored`를 `0` (또는 `12` 보다 작은 값)으로 설정해도 기능 자체가 비활성화되지는 _않지만_ 뉴렐릭에 로그 줄이 전송되지 않습니다.
    </Callout>

    전송되는 로그 라인의 양을 줄이려면 이 값을 더 낮게 설정합니다. (로그 샘플링이 발생할 수 있음) 더 많은 로그 라인을 보내려면 이 값을 더 높게 설정합니다.

    각 로그는 연결된 트랜잭션과 동일한 우선순위를 부여받습니다. 트랜잭션 외부에서 발생하는 로그는 랜덤 우선순위를 부여받습니다. 일부 로그는 `max_samples_stored`에 의해 제한되기 때문에 포함되지 않을 수 있습니다. 예를 들어, 로깅 `max_samples_stored`가 10,000으로 설정되고 트랜잭션 1에 10,000개의 로그 항목이 있는 경우 트랜잭션 1에 대한 로그 항목만 기록됩니다. 트랜잭션 1에 10,000개 미만의 로그가 있는 경우 트랜잭션 1에 대한 모든 로그를 수신합니다. 그래도 공간이 남아 있으면 트랜잭션 2의 모든 로그를 받습니다.

    샘플링된 트랜잭션에 대한 모든 로그가 기록된 후 `max_samples_stored`의 한도에 도달하지 않은 경우 샘플링에 없는 트랜잭션에 대한 로그 메시지가 전송됩니다. 남아 있는 경우 트랜잭션 외부의 로그 메시지가 기록됩니다.
  </Collapser>
</CollapserGroup>

#### 로그 컨텍스트 데이터 [#log-context-data]

Java 에이전트는 컨텍스트 데이터(logback/slf4j에 Mapped Diagnostic Context(매핑된 진단 컨텍스트), log4j2의 ThreadContext)를 캡처하고 해당 콘텐츠를 뉴렐릭에 전달되는 로그의 속성으로 추가할 수 있습니다. `forwarding` 스탠자 하단에 중첩된 `context_data` 스탠자 아래에서 설정을 통해 이를 제어할 수 있습니다. 이러한 설정은 `newrelic.config.application_logging.forwarding.context_data` 접두사가 붙은 시스템 속성으로 재정의할 수 있습니다. 사용 가능한 옵션은 다음과 같습니다.

<CollapserGroup>
  <Collapser
    id="cfg-application_logging_forwarding_context_data-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    컨텍스트 데이터를 뉴렐릭에 대한 로그 속성으로 보내는 `true`로 설정합니다.

    로그의 컨텍스트 데이터를 뉴렐릭으로 전송하지 않으려면 이 값을 `false`로 설정합니다.
  </Collapser>

  <Collapser
    id="cfg-application_logging_forwarding_context_data-include"
    title="include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    로그 전달에 `context_data`가 활성화된 경우 이 목록에 있는 모든 속성 키가 로그 레코드의 뉴렐릭으로 전송됩니다. 이 목록이 비어 있으면 모든 속성이 전송됩니다.
  </Collapser>

  <Collapser
    id="cfg-application_logging_forwarding_context_data-exclude"
    title="exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    로그 전달에 `context_data`가 활성화된 경우 이 목록에 있는 모든 속성 키가 로그 레코드의 뉴렐릭으로 전송되지 않습니다.

    포함 및 제외 목록은 [속성 규칙](/docs/apm/agents/java-agent/configuration/java-agent-configuration-config-file)에 정의된 `Exclude overrides include`, `More specific rules take priority`, `Keys are case-sensitive` 및 `Use an asterisk for wildcards` 규칙을 따릅니다.
  </Collapser>
</CollapserGroup>

### 로그 메트릭 [#log-metrics]

개별 로그 라인을 데코레이팅하고 전송할 수 있을 뿐만 아니라 Java 에이전트는 뉴렐릭 UI의 로그 차트에 표시되는 로깅 메트릭 데이터도 캡처할 수 있습니다. 이는 `metrics` 스탠자 아래의 설정으로 제어되며 `newrelic.config.application_logging.metrics` 접두사가 붙은 시스템 속성으로 재정의할 수 있습니다. 사용 가능한 유일한 옵션은 다음과 같습니다.

<CollapserGroup>
  <Collapser
    id="cfg-application_logging_metrics-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    NR1 요약 페이지의 차트에 표시되는 로그 및 로그 수준에 대한 메트릭 정보 캡처를 활성화합니다.

    에이전트가 로그에 대한 메트릭 정보를 캡처할 수 있도록 하려면 이 설정을 `true`로 둡니다.

    이 기능을 비활성화하려면 이를 `false`로 설정합니다.

    <Callout variant="important">
      로그 메트릭 수집을 비활성화하면 요약 페이지의 로그 차트가 공백으로 표시됩니다.
    </Callout>
  </Collapser>
</CollapserGroup>

### 로그 장식 [#log-decorating]

`local_decorating` 스탠자를 사용하면 로컬 로그 줄 데코레이팅을 제어할 수 있으며 `newrelic.config.application_logging.local_decorating` 접두사가 붙은 시스템 속성으로 재정의할 수 있습니다. 사용 가능한 유일한 옵션은 다음과 같습니다.

<CollapserGroup>
  <Collapser
    id="cfg-application_logging-local_decorating"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    이것은 로컬에 저장된 로그 라인의 데코레이팅 여부를 제어합니다. 이는 로그 전달 활성화 여부와 무관하며 별도의 로그 전달 솔루션을 사용하려는 고객을 지원하는 데 사용됩니다.

    에이전트가 지원되는 프레임워크에 의해 로컬로 저장된 로그 라인에 메타데이터를 연결하는 문맥적 로그를 추가하도록 하려면 이를 `true`로 설정합니다.

    <Callout variant="important">
      로컬 로그 데코레이팅을 활성화하면 애플리케이션 로그 파일에 연결 메타데이터가 추가되어 디스크 스토리지가 증가합니다. 이것이 허용되지 않는 경우, 이 기능을 비활성화하십시오.

      로그 전달이 활성화된 경우 로컬 로그 데코레이션을 사용할 필요가 없습니다. 그렇게 하면 연결 메타데이터가 로그 메시지에 불필요하게 추가되어 시각적으로 산만해질 수 있습니다.
    </Callout>

    로컬 데코레이팅 기능을 끄려면 이를 `false`로 설정합니다. 이는 뉴렐릭으로 전송되는 로그 라인의 데코레이팅에는 영향을 미치지 _않습니다_.
  </Collapser>
</CollapserGroup>

## 로깅 구성 [#Logging-Configuration]

이는 일반 구성 변수의 일부입니다. 디버깅을 위해 자주 조정되기 때문에 여기에서 분류됩니다.

일부 로깅 구성 변수는 동적이며 적용하기 위해 호스트를 다시 시작할 필요가 없습니다. 예를 들어, 로그 파일이 너무 빨리 증가하는 경우 [`log_level`](#cfg-log_level)을 덜 자세한 설정으로 설정하여 보고율을 줄일 수 있습니다.

다음은 로그 회전에 영향을 미치는 구성 변수의 우선순위입니다.

* `log_daily`가 `true`이면 다른 로그 회전 설정은 무시됩니다.
* `log_file_count`가 `1` 또는 `0`이면 크기 제한이 무시됩니다.
* 마지막으로 에이전트는 `log_limit_in_kbytes`를 적용합니다.

증가율에 따라 로그 파일 크기가 설정된 값을 약간 초과할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="cfg-log_file_count"
    title="log_daily"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    매일 로그를 롤링하려면 `true`로 설정합니다. 로그 회전에 영향을 미치는 다른 구성 변수를 재정의합니다.
  </Collapser>

  <Collapser
    id="cfg-log_file_count"
    title="log_file_count"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `1`
          </td>
        </tr>
      </tbody>
    </table>

    로그 회전을 사용할 때 보관할 최대 로그 파일 수입니다.
  </Collapser>

  <Collapser
    id="cfg-log_file_name"
    title="log_file_name"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `newrelic_agent.log`
          </td>
        </tr>
      </tbody>
    </table>

    정규화되지 않은 로그 파일 이름 또는 표준 출력에 로그할 문자열 `STDOUT`입니다.
  </Collapser>

  <Collapser
    id="cfg-log_file_path"
    title="log_file_path"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `logs` `newrelic.jar`가 있는 하위 디렉터리
          </td>
        </tr>
      </tbody>
    </table>

    로그 파일 경로입니다.

    <Callout variant="tip">
      `log_file_path`가 지정된 경우 디렉터리가 이미 존재해야 합니다. 기본값이 사용되는 경우 에이전트는 디렉터리 생성을 시도합니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-log_level"
    title="log_level"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `info`
          </td>
        </tr>
      </tbody>
    </table>

    로그 상세 수준입니다.

    에이전트는 자체 로그 파일을 사용하여 애플리케이션의 로깅과 별도로 로깅을 유지합니다. 자세한 옵션은 다음과 같습니다.

    * `off`

    * `severe`

    * `warning`

    * `info`

    * `fine`

    * `finer`

    * `finest`

      이 설정은 동적이므로 실행 중인 에이전트는 JVM을 다시 시작하지 않고도 `newrelic.yml`에 대한 변경 사항을 알 수 있습니다.
  </Collapser>

  <Collapser
    id="cfg-log_limit_in_kbytes"
    title="log_limit_in_kbytes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `0`
          </td>
        </tr>
      </tbody>
    </table>

    로그 파일이 회전되는 로그 파일 크기(KB)입니다. 제한이 없는 경우 `0`으로 설정합니다.
  </Collapser>
</CollapserGroup>

## 메시지 추적기 [#Transaction_Tracer]

이러한 옵션은 `message_tracer` 스탠자에 설정되며 `newrelic.config.message_tracer` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="tt-enabled"
    title="segment_parameters.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    추적 프로그램 속성에 메시지 속성을 추가합니다. 끄려면 이것을 `false`로 설정합니다.
  </Collapser>
</CollapserGroup>

## 스팬 이벤트

[분산 추적](#distributed-tracing)에 대한 [스팬 이벤트](/docs/apm/distributed-tracing/ui-data/span-event)가 보고됩니다. 스팬 이벤트를 보고하려면 분산 추적을 활성화해야 합니다.

스팬 구성은 `span_events` 스탠자에서 설정되며 `newrelic.config.span_events` 접두사가 있는 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다. 옵션에는 다음이 포함됩니다.

<CollapserGroup>
  <Collapser
    id="cfg-span-events-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    스팬 이벤트 보고를 활성화/비활성화하는 데 사용됩니다.
  </Collapser>

  <Collapser
    id="cfg-span-events-max-samples-stored"
    title="max_samples_stored"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `2000`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트 수집 주기 동안 캡처할 수 있는 Span 이벤트 수를 결정합니다. Java 에이전트 7.4.0 이상에서 사용할 수 있습니다.

    <Callout variant="important">
      Span 이벤트 수를 늘리면 추가 에이전트 오버헤드가 발생할 수 있습니다. 최대값은 뉴렐릭에 의해 서버 측에 부과될 수 있습니다.
    </Callout>
  </Collapser>

  <Collapser
    id="cfg-span-events-attributes-enabled"
    title="attributes.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 스팬 이벤트의 모든 속성을 켜거나 끌 수 있습니다. 루트 수준의 `attributes.enabled`가 `false`이면 이 속성( `span_events.attributes.enabled` )이 설정되는 방식에 관계없이 스팬 이벤트에 속성이 전송되지 않습니다.
  </Collapser>

  <Collapser
    id="cfg-span-events-attributes-include"
    title="attributes.include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    스팬 이벤트에 속성이 활성화된 경우, 이 목록에 있는 모든 속성 키는 `span_events`로 뉴렐릭으로 전송됩니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-span-events-attributes-exclude"
    title="attributes.exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 모든 속성 키는 스팬 이벤트에서 뉴렐릭으로 전송되지 않습니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>
</CollapserGroup>

<Callout variant="important">
  스팬 이벤트 속성 필터링에는 [Java 에이전트 버전 4.10.0 이상](/docs/agents/java-agent/installation/upgrade-java-agent)이 필요합니다.
</Callout>

## 스트립 예외 [#strip_exceptions]

이러한 옵션은 `strip_exception_messages` 스탠자에 설정되며 달리 명시되지 않는 한 `newrelic.config.strip_exception_messages` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다. 이 구성을 사용하여 Java 예외 메시지가 뉴렐릭에 보고되는지 여부를 제어할 수 있습니다.

<CollapserGroup>
  <Collapser
    id="strip_exception_messages"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    기본적으로 이는 에이전트가 모든 예외에서 [뉴렐릭 수집기](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#collector)로 메시지를 보낸다는 것을 의미하는 `false`로 설정됩니다.

    * 이것을 `true`로 설정하면, 에이전트가 민감한 정보를 실수로 캡처하는 것을 방지하기 위해 예외에서 메시지를 제거합니다.
    * [높은 보안 모드](/docs/agents/manage-apm-agents/configuration/high-security-mode)를 활성화하면 자동으로 `true`로 설정됩니다.
    * `enabled`를 `true`로 설정했지만 에이전트가 특정 예외에서 메시지를 캡처하도록 하려면 [허용 목록](#strip_exception_messages_whitelist)에 예외를 추가합니다.
  </Collapser>

  <Collapser
    id="strip_exception_messages_whitelist"
    title="whitelist(더 이상 사용되지 않음)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="important">
      이 구성은 에이전트 버전 5.10.0부터 더 이상 사용되지 않으며 향후 에이전트 버전에서 제거됩니다. 대신 `allowed_classes`을 사용합니다.
    </Callout>

    `enabled`를 `true`로 설정했지만 에이전트가 특정 예외에 대한 메시지를 캡처하도록 하려면 각 예외를 쉼표로 구분하여 `whitelist`에 추가합니다.
  </Collapser>

  <Collapser
    id="strip_exception_messages_allowed_classes"
    title="allowed_classes"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    `enabled`를 `true`로 설정했지만 에이전트가 특정 예외에 대한 메시지를 캡처하도록 하려면 각 예외를 쉼표로 구분하여 `allowed_classes`에 추가합니다.
  </Collapser>
</CollapserGroup>

## 스레드 프로파일러 [#Thread_Profiler]

이러한 옵션은 `thread_profiler` 스탠자에 설정되며 `newrelic.config.thread_profiler` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

스레드 프로파일러는 애플리케이션 스레드가 실행될 때 wall clock time, CPU 시간 및 메서드 호출 수를 측정합니다.

<CollapserGroup>
  <Collapser
    id="tp-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    스레드 프로파일러를 활성화합니다.
  </Collapser>
</CollapserGroup>

## 트랜잭션 이벤트 [#Transaction_Events]

이러한 옵션은 `transaction_events` 스탠자에 설정되며 `newrelic.config.transaction_events` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

트랜잭션 이벤트는 UI에 히스토그램과 백분위수를 표시하기 위한 데이터를 제공합니다.

<Callout variant="important">
  이전에는 이 스탠자를 `analytics_events`라고 했습니다. 구성 파일이 여전히 `analytics_events`를 사용하는 경우, `transaction_events`를 사용하도록 에이전트를 업데이트하십시오.
</Callout>

<CollapserGroup>
  <Collapser
    id="ae-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 이벤트 서비스를 활성화합니다.
  </Collapser>

  <Collapser
    id="ae-max_samples_stored"
    title="max_samples_stored"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `2000`
          </td>
        </tr>

        <tr>
          <th>
            Max
          </th>

          <td>
            `10000`
          </td>
        </tr>
      </tbody>
    </table>

    60초마다 보고되는 샘플링된 트랜잭션 이벤트의 최대 수입니다.
  </Collapser>

  <Collapser
    id="tt-custom-request-headers"
    title="custom_request_headers"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            맵 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="important">
      다른 설정과 달리 `custom_request_headers`는 함께 쌍을 이루어야 하며 `newrelic.yml` 파일에서 설정되어야 합니다. Java 가상 머신 인수(시스템 속성) 또는 환경 변수로 덮어쓸 수 없습니다.
    </Callout>

    쌍을 이룬 키 `header_name` 및 옵션 `header_alias`가 포함된 맵 목록입니다. 하나 이상의 커스텀 HTTP 요청 헤더를 선택하여 트랜잭션 속성으로 추가합니다.

    여러 헤더 구성을 나열할 수 있습니다.

    ```yml
    transaction_events:
      custom_request_headers:
        -
          header_name: "X-Custom-Header-1"
        -
          header_name: "X-Custom-Header-2"
          header_alias: "CustomHeader2alias"
    ```

    첫 번째 맵 세트에서 `X-Custom-Header-1`은 요청 객체의 해당 값에 대한 헤더 이름으로 에이전트에 의해 캡처되고 보고됩니다. `header_name`은 또한 뉴렐릭으로 전송되는 속성의 이름이 됩니다.

    두 번째 맵 세트에서 요청 헤더는 `X-Custom-Header-2`이지만 `CustomHeader2alias`는 뉴렐릭으로 전송된 이름입니다.
  </Collapser>

  <Collapser
    id="cfg-events-attributes-enabled"
    title="attributes.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 트랜잭션 이벤트의 모든 속성을 켜거나 끌 수 있습니다. 루트 수준에서 `attributes.enabled`가 `false`이면 이 속성이 `transaction_events`에서 설정되는 방식에 관계없이 트랜잭션 이벤트에 속성이 전송되지 않습니다.
  </Collapser>

  <Collapser
    id="cfg-te-attributes-include"
    title="attributes.include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 이벤트에 대해 속성이 활성화된 경우 이 목록에 있는 모든 속성 키가 트랜잭션 이벤트의 wall clock time으로 전송됩니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-te-attributes-exclude"
    title="attributes.exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 모든 속성 키는 트랜잭션 이벤트에서 wall clock time으로 전송되지 않습니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>
</CollapserGroup>

## 트랜잭션 세그먼트

이러한 옵션은 `transaction_segments` 스탠자에 설정되며 `newrelic.config.transaction_segments` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

트랜잭션 세그먼트는 개별 작업(일반적으로 메서드 호출)을 나타내며 [트랜잭션 트레이스](/docs/traces/transaction-traces) 내에 표시됩니다.

<Callout variant="important">
  트랜잭션 세그먼트 속성 필터링을 사용하려면 [Java 에이전트 버전 4.10.0 이상](/docs/agents/java-agent/installation/upgrade-java-agent)이 필요합니다.
</Callout>

<CollapserGroup>
  <Collapser
    id="cfg-ts-attributes-enabled"
    title="attributes.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 트랜잭션 세그먼트의 모든 속성을 켜거나 끌 수 있습니다. 루트 수준의 `attributes.enabled`가 `false`이면 이 속성( `transaction_segments.attributes.enabled` )이 설정되는 방식에 관계없이 속성이 트랜잭션 세그먼트로 전송되지 않습니다.
  </Collapser>

  <Collapser
    id="cfg-ts-attributes-include"
    title="attributes.include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 세그먼트에 대해 속성이 활성화된 경우, 이 목록에 있는 모든 속성 키는 트랜잭션 세그먼트의 뉴렐릭으로 전송됩니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-ts-attributes-exclude"
    title="attributes.exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 모든 속성 키는 트랜잭션 세그먼트의 뉴렐릭으로 전송되지 않습니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>
</CollapserGroup>

## 트랜잭션 추적기

이러한 옵션은 `transaction_tracer` 스탠자에 설정되며 `newrelic.config.transaction_tracer` 접두사가 붙은 시스템 속성을 사용하여 [재정의](#System_Properties)할 수 있습니다.

[트랜잭션 추적](/docs/traces/transaction-traces)은 느린 트랜잭션에 대한 심층 정보를 캡처하고 이를 뉴렐릭 서비스로 보냅니다. 트랜잭션에는 발행된 쿼리 문을 포함하여 트랜잭션의 정확한 호출 순서가 포함됩니다.

<Callout variant="important">
  트랜잭션 이름 끝에 대괄호 `[suffix]`를 사용하지 마십시오. 뉴렐릭은 자동으로 이름에서 괄호를 제거합니다. 대신 필요한 경우 괄호 `(suffix)` 또는 기타 기호를 사용합니다.
</Callout>

<CollapserGroup>
  <Collapser
    id="tt-enabled"
    title="활성화"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 추적기는 기본적으로 활성화되어 있습니다. 끄려면 이`false` 로 설정합니다.
  </Collapser>

  <Collapser
    id="tt-explain_enabled"
    title="explain_enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 느린 쿼리에 대한 `EXPLAIN` 계획을 캡처할지 여부를 결정합니다. [MySQL 및 PostgreSQL에 대해서만 지원됩니다.](/docs/agents/java-agent/getting-started/compatibility-requirements-java-agent#JDBC)
  </Collapser>

  <Collapser
    id="tt-explain_threshold"
    title="explain_threshold"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Float
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `0.5`
          </td>
        </tr>
      </tbody>
    </table>

    느린 쿼리 및 `EXPLAIN` 계획( [지원되는 경우](/docs/agents/java-agent/getting-started/compatibility-requirements-java-agent#JDBC) )이 캡처되지 않는 쿼리 실행 시간 임계값(초)입니다. `record_sql`이 `raw` 또는 `obfuscated`로 설정된 경우에만 느린 쿼리와 관련이 있습니다. `explain_enabled`가 `true`로 설정된 경우에만 `EXPLAIN` 계획과 관련이 있습니다.
  </Collapser>

  <Collapser
    id="insert_sql_max_length"
    title="insert_sql_max_length"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `2000`
          </td>
        </tr>
      </tbody>
    </table>

    SQL 쿼리 문자열의 문자 제한입니다. 많은 양의 정보가 포함된 느린 SQL 쿼리가 많은 경우 성능이나 뉴렐릭에서 데이터를 보는 속도에 부정적인 영향을 미칠 수 있습니다. 정보와 성능의 적조항한 균형을 찾을 때까지 값을 점진적으로 늘립니다.
  </Collapser>

  <Collapser
    id="tt-log_sql"
    title="log_sql"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    뉴렐릭에 업로드하는 대신 에이전트 로그 파일에 쿼리를 로깅하려면 `true`로 설정합니다. 쿼리는 `record_sql` 모드를 사용하여 기록됩니다.
  </Collapser>

  <Collapser
    id="tt-record_sql"
    title="record_sql"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `obfuscated`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 추적기가 켜져 있으면 쿼리 문을 선택적으로 기록할 수 있습니다. 레코더에는 세 가지 모드가 있습니다.

    * `off`: 쿼리를 보내지 않습니다.
    * `raw`: 쿼리문을 원래 형태로 보냅니다.
    * `obfuscated`: 숫자 및 문자열 리터럴을 제거합니다.
  </Collapser>

  <Collapser
    id="tt-stack_based_naming"
    title="stack_based_naming(Play 2.x 이상만 해당)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `false`

            Java 에이전트 버전 3.12.1이 `false`로 변경될 때까지 기본값은 `true` }입니다.
          </td>
        </tr>
      </tbody>
    </table>

    이 옵션은 Play 2.x+ 전용입니다. Play/Scala 계측은 `Thread.getStackTrace()`를 사용하여 추적 프로그램 이름을 개선할 수 있지만 오버헤드가 증가합니다.
  </Collapser>

  <Collapser
    id="tt-stack_trace_threshold"
    title="stack_trace_threshold"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `0.5`
          </td>
        </tr>
      </tbody>
    </table>

    SQL 호출에서 스택 트레이스를 수집하기 위한 임계값(초)을 정의합니다. SQL 문이 이 임계값을 초과하면 에이전트는 현재 스택 트레이스를 캡처합니다. 이는 긴 SQL 호출이 시작되는 위치를 정확히 찾아내는 데 유용합니다.
  </Collapser>

  <Collapser
    id="tt-top_n"
    title="top_n"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `20`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 다양한 트랜잭션 추적을 제어합니다. `top_n`은 추적이 생성될 고유하고 느린 트랜잭션의 수를 나타내는 정수입니다.

    * 트랜잭션 트레이스가 앱에서 가장 느린 실제 트랜잭션을 보다 정확하게 반영하려면 이 값을 **더 낮게** 설정합니다.

    * 더 다양한 트랜잭션 배열을 샘플링하려면 값을 **더 높게** 설정합니다.

      값이 `0`이면 가장 느린 트랜잭션 **만** 항상 추적됩니다. 항상 가장 느린 하나 또는 두 개의 트랜잭션이 있을 수 있고 동일한 트랜잭션 트레이스를 반복적으로 보는 것은 아마도 많은 가치를 제공하지 못할 것이기 때문에 이는 최적이 아닌 것으로 간주됩니다.

      동일한 트랜잭션이 가장 느린 경우가 많으면, `top_n` 설정을 통해 시간 경과에 따라 Java 에이전트가 가장 느린 `n` 트랜잭션을 샘플링하도록 만들 수 있습니다. 이를 통해 애플리케이션에 대한 더 많은 다양성과 인사이트를 얻을 수 있습니다.
  </Collapser>

  <Collapser
    id="tt-transaction_threshold"
    title="transaction_threshold"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열(부동 소수점)
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `apdex_f`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션을 추적할 수 있는 시기를 결정하는 데 사용되는 시간 임계값입니다. 트랜잭션의 응답 시간이 이 임계값을 초과하면 [트랜잭션 트레이스](/docs/apm/transactions/transaction-traces/transaction-traces)가 기록되어 뉴렐릭으로 전송됩니다.

    기본값은 `apdex_f` (기본값)이며 임계값을 ["Frustrated"](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#apdex_f) [Apdex](/docs/site/apdex-measuring-user-satisfaction) 수준(`apdex_t` 값의 4배)으로 설정합니다. 초 수를 나타내는 부동 소수점 값을 입력하여 특정 시간 임계값을 설정할 수도 있습니다.
  </Collapser>

  <Collapser
    id="tt-slow_query_whitelist"
    title="slow_query_whitelist(지원 중단됨)"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="important">
      이 구성은 에이전트 버전 5.10.0부터 더 이상 사용되지 않으며 향후 에이전트 버전에서 제거됩니다. 대신 `collect_slow_queries_from`을 사용합니다.
    </Callout>

    기본적으로 [높은 보안 모드](/docs/accounts-partnerships/accounts/security/high-security) 에서는 에이전트가 [느린 쿼리](/docs/apm/applications-menu/monitoring/viewing-slow-query-details) 를 수집할 수 없습니다. 높은 보안이 활성화된 경우에도 DataStax 드라이버에서 Cassandra 쿼리를 수집하려면 이 옵션을 활성화합니다. 높은 보안을 사용하지 않는 경우 에이전트는 느린 쿼리를 자동으로 수집합니다.

    DataStax 드라이버 2.1.2의 경우 다음 규칙을 허용 목록에 추가합니다.

    ```yml
    transaction_tracer:
      slow_query_whitelist:
        'com.newrelic.instrumentation.cassandra-datastax-2.1.2'
    ```

    DataStax 드라이버 3.0.0의 경우 다음 규칙을 허용 목록에 추가합니다.

    ```yml
    transaction_tracer:
      slow_query_whitelist:
        'com.newrelic.instrumentation.cassandra-datastax-3.0.0'
    ```
  </Collapser>

  <Collapser
    id="tt-collect_slow_queries_from"
    title="collect_slow_queries_from"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    기본적으로 [높은 보안 모드](/docs/accounts-partnerships/accounts/security/high-security) 에서는 에이전트가 [느린 쿼리](/docs/apm/applications-menu/monitoring/viewing-slow-query-details) 를 수집할 수 없습니다. 높은 보안이 활성화된 경우에도 DataStax 드라이버에서 Cassandra 쿼리를 수집하려면 이 옵션을 활성화합니다. 높은 보안을 사용하지 않는 경우 에이전트는 느린 쿼리를 자동으로 수집합니다.

    DataStax 드라이버 2.1.2의 경우 다음 규칙을 허용 목록에 추가합니다.

    ```yml
    transaction_tracer:
      collect_slow_queries_from:
        'com.newrelic.instrumentation.cassandra-datastax-2.1.2'
    ```

    DataStax 드라이버 3.0.0의 경우 다음 규칙을 허용 목록에 추가합니다.

    ```yml
    transaction_tracer:
      collect_slow_queries_from:
        'com.newrelic.instrumentation.cassandra-datastax-3.0.0'
    ```
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-enabled"
    title="attributes.enabled"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            Boolean
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 트랜잭션 트레이스에 대한 모든 속성을 켜거나 끌 수 있습니다. 루트 수준의 `attributes.enabled`가 `false`이면 이 속성( `transaction_tracer.attributes.enabled` )이 설정되는 방식에 관계없이 속성이 트랜잭션 트레이스로 전송되지 않습니다.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-include"
    title="attributes.include"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 추적에 대해 속성이 활성화된 경우, 이 목록에 있는 모든 속성 키가 트랜잭션 추적의 뉴렐릭으로 전송됩니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-exclude"
    title="attributes.exclude"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이 목록에 있는 모든 속성 키는 트랜잭션 추적에서 뉴렐릭으로 전송되지 않습니다. 자세한 내용은 [에이전트 속성 규칙](/docs/apm/other-features/attributes/agent-attributes)을 참조하십시오.
  </Collapser>

  <Collapser
    id="cfg-tt-token_limit"
    title="token_limit"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `3000`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션당 생성할 수 있는 토큰의 수를 제한합니다. 이 값을 늘리면 에이전트가 사용하는 메모리 양과 뉴렐릭으로 전송되는 데이터 양이 증가하기 때문에 성능에 영향을 미칠 수 있습니다.
  </Collapser>

  <Collapser
    id="cfg-tt-segment_limit"
    title="segment_limit"
  >
    <table>
      <tbody>
        <tr>
          <th>
            Type
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본
          </th>

          <td>
            `3000`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션당 생성할 수 있는 세그먼트 수를 제한합니다. 이 값을 높이면 에이전트가 사용하는 메모리 양과 뉴렐릭으로 전송되는 데이터 양이 증가하므로 성능에 영향을 미칠 수 있습니다.
  </Collapser>
</CollapserGroup>