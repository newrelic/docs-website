---
title: newrelic_ignore_transaction(PHP 에이전트 API)
type: apiDoc
shortDescription: 현재 트랜잭션을 계측하지 마십시오.
tags:
  - Agents
  - PHP agent
  - PHP agent API
metaDescription: New Relic PHP agent API call to ignore the current transaction entirely.
translationType: machine
---

## 통사론

```php
newrelic_ignore_transaction()
```

현재 트랜잭션을 계측하지 마십시오.

## 요구 사항

모든 에이전트 버전과 호환됩니다.

## 설명

이 트랜잭션에 대한 데이터를 생성하지 마십시오. 이것은 알려진 이유로 인해 특히 느린 트랜잭션이 있고 트랜잭션 추적을 자주 생성하거나 사이트 평균을 왜곡하는 것을 원하지 않을 때 유용합니다.

## 매개변수

이 호출은 매개변수를 허용하지 않습니다.

## 예

```php
function example() {
    if (extension_loaded('newrelic')) { // Ensure PHP agent is available
        newrelic_ignore_transaction();
    }
    ...
}
```