---
title: PHP 에이전트 구성
tags:
  - Agents
  - PHP agent
  - Configuration
metaDescription: 'New Relic PHP agent configuration: How to edit newrelic.ini config settings (like app name, slow traces, parameters, log file levels, custom frameworks).'
freshnessValidatedDate: never
translationType: machine
---

우리의 PHP 에이전트에는 보고되는 데이터의 유형과 양을 미세 조정할 수 있는 다양한 설정이 있습니다. 대부분의 사용자에게 기본값은 오버헤드와 유틸리티의 최상의 조합을 생성합니다. 그러나 특정 요구 사항에 맞게 설정을 변경할 수 있습니다.

<Callout variant="important">
  INI 설정을 변경한 후에는 항상 웹 서버를 다시 시작하십시오. 그렇지 않으면 [즉시 적용](/docs/agents/php-agent/troubleshooting/ini-settings-not-taking-effect-immediately) 되지 않을 수 있습니다.
</Callout>

본 문서는 **설치 후** 뉴렐릭 PHP 에이전트를 구성하는 방법을 설명합니다.

## 시작하기 전에 [#before-you-begin]

아직 PHP 에이전트를 설치하지 않았다면 [PHP 에이전트 설치 개요를](/docs/apm/agents/php-agent/installation/php-agent-installation-overview) 참조하세요.

* 에이전트 다운로드
* 설치 과정을 완료하세요

에이전트 설치가 완료되면 여기로 돌아와 구성 설정을 세부적으로 조정하십시오.

## 구성 우선 순위 [#config-options-precedence]

다음은 PHP 에이전트가 구성을 위해 따라야 하는 우선 순위에 대한 간단한 그림입니다. [서버 측 구성](/docs/agents/manage-apm-agents/configuration/server-side-agent-configuration) 은 적용할 수 없습니다. PHP 에이전트를 사용하는 앱의 UI에서 변경할 수 있는 유일한 값은 앱의 별칭과 Apdex-T 값입니다.

<img title="php-config-order.png" alt="php-config-order.png" src="/images/apm_diagram_php-agent-configuration-hierarchy.webp" />

<figcaption>
  New Relic의 PHP 에이전트를 사용하면 API 설정 [이 디렉토리별 구성 설정보다 우선 적용됩니다](/docs/agents/php-agent/configuration/php-directory-ini-settings) . 디렉토리별 설정은 `php.ini` 파일 설정을 재정의합니다. 서버 측 구성은 적용할 수 없습니다.
</figcaption>

## 구성 파일 변수 [#inivar-background]

설치하는 동안 [`newrelic-install`](/docs/agents/php-agent/installation/newrelic-install-script) 스크립트는 작성한 구성 파일에 대한 정보 또는 편집해야 하는 파일 목록을 제공합니다. 기본적으로 `newrelic.ini` 이라는 구성 파일을 생성하려고 시도합니다.

경우에 따라 `php.ini` 파일에 구성 옵션을 추가하라는 지시를 받을 수 있습니다. 필요한 경우에만 이 작업을 수행하십시오. 정확히 어떤 파일을 편집해야 하는지는 특정 버전의 PHP가 구성된 방식에 따라 다릅니다.

가장 일반적인 두 가지 구성은 다음과 같습니다.

* 단일 `newrelic.ini` 파일을 사용합니다. 이것은 특별한 옵션 없이 PHP를 직접 설치하거나 컴파일한 경우 일반적으로 기본값입니다.
* 모든 `.ini` 파일에 대해 지정된 디렉토리를 스캔합니다.

어떤 파일을 편집할지 확실하지 않은 경우:

* 명령줄에서 `php -i` 의 출력을 검토합니다.

* 브라우저에서 스크립트가 포함된 페이지의 출력을 검토합니다.

  ```php
  <?php phpinfo(); ?>
  ```

* `newrelic.ini` 파일이 나타나면 사용하십시오.

## 가변 범위 [#inivar-scope]

`newrelic.ini` 파일의 각 변수에는 정의된 범위가 있습니다. 범위는 설정을 지정하거나 수정할 수 있는 위치를 제어합니다.

New Relic 설정에 지원되는 두 가지 범위는 다음과 같습니다.

* <DNT>**SYSTEM**</DNT>: 전역 `newrelic.ini` 파일에 전역적으로 설정된 값입니다.
* <DNT>**PERDIR**</DNT>: [디렉토리별로](/docs/agents/php-agent/configuration/php-directory-ini-settings) 설정된 값입니다.

각각은 보다 일반적인 수준에서 설정할 수도 있습니다. 각각의 유효한 위치는 다음과 같습니다.

<table>
  <thead>
    <tr>
      <th width={275}>
        <DNT>
          **Can be set for:**
        </DNT>
      </th>

      <th>
        체계
      </th>

      <th>
        페르디르
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        전역 `newrelic.ini` 파일에서 설정합니다.
      </td>

      <td>
        <Icon style={{color: '#328787'}} name="fe-check" />
      </td>

      <td>
        <Icon style={{color: '#328787'}} name="fe-check" />
      </td>
    </tr>

    <tr>
      <td>
        디렉토리별로 설정합니다.
      </td>

      <td />

      <td>
        <Icon style={{color: '#328787'}} name="fe-check" />
      </td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
  New Relic 설정에 `ini_set()` 을(를) 사용할 수 없습니다.
</Callout>

## 변수 유형 [#inivar-type]

`newrelic.ini` 파일의 각 변수에는 정의된 유형이 있습니다. 유형은 사용하는 값의 구문을 지정합니다.

<table>
  <thead>
    <tr>
      <th width={100}>
        <DNT>
          **Variable type**
        </DNT>
      </th>

      <th>
        <DNT>
          **Formatting and contents**
        </DNT>
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        <DNT>
          **String**
        </DNT>
      </td>

      <td>
        문자열 값에는 영숫자 문자와 구두점이 포함될 수 있습니다. 값은 따옴표로 구분됩니다.
      </td>
    </tr>

    <tr>
      <td>
        <DNT>
          **Boolean**
        </DNT>
      </td>

      <td>
        논리적 참 또는 거짓 설정입니다. 유효한 값은 다음과 같습니다.

        * true 또는 활성화된 경우: `on` , `true` , 숫자 `1` .
        * false 또는 비활성화된 경우: `off` , `false` , 숫자 `0` .
      </td>
    </tr>

    <tr>
      <td>
        <DNT>
          **Number**
        </DNT>
      </td>

      <td>
        숫자 값에는 숫자와 부동 소수점을 나타내는 마침표만 포함될 수 있습니다. 달리 명시되지 않는 한 모든 숫자는 부동 소수점이 아닌 정수입니다.
      </td>
    </tr>

    <tr>
      <td>
        <DNT>
          **Duration**
        </DNT>
      </td>

      <td>
        기간을 나타내는 따옴표로 구분된 문자열 값입니다. 문자 플래그를 사용하여 시간 구성 요소를 구분합니다. 플래그가 없는 경우 시간은 밀리초 단위입니다.

        * `w`\\= 주

        * `d`\\= 일

        * `h`\\= 시간

        * `m`\\= 분

        * `s`\\= 초

        * `ms`\\= 밀리초

        * `us`\\= 마이크로초

          예시 기간:

        * `"1w3d23h10m"`\\= 1주 3일 23시간 10분

        * `"5h30m"`\\= 5시간 30분

        * `"500"`\\= 500밀리초
      </td>
    </tr>
  </tbody>
</table>

## 일반 구성 설정 [#inivar-settings]

이러한 설정은 `newrelic.ini` 파일에서 사용할 수 있습니다.

<CollapserGroup>
  <Collapser id="inivar-appname" title="newrelic.appname(강력 권장)">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"PHP Application"`
          </td>
        </tr>
      </tbody>
    </table>

    이 구성 옵션은 APM에서 데이터가 보고되는 애플리케이션 이름을 설정합니다. 혼동과 의도하지 않은 데이터 집계를 방지하려면 기본 이름을 설명이 포함된 이름으로 바꾸는 것이 좋습니다. 동일한 이름을 가진 모든 애플리케이션의 데이터는 New Relic에 병합되므로 신중하게 설정하십시오.

    디렉토리별 설정을 포함한 모든 애플리케이션 이름 지정 옵션 [은 PHP 애플리케이션 이름 지정](/docs/agents/php-agent/configuration/name-your-php-application) 을 참조하십시오.

    설정 값은 최대 3개의 애플리케이션 이름이 세미콜론으로 구분된 목록입니다. 목록의 첫 번째 이름은 <DNT>**primary**</DNT> 애플리케이션 이름입니다. 각 계정마다 [고유해야 합니다](/docs/apm/new-relic-apm/installation-configuration/name-your-application#app-name) . 애플리케이션 이름은 캐시에 대한 키로 사용됩니다. [앱에 여러 이름을](/docs/agents/manage-apm-agents/app-naming/use-multiple-names-app) 사용하는 경우 뉴렐릭은 캐싱을 위해 첫 번째 이름만 사용합니다. 따라서 각 애플리케이션 이름은 첫 번째 요소 <DNT>**once**</DNT> 로만 표시될 수 있습니다.

    <CollapserGroup>
      <Collapser id="cache-example" title="캐싱 예제">
        코드 세트 `newrelic.appname="App1;App3"` 에서 `newrelic.appname="App1;App2"` 이상을 설정하면 두 번째 코드가 작동하지 않는 것으로 나타납니다. 캐싱으로 인해 <DNT>**App1**</DNT> 및 <DNT>**App2**</DNT> 에 보고됩니다.
      </Collapser>
    </CollapserGroup>

    [중복되는 응용 프로그램 이름](/docs/agents/manage-apm-agents/app-naming/use-multiple-names-app) 이 필요한 경우 두 번째 또는 세 번째 위치에 공통 응용 프로그램 이름을 설정합니다. 예: `newrelic.appname="App2;App1"` 및 `newrelic.appname="App3;App1"` . 이는 예를 들어 응용 프로그램의 상위 집합을 추적하려는 경우에 유용합니다.
  </Collapser>

  <Collapser id="inivar-capture_params" title="newrelic.capture_params">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="caution">
      이 설정은 더 이상 사용되지 않으며 예상대로 작동하지 않을 수 있습니다. 대신 [속성 속성](/docs/agents/php-agent/attributes/enable-or-disable-attributes#deprecated-properties) 을 사용하십시오.
    </Callout>

    이 값을 프로그래밍 방식으로 설정하려면 [`newrelic_capture_params()`](/docs/agents/php-agent/php-agent-api/newrelic_capture_params) API 함수를 사용하세요.

    URL에 민감한 정보를 직접 전달하는 경우 비활성화된 상태로 유지하십시오.

    `true` 으로 설정하면 녹화를 활성화하고, VR(URL에서 `?` 이후의 모든 항목)을 통해 프로세서 트레이스에 전달됩니다. 이는 프로세서 트레이스를 표시할 때 <DNT>**Parameters**</DNT> 드롭다운 메뉴 아래에 나타납니다.
  </Collapser>

  <Collapser id="inivar-framework" title="newrelic.framework">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            (자동 감지)
          </td>
        </tr>
      </tbody>
    </table>

    New Relic PHP 에이전트는 지원하는 프레임워크를 자동으로 감지하지만, 실험적인 새 버전을 사용하거나 프레임워크를 사용자 정의한 경우 문제가 발생할 수 있습니다. 이 설정은 자동 프레임워크 감지를 비활성화하는 대신 에이전트에게 지정된 프레임워크에 따라 트랜잭션 이름을 지정하도록 지시합니다.

    `"no_framework"` 을 지정하면 프레임워크 관련 트랜잭션 이름이 완전히 비활성화됩니다.

    다음 값을 적절하게 사용합니다.

    * `"cakephp"`

    * `"codeigniter"`

    * `"drupal"` (Drupal 6 및 7용)

    * `"drupal8"` (Drupal 8 및 9용)

    * `"joomla"`

    * `"laravel"`

    * `"magento"`

    * `"magento2"`

    * `"mediawiki"`

    * `"slim"`

    * `"symfony2"` (Symfony 3의 경우 - Symfony 2.x는 지원되지 않음)

    * `"symfony4"` (심포니 4 및 5용)

    * `"wordpress"`

    * `"yii"`

    * `"zend"`

    * `"zend2"` (Zend 프레임워크 2 및 3용)

    * `"no_framework"` (감지된 경우에도 프레임워크를 강제 실행하지 않음)

      프레임워크 자동 감지가 실패하면 이 명령도 실패합니다.

      <Callout variant="tip">
        지원되는 프레임워크에 대한 최신 정보는 [PHP 호환성 페이지](/docs/apm/agents/php-agent/getting-started/php-agent-compatibility-requirements/) 를 확인하십시오.
      </Callout>
  </Collapser>

  <Collapser id="inivar-ignored_params" title="newrelic.ignored_params">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="caution">
      이 설정은 더 이상 사용되지 않으며 예상대로 작동하지 않을 수 있습니다. 대신 [속성 속성](/docs/agents/php-agent/attributes/enable-or-disable-attributes#deprecated-properties) 을 사용하십시오.
    </Callout>

    `newrelic.ignored_params` 설정을 사용하여 New Relic 서버로 전송되는 매개변수 목록에서 제외할 매개변수 이름의 쉼표로 구분된 목록을 지정합니다. 문자열 값을 인용 부호로 묶습니다.

    [`newrelic.capture_params`](#inivar-capture_params) 으로 매개변수 캡처를 활성화하면 민감한 사용자 데이터가 포함된 매개변수가 있을 수 있습니다. 이 데이터의 기록을 차단하려면 이것을 사용하십시오.
  </Collapser>

  <Collapser id="inivar-license" title="newrelic.license(필수)">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td>
            에이전트 버전 3.0에 도입되었습니다.
          </td>
        </tr>
      </tbody>
    </table>

    사용할 뉴렐릭 <InlinePopover type="licenseKey" />을 설정합니다. 다중 테넌트 시스템에서는 디렉터리별로 설정할 수 있습니다.

    <Callout variant="tip">
      이전 에이전트 버전에서 3.0 이상으로 업그레이드하면 데몬 구성 파일에서 라이선스가 제거되고(이유 설명 주석 포함) `/etc/newrelic/upgrade_please.key` 파일에 저장됩니다. 해당 파일에서 라이선스를 복사하고 `newrelic.ini` 파일에 설정합니다. `upgrade_please.key` 파일을 삭제합니다.
    </Callout>
  </Collapser>

  <Collapser id="inivar-loglevel" title="newrelic.loglevel">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"info"`
          </td>
        </tr>
      </tbody>
    </table>

    로그 파일에 전송된 메시지의 세부 수준을 설정합니다. 가능한 값은 다음과 같습니다.

    * `error`

    * `warning`

    * `info`

    * `verbose`

    * `debug`

    * `verbosedebug`

      에이전트 문제를 New Relic 기술 지원에 보고할 때 `verbosedebug` 이 가장 유용한 설정입니다. 그러나 이는 많은 정보를 매우 빠르게 생성할 수 있으므로 경험하고 있는 문제를 재현하는 데 걸리는 시간보다 더 오래 [에이전트를 `verbosedebug` 수준으로 유지하지 마십시오](/docs/agents/php-agent/troubleshooting/generating-logs-troubleshooting-php) .

      에이전트 문제를 New Relic 기술 지원에 보고할 때 특정 하위 시스템에 대해서만 디버깅을 활성화하는 사용자 지정 수준으로 설정하라는 메시지가 표시될 수 있습니다. 사용자 지정 수준은 이 문서의 범위를 벗어납니다.
  </Collapser>

  <Collapser id="inivar-tt-detail" title="newrelic.transaction_tracer.detail">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            숫자
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `1`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td id="inivar-tt-top100">
            이전에는 `newrelic.transaction_tracer.top100` 이었습니다.
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 추적의 세부 정보 수준을 설정합니다.

    * `1` 으로 설정하면 모든 서버 호출이 추적됩니다.

    * `0` 으로 설정하면 추적에는 New Relic에 의해 내부적으로 계측된 호출과 [`newrelic.transaction_tracer.custom`](#inivar-tt-custom) 을 사용하여 사용자가 정의한 호출만 포함됩니다.

      `1` 의 기본 수준은 성능에 영향을 미칩니다. 성능을 개선해야 하는 경우 세부 수준을 `0`로 설정해 보십시오.

      모든 서버 호출을 추적하지 않는 경우, 프로세서 트레이스의 시간 블록은 <DNT>**uninstrumented time**</DNT> 으로 표시되며 특정 기능(서버에서 내부적으로 정의됨)만 로그됩니다. 모든 통화를 보고하더라도 트레이스에 [계측되지 않는 시간이](/docs/agents/php-agent/troubleshooting/uninstrumented-time-traces) 있을 수 있습니다.
  </Collapser>

  <Collapser id="inivar-high-security" title="newrelic.high_security">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    모든 애플리케이션에 대한 높은 보안을 가능하게 합니다. 높은 보안이 `true` 으로 설정되면 다음 동작이 적용됩니다.

    * newrelic-daemon이 보안 연결(HTTPS)을 사용하지 않는 한 데이터는 New Relic으로 전송되지 않습니다. PHP 프로세스가 newrelic-daemon을 생성하면 `newrelic.daemon.ssl` 값에 관계없이 HTTPS를 사용하도록 구성됩니다.

    * 원시 쿼리 문자열은 `newrelic.transaction_tracer.record_sql` 값에 관계없이 수집되지 않습니다.

    * `newrelic.attributes` 구성 설정에 관계없이 요청 속성은 캡처되지 않습니다.

    * [`newrelic_add_custom_parameter`](/docs/agents/php-agent/php-agent-api/newrelic_add_custom_parameter) 및 [`newrelic_set_user_attributes`](/docs/agents/php-agent/php-agent-api/newrelic_set_user_attributes) API 함수는 효과가 없으며 `false` 를 반환합니다.

    * [`newrelic_record_custom_event`](/docs/agents/php-agent/php-agent-api/newrelic_record_custom_event) API 함수는 [New Relic](/docs/insights/new-relic-insights/understanding-insights/new-relic-insights) 에 이벤트를 제출하지 않습니다.

    * 에이전트는 오류에서 예외 메시지를 제거합니다.

      <Callout variant="caution">
        `newrelic.high_security` 설정을 변경하는 경우 <DNT>**also**</DNT> APM 사용자 인터페이스에서 보안 설정을 변경해야 합니다. 두 설정이 일치하지 않으면 데이터가 수집되지 않습니다. 자세한 내용은 [높은 보안을](/docs/subscriptions/high-security) 참조하세요.
      </Callout>
  </Collapser>

  <Collapser id="inivar-labels" title="newrelic.labels">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    [태그](/docs/apm/new-relic-apm/maintenance/labels-categories-organize-your-apps-servers) 를 추가합니다.

    <DNT>
      **Example tags**
    </DNT>

    ```ini
    "Server:One;Data Center:Primary"
    ```
  </Collapser>

  <Collapser id="inivar-process_host-display_name" title="newrelic.process_host.display_name">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    [APM UI에 표시](/docs/apm/new-relic-apm/maintenance/add-rename-remove-hosts#display_name) 할 호스트 이름을 설정합니다. 설정하면 에이전트가 자동으로 캡처하는 기본 호스트 이름을 무시합니다.
  </Collapser>
</CollapserGroup>

IDE에서 성능을 모니터링하기 위해 뉴렐릭 CodeStream 사용하는 경우 [, 저장소를 서비스와 연결](/docs/codestream/observability/repo-association) 하고 [빌드 SHA 또는 릴리스 태그를 오류와 연결할](/docs/codestream/observability/error-investigation/#buildsha) 수도 있습니다.

## 데몬 .ini 설정 [#inivar-daemon-settings]

이러한 설정 값은 데몬 시작을 제어합니다. 에이전트가 데몬을 시작해야 함을 감지하면 이러한 옵션을 데몬에 대한 적절한 명령줄 옵션으로 변환합니다.

이러한 모든 설정은 `newrelic.cfg` 파일의 설정을 미러링합니다. 모든 .ini 설정을 한 곳에 유지하기 위해 여기에서 반복됩니다. `newrelic.cfg` 의 각 설정에는 여기에 해당하는 항목이 있으며 `newrelic.daemon.` 가 접두사로 사용됩니다. 예를 들어, `newrelic.cfg` 의 `ssl` 설정은 .ini 파일의 `newrelic.daemon.ssl` 입니다.

<Callout variant="important">
  `/etc/newrelic/newrelic.cfg` 파일이 있는 경우 에이전트는 이러한 설정을 무시하고 에이전트는 데몬을 자동으로 시작 **하지 않습니다** .
</Callout>

데몬을 시작하는 방법과 외부 구성 파일을 사용하는 시기에 대한 자세한 내용은 [PHP 데몬 시작 모드](/docs/agents/php-agent/installation/starting-php-daemon) 를 참조하십시오.

<CollapserGroup>
  <Collapser id="inivar-daemon-app_timeout" title="newrelic.daemon.app_timeout">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            시간 지정 문자열; 예: `"5m"` 또는 `"1h20m"`
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"10m"`
          </td>
        </tr>
      </tbody>
    </table>

    애플리케이션이 비활성 상태로 간주될 때까지의 경과 시간을 설정합니다. 비활성 응용 프로그램은 최대 250개 응용 프로그램에 포함되지 않습니다. 허용되는 단위는 `"ns"` , `"us"` , `"ms"` , `"s"` , `"m"` 및 `"h"` 입니다.

    `0` 값은 &quot;시간 초과 없음&quot;으로 해석됩니다. 이로 인해 새 응용 프로그램은 항상 250개 응용 프로그램 제한에 포함되고 데몬은 응용 프로그램당 소량의 메모리를 운영 체제로 다시 해제할 수 없습니다.

    권장사항: 뉴렐릭 지원팀에서 요청하지 않는 한 이 값을 사용하지 마세요. 대신, 가끔 백그라운드 트랜잭션의 경우 간격의 두 배 값을 사용하십시오. 예를 들어 시간별 백그라운드 작업의 경우 제한 시간을 2시간으로 설정합니다.
  </Collapser>

  <Collapser id="inivar-daemon-app_connect_timeout" title="newrelic.daemon.app_connect_timeout">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            시간 지정 문자열; 예: `"1s"` 또는 `"5m"`
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"0"`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 애플리케이션을 연결하는 데몬을 기다려야 하는 최대 시간을 설정합니다. 값이 `"0"` 이면 에이전트가 데몬 연결을 한 번만 시도합니다. 허용되는 단위는 `"ns"` , `"us"` , `"ms"` , `"s"` , `"m"` 및 `"h"` 입니다.

    이 시간 제한을 설정하면 에이전트는 데몬이 아직 백엔드에 연결되지 않았을 때 트랜잭션을 즉시 삭제하지 않고 데몬에 연결을 설정할 시간을 부여합니다.

    권장 사항: 제한 시간을 설정하는 경우 권장 값은 `"15s"` 입니다. 연결 문제가 있는 경우에는 수용체가 모든 시작 시 지정된 시간 초과 동안 차단하므로, 수명이 긴 백그라운드 작업인 경우에만 이 시간 초과를 설정하세요.
  </Collapser>

  <Collapser id="inivar-daemon-auditlog" title="newrelic.daemon.auditlog">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 각 요청에 대한 전체 URL, 날짜, 시간 및 압축되지 않고 인코딩되지 않은 데이터를 포함하여 데몬에서 New Relic 서버로 보낸 모든 데이터를 포함하는 감사 로그의 이름을 지정합니다.

    이 파일은 데몬 로그와 같은 파일일 수 없습니다.

    <Callout variant="important">
      이 감사 로그는 매우 빠르게 커질 수 있습니다. 장기간 `newrelic.daemon.auditlog` 설정을 사용하지 마십시오. 주요 목적은 시스템 관리자가 보안 검토를 수행하고 데이터가 전송하는 내용을 정확히 관찰할 수 있도록 하는 것입니다.
    </Callout>
  </Collapser>

  <Collapser id="inivar-daemon-collector" title="newrelic.daemon.collector_host">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"collector.newrelic.com"`
          </td>
        </tr>
      </tbody>
    </table>

    데몬이 <InlinePopover type="licenseKey" />을 확인하고 에이전트 데이터를 보내기 위해 연결할 수집기 호스트의 이름과 선택적 포트를 설정합니다.

    이것은 호스트의 이름이거나 `"hostname:port"` 형식의 이름과 포트 번호입니다. 포트 번호를 `0` 로 지정하면 기본 포트인 80이 사용됩니다.

    <Callout variant="caution">
      New Relic Support의 안내 없이 이 값을 변경하지 마십시오.
    </Callout>
  </Collapser>

  <Collapser id="inivar-daemon-launch" title="newrelic.daemon.dont_launch">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            숫자
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `0`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 시작되기 전에 외부에서 데몬을 시작하려는 경우 이 값은 에이전트가 데몬을 시작할 수 있는 시기를 결정합니다.

    * `0`: 데몬 시작에 제한이 없으며 에이전트는 언제든지 데몬을 시작할 수 있습니다.

    * `1`: 명령줄이 아닌(예: Apache 또는 php-fpm) 에이전트는 데몬을 시작할 수 있습니다.

    * `2`: 명령줄 에이전트만 데몬을 시작할 수 있습니다.

    * `3`: 에이전트는 데몬을 시작하지 않습니다. `newrelic.cfg` 을 통해 데몬을 구성하고 에이전트 외부에서 시작하는 경우 이 설정을 사용합니다.

      <Callout variant="important">
        `/etc/newrelic/newrelic.cfg` 이 있는 경우 에이전트는 데몬이 에이전트를 통하지 않고 시작 스크립트에 의해 시작되어야 한다고 가정합니다. 이 설정은 `newrelic.cfg` 에 의미가 없으며 표시되지 않습니다.
      </Callout>
  </Collapser>

  <Collapser id="inivar-daemon-location" title="newrelic.daemon.location">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"/usr/bin/newrelic-daemon"`
          </td>
        </tr>
      </tbody>
    </table>

    실행할 데몬 실행 파일의 이름을 설정합니다.

    이 변수는 데몬 실행 파일의 전체 경로를 식별합니다. New Relic 시스템이 표준 패키지를 사용하여 설치된 경우 기본 위치가 정확합니다. tar 배포를 사용하여 사용자 지정 위치에 설치한 경우( `/usr/bin` 에 쓸 권한이 없음) 데몬이 다른 위치에 있을 수 있습니다.

    이 설정은 PHP에만 관련되므로 `newrelic.cfg` 파일에는 표시되지 않습니다.

    <Callout variant="important">
      OpenSolaris에서 `/usr` 은 종종 읽기 전용 파일 시스템입니다. OpenSolaris의 기본 데몬 위치는 `/opt/newrelic/bin/newrelic-daemon` 입니다.
    </Callout>
  </Collapser>

  <Collapser id="inivar-daemon-logfile" title="newrelic.daemon.log 파일">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"/var/log/newrelic/newrelic-daemon.log"`
          </td>
        </tr>
      </tbody>
    </table>

    데몬 특정 로그 메시지를 기록할 로그 파일의 이름을 설정합니다. 이 파일은 데몬이 쓸 수 있어야 합니다. 가장 일반적인 경우는 Apache 사용자가 Apache 자체가 시작될 때 데몬을 시작하게 되므로 Apache 프로세스를 실행하는 사용자가 파일에 쓸 수 있는지 확인해야 합니다.

    <Callout variant="caution">
      데몬 로그 파일에 쓸 수 없는 경우 데몬이 자동으로 시작되지 않아 데이터가 보고되지 않습니다.

      데몬을 시작할 수 있는 사람을 제한하는 수단으로 이 파일에 대한 쓰기 권한을 사용할 수 있습니다.
    </Callout>
  </Collapser>

  <Collapser id="inivar-daemon-loglevel" title="newrelic.daemon.loglevel">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"info"`
          </td>
        </tr>
      </tbody>
    </table>

    New Relic의 데몬 로그 파일에 기록되는 메시지의 상세 수준을 설정합니다. 가능한 값은 다음과 같습니다.

    * `error`

    * `warning`

    * `info`

    * `healthcheck`

    * `debug`

      더 자세한 설정은 많은 정보를 매우 빠르게 생성할 수 있습니다. `healthcheck` 연결된 애플리케이션의 정확한 수를 기록합니다. 필요한 경우 `debug` 짧은 시간으로 설정하여 문제를 식별합니다.
  </Collapser>

  <Collapser id="inivar-daemon-pidfile" title="newrelic.daemon.pid 파일">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            (OS에 따라 다름)
          </td>
        </tr>
      </tbody>
    </table>

    데몬이 프로세스 ID(pid)를 기록할 파일의 이름을 설정합니다.

    이 파일은 데몬이 이미 실행 중인지 여부를 결정하기 위해 데몬 시작 및 종료 스크립트에서 사용합니다.
  </Collapser>

  <Collapser id="inivar-daemon-port" title="newrelic.daemon.address(newrelic.daemon.port의 별칭)">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열 또는 숫자
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"@newrelic"` Linux의 경우 `"/tmp/.newrelic.sock"` 그렇지 않은 경우
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="tip">
      [v9.2.0.247](/docs/release-notes/agent-release-notes/php-release-notes/php-agent-920247)부터 PHP 에이전트는 `newrelic.daemon.port`에 대한 별칭 역할을 하는 새로운 설정 `newrelic.daemon.address`을 지원합니다. 둘 중 하나를 사용하여 New Relic PHP 데몬의 위치를 지정할 수 있습니다. 두 값이 모두 설정되면 `newrelic.daemon.address` 우선합니다.
    </Callout>

    에이전트 대 데몬 통신에 대한 소켓 끝점을 설정합니다.

    이는 4가지 방법으로 지정할 수 있습니다.

    * 지정된 파일을 UDS(UNIX 도메인 소켓)로 사용하려면 <DNT>**absolute**</DNT> 경로 이름을 문자열로 제공하십시오. 이는 Linux가 아닌 시스템에서는 기본값입니다.

    * 표준 TCP 포트를 사용하려면 1에서 65534 사이의 숫자를 지정하십시오.

    * 추상 소켓을 사용하려면 `@newrelic-daemon` 값을 사용하십시오(에이전트 버전 [5.2.0.141](/docs/release-notes/agent-release-notes/php-release-notes/php-agent-520141) 이상에서 사용 가능). 이것은 Linux 시스템의 기본값입니다.

    * 다른 호스트에서 실행 중인 데몬에 연결하려면( [컨테이너 환경](/docs/agents/php-agent/advanced-installation/install-php-agent-docker) 에 유용) 이 값을 `host:port` 으로 설정합니다. 여기서 `host` 은 호스트 이름 또는 IP를 나타내고 `port` 는 유효한 포트 번호를 나타냅니다. IPv4와 IPv6이 모두 지원됩니다. 에이전트 버전 9.2.0.247 이상에서 사용할 수 있습니다.

      <Callout variant="caution">
        에이전트에서 데몬으로 전송되는 데이터는 암호화 **되지 않습니다** . 이에 대한 유일한 예외는 데몬에 데이터를 보내기 전에 발생하는 [SQL 난독화](/docs/agents/php-agent/configuration/php-agent-configuration#inivar-tt-sql) 입니다. 에이전트와 데몬 간의 사설 네트워크 연결만 사용하는 것이 좋습니다(에이전트와 데몬이 서로 다른 호스트에서 실행 중인 경우에만 적용됨).
      </Callout>

      포트 번호를 사용하는 경우 Unix 시스템에서는 1에서 1023 사이의 포트에서 수퍼유저로 데몬을 실행해야 합니다. 데몬이 비표준 포트를 사용하는 경우 이 변수는 에이전트가 데몬과 통신하는 데 사용할 포트 번호도 설정합니다.

      <Callout variant="important">
        데몬에 대해 `newrelic.cfg` 시작 메커니즘을 사용하는 경우 이 설정과 해당 파일의 [`address`](/docs/agents/php-agent/configuration/proxy-daemon-newreliccfg-settings#cfgvar-port) 설정이 일치해야 합니다.
      </Callout>
  </Collapser>

  <Collapser id="inivar-daemon-proxy" title="newrelic.daemon.proxy">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    송신 프록시로 사용할 호스트 및 사용자 자격 증명을 설정합니다.

    이것은 사이트에서 New Relic 데이터 수집 서버에 액세스하기 위해 프록시가 필요한 경우에만 사용됩니다. 프록시 설정에 따라 다음 형식 중 하나를 사용할 수 있습니다.

    * `hostname`

    * `hostname:port`

    * `user@hostname`

    * `user@hostname:port`

    * `user:password@hostname`

    * `user:password@hostname:port`

    * `proxytype://user:password@hostname:port`

      다른 프록시 유형이 지정되지 않는 한 HTTP의 프록시 유형이 가정됩니다. 이 경우 프록시 유형은 http, socks4, socks4a 또는 socks5 중 하나일 수 있습니다.
  </Collapser>

  <Collapser id="inivar-daemon-ssl" title="newrelic.daemon.ssl">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 PHP 에이전트 버전 8.0 이상에서 무시되며 HTTPS는 항상 New Relic과 통신하는 데 사용됩니다.

    PHP 에이전트 버전 8.0 미만에서는 New Relic 수집기와 통신할 때 보안 HTTP 연결을 사용해야 하는지 여부를 제어합니다. 에이전트는 기본적으로 HTTPS를 통해 New Relic과 통신하며 New Relic은 APM 및 New Relic REST API에 대한 모든 트래픽에 대해 [HTTPS가 필요합니다](/docs/apis/rest-api-v2/troubleshooting/301-response-rest-api-calls) .
  </Collapser>

  <Collapser id="inivar-daemon-ssl-ca-bundle" title="newrelic.daemon.ssl_ca_bundle">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    PEM 형식의 CA 인증서가 포함된 파일의 위치를 설정합니다. 설정하면 이 파일의 인증서가 New Relic 수집기를 인증하는 데 사용됩니다. 대부분의 경우 CA 번들을 구성할 필요가 없습니다. New Relic PHP 에이전트는 필요한 CA 인증서와 함께 번들로 제공됩니다.

    [`newrelic.daemon.ssl_ca_path`](#inivar-daemon-ssl-ca-path) 도 설정되어 있으면 이 파일의 인증서가 먼저 검색되고 `newrelic.daemon.ssl_ca_path` 디렉토리에 포함된 인증서가 그 다음에 검색됩니다.

    이 설정은 [`newrelic.daemon.ssl`](#inivar-daemon-ssl) 이 `false` 로 설정된 경우에는 적용되지 않습니다.
  </Collapser>

  <Collapser id="inivar-daemon-ssl-ca-path" title="newrelic.daemon.ssl_ca_path">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    PEM 형식의 신뢰할 수 있는 CA 인증서가 포함된 디렉터리의 위치를 설정합니다. 설정하면 이 디렉토리의 인증서가 New Relic 수집기를 인증하는 데 사용됩니다. 대부분의 경우 CA 경로를 구성할 필요가 없습니다. New Relic PHP 에이전트는 필요한 CA 인증서와 함께 번들로 제공됩니다.

    [`newrelic.daemon.ssl_ca_bundle`](#inivar-daemon-ssl-ca-bundle) 도 설정되어 있으면 먼저 검색한 다음 `newrelic.daemon.ssl_ca_path` 에 포함된 인증서를 검색합니다.

    이 설정은 [`newrelic.daemon.ssl`](#inivar-daemon-ssl) 이 `false` 로 설정된 경우에는 적용되지 않습니다.
  </Collapser>

  <Collapser id="inivar-daemon-start_timeout" title="newrelic.daemon.start_timeout">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            시간 지정 문자열; 예: `"1s"` 또는 `"5m"`
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"0"`
          </td>
        </tr>
      </tbody>
    </table>

    데몬 실행이 트리거된 후 에이전트가 데몬이 시작될 때까지 기다려야 하는 최대 시간을 설정합니다. 값이 `"0"` 이면 에이전트가 기다리지 않습니다. 허용되는 단위는 `"ns"` , `"us"` , `"ms"` , `"s"` , `"m"` 및 `"h"` 입니다.

    지정된 시간 초과 값은 `--wait-for-port` 플래그를 통해 데몬에 전달됩니다. 이로 인해 소켓을 획득하거나 시간 초과가 경과할 때까지 데몬 시작이 차단됩니다.

    권장 사항: 제한 시간을 설정하는 경우 권장 값은 `"2s"` - `"5s"` 입니다. 데몬 시작 문제의 경우 에이전트는 모든 시작 시 지정된 시간 초과 동안 차단하므로, 수명이 긴 백그라운드 작업인 경우에만 이 시간 초과를 설정하는 것이 좋습니다.
  </Collapser>
</CollapserGroup>

## 컨텍스트의 로그인 [#logs-in-context]

PHP 에이전트 버전 10.1.0 [컨텍스트에서 APM 로그](/docs/apm/new-relic-apm/getting-started/get-started-logs-context) 와 함께 PHP 로그를 전달할 수 있습니다. 버전 10.3.0부터 로깅 지표 및 에이전트 로그 전달 기능이 기본적으로 활성화됩니다. `newrelic.application_logging.enabled` 값은 컨텍스트 기능의 로그가 활성인지 비활성인지 여부를 제어합니다.

로컬 에이전트 구성 파일(`newrelic.ini`)의 설정을 변경하려면 웹 서버를 다시 시작해야 적용됩니다. 그렇지 않으면 변경 사항이 [즉시 적용되지](/docs/agents/php-agent/troubleshooting/ini-settings-not-taking-effect-immediately) 않을 수 있습니다.

<CollapserGroup>
  <Collapser id="cfg-application_logging_forwarding-enabled" title="newrelic.application_logging.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    * 컨텍스트 기능의 핵심 로그를 활성화하려면 `true` 으로 설정합니다. 활성화되면 로그가 New Relic으로 전송되는지 여부에 관계없이 추가 로깅 프레임워크 계측이 활성화됩니다.
    * 로그 측정항목 수집 및 로그 전달을 포함하여 이 기능을 완전히 비활성화하려면 `false` 으로 설정합니다.
    * 이제 컨텍스트 내 로그인이 기본적으로 활성화됩니다. 이 기능을 비활성화한 경우 앱을 다시 시작해야 합니다. 그렇지 않으면 변경 사항이 즉시 적용되지 않습니다.

    예시 구성 파일:

    ```ini
    newrelic.application_logging.enabled = true
    newrelic.application_logging.metrics.enabled = true
    newrelic.application_logging.forwarding.enabled = true
    ```
  </Collapser>
</CollapserGroup>

### 로그 전달 [#log-forwarding]

[지원되는 로깅 프레임워크를](/docs/logs/logs-context/configure-logs-context-php) 사용하는 경우 에이전트에 앱 로그를 New Relic으로 전달하도록 지시할 수 있습니다.

* `newrelic.application_logging.forwarding.enabled` 로그인 포워딩을 활성화 또는 비활성화합니다.
* `newrelic.application_logging.forwarding.max_samples_stored` 앱이 뉴렐릭에 전달하는 쿠키 수를 제한합니다.
* `newrelic.application_logging.forwarding.log_level` 앱이 어떤 종류의 로그를 뉴럴릭에 전달하는지 선택할 수 있습니다.
* `newrelic.application_logging.forwarding.labels.enabled` 에이전트가 전달한 로그 메시지와 함께 사용자 정의 태그(라벨)를 보내는 것을 활성화하거나 비활성화합니다.
* `newrelic.application_logging.forwarding.labels.exclude` 에이전트가 전달하는 로그 메시지와 함께 전송할 라벨을 선택할 수 있습니다.

<CollapserGroup>
  <Collapser id="cfg-application_logging_forwarding-enabled" title="newrelic.application_logging.forwarding.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    New Relic에 애플리케이션 로그 전송을 활성화합니다.

    * 로그를 New Relic으로 전송하려면 이를 `true` 로 설정합니다. 이는 컨텍스트 로그인 기능이 작동할 수 있도록 하는 연결 메타데이터를 사용하여 PHP 에이전트에서 직접 로그를 전달합니다. PHP 에이전트는 애플리케이션 로그 출력에 메타데이터를 기록하지 않습니다.

    * 로그를 New Relic으로 전송하지 않으려면 이 값을 `false` 로 설정하세요. `enabled` `false` 로 설정되면 로그 데이터가 New Relic으로 전송되지 않으며 애플리케이션 로그 출력이 변경되지 않습니다.

      구성 파일의 예( `newrelic.ini` ):

      ```ini
      newrelic.application_logging.enabled = true
      ```

      기존 로그 포워딩 솔루션이 있고 컨텍스트에서 자동 로그인을 사용하도록 에이전트를 업데이트하는 경우 <DNT>**disable your old log forwarder**</DNT> 를 확인하세요. 그렇지 않으면 앱이 이중 로그 라인을 전송하게 됩니다. 귀하의 계정에 따라 이중 청구가 발생할 수 있습니다. 자세한 내용을 보려면 [특정 로그 전달자를](/docs/logs/forward-logs/enable-log-management-new-relic#log-forwarding) 비활성화하는 절차를 따르십시오.

      <Callout variant="important">
        로그 전달 기능을 사용하면 데이터 수집이 증가하여 청구에 영향을 미칠 수 있습니다. 자세한 내용은 [데이터 수집 추적](/docs/apm/new-relic-apm/getting-started/get-started-logs-context#ingest) 에 대한 설명서를 참조하세요.
      </Callout>
  </Collapser>

  <Collapser id="cfg-application_logging_forwarding-max_samples_stored" title="newrelic.application_logging.forwarding.max_samples_stored">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `10000`
          </td>
        </tr>

        <tr>
          <th>
            최대:
          </th>

          <td>
            `20000`
          </td>
        </tr>
      </tbody>
    </table>

    * 보낼 분당 최대 로그 줄 수를 지정하려면 값을 양의 정수로 설정하세요.

    * 로그 전송을 효과적으로 비활성화하려면 이 값을 `0` 으로 설정합니다.

    * 전송되는 로그 줄의 양을 줄이려면 이 값을 더 낮은 값으로 설정하십시오. 이로 인해 로그 샘플링이 발생할 수 있습니다.

    * 더 많은 로그 줄을 보내려면 이 값을 더 높게 설정하세요.

      각 로그는 연결된 트랜잭션과 동일한 우선 순위를 받습니다. 일부 로그는 `max_samples_stored` 에 의해 제한되기 때문에 포함되지 않을 수 있습니다. 예를 들어, 로깅 `max_samples_stored` 이 10,000으로 설정되고 트랜잭션 1에 10,000개의 로그 항목이 있는 경우 트랜잭션 1에 대한 로그 항목만 기록됩니다. 트랜잭션 1에 10,000개 미만의 로그가 있는 경우 트랜잭션 1에 대한 모든 로그를 수신합니다. 여전히 공간이 있으면 트랜잭션 2에 대한 모든 로그를 받는 식입니다. 우선 순위가 높은 로그 항목이 우선 순위가 낮은 로그 항목보다 유지됩니다.

    <Callout variant="tip">
      이는 *분당* 전송되는 로그 수를 나타냅니다. `max_samples_stored` 12보다 작은 값으로 설정하면 로그가 전송되지 않는 효과가 있습니다. 음수가 아닌 모든 정수가 유효하지만 현재 지원되는 최대 값은 20,000입니다.
    </Callout>

    구성 파일의 예( `newrelic.ini` ):

    ```ini
    newrelic.application_logging.forwarding.max_samples_stored = 10000
    ```
  </Collapser>

  <Collapser id="cfg-application_logging_forwarding-log-level" title="newrelic.application_logging.forwarding.log_level">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"WARNING"`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 전달하는 메시지의 로그 수준을 제어할 수 있습니다. 인식되는 로그 수준은 [PSR-3](https://www.php-fig.org/psr/psr-3/) 사양을 따르며 다음과 같습니다(가장 높은 심각도에서 가장 낮은 심각도로).

    * `EMERGENCY`
    * `ALERT`
    * `CRITICAL`
    * `ERROR`
    * `WARNING`
    * `NOTICE`
    * `INFO`
    * `DEBUG`

    에이전트에 대한 로그 수준이 지정되면 이 로그 수준 이상의 모든 메시지가 보고됩니다. 예를 들어 로그 수준을 `ALERT` 으로 설정하면 `ALERT` 및 `EMERGENCY` 심각도에 대한 로그를 수신하게 됩니다. 기본 로그 수준은 `WARNING` 입니다.

    구성 파일의 예( `newrelic.ini` ):

    ```ini
    newrelic.application_logging.forwarding.log_level = "INFO"
    ```
  </Collapser>

  <Collapser id="cfg-application_logging_forwarding_labels-enabled" title="newrelic.application_logging.forwarding.labels.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 전달한 로그가 포함된 사용자 정의 태그(레이블)를 뉴렐릭으로 보낼 수 있습니다.

    이 기능은 PHP 에이전트의 11.7.0 릴리스에서 추가되었습니다.

    태그는 속성 이름 앞에 접두사 `tags.` 이 삽입된 속성으로 로그 이벤트에 표시됩니다.

    구성 파일의 예( `newrelic.ini` ):

    ```ini
    newrelic.application_logging.forwarding.labels.enabled = true
    ```
  </Collapser>

  <Collapser id="cfg-application_logging_forwarding_labels-exclude" title="newrelic.application_logging.forwarding.labels.exclude">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열 목록(쉼표로 구분)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    에이전트 전달 로그와 함께 전송되는 사용자 정의 태그(레이블)를 필터링할 수 있습니다.

    값은 쉼표로 구분된 문자열 목록입니다. 목록에 있는 각 문자열은 대소문자를 구분하지 않고 현재 정의된 레이블과 일치하며, 전달되는 레이블에서 정확히 일치하는 항목은 제거됩니다.

    예를 들어, 다음과 같은 라벨 정의를 고려해 보겠습니다.

    ```ini
    newrelic.labels = "Data Center, Business, Cost code"
    ```

    다음 제외 규칙은 대소문자를 구분하지 않고 비교를 수행하기 때문에 `Business` 및 `Cost code` 레이블을 필터링합니다.

    ```ini
    newrelic.application_logging.forwarding.labels.exclude = business, Cost Code
    ```

    <Callout variant="tip">
      `newrelic.labels` INI 값을 사용하면 쉼표(,) 문자를 포함하는 레이블을 정의할 수 있습니다. 이러한 레이블은 제외 규칙을 구분하기 위해 쉼표에 의존하므로 `exclude` INI 값을 사용하여 필터링할 수 없습니다. 로그 메시지에 레이블이 추가되는 것을 제외하려면 레이블 값에 쉼표 문자를 사용하지 않는 것이 좋습니다.
    </Callout>
  </Collapser>
</CollapserGroup>

#### 로그 컨텍스트 데이터 [#log-context-data]

PHP 에이전트는 Monolog 라이브러리에 대한 컨텍스트 데이터를 캡처하고 해당 컨텍스트를 전달된 로그에 속성으로 추가합니다. `forwarding` 섹션 내 `context_data` 섹션 아래 설정을 통해 에이전트가 추가하는 컨텍스트를 제어할 수 있습니다.

* `newrelic.application_logging.forwarding.context_data.enabled` 로그 속성에 Monolog의 컨텍스트 데이터를 추가합니다.
* `newrelic.application_logging.forwarding.context_data.include` 전달된 로그에서 어떤 속성 키가 발견되는지 정의합니다.
* `newrelic.application_logging.forwarding.context_data.exclude` 전달된 로그에서 제외되는 속성 키를 정의합니다.

<CollapserGroup>
  <Collapser id="cfg-application_logging_forwarding_context_data-enabled" title="newrelic.application_logging.forwarding.context_data.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            유형
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본값
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    * 컨텍스트 데이터를 로그 속성으로 추가하려면 이를 `true` 로 설정합니다.
    * 컨텍스트 데이터를 로그에 속성으로 추가하지 않으려면 이 값을 `false` 로 설정하세요.
  </Collapser>

  <Collapser id="cfg-application_logging_forwarding_context_data-include" title="newrelic.application_logging.forwarding.context_data.include">
    <table>
      <tbody>
        <tr>
          <th>
            유형
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본값
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    로그 컨텍스트 속성에 속성 규칙을 적용하면 `include` 규칙은 규칙 세트에 정의되지 않은 모든 속성을 삭제합니다. 이는 다른 유형의 상담원 속성과는 다른 동작입니다.

    * 로그 컨텍스트 속성이 예상대로 표시되지 않으면 `newrelic.attributes.include` 규칙 세트를 검토하여 누락된 속성이 제외 항목의 일부인지 확인해야 합니다. 에이전트의 자세한 로그 메시지도 활성화하려면 `newrelic.loglevel=verbosedebug` 설정하는 것이 좋습니다. 업데이트되면 제외된 속성에 대한 에이전트의 메시지를 찾아보세요. 이를 통해 포함/제외 규칙을 조정하는 방법에 대한 단서를 얻을 수 있습니다.
    * 로그 전달을 위해 `context_data` 을 활성화하면 이 목록에 있는 모든 속성 키가 로그 레코드에 나타납니다. 빈 목록은 모든 속성 키를 기본값으로 보냅니다.
    * 모든 `include` 목록이 비어 있으면 모든 로그 컨텍스트 속성이 전달됩니다.
  </Collapser>

  <Collapser id="cfg-application_logging_forwarding_context_data-exclude" title="newrelic.application_logging.forwarding.context_data.exclude">
    <table>
      <tbody>
        <tr>
          <th>
            유형
          </th>

          <td>
            문자열 목록
          </td>
        </tr>

        <tr>
          <th>
            기본값
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    * 로그 전달을 위해 `context_data` 활성화하면 이 목록에 있는 모든 속성 키가 로그 레코드에서 제외됩니다.
    * 이 필드를 비워 두면 기본 동작이 수행됩니다. [`include` 규칙이 존재하는](#cfg-application_logging_forwarding_context_data-include) 경우에도 속성이 제외될 수 있습니다.
    * `include` 및 `exclude` 목록은 [속성 활성화 및 비활성화](/docs/php/php-agent-enabling-and-disabling-attributes#cfg-attributes-enabled) 에 정의된 규칙을 따릅니다.
  </Collapser>
</CollapserGroup>

<Callout variant="important">
  PHP 에이전트는 문자열 키와 값(문자열 또는 스칼라(int, double, boolean))이 있는 로그 컨텍스트 데이터만 전달합니다.
</Callout>

### 통나무 장식 [#log-decoration]

PHP 에이전트는 또한 모노로그 로그 레코드에 연결 메타데이터를 수집하여 컨텍스트 내 로그가 타사 로그 전달자가 전달한 로그 데이터와 작동할 수 있도록 허용할 수 있습니다. 이 기능을 활성화하려면 `newrelic.application_logging.local_decorating.enable` 옵션을 사용하세요.

<CollapserGroup>
  <Collapser id="cfg-application_logging_local_decorating-enabled" title="newrelic.application_logging.local_corating.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    이를 통해 모노로그 로그 기록에 메타데이터 연결을 추가할 수 있습니다. 이 기능을 활성화하려면 이를 `true` 로 설정하세요. APM 로그 전달 기능을 사용하여 로그를 New Relic에 전달하는 경우 이 설정을 `false` 로 유지합니다.

    <Callout variant="important">
      로그 장식 기능은 PHP 에이전트 릴리스 10.13.0.1에 처음 나타납니다.

      로그 데코레이션 기능을 사용하려면 연결 메타데이터가 로그 메시지에 포함되어 있는지 확인하기 위한 추가 단계가 필요합니다. 이 기능 사용에 대한 자세한 내용은 (컨텍스트의 PHP 로그)\[/docs/logs/logs-context/configure-logs-context-php] 페이지를 참조하세요.

      로그 전달과 로그 꾸미기를 모두 활성화하면 로그가 New Relic으로 중복 전송될 가능성이 높습니다.
    </Callout>
  </Collapser>
</CollapserGroup>

### 로그 메트릭 [#log-metrics]

PHP 에이전트는 [지원되는 로깅 프레임워크](/docs/logs/logs-context/configure-logs-context-php) 에 대한 로그 이벤트와 관련된 메트릭을 수집할 수 있습니다. 이러한 측정항목의 생성은 `newrelic.application_logging.metrics.enable` 옵션으로 제어됩니다.

<CollapserGroup>
  <Collapser id="cfg-application_logging_metrics-enabled" title="newrelic.application_logging.metrics.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    APM <DNT>**Summary**</DNT> 페이지의 차트에 표시되는 로그와 로그 레벨에 대한 메트릭 정보의 수집을 활성화합니다.

    에이전트가 로그에 대한 메트릭 정보를 캡처할 수 있도록 하려면 이 설정을 true로 둡니다.

    이 기능을 비활성화하려면 이것을 `false` 으로 설정하십시오.

    <Callout variant="important">
      로그 메트릭 수집을 비활성화하면 요약 페이지의 로그 차트가 공백으로 표시됩니다.
    </Callout>
  </Collapser>
</CollapserGroup>

<Callout variant="important">
  기존 로그 포워딩 솔루션이 있고 컨텍스트에서 자동 로그인을 사용하도록 에이전트를 업데이트하는 경우 <DNT>**disable your manual log forwarder**</DNT> 를 확인하세요. 그렇지 않으면 앱이 두 배의 로그 데이터를 전송하게 됩니다. 귀하의 계정에 따라 이중 청구가 발생할 수 있습니다. 자세한 내용을 보려면 [특정 로그 전달자를](/docs/logs/forward-logs/enable-log-management-new-relic#log-forwarding) 비활성화하는 절차를 따르십시오.
</Callout>

## 트랜잭션 추적기 .ini 설정 [#inivar-tt-settings]

이러한 설정의 값은 트랜잭션 추적을 제어하는 데 사용됩니다.

<CollapserGroup>
  <Collapser id="inivar-tt-custom" title="newrelic.transaction_tracer.custom">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    계측할 사용자 정의 함수 또는 메서드의 쉼표로 구분된 목록으로 설정합니다.

    ```
    "myfunction,myclass::method,otherfunction"
    ```

    내부 PHP 함수는 사용자 정의 추적을 가질 수 없습니다.

    각 PERDIR 파일과 함께 사용자 정의 함수 목록에 추가할 수 있지만 이미 추가된 함수나 메소드는 제거할 수 없습니다. 계측할 함수 또는 메서드를 프로그래밍 방식으로 추가하려면 [`newrelic_add_custom_tracer()`](/docs/agents/php-agent/php-agent-api/newrelic_add_custom_tracer) API 함수 호출을 사용합니다.
  </Collapser>

  <Collapser id="inivar-tt-enable" title="newrelic.transaction_tracer.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    트랜잭션 추적기를 활성화하거나 비활성화합니다.

    추적 프로그램은 `newrelic.transaction_tracer.threshold` 값으로 설정된 임계값보다 더 많은 시간이 소요되는 트랜잭션에 대한 자세한 트랜잭션 추적을 생성합니다. 수확 주기당 애플리케이션당 하나의 트랜잭션 추적만 저장되며 해당 주기 동안 항상 가장 느린 트랜잭션입니다.

    [트랜잭션 추적](/docs/apm/transactions/transaction-traces/transaction-traces) 은 특정 트랜잭션이 느린 이유를 진단하는 데 유용합니다. 메모리와 성능이 중요한 경우가 아니면 거의 비활성화되지 않아야 합니다. 이것을 활성화하면 성능에 약간의 영향을 미치고 더 많은 메모리를 사용합니다.

    자세한 내용은 [`newrelic.transaction_tracer.detail`](#inivar-tt-detail) 을(를) 참조하세요.
  </Collapser>

  <Collapser id="inivar-tt-expenable" title="newrelic.transaction_tracer.explain_enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    느린 쿼리 호출을 위해 MySQLi 및 PDO MySQL 라이브러리에서 계획 설명 요청을 활성화하거나 비활성화합니다. 계획 설명은 [`newrelic.transaction_tracer.slow_sql`](#inivar-tt-slowsql) 이 `true` 으로 설정되고 [`newrelic.transaction_tracer.record_sql`](#inivar-tt-sql) 가 `"off"` 으로 설정 **되지 않은** 경우에만 사용할 수 있습니다.
  </Collapser>

  <Collapser id="inivar-tt-epthreshold" title="newrelic.transaction_tracer.explain_threshold">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            지속 기간
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"500"`
          </td>
        </tr>
      </tbody>
    </table>

    &quot;느린&quot; 것으로 간주되어 [<DNT>**Slow Queries**</DNT>](/docs/apm/applications-menu/monitoring/databases-slow-queries-dashboard) 페이지의 후보가 되는 쿼리 이외의 청년 값을 설정합니다. 단위를 사용하여 기간을 절대값으로 지정합니다. 아무것도 지정되지 않은 경우 기본 단위는 밀리초입니다.

    <DNT>
      **Example durations**
    </DNT>

    * `"200ms"`
    * `"1s250ms"`
    * `"1h30m"`
    * `"750us"`
  </Collapser>

  <Collapser id="inivar-tt-internalfun" title="newrelic.transaction_tracer.internal_functions_enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    내부 함수(즉, C로 작성되고 PHP 표준 라이브러리 또는 PECL 확장을 통해 제공되는 함수) 추적 지원을 활성화하거나 비활성화합니다. 활성화되면 내부 함수가 PHP로 작성된 함수와 같은 [트랜잭션 추적](/docs/apm/transactions/transaction-traces/introduction-transaction-traces) 에 나타납니다.

    이 옵션을 활성화하면 특히 PHP 5.x에서 많은 추적을 수집할 때 트랜잭션이 느려질 수 있습니다. 이 옵션을 활성화하는 것은 내부 기능이 느린 것으로 의심되는 성능 문제를 구체적으로 디버깅하는 경우에만 권장됩니다.
  </Collapser>

  <Collapser id="inivar-tt-sql" title="newrelic.transaction_tracer.record_sql">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"obfuscated"`
          </td>
        </tr>
      </tbody>
    </table>

    쿼리가 기록되는 방법을 설정합니다(있는 경우).

    내부적으로 트랜잭션 추적을 기록할 때 느린 쿼리 호출에 대한 전체 쿼리가 기록됩니다. 이 쿼리를 New Relic에 보내는 것은 보안에 심각한 영향을 미칠 수 있으므로 기본적으로 모든 데이터베이스 문은 New Relic으로 보내기 전에 난독화됩니다. 난독화는 모든 쿼리의 문자열과 숫자를 물음표로 바꾸므로 되돌릴 수 없습니다. 손상되지 않은 상태로 남아 있는 유일한 것은 해당 값이 아니라 필드 이름입니다.

    * 민감하지 않은 데이터로 테스트하고 트랜잭션 추적에서 원시 쿼리를 확인해야 하는 경우 이 값을 `"raw"` 으로 설정합니다.

    * 쿼리를 전혀 기록하지 않으려면 이것을 `"off"` 으로 설정하십시오.

      <Callout variant="important">
        모든 프로덕션 환경에서 항상 이 값을 `"obfuscated"` 또는 `"off"` 로 다시 설정합니다.
      </Callout>
  </Collapser>

  <Collapser id="inivar-tt-slowsql" title="newrelic.transaction_tracer.slow_sql">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정을 사용하여 느린 쿼리 기능을 활성화하거나 비활성화합니다. 활성화되면 다음이 수행됩니다.

    * 분당 애플리케이션당 가장 느린 데이터베이스 쿼리 상위 10개를 기록합니다.

    * 쿼리로 이어지는 스택 추적을 만듭니다.

      이 변수가 `false` 으로 설정되거나 SQL 기록이 비활성화된 경우 New Relic 에이전트는 느린 쿼리 데이터를 수집하지 않습니다.
  </Collapser>

  <Collapser id="inivar-tt-stthreshold" title="newrelic.transaction_tracer.stack_trace_threshold">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            지속 기간
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"500"`
          </td>
        </tr>
      </tbody>
    </table>

    이것은 에이전트가 쿼리가 기록되는 트랜잭션 추적에 대한 전체 PHP 스택을 기록하는 임계값을 설정합니다.

    기간을 단위가 있는 절대값으로 지정하십시오. 지정되지 않은 경우 기본 단위는 밀리초입니다.

    <DNT>
      **Example durations**
    </DNT>

    * `"200ms"`

    * `"1s250ms"`

    * `"1h30m"`

    * `"750us"`

      스택 추적은 메모리를 소모할 수 있으므로 이 값을 너무 낮게 설정하지 않도록 주의하십시오. 이 변수는 [`newrelic.transaction_tracer.record_sql`](#inivar-tt-sql) 이 `"off"` 로 설정되어 있으면 의미가 없습니다.
  </Collapser>

  <Collapser id="inivar-tt-threshold" title="newrelic.transaction_tracer.threshold">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            지속 기간
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"apdex_f"`
          </td>
        </tr>
      </tbody>
    </table>

    이 변수를 사용하여 트랜잭션을 추적할 수 있는 임계값을 초과하여 설정하십시오. 사용 가능한 값은 다음과 같습니다.

    * `"apdex_f"` 을 사용하여 임계값을 애플리케이션의 [apdex\_t](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#apdex_t) 값의 4배로 설정합니다. 자세한 내용은 [Apdex: 사용자 만족도 측정](/docs/apm/new-relic-apm/apdex/apdex-measuring-user-satisfaction) 을 참조하십시오.

    * 특정 시간 임계값을 설정하려면 다른 기간 문자열을 사용하십시오. 기간을 절대값과 측정 단위로 지정합니다. 아무 것도 지정하지 않으면 단위는 기본적으로 밀리초로 설정됩니다.

      <DNT>
        **Example durations**
      </DNT>

    * `"200ms"`

    * `"1s250ms"`

    * `"1h30m"`

    * `"750us"`
  </Collapser>

  <Collapser id="inivar-tt-gather-input-queries" title="newrelic.transaction_tracer.gather_input_queries">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    느린 쿼리 및 트랜잭션 추적이 있는 DQL 입력 쿼리 수집을 활성화하거나 비활성화하려면 이 설정을 사용합니다. 활성화되면 느린 쿼리 및 트랜잭션 추적과 함께 표시됩니다.
  </Collapser>

  <Collapser id="inivar-tt-maxsegweb" title="newrelic.transaction_tracer.max_segments_web">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            숫자
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `0`
          </td>
        </tr>
      </tbody>
    </table>

    PHP 에이전트가 웹 트랜잭션에 대해 기록할 최대 세그먼트 수를 지정합니다. 해당 최대값에 도달하면 샘플링이 발생합니다.

    PHP 에이전트는 트랜잭션 추적 및 분산 추적을 세그먼트 모음으로 보고합니다. 각 세그먼트는 트랜잭션 추적의 메서드 또는 함수 호출을 나타냅니다. 이 구성의 기본값은 `0` 이며, 이는 PHP 에이전트가 웹 트랜잭션 중에 모든 세그먼트를 캡처해야 함을 나타냅니다. 트랜잭션이 끝나면 트랜잭션 추적에서 보고할 가장 높은 우선 순위 세그먼트를 조합합니다.

    수십만 또는 수백만 개의 함수 호출이 있는 장기 실행 PHP 프로세스의 경우 이 값을 `1` 보다 큰 값으로 설정하면 PHP 에이전트가 세그먼트를 기록할 때 시스템 메모리를 소진하는 것을 방지할 수 있습니다.

    세그먼트 크기는 해당 메서드 이름의 길이, 클래스 이름의 길이, 메서드에서 수행한 후속 호출 수에 따라 달라질 수 있습니다. 즉, 보수적인 추정치는 세그먼트당 400바이트입니다. 세그먼트 캡처를 위해 PHP 에이전트를 40MB로 제한하려면 이 값을 `100000` 으로 설정합니다. 이 값이 `2000` 보다 낮게 설정되면 트랜잭션 추적에 대해 보고되는 총 세그먼트 수를 추가로 제한합니다.

    이 구성 설정은 PHP 웹 프로세스에만 적용됩니다. PHP CLI 프로세스에는 영향을 미치지 않습니다. CLI 프로세스에 대한 제한을 설정하려면 `newrelic.transaction_tracer.max_segments_cli` 을 사용합니다.
  </Collapser>

  <Collapser id="inivar-tt-maxsegcli" title="newrelic.transaction_tracer.max_segments_cli">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            숫자
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `100000`
          </td>
        </tr>
      </tbody>
    </table>

    PHP 에이전트가 CLI 트랜잭션에 대해 기록할 최대 세그먼트 수를 지정합니다. 해당 최대값에 도달하면 샘플링이 발생합니다.

    PHP 에이전트는 트랜잭션 추적 및 분산 추적을 세그먼트 모음으로 보고합니다. 각 세그먼트는 트랜잭션 추적의 메서드 또는 함수 호출을 나타냅니다. 이 구성의 기본값은 `100000` 이며, 이는 PHP 에이전트가 웹 트랜잭션 중에 모든 세그먼트를 캡처해야 함을 나타냅니다. 트랜잭션이 끝나면 트랜잭션 추적에서 보고할 가장 높은 우선 순위 세그먼트를 조합합니다.

    수십만 또는 수백만 개의 함수 호출이 있는 장기 실행 PHP 프로세스의 경우 이 값을 `1` 보다 큰 값으로 설정하면 PHP 에이전트가 세그먼트를 기록할 때 시스템 메모리를 소진하는 것을 방지할 수 있습니다.

    세그먼트 크기는 해당 메서드 이름의 길이, 클래스 이름의 길이, 메서드에서 수행한 후속 호출 수에 따라 달라질 수 있습니다. 즉, 보수적인 추정치는 세그먼트당 400바이트입니다. 세그먼트 캡처를 위해 PHP 에이전트를 40MB로 제한하려면 이 값을 `100000` 으로 설정합니다. 이 값이 `2000` 보다 낮게 설정되면 트랜잭션 추적에 대해 보고되는 총 세그먼트 수를 추가로 제한합니다.

    이 구성 설정은 PHP CLI 프로세스에만 적용됩니다. PHP 웹 프로세스에는 영향을 미치지 않습니다. 웹 프로세스에 대한 제한을 설정하려면 `newrelic.transaction_tracer.max_segments_web` 을 사용합니다.
  </Collapser>
</CollapserGroup>

## 기타 추적 프로그램 .ini 설정 [#inivar-misctt-settings]

이 설정의 값은 다양한 추적 기능을 제어하는 데 사용됩니다.

<CollapserGroup>
  <Collapser id="inivar-cross-app-tracer-enabled" title="newrelic.cross_application_tracer.enabled">
    <Callout variant="important">
      교차 응용 프로그램 추적은 더 이상 사용되지 않으며 PHP 에이전트 버전 9.21.0.311에서는 기본적으로 비활성화되었습니다.[교차 애플리케이션 추적](/docs/traces/cross-application-traces) 을 개선하는 [분산 추적](/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent) 기능을 권장합니다.
    </Callout>

    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    교차 애플리케이션 추적기를 활성화하거나 비활성화합니다. 크로스 애플리케이션 추적기는 웹 트랜잭션 메트릭과 애플리케이션 간의 트랜잭션 추적을 함께 연결하기 위해 HTTP 헤더를 아웃바운드 요청 및 응답에 삽입합니다.

    분산 추적과 교차 애플리케이션 추적은 동시에 사용할 수 없습니다. PHP 에이전트의 기본 구성은 분산 추적을 활성화하고 애플리케이션 간 추적을 비활성화합니다.
  </Collapser>

  <Collapser id="inivar-distributed-enabled" title="newrelic.distributed_tracing_enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    <Callout variant="important">
      분산 추적을 활성화하면 [애플리케이션 간](#inivar-cross-app-tracer-enabled) 추적이 비활성화됩니다. New Relic 기능에 대한 효과를 이해하려면 [전환 가이드](/docs/apm/distributed-tracing/getting-started/transition-guide-distributed-tracing) 를 읽으십시오.
    </Callout>

    [분산 추적을](/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent) 사용하거나 사용하지 않도록 설정합니다.PHP 에이전트 9.21.0 이상에서는 기본적으로 켜져 있습니다.에이전트의 트랜잭션 추적기 및 분산 추적 기능이 활성화되면 에이전트는 아웃바운드 요청에 헤더를 삽입하고 분산 추적 헤더에 대한 수신 요청을 검색합니다.

    ```ini
    newrelic.transaction_tracer.enabled = true
    newrelic.distributed_tracing_enabled = true
    ```

    자세한 내용 [은 PHP 서비스에 대한 분산 추적을](/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent) 참조하십시오.
  </Collapser>

  <Collapser id="inivar-dt-exclude-newrelic" title="newrelic.distributed_tracing_exclude_newrelic_header">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    아웃바운드 요청에 첨부된 New Relic 분산 추적 헤더를 제외하고 분산 추적을 위해 W3C 추적 컨텍스트 헤더에만 의존하려면 이것을 true로 설정하십시오. 이것이 거짓이면 두 가지 유형의 헤더가 아웃바운드 요청에 첨부됩니다.

    New Relic 분산 추적 헤더는 W3C 추적 컨텍스트 헤더를 지원하지 않는 이전 에이전트와의 상호 운용성을 허용합니다. W3C Trace Context 헤더를 지원하는 에이전트 버전은 분산 추적을 위해 New Relic 헤더보다 우선 순위가 높습니다.
  </Collapser>

  <Collapser id="inivar-sampler-remote-parent-sampled" title="newrelic.distributed_tracing.sampler.remote_parent_sampled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `default`
          </td>
        </tr>
      </tbody>
    </table>

    이 옵션은 업스트림 부분의 상위 스팬이 샘플링되었을 때 에이전트가 샘플링 범위를 처리하는 방법을 정의합니다. 예를 들어, remote\_parent\_sampled: always\_on을 설정하면 업스트림이 부모를 샘플링한 경우 에이전트가 모든 것을 샘플링합니다. 부모를 샘플링했습니다.

    가능한 값은 다음과 같습니다.

    * &quot;기본값&quot;: 뉴렐릭의 표준 샘플링 규칙을 사용합니다.
    * &quot;always\_on&quot;: 업스트림 부모가 샘플링된 스팬을 항상 샘플링합니다.
    * &quot;always\_off&quot;: 상위 부모가 샘플링된 스팬의 샘플링을 항상 건너뜁니다.
  </Collapser>

  <Collapser id="inivar-sampler-remote-parent-not-sampled" title="newrelic.distributed_tracing.sampler.remote_parent_not_sampled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `default`
          </td>
        </tr>
      </tbody>
    </table>

    이 옵션은 업스트림 부분의 상위 스팬이 샘플링되지 않았을 때 에이전트가 샘플링 범위를 처리하는 방법을 정의합니다. 예를 들어, remote\_parent\_not\_sampled: always\_off를 설정하면 업스트림이 부모를 샘플링하지 않으면 에이전트가 아무것도 샘플링하지 않습니다.

    가능한 값은 다음과 같습니다.

    * &quot;기본값&quot;: 뉴렐릭의 표준 샘플링 규칙을 사용합니다.
    * &quot;always\_on&quot;: 업스트림 부모가 샘플링되지 않은 스팬을 항상 샘플링합니다.
    * &quot;always\_off&quot;: 상위 부모가 샘플링되지 않은 스팬의 샘플링을 항상 건너뜁니다.
  </Collapser>

  <Collapser id="inivar-span-enabled" title="newrelic.span_events_enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [분산 추적](#inivar-distributed-enabled) 에 대해 스팬 이벤트가 보고됩니다.스팬 이벤트 보고를 사용하려면 [분산 추적](/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent) 을 활성화해야 합니다.이 옵션은 스팬 이벤트 보고를 활성화/비활성화합니다.
  </Collapser>

  <Collapser id="inivar-span-events-max-samples-stored" title="newrelic.span_events.max_samples_stored">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `0`
          </td>
        </tr>
      </tbody>
    </table>

    스팬 이벤트는 [분산 추적](#inivar-distributed-enabled)에 대해 보고됩니다. 이 값은 수집 주기당 수집기로 전송되는 범위 이벤트의 최대 수를 설정합니다. `0` 값은 에이전트 기본값 `2000`을 사용합니다. 최대 설정은 `10000`입니다.
  </Collapser>

  <Collapser id="inivar-inftr-host" title="newrelic.infinite_tracing.trace_observer.host">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    무한 추적에 사용되는 추적 관찰자를 구성합니다. 비어 있으면 무한 추적 지원이 비활성화됩니다. 이를 위해서는 [분산 추적](#inivar-distributed-enabled) 및 범위 이벤트를 활성화해야 합니다.
  </Collapser>

  <Collapser id="inivar-inftr-port" title="newrelic.infinite_tracing.trace_observer.port">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `443`
          </td>
        </tr>
      </tbody>
    </table>

    Infinite Tracing Trace Observer와 통신하는 데 사용되는 TCP/IP 포트를 구성합니다. `newrelic.infinite_tracing.trace_observer.host` 이 비어 있으면 이 설정이 무시됩니다. 이 설정은 일반적으로 변경할 필요가 없습니다.
  </Collapser>

  <Collapser id="inivar-inftr-queue" title="newrelic.infinite_tracing.span_events.queue_size">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            정수(1000 이상)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `100000`
          </td>
        </tr>
      </tbody>
    </table>

    Infinite Tracing Trace Observer로 전송하기 위해 대기할 수 있는 스팬 이벤트 수를 설정합니다. 에이전트는 무한 추적에 대한 스팬 이벤트를 스팬 일괄 처리로 내부적으로 관리합니다. 이러한 스팬 배치는 최대 1000개의 스팬을 보유할 수 있습니다. 따라서 스팬 이벤트 큐 크기는 `1000`보다 작을 수 없습니다. 그렇지 않으면 단일 스팬 배치도 큐에 넣을 수 없기 때문입니다. 1000보다 작은 대기열 크기를 지정하면 최소 크기 `1000` 사용됩니다.
  </Collapser>

  <Collapser id="inivar-err-enabled" title="newrelic.error_collector.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    오류 수집기를 활성화 또는 비활성화합니다. 활성화되면 해당 도구는 PHP 오류 및 예외를 수집하여 뉴럴릭 UI 에 보고합니다.

    ### PHP 에이전트가 오류 및 예외를 수집하는 방법

    PHP 에이전트는 다음과 같은 방식으로 오류 및 예외를 자동으로 수집합니다.

    * **포착되지 않은 예외**: 에이전트는 소니 실행 중에 발생하는 포착되지 않은 예외(PHP 5의 경우 `Exception` 의 인스턴스, PHP 7+의 경우 `Throwable`)를 자동으로 캡처합니다.
    * **PHP 오류**: 에이전트는 구성된 오류 수준(아래 [`error_collector.ignore_errors`](#inivar-err-ignore-errors) 참조)에 따라 PHP 오류를 캡처합니다.
    * **수동 오류 보고**: [`newrelic_notice_error()`](/docs/agents/php-agent/php-agent-api/newrelic_notice_error) API를 사용하여 처리된 예외 또는 사용자 지정 오류 조건과 같이 자동으로 캡처되지 않는 오류 또는 예외를 수동으로 보고합니다.

    에이전트는 각 트랜잭션에 대해 가장 심각한 오류 하나만 기록하고 [수집 주기](/docs/apm/new-relic-apm/getting-started/glossary#harvest-cycle) 당 최대 20개의 오류를 기록합니다.

    <Callout variant="tip">
      APM의 오류 구성에 대한 개요는 [APM에서 오류 관리](/docs/agents/manage-apm-agents/agent-data/manage-errors-apm-collect-ignore-mark-expected)를 참조하십시오.
    </Callout>
  </Collapser>

  <Collapser id="inivar-err-db" title="newrelic.error_collector.record_database_errors">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [활성화](#inivar-err-enabled) 되면 에이전트는 MySQL 함수에서 반환된 오류를 PHP 오류인 것처럼 수집합니다.
  </Collapser>

  <Collapser id="inivar-err-priority" title="newrelic.error_collector.prioritize_api_errors">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    [`newrelic_notice_error()`](/docs/agents/php-agent/php-agent-api/newrelic_notice_error) API 함수를 통해 식별된 오류에 가장 높은 우선순위를 할당하려면 이것을 `true` 으로 설정합니다.

    참고: 처리되지 않은 오류에는 더 높은 우선순위가 지정되므로 [`newrelic_notice_error()`](/docs/agents/php-agent/php-agent-api/newrelic_notice_error) API 함수를 통해 보고된 오류보다 우선합니다.
  </Collapser>

  <Collapser id="inivar-err-ignore-exceptions" title="newrelic.error_collector.ignore_exceptions">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    에이전트가 무시해야 하는 정규화된 예외 클래스 이름의 쉼표로 구분된 목록입니다. 처리되지 않은 예외가 발생하면 에이전트는 나열된 각 클래스에 대해 `$exception instanceof Class` 에 해당하는 작업을 수행합니다. 이러한 검사 중 하나라도 true를 반환하면 에이전트는 예외를 기록하지 않습니다.

    [지원되는 PHP 릴리스](/docs/agents/php-agent/getting-started/php-agent-compatibility-requirements#php-release) 에 따라 이러한 값은 <DNT>**disabling**</DNT> 예외 보고와 동일합니다.

    * 지원되는 PHP 5 버전: `newrelic.error_collector.ignore_exceptions = Exceptions`
    * 지원되는 PHP 7 버전: `newrelic.error_collector.ignore_exceptions = Throwable`
  </Collapser>

  <Collapser id="inivar-err-ignore-errors" title="newrelic.error_collector.ignore_errors">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            [PHP 정의 오류 상수](http://php.net/manual/en/errorfunc.constants.php) 의 정수 또는 비트 표현
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `0`
          </td>
        </tr>
      </tbody>
    </table>

    PHP 에이전트의 [오류 수준을](http://php.net/manual/en/errorfunc.constants.php) <DNT>**ignore**</DNT> 으로 설정합니다. 이 설정의 값은 [PHP의 `error_reporting` 옵션](http://php.net/manual/en/errorfunc.configuration.php#ini.error-reporting) 과 유사한 구문을 사용합니다. 예를 들어, `E_WARNING` 및 `E_ERROR` 수준 오류를 무시하도록 PHP 에이전트를 구성하려면 다음을 사용합니다.

    ```ini
    newrelic.error_collector.ignore_errors = E_WARNING | E_ERROR
    ```

    또는

    ```ini
    newrelic.error_collector.ignore_errors = 3
    ```

    `E_WARNING` 를 제외한 모든 것을 무시하도록 PHP 에이전트를 구성하려면 다음을 사용하세요.

    ```ini
    newrelic.error_collector.ignore_errors = E_ALL & ~E_WARNING
    ```
  </Collapser>

  <Collapser id="inivar-ignore-framework-exception-handler" title="newrelic.error_collector.ignore_framework_error_exception_handler">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    이 기능을 활성화하면, NEW렐릭 오류 수집기는 오류 또는 예외 처리기에서 처리하는 모든 오류나 예외를 무시합니다. 이 기능은 프레임워크에서 다시 throw되는 예외만 보려는 경우나 오류/예외를 필터링하고 Measuring, Measuring이 필요할 때 Newrellic API 수동으로 호출하는 예외 처리 대시보드에 고유한 처리 논리를 삽입하려는 경우에 유용합니다.
  </Collapser>

  <Collapser id="inivar-framework-drupal-modules" title="newrelic.framework.drupal.modules">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td>
            버전 3.0 이전의 에이전트에서 이 값의 기본값은 `false` 입니다. 이 옵션을 설정하면 `newrelic.transaction_tracer.detail = 1` 이(가) 암시됩니다.
          </td>
        </tr>
      </tbody>
    </table>

    Drupal 모듈 추적을 활성화하려면 이를 `true` 으로 설정하세요. 이 옵션은 [`newrelic.transaction_tracer.detail`](#inivar-tt-detail) 과 동일한 성능 영향을 미칩니다. Drupal 모듈 기능 및 호출 횟수를 기록할 수 있으며 웹 트랜잭션과 마찬가지로 <DNT>**Modules**</DNT> 탭 아래에 표시됩니다.
  </Collapser>

  <Collapser id="inivar-framework-wordpress-hooks" title="newrelic.framework.wordpress.hooks">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    WordPress 후크 추적을 활성화하려면 이를 `true` 으로 설정합니다. WordPress 후크, 플러그인, 테마 기록이 [<DNT>**WordPress**</DNT>](/docs/agents/php-agent/frameworks-libraries/wordpress-specific-functionality) UI활성화되며 뉴렐릭 의 탭에 표시됩니다.
  </Collapser>

  <Collapser id="inivar-framework-wordpress-hooks-options" title="newrelic.framework.wordpress.hooks.options">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"plugin_callbacks"`
          </td>
        </tr>
      </tbody>
    </table>

    WordPress 후크를 다운로드하는 방법에 대한 옵션을 설정합니다. 뉴렐릭 에이전트는 WordPress 후크에 다양한 수준의 인사이트를 제공할 수 있습니다. 기본적으로 플러그인/테마 터키백만 포함됩니다(&quot;plugin\_callbacks&quot;). 에이전트의 오버헤드가 증가하는 대신 계측을 모든 후크 콜백 함수(&quot;all\_callbacks&quot;)로 확장하는 것이 가능합니다. 3 番目のオプションは、コールバック (「threshold」) をインストルメントせずにフックを監視することです。이 옵션은 인사이트에 대한 내용/테마를 제공하지 않습니다. [여기에서](/docs/agents/php-agent/frameworks-libraries/wordpress-specific-functionality) WordPress 관련 계측에 대해 자세히 알아보세요.
  </Collapser>

  <Collapser id="inivar-framework-wordpress-hooks-threshold" title="newrelic.framework.wordpress.hooks.threshold">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            지속 기간
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `1ms`
          </td>
        </tr>
      </tbody>
    </table>

    New Relic 에이전트가 WordPress 후크 실행을 기록하는 임계값을 설정합니다. newrelic.framework.wordpress.hooks.options가 &quot;임계값&quot;으로 설정된 경우 사용됩니다.
  </Collapser>

  <Collapser id="inivar-wt-files" title="newrelic.webtransaction.name.files">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    파일 이름을 사용하여 웹 트랜잭션의 이름을 지정한다는 점을 제외하고는 [`newrelic.webtransaction.name.functions`](#inivar-wt-funcs) 과 동일합니다. 파일 이름은 PCRE와 함께 사용되는 표준 POSIX 정규식일 수 있습니다. 예: `"controllers/actions/.*"` .
  </Collapser>

  <Collapser id="inivar-wt-funcs" title="newrelic.webtransaction.name.functions">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>

    New Relic 에이전트가 Drupal이나 Wordpress와 같은 특정 프레임워크를 감지하지 않는 한 웹 트랜잭션은 초기 PHP 파일의 이름을 따서 명명됩니다. 예: `article.php` .

    초기 파일이 디스패처인 경우 이 명명 체계는 유용한 데이터보다 적게 생성합니다. 이 변수를 사용하여 디스패처에 의해 생성된 &quot;작업&quot;인 기능 목록을 지정합니다. 웹 트랜잭션의 이름은 실행되는 첫 번째 작업 기능이 됩니다.

    <DNT>
      **Example dispatch function naming**
    </DNT>

    `index.php` 이 이름이 지정된 함수에 전달하는 경우:

    * `login`,

    * `logout`,

    * `admin`,

    * `show`, 그리고

    * `edit`

      이 값을 `"login,logout,admin,show,edit"` 으로 설정합니다.

      웹 트랜잭션의 이름은 `/index.php` (초기 파일 이름) 대신 `login` , `logout` 등으로 지정됩니다.
  </Collapser>

  <Collapser id="inivar-wt-remove-path" title="newrelic.webtransaction.name.remove_trailing_path">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    활성화하면 스크립트 이름 뒤의 요청 URI에서 모든 콘텐츠가 제거됩니다. 예를 들어, 이것은 `/path/to/foo.php/xyz/zy` 에서 후미 `/xyz/zy` 을 제거합니다.
  </Collapser>

  <Collapser id="ini-var-dt-instance-reporting" title="newrelic.datastore_tracer.instance_reporting.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    `false` 인 경우 에이전트는 데이터 저장소 인스턴스 측정항목을 보고하지 않으며 `host` 또는 `port_path_or_id` 매개변수를 트랜잭션 또는 느린 SQL 추적에 추가하지 않습니다.
  </Collapser>

  <Collapser id="ini-var-dt-database-name-reporting" title="newrelic.datastore_tracer.database_name_reporting.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    `false` 인 경우 에이전트는 트랜잭션 또는 느린 SQL 추적에 `database_name` 매개변수를 추가하지 않습니다.
  </Collapser>
</CollapserGroup>

## 속성 설정 [#inivar-attribute-settings]

이 섹션에는 속성 수집 및 보고에 영향을 주는 설정이 나열되어 있습니다.

<CollapserGroup>
  <Collapser id="inivar-attributes-enabled" title="newrelic.attributes.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    PHP 에이전트가 생성하거나 [`newrelic_add_custom_parameter()`](/docs/agents/php-agent/php-agent-api/newrelic_add_custom_parameter)통해 사용자가 생성한 속성 수집을 활성화 또는 비활성화합니다. 이 설정은 다른 모든 특성 구성 설정보다 우선합니다.

    자세한 내용은 [속성 활성화 및 비활성화](/docs/php/php-agent-enabling-and-disabling-attributes#cfg-attributes-enabled) 를 참조하십시오.
  </Collapser>

  <Collapser id="inivar-attributes-enabled" title="newrelic.{목적지}.attributes.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`, 제외 `browser_monitoring.attributes.enabled`
          </td>
        </tr>
      </tbody>
    </table>

    속성을 수신할 대상을 제어합니다. 이러한 구성 설정은 `.include` 및 `.exclude` [설정](#inivar-attributes-include-exclude) 을 재정의합니다.

    사용 가능한 대상에 대한 자세한 내용은 [속성 활성화 및 비활성화](/docs/agents/php-agent/attributes/enabling-and-disabling-attributes#properties) 를 참조하십시오.
  </Collapser>

  <Collapser id="inivar-attributes-include-exclude" title="newrelic.attributes.{include|exclude}">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    이것은 속성의 대상을 미세하게 제어할 수 있는 구성 옵션 제품군입니다. 자세한 내용은 [속성 활성화 및 비활성화](/docs/php/php-agent-enabling-and-disabling-attributes#cfg-attributes-include) 를 참조하십시오.
  </Collapser>
</CollapserGroup>

## 맞춤 이벤트 [#inivar-custom-events]

이 섹션에는 맞춤 이벤트 보고에 영향을 미치는 설정이 나열되어 있습니다.

<CollapserGroup>
  <Collapser id="inivar-custom-events-enabled" title="newrelic.custom_insights_events.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [`newrelic_record_custom_event()`](/docs/agents/php-agent/php-agent-api/newrelic_record_custom_event)통해 사용자가 생성한 이벤트 수집을 활성화 또는 비활성화합니다. 이 설정은 다른 모든 특성 구성 설정보다 우선합니다.

    자세한 내용은 [사용자 정의 이벤트 및 속성 보고를](/docs/data-apis/custom-data/custom-events/apm-report-custom-events-attributes/#php-att) 참조하십시오.
  </Collapser>

  <Collapser id="inivar-custom-events-maxsamples" title="newrelic.custom_events.max_samples_stored">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `30000`
          </td>
        </tr>

        <tr>
          <th>
            최대:
          </th>

          <td>
            `100000`
          </td>
        </tr>
      </tbody>
    </table>

    전송할 분당 사용자 지정 이벤트의 최대 수를 지정하는 양의 정수입니다.

    이를 `0` 로 설정하면 맞춤 이벤트 전송이 효과적으로 비활성화됩니다. [`newrelic.custom_insights_events.enabled`](#inivar-custom-events-enabled) 설정은 사용자 정의 이벤트 생성을 활성화 또는 비활성화하는 기본 방법입니다.

    전송되는 사용자 정의 이벤트의 양을 줄이려면 이 값을 더 낮은 값으로 설정하십시오(사용자 정의 이벤트 샘플링이 발생할 수 있음). 더 많은 사용자 지정 이벤트 라인을 보내려면 이 값을 더 높게 설정하십시오.

    <Callout variant="important">
      `newrelic.custom_events.max_samples_stored` INI 변수는 10.4.0.316 릴리스에 추가되었습니다.
    </Callout>
  </Collapser>
</CollapserGroup>

## 코드 수준 측정항목 [#inivar-code-level-metrics]

이 섹션에는 코드 수준 메트릭의 보고에 영향을 미치는 설정이 나열되어 있습니다.

<CollapserGroup>
  <Collapser id="inivar-code-level-metrics-enabled" title="newrelic.code_level_metrics.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>

    코드 수준 메트릭 수집을 활성화하거나 비활성화합니다.

    <Callout variant="important">
      `newrelic.code_level_metrics.enabled` INI 변수는 10.4.0.316 릴리스에서 추가되었으며 기본적으로 비활성화되었습니다. 코드 레벨 지표는 버전 10.6.0 - 11.10.0에서 기본적으로 활성화됩니다. 코드 레벨 지표는 버전 12.0.0부터 기본적으로 비활성화되어 있습니다.
    </Callout>
  </Collapser>

  <Collapser id="NEW_RELIC_METADATA_COMMIT" title="NEW_RELIC_METADATA_COMMIT">
    <Callout variant="important">
      이것은 환경 변수를 통해서만 설정할 수 있습니다.
    </Callout>

    <table>
      <tbody>
        <tr>
          <th>
            유형
          </th>

          <td>
            정수
          </td>
        </tr>

        <tr>
          <th>
            기본값
          </th>

          <td>
            (없음)
          </td>
        </tr>

        <tr>
          <th>
            [에 설정](#options)
          </th>

          <td>
            환경 변수
          </td>
        </tr>
      </tbody>
    </table>

    커밋 샤. 전체 샤를 사용하거나 처음 7자만 사용할 수 있습니다(예: 734713b).
  </Collapser>

  <Collapser id="NEW_RELIC_METADATA_RELEASE_TAG" title="NEW_RELIC_METADATA_RELEASE_TAG">
    <Callout variant="important">
      이것은 환경 변수를 통해서만 설정할 수 있습니다.
    </Callout>

    <table>
      <tbody>
        <tr>
          <th>
            유형
          </th>

          <td>
            문자열
          </td>
        </tr>

        <tr>
          <th>
            기본값
          </th>

          <td>
            (없음)
          </td>
        </tr>

        <tr>
          <th>
            [에 설정](#options)
          </th>

          <td>
            환경 변수
          </td>
        </tr>
      </tbody>
    </table>

    릴리스 태그(예: v0.1.209 또는 릴리스-209).
  </Collapser>
</CollapserGroup>

## 받은편지함 구성 오류 [#errors-inbox-configuration]

다음 태그 중 하나를 설정하면 오류를 생성하는 소프트웨어 버전을 식별하는 데 도움이 됩니다.

* `NEW_RELIC_METADATA_SERVICE_VERSION` 배포된 코드 버전(대부분의 경우 1.2.3과 같은 의미 버전)이 포함된 이벤트 데이터에 대한 tagged.service.version을 생성하지만 항상 그런 것은 아닙니다.
* `NEW_RELIC_METADATA_RELEASE_TAG `릴리스 태그(예: v0.1.209)가 포함된 이벤트 데이터에 tagged.releaseTag를 생성합니다. 또는 릴리스-209).
* `NEW_RELIC_METADATA_COMMIT` 커밋 샤를 포함하는 이벤트 데이터에 대한 tagged.commit을 생성합니다. 전체 sha를 사용하거나 처음 7자만 사용할 수 있습니다(예: 734713b).

향후 릴리스된 오류 받은 편지함에서는 오류를 생성하는 소프트웨어 버전을 자동으로 추적합니다. 모든 버전 데이터는 [CodeStream](/docs/codestream/how-use-codestream/performance-monitoring/#buildsha) 에도 표시됩니다.

## 취약점 관리 설정 [#inivar-vulnerability-management]

이 섹션에서는 PHP 패키지 보고에 영향을 미치는 설정을 나열합니다.

<CollapserGroup>
  <Collapser id="inivar-package-detection" title="newrelic.취약점.package_Detection.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td>
            PHP 에이전트 버전 10.17에서 도입됨
          </td>
        </tr>
      </tbody>
    </table>

    `true` 으로 설정하면 에이전트가 환경 페이지에서 볼 수 있는 패키지 감지 정보를 전송합니다.
  </Collapser>

  <Collapser id="inivar-package-detection-use-composer-api" title="newrelic.취약점.composer_api.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td>
            PHP 에이전트 버전 11.2에 도입됨
          </td>
        </tr>
      </tbody>
    </table>

    `true` 로 설정하면 에이전트는 패키지 감지를 위해 [Composer의 런타임 API를](https://getcomposer.org/doc/07-runtime.md) 사용하려고 합니다.
  </Collapser>

  <Collapser id="inivar-package-detection-use-composer-api-once-per-process" title="newrelic.vulnerability_management.composer_api.per_process_detection">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td>
            PHP 에이전트 버전 11.10에 도입됨
          </td>
        </tr>
      </tbody>
    </table>

    Composer API가 패키지 데이터에 대한 런타임 환경을 샘플링하는 빈도를 제어합니다. `true` 로 설정하면 샘플링은 프로세스당 한 번만 발생합니다. 거짓인 경우, Composer는 요청마다 환경을 샘플링하여 패키지 감지가 수행되는 빈도를 높입니다.
  </Collapser>
</CollapserGroup>

## 기타 .ini 설정 [#inivar-rare-settings]

이 섹션에서는 나머지 newrelic.ini 설정을 나열합니다.

<CollapserGroup>
  <Collapser id="inivar-enabled" title="newrelic.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            (없음)
          </td>
        </tr>
      </tbody>
    </table>

    New Relic 에이전트를 활성화하거나 비활성화합니다. 기본적으로 New Relic PHP 에이전트는 모든 디렉터리에 대해 활성화되어 있습니다.

    웹 서버에 여러 사이트가 있지만 PHP 에이전트가 특정 사이트만 모니터링하도록 하려는 경우:

    1. `newrelic.ini` 파일의 전역(SYSTEM) 수준에서 `newrelic.enabled` 이 `true` 로 설정되어 있는지 확인하십시오.

    2. 디렉토리별로(PERDIR) 하나 이상의 특정 사이트에 대해 `newrelic.enabled` 을 `false` 로 설정합니다.

       에이전트를 전역적으로 비활성화해야 하는 경우 `newrelic.ini` 파일에서 `newrelic.enabled` 값을 `false` 로 설정합니다.

       에이전트가 전역적으로 비활성화되면 완전히 초기화되지 않습니다. 중요한 이유로 전역적으로 비활성화한다고 가정하고 에이전트는 PHP에 최대한 영향을 미치지 않도록 시도합니다.

       <Callout variant="important">
         에이전트를 전역적으로 비활성화한 다음 `.htaccess` 을 사용하여 디렉토리별로 선택적으로 활성화할 수 없습니다.
       </Callout>
  </Collapser>

  <Collapser id="inivar-logfile" title="newrelic.log 파일">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            문자열(따옴표 사용)
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `"/var/log/newrelic/php_agent.log"`
          </td>
        </tr>
      </tbody>
    </table>

    이것은 로깅 메시지의 파일 이름을 식별합니다. 이는 에이전트 문제를 디버깅하는 데 유용합니다. 무엇을 설정하든 다음을 확인하십시오.

    * 포함하는 디렉토리와 파일 자체에 대한 권한이 정확합니다.
    * PHP가 실행하는 사용자는 파일에 쓸 수 있습니다. 일반적으로 이것은 웹 서버와 동일한 사용자입니다.
  </Collapser>

  <Collapser id="inivar-analytics-events" title="newrelic.transaction_events.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    `true` 으로 설정하면 에이전트가 분석 이벤트 데이터를 수집하고 보고합니다. 이벤트 데이터를 통해 New Relic UI는 [히스토그램](/docs/applications-menu/histograms-viewing-data-distribution) 및 [백분위수](/docs/applications-menu/percentiles-comparing-ranked-data) 와 같은 추가 정보를 표시할 수 있습니다.
  </Collapser>

  <Collapser id="inivar-error-events" title="newrelic.error_collector.capture_events">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    `true` 으로 설정하면 에이전트는 오류 이벤트 데이터를 수집하고 보고합니다. 이벤트 데이터를 통해 New Relic UI는 오류 분석을 표시할 수 있습니다.
  </Collapser>

  <Collapser id="inivar-feature-flag" title="newrelic.feature_flag">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이 설정은 PHP 에이전트 내에서 새롭고 실험적인 기능을 활성화합니다. 이 플래그는 PHP 에이전트의 이후 버전에서 기본적으로 활성화되도록 의도된 기능을 선택적으로 활성화하는 데 사용됩니다.
  </Collapser>

  <Collapser id="inivar-autorum" title="newrelic.browser_monitoring.auto_instrument">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    이렇게 하면 <InlinePopover type="browser" />에 대한 JavaScript 조각의 자동 삽입이 활성화됩니다. 활성화되면 에이전트는 최종 사용자 환경에 대한 페이지 로드 타이밍에 지표를 생성하는 HTML 출력에 머리글과 바닥글을 삽입하게 됩니다. 자세한 내용은 [PHP의 페이지 로드 타이밍을](/docs/php/page-load-timing-in-php) 참조하세요.
  </Collapser>

  <Collapser id="inivar-autorum" title="newrelic.guzzle.enabled">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            페르디르
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>

    [New Relic 과 함께 사용할 때 Guzzle 라이브러리](/docs/agents/php-agent/frameworks-libraries/guzzle) 에 대한 지원을 활성화하거나 비활성화합니다.

    * PHP 에이전트 버전 6.4 이상: 이 설정은 기본적으로 `true` 으로 설정됩니다.
  </Collapser>

  <Collapser id="inivar-autorum" title="newrelic.preload_framework_library_detection">
    <table>
      <tbody>
        <tr>
          <th>
            범위:
          </th>

          <td>
            체계
          </td>
        </tr>

        <tr>
          <th>
            유형:
          </th>

          <td>
            부울
          </td>
        </tr>

        <tr>
          <th>
            기본:
          </th>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <th>
            메모:
          </th>

          <td>
            PHP 에이전트 버전 9.4에 도입됨
          </td>
        </tr>
      </tbody>
    </table>

    사전 로드가 활성화된 경우 프레임워크 및 라이브러리 감지를 활성화합니다. 미리 로드는 PHP 버전 7.4에서 도입되었습니다. `newrelic.preload_framework_library_detection` 은 `opcache.preload` 이 `php.ini` 파일에 설정된 경우에만 적용됩니다.
  </Collapser>
</CollapserGroup>