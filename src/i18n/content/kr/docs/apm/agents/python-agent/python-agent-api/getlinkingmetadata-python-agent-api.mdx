---
title: get_linking_metadata(파이썬 에이전트 API)
type: apiDoc
shortDescription: 로그 메시지에 사용할 컨텍스트 데이터 사전을 반환합니다.
tags:
  - Agents
  - Python agent
  - Python agent API
metaDescription: 'Python API: The get_linking_data call for providing contextual information for reported data.'
translationType: machine
---

## 통사론

```py
get_linking_metadata()
```

로그 메시지에 사용할 컨텍스트 데이터 사전을 반환합니다.

## 요구 사항

Python 에이전트 버전 5.4.0 이상

## 설명

컨텍스트 정보를 제공하는 키/값 쌍의 사전입니다. 이 API를 사용하여 수동 로깅을 위한 에이전트 메타데이터를 추가할 수 있습니다.

## 반환 값

사전을 반환합니다.

## 예

```py
import json
import newrelic.agent
log_message = {"message": "Hello World"}
context = newrelic.agent.get_linking_metadata()
log_message.update(context)
print(json.dumps(log_message))
```