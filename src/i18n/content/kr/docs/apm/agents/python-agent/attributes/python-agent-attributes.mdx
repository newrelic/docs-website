---
title: Python 에이전트 속성
tags:
  - Agents
  - Python agent
  - Attributes
metaDescription: A summary of attributes captured by the Python agent.
translationType: machine
---

[속성](/docs/features/agent-attributes) 은 이벤트 또는 트랜잭션의 속성을 결정하는 정보를 포함하는 키-값 쌍입니다. 사용자가 다음 각 대상으로 보낼 속성을 정확히 사용자 지정할 수 있습니다.

* [트랜잭션 추적](/docs/apm/transactions/transaction-traces/transaction-traces)
* [추적된 오류](/docs/apm/applications-menu/events/viewing-apm-errors-error-traces)
* [트랜잭션 이벤트](/docs/insights/new-relic-insights/decorating-events/apm-default-attributes-insights)
* [페이지 조회수](/docs/insights/new-relic-insights/decorating-events/browser-default-attributes-insights)

이러한 속성 설정은 Python 에이전트 버전 2.56.0.42 이상에 적용됩니다. 사용 가능한 모든 기능을 활용하려면 [Python 에이전트](/docs/agents/python-agent/installation-configuration/upgrade-python-agent) 를 [최신 릴리스](/docs/release-notes/agent-release-notes/python-release-notes) 로 업데이트하십시오.

## Python 에이전트 웹 트랜잭션 속성 [#attributes]

다음은 [웹 트랜잭션](/docs/apm/transactions/intro-transactions/transactions-new-relic-apm) 용 Python 에이전트에서 구성할 수 있는 속성 목록입니다. 자세한 내용은 [Python 에이전트 구성: 속성](/docs/python/python-agent-configuration#attributes) 을 참조하세요.

<CollapserGroup>
  <Collapser
    id="requestHeadersAccept"
    title="request.headers.accept"
  >
    HTTP Accept 요청 헤더에서 읽은 유형입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestHeadersContentLength"
    title="request.headers.content길이"
  >
    `Content-Length` 요청 헤더에서 읽은 수신 요청 크기(바이트)입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestHeadersContentType"
    title="request.headers.contentType"
  >
    `Content-Type` 요청 헤더에서 읽은 수신 요청 콘텐츠 유형입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestHeadersHost"
    title="요청.헤더.호스트"
  >
    HTTP 호스트 요청 헤더의 이름입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestHeadersReferer"
    title="request.headers.referer"
  >
    `Referer` 요청 헤더에서 읽은 수신 요청 참조자.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestHeadersUserAgent"
    title="request.headers.userAgent"
  >
    `User-Agent` 요청 헤더에서 읽은 수신 요청 user-agent입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestMethod"
    title="요청.메소드"
  >
    들어오는 요청의 HTTP 메서드입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="requestParameters"
    title="요청.매개변수.*"
  >
    요청 URL의 쿼리 문자열에서 가져온 매개변수입니다.

    <Callout variant="tip">
      `capture_params` 속성은 더 이상 사용되지 않습니다. 그러나 `true` 로 설정하면 트랜잭션 추적 및 추적된 오류에 대한 요청 매개변수가 활성화됩니다.
    </Callout>

    기본값:

    * 트랜잭션 추적: 비활성화됨
    * 오류 수집기(추적된 오류): 비활성화됨
    * 거래 이벤트: 비활성화됨
    * 페이지 보기(브라우저 모니터링): 비활성화됨
  </Collapser>

  <Collapser
    id="requestUri"
    title="request.uri"
  >
    들어오는 요청의 URI입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="responseContentLength"
    title="응답.헤더.콘텐츠길이"
  >
    `Content-Length` 응답 헤더에서 가져오는 발신 응답의 크기를 바이트 단위로 지정합니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="responseContentType"
    title="응답.헤더.콘텐츠 유형"
  >
    `Content-Type` 응답 헤더에서 가져오는 발신 응답의 콘텐츠 유형을 지정합니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="responseStatus"
    title="응답 상태"
  >
    발신 응답 상태입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="threadConcurrency"
    title="스레드.동시성"
  >
    동시 요청을 처리하는 스레드 수를 나타내는 "스레드 사용률" 측정값입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="webfrontendQueueSeconds"
    title="webfrontend.queue.seconds"
  >
    요청 대기열에 소요된 시간(초)입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiInputBytes"
    title="wsgi.input.bytes"
  >
    WSGI 입력 스트림(파일류 객체)에서 수신된 바이트 수

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiInputCallsRead"
    title="wsgi.input.calls.read"
  >
    요청을 처리하기 위해 WSGI 서버가 `read()` 에 호출한 횟수입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiInputCallsReadline"
    title="wsgi.input.calls.readline"
  >
    요청을 처리하기 위해 WSGI 서버가 `readline()` 에 호출한 횟수입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiInputCallsReadlines"
    title="wsgi.input.calls.readlines"
  >
    요청을 처리하기 위해 WSGI 서버가 `readlines()` 에 호출한 횟수입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiInputSeconds"
    title="wsgi.input.seconds"
  >
    WSGI 입력 스트림을 읽는 시간(초)입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiOutputBytes"
    title="wsgi.output.bytes"
  >
    WSGI 출력 응답에서 보낸 바이트 수입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiOutputCallsWrite"
    title="wsgi.output.calls.write"
  >
    응답을 보내기 위해 WSGI 서버가 `write()` 에 호출한 횟수입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiOutputCallsYield"
    title="wsgi.output.calls.yield"
  >
    응답을 보내기 위해 WSGI 서버가 `yield` 에 호출한 횟수입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>

  <Collapser
    id="wsgiOutputSeconds"
    title="wsgi.output.seconds"
  >
    WSGI 응답을 보낼 시간(초)입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 비활성화됨
    * 페이지 조회수(브라우저 모니터링): 사용할 수 없음
  </Collapser>
</CollapserGroup>

## Python 에이전트 메시지 트랜잭션 속성 [#message_attributes]

다음 섹션에서는 RabbitMQ와 같은 수신 메시지 브로커가 시작한 [메시지 트랜잭션](/docs/agents/python-agent/supported-features/python-message-queues) 에 대해 Python 에이전트에서 구성할 수 있는 속성을 나열합니다. 자세한 내용은 [Python 에이전트 구성: 속성](/docs/python/python-agent-configuration#attributes) 을 참조하세요.

<CollapserGroup>
  <Collapser
    id="messageCorrelationId"
    title="message.correlationId"
  >
    RPC 구성에 사용되는 애플리케이션 생성 식별자입니다.

    기본값:

    * 트랜잭션 추적: 비활성화됨
    * 오류 수집기(추적된 오류): 비활성화됨
    * 거래 이벤트: 비활성화됨
  </Collapser>

  <Collapser
    id="messageExchangeType"
    title="메시지.교환 유형"
  >
    이 메시지가 소비된 교환 유형: 직접, 팬아웃, 주제 또는 헤더

    기본값:

    * 트랜잭션 추적: 비활성화됨
    * 오류 수집기(추적된 오류): 비활성화됨
    * 거래 이벤트: 비활성화됨
  </Collapser>

  <Collapser
    id="messageHeaders"
    title="메시지.헤더.*"
  >
    들어오는 메시지의 헤더입니다.

    기본값:

    * 트랜잭션 추적: 비활성화됨
    * 오류 수집기(추적된 오류): 비활성화됨
    * 거래 이벤트: 비활성화됨
  </Collapser>

  <Collapser
    id="messageQueueName"
    title="메시지.대기열 이름"
  >
    이 메시지가 소비된 큐의 이름입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
  </Collapser>

  <Collapser
    id="messageReplyTo"
    title="message.replyTo"
  >
    RPC 구성에 사용되는 콜백 대기열입니다.

    기본값:

    * 트랜잭션 추적: 비활성화됨
    * 오류 수집기(추적된 오류): 비활성화됨
    * 거래 이벤트: 비활성화됨
  </Collapser>

  <Collapser
    id="messageRoutingKey"
    title="메시지.라우팅키"
  >
    들어오는 메시지의 라우팅 키입니다.

    기본값:

    * 트랜잭션 추적: 사용
    * 오류 수집기(추적된 오류): 사용
    * 거래 이벤트: 사용
  </Collapser>
</CollapserGroup>

## Python 에이전트 외부 추적 속성 [#message_attributes]

다음 섹션에는 [httplib, boto3, 요청](/docs/agents/python-agent/getting-started/instrumented-python-packages#external-web-services) 등과 같은 나가는 라이브러리에서 시작하거나 [외부 추적 API](/docs/agents/python-agent/python-agent-api/external-trace) 를 사용할 때 Python 에이전트에서 구성할 수 있는 속성이 나열되어 있습니다. 자세한 내용은 [Python 에이전트 구성: 속성](/docs/python/python-agent-configuration#attributes) 을 참조하세요.

<CollapserGroup>
  <Collapser
    id="httpUrl"
    title="http.url"
  >
    요청 매개변수가 제거된 발신 호출의 URL입니다.

    다음과 같은 경우 제외됨:

    * 'http.url'과 일치하는 규칙이 'transaction_segments.attributes.exclude' 설정에 있습니다. (세그먼트용)

    * 'http.url'과 일치하는 규칙이 "span_events.attributes.exclude" 구성 설정에 있습니다. (스팬의 경우)

      기본값:

    * 거래 세그먼트: 사용

    * 스팬 이벤트: 사용
  </Collapser>
</CollapserGroup>

## Python 에이전트 데이터베이스 추적 속성 [#message_attributes]

다음 섹션에서는 [mysql, psycopg2, cx_Oracle](/docs/agents/python-agent/getting-started/instrumented-python-packages#sql-database-adapters) 등과 같은 데이터베이스 라이브러리에서 시작하거나 데이터베이스 추적 API를 사용할 때 Python 에이전트에서 구성할 수 있는 속성을 나열합니다. 자세한 내용은 [Python 에이전트 구성: 속성](/docs/python/python-agent-configuration#attributes) 을 참조하세요.

<CollapserGroup>
  <Collapser
    id="dbdbInstance"
    title="db.instance"
  >
    쿼리에 사용된 데이터베이스의 이름입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.database_name_reporting.enabled" 구성 설정이 False로 설정됩니다.

    * 'db.instance'와 일치하는 규칙이 "transaction_segments.attributes.exclude" 설정에 있는 경우(세그먼트의 경우)

    * 'db.instance'와 일치하는 규칙이 "span_events.attributes.exclude" 구성 설정에 있는 경우. (스팬의 경우)

      기본값:

    * 거래 세그먼트: 사용

    * 스팬 이벤트: 사용
  </Collapser>

  <Collapser
    id="dbHost"
    title="주인"
  >
    데이터베이스에 연결하는 데 사용되는 호스트입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다.

      기본값:

    * 거래 세그먼트: 사용
  </Collapser>

  <Collapser
    id="dbPortPathOrId"
    title="port_path_or_id"
  >
    데이터베이스의 포트, 경로 또는 ID입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다.

      기본값:

    * 거래 세그먼트: 사용
  </Collapser>

  <Collapser
    id="sql"
    title="SQL"
  >
    [transaction_tracer.record_sql 설정](/docs/agents/python-agent/configuration/python-agent-configuration#txn-tracer-sql) 에 따라 처리된 쿼리에 사용된 SQLl입니다. 큰 쿼리는 크기가 제한됩니다.

    다음과 같은 경우 제외됨:

    * "transaction_tracer.record_sql 설정" 설정이 "꺼짐"으로 설정되어 있습니다.

      기본값:

    * 트랜잭션 세그먼트: 사용, "난독화된" SQL로 보고됨.
  </Collapser>

  <Collapser
    id="dbPeerHostname"
    title="피어.호스트 이름"
  >
    데이터베이스에 연결하는 데 사용되는 호스트입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다. (그렇지 않으면 "알 수 없음"이 대신 보고됩니다.)

      기본값:

    * 스팬 이벤트: 사용
  </Collapser>

  <Collapser
    id="dbPeerAddress"
    title="피어 주소"
  >
    포트 또는 경로 정보를 포함하여 데이터베이스에 연결하는 데 사용되는 호스트입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다. (그렇지 않으면 "알 수 없음"이 대신 보고됩니다.)

      기본값:

    * 스팬 이벤트: 사용
  </Collapser>

  <Collapser
    id="dbStatement"
    title="db.statement"
  >
    [transaction_tracer.record_sql 설정](/docs/agents/python-agent/configuration/python-agent-configuration#txn-tracer-sql) 에 따라 처리된 쿼리에 사용된 SQL입니다. 큰 쿼리는 크기가 제한됩니다.

    다음과 같은 경우 제외됨:

    * "transaction_tracer.record_sql 설정" 설정이 "꺼짐"으로 설정되어 있습니다.

      기본값:

    * 스팬 이벤트: 사용, "난독화된" SQL로 보고됨.
  </Collapser>

  <Collapser
    id="dbOperation"
    title="db.작업"
  >
    이 범위에서 수행한 데이터베이스 작업입니다. db.statement가 적용되지 않는 경우에만 포함됩니다.

    다음과 같은 경우 제외됨:

    * db.statement 속성이 보고됩니다.

      기본값:

    * 거래 세그먼트: 사용

    * 스팬 이벤트: 사용
  </Collapser>
</CollapserGroup>

## Python 에이전트 데이터 저장소 추적 속성 [#message_attributes]

다음 섹션에는 [redis, memcache, pymongo](/docs/agents/python-agent/getting-started/instrumented-python-packages#sql-database-adapters) 등과 같은 데이터 저장소 라이브러리에서 시작하거나 [데이터 저장소 추적 API](/docs/agents/python-agent/python-agent-api/datastore_trace) 를 사용할 때 Python 에이전트에서 구성할 수 있는 속성이 나열되어 있습니다. 자세한 내용은 [Python 에이전트 구성: 속성](/docs/python/python-agent-configuration#attributes) 을 참조하세요.

<CollapserGroup>
  <Collapser
    id="dbdbInstance"
    title="db.instance"
  >
    쿼리에 사용된 데이터 저장소 인스턴스의 이름입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.database_name_reporting.enabled" 구성 설정이 False로 설정됩니다.

    * 'db.instance'와 일치하는 규칙이 "transaction_segments.attributes.exclude" 설정에 있는 경우(세그먼트용).

    * 'db.instance'와 일치하는 규칙이 "span_events.attributes.exclude" 구성 설정에 있는 경우(스팬의 경우).

      기본값:

    * 거래 세그먼트: 사용

    * 스팬 이벤트: 사용
  </Collapser>

  <Collapser
    id="dsHost"
    title="주인"
  >
    데이터 저장소에 연결하는 데 사용되는 호스트입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다.

      기본값:

    * 거래 세그먼트: 사용
  </Collapser>

  <Collapser
    id="dsPortPathOrId"
    title="port_path_or_id"
  >
    데이터 저장소의 포트, 경로 또는 ID입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다.

      기본값:

    * 거래 세그먼트: 사용
  </Collapser>

  <Collapser
    id="dsPeerHostname"
    title="피어.호스트 이름"
  >
    데이터 저장소에 연결하는 데 사용되는 호스트입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다. (그렇지 않으면 "알 수 없음"이 대신 보고됩니다.)

      기본값:

    * 스팬 이벤트: 사용
  </Collapser>

  <Collapser
    id="dsPeerAddress"
    title="피어 주소"
  >
    포트, 경로 또는 ID 정보를 포함하여 데이터 저장소에 연결하는 데 사용되는 호스트입니다.

    다음과 같은 경우 제외됨:

    * "datastore_tracer.instance_reporting.enabled 구성" 설정이 False로 설정됩니다. (그렇지 않으면 "알 수 없음"이 대신 보고됩니다.)

      기본값:

    * 스팬 이벤트: 사용
  </Collapser>
</CollapserGroup>

## Python 에이전트 범위 속성 [#attributes]

다음 섹션에서는 [범위 이벤트](/docs/apm/distributed-tracing/ui-data/span-event) 에 대해 Python 에이전트에서 구성할 수 있는 속성을 나열합니다. 자세한 내용은 [Python 에이전트 구성: 속성](/docs/python/python-agent-configuration#attributes) 을 참조하세요.

<CollapserGroup>
  <Collapser
    id="errorClass"
    title="error.class"
  >
    범위 이벤트에 기록되는 예외 클래스(있는 경우)입니다. [notice_error](/docs/agents/python-agent/python-agent-api/noticeerror-python-agent-api/) API를 사용하여 스팬에 예외 세부 정보를 기록할 수 있습니다.

    기본값:

    * 스팬 이벤트: 사용
  </Collapser>

  <Collapser
    id="errorMessage"
    title="에러 메시지"
  >
    예외 메시지가 있는 경우 범위 이벤트에 기록됩니다. [notice_error](/docs/agents/python-agent/python-agent-api/noticeerror-python-agent-api/) API를 사용하여 스팬에 예외 세부 정보를 기록할 수 있습니다.

    기본값:

    * 스팬 이벤트: 사용
  </Collapser>
</CollapserGroup>

## 사용자 정의 속성 추가 [#adding_custom_attributes]

애플리케이션에서 추가 사용자 정의 속성을 캡처하려면 `newrelic.agent.add_custom_attribute()` 을 사용합니다.

전체 참조는 [사용자 정의 속성 수집](/docs/apm/other-features/attributes/collecting-custom-attributes#python-att) 을 참조하십시오.

기본값:

* 트랜잭션 추적: 사용
* 오류 수집기(추적된 오류): 사용
* 거래 이벤트: 사용
* 페이지 보기(브라우저 모니터링): 비활성화됨

<Callout variant="important">
  맞춤 속성을 만들기 전에 [예약어](/docs/insights/event-data-sources/custom-events/data-requirements-limits-custom-event-data/#reserved-words) 목록을 검토하세요. 그렇지 않으면 예기치 않은 결과가 발생할 수 있습니다.
</Callout>

## 예외에 사용자 정의 속성 추가 [#adding_error_attributes]

`newrelic.agent.notice_error()` API 호출을 사용할 때 추적된 오류의 속성으로 기록될 `params` 사전을 전달할 수 있습니다.

기본값:

* 거래 추적: 사용할 수 없음
* 오류 수집기(추적된 오류): 사용
* 거래 이벤트: 사용할 수 없음
* 페이지 조회수(브라우저 모니터링): 사용할 수 없음

## 구성 파일 업그레이드 [#upgrading]

Python 에이전트 **2.56.0.42** 이상으로 업그레이드할 때 **newrelic.ini** 구성 파일을 업그레이드하십시오. 더 이상 사용되지 않는 구성 설정에 대한 자세한 내용은 [속성 활성화 및 비활성화](/docs/agents/python-agent/attributes/enabling-disabling-attributes-python#deprecated) 를 참조하십시오.