---
title: V3.0 SP03 이전에 작업 일정을 예약하세요
metaDescription: Instructions for scheduling collection manager jobs before SP03.
freshnessValidatedDate: never
translationType: machine
---

<Callout variant="important">
  이 섹션은 <DNT>**V3.0**</DNT> 이전의 <DNT>**NRM4SAP**</DNT> 버전에만 적용됩니다.
</Callout>

수집 작업 예약에는 세 가지 주요 단계가 포함됩니다: `/NEWR/ABAP_COLLECTION_MANAGERN`에 대한 변형 생성, `RSBTONEJOB2`에 대한 변형 생성 및 수집 작업 예약.

## /NEWR/ABAP\_COLLECTION\_MANAGERN에 대한 변형을 생성하십시오.

1. 트랜잭션 `SE38` 을 사용하여 `/NEWR/ABAP_COLLECTION_MANAGERN`을 설정합니다.
2. <DNT>**ABAP: Variants - Initial Screen**</DNT>에서 <DNT>**Program**</DNT> 필드에 `/NEWR/ABAP_COLLECTION_MANAGERN` 을(를) 입력합니다.
3. <DNT>**Variant**</DNT> 선택하고 <DNT>**Create**</DNT> 클릭합니다.
4. 값을 업데이트하려면 다음 단계를 따르십시오:

<Tabs>
  <TabsBar>
    <TabsBarItem id="trace-variant">
      트레이스 변종
    </TabsBarItem>

    <TabsBarItem id="idoc-variant">
      IDOC 변형
    </TabsBarItem>

    <TabsBarItem id="system-variant">
      시스템 변형
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="trace-variant">
      1. 변형 이름(예: <DNT>**TRACE\_ONLY**</DNT>)을 입력하고 <DNT>**Create**</DNT>을(를) 클릭합니다.

      2. <DNT>**New Relic Account ID**</DNT> 이(가) 자동으로 채워지도록 하세요.

      3. <DNT>**Records per transmission**</DNT>을 <DNT>**10000**</DNT>로 설정합니다. 이는 배치 크기를 제어합니다: 값이 너무 작으면 효율성이 감소하고, 값이 너무 크면 뉴렐릭 API 수집에 과부하가 걸릴 수 있습니다.

      4. 데이터 공급자 이름을 추가하려면 <DNT>**Multiple Selection (Active)**</DNT> 을 클릭하세요.

      5. <DNT>**Select Single Values**</DNT>에 다음을 추가합니다:

         * `NR.SAP.TRANSACTION`
         * `NR.SAP.TRANSACTION.COMPLETE`
         * `NR.SAP.SYSTEM.PROCESS`
         * `NR_SAP_PROGRAM`
         * `NR_SAP_WORKLOAD_COMPLETE`
         * `NR.SAP.WORKLOAD.COMPLETE`

      6. <DNT>**Execution Mode**</DNT> 로 <DNT>**Sequential Processing**</DNT> 선택합니다.

      7. <DNT>**DIA WP**</DNT> ≥ 30이면 <DNT>**6**</DNT>로 설정하고, 그렇지 않으면 <DNT>**3**</DNT>로 설정합니다.

      8. <DNT>**Attributes**</DNT>을(를) 클릭하고 설명을 추가한 다음 <DNT>**Save**</DNT>을(를) 클릭합니다.
    </TabsPageItem>

    <TabsPageItem id="idoc-variant">
      1. 변형 이름(예: <DNT>**IDOC\_ONLY**</DNT>)을 입력하고 <DNT>**Create**</DNT>을(를) 클릭합니다.

      2. <DNT>**New Relic Account ID**</DNT> 이(가) 자동으로 채워지도록 하세요.

      3. <DNT>**Records per transmission**</DNT>을 <DNT>**10000**</DNT>로 설정합니다. 이는 배치 크기를 제어합니다: 값이 너무 작으면 효율성이 감소하고, 값이 너무 크면 뉴렐릭 API 수집에 과부하가 걸릴 수 있습니다.

      4. 데이터 공급자 이름을 추가하려면 <DNT>**Multiple Selection (Active)**</DNT> 을 클릭하세요.

      5. <DNT>**Select Single Values**</DNT>에 다음을 추가합니다:

         * `NR_SAP_IDOC`
         * `NR_SAP_IDOC_COMPLETE`
         * `NR_SAP_IDOC_Configuration`

      6. <DNT>**Execution Mode**</DNT> 로 <DNT>**Sequential Processing**</DNT> 선택합니다.

      7. <DNT>**Attributes**</DNT>을(를) 클릭하고 설명을 추가한 다음 <DNT>**Save**</DNT>을(를) 클릭합니다.
    </TabsPageItem>

    <TabsPageItem id="system-variant">
      1. 변형 이름(예: <DNT>**System**</DNT>)을 입력하고 <DNT>**Create**</DNT>을(를) 클릭합니다.
      2. <DNT>**New Relic Account ID**</DNT> 이(가) 자동으로 채워지도록 하세요.
      3. <DNT>**Records per transmission**</DNT>을 <DNT>**10000**</DNT>로 설정합니다. 이는 배치 크기를 제어합니다: 값이 너무 작으면 효율성이 감소하고, 값이 너무 크면 뉴렐릭 API 수집에 과부하가 걸릴 수 있습니다.
      4. 데이터 공급자 이름을 추가하려면 <DNT>**Multiple Selection (Active)**</DNT> 을 클릭하세요.
      5. <DNT>**Exclude Single Values**</DNT>에서 <DNT>**TRACE**</DNT> 및 <DNT>**IDOC**</DNT> 공급자의 변형을 추가합니다.
      6. <DNT>**Execution Mode**</DNT> 로 <DNT>**Parallel Processing**</DNT> 선택합니다.
      7. <DNT>**DIA WP**</DNT> ≥ 30이면 <DNT>**6**</DNT>로 설정하고, 그렇지 않으면 <DNT>**3**</DNT>로 설정합니다.
      8. <DNT>**Attributes**</DNT>을(를) 클릭하고 설명을 추가한 다음 <DNT>**Save**</DNT>을(를) 클릭합니다.
    </TabsPageItem>
  </TabsPages>
</Tabs>

<img title="Variants" alt="A screenshot displaying the Variants" src="/images/parallel-processing.webp" width="" />

## RSBTONEJOB2에 대한 변형을 만듭니다.

SAP 유틸리티 프로그램 `RSBTONEJOB2`을(를) 사용하여 컬렉션 관리자 작업을 단일 활성 인스턴스로 제한하십시오. 이는 모니터링 및 모니터 시스템 모두에서 작업 프로세스 소비를 최소화합니다.

1. 트랜잭션 `SE38` 을 사용하여 `RSBTONEJOB2`을 설정합니다.

<img title="Message Processing" alt="A screenshot displaying the Message Processing" src="/images/cua-message-processing.webp" width="600" />

2. 다음 값으로 변형을 만듭니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **변형 이름**
      </th>

      <th>
        **직책명**
      </th>

      <th>
        **프로그램**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        **트레이스**
      </td>

      <td>
        `NR_COLLECTION_TRACE`
      </td>

      <td>
        `/NEWR/ABAP_COLLECTION_MANAGERN`
      </td>
    </tr>

    <tr>
      <td>
        **IDOC**
      </td>

      <td>
        `NR_COLLECTION_IDOC`
      </td>

      <td>
        `/NEWR/ABAP_COLLECTION_MANAGERN`
      </td>
    </tr>

    <tr>
      <td>
        **체계**
      </td>

      <td>
        `NR_COLLECTION_SYSTEM`
      </td>

      <td>
        `/NEWR/ABAP_COLLECTION_MANAGERN`
      </td>
    </tr>
  </tbody>
</table>

## 일정 수집 작업

1. 트랜잭션 `SM36` 을 사용하여 세 개의 작업을 예약합니다.
2. 각 작업의 작업 이름을 입력하고 <DNT>**Job Class**</DNT> 을(를) <DNT>**C**</DNT>(으)로 설정합니다.
3. <DNT>**Step**</DNT> 클릭하고, 프로그램 이름 `RSBTONEJOB2` 입력하고, 각 작업에 대한 변형을 지정합니다.
4. <DNT>**Start Condition**</DNT>을(를) 클릭하고 시작 날짜와 시간을 설정하십시오. <DNT>**Periodic Job**</DNT>을(를) 확인한 다음 <DNT>**Period Values**</DNT> &gt; <DNT>**Other Period**</DNT>을(를) 클릭하고 분에 <DNT>**5**</DNT>을(를) 입력하십시오.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **직업 이름**
      </th>

      <th>
        **프로그램**
      </th>

      <th>
        **변종**
      </th>

      <th>
        **실행 빈도**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `NR_COLLECTION_TRACE`
      </td>

      <td>
        `RSBTONEJOB2`
      </td>

      <td>
        **트레이스**
      </td>

      <td>
        5분마다
      </td>
    </tr>

    <tr>
      <td>
        `NR_COLLECTION_IDOC`
      </td>

      <td>
        `RSBTONEJOB2`
      </td>

      <td>
        **IDOC**
      </td>

      <td>
        5분마다
      </td>
    </tr>

    <tr>
      <td>
        `NR_COLLECTION_SYSTEM`
      </td>

      <td>
        `RSBTONEJOB2`
      </td>

      <td>
        **체계**
      </td>

      <td>
        5분마다
      </td>
    </tr>
  </tbody>
</table>