---
title: interaction (SPA API)
type: apiDoc
shortDescription: 현재 SPA 상호 작용에 바인딩된 새 API 개체를 반환합니다.
tags:
  - Browser
  - Browser monitoring
  - Browser agent and SPA API
metaDescription: SPA API call for browser monitoring to return a new API object that is bound to the current interaction.
translationType: machine
---

<Callout variant="important">
  이 API를 사용하려면 브라우저 에이전트의 Browser Pro+SPA 버전이 필요합니다.
</Callout>

## 통사론

```js
newrelic.interaction()
```

현재 SPA 상호 작용에 바인딩된 새 API 개체를 반환합니다.

## 요구 사항

에이전트 버전 [nr-963](/docs/release-notes/new-relic-browser-release-notes/browser-agent-release-notes) 이상입니다.

## 설명

[SPA 모니터링](/docs/browser/single-page-app-monitoring/get-started/introduction-single-page-app-monitoring) `interaction()` 호출은 현재 상호작용에 바인딩된 새 API 객체를 반환합니다.

* **새 상호 작용:** New Relic이 현재 상호 작용을 모니터링하지 않을 때 API가 이를 호출하면 새 상호 작용이 생성됩니다.
* **새 객체:** API가 동일한 상호 작용 내에서 이를 다시 호출하면 현재 상호 작용을 참조하는 새 객체가 생성됩니다.

## 매개변수

매개변수는 특정 SPA 상호작용 API 호출에 따라 다릅니다.

## 반환 값

이 메서드는 특정 [`BrowserInteraction` 이벤트](/docs/insights/explore-data/attributes/browser-default-attributes-insights#browserinteraction-attributes) 에 바인딩된 API 객체를 반환합니다. 동일한 `BrowserInteraction` 에 대해 이 메서드가 호출될 때마다 새 객체가 생성되지만 여전히 동일한 상호작용을 참조합니다.

## 예

SPA API 메소드는 `newrelic.interaction()` 에서 사용할 수 있습니다. 메서드는 변수로 할당한 핸들에서도 사용할 수 있습니다. 예를 들어:

```js
myInteraction = newrelic.interaction();
myInteraction.save();
```

명명된 핸들은 상호 작용 외부에서 저장하고 사용할 수 있지만 상호 작용이 종료된 후에는 메서드가 적용되지 않습니다.