---
subject: Lambda-Extension
releaseDate: '2026-08-19'
version: 2.7.0
features:
  - Added LIM support
  - Added 'entity.name' for logs
enhancements:
  - Updated NEW_RELIC_LABELS configuration to support Transaction and Span attribute propagation.
bugs:
  - Fixed telemetry dispatch timing to send agent payloads immediately upon arrival instead of waiting to pair with platform.report events.
security: []
translationType: machine
---

## Notes

* AWS Lambda 관리형 인스턴스(LMI)에 대한 지원을 추가했습니다.
* `Updated NEW_RELIC_LABELS` 트랜잭션 및 스팬 속성 전파를 지원하기 위한 설정입니다.
* 서버리스 모드에 대한 `NEW_RELIC_DATA_COLLECTION_TIMEOUT` 및 `NEW_RELIC_HTTP_TIMEOUT` 지원을 추가했습니다.
* 로그에 대한 `entity.name`을(를) 추가했습니다

## 결정된

* ...와(과) 페어링을 기다리는 대신 도착 즉시 에이전트 페이로드를 전송하도록 텔레메트리 디스패치 타이밍을 수정했습니다. `platform.report`
* 구형 smithy rustls 기본값을 비활성화했습니다