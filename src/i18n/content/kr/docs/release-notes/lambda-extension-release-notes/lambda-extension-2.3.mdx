---
subject: Lambda-Extension
releaseDate: '2025-06-05'
version: 2.3.20
translationType: machine
---

## Notes

* `logserver` 종료를 확인하고 핸들러가 로그 채널에 메시지를 보내는 것을 방지하기 위한 논리가 추가되었습니다.

* 서클 CI 흐름 제거

## 결정된

* 여러 로그 서버 핸들러가 닫힌 채널로 로그를 보내는 것으로 인해 발생하는 `log server panic` 해결하기 위해 `WaitGroup` 추가합니다.