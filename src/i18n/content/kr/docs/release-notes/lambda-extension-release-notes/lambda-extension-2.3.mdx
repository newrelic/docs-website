---
subject: Lambda-Extension
releaseDate: '2025-04-08'
version: 2.3.18
translationType: machine
---

## Notes

* 테스트, 응답 스트리밍을 통한 Lambda 함수 작성과 함께 뉴렐릭 AI 모델링에 대한 예제가 추가되었습니다.

* 최신 레이어와 패키지를 포함하도록 `SAM` 예시를 업데이트했습니다.

* 업데이트된 `Ruby` Lambda 측정, 로그 예시

* Lambda 로그에 `trace-id` 추가하는 데 사용할 수 있는 `NEW_RELIC_COLLECT_TRACE_ID` 환경 변수를 포함하도록 Readme를 업데이트했습니다.

## 해결된 문제

* Lambda 함수 시간 초과 오류에 대한 시간 초과 기간을 Extension이 부정확하게 캡처하는 문제가 해결되었습니다.

* 단위 테스트에서 간헐적인 문제를 해결하기 위해 수면 시간을 업데이트했습니다. `TestClientSendRetry`