---
subject: Lambda-Extension
releaseDate: '2025-05-12'
version: 2.3.19
translationType: machine
---

## Notes

* `go/stdlib` 취약점을 해결하기 위해 Go 버전을 업데이트했습니다.

* 응답 스트리밍에서 AI 모니터링을 위한 업데이트 예시

* 로컬 테스트 `newrelic_lambda_wrapper` 코드를 업데이트합니다.

* 업데이트 `ubuntu` 러너

* 오류 `/lib64/libc.so.6` 을 방지하기 위해 확장 빌드를 업데이트했습니다. 버전 `GLIBC_X.YZ` 찾을 수 없습니다.

* /examples/sam/containerized-lambda/nodejs-sam-example/hello-world에서 축을 `1.7.7` 에서 `1.8.2` 로 범프합니다.

* golang.org/x/net을 범프하세요 `0.33.0` 부터 `0.38.0`