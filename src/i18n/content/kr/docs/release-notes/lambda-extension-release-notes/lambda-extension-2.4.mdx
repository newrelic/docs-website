---
subject: Lambda-Extension
releaseDate: '2026-01-23'
version: 2.4.4
translationType: machine
---

## Notes

* 설정: `NEW_RELIC_EXTENSION_SEND_PLATFORM_LOGS` 환경 변수 지원 추가

* 메타데이터: `Extension version` 에 대한 태그를 엔티티에 추가합니다. `Agent version`

* 로깅: 보조 `Log level` DEBUG로 변경

## 결정된

* 런타임 감지: 에이전트 호환성 향상을 위해 런타임 감지 로직을 개선합니다.

* 델메트리: 델메트리 구성 요소 전반에 걸쳐 전역 폴백 ARN 처리 구현

* 유효성 검사: 추가 검사를 위해 코드를 업데이트하세요