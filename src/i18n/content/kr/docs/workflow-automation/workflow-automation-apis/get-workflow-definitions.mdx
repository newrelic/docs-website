---
title: GetWorkflowDefinitions
tags:
  - workflow automation
  - workflow
  - Get Workflow Definitions
  - workflow automation API
metaDescription: Used to get and return a list of workflow definitions under the user account’s scope.
freshnessValidatedDate: never
translationType: machine
---

이 API 지정된 범위 내의 모든 개요 정의 개요 목록을 가져와 반환합니다. 참고로 이 쿼리에서는 내부 플로우 정의 yaml을 사용할 수 없습니다. A 워크플로우 정의 yaml은 `GetWorkflowDefinition` 쿼리를 사용해서만 반환됩니다. `/{actor/account(:id)/workflowAutomation/workflow(:name, :version)`

## 예시

```graphql
{
  actor {
    account(id: 11933347) {
      workflowAutomation {
        workflows(cursor: "xyz123") {
          results {
            definition {
              name
              scope {
                id
                type
              }
              version
            }
          }
        }
      }
    }
  }
}
```