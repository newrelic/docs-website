---
title: AWS S3 작업
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS S3 actions
metaDescription: A reference for AWS S3 actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

이 페이지는 워크플로우 자동화 액션 카탈로그에서 사용할 수 있는 AWS S3 액션에 대한 참조를 제공합니다. 이러한 액션을 사용하여 Amazon S3 버킷의 객체를 나열, 검색, 업로드 및 삭제하십시오.

## 전제 조건

블리자드 자동화에서 AWS 작업을 사용하기 전에 다음 사항이 있는지 확인하세요.

* 적절한 권한이 있는 AWS 계정.
* AWS 자격 증명이 구성되었습니다(IAM 사용자 자격 증명, IAM 역할 ARN 또는 세션 자격 증명).
* 사용하려는 특정 AWS 서비스에 필요한 IAM 권한입니다.

IAM 사용자 및 IAM 역할을 생성하는 방법과 AWS 작업 자동화와의 통합을 위한 정적 및 세션 AWS 자격 증명을 설정하는 방법에 대한 자세한 내용은 [AWS 자격 증명 설정하기를](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) 참조하세요.

## 버킷에 있는 객체 목록을 표시합니다.

작업 식별자는 `aws.s3.listObjectsV2`입니다.

버킷에 있는 객체의 일부 또는 전체(최대 1,000개)를 메타데이터와 함께 반환합니다. `listObjects`의 권장 버전입니다.

다음 표에서는 이 작업의 입력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **입력**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **설명**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS API 호출을 위해 수임할 IAM 역할 ARN입니다.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 액세스 키 ID. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 비밀 액세스 키. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. STS 인증을 위한 임시 세션 토큰입니다. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 타겟 버킷의 AWS 리전입니다.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 객체를 나열할 S3 버킷의 이름입니다.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `prefix`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. 이 접두사로 시작하는 키로 응답을 제한합니다.
      </td>

      <td>
        `path/to/folder/`
      </td>
    </tr>

    <tr>
      <td>
        `maxKeys`
      </td>

      <td>
        정수
      </td>

      <td>
        선택 사항입니다. 반환할 키의 최대 수입니다(최대 1,000개).
      </td>

      <td>
        `100`
      </td>
    </tr>

    <tr>
      <td>
        `continuationToken`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. 다음 결과 페이지를 가져오기 위한 이전 응답의 토큰입니다.
      </td>

      <td>
        `some-token`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        지도
      </td>

      <td>
        선택 사항입니다. API 호출에 전달할 추가 boto3 파라미터입니다.
      </td>

      <td>
        `{"Delimiter": "/"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        목록
      </td>

      <td>
        선택 사항입니다. 작업 출력에서 특정 필드를 추출하기 위한 JQ 선택자입니다.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

추가 선택적 boto3 필드(`EncodingType`, `FetchOwner`, `StartAfter`, `RequestPayer`, `ExpectedBucketOwner`, `OptionalObjectAttributes`, `Delimiter`)는 `parameters` 입력을 통해 사용할 수 있습니다. 자세한 내용은 [list\_objects\_v2 boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html)를 확인하십시오.

다음 표는 이 작업의 출력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **산출**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        물체
      </td>

      <td>
        [list_objects_v2 boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html)

        를 확인하십시오.
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        부울
      </td>

      <td>
        `true`

         또는 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        문자열
      </td>

      <td>
        `"Parameter validation failed: Invalid bucket name"`
      </td>
    </tr>
  </tbody>
</table>

**예제 워크플로우**

```yaml
name: aws-s3-list-objects-v2
description: 'List Objects in an AWS S3 Bucket'
steps:
  - name: aws_s3_listObjectsV2_1
    type: action
    action: aws.s3.listObjectsV2
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      prefix: "path/to/folder/"
      maxKeys: 100
    next: end
```

## 버킷에서 객체를 삭제합니다

작업 식별자는 `aws.s3.deleteObject`입니다.

버킷에서 단일 객체를 영구적으로 제거합니다. 버전이 지정된 버킷의 경우, 이 작업은 삭제 마커를 삽입하여 `VersionId`이(가) 지정되지 않는 한 객체를 영구적으로 삭제하지 않고 숨깁니다.

다음 표에서는 이 작업의 입력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **입력**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **설명**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS API 호출을 위해 수임할 IAM 역할 ARN입니다.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 액세스 키 ID. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 비밀 액세스 키. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. STS 인증을 위한 임시 세션 토큰입니다. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 타겟 버킷의 AWS 리전입니다.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 객체가 포함된 S3 버킷의 이름입니다.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 삭제할 객체의 전체 키(경로)입니다.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        지도
      </td>

      <td>
        선택 사항입니다. API 호출에 전달할 추가 boto3 파라미터입니다.
      </td>

      <td>
        `{"VersionId": "testVersion", "MFA": "Test"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        목록
      </td>

      <td>
        선택 사항입니다. 작업 출력에서 특정 필드를 추출하기 위한 JQ 선택자입니다.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

추가적인 선택적 boto3 필드(`RequestPayer`, `BypassGovernanceRetention`, `ExpectedBucketOwner`, `IfMatch`, `VersionId`, `MFA`)는 `parameters` 입력을 통해 사용할 수 있습니다. 자세한 내용은 [delete\_object boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html)를 확인하십시오.

다음 표는 이 작업의 출력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **산출**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        물체
      </td>

      <td>
        [delete_object boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html)

        를 확인하십시오.
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        부울
      </td>

      <td>
        `true`

         또는 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        문자열
      </td>

      <td>
        `"Parameter validation failed: Invalid bucket name"`
      </td>
    </tr>
  </tbody>
</table>

**예제 워크플로우**

```yaml
name: aws-s3-delete-object
description: 'Delete an AWS S3 Object'
steps:
  - name: aws_s3_deleteObject_1
    type: action
    action: aws.s3.deleteObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-west-2"
      bucket: "my-bucket"
      key: "path/to/object.txt"
    next: end
```

## 버킷에 객체 추가

작업 식별자는 `aws.s3.putObject`입니다.

버킷에 객체를 추가합니다. Amazon S3는 분산 시스템입니다 ― 동일한 객체에 대해 동시에 여러 쓰기 requests를 수신하면 마지막에 작성된 객체를 제외한 모든 객체를 덮어씁니다.

다음 표에서는 이 작업의 입력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **입력**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **설명**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS API 호출을 위해 수임할 IAM 역할 ARN입니다.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 액세스 키 ID. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 비밀 액세스 키. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. STS 인증을 위한 임시 세션 토큰입니다. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 타겟 버킷의 AWS 리전입니다.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 객체를 업로드할 S3 버킷의 이름입니다.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 버킷에 있는 객체의 전체 키(경로)입니다.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `body`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 업로드할 객체 데이터입니다.
      </td>

      <td>
        `file content`
      </td>
    </tr>

    <tr>
      <td>
        `contentType`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 객체의 표준 MIME 타입입니다.
      </td>

      <td>
        `plain/text`
      </td>
    </tr>

    <tr>
      <td>
        `tagging`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. 객체에 적용할 URL 인코딩된 태그 세트입니다.
      </td>

      <td>
        `key1=value1`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        지도
      </td>

      <td>
        선택 사항입니다. API 호출에 전달할 추가 boto3 파라미터입니다.
      </td>

      <td>
        `{"ServerSideEncryption": "AES256", "Metadata": {"metadata1": "value1"}}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        목록
      </td>

      <td>
        선택 사항입니다. 작업 출력에서 특정 필드를 추출하기 위한 JQ 선택자입니다.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

`parameters` 입력을 통해 추가적인 선택적 boto3 필드를 사용할 수 있습니다. 전체 목록은 [put\_object boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html)를 확인해 주십시오.

다음 표는 이 작업의 출력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **산출**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        물체
      </td>

      <td>
        [put_object boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html)

        를 확인해 주십시오.
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        부울
      </td>

      <td>
        `true`

         또는 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        문자열
      </td>

      <td>
        `"An error occurred (NoSuchBucket) when calling the PutObject operation: The specified bucket does not exist"`
      </td>
    </tr>
  </tbody>
</table>

**예제 워크플로우**

```yaml
name: s3-put-object
description: 'Put an AWS S3 Object'
steps:
  - name: aws_s3_putObject_1
    type: action
    action: aws.s3.putObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
      body: "Hello world"
      contentType: "plain/text"
      tagging: "key1=value1"
    next: end
```

## 버킷에서 객체를 검색합니다.

작업 식별자는 `aws.s3.getObject`입니다.

Amazon S3에서 객체를 가져옵니다. 요청에서 객체의 전체 키 이름을 지정하십시오.

<Callout variant="important">
  객체가 100 KB보다 크면 이 액션은 실패합니다.
</Callout>

다음 표에서는 이 작업의 입력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **입력**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **설명**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS API 호출을 위해 수임할 IAM 역할 ARN입니다.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 액세스 키 ID. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. AWS 비밀 액세스 키. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. STS 인증을 위한 임시 세션 토큰입니다. 시크릿으로 전달하십시오.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 타겟 버킷의 AWS 리전입니다.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 객체가 포함된 S3 버킷의 이름입니다.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        문자열
      </td>

      <td>
        필수 항목입니다. 가져올 객체의 전체 키(경로)입니다.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `versionId`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. 가져올 객체의 버전 ID입니다. 생략하면 현재 버전이 반환됩니다.
      </td>

      <td>
        `some-version-id`
      </td>
    </tr>

    <tr>
      <td>
        `range`
      </td>

      <td>
        문자열
      </td>

      <td>
        선택 사항입니다. 객체의 특정 바이트 범위를 가져오기 위한 HTTP 범위 헤더입니다.
      </td>

      <td>
        `bytes=0-99`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        지도
      </td>

      <td>
        선택 사항입니다. API 호출에 전달할 추가 boto3 파라미터입니다.
      </td>

      <td>
        `{"ChecksumMode": "ENABLED"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        목록
      </td>

      <td>
        선택 사항입니다. 작업 출력에서 특정 필드를 추출하기 위한 JQ 선택자입니다.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

추가적인 선택적 boto3 필드는 `parameters` 입력을 통해 사용할 수 있습니다. 전체 목록은 [get\_object boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html)를 확인하십시오.

다음 표는 이 작업의 출력 필드를 설명합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **산출**
      </th>

      <th>
        **유형**
      </th>

      <th>
        **예시**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        물체
      </td>

      <td>
        [get_object boto3 참조](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html)

        를 확인해 주십시오.
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        부울
      </td>

      <td>
        `true`

         또는 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        문자열
      </td>

      <td>
        `"An error occurred (InvalidArgument) when calling the GetObject operation: Invalid version id specified"`
      </td>
    </tr>
  </tbody>
</table>

**예제 워크플로우**

```yaml
name: s3-get-object
description: 'Get an AWS S3 Object'
steps:
  - name: aws_s3_getObject_1
    type: action
    action: aws.s3.getObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
    next: end
```