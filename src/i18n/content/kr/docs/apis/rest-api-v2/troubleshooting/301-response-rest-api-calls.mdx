---
title: REST API 호출에 대한 301 응답
tags:
  - APIs
  - REST API v2
  - Troubleshooting
metaDescription: 'If you see a 301 error and your New Relic REST API call uses HTTP, change your script or program to HTTPS.'
translationType: human
---

## 문제

REST API 호출은 다음 정보의 일부 또는 전부를 포함할 수 있는 `301` 오류 메시지를 반환합니다.

```
curl -X GET 'http://rpm.newrelic.com/v2/applications/1234567890.json' \
     -H 'X-Api-Key:REDACTED' -i
HTTP/1.1 301 Moved Permanently
Status: 301 Moved Permanently
Location: http://rpm.newrelic.com/v2/applications/1234567890.json
...
You are being redirected https://rpm.newrelic.com/v2/applications/1234567890.json
```

또는

```
curl -X GET 'http://rpm.newrelic.com/v2/applications/1234567890.json' \
     -H 'X-Api-Key:REDACTED'
You are being redirected https://rpm.newrelic.com/v2/applications/1234567890.json
```

## 솔루션

뉴렐릭은 <InlinePopover type="apm"/>로의 모든 트래픽에 HTTPS와 REST API를 요구합니다. 여기에는 인바운드 및 아웃바운드 트래픽이 모두 포함됩니다. REST API 호출이 HTTP를 사용하는 경우 스크립트 또는 프로그램을 HTTPS로 변경하십시오.