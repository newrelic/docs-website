---
title: 평균 브라우저(최종 사용자) 페이지 처리량 예(v2)
tags:
  - APIs
  - REST API v2
  - Browser examples (v2)
metaDescription: How to use the New Relic REST API (v2) to get your average browser page throughput time.
translationType: machine
---

평균 브라우저 처리량은 [**요약** 페이지](/docs/browser/new-relic-browser/getting-started/browser-overview-website-performance-glance#viewing) 의 **브라우저별 처리량** 차트 오른쪽 상단에 나타납니다. 차트에 표시된 시간 범위에서 브라우저 유형별 평균 PPM(분당 페이지 수)입니다.

## 평균 페이지 처리량 [#h2-average]

[시간 범위](/docs/apm/apis/requirements/specifying-time-range-api-v2) (기본값은 마지막 30분)에 대한 [평균](/docs/apm/apis/requirements/calculating-average-metric-values-summarize) 을 얻으려면 다음 명령을 사용하십시오. 이 예의 `${APP_ID}` 및 `${API_KEY}` 변수를 특정 [애플리케이션 ID](/docs/apm/apis/requirements/identification-code) 및 [REST API 키로](/docs/apis/rest-api-v2/requirements/rest-api-key#viewing) 대체해야 합니다.

```
curl -X GET "https://api.newrelic.com/v2/applications/${APP_ID}/metrics/data.json" \
     -H "X-Api-Key:${API_KEY}" -i \
     -d 'names[]=EndUser&values[]=requests_per_minute&summarize=true'
```