---
title: '대시보드 및 차트 가져오기, 내보내기 및 추가'
tags:
  - Query your data
  - Explore and query data
  - Dashboards
  - Charts
metaDescription: Import and export data from dashboards and charts.
freshnessValidatedDate: '2024-02-07T00:00:00.000Z'
translationType: machine
---

[대시보드를](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) 사용하여 시스템 상태를 시각화 및 추적하고, 문제를 해결하고, 추가 단계를 계획할 수 있습니다. 다양한 가져오기 및 내보내기 옵션을 사용하여 팀, 고객 또는 기타 이해관계자와 모든 정보를 공유하는 방법을 알아보세요.

## 대시보드를 JSON으로 가져오기 [#import-json]

코드 레벨에서 기본 대시보드를 수정하려는 경우 대시보드를 JSON으로 가져올 수 있습니다. 예를 들어, 두 개의 기존 대시보드를 병합하여 맞춤형 대시보드를 생성하려는 경우 기본 대시보드의 JSON을 수정한 다음 이를 다시 뉴렐릭으로 가져올 수 있습니다.

대시보드를 JSON으로 가져오려면 다음 안내를 따르세요.

1. <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Dashboards**</DNT> 으)로 이동합니다.

2. Open an existing dashboard&apos;s JSON by clicking the <DNT>**...**</DNT> icon and selecting <DNT>**Download dashboard**</DNT>.

3. Choose <DNT>**JSON**</DNT> from the format options and select <DNT>**Copy to clipboard**</DNT>.

4. <DNT>**Dashboards**</DNT> 페이지로 돌아갑니다.

5. 오른쪽 상단에서 <DNT>**Import dashboard**</DNT>를 클릭합니다.

6. JSON 코드를 붙여넣으세요.

7. 대시보드에 대한 계정 및 권한 설정을 선택합니다. 계정을 설정한 후에는 변경할 수 없지만 [권한은 언제든지 변경할](/docs/query-your-data/explore-query-data/dashboards/manage-your-dashboard/#dash-settings) 수 있습니다.

8. <DNT>**Save**</DNT> 을(를) 클릭합니다.

## 대시보드 내보내기 [#export-dashboards-pdf]

To export a dashboard as a JSON, PDF, or PNG file:

1. In the dashboard&apos;s top right corner, click the <DNT>**...**</DNT> icon and select <DNT>**Download dashboard**</DNT>.

2. Choose <DNT>**JSON**</DNT>, <DNT>**PDF**</DNT>, or <DNT>**PNG**</DNT> as the output format.

3. (Optional) For <DNT>**PDF**</DNT> or <DNT>**PNG**</DNT>, set the export width and height, in pixels. Both default to 1920 x 1080.

4. <DNT>**Download**</DNT> 을(를) 클릭합니다.

New Relic doesn&apos;t support [custom visualizations](/docs/query-your-data/explore-query-data/dashboards/add-custom-visualizations-your-dashboards/) when exporting a dashboard as PDF or PNG via the UI.

To export as PDF or PNG with additional options such as template variable overrides and entity filters, use the [NerdGraph API](#export-api).

## NerdGraph API를 통해 대시보드 및 차트 내보내기 [#export-api]

NerdGraph API를 사용하여 대시보드 및 개별 차트 위젯을 스냅샷 이미지로 프로그래밍 방식으로 내보냅니다. API는 사용자 지정 차원, 템플릿 변수 재정의, 엔티티 필터 등 UI에서 사용할 수 없는 옵션을 지원합니다.

### 대시보드를 PDF 또는 PNG로 내보내기 [#api-dashboard-snapshot]

`dashboardCreateSnapshotUrl` 뮤테이션은 전체 대시보드 페이지에 대한 스냅샷 URL을 생성합니다.

<Callout variant="tip">
  `guid` 파라미터에는 대시보드 GUID가 아닌 **페이지** GUID가 필요합니다. 대시보드에는 여러 페이지가 있을 수 있습니다. 페이지 GUID를 찾으려면 `entitySearch` 쿼리를 사용하거나 대시보드 페이지를 열고 <DNT>**...**</DNT> 메뉴에서 <DNT>**Manage JSON**</DNT> 을(를) 선택합니다.

  ```graphql
  {
    actor {
      entitySearch(query: "parentId ='YOUR_DASHBOARD_GUID' AND tags.isDashboardPage = 'true'") {
        results {
          entities {
            guid
            name
          }
        }
      }
    }
  }
  ```
</Callout>

**뮤테이션 스키마:**

```graphql
mutation {
  dashboardCreateSnapshotUrl(
    guid: EntityGuid!
    params: SnapshotUrlInput
  ): String
}

input SnapshotUrlInput {
  timeWindow: SnapshotUrlTimeWindowInput
  display: SnapshotUrlDisplayInput
  variables: [SnapshotUrlVariableInput!]
  filter: String
  format: SnapshotUrlFormat
}

input SnapshotUrlTimeWindowInput {
  beginTime: EpochMilliseconds
  endTime: EpochMilliseconds
  duration: Milliseconds
}

input SnapshotUrlDisplayInput {
  width: Int
  height: Int
}

input SnapshotUrlVariableInput {
  name: String!
  values: [String!]!
}

enum SnapshotUrlFormat {
  PDF
  PNG
}
```

**매개변수 참조:**

<table>
  <thead>
    <tr>
      <th>
        매개변수
      </th>

      <th>
        유형
      </th>

      <th>
        기본값
      </th>

      <th>
        설명
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `guid`
      </td>

      <td>
        `EntityGuid!`
      </td>

      <td>
        —
      </td>

      <td>
        필수의. 대시보드 페이지의 엔티티 GUID입니다.
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.beginTime`
      </td>

      <td>
        `EpochMilliseconds`
      </td>

      <td>
        대시보드의 현재 시간 창
      </td>

      <td>
        데이터 창의 시작(epoch ms)입니다.
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.endTime`
      </td>

      <td>
        `EpochMilliseconds`
      </td>

      <td>
        대시보드의 현재 시간 창
      </td>

      <td>
        데이터 창의 끝(epoch ms).
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.duration`
      </td>

      <td>
        `Milliseconds`
      </td>

      <td>
        —
      </td>

      <td>
        상대적 시간 창, 

        `beginTime`

        /

        `endTime`

        의 대안.
      </td>
    </tr>

    <tr>
      <td>
        `params.display.width`
      </td>

      <td>
        `Int`
      </td>

      <td>
        1920
      </td>

      <td>
        스냅샷의 픽셀 단위 너비입니다.
      </td>
    </tr>

    <tr>
      <td>
        `params.display.height`
      </td>

      <td>
        `Int`
      </td>

      <td>
        1080
      </td>

      <td>
        스냅샷의 픽셀 단위 높이입니다.
      </td>
    </tr>

    <tr>
      <td>
        `params.variables`
      </td>

      <td>
        `[SnapshotUrlVariableInput!]`
      </td>

      <td>
        대시보드의 현재 변수 값
      </td>

      <td>
        대시보드 템플릿 변수에 대한 특정 값을 고정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `params.filter`
      </td>

      <td>
        `String`
      </td>

      <td>
        필터 없음
      </td>

      <td>
        스냅샷 시간에 적용된 NRQL 스타일 엔티티 필터입니다.
      </td>
    </tr>

    <tr>
      <td>
        `params.format`
      </td>

      <td>
        `SnapshotUrlFormat`
      </td>

      <td>
        `PDF`
      </td>

      <td>
        출력 형식: 

        `PDF`

         또는 

        `PNG`

        .
      </td>
    </tr>
  </tbody>
</table>

**예:**

<CollapserGroup>
  <Collapser id="basic-export" title="기본 내보내기(PDF, 기본 설정)">
    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
      )
    }
    ```
  </Collapser>

  <Collapser id="png-custom-dimensions" title="사용자 지정 차원을 사용하여 PNG로 내보내기">
    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
        params: {
          format: PNG
          display: {
            width: 1920
            height: 1080
          }
        }
      )
    }
    ```
  </Collapser>

  <Collapser id="variables-filter" title="변수 재정의 및 필터를 사용하여 내보내기">
    이를 사용하여 특정 변수 값과 필터를 스냅샷에 고정할 수 있으며, 예약된 보고서 자동화에 유용합니다.

    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
        params: {
          variables: [
            { name: "environment", values: ["production"] }
            { name: "region", values: ["us-east-1", "eu-west-1"] }
          ]
          filter: "env = 'production' AND service = 'checkout'"
        }
      )
    }
    ```
  </Collapser>
</CollapserGroup>

<Callout variant="important">
  스냅샷 URL은 인증 없이 공개적으로 액세스할 수 있으며 3개월 후에 만료됩니다. 조직의 데이터 정책에 따라 주의해서 공유하십시오.
</Callout>

### 차트 이미지 가져오기 [#api-widget-snapshot]

The `dashboardWidgetCreateSnapshotUrl` mutation generates a PNG snapshot URL for a single chart widget. The widget doesn&apos;t need to exist on a dashboard — you can define it inline, which makes this useful for generating chart images programmatically.

**뮤테이션 스키마:**

```graphql
mutation {
  dashboardWidgetCreateSnapshotUrl(
    widget: DeclarativeUiWidget!
  ): WidgetSnapshotResult
}

type WidgetSnapshotResult {
  url: String!
}
```

`widget` 파라미터는 `DeclarativeUiWidget` 을(를) 허용합니다 — 세 개의 중첩된 계층이 있는 JSON 객체:

```
declarative/widget envelope
└── widget node        ← title and widget-level props
    └── visualization  ← visualization type and configuration
```

```json
{
  "type": "declarative/widget",
  "version": 1,
  "content": {
    "type": "widget",
    "props": {
      "title": "My Chart Title"
    },
    "content": {
      "type": "visualization",
      "id": "viz.line",
      "props": {
        // visualization-specific config (nrqlQueries, colors, thresholds, etc.)
      }
    }
  }
}
```

JSON으로 내보낸 대시보드에서 위젯 정의를 빌드하는 경우 다음 필드 매핑을 사용하십시오:

<table>
  <thead>
    <tr>
      <th>
        대시보드 JSON 필드
      </th>

      <th>
        DeclarativeUiWidget 필드
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `title`
      </td>

      <td>
        `content.props.title`
      </td>
    </tr>

    <tr>
      <td>
        `visualization.id`
      </td>

      <td>
        `content.content.id`
      </td>
    </tr>

    <tr>
      <td>
        `rawConfiguration.*`
      </td>

      <td>
        `content.content.props.*`

         (동일한 필드 이름)
      </td>
    </tr>

    <tr>
      <td>
        `layout`

        , 

        `id`

         , 

        `linkedEntityGuids`
      </td>

      <td>
        포함되지 않음 — 독립 실행형 스냅샷에는 필요하지 않음
      </td>
    </tr>
  </tbody>
</table>

**예:**

<CollapserGroup>
  <Collapser id="line-chart-snapshot" title="선 차트 내보내기">
    ```graphql
    mutation {
      dashboardWidgetCreateSnapshotUrl(
        widget: {
          version: 1
          type: "declarative/widget"
          content: {
            type: "widget"
            props: { title: "Throughput by environment" }
            content: {
              type: "visualization"
              id: "viz.line"
              props: {
                nrqlQueries: [
                  {
                    accountIds: [YOUR_ACCOUNT_ID]
                    query: "SELECT count(*) FROM Transaction TIMESERIES"
                  }
                ]
                legend: { enabled: true }
              }
            }
          }
        }
      ) {
        url
      }
    }
    ```
  </Collapser>

  <Collapser id="billboard-snapshot" title="임계값이 있는 빌보드 차트 내보내기">
    ```graphql
    mutation {
      dashboardWidgetCreateSnapshotUrl(
        widget: {
          version: 1
          type: "declarative/widget"
          content: {
            type: "widget"
            props: { title: "Error rate" }
            content: {
              type: "visualization"
              id: "viz.billboard"
              props: {
                nrqlQueries: [
                  {
                    accountIds: [YOUR_ACCOUNT_ID]
                    query: "SELECT percentage(count(*), WHERE error IS true) AS 'Error rate' FROM Transaction SINCE 1 hour ago"
                  }
                ]
                thresholds: [
                  { alertSeverity: "WARNING", value: 1 }
                  { alertSeverity: "CRITICAL", value: 5 }
                ]
              }
            }
          }
        }
      ) {
        url
      }
    }
    ```
  </Collapser>
</CollapserGroup>

<Callout variant="tip">
  The legacy `actor.account.nrql.staticChartUrl` field in NerdGraph remains available but doesn&apos;t support the latest visualization features such as thresholds, color formatting, and newer chart types. `dashboardWidgetCreateSnapshotUrl` is the recommended approach going forward.
</Callout>

<Callout variant="important">
  `dashboardWidgetCreateSnapshotUrl` generates PNG image snapshot URLs. These are publicly accessible without authentication and expire after 3 months.
</Callout>

## 차트를 CSV로 내보내기 [#export-charts-csv]

대시보드의 표 차트를 CSV 파일로 내보내려면 차트 오른쪽 상단에서 <DNT>**...**</DNT> 클릭하고 <DNT>**Export as CSV**</DNT> 선택합니다.

<img title="Export chart as CSV" alt="Screenshot of how to export a table chart as a CSV file" src="/images/dashboards_screenshot-crop_export-csv.webp" />

## 차트 쿼리 및 탐색 [#explore-chart-data]

차트로 수행할 수 있는 작업에 대한 자세한 내용은 [쿼리 및 차트 옵션을](/docs/new-relic-solutions/new-relic-one/ui-data/basic-ui-features/#data-analysis) 참조하세요.