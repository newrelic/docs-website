---
title: NRQL 예측
tags:
  - Prediction on a chart
  - Forecasting on a chart
  - NRQL Predictions
  - Forecasting
metaDescription: Use NRQL Predictions to predict future trends and potential performance issues.
freshnessValidatedDate: never
translationType: machine
---

<Callout title="시사">
  이 기능은 아직 개발 중이지만 꼭 사용해 보시기 바랍니다!

  이 기능은 현재 [출시 전 정책](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy) 에 따라 미리보기 프로그램의 일부로 제공됩니다.
</Callout>

<DNT>**NRQL Predictions**</DNT> 뉴렐릭에서는 지표의 이력 데이터를 사용하여 미래 추세를 예측하고 인사이트에서 지표가 미래에 어떻게 행동할지 알려줍니다. 이러한 사전 예방적 접근 방식은 추세를 시각화하고 잠재적인 문제를 예상하는 데 도움이 되며, 적절한 개입을 통해 최적의 시스템 성능을 유지할 수 있습니다. <DNT>**NRQL Predictions**</DNT> 자체는 잠재적인 문제를 식별하지 못하지만 추세를 평가하고 미래의 과제를 계획하는 데 필요한 정보를 제공합니다.

잠재적인 문제를 자동으로 식별하려면 현재 공개 미리 보기로 제공되는 <DNT>**[Predictive Alerts](/docs/query-your-data/explore-query-data/use-charts/nrql-predictions)**</DNT> 기능에 등록하세요. <DNT>**Predictive Alerts**</DNT> <DNT>**NRQL Predictions**</DNT> 와 함께 작동하여 예상 추세가 잠재적인 문제를 나타낼 때 자동으로 알림을 트리거하여 시스템을 사전에 관리할 수 있도록 합니다.

## 가격 [#pricing]

<DNT>**NRQL Predictions**</DNT> 의 사용은 귀하의 계정과 관련된 가격 모델에 적용되는 주문에 따라 미리 보기 중에 청구될 수 있습니다. 이 기능의 사용과 관련된 뉴렐릭 쿼리 언어 쿼리(NRQLS)는 핵심 컴퓨트 제품으로 청구됩니다. 이 기능이 일반적으로 제공되면, 귀하의 주문에 따라 사용료가 청구됩니다.

가격에 대한 자세한 내용은 당사 영업팀에 문의하시거나 주문서를 참조하세요.

## 등록 [#enrollment]

<DNT>**NRQL Predictions**</DNT> 기능에 등록하려면 <DNT>**[one.newrelic.com &gt; Administration](https://one.newrelic.com//admin-portal) &amp;gt; Preview &amp;amp; Trials**</DNT> 으로 이동하여 <DNT>**NRQL predictions**</DNT> 활성화하세요.

## 선 또는 영역 차트에 예측 추가 [#add-prediction]

다음 두 가지 방법으로 선 및 영역 차트에 예측을 추가할 수 있습니다.

<CollapserGroup>
  <Collapser id="prediction-on-existing-chart" title="기존 차트에 예측을 추가합니다.">
    기존 차트에 예측을 추가하려면 다음 단계를 따르세요.

    <Steps>
      <Step>
        ### 라인 또는 영역 차트에 액세스 [#access-a-chart]

        1. [one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) 으로 이동하세요.
        2. 선형 또는 영역형 차트에 예측을 추가하려면 <DNT>**Dashboards**</DNT> 에서 선택하거나 <DNT>**Metrics and events**</DNT> 에서 엽니다.
      </Step>

      <Step>
        ### 예측 추가 [#add-prediction]

        예측을 추가하려면 <Icon name="fe-more-horizontal" /> 메뉴에서 <DNT>**Predict trend**</DNT> 선택하세요. **Data Explorer** 에서는 예측 추세가 차트에 표시되어 미래 추세를 예측합니다. 예측 범위는 자동으로 쿼리 창의 20%로 설정되고 회색으로 강조 표시됩니다. 그래프는 예측 범위 내에서 점선으로 나타납니다.

        `PREDICT` 키워드가 차트 쿼리에 추가됩니다. 귀하의 요구 사항에 맞게 [쿼리를 구체화](#customize-prediction-range) 하고 실행하여 최신 추세를 얻을 수 있습니다.

        <Callout variant="tip">
          영역형 차트에 예측을 추가하면 차트 유형이 예측이 포함된 선형 차트로 변경됩니다.
        </Callout>
      </Step>

      <Step>
        ### 대시보드에 추가 [#add-to-dashboard]

        1. 예측이 포함된 차트를 대시보드에 추가하려면 <DNT>**Add to dashboard**</DNT> 클릭하세요.
        2. 위젯 이름을 입력하세요.
        3. 위젯을 추가할 대시보드를 선택하거나 해당 대시보드에 대한 새 대시보드를 만듭니다.

        이제 선택한 대시보드에서 예측 차트를 볼 수 있습니다.
      </Step>
    </Steps>
  </Collapser>

  <Collapser id="use-predict-clause" title="NRQL 쿼리에서 `PREDICT` 절을 사용합니다.">
    NRQL 쿼리에 `PREDICT` 절을 추가하려면 다음 단계를 따르세요.

    <Steps>
      <Step>
        ### NRQL 쿼리 만들기 [#create-query]

        1. [one.newrelic.com](https://one.newrelic.com) 으로 이동하여 쿼리를 만들려는 계정을 선택하세요.
        2. <DNT>**Data Explorer**</DNT> 열려면 <DNT>**Query Your Data**</DNT> 선택하세요.
        3. NRQL 구문을 사용하여 시계열 쿼리를 작성합니다. 자세한 내용은 [NRQL 구문 문서를](https://docs.newrelic.com/docs/nrql/nrql-syntax-clauses-functions) 참조하세요.

        <Callout variant="important">
          예측이 이러한 경우 지원되지 않으므로 쿼리에 [지표 타임슬라이스](/docs/data-analysis/metrics/analyze-your-metrics/data-collection-metric-timeslice-event-data#timeslice-data) [데이터](/docs/nrql/using-nrql/subqueries-in-nrql/) , 절 [`COMPARE WITH`](/docs/nrql/nrql-syntax-clauses-functions/#sel-compare) 또는 하위 쿼리 내의 중첩 집계가 포함되지 [않도록 주의하세요.](/docs/nrql/using-nrql/nested-aggregation-make-ordered-computations-single-query)
        </Callout>
      </Step>

      <Step>
        ### 예측 추가 [#add-prediction-to-query]

        차트에 예측을 추가하려면 쿼리에 `PREDICT` 절을 추가하고 실행합니다. 예측 범위는 자동으로 쿼리 창의 20%로 설정되고 회색으로 강조 표시됩니다. 그래프는 예측 범위 내에서 점선으로 나타납니다.

        귀하의 요구 사항에 맞게 [쿼리를 더욱 세분화](#customize-prediction-range) 할 수 있습니다.
      </Step>

      <Step>
        ### 대시보드에 추가 [#add-query-to-dashboard]

        1. 예측이 포함된 차트를 대시보드에 추가하려면 <DNT>**Add to dashboard**</DNT> 클릭하세요.
        2. 위젯 이름을 입력하세요.
        3. 위젯을 추가할 대시보드를 선택하거나 해당 대시보드에 대한 새 대시보드를 만듭니다.

        이제 선택한 대시보드에서 예측 차트를 볼 수 있습니다.
      </Step>
    </Steps>
  </Collapser>
</CollapserGroup>

## 지원되는 예측 알고리즘 [#supported-algorithm]

현재 <DNT>**NRQL predictions**</DNT> 계절 및 비계절 시계열 모두에 대해 <DNT>**Holt-Winters**</DNT> 알고리즘만 지원합니다. 이는 예측 및 예측 작업에 일반적으로 사용되는 표준 알고리즘입니다. 우리는 Holt-Winters 모델에 대해 시간별, 일별, 주별 계절 길이를 지원합니다.

## 기본 동작 [#defaults]

쿼리의 `PREDICT` 절에는 다음과 같은 기본 동작이 적용됩니다.

* **계절성**: 데이터의 계절성을 자동으로 감지하고 동일한 계절 길이를 사용하여 예측 모델을 구성합니다. 계절성이 발견되지 않으면 비계절 모델을 구성합니다.
* **예측 창**: 쿼리 창에 지정된 총 시간 범위의 20%를 예상합니다.
* **학습용 이력 데이터**: 현재 쿼리 창과 이전 두 창의 이력 데이터를 사용하여 예측을 생성합니다.
* **시간 간격**: 데이터 투영의 일관성을 보장하기 위해 쿼리 창에서 시계열의 데이터 포인트 간격에 맞춥니다.

NRQL 쿼리에서 `PREDICT` 사용하는 예:

```sql
FROM Transaction SELECT count(*) WHERE error IS TRUE TIMESERIES PREDICT
```

차트의 기본 예측은 다음과 같습니다.

<img title="Default prediction" alt="Default prediction" src="/images/nrql-prediction-default.webp" />

<figcaption>
  <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &amp;gt; dashboards**</DNT>: 기본 예측.
</figcaption>

## 예측 모델 사용자 정의 [#customize-prediction-range]

학습 모델의 하이퍼파라미터를 수동으로 설정하고 `USING` 및 `BY` 키워드를 추가하여 예측을 사용자 정의할 수 있습니다. NRQL에 대해 알아보려면 [NRQL 설명서를](/docs/nrql/get-started/introduction-nrql-new-relics-query-language) 참조하세요.

<CollapserGroup>
  <Collapser id="hyperparameters" title="모델 하이퍼파라미터를 설정합니다.">
    현재 우리가 지원하는 유일한 예측 알고리즘은 <DNT>**Holt-Winters**</DNT> 이며, 이 알고리즘에는 예측을 미세하게 조정하는 데 도움이 되는 여러 가지 하이퍼 매개변수가 함께 제공됩니다. 다음 표를 참조하여 각 하이퍼파라미터의 동작을 이해하고 사용 사례에 맞는 하이퍼파라미터를 선택하세요.

    쿼리를 수정하려면 `PREDICT` 절 뒤에 `holtwinters(<hyperparameters>: <value>)` 추가하세요.

    <table id="hyperparameters">
      <thead>
        <tr>
          <th colSpan="3" />
        </tr>

        <tr>
          <th>
            하이퍼파라미터
          </th>

          <th>
            값
          </th>

          <th>
            행동
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td rowspan="3" style="width: 300px;">
            `seasonality`
          </td>

          <td>
            `<season length>`
          </td>

          <td>
            시계열의 계절성을 특정 시즌 길이로 설정합니다. 시즌 길이가 `integer unit` 시간인지 확인하세요.\
            **예**: `1 day` 또는 `3 hours`
          </td>
        </tr>

        <tr>
          <td>
            `AUTO`
          </td>

          <td>
            시계열의 계절성을 자동으로 감지하고 식별된 계절성을 기반으로 예측을 생성합니다.
          </td>
        </tr>

        <tr>
          <td>
            `NONE`
          </td>

          <td>
            시계열의 계절성을 비활성화하고 비계절적 예측을 생성합니다.

            <Callout variant="important">
              `seasonality: NONE` is not compatible with the `gamma` hyperparameter, which sets the season smoothing factor.
            </Callout>
          </td>
        </tr>

        <tr>
          <td>
            `alpha`
          </td>

          <td>
            0에서 1까지
          </td>

          <td>
            레벨 평활화 요소를 설정합니다.

            * **낮은 값**: 이력 데이터에 더 많은 가중치를 둡니다.
            * **값이 높을수록** 최근 데이터에 더 많은 가중치를 둡니다.
          </td>
        </tr>

        <tr>
          <td>
            `beta`
          </td>

          <td>
            0에서 1까지
          </td>

          <td>
            추세 평활화 요소를 설정합니다.

            * **낮은 값**: 이력 데이터에 더 많은 가중치를 둡니다.
            * **값이 높을수록** 최근 데이터에 더 많은 가중치를 둡니다.
          </td>
        </tr>

        <tr>
          <td>
            `gamma`
          </td>

          <td>
            0에서 1까지
          </td>

          <td>
            계절 평활화 요소를 설정합니다.

            * **낮은 값**: 이력 데이터에 더 많은 가중치를 둡니다.
            * **값이 높을수록** 최근 데이터에 더 많은 가중치를 둡니다.
          </td>
        </tr>

        <tr>
          <td>
            `phi`
          </td>

          <td>
            0.98 ～ 1
          </td>

          <td>
            추세 감쇠 패턴을 설정합니다.

            * **값이 낮을수록** 예측 곡선에 대한 감쇠 효과가 커지고 점차 평평한 곡선으로 평탄해집니다.
            * **값이 높을수록** 예측 곡선에 대한 감쇠 효과가 약해집니다.
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser
    id="by"
    title={<><InlineCode>
      BY
    </InlineCode> 키워드를 추가합니다.</>
    }
  >
    `BY` 키워드를 사용하여 모델이 예측할 시간을 미리 설정합니다. 예를 들어, `PREDICT BY 3 hours` 시간 계열의 최신 지점으로부터 3시간 후의 미래를 예측합니다. 시간 범위가 `integer unit` 시간인지 확인하세요.
  </Collapser>

  <Collapser
    id="using"
    title={<><InlineCode>
      USING
    </InlineCode> 키워드를 추가합니다.</>
    }
  >
    `USING` 키워드를 사용하여 모델을 학습하는 데 사용할 역량 데이터의 양을 지정합니다. 예를 들어, `PREDICT USING 1 day` 이전 날의 데이터와 함께 쿼리 창을 사용하여 모델을 학습합니다. 시간 범위가 `integer unit` 시간인지 확인하세요.
  </Collapser>
</CollapserGroup>

`PREDICT` 하이퍼파라미터, `BY` 및 `USING` 키워드와 함께 사용하는 예:

```sql
FROM Transaction SELECT count(*) WHERE error IS TRUE TIMESERIES PREDICT holtwinters(seasonality: AUTO, alpha: 0.2) BY 1 hour USING 2 hours
```

이 쿼리는 계절성을 자동 감지로 설정하고, 레벨 평활화 요소를 조정하여 이력데이터를 강조합니다. 이 기능은 1시간 뒤의 미래를 예측하고 이전 2시간의 이력 데이터와 쿼리 창 길이를 사용하여 모델을 학습합니다. 차트에는 사용자 정의된 예측이 다음과 같이 표시됩니다.

<img title="Custom prediction" alt="Custom prediction" src="/images/nrql-prediction-custom.webp" />

<figcaption>
  <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &amp;gt; dashboards**</DNT>: 맞춤형 예측.
</figcaption>