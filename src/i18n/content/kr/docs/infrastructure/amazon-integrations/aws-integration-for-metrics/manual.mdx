---
title: AWS 콘솔에서 수동 통합
tags:
  - Integrate manually
  - Manual integration
  - AWS metric integration
  - Amazon integrations
  - AWS integrations
metaDescription: Integrate New Relic with AWS services manually from the AWS console.
freshnessValidatedDate: never
translationType: machine
---

For a seamless integration of your AWS account with New Relic, [integration via CloudFormation and CloudWatch metric stream](/docs/infrastructure/amazon-integrations/aws-integration-for-metrics/via-cloudformation) is the recommended approach. If you can&apos;t use CloudFormation templates and don&apos;t want to use Terraform, follow this procedure to integrate manually from the AWS console.

<Callout variant="tip">
  * If you manage multiple AWS accounts, [connect each account to a single New Relic account](/docs/infrastructure/amazon-integrations/manage-aws-data/aws-multi-account).
  * If you manage multiple regions within those accounts, create a separate [Kinesis Data Firehose](#create-firehose) for each region.
</Callout>

<Steps>
  <Step>
    ## 전제 조건 [#prerequisites]

    AWS 계정을 뉴렐릭과 통합하기 전에 다음 필수 조건을 충족하는지 확인하세요.

    * **A 뉴렐릭 [Ingest - 볼륨 키 및 사용자 키](https://one.newrelic.com/api-keys)**: AWS 서비스를 뉴렐릭 계정에 연결하려면 이 키가 필요합니다.
    * **[Permissions](https://docs.aws.amazon.com/ARG/latest/userguide/gettingstarted-prereqs-permissions.html)** to deploy new AWS resources and **[IAM roles](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles.html)**: You&apos;ll need this permission for your AWS account.
    * 클라우드 통합을 위한 **[권한](/docs/accounts/accounts-billing/new-relic-one-user-management/user-permissions/#infrastructure)**: 뉴렐릭 계정에 이러한 권한이 필요합니다.
  </Step>

  <Step>
    ## Create a Kinesis Data Firehose delivery stream [#create-firehose]

    In the AWS console, set the following destination parameters for the delivery stream:

    * 소스: <DNT>Direct PUT</DNT> 또는 기타 소스

    * 목적지: New Relic

    * Data transformation: <DNT>Disabled</DNT>

    * Record format conversion: <DNT>Disabled</DNT>

    * 다음 설정이 정의되어 있는지 확인하십시오.

      * 뉴렐릭 설정(대상 설정):

        * HTTP 엔드포인트 URL:

          * 미국 데이터 센터: `https://aws-api.newrelic.com/cloudwatch-metrics/v1`
          * EU 데이터 센터: `https://aws-api.eu01.nr-data.net/cloudwatch-metrics/v1`
          * JP 데이터 센터: `https://aws-api.jp.nr-data.net/cloudwatch-metrics/v1`

        * API 키:<InlinePopover type="licenseKey" />

        * 콘텐츠 인코딩: `GZIP`

        * 재시도 기간: `60`

        * 뉴렐릭 버퍼 조건:

          * 버퍼 크기: `1 MB`
          * 버퍼 간격: `60 (seconds)`

      * S3 backup mode: <DNT>Failed data only</DNT>

      * S3 백업 버킷: 전송에 실패한 메트릭을 저장할 버킷을 선택하거나 새로 만듭니다.

      * 고급 설정:
        * 서비스 액세스: IAM 역할에 대한 권한을 제공합니다:
          * <DNT>Create or update IAM role</DNT>
  </Step>

  <Step>
    ## CloudWatch Metric Streams 생성 [#create-metric-stream]

    1. In the AWS console, go to <DNT>**CloudWatch service**</DNT>, select <DNT>**Streams**</DNT> under the <DNT>**Metrics**</DNT> menu, and click <DNT>**Create metric stream**</DNT>.
    2. To control which AWS services send metrics to New Relic, configure inclusion and exclusion filters.
    3. From the Firehose delivery stream list, select the stream you created, then enter a meaningful name for the metric stream, for example, `newrelic-metric-stream`.
    4. 출력 형식 필드에서 `Open Telemetry 0.7` 또는 `Open Telemetry 1.0`을(를) 선택합니다. JSON은 지원되지 않습니다.
  </Step>

  <Step>
    ## Connect your AWS account to New Relic [#connect-aws-account]

    1. 뉴렐릭에서 [<DNT>**Infrastructure &gt; AWS**</DNT>](https://one.newrelic.com/infra/infrastructure/cloud-aws)(으)로 이동하세요.

    2. 올바른 계정을 선택하세요.

    3. <DNT>**Set up integration**</DNT> 을(를) 클릭합니다.

    4. 각 화면에서 해당되는 필드를 작성합니다. 이 통합을 위해 다음 옵션을 선택하세요:

       * <DNT>**Choose Data type(s)**</DNT>: <DNT>Metrics</DNT>
       * <DNT>**Choose a setup method**</DNT>: <DNT>Manually integrate your AWS account</DNT>

    5. <DNT>**Select services**</DNT> 화면에서 필요한 경우 <DNT>**Show resources not supported by CloudWatch Metric Streams**</DNT> 을(를) 전환하고 모니터할 서비스를 선택한 후 계속합니다.

    6. <DNT>**Choose data integration type**</DNT> 화면에서 다음 옵션 중에서 선택할 수 있습니다:

       * **Real-time AWS monitoring (Recommended)**: This option is for services that support CloudWatch Metric Streams.
       * **API 폴링**: 이 옵션은 CloudWatch Metric Streams에서 지원하지 않는 서비스를 위한 것입니다.

    7. From the <DNT>**Create role**</DNT> page to the <DNT>**Configure budgets policy**</DNT> page, follow the on-screen instructions and complete the steps in the AWS console.
  </Step>

  <Step>
    ## 데이터 검증 [#validate]

    메트릭 스트림에서 데이터를 수신하고 있는지 확인하려면:

    1. In New Relic, go to <DNT>**[one.newrelic.com &gt; All capabilities &gt; Infrastructure &gt; AWS](https://one.newrelic.com/infra/infrastructure/cloud-aws)**</DNT>, search for your connected AWS account, and look for **Stream** accounts.
    2. 계정 상태 대시보드에서 뉴렐릭이 오류와 수집된 네임스페이스 및 메트릭 수를 포함한 메트릭 데이터를 수신하고 있는지 확인합니다.
    3. 특정 메트릭을 탐색하려면 분석하려는 메트릭 세트에 대한 쿼리를 작성합니다.

    뉴렐릭이 새로운 리소스를 감지하고 이를 합성하는 데 몇 분이 걸릴 수 있습니다.

    <Callout variant="tip">
      AWS CloudWatch metrics for global services, such as AWS Billing, are only available in the <DNT>**us-east-1**</DNT> region. Make sure there&apos;s an active CloudWatch metric stream configured in that region.
    </Callout>
  </Step>
</Steps>

## 관련 주제 [#related-topics]

<DocTiles>
  <DocTile title="여러 AWS 계정에서 데이터 수집" path="/docs/infrastructure/amazon-integrations/manage-aws-data/aws-multi-account/">
    여러 AWS 계정에서 데이터를 수집하는 방법을 알아보세요.
  </DocTile>

  <DocTile title="AWS 통합 지표" path="/docs/infrastructure/amazon-integrations/manage-aws-data/aws-integrations-metrics/">
    CloudWatch Metric Streams의 지표를 탐색하고 사용하세요.
  </DocTile>

  <DocTile title="CloudFormation 및 CWMS를 통한 AWS 통합" path="/docs/infrastructure/amazon-integrations/aws-integration-for-metrics/via-cloudformation-cwms">
    CloudFormation 및 CloudWatch Metric Streams 사용하여 AWS 서비스를 뉴렐릭과 통합합니다.
  </DocTile>

  <DocTile title="CloudFormation 및 API 폴링을 통한 AWS 통합" path="/docs/infrastructure/amazon-integrations/aws-integration-for-metrics/via-cloudformation-and-api-polling">
    CloudFormation 및 API 폴링을 사용하여 AWS 서비스를 뉴렐릭과 통합하세요.
  </DocTile>

  <DocTile title="Terraform을 통한 AWS 통합" path="/docs/infrastructure/amazon-integrations/aws-integration-for-metrics/terraform">
    Terraform을 사용하여 AWS 서비스를 뉴렐릭과 통합합니다.
  </DocTile>
</DocTiles>