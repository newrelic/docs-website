---
title: Podman을 사용한 듀얼 스택용 신세틱스 설치
tags:
  - Synthetics
  - Synthetic monitoring
  - Private locations
metaDescription: Install and configure New Relic Synthetics for Dual Stack environments using Podman to support both IPv4 and IPv6 testing.
freshnessValidatedDate: never
translationType: machine
---

이 가이드는 Podman을 사용하여 프라이빗 로케이션 환경에서 듀얼 스택(IPv4 및 IPv6) 테스트를 지원하기 위한 뉴렐릭 Synthetics 작업 관리자의 설정 및 배포를 다룹니다.

## 전제 조건 [#prerequisites]

시작하기 전에 다음 사항이 있는지 확인하세요.

* <DNT>**Podman**</DNT>: 호스트 인스턴스에 Podman이 설치되어 실행 중이어야 합니다

* <DNT>**slirp4netns**</DNT>: 루트리스 컨테이너의 IPv6 지원에 필요합니다(버전 1.0.0 이상, 일반적으로 Podman과 함께 설치됨)

* <DNT>**Permissions**</DNT>: 루트리스 컨테이너를 실행하려면 적절한 액세스 권한이 있어야 합니다.

* <DNT>**Public images**</DNT>: 듀얼 스택 지원을 위해서는 도커 허브에서 사용할 수 있는 다음 이미지 버전 이상이 필요합니다:

  * [작업 관리자 `519` 이상](https://docs.newrelic.com/docs/release-notes/synthetics-release-notes/job-manager-release-notes/job-manager-release-519/): `newrelic/synthetics-job-manager`
  * [Ping 런타임 `1.65.0` 이상](https://docs.newrelic.com/docs/release-notes/synthetics-release-notes/ping-runtime-release-notes/ping-runtime-1.65.0/): `newrelic/synthetics-ping-runtime`
  * [Node API 런타임 `rc1.15` 이상](https://docs.newrelic.com/docs/release-notes/synthetics-release-notes/node-api-runtime-release-notes/node-api-runtime-rc1.15/): `newrelic/synthetics-node-api-runtime`
  * [노드 브라우저 런타임 `rc1.15` 이상](https://docs.newrelic.com/docs/release-notes/synthetics-release-notes/node-browser-runtime-release-notes/node-browser-runtime-rc1.15/): `newrelic/synthetics-node-browser-runtime`

<Steps>
  <Step>
    ## 인프라 검증 [#validate-infrastructure]

    계속 진행하기 전에 호스트 머신과 네트워크가 듀얼 스택 네트워킹으로 구성되어 있는지 확인하십시오.

    `ping6 ipv6.google.com` 또는 `ping -6 ipv6.google.com`, 또는 사용 중인 머신에 해당하는 명령을 실행하여 IPv6 연결을 테스트합니다.

    <Callout variant="important">
      테스트가 실패하면 이 설치를 계속하기 전에 VPC, 서브넷 및 보안 그룹이 IPv6 트래픽을 허용하도록 구성되어 있는지 확인하십시오. 사용자 환경은 듀얼 스택 작동을 위해 IPv4와 IPv6를 모두 지원해야 합니다.
    </Callout>
  </Step>

  <Step>
    ## Podman 환경 설정 [#setup-podman]

    [Podman 의존성/종속성 가이드](/docs/synthetics/synthetic-monitoring/private-locations/install-job-manager/#podman-dependencies)를 따라 Podman 환경을 설정합니다.
  </Step>

  <Step>
    ## Podman 파드 생성 [#create-pod]

    호스트 머신 IP 주소로 파드를 생성합니다:

    ```bash
    podman pod create --network slirp4netns --name SYNTHETICS --add-host=podman.service:IP_ADDRESS
    ```

    `IP_ADDRESS` 를 호스트 머신의 IP 주소로 교체합니다.
  </Step>

  <Step>
    ## 신세틱 작업 관리자를 출시하십시오. [#launch-job-manager]

    <Callout variant="tip">
      `<YOUR_PRIVATE_LOCATION_KEY>` 을 뉴렐릭 UI의 실제 프라이빗 로케이션 키로 바꾸십시오.
    </Callout>

    ```bash
    podman run \
      -e PRIVATE_LOCATION_KEY=<YOUR_PRIVATE_LOCATION_KEY> \
      --pod SYNTHETICS \
      -d \
      --restart unless-stopped \
      -e "CONTAINER_ENGINE=PODMAN" \
      -e "PODMAN_API_SERVICE_PORT=8000" \
      -e "PODMAN_POD_NAME=SYNTHETICS" \
      newrelic/synthetics-job-manager
    ```

    이제 신세틱 작업 관리자가 듀얼 스택 지원이 활성화된 상태로 실행되어 모니터가 IPv4 및 IPv6 엔드포인트를 모두 테스트할 수 있습니다.
  </Step>

  <Step>
    ## 설치 확인 [#verify-installation]

    신세틱 작업 관리자가 올바르게 실행되고 있는지 확인하려면:

    1. 작업 관리자 로그 보기:

       ```bash
       podman logs -f YOUR_CONTAINER_ID
       ```

    2. 뉴렐릭 UI에서 <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Synthetic monitoring &gt; Private locations**</DNT> (으)로 이동하여 프라이빗 로케이션이 정상으로 표시되는지 확인하세요.
  </Step>
</Steps>

## 다음은 뭐지? [#whats-next]

Dual 스택이 활성화된 프라이빗 로케이션을 설정한 후 다음을 수행할 수 있습니다:

* 프라이빗 로케이션을 사용하여 IPv4 및 IPv6 엔드포인트를 모두 테스트하는 [신세틱 모니터를 생성](/docs/synthetics/synthetic-monitoring/using-monitors/add-edit-monitors) 합니다.
* 고급 옵션을 위해 [작업 관리자 설정을 구성](/docs/synthetics/synthetic-monitoring/private-locations/job-manager-configuration) 하세요.
* [프라이빗 로케이션](/docs/synthetics/synthetic-monitoring/private-locations/monitor-private-locations) 상태 및 성능 모니터링