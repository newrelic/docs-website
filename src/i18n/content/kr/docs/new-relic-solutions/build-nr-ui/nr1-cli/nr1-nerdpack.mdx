---
title: Nerdpack 명령
tags:
  - New Relic CLI
  - Nerdpack commands
metaDescription: An overview of the CLI commands you can use to set up your New Relic Nerdpacks.
freshnessValidatedDate: '2024-04-29T00:00:00.000Z'
translationType: machine
---

Nerdpack을 설정하려면 아래 명령을 사용하세요. 명령을 클릭하면 사용법 옵션과 명령에 대한 추가 세부 정보를 볼 수 있습니다.

<table>
  <thead>
    <tr>
      <th>
        명령
      </th>

      <th>
        설명
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [`nr1 nerdpack:build`](#nr1-nerdpackbuild)
      </td>

      <td>
        Nerdpack을 구축합니다
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:clean`](#nr1-nerdpackclean)
      </td>

      <td>
        모든 빌드된 아티팩트를 제거합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:clone`](#nr1-nerdpackclone)
      </td>

      <td>
        Git 저장소에서 Nerdpack을 복제합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:info`](#nr1-nerdpackinfo)
      </td>

      <td>
        뉴렐릭 레지스트리에서 Nerdpack의 상태를 표시합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:publish`](#nr1-nerdpackpublish)
      </td>

      <td>
        이 Nerdpack 게시
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:serve`](#nr1-nerdpackserve)
      </td>

      <td>
        테스트 및 개발 목적으로 Nerdpack을 제공합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:tag`](#nr1-nerdpacktag)
      </td>

      <td>
        특정 Nerdpack 버전에 태그를 지정합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:untag`](#nr1-nerdpackuntag)
      </td>

      <td>
        레지스트리에서 태그를 제거합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:uuid`](#nr1-nerdpackuuid)
      </td>

      <td>
        Nerdpack의 UUID를 표시하거나 재생성합니다.
      </td>
    </tr>

    <tr>
      <td>
        [`nr1 nerdpack:validate`](#nr1-nerdpackvalidate)
      </td>

      <td>
        Nerdpack 내부의 아티팩트를 검증합니다.
      </td>
    </tr>
  </tbody>
</table>

## `nr1 nerdpack:build` [#nr1-nerdpackbuild]

너드팩을 구축합니다.

### 용법 [#build-usage]

```sh
nr1 nerdpack:build
```

### 옵션 [#build-options]

<table>
  <tbody>
    <tr>
      <td>
        `--extra-metadata-path=extra-metadata-path`
      </td>

      <td>
        `default: extra-metadata.json` 추가 메타데이터로 json 파일 경로를 지정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `--prerelease=prerelease`
      </td>

      <td>
        지정된 경우 생성된 파일의 현재 버전에 값이 추가됩니다. 예를 들어 `--prerelease=abc`. 그러면 버전은 `1.2.3-abc` 이 됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#build-description]

Nerdpack을 자바스크립트 및 CSS 번들로 조합하기 위해 웹팩 프로세스를 실행합니다. 다른 많은 CLI 명령과 마찬가지로 Nerdpack의 `package.json` 수준에서 실행해야 합니다. 하위 디렉터리에서 `nr1.json` 파일을 검색하고 유효성을 검사한 후 빌드를 시도합니다. 또한 빌드 프로세스를 시작하기 전에 `nr1 nerdpack:validate` 실행합니다.

## `nr1 nerdpack:clean` [#nr1-nerdpackclean]

모든 빌드된 아티팩트를 제거합니다.

### 용법 [#clean-usage]

```sh
nr1 nerdpack:clean
```

### 옵션 [#clean-options]

<table>
  <tbody>
    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#clean-description]

콘텐츠 및 개발 폴더(`dist/`, `tmp/`)를 정리하고 제거합니다.

## `nr1 nerdpack:clone` [#nr1-nerdpackclone]

git 저장소에서 nerdpack을 복제합니다.

### 용법 [#clone-usage]

```sh
nr1 nerdpack:clone
```

### 옵션 [#clone-options]

<table>
  <tbody>
    <tr>
      <td>
        `-f`, `--force`
      </td>

      <td>
        대상 폴더가 있으면 교체하십시오.
      </td>
    </tr>

    <tr>
      <td>
        `-p`, `--path=path`
      </td>

      <td>
        복제할 디렉터리(기본값은 저장소 이름)입니다.
      </td>
    </tr>

    <tr>
      <td>
        `-r`, `--repo=repo`
      </td>

      <td>
        이는 **필수** 입니다. 위치는 HTTPS 또는 SSH 경로입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#clone-description]

git 저장소가 주어지면 사용을 시작할 수 있도록 모든 작업을 수행합니다. 여기에는 다음이 포함됩니다.

1. 저장소를 복제합니다.
2. 저장소를 원격 업스트림으로 설정합니다.
3. `npm` 사용하여 의존성/종속성을 모두 설치합니다.
4. 프로필을 사용하여 새 UUID를 생성하고 커밋합니다.

## `nr1 nerdpack:info` [#nr1-nerdpackinfo]

뉴렐릭 레지스트리에 있는 nerdpack의 상태를 표시합니다.

### 용법 [#info-usage]

```sh
nr1 nerdpack:info
```

### 옵션 [#info-options]

<table>
  <tbody>
    <tr>
      <td>
        `-a`, `--all`
      </td>

      <td>
        모든 버전을 표시합니다.
      </td>
    </tr>

    <tr>
      <td>
        `-i`, `--nerdpack-id=nerdpack-id`
      </td>

      <td>
        로컬 Nerdpack 대신 지정된 Nerdpack에서 정보를 얻으세요.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#info-description]

표시되는 버전의 기본 개수는 10개이지만 `--all` 또는 `-a` 플래그를 사용하면 모든 버전을 표시할 수 있습니다.

## `nr1 nerdpack:publish` [#nr1-nerdpackpublish]

이 너드팩을 게시하세요.

### 용법 [#publish-usage]

```sh
nr1 nerdpack:publish
```

### 옵션 [#publish-options]

<table>
  <tbody>
    <tr>
      <td>
        `-B`, `--skip-build`
      </td>

      <td>
        이전 빌드 프로세스를 건너뜁니다.
      </td>
    </tr>

    <tr>
      <td>
        `-T`, `--skip-tag`
      </td>

      <td>
        버전에 태그를 지정하지 마세요.
      </td>
    </tr>

    <tr>
      <td>
        `-t`, `--tag=tag`
      </td>

      <td>
        `default: STABLE`
      </td>
    </tr>

    <tr>
      <td>
        `--dry-run`
      </td>

      <td>
        실제로 아무것도 게시하지 않고 프로세스를 게시합니다.
      </td>
    </tr>

    <tr>
      <td>
        `--extra-metadata-path=extra-metadata-path`
      </td>

      <td>
        `default: extra-metadata.json` 추가 메타데이터로 json 파일 경로를 지정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `--prerelease=prerelease`
      </td>

      <td>
        지정된 경우 생성된 파일의 현재 버전에 값이 추가됩니다. 예를 들어, `--prerelease=abc`. 그러면 버전은 `1.2.3-abc` 이 됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#publish-description]

이 명령은 Nerdpack을 빌드하고 레지스트리에 업로드합니다.

추가 매개변수가 전달되지 않으면 이 명령은 자동으로 nerdpack 버전을 `STABLE` 으로 태그합니다. 이 동작을 비활성화하려면 `--skip-tag` 플래그를 명령에 전달하세요.

태그에 대한 자세한 내용은 `nr1 nerdpack:tag --help` 참조하세요.

## `nr1 nerdpack:serve` [#nr1-nerdpackserve]

테스트 및 개발 목적으로 nerdpack을 제공합니다.

### 용법 [#serve-usage]

```sh
nr1 nerdpack:serve
```

### 옵션 [#serve-options]

<table>
  <tbody>
    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#serve-description]

지역 코드가 포함된 서버를 플랫폼에서 실시간으로 테스트할 준비가 되었습니다.

## `nr1 nerdpack:tag` [#nr1-nerdpacktag]

특정 nerdpack 버전에 태그를 지정합니다.

### 용법 [#tag-usage]

```sh
nr1 nerdpack:tag
```

### 옵션 [#tag-options]

<table>
  <tbody>
    <tr>
      <td>
        `-V`, `--from-version=from-version`
      </td>

      <td>
        태그할 버전입니다. 제공되지 않은 경우 `package.json` 에 있는 항목을 사용합니다.
      </td>
    </tr>

    <tr>
      <td>
        `-i`, `--nerdpack-id=nerdpack-id`
      </td>

      <td>
        Nerdpack uuid를 구현하다, 배포하다. 제공되지 않은 경우 `nr1.json` 에 있는 항목을 사용합니다.
      </td>
    </tr>

    <tr>
      <td>
        `-t`, `--tag=tag`
      </td>

      <td>
        `default: STABLE` 태그 이름
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#tag-description]

하나의 버전에는 여러 개의 태그가 있을 수 있지만, 각 태그는 하나의 버전에만 적용할 수 있습니다. 기존 태그가 있는 다른 버전의 태그는 이전 태그의 태그를 해제합니다.

구독자에게 표시하려는 버전에 태그를 지정하려면 `STABLE` 사용하는 것이 좋습니다. 자세한 내용은 `nr1 nerdpack:subscribe` 명령을 참조하세요.

### 별칭 [#tag-aliases]

```sh
$ nr1 nerdpack:deploy
```

## `nr1 nerdpack:untag` [#nr1-nerdpackuntag]

레지스트리에서 태그를 제거합니다.

### 용법 [#untag-usage]

```sh
nr1 nerdpack:untag
```

### 옵션 [#untag-options]

<table>
  <tbody>
    <tr>
      <td>
        `-i`, `--nerdpack-id=nerdpack-id`
      </td>

      <td>
        Nerdpack uuid를 구현하다, 배포하다. 제공되지 않은 경우 `nr1.json` 에 있는 항목을 사용합니다.
      </td>
    </tr>

    <tr>
      <td>
        `-t`, `--tag=tag`
      </td>

      <td>
        `default: STABLE` 태그 이름.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#untag-description]

지정된 태그가 제거됩니다. 실제로 파일은 삭제되지 않습니다.

### 별칭 [#untag-aliases]

```sh
$ nr1 nerdpack:undeploy
```

## `nr1 nerdpack:uuid` [#nr1-nerdpackuuid]

Nerdpack의 UUID를 표시하거나 재생성합니다.

### 용법 [#uuid-usage]

```sh
nr1 nerdpack:uuid
```

### 옵션 [#uuid-options]

<table>
  <tbody>
    <tr>
      <td>
        `-f`, `--force`
      </td>

      <td>
        존재하는 경우 묻지 않고 기존 UUID를 재정의합니다.
      </td>
    </tr>

    <tr>
      <td>
        `-g`, `--generate`
      </td>

      <td>
        사용할 수 없는 경우 새 UUID를 생성합니다.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 설명 [#uuid-description]

기본적으로 지정된 패키지의 범용 고유 ID를 인쇄합니다. 또한 전달된 플래그에 따라 패키지의 UUID를 생성하거나 재생성할 수 있습니다.

## `nr1 nerdpack:validate` [#nr1-nerdpackvalidate]

nerdpack 내부의 아티팩트를 검증합니다.

### 용법 [#validate-usage]

```sh
nr1 nerdpack:validate
```

### 옵션 [#validate-options]

<table>
  <tbody>
    <tr>
      <td>
        `-l`, `--force-local`
      </td>

      <td>
        새 스키마 파일을 다운로드하지 마세요.
      </td>
    </tr>

    <tr>
      <td>
        `-r`, `--force-remote`
      </td>

      <td>
        새 스키마 파일을 강제로 다운로드합니다.
      </td>
    </tr>

    <tr>
      <td>
        `--profile=profile`
      </td>

      <td>
        사용하려는 인증 프로필입니다.
      </td>
    </tr>

    <tr>
      <td>
        `--verbose`
      </td>

      <td>
        출력에 추가 정보를 추가합니다.
      </td>
    </tr>
  </tbody>
</table>

### 별칭 [#validate-aliases]

```sh
nr1 nerdpack:ls
```

```sh
nr1 nerdpack:val
```