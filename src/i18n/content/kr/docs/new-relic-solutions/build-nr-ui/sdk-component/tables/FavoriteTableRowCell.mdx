---
title: 즐겨찾는테이블행세포
metaDescription: Learn how to work the FavoriteTableRowCell component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

특정 엔터티의 즐겨찾기 상태를 보여주는 행 테이블 셀을 렌더링합니다. 즐겨찾기 상태는 `checked` 속성을 통해 제어할 수 있으며 상태 변경은 `onChange` 속성을 통해 들을 수 있습니다.

### 용법

```js
import { FavoriteTableRowCell } from 'nr1'
```

### 예

#### 실시예 1

```js
function render() {
  const items = [
    {
      name: 'Login service',
      alertSeverity: 'CRITICAL',
      reporting: true,
      favorite: true,
    },
    {
      name: 'Service Gateway',
      alertSeverity: 'NOT_ALERTING',
      reporting: true,
      favorite: false,
    },
  ];
  return (
    <Table items={items}>
      <TableHeader>
        <TableHeaderCell width="56px" />
        <TableHeaderCell>Entity</TableHeaderCell>
      </TableHeader>
      {({ item }) => (
        <TableRow>
          <FavoriteTableRowCell checked={item.favorite} />
          <EntityTitleTableRowCell value={item} />
        </TableRow>
      )}
    </Table>
  );
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `checked` <h5>부울</h5>
      </td>

      <td>
        `true` 이면 사용자가 엔터티를 즐겨찾기로 표시했음을 나타냅니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        즐겨찾기 상태가 변경될 때마다 콜백이 실행됩니다. 새로운 즐겨찾기 상태는 두 번째 시위로 전달될 것입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""},{"name":"checked","type":"boolean","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>모양|끈</h5>
      </td>

      <td>
        링크할 위치 개체 또는 URL 문자열입니다.링크된 `TableRowCell`은 스타일이 지정되지 않으며 외부 링크에 대한 아이콘을 표시하지 않습니다. Link 구성요소와 동일한 스타일을 원하는 경우 셀 내의 하위 구성요소로 대신 `Link` 를 사용하십시오.<h3>모양</h3> `pathname` <h5>필수</h5><h5>문자열</h5>

        `search` <h5>끈</h5>

        `hash` <h5>끈</h5>
      </td>
    </tr>
  </tbody>
</table>