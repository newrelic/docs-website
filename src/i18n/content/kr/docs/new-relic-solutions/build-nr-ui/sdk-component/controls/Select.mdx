---
title: 선택하다
metaDescription: Learn how to work the Select component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Select>` 구성요소는 `<Dropdown>` 구성요소의 변형이며, 특히 양식 구성요소에 사용되는 목표입니다. 주요 차이점은 다음과 같습니다.

* 이는 선언 모드에서만 작동할 수 있습니다. 즉, 하위 함수를 전달하여 가상화할 수 없습니다.
* 모든 `<SelectItem>` `<Select>` 에 전달된 `value` 와 일치하는 `value` 소품을 허용하고 올바른 옵션이 선택됩니다. 기존의 `<SELECT>` DOM 요소와 달리 기본 요소와 객체 모두에서 모든 참조를 값으로 사용할 수 있습니다.
* 항목은 `onClick` 을 허용하지 않습니다. 대신, 선택한 옵션의 값을 `onChange` 메소드에서 수신하여 얻습니다.

구성요소는 항상 제어됩니다. 즉, 새로 선택한 속성을 반영하려면 `onChange` 콜백에서 `value` 업데이트해야 합니다.

### 용법

```js
import { Select } from 'nr1'
```

### 예

#### 기초적인

```js
<Select onChange={(evt, value) => alert(value)}>
  <SelectItem value="a">Value is "a"</SelectItem>
  <SelectItem value="b">Value is "b"</SelectItem>
  <SelectItem value="c">Value is "c"</SelectItem>
</Select>
```

#### 라벨 및 정보 포함

```js
<Select label="Items" info="Info value" onChange={(evt, value) => alert(value)}>
  <SelectItem value="a">Value is "a"</SelectItem>
  <SelectItem value="b">Value is "b"</SelectItem>
  <SelectItem value="c">Value is "c"</SelectItem>
</Select>
```

#### 인라인 라벨 포함

```js
<Select label="Items" labelInline onChange={(evt, value) => alert(value)}>
  <SelectItem value="a">Value is "a"</SelectItem>
  <SelectItem value="b">Value is "b"</SelectItem>
  <SelectItem value="c">Value is "c"</SelectItem>
</Select>
```

#### 설명 포함

```js
<Select description="Description value" onChange={(evt, value) => alert(value)}>
  <SelectItem value="a">Value is "a"</SelectItem>
  <SelectItem value="b">Value is "b"</SelectItem>
  <SelectItem value="c">Value is "c"</SelectItem>
</Select>
```

#### 잘못된 메시지 포함

```js
<Select invalid="Invalid message value" onChange={(evt, value) => alert(value)}>
  <SelectItem value="a">Value is "a"</SelectItem>
  <SelectItem value="b">Value is "b"</SelectItem>
  <SelectItem value="c">Value is "c"</SelectItem>
</Select>
```

#### 제어되는 구성 요소

```js
class ControlledSelect extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      value: null,
    };


    this._onChange = this._onChange.bind(this);
  }


  _onChange(event, value) {
    this.setState({ value });
  }


  render() {
    return (
      <Select onChange={this._onChange} value={this.state.value}>
        <SelectItem value="1">Item 1</SelectItem>
        <SelectItem value="2">Item 2</SelectItem>
        <SelectItem value="3">Item 3</SelectItem>
      </Select>
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        이 컨트롤에 대한 기술 라벨을 제공하세요. 예: "계정".
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드</h5>
      </td>

      <td>
        `<SelectItem>`세트로 표현된 옵션 목록입니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>끈</h5>
      </td>

      <td>
        양식 필드를 채우는 방법에 대한 지침이 포함된 메시지입니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 상호작용에 선택을 사용할 수 없습니다.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>끈</h5>
      </td>

      <td>
        추가 정보는 라벨 옆의 정보 도구 설명에 표시될 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>부울|문자열</h5>
      </td>

      <td>
        true인 경우 필드를 유효하지 않은 상태로 설정하여 사용자에게 이 특정 필드에 대한 주의가 필요함을 알립니다. 이 속성은 `boolean` 필드 또는 `string` 일 수 있습니다. `string` 이고 유효하지 않은 상태가 표시되면 텍스트가 아래에 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        라벨로 표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>부울</h5>
      </td>

      <td>
        양식 컨트롤 인라인에 레이블을 표시합니다. 구성요소가 `Form` 내부에 없는 경우에만 사용하십시오. 이 경우 `Form` 구성요소에서 `layoutType` `Form.LAYOUT_TYPE.SPLIT` 로 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        선택 값이 변경될 때마다 콜백이 실행됩니다. 값을 `onChange` 이벤트의 두 번째 인수로 다시 가져올 수 있습니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""},{"name":"value","type":"any","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 필수 양식 필드를 나타냅니다.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          선택.SPACING_TYPE.EXTRA_LARGE, 선택.SPACING_TYPE.LARGE, 선택.SPACING_TYPE.MEDIUM, 선택.SPACING_TYPE.NONE, 선택.SPACING_TYPE.OMIT, 선택.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`. **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>어느</h5>
      </td>

      <td>
        선택한 항목과 일치하는 값입니다.
      </td>
    </tr>
  </tbody>
</table>