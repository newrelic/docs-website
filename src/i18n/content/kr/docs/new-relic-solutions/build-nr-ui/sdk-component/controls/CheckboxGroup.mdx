---
title: 체크박스그룹
metaDescription: Learn how to work the CheckboxGroup component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Checkbox>` 버튼 그룹입니다. `<Checkbox>` 버튼은 체크박스 그룹의 직계 하위 항목이거나 하위 항목일 수 있습니다.

**참고**: `value` 설정하면 확인란 그룹이 제어된 상태가 되므로 `defaultValue` 재정의됩니다. `value` 는 `checked` 이 설정된 확인란을 재정의하지 않습니다. 왜냐하면 `CheckboxGroup` 는 제어되지 않는 `Checkbox` 구성요소를 제어하고 제어되는 구성요소를 재정의할 수 없기 때문입니다. 그러나 `value` `Checkbox` 에서 `defaultChecked` 재정의합니다.

`CheckboxGroup` 에 대한 `onChange` 이벤트 핸들러는 개별 `Checkbox` 구성요소에 설정된 `onChange` 이벤트 핸들러 이후에 실행됩니다. 그러나 제어된 `CheckboxGroup` 에서 전체 그룹에 대해 하나의 이벤트 핸들러만 설정하는 것이 좋습니다.

`Checkbox` 구성요소는 `CheckboxGroup` 에 의해 제어되기 위해 `value` 가 설정되어 있어야 **합니다** .

### 용법

```js
import { CheckboxGroup } from 'nr1'
```

### 예

#### 기초적인

```js
<CheckboxGroup label="My checkbox group">
  <Checkbox label="Checkbox 1" />
  <Checkbox label="Checkbox 2" />
  <Checkbox label="Checkbox 3" />
</CheckboxGroup>
```

#### 라벨 및 정보 포함

```js
<CheckboxGroup label="My checkbox group" info="Info value">
  <Checkbox label="Checkbox 1" />
  <Checkbox label="Checkbox 2" />
  <Checkbox label="Checkbox 3" />
</CheckboxGroup>
```

#### 인라인 라벨 포함

```js
<CheckboxGroup label="My checkbox group" labelInline>
  <Checkbox label="Checkbox 1" />
  <Checkbox label="Checkbox 2" />
  <Checkbox label="Checkbox 3" />
</CheckboxGroup>
```

#### 설명 포함

```js
<CheckboxGroup label="My checkbox group" description="Description value">
  <Checkbox label="Checkbox 1" />
  <Checkbox label="Checkbox 2" />
  <Checkbox label="Checkbox 3" />
</CheckboxGroup>
```

#### 잘못된 메시지 포함

```js
<CheckboxGroup label="My checkbox group" invalid="Invalid message value">
  <Checkbox label="Checkbox 1" />
  <Checkbox label="Checkbox 2" />
  <Checkbox label="Checkbox 3" />
</CheckboxGroup>
```

#### 기본값 사용

```js
<CheckboxGroup label="My checkbox group" defaultValue={['1', '2']}>
  <Checkbox label="Checkbox 1" value="1" />
  <Checkbox label="Checkbox 2" value="2" />
  <Checkbox label="Checkbox 3" value="3" />
</CheckboxGroup>
```

#### 제어되는 구성 요소

```js
class MyNerdlet extends React.PureComponent {
  constructor(props) {
    super(props);


    this.state = {
      values: ['2'],
    };


    this.onChange = this.onChange.bind(this);
  }


  onChange(event, values) {
    this.setState({ values });
  }


  render() {
    return (
      <CheckboxGroup value={this.state.values} onChange={this.onChange}>
        <Checkbox label="Checkbox 1" value="1" />
        <Checkbox label="Checkbox 2" value="2" />
        <Checkbox label="Checkbox 3" value="3" />
      </CheckboxGroup>
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>마디</h5>
      </td>

      <td>
        CheckboxGroup의 콘텐츠입니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>끈\[]</h5>
      </td>

      <td>
        체크박스 그룹의 기본값입니다. 값이 일치하는 `<Checkbox>` 구성요소가 선택됩니다. [제어되는 구성요소를](https://facebook.github.io/react/docs/forms.html) 사용하고 싶지 않을 때 유용합니다.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>끈</h5>
      </td>

      <td>
        양식 필드를 채우는 방법에 대한 지침이 포함된 메시지입니다.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>끈</h5>
      </td>

      <td>
        추가 정보는 라벨 옆의 정보 도구 설명에 표시될 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>부울|문자열</h5>
      </td>

      <td>
        true인 경우 필드를 유효하지 않은 상태로 설정하여 사용자에게 이 특정 필드에 대한 주의가 필요함을 알립니다. 이 속성은 `boolean` 필드 또는 `string` 일 수 있습니다. `string` 이고 유효하지 않은 상태가 표시되면 텍스트가 아래에 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        라벨로 표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>부울</h5>
      </td>

      <td>
        양식 컨트롤 인라인에 레이블을 표시합니다. 구성요소가 `Form` 내부에 없는 경우에만 사용하십시오. 이 경우 `Form` 구성요소에서 `layoutType` `Form.LAYOUT_TYPE.SPLIT` 로 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        체크박스 그룹 값이 변경되면 실행되는 콜백(그룹의 `<Checkbox>` 이 선택됨)<FunctionDefinition returnValue={[]} arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback."},{"name":"value","type":"string[]","description":"The values of the selected checkboxes."}]}/>
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 필수 양식 필드를 나타냅니다.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`입니다.**참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>끈\[]</h5>
      </td>

      <td>
        체크박스 그룹의 값입니다. 값이 일치하는 `<Checkbox>` 구성요소가 선택됩니다. 정의되면 구성요소가 [제어되는 구성요소](https://facebook.github.io/react/docs/forms.html) 로 전환됩니다.
      </td>
    </tr>
  </tbody>
</table>