---
title: 라디오
metaDescription: Learn how to work the Radio component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

사용자가 제공된 옵션 중 하나만 선택할 수 있는 경우

라디오 버튼 상태는 `checked` 속성을 통해 제어할 수 있습니다. `checked` 속성이 설정되지 않은 경우 라디오 버튼은 자체 상태를 관리합니다.

`onClick` 또는 `onChange` 소품을 통해 라디오 버튼 이벤트를 들을 수 있습니다.

### 용법

```js
import { Radio } from 'nr1'
```

### 예

#### 기초적인

```js
<div className="nr1-Docs-prettify">
  <Radio onChange={(event) => alert('Foo')} label="Foo" />
  <Radio checked label="Bar" />
  <Radio checked disabled label="Baz" />
</div>
```

#### 정보 포함

```js
<Radio onChange={(event) => alert('Foo')} info="Info value" label="Foo" />
```

#### 설명 포함

```js
<Radio
  onChange={(event) => alert('Foo')}
  description="Description value"
  label="Foo"
/>
```

#### 잘못된 메시지 포함

```js
<Radio
  onChange={(event) => alert('Foo')}
  invalid="Invalid message value"
  label="Foo"
/>
```

#### 제어되는 구성 요소

```js
class MyNerdlet extends React.PureComponent {
  constructor(props) {
    super(props);


    this.state = {
      isChecked: false,
    };


    this.onClick = this.onClick.bind(this);
  }


  onClick(event) {
    this.setState((state) => {
      return { isChecked: !state.isChecked };
    });
  }


  render() {
    return (
      <Radio
        checked={this.state.isChecked}
        onClick={this.onClick}
        label="Radio button"
      />
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `checked` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 라디오 버튼이 선택됩니다. 정의된 경우 입력을 [제어되는 구성요소](https://facebook.github.io/react/docs/forms.html) 로 바꿉니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>끈</h5>
      </td>

      <td>
        양식 필드를 채우는 방법에 대한 지침이 포함된 메시지입니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 상호작용에 라디오 버튼을 사용할 수 없습니다.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>끈</h5>
      </td>

      <td>
        추가 정보는 라벨 옆의 정보 도구 설명에 표시될 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>부울|문자열</h5>
      </td>

      <td>
        true인 경우 필드를 유효하지 않은 상태로 설정하여 사용자에게 이 특정 필드에 대한 주의가 필요함을 알립니다. 이 속성은 `boolean` 필드 또는 `string` 일 수 있습니다. `string` 이고 유효하지 않은 상태가 표시되면 텍스트가 아래에 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        라벨로 표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        라디오 버튼의 선택된 상태가 변경될 때마다 콜백이 실행됩니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback."},{"name":"value","type":"any","description":"The value of the radio button."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>기능</h5>
      </td>

      <td>
        라디오 버튼을 클릭할 때마다 콜백이 실행됩니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          라디오.SPACING_TYPE.EXTRA_LARGE, 라디오.SPACING_TYPE.LARGE, 라디오.SPACING_TYPE.MEDIUM, 라디오.SPACING_TYPE.NONE, 라디오.SPACING_TYPE.OMIT, 라디오.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`. **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>끈</h5>
      </td>

      <td>
        구성 요소의 값입니다. 선택한 라디오 버튼을 식별하기 위해 `<RadioGroup/>` 에서 사용됩니다.
      </td>
    </tr>
  </tbody>
</table>