---
title: 여러 줄 텍스트 필드
metaDescription: Learn how to work the MultilineTextField component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

양식에서 여러 줄 입력 목적으로 사용되는 구성 요소입니다.

### 용법

```js
import { MultilineTextField } from 'nr1'
```

### 예

#### 기초적인

```js
<MultilineTextField label="Summary" placeholder="Enter a short description" />
```

#### 인라인 라벨 포함

```js
<MultilineTextField
  label="Summary"
  labelInline
  placeholder="Enter a short description"
/>
```

#### 미리 포맷된

```js
<MultilineTextField
  type={MultilineTextField.TYPE.PREFORMATTED}
  label="Build command"
  placeholder="npm run build"
/>
```

#### 로드 중

```js
<MultilineTextField loading placeholder="Enter a short description" />
```

#### 장애가 있는

```js
<MultilineTextField disabled placeholder="Enter a short description" />
```

#### 읽기 전용

```js
<MultilineTextField
  readOnly
  defaultValue="Some value"
  placeholder="Enter a short description"
/>
```

#### 설명 포함

```js
<MultilineTextField
  description="Description value"
  placeholder="Enter a short description"
/>
```

#### 잘못된 메시지 포함

```js
<MultilineTextField
  invalid="Not enough text, needs more"
  placeholder="Enter a short description"
/>
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        이 입력을 설명하기 위해 화면 판독기에 레이블을 제공하십시오. 이는 스크린 리더 라벨이 `label` 소품과 달라야 하거나 `label` 소품을 전혀 제공하지 않는 경우에만 필요합니다.
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabelledBy` <h5>끈</h5>
      </td>

      <td>
        스크린 리더에 대한 이 입력을 설명하는 콘텐츠가 있는 요소의 고유한 `id` 제공합니다. 이는 스크린 리더 라벨이 `label` 소품과 달라야 하거나 `label` 소품을 전혀 제공하지 않는 경우에만 필요합니다. 이 항목 또는 `ariaLabel` 사용하세요. 둘 다 사용할 수는 없습니다.
      </td>
    </tr>

    <tr>
      <td>
        `autoFocus` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 여러 줄 텍스트 필드가 마운트 시 자동으로 초점을 맞춥니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>끈</h5>
      </td>

      <td>
        여러 줄 텍스트 필드의 기본값입니다.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>끈</h5>
      </td>

      <td>
        양식 필드를 채우는 방법에 대한 지침이 포함된 메시지입니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        필드를 편집할 수 있는지 여부를 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>끈</h5>
      </td>

      <td>
        추가 정보는 라벨 옆의 정보 도구 설명에 표시될 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>부울|문자열</h5>
      </td>

      <td>
        true인 경우 필드를 유효하지 않은 상태로 설정하여 사용자에게 이 특정 필드에 대한 주의가 필요함을 알립니다. 이 속성은 `boolean` 필드 또는 `string` 일 수 있습니다. `string` 이고 유효하지 않은 상태가 표시되면 텍스트가 아래에 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        라벨로 표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>부울</h5>
      </td>

      <td>
        양식 컨트롤 인라인에 레이블을 표시합니다. 구성요소가 `Form` 내부에 없는 경우에만 사용하십시오. 이 경우 `Form` 구성요소에서 `layoutType` `Form.LAYOUT_TYPE.SPLIT` 로 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>부울</h5>
      </td>

      <td>
        데이터 목록이나 다른 항목을 양식 필드에 로드하는 경우 로드 상태 양식을 사용하여 항목이 로드 중임을 사용자에게 나타냅니다. 필드가 값을 동적으로 로드할 수 있음을 알리는 데 사용됩니다. `true` 으로 설정하면 로딩 스피너가 표시됩니다. `false` 로 설정하면 스피너가 잠재적으로 표시될 수 있도록 UI에서 일부 공간을 예약합니다. 특정 지점에서 필드가 로드되지 않을 것으로 예상되는 경우 속성을 설정하지 마십시오(또는 `undefined` 로 설정). 제 시간에.
      </td>
    </tr>

    <tr>
      <td>
        `onBlur` <h5>기능</h5>
      </td>

      <td>
        여러 줄의 텍스트 필드가 포커스를 잃을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        여러 줄의 텍스트 필드 값이 변경되면 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback. You can pull out the new value by accessing `event.target.value`."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onFocus` <h5>기능</h5>
      </td>

      <td>
        여러 줄의 텍스트 필드에 포커스가 있을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyDown` <h5>기능</h5>
      </td>

      <td>
        키를 눌렀을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyUp` <h5>기능</h5>
      </td>

      <td>
        키를 놓을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `placeholder` <h5>끈</h5>
      </td>

      <td>
        값이 없을 때 여러 줄 텍스트 필드에 힌트가 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `readOnly` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 여러 줄 텍스트 필드를 읽기 전용으로 만듭니다.
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 필수 양식 필드를 나타냅니다.
      </td>
    </tr>

    <tr>
      <td>
        `rows` <h5>숫자</h5>
      </td>

      <td>
        텍스트 필드의 행 수입니다. 필요한 최소 개수는 5개 행입니다.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          MultilineTextField.SPACING_TYPE.EXTRA_LARGE, MultilineTextField.SPACING_TYPE.LARGE, MultilineTextField.SPACING_TYPE.MEDIUM, MultilineTextField.SPACING_TYPE.NONE, MultilineTextField.SPACING_TYPE.OMIT, MultilineTextField.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spellCheck` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 브라우저에서 철자 오류 검사를 활성화합니다.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>열거</h5>
      </td>

      <td>
        * NORMAL — 사용자가 필드에 텍스트를 입력할 때 \* PERFORMATTED — 사용자가 고정폭 콘텐츠를 입력해야 할 때

          <OptionReference>
            MultilineTextField.TYPE.NORMAL, MultilineTextField.TYPE.PREFORMATTED,
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>끈</h5>
      </td>

      <td>
        여러 줄 텍스트 필드의 값입니다. 이는 제어되는 구성 요소에 필요합니다.
      </td>
    </tr>
  </tbody>
</table>