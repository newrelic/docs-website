---
title: 스위치
metaDescription: Learn how to work the Switch component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

스위치는 콘텐츠 필터에서 토글로 사용되거나 사용자가 바이너리 기본 설정을 선택할 수 있는 방법을 제공합니다.

### 용법

```js
import { Switch } from 'nr1'
```

### 예

#### 기초적인

```js
<div className="nr1-Docs-prettify">
  <Switch onChange={(e) => alert(`Toggle to: ${e.target.checked}`)} />
  <Switch
    defaultChecked
    onChange={(e) => alert(`Toggle to: ${e.target.checked}`)}
  />
</div>
```

#### 장애가 있는

```js
<div className="nr1-Docs-prettify">
  <Switch disabled />
  <Switch defaultChecked disabled />
</div>
```

#### 로딩 상태

```js
<div className="nr1-Docs-prettify">
  <Switch loading />
  <Switch defaultChecked loading />
</div>
```

#### 라벨 포함

```js
<div className="nr1-Docs-prettify">
  <Switch label="This is a label" />
  <Switch disabled label="This is a label" />
</div>
```

#### 라벨 및 정보 포함

```js
<Switch label="This is a label" info="Info value" />
```

#### 설명 포함

```js
<Switch label="This is a label" description="Description value" />
```

#### 잘못된 메시지 포함

```js
<Switch label="This is a label" invalid="Invalid message value" />
```

#### 요청을 수행하는 동안 로드되도록 설정된 제어 구성 요소

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(resolve, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

#### 요청이 실패하면 선택 해제 상태로 다시 전환되는 요청을 수행하는 동안 로드되도록 설정된 제어 구성요소

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(reject, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        이 컨트롤에 대한 기술 라벨을 제공하세요. 예: "보기 전환".
      </td>
    </tr>

    <tr>
      <td>
        `checked` <h5>부울</h5>
      </td>

      <td>
        스위치의 확인된 상태를 결정합니다. 여기서 `true` "켜짐"으로 나타나는 스위치에 해당하고, `false` 값은 "꺼짐"으로 나타나는 스위치에 해당합니다. `checked` 가 정의되면 스위치 구성요소 [제어되는 구성 요소](https://reactjs.org/docs/forms.html#controlled-components) 처럼 동작합니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 조정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `defaultChecked` <h5>부울</h5>
      </td>

      <td>
        [제어되지 않는 구성 요소](https://reactjs.org/docs/uncontrolled-components.html) 로 작동할 때 스위치에 대한 기본 확인 값을 설정합니다. 즉. `checked` 소품이 아직 규정되지 않은 경우.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>끈</h5>
      </td>

      <td>
        양식 필드를 채우는 방법에 대한 지침이 포함된 메시지입니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        사용자가 스위치와 상호 작용할 수 있는지, 아니면 비활성화된 상태로 렌더링되어야 하는지를 결정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>끈</h5>
      </td>

      <td>
        추가 정보는 라벨 옆의 정보 도구 설명에 표시될 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>부울|문자열</h5>
      </td>

      <td>
        true인 경우 필드를 유효하지 않은 상태로 설정하여 사용자에게 이 특정 필드에 대한 주의가 필요함을 알립니다. 이 속성은 `boolean` 필드 또는 `string` 일 수 있습니다. `string` 이고 유효하지 않은 상태가 표시되면 텍스트가 아래에 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        스위치로 제어되는 설정에 대한 간단한 제목이나 설명을 사용자에게 제공하려면 텍스트 레이블을 연결하십시오. 라벨은 스위치의 왼쪽에 위치합니다.
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>부울</h5>
      </td>

      <td>
        작업이 진행 중인지 여부를 나타내려면, 특히 완료하는 데 1초 이상 걸리는 경우 로딩 상태를 표시해야 합니다. 로드하는 동안 스위치는 대화형이 아닙니다. 로드 상태와 관련된 작업이 실패하면 확인된 속성을 이전 값으로 다시 설정해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        사용자가 스위치를 전환할 때마다 콜백이 실행됩니다. 현재 확인된 상태는 `event.target.checked` 통해 콜백에서 평가될 수 있습니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          스위치.SPACING_TYPE.EXTRA_LARGE, 스위치.SPACING_TYPE.LARGE, 스위치.SPACING_TYPE.MEDIUM, 스위치.SPACING_TYPE.NONE, 스위치.SPACING_TYPE.OMIT, 스위치.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일. 위치 지정 및 간격 조정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`. **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>
  </tbody>
</table>