---
title: 목록
metaDescription: Learn how to work the List component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<List>` 대규모 항목 목록을 효율적으로 렌더링할 수 있습니다.

`<List>` 는 두 가지 방법, 즉 `<ListItem>` 요소의 다시 또는 콜백(Function as Children) 렌더링으로 항목을 렌더링할 수 있습니다. 렌더링 콜백은 모든 항목을 가상화하므로 많은 수의 항목 제공 시 매우 빠르게 처리되므로 사용하는 것이 좋습니다.

`style` 또는 `className` 소품을 통해 너비와 높이를 지정하지 않으면 `width="fit-content"` 소품으로 제공되지 않는 한 컨테이너의 높이와 너비가 채워집니다. 그런 다음 `<List>` 컨테이너의 크기를 측정된 가장 넓은 `<ListItem>` 구성요소만큼 넓게 조정합니다.

### 용법

```js
import { List } from 'nr1'
```

### 예

#### 기초적인

```js
function render() {
  return (
    <List rowHeight={20}>
      <ListItem>Item 1</ListItem>
      <ListItem>Item 2</ListItem>
      <ListItem>Item 3</ListItem>
      <ListItem>Item 4</ListItem>
    </List>
  );
}
```

#### 콜백 렌더링

```js
function render() {
  const items = new Array(10000).fill().map((_, i) => {
    return { key: `foo-${i}`, name: `Item ${i}` };
  });


  const style = { width: 200, height: 300 };


  return (
    <List style={style} rowHeight={20} items={items}>
      {({ item }) => <ListItem key={item.key}>{item.name}</ListItem>}
    </List>
  );
}
```

#### 지연 로딩

```js
function render() {
  // This example assumes you have a way to know/load this information.
  const remoteTotalNumberOfItems = 9000;


  const items = [
    { id: 1, text: 'Item 1' },
    { id: 2, text: 'Item 2' },
    { id: 3, text: 'Item 3' },
    { id: 4, text: 'Item 4' },
  ];


  function loadMoreItems({ startIndex, stopIndex }) {
    return fetch(`path/to/api?start=${startIndex}&stop=${stopIndex}`).then(
      () => {
        // Store items in list...
      },
    );
  }


  const style = { width: 200, height: 300 };


  return (
    <div style={style}>
      <List
        items={items}
        rowCount={remoteTotalNumberOfItems}
        onLoadMore={loadMoreItems}
      >
        {({ item }) => <ListItem key={item.id}>{item.text}</ListItem>}
      </List>
    </div>
  );
}
```

#### 콘텐츠 너비에 맞게 자동 크기 조정

```js
class AutoSizingList extends React.Component {
  constructor() {
    super(...arguments);
    this.state = {
      items: this._generateItems(0),
    };
    this._maxItems = 1000;
    this._onLoadMore = this._onLoadMore.bind(this);
  }


  _onLoadMore(cursor) {
    return (
      Promise.resolve()
        // Fetch more items to load into <List>
        .then(() => this._generateItems(this.state.items.length))
        .then((moreItems) =>
          // Add retrieved items to the stored list
          this.setState(({ items }) => ({ items: items.concat(moreItems) })),
        )
    );
  }


  _generateString(num, extra = 0) {
    return `Entity ${num.toString(10)}: 1${'00'.repeat(
      Math.floor(Math.random() * 10) + 1 + extra,
    )} bytes`;
  }


  _generateItems(start) {
    return new Array(200).fill().map((_, i) => ({
      key: `foo-${start + i}`,
      name: this._generateString(start + i, start / 100),
    }));
  }


  render() {
    const style = { 'height': 300, 'background-color': 'white' };
    const parentStyle = { width: 260 };


    return (
      <div style={parentStyle}>
        <List
          rowCount={this._maxItems}
          onLoadMore={this._onLoadMore}
          style={{ ...style }}
          rowHeight={20}
          items={this.state.items}
          width="fit-content"
        >
          {({ item }) => <ListItem key={item.key}>{item.name}</ListItem>}
        </List>
      </div>
    );
  }
}
```

#### 쿼리 구성요소와 통합

```js
function render() {
  const style = { width: 200, height: 300 };


  const list = (
    <div style={style}>
      <EntitiesByDomainTypeQuery entityDomain="APM" entityType="APPLICATION">
        {({ fetchMore, loading, data }) => {
          const { results, count } = data.actor.entitySearch;


          return (
            <List
              rowHeight={40}
              items={results.entities}
              onLoadMore={fetchMore}
              rowCount={count}
            >
              {({ item }) => <ListItem key={item.id}>{item.text}</ListItem>}
            </List>
          );
        }}
      </EntitiesByDomainTypeQuery>
    </div>
  );
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드|함수</h5>
      </td>

      <td>
        `<ListItem>` 요소의 빼기 또는 렌더링 콜백(Function as Children)일 수 있습니다. 모든 항목을 가상화하여 많은 수의 항목이 제공될 때 매우 빠르게 렌더링 콜백을 사용하는 것이 좋습니다. 렌더링 콜백을 사용하는 경우 항목은 `items` 소품을 통해 제공되어야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        맞춤 스타일 지정을 위한 클래스 이름입니다.
      </td>
    </tr>

    <tr>
      <td>
        `fullHeight` <h5>부울</h5>
      </td>

      <td>
        사용 가능한 모든 높이를 차지하도록 스택을 확장합니다.
      </td>
    </tr>

    <tr>
      <td>
        `fullWidth` <h5>부울</h5>
      </td>

      <td>
        사용 가능한 모든 너비를 차지하도록 스택을 확장합니다.
      </td>
    </tr>

    <tr>
      <td>
        `items` <h5>어느\[]</h5>
      </td>

      <td>
        렌더링할 때 사용할 항목입니다. 렌더링 콜백을 사용하여 항목을 렌더링할 때 필요합니다. 각 항목은 가능한 모든 구조와 유형을 가질 수 있으며 각 요소 목록을 렌더링할 때 해당 항목이 제공됩니다.

        ```js
        function render() {
          const style = { width: 200, height: 300 };

          const items = [
            { foo: 'a', bar: 'x' },
            { foo: 'b', bar: 'y' },
          ];

          return (
            <div style={style}>
              <List items={items}>
                {({ item, index }) => (
                  <ListItem key={index}>{`${item.foo} - ${item.bar}`}</ListItem>
                )}
              </List>
            </div>
          );
        }
        ```
      </td>
    </tr>

    <tr>
      <td>
        `onLoadMore` <h5>기능</h5>
      </td>

      <td>
        더 많은 항목을 로드해야 할 때 콜백이 실행됩니다. 이는 항목을 늦게 로드하여 렌더링하려는 항목을 `items` 에서 찾을 수 없을 때 발생합니다. 이 콜백은 백앤드 또는 다른 소스에서 누락된 항목을 가져오거나 로드하는 데 사용해야 합니다.반환된 항목 항목 데이터 로드가 완료되면 Promise가 해결되어야 합니다. 새로 로드된 데이터로 목록을 새로 고칠 시기를 결정하는 데 사용됩니다. 이 콜백은 단일 스크롤 이벤트에 대한 반응으로 여러 번 호출될 수 있습니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"cursor","type":"Cursor","description":"Items to load."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `rowCount` <h5>숫자</h5>
      </td>

      <td>
        행 수. 기본적으로 항목 소품에 전달된 다시의 길이와 같습니다. 총 항목 수를 알고 있지만 스크롤하는 동안 항목을 지연 로드하려는 경우 `rowCount` 를 지정해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `rowHeight` <h5>필수</h5> <h5>숫자</h5>
      </td>

      <td>
        목록 행의 높이입니다. 렌더링 콜백(자식 기능)을 사용하여 항목을 렌더링할 때 필요합니다. 이는 사용자에게 보이지 않는 방식으로 셀 내용을 일시적으로 렌더링하여 측정할 때까지의 임시 높이입니다.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          목록.SPACING_TYPE.EXTRA_LARGE, 목록.SPACING_TYPE.LARGE, 목록.SPACING_TYPE.MEDIUM, 목록.SPACING_TYPE.NONE, 목록.SPACING_TYPE.OMIT, 목록.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `width` <h5>끈</h5>
      </td>

      <td>
        목록의 너비를 나타냅니다. 이는 항상 문자열이며 다음 유형 중 하나일 수 있습니다.

        * `'fit-content'`: `<List>` 컨테이너의 자동 크기 조정을 활성화합니다. 컨테이너가 가장 넓은 `<ListItem>` 의 너비에 맞는지 확인하기 위해 가상화된 `<List>`과 함께 사용하기 위한 것입니다. `<List>` 구성요소가 더 많은 항목을 로드하면 크기가 조정됩니다. `<List>` 구성요소가 있는 컨테이너의 높이에 계속 맞춰지는 `<List>` 구성요소의 높이에 영향을 주지 않습니다.
        * `'auto'`: `<List>` 의 기본값입니다. 이 항목은 기본 스타일에 따라 `<List>` 구성 요소의 크기를 조정하여 해당 컨테이너의 너비와 높이를 채울 수 있도록 확장할 수 있도록 합니다. 그러면 콘텐츠의 크기가 `<List>` 구성요소의 결과 너비에 맞게 조정됩니다.
      </td>
    </tr>
  </tbody>
</table>

### 유형 정의

<TypeDefReference typeDef={{"name":"RenderCallbackArguments","properties":[{"description":"Item to render.","name":"item","type":"any"},{"description":"Index of the item in the items array.","name":"index","type":"number"},{"description":"Array of items which we're iterating on.","name":"items","type":"any[]"}]}}/><TypeDefReference typeDef={{"name":"Cursor","properties":[{"description":"First index of the range of items to load.","name":"startIndex","type":"number"},{"description":"Last index of the range of items to load.","name":"stopIndex","type":"number"}]}}/>