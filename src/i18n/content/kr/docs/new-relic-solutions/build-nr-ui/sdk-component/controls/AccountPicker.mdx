---
title: AccountPicker
metaDescription: Learn how to work the AccountPicker component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

모든 계정을 조회하고, 드롭다운 메뉴 및 검색을 통해 계정을 선택할 수 있습니다. 현재 선택된 계정을 캡처하려면 `onChange` 리스너를 사용할 수 있습니다.

플랫폼에는 이미 헤더에 있는 자체 계정 선택기가 포함되어 있습니다. 이는 `nerdlet.setConfig` API를 사용하여 표시되도록 구성할 수 있으며( `{ accountPicker: true }` 전달) 해당 값은 `<PlatformStateContext.Consumer>` 사용하여 읽을 수 있습니다. 전체 내용을 보려면 다음 API를 확인하세요.

### 용법

```js
import { AccountPicker } from 'nr1'
```

### 예

#### 기초적인

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = { accountId: null };


    this.onChangeAccount = this.onChangeAccount.bind(this);
  }


  onChangeAccount(_, value) {
    alert(`Selected account: ${value}`);


    this.setState({ accountId: value });
  }


  render() {
    return (
      <AccountPicker
        value={this.state.accountId}
        onChange={this.onChangeAccount}
      />
    );
  }
}
```

#### 인라인 라벨 포함

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = { accountId: null };


    this.onChangeAccount = this.onChangeAccount.bind(this);
  }


  onChangeAccount(_, value) {
    alert(`Selected account: ${value}`);


    this.setState({ accountId: value });
  }


  render() {
    return (
      <AccountPicker
        label="Account"
        labelInline
        value={this.state.accountId}
        onChange={this.onChangeAccount}
      />
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        이 컨트롤에 대한 기술 라벨을 제공하세요. 예: "계정".
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `crossAccount` <h5>부울</h5>
      </td>

      <td>
        상단에 "모든 계정"이라는 옵션이 표시됩니다. 선택하면 `onChange` 콜백에서 제공되는 값은 `AccountPicker.CROSS_ACCOUNT` 입니다. 이는 여러 계정에서 작동할 수 있는 기능이 있는 경우 유용합니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우, 드롭다운 메뉴를 사용할 수 없습니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        라벨로 표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>부울</h5>
      </td>

      <td>
        양식 컨트롤 인라인에 레이블을 표시합니다. 구성요소가 `Form` 내부에 없는 경우에만 사용하십시오. 이 경우 `Form` 구성요소에서 `layoutType` `Form.LAYOUT_TYPE.SPLIT` 로 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        사용자가 목록에서 계정을 클릭할 때마다 콜백이 실행됩니다.<FunctionDefinition returnValue={[]} arguments={[{"name":"event","type":"React.MouseEvent","description":""},{"name":"value","type":"number","description":"Id of the account selected."}]}/>
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0~4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요. <OptionReference array>AccountPicker.SPACING_TYPE.EXTRA_LARGE, AccountPicker.SPACING_TYPE.LARGE, AccountPicker.SPACING_TYPE.MEDIUM, AccountPicker.SPACING_TYPE.NONE, AccountPicker.SPACING_TYPE.OMIT, AccountPicker.SPACING_TYPE.SMALL,</OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`입니다.**참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>숫자|열거형</h5>
      </td>

      <td>
        선택한 계정의 ID입니다.
      </td>
    </tr>
  </tbody>
</table>