---
title: 모달
metaDescription: Learn how to work the Modal component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

모달은 단일 단계 생성, 추가, 편집 또는 삭제 작업에 사용됩니다. 또한 화면이나 화면의 특정 개체에 대한 추가 메타데이터를 표시하는 데에도 사용됩니다.

### 용법

```js
import { Modal } from 'nr1'
```

### 예

#### 기초적인

```js
class MyNerdlet extends React.PureComponent {
  constructor() {
    super(...arguments);


    this._onClose = this._onClose.bind(this);


    this.state = {
      hidden: true,
    };
  }


  _onClose() {
    this.setState({ hidden: true });
  }


  render() {
    return (
      <>
        <Button onClick={() => this.setState({ hidden: false })}>
          Open modal
        </Button>


        <Modal hidden={this.state.hidden} onClose={this._onClose}>
          <HeadingText type={HeadingText.TYPE.HEADING_3}>Modal</HeadingText>


          <BlockText
            spacingType={[
              BlockText.SPACING_TYPE.EXTRA_LARGE,
              BlockText.SPACING_TYPE.OMIT,
            ]}
          >
            Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
            eiusmod tempor incididunt ut labore et dolore magna aliqua. Dictumst
            quisque sagittis purus sit amet.
          </BlockText>


          <Button onClick={this._onClose}>Close</Button>
        </Modal>
      </>
    );
  }
}
```

#### 마운트 해제

```js
class MyNerdlet extends React.PureComponent {
  constructor() {
    super(...arguments);


    this._onClick = this._onClick.bind(this);
    this._onClose = this._onClose.bind(this);
    this._onHideEnd = this._onHideEnd.bind(this);


    this.state = {
      hidden: true,
      mounted: false,
    };
  }


  _onClick() {
    this.setState({
      hidden: false,
      mounted: true,
    });
  }


  _onClose() {
    this.setState({ hidden: true });
  }


  _onHideEnd() {
    this.setState({ mounted: false });
  }


  render() {
    return (
      <>
        <Button onClick={this._onClick}>Open modal</Button>


        {this.state.mounted && (
          <Modal
            hidden={this.state.hidden}
            onClose={this._onClose}
            onHideEnd={this._onHideEnd}
          >
            <HeadingText type={HeadingText.TYPE.HEADING_3}>Modal</HeadingText>


            <BlockText
              spacingType={[
                BlockText.SPACING_TYPE.EXTRA_LARGE,
                BlockText.SPACING_TYPE.OMIT,
              ]}
            >
              Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
              eiusmod tempor incididunt ut labore et dolore magna aliqua.
              Dictumst quisque sagittis purus sit amet.
            </BlockText>


            <Button onClick={this._onClose}>Close</Button>
          </Modal>
        )}
      </>
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabelledBy` <h5>끈</h5>
      </td>

      <td>
        화면 판독기에서 올바르게 발표되도록 모달의 목적을 더 잘 설명하는 텍스트 콘텐츠의 문자열을 전달합니다.

        ```js
        const modalLabelId = 'my-nerdlet.my-modal';

        <Modal ariaLabelledBy={modalLabelId}>
          <span id={modalLabelId}>
            <HeadingText>My modal</HeadingText>
          </span>
        </Modal>;
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>마디</h5>
      </td>

      <td>
        모달 내부에 렌더링할 콘텐츠입니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다.
      </td>
    </tr>

    <tr>
      <td>
        `hidden` <h5>부울</h5>
      </td>

      <td>
        `true` 이면 모달이 숨겨집니다.
      </td>
    </tr>

    <tr>
      <td>
        `onClose` <h5>필수</h5><h5>기능</h5>
      </td>

      <td>
        Modal의 닫기 버튼을 클릭하면 콜백이 실행됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `onHideEnd` <h5>기능</h5>
      </td>

      <td>
        모달이 닫는 애니메이션을 완료하면 콜백이 시작됩니다. 이를 사용하여 모달 구성요소를 마운트 해제합니다. 이렇게 하면 닫는 애니메이션이 제대로 작동합니다.

        ```js
        function render() {
          <>
            {this.state.mounted && (
              <Modal
                hidden={this.state.hidden}
                onHideEnd={() => this.setState({ mounted: false })}
                onClose={() => this.setState({ hidden: true })}
              >
                <h1>Modal</h1>
                <p>
                  Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
                  eiusmod tempor incididunt ut labore et dolore magna aliqua.
                </p>
              </Modal>
            )}
          </>;
        }
        ```
      </td>
    </tr>

    <tr>
      <td>
        `onHideStart` <h5>기능</h5>
      </td>

      <td>
        Modal이 닫기 애니메이션을 시작할 때 콜백이 시작됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `onShowEnd` <h5>기능</h5>
      </td>

      <td>
        Modal이 오프닝 애니메이션을 마치면 콜백이 실행됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `onShowStart` <h5>기능</h5>
      </td>

      <td>
        Modal이 오프닝 애니메이션을 시작할 때 콜백이 시작됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>
  </tbody>
</table>