---
title: 세그먼트 제어
metaDescription: Learn how to work the SegmentedControl component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

한 번에 하나만 활성화할 수 있는 버튼 세트를 표시하는 구성 요소입니다. '제어 모드'( `value` prop 설정을 통해)에서 사용하거나 제어되지 않을 수 있습니다.

### 용법

```js
import { SegmentedControl } from 'nr1'
```

### 예

#### 기초적인

```js
<SegmentedControl onChange={(evt, value) => console.log(evt, value)}>
  <SegmentedControlItem value="one" label="One" />
  <SegmentedControlItem value="two" label="Two" />
  <SegmentedControlItem disabled value="three" label="Three" />
  <SegmentedControlItem value="four" label="Four" />
</SegmentedControl>
```

#### 몇 가지 힌트와 함께

```js
<SegmentedControl onChange={(evt, value) => console.log(evt, value)}>
  <SegmentedControlItem value="one" label="One" />
  <SegmentedControlItem value="two" label="Two" />
  <SegmentedControlItem
    hint="The third item"
    disabled
    value="three"
    label="Three"
  />
  <SegmentedControlItem hint="The fourth item" value="four" label="Four" />
</SegmentedControl>
```

#### 통제됨

```js
class TestComponent extends React.Component {
  constructor(props) {
    super(props);


    this.state = {
      value: 'one',
    };
  }


  render() {
    return (
      <SegmentedControl
        value={this.state.value}
        onChange={(evt, value) => this.setState({ value })}
      >
        <SegmentedControlItem value="one" label="One" />
        <SegmentedControlItem value="two" label="Two" />
        <SegmentedControlItem disabled value="three" label="Three" />
        <SegmentedControlItem value="four" label="Four" />
      </SegmentedControl>
    );
  }
}
```

#### 아이콘 포함

```js
class TestComponent extends React.Component {
  constructor(props) {
    super(props);


    this.state = {
      value: 'one',
    };
  }


  render() {
    return (
      <SegmentedControl
        value={this.state.value}
        onChange={(evt, value) => this.setState({ value })}
      >
        <SegmentedControlItem
          label="One"
          value="one"
          iconType={
            SegmentedControlItem.ICON_TYPE.HARDWARE_AND_SOFTWARE__SOFTWARE__CODE
          }
        />
        <SegmentedControlItem
          label="Two"
          value="two"
          iconType={
            SegmentedControlItem.ICON_TYPE.INTERFACE__OPERATIONS__FILTER
          }
        />
        <SegmentedControlItem
          label="Three"
          value="three"
          iconType={SegmentedControlItem.ICON_TYPE.INTERFACE__SIGN__NUMBER}
        />
      </SegmentedControl>
    );
  }
}
```

#### 몇 가지 힌트만 있는 아이콘

```js
class TestComponent extends React.Component {
  constructor(props) {
    super(props);


    this.state = {
      value: 'one',
    };
  }


  render() {
    return (
      <SegmentedControl
        type={SegmentedControl.TYPE.ICONS_ONLY}
        value={this.state.value}
        onChange={(evt, value) => this.setState({ value })}
      >
        <SegmentedControlItem
          label="One"
          value="one"
          iconType={
            SegmentedControlItem.ICON_TYPE.HARDWARE_AND_SOFTWARE__SOFTWARE__CODE
          }
        />
        <SegmentedControlItem
          label="Two"
          value="two"
          iconType={
            SegmentedControlItem.ICON_TYPE.INTERFACE__OPERATIONS__FILTER
          }
          hint="The second item."
        />
        <SegmentedControlItem
          label="Three"
          value="three"
          iconType={SegmentedControlItem.ICON_TYPE.INTERFACE__SIGN__NUMBER}
          hint="The third item."
        />
      </SegmentedControl>
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        이 컨트롤에 대한 기술 라벨을 제공하세요. 예: "테마 선택기".
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드</h5>
      </td>

      <td>
        컨트롤에서 클릭할 수 있는 세그먼트를 설명하는 최대 5개의 `<SegmentedControlItem />`에 대한 설명입니다. 요소는 단지 `label` 또는 `label` 및 `icon` 중 하나를 가질 수 있지만 하위 항목 전체에서 동일해야 합니다. 모든 요소에는 해당 버튼을 비활성화하는 선택적 `disabled` 소품이 있을 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        `<SegmentedControlItem />` 을 클릭할 때마다 해당 요소의 `value` 속성을 사용하여 호출됩니다. 현재 선택된 요소를 다시 클릭해도 실행됩니다. 상태를 제어하려는 경우 이 콜백을 사용하여 `value` 소품을 업데이트할 수 있습니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":""},{"name":"value","type":"any","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          SegmentedControl.SPACING_TYPE.EXTRA_LARGE, SegmentedControl.SPACING_TYPE.LARGE, SegmentedControl.SPACING_TYPE.MEDIUM, SegmentedControl.SPACING_TYPE.NONE, SegmentedControl.SPACING_TYPE.OMIT, SegmentedControl.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`입니다.**참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>열거</h5>
      </td>

      <td>
        아이콘만 표시하려면 이를 `SegmentedControl.TYPE.ICONS_ONLY` 로 설정합니다. 하위 항목에서 아이콘을 찾을 수 없으면 무시됩니다.

        <OptionReference>
          SegmentedControl.TYPE.ICONS_ONLY, SegmentedControl.TYPE.NORMAL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>어느</h5>
      </td>

      <td>
        이 소품을 업데이트하여 컨트롤에서 현재 선택된 옵션을 설정합니다.
      </td>
    </tr>
  </tbody>
</table>