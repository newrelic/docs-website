---
title: 링크
metaDescription: Learn how to work the Link component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

애플리케이션 및 외부 링크에 대해 선언적이고 접근 가능한 탐색 기능을 제공합니다.

### 용법

```js
import { Link } from 'nr1'
```

### 예

#### 기초적인

```js
<Link to="https://www.google.com">Click me</Link>
```

#### 위치 객체 포함

```js
function render() {
  const launcher = { id: 'nr1-core.explorer' };
  const location = navigation.getOpenLauncherLocation(launcher);


  return <Link to={location}>Click me</Link>;
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        화면 판독기에서 사용자를 위한 구성 요소 작업의 컨텍스트를 더 잘 설명하는 데 사용합니다.

        ```js
        <Link to="/dashboards" ariaLabel="Go to your dashboard">Go</Link> 
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드</h5>
      </td>

      <td>
        링크의 내용.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>기능</h5>
      </td>

      <td>
        사용자가 링크를 클릭할 때마다 콜백이 실행됩니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `role` <h5>끈</h5>
      </td>

      <td>
        ARIA 속성은 `role="menuitem"` 과 같이 접근성을 위한 `Link` 의 역할을 설명하는 데 사용됩니다. 기본적으로 `Link` 의 역할은 "링크"이므로 명시적인 역할은 `role="menu"` 요소 및 기타 요소와 같은 액세스 가능한 다른 요소 내에서 사용하기 위한 것입니다. `role`가 적용될 수 있는 항목을 보려면 [ARIA 모범 사례를 참조하세요.](https://www.w3.org/TR/wai-aria-practices/) `Link` 및 어떤 맥락에서.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          링크.SPACING_TYPE.EXTRA_LARGE, 링크.SPACING_TYPE.LARGE, 링크.SPACING_TYPE.MEDIUM, 링크.SPACING_TYPE.NONE, 링크.SPACING_TYPE.OMIT, 링크.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>모양|끈</h5>
      </td>

      <td>
        연결할 위치 개체 또는 URL 문자열입니다. <h3>모양</h3>

        `pathname` <h5>필수</h5><h5>문자열</h5>

        `search` <h5>끈</h5>

        `hash` <h5>끈</h5>
      </td>
    </tr>
  </tbody>
</table>