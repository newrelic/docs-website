---
title: 블록텍스트
metaDescription: Learn how to work the BlockText component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

타이포그래피 스타일은 UI 전반에 걸쳐 사용됩니다. 일관되게 사용하면 사용자에게 명확한 계층 구조를 유지하는 데 도움이 됩니다.

### 용법

```js
import { BlockText } from 'nr1'
```

### 예

#### 기초적인

```js
<>
  <BlockText>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
</>
```

#### 유형

```js
<>
  <BlockText type={BlockText.TYPE.NORMAL}>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
  <BlockText type={BlockText.TYPE.PARAGRAPH}>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
</>
```

#### 의미 변경

```js
<BlockText type={BlockText.TYPE.PARAGRAPH} tagType={BlockText.TYPE.DIV}>
  This is a div element styled as a paragraph
</BlockText>
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>마디</h5>
      </td>

      <td>
        표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          BlockText.SPACING_TYPE.EXTRA_LARGE, BlockText.SPACING_TYPE.LARGE, BlockText.SPACING_TYPE.MEDIUM, BlockText.SPACING_TYPE.NONE, BlockText.SPACING_TYPE.OMIT, BlockText.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `tagType` <h5>열거</h5>
      </td>

      <td>
        텍스트를 래핑할 태그입니다. 텍스트의 의미론적 의미를 변경하는 데 유용합니다. 이는 어떤 스타일에도 적용되지 않습니다.

        <OptionReference>
          BlockText.태그.DIV, BlockText.태그.P
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>열거</h5>
      </td>

      <td>
        표시하려는 텍스트 유형입니다. 유형에 따라 스타일이 적용됩니다.

        <OptionReference>
          BlockText.TYPE.NORMAL, BlockText.TYPE.PARAGRAPH
        </OptionReference>
      </td>
    </tr>
  </tbody>
</table>