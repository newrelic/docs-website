---
title: 텍스트 필드
metaDescription: Learn how to work the TextField component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

양식을 사용하면 사용자가 작업을 완료하기 위한 입력을 제공할 수 있습니다.

### 용법

```js
import { TextField } from 'nr1'
```

### 예

#### 기초적인

```js
<TextField label="Username" placeholder="e.g. John Doe" />
```

#### 인라인 라벨 포함

```js
<TextField label="Username" labelInline placeholder="e.g. John Doe" />
```

#### 찾다

```js
<TextField type={TextField.TYPE.SEARCH} placeholder="e.g. Placeholder" />
```

#### 비밀번호

```js
<TextField type={TextField.TYPE.PASSWORD} placeholder="e.g. Placeholder" />
```

#### 미리 포맷된

```js
<TextField type={TextField.TYPE.PREFORMATTED} placeholder="npm run build" />
```

#### 로드 중

```js
<TextField loading placeholder="e.g Placeholder" />
```

#### 장애가 있는

```js
<TextField defaultValue="Some value" disabled placeholder="e.g Placeholder" />
```

#### 읽기 전용

```js
<TextField defaultValue="Some value" readOnly placeholder="e.g Placeholder" />
```

#### 설명 포함

```js
<TextField description="Description value" placeholder="e.g Placeholder" />
```

#### 잘못된 메시지 포함

```js
<TextField
  invalid="Not enough text, needs more"
  placeholder="e.g Placeholder"
/>
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>끈</h5>
      </td>

      <td>
        이 입력을 설명하기 위해 화면 판독기에 레이블을 제공하십시오. 이는 스크린 리더 라벨이 `label` 소품과 달라야 하거나 `label` 소품을 전혀 제공하지 않는 경우에만 필요합니다.
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabelledBy` <h5>끈</h5>
      </td>

      <td>
        스크린 리더에 대한 이 입력을 설명하는 콘텐츠가 있는 요소의 고유한 `id` 제공합니다. 이는 스크린 리더 라벨이 `label` 소품과 달라야 하거나 `label` 소품을 전혀 제공하지 않는 경우에만 필요합니다. 이 항목 또는 `ariaLabel` 사용하세요. 둘 다 사용할 수는 없습니다.
      </td>
    </tr>

    <tr>
      <td>
        `autoFocus` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 마운트 시 텍스트 필드가 자동으로 초점을 맞춥니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>끈</h5>
      </td>

      <td>
        텍스트 필드의 기본값입니다.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>끈</h5>
      </td>

      <td>
        양식 필드를 채우는 방법에 대한 지침이 포함된 메시지입니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        필드를 편집할 수 있는지 여부를 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>끈</h5>
      </td>

      <td>
        추가 정보는 라벨 옆의 정보 도구 설명에 표시될 수 있습니다.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>부울|문자열</h5>
      </td>

      <td>
        true인 경우 필드를 유효하지 않은 상태로 설정하여 사용자에게 이 특정 필드에 대한 주의가 필요함을 알립니다. 이 속성은 `boolean` 필드 또는 `string` 일 수 있습니다. `string` 이고 유효하지 않은 상태가 표시되면 텍스트가 아래에 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>끈</h5>
      </td>

      <td>
        라벨로 표시할 텍스트입니다.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>부울</h5>
      </td>

      <td>
        양식 컨트롤 인라인에 레이블을 표시합니다. 구성요소가 `Form` 내부에 없는 경우에만 사용하십시오. 이 경우 `Form` 구성요소에서 `layoutType` `Form.LAYOUT_TYPE.SPLIT` 로 설정합니다.
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>부울</h5>
      </td>

      <td>
        데이터 목록이나 다른 항목을 양식 필드에 로드하는 경우 로드 상태 양식을 사용하여 항목이 로드 중임을 사용자에게 나타냅니다. 필드가 값을 동적으로 로드할 수 있음을 알리는 데 사용됩니다. `true` 으로 설정하면 로딩 스피너가 표시됩니다. `false` 로 설정하면 스피너가 잠재적으로 표시될 수 있도록 UI에서 일부 공간을 예약합니다. 특정 지점에서 필드가 로드되지 않을 것으로 예상되는 경우 속성을 설정하지 마십시오(또는 `undefined` 로 설정). 제 시간에.
      </td>
    </tr>

    <tr>
      <td>
        `multiline` <h5>더 이상 사용되지 않는</h5> <h5>부울</h5>
      </td>

      <td>
        <Callout
          variant="caution"
          title="마감일: 2022년 1월 1일"
        >
          "multiline" 소품은 더 이상 사용되지 않습니다. 대신 여러 줄 용도로 MultilineTextField 구성 요소를 사용하세요.
        </Callout>

        입력이 하나 이상의 라인을 허용하는지 여부입니다. 이는 단일 라인 필드에서 작동하지 않는 것과는 반대로 새 라인을 생성하는 \[Enter] 키의 동작에도 영향을 미칩니다.
      </td>
    </tr>

    <tr>
      <td>
        `onBlur` <h5>기능</h5>
      </td>

      <td>
        텍스트 필드가 포커스를 잃을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        텍스트 필드 값이 변경되면 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback. You can pull out the new value by accessing `event.target.value`."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onFocus` <h5>기능</h5>
      </td>

      <td>
        텍스트 필드에 포커스가 있을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyDown` <h5>기능</h5>
      </td>

      <td>
        키를 눌렀을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyUp` <h5>기능</h5>
      </td>

      <td>
        키를 놓을 때 실행되는 콜백입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `placeholder` <h5>끈</h5>
      </td>

      <td>
        값이 없을 때 텍스트 필드에 힌트가 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `readOnly` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 텍스트 필드를 읽기 전용으로 만듭니다.
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 필수 양식 필드를 나타냅니다.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          TextField.SPACING_TYPE.EXTRA_LARGE, TextField.SPACING_TYPE.LARGE, TextField.SPACING_TYPE.MEDIUM, TextField.SPACING_TYPE.NONE, TextField.SPACING_TYPE.OMIT, TextField.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spellCheck` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 브라우저에서 철자 오류 검사를 활성화합니다.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`. **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>열거</h5>
      </td>

      <td>
        * 텍스트 — 사용자가 필드에 한 줄의 텍스트를 입력하는 경우

        * 이메일 — 사용자가 이메일 주소를 입력해야 하는 경우

        * 검색 — 사용자가 검색해야 하는 경우

        * 사전 형식화됨 — 사용자가 코드 내용을 입력해야 하는 경우

        * 비밀번호 — 사용자가 비밀번호를 입력할 때

        * URL — 사용자가 URL을 입력할 때

          <OptionReference>
            TextField.TYPE.EMAIL, TextField.TYPE.PASSWORD, TextField.TYPE.PREFORMATTED, TextField.TYPE.SEARCH, TextField.TYPE.TEXT, TextField.TYPE.URL,
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>끈</h5>
      </td>

      <td>
        텍스트 필드의 값입니다. 이는 제어되는 구성 요소에 필요합니다.
      </td>
    </tr>
  </tbody>
</table>