---
title: 드롭다운항목
metaDescription: Learn how to work the DropdownItem component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Dropdown>` 구성요소의 하위 요소입니다.

`onClick` 소품을 통해 클릭 이벤트를 지원하고 `to` 소품을 통해 다른 페이지로 리디렉션할 수 있는 가능성을 지원합니다.

`items` 소품을 통해 그리고 렌더링 함수를 `children` 소품에 전달하여 하위 항목을 지원합니다.

### 용법

```js
import { DropdownItem } from 'nr1'
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드 | (노드|함수)\[]</h5>
      </td>

      <td>
        항목 내부에 표시할 콘텐츠입니다. 또한 중첩된 목록을 렌더링할 때 렌더링 메서드를 마지막 하위 항목으로 허용합니다. 뒷면을 `items` prop에 전달해야 합니다.

        ```js
        import { Dropdown, DropdownItem } from 'nr1';

        const nestedItems = [
          { id: '3.1', text: 'Item 3.1' },
          { id: '3.2', text: 'Item 3.2' },
          { id: '3.3', text: 'Item 3.3' },
          { id: '3.4', text: 'Item 3.4' },
        ];

        const nestedDropdown = (
          <Dropdown title="Nested Dropdown">
            <DropdownItem>Item 1</DropdownItem>
            <DropdownItem>Item 2</DropdownItem>
            <DropdownItem items={nestedItems}>
              Item 3
              {({ item }) => <DropdownItem key={item.id}>{item.text}</DropdownItem>}
            </DropdownItem>
            <DropdownItem>Item 4</DropdownItem>
          </Dropdown>
        );
        ```
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 해당 항목을 상호작용할 수 없으며 onClick 콜백이 실행되지 않습니다.
      </td>
    </tr>

    <tr>
      <td>
        `items` <h5>정렬</h5>
      </td>

      <td>
        현재 항목의 하위 목록을 렌더링할 항목입니다. 또한 이 소품은 항목의 콘텐츠가 작동하려면 마지막 `children` 으로 함수를 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>기능</h5>
      </td>

      <td>
        사용자가 항목을 클릭할 때마다 콜백이 실행됩니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selected` <h5>부울</h5>
      </td>

      <td>
        옵션이 선택된 것으로 표시됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`입니다.**참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>모양|끈</h5>
      </td>

      <td>
        연결할 위치 개체 또는 URL 문자열입니다.

        <h3>모양</h3>

        `pathname` <h5>필수</h5><h5>문자열</h5>

        `search` <h5>끈</h5>

        `hash` <h5>끈</h5>
      </td>
    </tr>
  </tbody>
</table>