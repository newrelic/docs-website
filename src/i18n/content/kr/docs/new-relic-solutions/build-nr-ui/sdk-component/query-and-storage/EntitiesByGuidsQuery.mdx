---
title: EntitiesByGuidsQuery
metaDescription: Learn how to work the EntitiesByGuidsQuery component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

GUID별로 엔터티 목록을 쿼리합니다.

하나의 쿼리로 최대 25개의 엔터티를 가져올 수 있습니다.

### 용법

```js
import { EntitiesByGuidsQuery } from 'nr1'
```

### 예

#### 선언적 쿼리

```js
<EntitiesByGuidsQuery
  entityGuids={[
    'MTIzNDU2fEZPT3xCQVJ8OTg3NjU0MzIx',
    'MTIzNDU2fEZPT3xCQVJ8OTg3NjU0MzUz',
  ]}
>
  {({ loading, error, data }) => {
    if (loading) {
      return <Spinner />;
    }


    if (error) {
      return 'Error!';
    }


    return (
      <List items={data.entities} rowHeight={20}>
        {({ item }) => <ListItem key={item.guid}>{item.name}</ListItem>}
      </List>
    );
  }}
</EntitiesByGuidsQuery>
```

#### 필수 쿼리

```js
EntitiesByGuidsQuery.query({
  entityGuids: [
    'MTIzNDU2fEZPT3xCQVJ8OTg3NjU0MzIx',
    'MTIzNDU2fEZPT3xCQVJ8OTg3NjU0MzUz',
  ],
}).then(({ data }) => console.log(data));
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>기능</h5>
      </td>

      <td>
        소품 기능을 자식으로 렌더링합니다.

        <FunctionDefinition
          returnValue={[{"type":"React.ReactNode","description":""}]}
          arguments={[{"name":"queryResult","type":"QueryResult","description":"Results of the query."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `entityFragmentExtension` <h5>물체</h5>
      </td>

      <td>
        `graphql-tag` 에 의해 AST로 구문 분석된 GraphQL 조각 문서. 쿼리 구성 요소는 엔터티에서 사용할 수 있는 가장 일반적으로 사용되는 필드를 반환합니다. 쿼리에서 반환된 엔터티에 대한 추가 필드를 요청하려는 경우 이 소품을 사용할 수 있습니다. 조각 이름은 `EntityFragmentExtension` 이어야 하며 `EntityOutline` 유형에 적용되어야 합니다.

        ```js
        const entityFragmentExtension = ngql`
          fragment EntityFragmentExtension on EntityOutline {
            indexedAt
            guid
          }
        `;
        ```
      </td>
    </tr>

    <tr>
      <td>
        `entityGuids` <h5>필수</h5><h5>문자열\[]</h5>
      </td>

      <td>
        쿼리할 엔터티의 GUID입니다.
      </td>
    </tr>

    <tr>
      <td>
        `fetchPolicyType` <h5>열거</h5>
      </td>

      <td>
        쿼리가 캐시된 데이터와 상호 작용하는 방법을 지정할 수 있습니다.

        * `CACHE_AND_NETWORK`: 쿼리는 가능한 경우 캐시에서 초기 데이터를 반환합니다. 그러나 전체 데이터가 캐시에 있는지 여부에 관계없이 쿼리는 항상 네트워크 인터페이스를 사용하여 요청하고 업데이트된 데이터를 반환합니다. 구성요소의 정적 `query()` 메소드를 사용하는 경우에는 이 옵션을 사용할 수 없습니다.

        * `CACHE_FIRST`: 쿼리는 쿼리에 대한 데이터가 아직 캐시에 없는 경우 **에만** 네트워크 인터페이스를 사용하여 요청합니다.

        * `CACHE_ONLY`: 쿼리는 네트워크 인터페이스를 사용하여 요청 **하지 않습니다** . 대신 캐시에서 사용 가능한 데이터를 반환합니다. 쿼리에 대한 데이터가 캐시에 없으면 오류가 발생합니다.

        * `NETWORK_ONLY`: 쿼리는 캐시에서 초기 데이터를 반환 **하지 않습니다** . 대신 **항상** 네트워크 인터페이스를 사용하여 요청합니다.

        * `NO_CACHE`: 쿼리는 캐시에서 초기 데이터를 반환 **하지 않습니다** . 대신 **항상** 네트워크 인터페이스를 사용하여 요청합니다. `NETWORK_ONLY` 정책과 달리 쿼리가 완료된 후 캐시에 데이터를 쓰지 않습니다.

          <OptionReference>
            EntitiesByGuidsQuery.FETCH_POLICY_TYPE.CACHE_AND_NETWORK, EntitiesByGuidsQuery.FETCH_POLICY_TYPE.CACHE_FIRST, EntitiesByGuidsQuery.FETCH_POLICY_TYPE.CACHE_ONLY, EntitiesByGuidsQuery.FETCH_POLICY_TYPE.NETWORK_ONLY, EntitiesByGuidsQuery.FETCH_POLICY_TYPE.NO_CACHE,
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `includeSummaryMetrics` <h5>부울</h5>
      </td>

      <td/>
    </tr>

    <tr>
      <td>
        `includeTags` <h5>부울</h5>
      </td>

      <td>
        `true` 인 경우 반환된 엔터티에는 해당 태그가 포함됩니다.
      </td>
    </tr>

    <tr>
      <td>
        `pollInterval` <h5>숫자</h5>
      </td>

      <td>
        새 데이터를 폴링하는 간격(밀리초)입니다. 모든 종류의 정기적인 폴링을 방지하려면 0으로 설정하십시오.
      </td>
    </tr>

    <tr>
      <td>
        `skip` <h5>부울</h5>
      </td>

      <td>
        `true` 으로 설정하면 렌더링에서 쿼리를 완전히 건너뜁니다.
      </td>
    </tr>

    <tr>
      <td>
        `sortBy` <h5>열거형\[]</h5>
      </td>

      <td>
        엔터티 검색 결과를 정렬하는 데 사용되는 기준입니다.

        <OptionReference array>
          EntitiesByGuidsQuery.SORT_TYPE.알림, EntitiesByGuidsQuery.SORT_TYPE.도메인, EntitiesByGuidsQuery.SORT_TYPE.MOST_RELEVANT, EntitiesByGuidsQuery.SORT_TYPE.NAME, EntitiesByGuidsQuery.SORT_TYPE.REPORTING, EntitiesByGuidsQuery.SORT_TYPE.TYPE,
        </OptionReference>
      </td>
    </tr>
  </tbody>
</table>

### 행동 양식

### `EntitiesByGuidsQuery.query`

<FunctionDefinition
  arguments={[{"description":"Object containing the query options. Any `EntitiesByGuidsQuery` prop is a valid option except `children` and `pollInterval`.","name":"props","type":"Object"}]}
  returnValue={{"description":"","type":"PromiseQueryResult"}}
/>

### 유형 정의

<TypeDefReference typeDef={{"name":"PromiseQueryResult","properties":[{"description":"Runtime error with `graphQLErrors` and `networkError` properties.","name":"error","type":"ApolloClient.ApolloError"},{"description":"Object containing the result of your query.","name":"data","type":"Object"},{"description":"If not `null`, `fetchMore` allows you to load more results for your query. New data is merged with previous data.","name":"fetchMore","type":"function|null"},{"description":"Refetch the query.","name":"refetch","type":"function"}]}}/>

<TypeDefReference typeDef={{"name":"QueryResult","properties":[{"description":"Indicates that the request is in flight.","name":"loading","type":"boolean"},{"description":"Runtime error with `graphQLErrors` and `networkError` properties.","name":"error","type":"ApolloClient.ApolloError"},{"description":"Object containing the result of your query.","name":"data","type":"Object"},{"description":"If not `null`, `fetchMore` allows you to load more results for your query. New data is merged with previous data.","name":"fetchMore","type":"function|null"},{"description":"Refetch the query.","name":"refetch","type":"function"}]}}/>