---
title: 타일그룹
metaDescription: Learn how to work the TileGroup component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

### 용법

```js
import { TileGroup } from 'nr1'
```

### 예

#### 기본 타일 그룹

```js
<TileGroup>
  <Tile onClick={console.log}>Tile 1</Tile>
  <Tile onClick={console.log}>Tile 2</Tile>
  <Tile onClick={console.log}>Tile 3</Tile>
</TileGroup>
```

#### 간격이 작은 타일 그룹

```js
<TileGroup gapType={TileGroup.GAP_TYPE.SMALL}>
  <Tile onClick={console.log}>Tile 1</Tile>
  <Tile onClick={console.log}>Tile 2</Tile>
  <Tile onClick={console.log}>Tile 3</Tile>
</TileGroup>
```

#### 3개의 열이 있는 타일 그룹

```js
<TileGroup tileWidth="4fr">
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### 사용자 정의 타일 너비가 있는 타일 그룹

```js
<TileGroup tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### 선택 가능한 타일

```js
<TileGroup selectionType={TileGroup.SELECTION_TYPE.SINGLE} tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### 기본 타일이 선택된 선택 가능한 타일

```js
<TileGroup
  defaultValue="tile-0"
  selectionType={TileGroup.SELECTION_TYPE.SINGLE}
  tileWidth={120}
>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile value={`tile-${index}`} onClick={console.log}>
      Tile {index + 1}
    </Tile>
  ))}
</TileGroup>
```

#### 제어 가능한 타일

```js
class MyComponent extends React.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      checkedTile: 'tile-0',
    };
    this._onChange = this._onChange.bind(this);
  }
  _onChange(evt, tileValue) {
    this.setState({ checkedTile: tileValue });
  }
  render() {
    const { checkedTile } = this.state;
    return (
      <TileGroup
        value={checkedTile}
        onChange={this._onChange}
        tileWidth={120}
        selectionType={TileGroup.SELECTION_TYPE.SINGLE}
      >
        {Array.from({ length: 12 }).map((_, index) => (
          <Tile
            sizeType={Tile.SIZE_TYPE.SMALL}
            value={`tile-${index}`}
            key={index}
            onClick={console.log}
          >
            Tile {index + 1}
          </Tile>
        ))}
      </TileGroup>
    );
  }
}
```

#### 다중 선택 타일

```js
<TileGroup selectionType={TileGroup.SELECTION_TYPE.MULTIPLE} tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### 제어된 다중 선택 타일

```js
class MyComponent extends React.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      checkedTiles: [],
    };
    this._onChange = this._onChange.bind(this);
  }
  _onChange(evt, tileValue, checked) {
    this.setState((state) => {
      return {
        checkedTiles: checked
          ? [...state.checkedTiles, tileValue]
          : state.checkedTiles.filter((value) => value !== tileValue),
      };
    });
  }
  render() {
    const { checkedTiles } = this.state;
    return (
      <TileGroup
        value={checkedTiles}
        onChange={this._onChange}
        tileWidth={120}
        selectionType={TileGroup.SELECTION_TYPE.MULTIPLE}
      >
        {Array.from({ length: 12 }).map((_, index) => (
          <Tile
            sizeType={Tile.SIZE_TYPE.SMALL}
            value={`tile-${index}`}
            key={index}
            onClick={console.log}
          >
            Tile {index + 1}
          </Tile>
        ))}
      </TileGroup>
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드</h5>
      </td>

      <td>
        표시할 타일입니다.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>끈</h5>
      </td>

      <td>
        구성 요소에 클래스 이름을 추가합니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>문자열|번호|문자열\[]|번호\[]</h5>
      </td>

      <td>
        확인해야 할 초기 타일입니다.
      </td>
    </tr>

    <tr>
      <td>
        `gapType` <h5>열거</h5>
      </td>

      <td>
        타일 사이의 간격 크기입니다.<OptionReference>TileGroup.GAP_TYPE.MEDIUMTileGroup.GAP_TYPE.SMALL</OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        타일을 선택하거나 선택 취소할 때 호출되는 함수입니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":"Event source of the callback."},{"name":"tileValue","type":"any","description":"The value of the tile."},{"name":"checked","type":"boolean","description":"The new checked state."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selectionType` <h5>열거</h5>
      </td>

      <td>
        <OptionReference>
          TileGroup.SELECTION_TYPE.MULTIPLE, TileGroup.SELECTION_TYPE.NONE, TileGroup.SELECTION_TYPE.SINGLE,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>열거형\[]</h5>
      </td>

      <td>
        간격 속성. 간격은 0-4개 값의 튜플로 정의되며, 이는 `margin` 또는 `padding` 같은 CSS 속성과 동일한 규칙을 따릅니다. 값을 생략하려면 `SPACING_TYPE.OMIT` 사용하세요.

        <OptionReference array>
          TileGroup.SPACING_TYPE.EXTRA_LARGE, TileGroup.SPACING_TYPE.LARGE, TileGroup.SPACING_TYPE.MEDIUM, TileGroup.SPACING_TYPE.NONE, TileGroup.SPACING_TYPE.OMIT, TileGroup.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>물체</h5>
      </td>

      <td>
        사용자 정의 스타일을 위한 인라인 스타일입니다. 위치 지정 및 간격 지정 목적으로만 사용해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>끈</h5>
      </td>

      <td>
        `data-test-id` 속성을 추가합니다. 이를 사용하여 단위 및 E2E 테스트의 구성 요소를 목표로 삼습니다. 테스트 ID가 유효하려면 앞에 nerdpack ID를 붙이고 그 뒤에 점이 옵니다. 예를 들어 `my-nerdpack.some-element`.

        **참고:** `data-test-id` 속성이 DOM 에서 제거될 때 표시되지 않을 수 있습니다. 이를 디버그하려면 `e2e-test` 쿼리 변수를 URL에 전달해야 합니다.
      </td>
    </tr>

    <tr>
      <td>
        `tileWidth` <h5>문자열|숫자</h5>
      </td>

      <td>
        열의 너비를 나타냅니다. 열의 너비는 항상 동일합니다. 다음 유형 중 하나일 수 있습니다.

        * `'YYpx'`: 여기서 `YY` 은 숫자이며 열의 최소 너비를 픽셀 단위로 나타냅니다. 타일이 사용 가능한 너비에 맞지 않으면 새 행에 래핑됩니다.
        * `'YYfr'`: 여기서 `YY` 은 숫자이며 12분수 그리드에서 사용 가능한 너비의 일부를 나타냅니다. 타일 너비가 `'6fr'` 인 타일 그룹인 경우 각 타일이 사용 가능한 너비의 50%를 차지하는 두 개의 열(`12fr/6fr = 2`)을 항상 렌더링합니다.
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>문자열|번호|문자열\[]|번호\[]</h5>
      </td>

      <td>
        일치하는 값을 가진 타일이 확인됩니다. 선택 유형이 `multiple` 인 경우 단일 값이거나 값의 제외일 수 있습니다. 정의된 경우 구성요소를 [제어된 구성요소](https://facebook.github.io/react/docs/forms.html) 로 전환합니다.
      </td>
    </tr>
  </tbody>
</table>