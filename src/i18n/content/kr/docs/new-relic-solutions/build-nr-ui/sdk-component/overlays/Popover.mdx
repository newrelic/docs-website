---
title: 팝오버
metaDescription: Learn how to work the Popover component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Popover>` 필요에 따라 작은 오버레이를 표시하기 위한 전환 가능한 오버레이입니다.

`<Popover>` 구성요소를 사용하려면 오버레이 열기/닫기와 `<PopoverBody />` 통한 오버레이 콘텐츠를 처리하는 `<PopoverTrigger>` 트리거를 제공해야 합니다.

### 용법

```js
import { Popover } from 'nr1'
```

### 예

#### 기초적인

```js
<Popover>
  <PopoverTrigger>Trigger</PopoverTrigger>
  <PopoverBody>
    <BlockText>Body</BlockText>
  </PopoverBody>
</Popover>
```

#### 트리거로서의 버튼

```js
<Popover>
  <PopoverTrigger>
    <Button>Trigger</Button>
  </PopoverTrigger>
  <PopoverBody>
    <BlockText>Body</BlockText>
  </PopoverBody>
</Popover>
```

#### 트리거로서의 TextField

```js
<Popover>
  <PopoverTrigger>
    <TextField />
  </PopoverTrigger>
  <PopoverBody>
    <BlockText>Body</BlockText>
  </PopoverBody>
</Popover>
```

#### 팝오버바닥글 사용

```js
<Popover>
  <PopoverTrigger>
    <Button>Trigger</Button>
  </PopoverTrigger>
  <PopoverBody>
    <Card style={{ width: '250px' }}>
      <CardBody>
        <HeadingText>Heading</HeadingText>
        <BlockText
          spacingType={[
            BlockText.SPACING_TYPE.MEDIUM,
            BlockText.SPACING_TYPE.NONE,
          ]}
        >
          Item content
        </BlockText>
        <BlockText>Item content</BlockText>
      </CardBody>
    </Card>
    <PopoverFooter style={{ textAlign: 'right' }}>Info message</PopoverFooter>
  </PopoverBody>
</Popover>
```

#### 제어된 팝오버

```js
class ControlledPopover extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      opened: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _onChange(evt, opened) {
    this.setState({ opened }, () =>
      console.log('controlled state', evt, this.state),
    );
  }


  render() {
    const { opened } = this.state;


    return (
      <Popover opened={opened} onChange={this._onChange}>
        <PopoverTrigger>
          <Button>Trigger</Button>
        </PopoverTrigger>
        <PopoverBody>
          <Card style={{ width: '250px' }}>
            <CardBody>
              <HeadingText>Heading</HeadingText>
              <BlockText
                spacingType={[
                  BlockText.SPACING_TYPE.MEDIUM,
                  BlockText.SPACING_TYPE.NONE,
                ]}
              >
                Item content
              </BlockText>
              <BlockText>Item content</BlockText>
            </CardBody>
          </Card>
          <PopoverFooter style={{ textAlign: 'right' }}>
            Info message
          </PopoverFooter>
        </PopoverBody>
      </Popover>
    );
  }
}
```

### 소품

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>필수</h5> <h5>노드\[]</h5>
      </td>

      <td>
        이 구성 요소는 두 가지 유형의 하위 항목을 허용합니다.

        * `<PopoverTrigger>` 오버레이를 열거나 닫을 요소를 렌더링합니다.
        * `<PopoverBody>` 오버레이 내부에 콘텐츠를 렌더링합니다.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>기능</h5>
      </td>

      <td>
        제어 모드에서 `Popover` 오버레이가 열리거나 닫힐 때를 추적하는 콜백입니다. 이 메서드는 사용자가 `PopoverTrigger` 작업을 수행하거나 `PopoverBody` 열려 있을 때 외부를 클릭할 때 실행됩니다.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent|React.MouseEvent","description":""},{"name":"opened","type":"boolean","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `openOnHover` <h5>부울</h5>
      </td>

      <td>
        true인 경우 마우스를 올리면 팝오버 오버레이가 열립니다.
      </td>
    </tr>

    <tr>
      <td>
        `opened` <h5>부울</h5>
      </td>

      <td>
        제공되면 `Popover` 제어 모드로 전환됩니다. 부울 값은 팝오버 오버레이를 열거나 닫습니다.
      </td>
    </tr>
  </tbody>
</table>