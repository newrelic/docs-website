---
title: NRDOT를 사용한 Linux RDS MSSQL 모니터링
tags:
  - OpenTelemetry
  - NRDOT
  - SQL Server
  - Linux
  - RDS
  - AWS
metaDescription: Set up Microsoft SQL Server monitoring with NRDOT Collector on Linux for AWS RDS SQL Server instances
freshnessValidatedDate: never
contentType: onboarding-intro
audience:
  - human
  - ai
surface:
  - concept
integration: Microsoft-SQL-Server-with-NRDOT
flow:
  prev: /docs/opentelemetry/database/mssql/introduction
  next: null
translationType: machine
---

<Callout title="시사">
  이 기능은 아직 개발 중이지만 꼭 사용해 보시기 바랍니다!

  이 기능은 현재 [사전 출시 정책](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/) 에 따라 미리보기의 일부로 제공됩니다.
</Callout>

뉴렐릭 Distribution of OpenTelemetry(NRDOT) 수집기를 사용하여 데이터베이스 모니터링, 쿼리 분석 및 시스템 상태 메트릭을 통해 AWS RDS SQL Server 성능에 대한 포괄적인 인사이트를 얻으십시오.

대신 뉴렐릭 UI를 통해 이를 설치하려면 <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; Integrations &amp; Agents &gt; MSSQL (OpenTelemetry)** </DNT>로 이동하십시오. 화면의 지시에 따라 통합을 설정하십시오. 이 페이지는 CLI 설치 경로만 다룹니다.

<Callout variant="tip">
  Linux 자체 호스팅 환경에서 SQL Server를 사용하는 경우 [RDS용 Linux 계측](/docs/opentelemetry/database/mssql/linux-hosted)을 참조하십시오.
</Callout>

<Steps>
  <Step>
    ## 전제 조건 [#prerequisites]

    * 유효한 [라이선스 키](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)가 있는 뉴렐릭 계정

    * 해당 리전의 뉴렐릭 [OTLP 엔드포인트](/docs/opentelemetry/best-practices/opentelemetry-otlp)

    * AWS RDS SQL Server 요구 사항:

      * AWS RDS SQL Server 2017 이상
      * RDS 마스터 사용자 이름 및 비밀번호
      * 포트 `1433` 또는 사용자 지정 포트에서 인바운드 연결을 허용하는 보안 그룹

    * Linux 시스템 요구사항:

      * `sqlcmd` Linux 시스템에 설치된 유틸리티
      * [뉴렐릭 OTLP 엔드포인트](/docs/opentelemetry/best-practices/opentelemetry-otlp)에 대한 네트워크 연결
  </Step>

  <Step>
    ## 미리보기에 등록하십시오 [#enroll]

    이 통합은 뉴렐릭 공개 프리뷰 프로그램의 일환으로 제공됩니다. [Previews &amp; Trials](https://one.newrelic.com/admin-portal/promotion-management/home) 페이지에서 옵트인하려면 조직 관리자에게 문의하십시오.
  </Step>

  <Step>
    ## NRDOT Collector설치하세요 [#setup]

    Linux 배포판용 NRDOT 패키지를 다운로드하고 설치하십시오. `<NRDOT_VERSION>`을(를) [nrdot-collector-releases](https://github.com/newrelic/nrdot-collector-releases/releases) 페이지의 최신 릴리스 태그로 교체하십시오.

    <Callout variant="important">
      RDS SQL Server 인스턴스에 대한 네트워크 연결이 있는 EC2 인스턴스 또는 로컬 머신에 NRDOT Collector를 설치합니다. 보안 그룹이 포트 1433(또는 사용자 지정 포트)에서 인바운드 연결을 허용하는지 확인합니다.
    </Callout>

    <CollapserGroup>
      <Collapser id="deb" title="데비안/우분투용">
        * AMD64 아키텍처용:

          ```bash
          NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_amd64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
          ```

        * ARM64 아키텍처용:

          ```bash
          NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_arm64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
          ```
      </Collapser>

      <Collapser id="rpm" title="RHEL/CentOS용">
        * AMD64 아키텍처용:

          ```bash
          NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_x86_64.rpm" --output nrdot-collector.rpm && sudo rpm -ivh nrdot-collector.rpm
          ```

        * ARM64 아키텍처용:

          ```bash
          NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_arm64.rpm" --output nrdot-collector.rpm && sudo rpm -ivh nrdot-collector.rpm
          ```
      </Collapser>
    </CollapserGroup>
  </Step>

  <Step>
    ## 모니터링 사용자 생성 [#user]

    루트 사용자로 이 스크립트를 실행하여 `newrelic` 모니터링 사용자를 생성하고 RDS SQL Server 메트릭을 수집하는 데 필요한 권한을 부여하십시오.

    1. `nr-grant-permission-rds.sql`(이)라는 이름의 파일을 생성하십시오.

    2. 다음 SQL 스크립트를 붙여넣어 `newrelic` 모니터링 사용자를 생성하고 `<YOUR_PASSWORD>`을(를) 원하는 비밀번호로 변경하십시오:

       ```sql
       USE [master];
       GO
       CREATE LOGIN [newrelic] WITH PASSWORD = '<YOUR_PASSWORD>';
       GO
       -- Instance-level permissions
       GRANT VIEW SERVER STATE TO [newrelic];
       GRANT VIEW ANY DEFINITION TO [newrelic];
       GRANT VIEW ANY DATABASE TO [newrelic];
       GO
       -- Grant read access privileges to all user databases
       DECLARE @name SYSNAME;
       DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
       SELECT [name]
       FROM [master].[sys].[databases]
       WHERE [name] NOT IN ('master', 'msdb', 'model', 'rdsadmin', 'distribution')
       AND [state] = 0; -- Only online databases
       OPEN db_cursor;
       FETCH NEXT FROM db_cursor INTO @name;
       WHILE @@FETCH_STATUS = 0
       BEGIN
         BEGIN TRY
           EXEC('USE [' + @name + '];
             IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''newrelic'')
             BEGIN
               CREATE USER [newrelic] FOR LOGIN [newrelic];
             END;
             GRANT VIEW DATABASE STATE TO [newrelic];');
         END TRY
         BEGIN CATCH
           PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
         END CATCH
         FETCH NEXT FROM db_cursor INTO @name;
       END
       CLOSE db_cursor;
       DEALLOCATE db_cursor;
       GO
       ```

    3. `sqlcmd`을(를) 사용하여 스크립트를 실행하십시오. `<YOUR-RDS-ENDPOINT>`을(를) RDS 엔드포인트로, `<YOUR_MASTER_PASSWORD>`을(를) 마스터 비밀번호로 교체하십시오:

    ```bash
    sqlcmd -S <YOUR-RDS-ENDPOINT> -U <YOUR_MASTER_USER> -P '<YOUR_MASTER_PASSWORD>' -C -i nr-grant-permission-rds.sql
    ```

    3. *(선택 사항)* 올바른 권한을 가진 사용자가 성공적으로 생성되었는지 확인하십시오:

    ```bash
    sqlcmd -S <YOUR-RDS-ENDPOINT> -U <YOUR_MASTER_USER> -P '<YOUR_MASTER_PASSWORD>' -C -Q "SELECT sp.name AS [User], p.permission_name AS [Permission_Granted] FROM sys.server_permissions p JOIN sys.server_principals sp ON p.grantee_principal_id = sp.principal_id WHERE sp.name = 'newrelic';"
    ```

    <Callout variant="tip">
      * RDS 마스터 사용자 이름과 비밀번호를 사용하여 이러한 명령을 실행하십시오. 스크립트는 AWS에서 예약한 `rdsadmin` 데이터베이스를 자동으로 제외합니다.
      * 데이터베이스 자격 증명과 같은 민감한 정보를 안전하게 관리하려면 [비밀 관리](/docs/opentelemetry/database/capabilities/db-apm/#secret-management) 도구에 저장하십시오.
    </Callout>
  </Step>

  <Step>
    ## NRDOT Collector구성 [#configure-collector]

    모니터링 요구 사항에 따라 설정 옵션을 선택하십시오:

    1. 이름이 `mssql-config.yaml`인 설정 파일을 생성합니다:

       ```bash
       sudo nano /etc/nrdot-collector/mssql-config.yaml
       ```

    2. 이전 단계에서 생성한 `mssql-config.yaml` 파일에 다음 설정을 추가하십시오:

    <Callout variant="tip" title="전체 설정">
      이 설정은 RDS 환경의 필수 데이터베이스 모니터링에 중점을 둡니다. 사용 가능한 모든 메트릭으로 포괄적인 모니터링을 활성화하려면 [설정 참조](/docs/opentelemetry/database/mssql/config-reference/#rds-full)를 확인하십시오.
    </Callout>

    <OtelConfig
      inputOptions={[{"name":"username","label":"데이터베이스 사용자 이름","codeLine":5,"defaultValue":"newrelic","toolTip":"인증을 위한 데이터베이스 사용자 이름을 입력하십시오(일반적으로 newrelic)"},{"name":"password","label":"데이터베이스 비밀번호","codeLine":6,"defaultValue":"YOUR_PASSWORD","toolTip":"newrelic 사용자에 대해 설정한 비밀번호를 입력하십시오."},{"name":"server","label":"RDS 엔드포인트","codeLine":7,"defaultValue":"mydb-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com","toolTip":"RDS SQL Server 엔드포인트를 입력하세요"},{"name":"port","label":"SQL Server 포트","codeLine":8,"defaultValue":"1433","toolTip":"SQL Server 포트 번호를 입력합니다. 기본값은 1433입니다."},{"name":"endpoint","label":"뉴렐릭 OTLP 엔드포인트","codeLine":69,"defaultValue":"https://otlp.nr-data.net","toolTip":"뉴렐릭 OTLP 엔드포인트를 입력하십시오. 자세한 내용은 https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp/를 참조하십시오."},{"name":"licenseKey","label":"뉴렐릭 라이선스 키","codeLine":71,"defaultValue":"YOUR_LICENSE_KEY","toolTip":"뉴렐릭 라이선스 키를 입력하십시오."}]}
      config={`receivers:
      nrsqlserver:
       collection_interval: 15s
       # Connection details - Use your RDS endpoint
       username: <input1>
       password: <input2>
       server: <input3>
       port: <input4>

       # Enable comprehensive database metrics
       metrics:
         sqlserver.database.count:
           enabled: true
         sqlserver.database.io:
           enabled: true
         sqlserver.database.latency:
           enabled: true
         sqlserver.database.operations:
           enabled: true
         sqlserver.database.tempdb.space:
           enabled: true
         sqlserver.database.tempdb.version_store.size:
           enabled: true
         sqlserver.deadlock.rate:
           enabled: true
         sqlserver.os.wait.duration:
           enabled: true
         sqlserver.processes.blocked:
           enabled: true
         sqlserver.memory.grants.pending.count:
           enabled: true
         sqlserver.database.file.size:
           enabled: true
         sqlserver.memory.area:
           enabled: true

       # Enable top query and query sample log collection
       events:
         db.server.query_sample:
           enabled: true
         db.server.top_query:
           enabled: true

       # Top query collection configuration
       top_query_collection:
         lookback_time: 60s
         max_query_sample_count: 1000
         top_query_count: 250
         collection_interval: 60s

       collect_full_query_text: true
       allowed_comment_keys:
         - nr_service_guid

       # Query sample collection configuration
       query_sample_collection:
         max_rows_per_query: 100

      processors:
      memory_limiter:
       check_interval: \${env:NR_MEM_LIMITER_CHECK_INTERVAL:-1s}
       limit_mib:       \${env:NR_MEM_LIMITER_LIMIT_MIB:-200}
       spike_limit_mib: \${env:NR_MEM_LIMITER_SPIKE_MIB:-50}

      batch:

      exporters:
      otlp:
       endpoint: <input5>:4318
       headers:
         api-key: <input6>
       tls:
         insecure: false
       compression: gzip

      service:
      telemetry:
       metrics:
         level: none

      pipelines:
       metrics:
         receivers: [nrsqlserver]
         processors: [memory_limiter, batch]
         exporters: [otlp]

       logs:
         receivers: [nrsqlserver]
         processors: [memory_limiter, batch]
         exporters: [otlp]`}
      fileName="mssql-config.yaml"
      tipMdx={{ body: null }}
      onChange={() => {}}
    />
  </Step>

  <Step>
    ## NRDOT Collector 설정 업데이트 [#update]

    위의 대화형 입력으로 YAML 파일을 구성한 후, NRDOT Collector 설정을 완료하십시오:

    1. 다음 명령을 사용하여 서비스 설정 파일을 편집합니다:

       ```bash
       sudo nano /etc/nrdot-collector/nrdot-collector.conf
       ```

    2. 새 `mssql-config.yaml` 파일을 가리키도록 설정 경로를 업데이트하십시오:

       ```bash
       OTELCOL_CONFIG="/etc/nrdot-collector/mssql-config.yaml"
       ```

    3. (선택 사항) NRDOT Collector 설정을 확인하십시오:

       ```bash
       sudo /usr/bin/nrdot-collector validate --config=/etc/nrdot-collector/mssql-config.yaml
       ```

       <Callout variant="tip">
         애플리케이션 성능을 데이터베이스 작업과 연관시키려면 데이터베이스 서비스 식별을 설정할 수 있습니다. 자세한 내용은 [데이터베이스 서비스 식별 설정 가이드](/docs/opentelemetry/database/capabilities/db-apm)를 참조하십시오.
       </Callout>
  </Step>

  <Step>
    ## NRDOT Collector를 다시 시작하십시오 [#restart]

    설정을 업데이트한 후 NRDOT Collector 서비스를 다시 시작하십시오:

    ```bash
    sudo systemctl restart nrdot-collector
    ```

    <Callout variant="tip">
      새로운 설정이 적용되도록 설정 변경 후에는 항상 NRDOT Collector 서비스를 다시 시작하십시오.
    </Callout>
  </Step>

  <Step>
    ## 데이터 찾기 및 사용 [#find]

    데이터가 수집되면 뉴렐릭 UI를 통해 포괄적인 SQL Server 데이터베이스 모니터링에 액세스할 수 있습니다.

    뉴렐릭에서 SQL Server 데이터베이스를 찾으려면 다음을 수행하세요.

    1. <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; All capabilities &gt; Databases**</DNT> 으)로 이동합니다.

    2. **<DNT>Entity type</DNT>** 드롭다운 메뉴에서 <DNT>**MSSQL instance**</DNT>을(를) 선택한 다음 <DNT>**Apply**</DNT>을(를) 클릭합니다.

    3. 모임 목록에서 SQL Server 데이터베이스를 선택합니다.

       NRDOT로 SQL Server 모니터링을 설정한 후 다음을 수행할 수 있습니다:

       * [사용자 정의 대시보드를 만들어](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) 데이터베이스 지표를 시각화하세요.
       * 중요한 데이터베이스 성능에 대한 [알림 설정](/docs/alerts/create-alert/create-alert-condition/alert-conditions/), 참조 값
       * 뉴렐릭 쿼리 기능을 사용하여 [데이터 탐색](/docs/query-your-data/explore-query-data/browse-data/introduction-data-explorer/)
  </Step>
</Steps>

## 관련 문서 [#related-docs]

<DocTiles>
  <DocTile title="APM-데이터베이스 상관관계 설정" path="/docs/opentelemetry/database/capabilities/db-apm">
    뉴렐릭에서 애플리케이션 성능을 데이터베이스 작업과 연관시키는 방법을 알아보십시오.
  </DocTile>

  <DocTile title="문제점 해결" path="/docs/opentelemetry/database/mssql/troubleshooting">
    뉴렐릭에서 MSSQL Linux RDS 모니터링 설정 문제를 해결하는 방법을 알아보십시오.
  </DocTile>

  <DocTile title="지표 참조" path="/docs/opentelemetry/database/mssql/metrics-reference">
    NRDOT Collector가 수집하는 사용 가능한 메트릭에 대해 알아보십시오.
  </DocTile>
</DocTiles>