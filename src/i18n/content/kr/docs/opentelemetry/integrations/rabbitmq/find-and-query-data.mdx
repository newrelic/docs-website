---
title: 뉴렐릭에서 RabbitMQ 데이터 찾기 및 조회
tags:
  - Integrations
  - Open source telemetry integrations
  - OpenTelemetry
  - RabbitMQ
  - Query data
metaDescription: Learn how to find your RabbitMQ metrics in New Relic UI and write NRQL queries to analyze your data.
freshnessValidatedDate: '2024-12-24T00:00:00.000Z'
translationType: machine
---

RabbitMQ 모니터링이 OpenTelemetry 로 구성되면 지표가 뉴렐릭의 여러 위치에 표시됩니다. 이 가이드는 데이터를 찾는 방법과 효과적으로 쿼리하는 방법을 안내합니다.

## RabbitMQ 데이터로 이동하세요 [#find-data]

RabbitMQ 경로는 뉴렐릭 플랫폼의 여러 곳에 나타나며, 각 위치는 서로 다른 사용 사례에 최적화되어 있습니다.

### 1. 엔티티 탐색기

최적의 용도: 빠른 상태 점검 및 엔티티 관계 분석

1. 이동 <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; All capabilities &gt; On host integrations**</DNT>
2. 메트릭에서 자동으로 생성된 RabbitMQ 엔티티를 확인하세요.

### 2. 사전 구축된 대시보드

최적 사용 용도: 종합적인 모니터링 및 시각화

1. 이동 <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; Dashboards &gt; Recommended dashboards (View all)**</DNT>
2. 검색 <DNT>**&quot;OpenTelemetry RabbitMQ&quot;**</DNT>

### 3. 제3자 서비스

최적 사용 분야: 통합 중심 모니터링

1. 이동 <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; All capabilities &gt; Infrastructure &gt; Third-party services**</DNT>
2. <DNT>**&quot;RabbitMQ&quot;**</DNT> 을 검색하여 모니터링 중인 인스턴스를 빠르게 찾으세요.

## RabbitMQ 데이터 조회 [#query-data]

모든 RabbitMQ 메트릭에는 `instrumentation.provider = 'opentelemetry'` 속성이 포함됩니다. 다음은 몇 가지 유용한 쿼리입니다.

### 사용 가능한 모든 링크를 확인하세요

```sql
SELECT count(*)
FROM Metric
WHERE metricName LIKE 'rabbitmq.%'
  AND instrumentation.provider = 'opentelemetry'
FACET metricName
SINCE 10 minutes ago
```

### 큐 깊이를 모니터링합니다.

```sql
SELECT latest(rabbitmq.queue.message.count)
FROM Metric
WHERE instrumentation.provider = 'opentelemetry'
FACET rabbitmq.queue.name
TIMESERIES
```

### 소비자 지연을 추적하세요

```sql
SELECT latest(rabbitmq.queue.message.unacknowledged)
FROM Metric
WHERE instrumentation.provider = 'opentelemetry'
  AND rabbitmq.queue.name = 'your-queue-name'
TIMESERIES
```

## 사용 가능한 지표 [#metrics]

통합은 다음과 같은 지표 범주를 수집합니다.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        범주
      </th>

      <th>
        측정하는 것
      </th>

      <th>
        예시 지표
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        **큐 메트릭**
      </td>

      <td>
        메시지 흐름 및 큐 상태
      </td>

      <td>
        * `rabbitmq.queue.message.count` - 총 메시지 수
        * `rabbitmq.queue.message.ready` - 메시지 전송 준비 완료
        * `rabbitmq.queue.message.unacknowledged` - 메시지 처리 중
        * `rabbitmq.queue.consumer.count` - 적극적인 소비자
      </td>
    </tr>

    <tr>
      <td>
        **연결 메트릭**
      </td>

      <td>
        클라이언트 연결
      </td>

      <td>
        * `rabbitmq.connection.count` - 활성 연결
        * `rabbitmq.consumer.count` - 모든 대기열의 총 소비자 수
      </td>
    </tr>
  </tbody>
</table>

자세한 내용은 [메트릭 참조 자료를](/docs/opentelemetry/integrations/rabbitmq/metrics-reference) 확인하세요.

## 다음은 뭐지? [#whats-next]

이제 RabbitMQ 데이터를 찾고 쿼리할 수 있으므로 모니터링 설정을 강화하세요.

**더 알아보기:**

* [개요](/docs/opentelemetry/integrations/rabbitmq/overview) - RabbitMQ 통합 및 구현, 배포 옵션에 대해 알아보세요.
* [지표 참조](/docs/opentelemetry/integrations/rabbitmq/metrics-reference) - NRQL 쿼리 예제가 포함된 전체 지표 목록

**모니터링 강화:**

* [알림 구성](/docs/alerts-applied-intelligence/new-relic-alerts/get-started/introduction-alerts) - 큐 깊이, 메모리 사용량 및 연결 수에 대한 알림을 설정합니다.
* [대시보드 구축](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards) - RabbitMQ 지표를 시각화하기 위한 맞춤형 대시보드 생성