---
title: 에이전트 제어 설정 및 설치
metaDescription: 'Learn how to install and configure Agent Control on a Kubernetes cluster, Linux host, or Windows host.'
freshnessValidatedDate: never
translationType: machine
---

<Callout variant="important">
  Agent Control 및 뉴렐릭 Control은 **일반적으로** Kubernetes 에서 사용할 수 있습니다. Linux 및 Windows 호스트 지원은 **공개 미리 보기** 프로그램에 포함되어 있습니다, [사전 출시 정책](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy)에 따라.
</Callout>

## 개요

에이전트 제어는 측정, 에이전트 에이전트의 관리를 단순화합니다. 이 가이드는 다양한 방법을 사용하여 쿠버네티스 클러스터, Linux 호스트 또는 Windows 호스트에 Control을 설치 및 제거하는 과정을 안내합니다.

## 에이전트 제어 설치

<Callout title="팁">
  대규모 안드로이드에서 에이전트 제어 설정을 자동화하려면 [Terraform을 사용하여 에이전트 제어 설정을](/docs/infrastructure-as-code/terraform/agent-control) 참조하세요.
</Callout>

### 안내 설치

1. 뉴렐릭에 로그인하세요.
2. 올바른 계정을 선택했는지 확인하세요.

<Callout title="중요한">
  Control을 설치하려면 차량N을 보유해야 합니다. 이 관리 대상에 대한 플릿을 아직 생성하지 않은 경우 설치 안내 단계에서 플릿을 생성하거나 **뉴럴릭 컨트롤** 에서 플릿 생성 프로세스를 완료한 다음 이 안내 설치 단계로 돌아올 수 있습니다.
</Callout>

3. 통합 및 에이전트에서 **Install Agent Control** 클릭하거나 **Agent Control**을 검색하세요.

   <img title="Agent Control guided install" alt="Screenshot of the guided install tasks for Agent Control" src="/images/agent-control-guided-install.webp" />

4. 설치 및 설정 과정을 완료하려면 다음 단계를 따르십시오.

<Callout title="팁">
  Control을 설치하려면 차량N을 보유해야 합니다. 이 관리 대상에 대한 플릿을 아직 생성하지 않은 경우 안내 설치에서 설치 중에 플릿을 생성하거나 **플릿 제어** 에서 플릿 생성 프로세스를 완료한 다음 이 안내 설치 단계로 돌아올 수 있습니다.
</Callout>

5. 생성된 설정 파일을 컴퓨터로 다운로드하고 터미널에서 제공된 명령어를 실행하여 Control을 설치하십시오. 설치가 완료되면 **Continue** \[계속]을 클릭하세요.
6. 설치가 성공적으로 완료되었는지 확인하기 위해 연결을 테스트하세요. 이 단계를 완료하는 데 5-10분이 걸릴 수 있습니다.

<Callout variant="important" title="Windows 설치 참고 사항">
  **설치 방법:** Windows용 에이전트 제어는 PowerShell 스크립트를 사용하여 설치됩니다. MSI 또는 MSIX 인스톨러는 현재 이용할 수 없습니다. 제공된 PowerShell 설치 스크립트를 관리자 권한으로 실행해야 합니다.

  **플릿 유형 요구 사항:** Windows 호스트의 경우 안내 설치 중에 **&quot;Host - Windows&quot;** 유형의 플릿을 선택하거나 생성해야 합니다. 다른 유형(예: &quot;호스트 - Linux&quot;)의 플릿을 사용하면 설치 또는 작동 문제가 발생할 수 있습니다.

  **에이전트 버전 요구 사항:** 설치 중에 호환되는 에이전트 버전을 선택했는지 확인하세요.

  * 인프라 에이전트: 버전 1.71.0 이상
  * NRDOT(OpenTelemetry Collector): 버전 1.5.0 이상

  Linux 환경의 템플릿 및 설정은 Windows 호환성을 위해 조정이 필요할 수 있습니다.

  **기존 에이전트:** Windows 호스트에 뉴렐릭 인프라 에이전트가 이미 설치되어 있는 경우 Agent Control을 설치하기 전에 이를 제거해야 합니다. Agent Control은 Windows 에서 기존 에이전트의 자동 마이그레이션을 지원하지 않습니다. 에이전트 컨트롤을 설치한 후 플릿셀을 통해 에이전트를 관리할 수 있습니다. (현재 Windows의 에이전트 제어에서 관리되지 않는) APM 에이전트는 설치된 상태를 유지하며 독립적으로 계속 작동합니다.
</Callout>

### 인증 관련 참고 사항

뉴럴릭 제어는 시스템 ID를 사용해야 하는데, 이는 서비스와 애플리케이션 간의 인증 및 신뢰 구축에 사용되는 비인간 ID입니다.

Control 안내 설치 프로세스 중에 Helm 차트 값에 포함된 클라이언트 자격 증명을 사용하여 첫 번째 시스템 ID가 생성됩니다. **이 시스템 ID에 대한 자격 증명은 12시간 후에 만료됩니다.** 만료되면 에이전트 Control Helm 차트 구현, 배포가 플릿 취소 서비스를 인증하지 못해 다음 오류가 발생합니다.

```
Error getting system identity auth token. The API endpoint returned 400: Expired client secret.
```

이 경우 Helm 차트를 새로운 시스템 ID 자격 증명으로 업데이트해야 합니다.

```yaml
global:
  cluster: "cluster-name"
  licenseKey: "*************************"
agentControlDeployment:
  chartValues:
    systemIdentity:
      organizationId: "00000000-0000-0000-0000-000000000000"
      parentIdentity:
        clientId: "CLIENT_ID"
        clientSecret: "CLIENT_SECRET"
    config:
      fleet_control:
        fleet_id: "SAMPLE_FLEET_ID"
      agents:
        ...
```

## 설치 확인

이제 에이전트 Control이 설치 및 실행되었으므로 [에이전트 또는 구현하다, 배포하다 변경 사항을 에이전트에 대해 플릿 컨트롤\](/docs/뉴렐릭-control/fleet-control/overview)을 사용하여 구성하고 관리](/docs/new-relic-control/agent-control/configuration) 할 준비가 되었습니다.

### Kubernetes

1. 다음 명령을 실행하여 파드 상태를 확인하세요.

Agent Control은 보안상의 이유로 서브에이전트를 다른 네임스페이스에 설치합니다. 모든 것이 작동하는지 확인하려면 에이전트 제어 파드가 `newrelic-agent-control` 라벨스페이스에서 실행되고 하위 에이전트 파드가 `newrelic` 과 같은 다른 라벨스페이스에서 실행되고 있는지 확인하세요.

```shell
kubectl get pods -n newrelic-agent-control    # Check Agent Control pods
kubectl get pods -n newrelic                  # Check subagent pods
```

2. 뉴렐릭에 로그인하고 **Fleet Control** \[플릿위험] 으로 이동하세요.
3. 함대 페이지로 가서 설치 중에 선택한 함대를 선택하세요.
4. 쿠버네 **Entities** \[티스] 클러스터 목록에서 귀하의 쿠버네티스 클러스터가 나타나는지 확인하세요.
5. 클러스터에 대한 측정, 로그 상태가 **healthy** 인지 확인하세요.

#### 기존 Flux 설치에 대한 지원

기본적으로 에이전트 Control Helm 차트는 포함된 [Flux CD를](https://fluxcd.io/) 활용하여 Kubernetes 에서 에이전트의 수명 주기를 관리합니다. 하지만 조직에서 이미 GitOps에 **Flux v2**를 사용하고 있다면 기존 설치를 활용하도록 Control을 구성할 수 있습니다.

이 접근 방식은 에이전트 제어를 임베디드 연속 배포 엔진에서 분리하여 에이전트 제어의 관리 기능을 활용하는 동시에 클러스터 작업을 위한 단일 Flux 연결을 유지할 수 있도록 합니다.

##### 요구 사항 및 호환성

외부 Flux 설치를 컨트롤과 함께 사용하려면 환경이 다음 요구 사항을 충족해야 합니다. 이러한 사양에서 벗어나는 설정은 유효성 검사를 통과하지 못합니다.

* **Flux 버전:** Flux v2 이상.

* **필수 구성 요소:** Flux 설치에는 다음이 포함되어야 합니다.

  * **Helm 컨트롤러:** HelmRelease CRD(helm.toolkit.fluxcd.io/v2)를 사용합니다.
  * **소스 컨트롤러:** HelmRepository CRD(source.toolkit.fluxcd.io/v1)를 사용합니다.

* **접착스페이스 범위:** Flux 외부는 에이전트 Control이 설치될 접착스페이스를 감시하도록 구성되어야 합니다(또는 모든 접착스페이스를 감시하도록 구성해야 합니다).

##### 구성

이 모드를 활성화하려면 Control Helm 차트 설정에서 번들로 제공되는 Flux 구성 요소를 명시적으로 비활성화해야 합니다.

`values.yaml` 파일에서 `agentControlCd.enabled` `false` 로 설정하세요.

```
global:
  cluster: "<YOUR_CLUSTER_NAME>"
  licenseKey: "<YOUR_LICENSE_KEY>"

# Disable the embedded Flux instance
agentControlCd:
  enabled: false

agentControlDeployment:
  chartValues:
    # ... other configurations ...
```

##### 외부 Flux에 대한 권한

자체 Flux 설치를 사용하는 경우 클러스터의 Flux 서비스 계정은 에이전트 Control에서 생성된 설정을 적용하는 역할을 합니다. 따라서 기존 Flux를 구현하려면 리소스를 구현하기 위한 특정 권한이 필요합니다. 아래 방법 중 하나를 강력히 추천합니다.

* **Cluster 관리자(권장):** 가장 간단한 설정은 Flux 인스턴스가 `cluster-admin` 권한으로 실행되도록 하는 것입니다. 이는 Flux 커뮤니티 차트의 표준 설정이며 뉴렐릭 에이전트에 필요한 모든 필수 리소스(구현, 배포, DaemonSets, 서비스 등)를 관리할 수 있도록 보장합니다.
* **최소 권한 설정:** 보안 정책에서 `cluster-admin` 사용을 제한하는 경우, Flux 서비스 계정에 소스 컨트롤러, Helm 컨트롤러, 컨트롤 및 설치하려는 모든 특정에 필요한 권한이 있는지 확인하기 위해 특정 `ClusterRole` 생성해야 합니다.

<Callout variant="important">
  참고: 새로운 기능이나 에이전트가 추가되면 에이전트 권한이 변경될 수 있습니다. 사용자 지정 역할에서 이러한 권한을 유지 관리할 책임은 사용자에게 있습니다.
</Callout>

아래는 `ClusterRole` 예시로, 컨트롤 및 Flux 구성 요소가 상호 운용되는 데 필요한 최소 권한을 보여줍니다.

```
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: external-flux-agent-control-role
rules:
  # Permissions required by Flux to operate Agent Control components
  - apiGroups: ["apiextensions.k8s.io"]
    resources: ["customresourcedefinitions"]
    verbs: ["get"]
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "create", "update"]
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["clusterroles", "rolebindings"]
    verbs: ["get", "create", "delete"]
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["watch"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["create"]
  - apiGroups: [""]
    resources: ["serviceaccounts"]
    verbs: ["get", "create", "delete"]
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get", "create"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["create"]
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["get", "create"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list", "watch", "create", "delete"]
  
  # Permissions required by Agent Control logic
  - apiGroups: ["helm.toolkit.fluxcd.io", "newrelic.com", "source.toolkit.fluxcd.io"]
    resources: ["*"]
    verbs: ["*"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["*"]
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "create", "patch", "update", "delete", "deletecollection"]
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get"]
  - apiGroups: ["apps"]
    resources: ["daemonsets", "deployments", "statefulsets"]
    verbs: ["get", "list", "watch"]
```

##### 경계를 지지합니다

외부 Flux 설치를 사용할 때 뉴렐릭 지원은 에이전트 제어 소프트웨어(유효한 설정 매니페스트 생성)로 제한됩니다. 이 모드에서는 다음과 같은 공동 책임 체계를 고려해야 합니다.

* **뉴렐릭 책임:** 에이전트 제어가 뉴렐릭 백앤드와 올바르게 상호작용하고 유효한 `HelmRelease` 및 `HelmRepository` 정의를 생성하는지 확인합니다.
* **고객의 책임:** 귀하는 자신의 Flux 설치의 상태, 버전 유지 관리, 네트워킹, 문제 해결, 문제 해결에 대한 책임이 있습니다. 외부 Flux 컨트롤러의 설정 또는 오류로 인해 발생하는 문제는 Control 지원 범위에서 제외됩니다.

### Linux

* `newrelic-agent-control` 서비스의 상태를 확인하세요:

  ```bash
    sudo systemctl status newrelic-agent-control
  ```

  서비스가 `Failed` 또는 `Stopped` 상태로 표시되면 에이전트가 설치되었지만 정상적인 작동을 방해하는 문제가 있음을 의미합니다. `journalctl` (또는 이와 유사한 Linux 도구)를 사용하여 에이전트 서비스 로그를 확인하십시오.

  ```bash
    journalctl -u newrelic-agent-control
  ```

  인사이트를 사용할 수 없는 경우 [에이전트를 디버그 모드로 실행하여](/docs/new-relic-control/agent-control/troubleshooting/#debug) 자세한 로그에 액세스하여 서비스를 시작할 수 없는 이유에 대해 자세히 알아보세요.

* 서비스가 설치되지 않은 경우 안내 설치의 CLI 설치 명령 끝에 `--debug` 추가하고 다시 실행해 보세요. 이 기능을 사용하면 설치 스크립트에 대한 자세한 로깅이 활성화되어 오류를 설명하는 추가적인 컨텍스트를 제공할 수 있습니다.

* 선택적으로 문제 해결, 설치 해결을 돕기 위해 로그를 뉴렐릭으로 보내라는 메시지가 표시되면 `yes` 에 응답하세요. 제출이 완료되면 다음 NRQL 쿼리를 사용하여 로그에 액세스할 수 있습니다.

  ```sql
    SELECT * FROM Log WHERE hostname = `your-host-name`
  ```

### 윈도우

#### 설치 전 체크리스트

Windows 에 Control을 설치하기 전에 다음 필수 조건을 확인하십시오.

* [ ] **관리자 권한** - PowerShell을 마우스 오른쪽 버튼으로 클릭하고 Run as Administrator \[관리자 권한으로 실행]을 선택합니다.
* [ ] **PowerShell 버전** - PowerShell 5.0 이상인지 확인하십시오.
  ```powershell
  $PSVersionTable.PSVersion
  ```
* [ ] **포트 가용성** - 51200번 포트가 사용 중이 아닌지 확인하십시오.
  ```powershell
  netstat -ano | findstr :51200
  ```
  명령 실행 후 아무런 출력이 없으면 해당 포트를 사용할 수 있는 것입니다.
* [ ] **인프라 에이전트가 제거되었습니다**. 이미 설치된 경우 인프라 에이전트를 제거하십시오.
  ```powershell
  Get-Service -Name newrelic-infra -ErrorAction SilentlyContinue
  ```
  만약 이 과정에서 서비스가 반환된다면, 먼저 인프라 에이전트를 제거하십시오. APM 에이전트는 설치된 상태로 유지할 수 있습니다.
* [ ] **플릿 생성됨** - **&quot;Host - Windows&quot;** 유형을 선택하여 플릿을 생성합니다.
* [ ] **네트워크 연결** - 뉴렐릭 서비스 접속 테스트:
  ```powershell
  Test-NetConnection -ComputerName download.newrelic.com -Port 443
  ```
* [ ] **바이러스 백신 설정** - 제외 항목 추가 (아래 보안 소프트웨어 참고 사항 참조)

<Callout variant="caution" title="바이러스 백신 및 보안 소프트웨어">
  Windows Defender 또는 타사 바이러스 백신 소프트웨어가 Control이 서비스로 실행되는 것을 차단할 수 있습니다. 설치하기 전에 다음 디렉터리를 바이러스 백신 프로그램의 예외 목록에 추가하십시오.

  * `C:\Program Files\New Relic\newrelic-agent-control\`
  * `C:\ProgramData\New Relic\newrelic-agent-control\`

  설치 후Control이 시작되지 않고 명령줄에서 성공적으로 실행되는 경우, 이는 안티바이러스 프로그램의 간섭을 나타냅니다. 보안팀과 협력하여 뉴렐릭 실행 파일에 대한 적절한 예외를 구성하십시오.
</Callout>

#### 설치 확인

1. `newrelic-agent-control` 서비스의 상태를 확인하세요:

   관리자 권한으로 PowerShell을 열고 다음 명령을 실행하세요.

   ```powershell
   Get-Service -Name newrelic-agent-control
   ```

   서비스는 상태 `Running` 을 표시해야 합니다. 서비스가 `Stopped` 상태로 표시되거나 존재하지 않는 경우 아래의 문제 진단 및 해결을 진행하십시오.

2. 에이전트 제어 상태 엔드포인트를 확인합니다.

   ```powershell
   Invoke-WebRequest -Uri "http://localhost:51200/status" -UseBasicParsing
   ```

   정상적인 에이전트 제어는 `"healthy": true` 이 포함된 JSON 응답을 반환해야 합니다.

3. 뉴렐릭에 로그인하고 **Fleet Control** \[플릿위험] 으로 이동하세요.

4. 함대 페이지로 가서 설치 중에 선택한 함대를 선택하세요.

5. **Entities** \[엔티티] 테이블에서 Windows 호스트가 목록에 나타나는지 확인하십시오.

6. 호스트의 측정 및 계측 상태가 **정상** 인지 확인하십시오.

#### 설치 후 예상되는 사항

설치 파일을 실행한 후 에이전트 제어는 감독자 서비스만 설정합니다. 측정 없음, 도구 배포는 자동으로 구현됩니다.

**즉시 (0-2분):**

* **0-30초:** 에이전트 제어 서비스가 등록 및 시작됩니다.
* **30-60초:** 플릿위험에 대한 첫 번째 연결이 설정되었습니다.
* **1-2분:** 설정 동기화가 완료되고 호스트가 플릿 위험에 나타납니다.

**필요한 다음 단계:** 에이전트 컨트롤이 설치되고 플릿 컨트롤에 연결된 후, 배포하다 에이전트를 수동으로 구현하여 텔레트리 수집을 시작해야 합니다.

1. 뉴렐릭에 로그인하고 **플릿위험**으로 이동하세요.
2. 사용 중인 시스템을 선택하고 **Entities** \[엔티티] 테이블에서 Windows 호스트를 찾으세요.
3. 플릿시티를 사용하여 호스트에 구현하다, 배포하다 에이전트(인프라 에이전트, NRDOT 등)
4. 구현하다, 배포하다 에이전트가 시작되고 델메트리를 뉴렐릭으로 보낼 때까지 5-10분 정도 기다립니다.

<Callout variant="important">
  **기본적으로 텔레메트리 없음:** Agent Control은 에이전트를 관리하는 감독자 서비스입니다. 텔레메트리 데이터 자체를 수집하거나 전송하지 않습니다. 뉴렐릭에서 클러스터 리소스, 클러스터 또는 기타 스프레드시트를 보려면 설치 후 배포 에이전트를 통해 배포하고 에이전트(예: 배포 에이전트)를 구성해야 합니다.
</Callout>

에이전트 제어 서비스가 2-3분 이내에 플릿독성에 연결되지 않으면 [Windows 호스트 문제 해결, 해결을](/docs/new-relic-control/agent-control/troubleshooting/#windows-hosts-troubleshooting) 참조하세요.

#### 서비스 관리 명령

다음 PowerShell 명령을 사용하여 에이전트 제어 Windows 서비스를 관리합니다(관리자 권한으로 실행):

**서비스를 시작하세요:**

```powershell
Start-Service -Name newrelic-agent-control
```

**서비스를 중지하세요:**

```powershell
Stop-Service -Name newrelic-agent-control
```

**서비스를 재시작하세요:**

```powershell
Restart-Service -Name newrelic-agent-control
```

**서비스 상태를 확인하세요:**

```powershell
Get-Service -Name newrelic-agent-control | Format-List Status, StartType
```

정상 상태일 때 예상되는 출력:

```
Status    : Running
StartType : Automatic
```

**서비스 설정 보기:**

```powershell
Get-WmiObject -Class Win32_Service -Filter "Name='newrelic-agent-control'" | Format-List Name, State, StartMode, StartName
```

#### 디렉토리 구조

Key Control은 Windows 에 다음과 같은 디렉터리 구조를 생성합니다.

**프로그램 파일(실행 파일):**

```
C:\Program Files\New Relic\newrelic-agent-control\
├── newrelic-agent-control.exe       # Main service executable
├── uninstall.ps1                     # Uninstallation script
└── local-data\
    └── agent-control\
        └── local_config.yaml         # Local configuration file
```

**프로그램 데이터(실행 데이터):**

```
C:\ProgramData\New Relic\newrelic-agent-control\
├── fleet-data\
│   └── agent-control\
│       └── remote_config.yaml        # Configuration synced from Fleet Control
├── logs\
│   └── newrelic-agent-control.log    # Service logs
├── filesystem\                       # Managed agent configurations and data
└── packages\                         # Downloaded agent packages (cached)
```

<Callout variant="tip" title="로그 위치">
  **에이전트 제어 로그는 다음 위치에서 찾을 수 있습니다.** `C:\ProgramData\New Relic\newrelic-agent-control\logs\`

  로그 파일에는 날짜 접미사(예: `newrelic-agent-control.log.2026-02-09-11`)가 포함됩니다.

  최신 파일부터 최근 로그를 확인하세요:

  ```powershell
  Get-ChildItem "C:\ProgramData\New Relic\newrelic-agent-control\logs\" -Filter "newrelic-agent-control.log.*" | Sort-Object LastWriteTime -Descending | Select-Object -First 1 | Get-Content -Tail 100
  ```
</Callout>

#### Windows 전용 기능

**로그 수집:** Windows 호스트는 에이전트 에이전트의 통합 Fluent Bit 통해 쿼리를 수집할 수 있습니다. 에이전트 에이전트 패키지에는 Windows 용 Fluent Bit 바이너리가 포함되어 있어 에이전트 설정에서 `config_logging` 에이전트를 사용하여 로그 포워딩을 구성할 수 있습니다. 로그 설정은 Linux 호스트와 유사하게 작동하지만 Windows 전용 경로 및 형식을 사용합니다.

**설정 참고 사항:**

* Windows 설정에서 파일 경로는 백슬래시(`\\`)와 Windows스타일 경로(예: `C:\\ProgramData\\...`)를 사용합니다.
* 환경 변수는 Windows 규칙에 따라 설정됩니다.
* 에이전트 바이너리는 `.exe` 확장자를 사용합니다.
* 설정 파일은 위의 디렉터리 구조에 표시된 위치에 저장됩니다.

#### 구성 매개변수

주요 설정, Windows 용 에이전트 제어:

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        매개변수
      </th>

      <th>
        설명
      </th>

      <th>
        기본 위치
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `license_key`
      </td>

      <td>
        귀하의 뉴렐릭 클러스터 키 (필수)
      </td>

      <td>
        에 설정 

        `local_config.yaml`
      </td>
    </tr>

    <tr>
      <td>
        `fleet_id`
      </td>

      <td>
        플릿 취소 함대 배제(필수)
      </td>

      <td>
        에 설정 

        `local_config.yaml`
      </td>
    </tr>

    <tr>
      <td>
        설치 디렉터리
      </td>

      <td>
        에이전트 제어 실행 파일 위치
      </td>

      <td>
        `C:\Program Files\New Relic\newrelic-agent-control\`
      </td>
    </tr>

    <tr>
      <td>
        설정 디렉토리
      </td>

      <td>
        로컬 및 원격 설정 파일
      </td>

      <td>
        `C:\Program Files\New Relic\newrelic-agent-control\local-data\`
      </td>
    </tr>

    <tr>
      <td>
        데이터 디렉토리
      </td>

      <td>
        런타임 데이터, 관리형 에이전트 파일, 로그
      </td>

      <td>
        `C:\ProgramData\New Relic\newrelic-agent-control\`
      </td>
    </tr>

    <tr>
      <td>
        로그 디렉터리
      </td>

      <td>
        서비스 로그 파일
      </td>

      <td>
        `C:\ProgramData\New Relic\newrelic-agent-control\logs\`
      </td>
    </tr>

    <tr>
      <td>
        상태 엔드포인트 포트
      </td>

      <td>
        상태 확인을 위한 HTTP 엔드포인트
      </td>

      <td>
        `51200`

         (설정 불가)
      </td>
    </tr>

    <tr>
      <td>
        서비스 이름
      </td>

      <td>
        Windows 서비스 식별자
      </td>

      <td>
        `newrelic-agent-control`
      </td>
    </tr>

    <tr>
      <td>
        서비스 계정
      </td>

      <td>
        서비스를 실행하는 Windows 계정
      </td>

      <td>
        `LocalSystem`

         (에이전트 관리에 필요)
      </td>
    </tr>
  </tbody>
</table>

**현재 설정 보기:**

```powershell
Get-Content "C:\Program Files\New Relic\newrelic-agent-control\local-data\agent-control\local_config.yaml"
```

**서비스 설정 확인:**

```powershell
Get-WmiObject -Class Win32_Service -Filter "Name='newrelic-agent-control'" | Format-List PathName, StartName, State
```

<Callout variant="important" title="보안 참고 사항">
  에이전트 제어는 에이전트 수명 주기 작업을 관리하기 위해 Windows 에서 `LocalSystem` 권한으로 실행됩니다. 설정 파일에 적절한 권한이 있는지 확인하십시오.

  * 프로그램 파일 디렉터리: 일반 사용자는 읽기/실행 권한, 관리자만 쓰기 권한 보유
  * ProgramData 디렉터리: 관리자 및 SYSTEM 계정에만 쓰기 권한이 부여됩니다.

  신뢰할 수 있는 출처의 설정 및 에이전트 소스만 사용하세요. Agent Control은 상승된 권한으로 관리 에이전트를 실행할 수 있습니다.
</Callout>

{/* ## Manage Agent Control

### Linux hosts

See the following sections on how to:

* [Start, stop, and restart Agent Control](#start-stop-restart)
* [Upgrade Agent Control](#upgrade)
* [Uninstall Agent Control](#uninstall)

### Start, stop, and restart Agent Control [#start-stop-restart]

Our agent starts automatically after you run the installation script.

For Linux, ensure you use the correct command for your init system. Select `start`, `stop`, `restart`, or `status` as appropriate.

Here's an example for Systemd:

```shell
sudo systemctl <start|stop|restart|status> newrelic-agent-control
``` */}

## 에이전트 제어 제거 [#uninstall]

### Kubernetes

쿠버네티스 클러스터에서 Agent Control을 제거하려면 다음 명령을 실행하세요:

<Steps>
  <Step>
    #### 설치된 릴리스 보기 [#list-releases]

    다음 명령을 실행하여 설치된 모든 릴리스를 나열하고 Agent Control용 릴리스를 식별하세요.

    ```shell
    helm list --all-namespaces
    ```
  </Step>

  <Step>
    #### 에이전트 제어 제거 [#uninstall-agent-control]

    * `<RELEASE>` 및 `<NAMESPACE>` 설치 및 환경에 적합한 값으로 바꾸세요.

      ```shell
      helm uninstall <RELEASE> -n <NAMESPACE>
      ```

    * 예를 들어:

      ```shell
      helm uninstall agent-control-bootstrap -n newrelic
      ```
  </Step>
</Steps>

### 리눅스 호스트

<Callout variant="important">
  일반적으로 제거 과정은 설정 파일 및 기타 여러 파일을 남겨둡니다. 사전에 서비스를 중단할 필요는 없습니다. 제거 과정은 몇 분 정도 소요될 수 있습니다. 제거 과정에서 삭제되지 않을 수 있는 자산의 예:

  * 로컬 또는 원격 설정 파일: `/etc/newrelic-agent-control` 및 `/var/lib/newrelic-agent-control` 폴더를 검토하고 삭제하십시오.
  * 뉴렐릭 CLI: `/usr/bin/newrelic-cli` 바이너리를 검토하고 제거합니다.
</Callout>

<Tabs>
  <TabsBar>
    <TabsBarItem id="uninstall-debian">데비안, 우분투,</TabsBarItem> <TabsBarItem id="uninstall-suse">SLES,</TabsBarItem> <TabsBarItem id="uninstall-redhat">아마존 리눅스, 센토오스, 데비안</TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="uninstall-debian">
      루트로 다음 명령을 실행합니다.

      ```bash
      sudo apt-get remove newrelic-agent-control
      ```
    </TabsPageItem>

    <TabsPageItem id="uninstall-suse">
      루트로 다음 명령을 실행합니다.

      ```bash
      sudo zypper -n remove newrelic-infra
      ```
    </TabsPageItem>

    <TabsPageItem id="uninstall-redhat">
      루트로 다음 명령을 실행합니다.

      ```bash
      sudo yum remove newrelic-agent-control
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>

### Windows 호스트

<Callout variant="important">
  제거 프로세스는 에이전트 제어 서비스와 실행 파일을 제거합니다. 설정 파일 및 기타 파일은 남아 있을 수 있습니다. 제거 과정에서 삭제되지 않을 수 있는 자산의 예:

  * 설정 파일: 필요한 경우 `C:\Program Files\New Relic\newrelic-agent-control` 및 `C:\ProgramData\New Relic\newrelic-agent-control` 폴더를 검토하고 삭제하십시오.
</Callout>

Windows 호스트에서 Agent Control을 제거하려면:

1. 관리자 권한으로 PowerShell을 엽니다.

2. 에이전트 Control 설치 디렉터리로 이동합니다.

   ```powershell
   cd "C:\Program Files\New Relic\newrelic-agent-control"
   ```

3. 제거 스크립트를 실행하세요:

   ```powershell
   .\uninstall.ps1
   ```

   이 스크립트는 다음과 같은 작업을 수행합니다.

   * `newrelic-agent-control` 서비스를 중지하세요
   * 서비스 등록을 삭제하세요
   * 에이전트 제어 실행 파일 삭제

4. (선택 사항) 모든 사용자 제어 데이터를 완전히 제거하려면 설정 디렉터리를 수동으로 제거하십시오.

   ```powershell
   Remove-Item -Path "C:\Program Files\New Relic\newrelic-agent-control" -Recurse -Force
   Remove-Item -Path "C:\ProgramData\New Relic\newrelic-agent-control" -Recurse -Force
   ```