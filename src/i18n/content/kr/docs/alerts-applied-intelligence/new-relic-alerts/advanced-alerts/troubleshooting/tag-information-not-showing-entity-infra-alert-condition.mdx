---
title: Infra 경보 상태에서 엔터티에 대한 태그 정보가 표시되지 않음
type: troubleshooting
tags:
  - Alerts and applied intelligence
  - Alerts
  - Troubleshooting
metaDescription: Tags from events not indexed by New Relic do not appear.
translationType: machine
---

## 문제

[사용자 정의 위반 설명 태그](/docs/alerts-applied-intelligence/new-relic-alerts/alert-conditions/alert-custom-violation-descriptions#example) 는 이벤트의 값으로 대체되지 않습니다.

## 해결책

태그는 New Relic에 의해 인덱싱된 이벤트에만 사용할 수 있습니다.

이벤트가 인덱싱되었는지 여부를 확인하는 가장 쉬운 방법은 `SELECT nr.entityType FROM ${EventSampleName}` 쿼리를 실행하여 `${EventSampleName}` 을 해당 이벤트의 이름으로 바꾸는 것입니다.

결과의 열 `nr.entity Type` 에 값이 있으면 이벤트가 인덱싱됩니다. 그렇지 않으면 그렇지 않습니다.

## 원인

New Relic에서 인덱싱되지 않은 샘플에는 설명을 추가할 수 없습니다.