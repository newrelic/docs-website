---
title: 맞춤 속성 만들기
type: apiDoc
shortDescription: 여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 생성합니다.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to create a session-level attribute shared by multiple mobile event types.
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      콘덴서
    </TabsBarItem>

    <TabsBarItem id="cordova">
      코르도바
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET 마우이
    </TabsBarItem>

    <TabsBarItem id="flutter">
      실룩 거리다
    </TabsBarItem>

    <TabsBarItem id="react">
      네이티브 반응
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      자마린
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 통사론 [#syntax]

      ### 자바 [#java]

      ```java
      NewRelic.setAttribute(string $name, string or float $value)
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.setAttribute(name: String?, value: String?)
      NewRelic.setAttribute(name: String?, value: Double)
      NewRelic.setAttribute(name: String?, value: Boolean)
      ```

      ## 설명 [#description]

      이 정적 메서드는 지정된 이름과 값을 사용하여 세션 수준 사용자 지정 [특성](/docs/insights/explore-data/custom-events/insert-custom-attributes-events-new-relic-mobile#attribute-definition) 을 만듭니다. 이 메서드는 호출될 때마다 이전 값과 형식을 덮어씁니다. 생성된 속성은 여러 [모바일 이벤트 유형](/docs/insights/insights-data-sources/default-data/mobile-events) 에서 공유됩니다.

      다음을 제외한 모든 [MobileSession 기본 속성](/docs/insights/insights-data-sources/default-attributes/mobile-default-attributes-insights) 을 재정의할 수 있습니다.

      * `appId`
      * `appName`
      * `accountId`
      * `carrier`
      * `category`
      * `deviceManufacturer`
      * `deviceModel`
      * `eventType`
      * `install`
      * `lastInteraction`
      * `memUsageMb`
      * `newRelicVersion`
      * `osMajorVersion`
      * `osName`
      * `osVersion`
      * `platform`
      * `platformVersion`
      * `sessionDuration`
      * `sessionId`
      * `timestamp`
      * `type`
      * `upgradeFrom`
      * `uuid`
      * 접두어가 붙은 모든 것 `NewRelic`

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `$value`
            </td>

            <td>
              `string` 또는 `float`
            </td>

            <td>
              필수의. 전달된 두 번째 매개변수는 `string` 값 또는 `float` 값일 수 있습니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 반환 값 [#return-values]

      성공적으로 기록된 경우 `true` 을 반환하고 기록되지 않은 경우 `false` 을 반환합니다.

      ## 예시 [#example]

      다음은 지정된 텍스트 이름과 텍스트 값으로 속성을 만드는 예입니다. `setAttribute` 호출될 때마다 이전 값과 유형을 덮어씁니다.

      ### 자바 [#java]

      ```java
      boolean attributeSet = NewRelic.setAttribute("storeId", "SampleStoreId000");
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
          val attributeSet = NewRelic.setAttribute("storeId", "SampleStoreId000")
      ```

      다음은 지정된 텍스트 이름과 숫자 값을 사용하여 속성을 만드는 예입니다.

      ### 자바 [#java]

      ```java
      boolean attributeSet = NewRelic.setAttribute("rate", 9999.99);
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
          val attributeSet = NewRelic.setAttribute("rate", 9999.99)
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## 통사론 [#syntax]

      ### 목표-c [#objc]

      ```objectivec
      setAttribute:(NSString*)name value:(id) value;
      ```

      ### 빠른 [#swift]

      ```swift
      NewRelic.setAttribute(string $name, string or float $value)
      ```

      ## 설명 [#description]

      이 정적 메서드는 지정된 이름과 값을 사용하여 세션 수준 사용자 지정 [특성](/docs/insights/explore-data/custom-events/insert-custom-attributes-events-new-relic-mobile#attribute-definition) 을 만듭니다. 호출되면 이전 값과 유형을 덮어씁니다. 생성된 속성은 여러 [모바일 모니터링 이벤트 유형](/docs/insights/insights-data-sources/default-data/mobile-events) 에서 공유됩니다.

      다음을 제외한 모든 [MobileSession 기본 속성](/docs/insights/insights-data-sources/default-attributes/mobile-default-attributes-insights) 을 재정의할 수 있습니다.

      * `appId`
      * `appName`
      * `accountId`
      * `carrier`
      * `category`
      * `deviceManufacturer`
      * `deviceModel`
      * `eventType`
      * `install`
      * `lastInteraction`
      * `memUsageMb`
      * `newRelicVersion`
      * `osMajorVersion`
      * `osName`
      * `osVersion`
      * `platform`
      * `platformVersion`
      * `sessionDuration`
      * `sessionId`
      * `timestamp`
      * `type`
      * `upgradeFrom`
      * `uuid`
      * 접두어가 붙은 모든 것 `NewRelic`

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 세션 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `$value`
            </td>

            <td>
              `string` 또는 `float`
            </td>

            <td>
              필수의. 전달된 두 번째 매개변수는 `string` 값 또는 `float` 값일 수 있습니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 반환 값 [#return-values]

      성공적으로 기록된 경우 `true` 을 반환하고 기록되지 않은 경우 `false` 을 반환합니다.

      ## 예 [#examples]

      ### 오브젝티브-C [#obj-c]

      ```objectivec
      BOOL attributeSet = [NewRelic setAttribute:@"buildConfiguration" value:@"AppStore-release"];
      BOOL attributeSet = [NewRelic setAttribute:@"rate" value:@9999.99];
      ```

      ### 빠른 [#swift]

      ```swift
      var attributeSet = NewRelic.setAttribute("buildConfiguration", value: "AppStore-release")
      attributeSet = NewRelic.setAttribute("rate", value: 9999.99)
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## 통사론 [#syntax]

      ```typescript
      setAttribute(options: { name: string; value: string; }) => void
      ```

      ## 설명 [#description]

      여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 만듭니다. 이전 값을 덮어쓰고 호출될 때마다 입력합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `value`
            </td>

            <td>
              `string` 또는 `float`
            </td>

            <td>
              필수의. 속성의 값입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```typescript
           NewRelicCapacitorPlugin.setAttribute({ name: "CapacitorAttribute", value: "123" });
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## 통사론 [#syntax]

      ```typescript
      setAttribute(attributeName: string, value: boolean | number | string): void;
      ```

      ## 설명 [#description]

      여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 만듭니다. 이전 값을 덮어쓰고 호출될 때마다 입력합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `attributeName`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `value`
            </td>

            <td>
              `bool`
            </td>

            <td>
              필수의. 속성의 값입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```js
      NewRelic.setAttribute(Name: "CordovaAttribute", value: "123"): void;
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## 통사론 [#syntax]

      ```csharp
      SetAttribute(string name, string value) : bool;

      SetAttribute(string name, double value) : bool;

      SetAttribute(string name, bool value) : bool;
      ```

      ## 설명 [#description]

      여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 만듭니다. 이전 값을 덮어쓰고 호출될 때마다 입력합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `value`
            </td>

            <td>
              `string`, `double` 또는 `bool`
            </td>

            <td>
              필수의. 속성의 값입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```C#
          CrossNewRelic.Current.SetAttribute("MAUIBoolAttr", false);
          CrossNewRelic.Current.SetAttribute("MAUIStrAttr", "Cat");
          CrossNewRelic.Current.SetAttribute("MAUINumAttr", 13.5);
      ```
    </TabsPageItem>

    <TabsPageItem id="flutter">
      ## 통사론 [#syntax]

      ```dart
      setAttribute (String name, dynamic value) : void;
      ```

      ## 설명 [#description]

      여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 만듭니다. 이전 값을 덮어쓰고 호출될 때마다 입력합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `dynamic value`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 값입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```dart
          NewrelicMobile.instance.setAttribute('RNCustomAttrNumber', 37);
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## 통사론 [#syntax]

      ```js
      setAttribute(name: string, value: boolean | number | string): void;
      ```

      ## 설명 [#description]

      여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 만듭니다. 이전 값을 덮어쓰고 호출될 때마다 입력합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `value`
            </td>

            <td>
              `bool`, `number` 또는 `string`
            </td>

            <td>
              필수의. 속성의 값입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```js
        NewRelic.setAttribute('RNCustomAttrNumber', 37);
      ```
    </TabsPageItem>

    <TabsPageItem id="xamarin">
      ## 통사론 [#syntax]

      ```csharp
      SetAttribute(string name, string value) : bool;

      SetAttribute(string name, double value) : bool;

      SetAttribute(string name, bool value) : bool;
      ```

      ## 설명 [#description]

      여러 모바일 이벤트 유형에서 공유하는 세션 수준 속성을 만듭니다. 이전 값을 덮어쓰고 호출될 때마다 입력합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 속성의 이름입니다.
            </td>
          </tr>

          <tr>
            <td>
              `value`
            </td>

            <td>
              `string`, `double` 또는 `bool`
            </td>

            <td>
              필수의. 속성의 값입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```csharp
          CrossNewRelicClient.Current.SetAttribute("XamarinBoolAttr", false);
          CrossNewRelicClient.Current.SetAttribute("XamarinStrAttr", "Cat");
          CrossNewRelicClient.Current.SetAttribute("XamarinNumAttr", 13.5);
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>