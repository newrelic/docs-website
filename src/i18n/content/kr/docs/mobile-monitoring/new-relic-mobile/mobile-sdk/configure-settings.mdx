---
title: 모바일 모니터링 설정 구성
type: apiDoc
shortDescription: 모바일 모니터링 설정을 활성화 또는 비활성화하는 API.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: New Relic mobile monitoring API to enable or disable mobile monitoring settings.
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      콘덴서
    </TabsBarItem>

    <TabsBarItem id="cordova">
      코르도바
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET 마우이
    </TabsBarItem>

    <TabsBarItem id="flutter">
      실룩 거리다
    </TabsBarItem>

    <TabsBarItem id="react">
      네이티브 반응
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      자마린
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 설명 [#description]

      아래 방법을 사용하여 기본 모바일 모니터링 설정을 변경하십시오. 에이전트 호출을 포함한 모든 설정은 `MainActivity` 클래스의 `onCreate` 메서드에서 호출됩니다.

      설정을 변경하려면 두 가지 옵션이 있습니다(설정이 지원하는 경우).

      * 각 특정 조건에 대해 자체 행에서 설정을 변경하십시오. 예를 들어:

        ```java
        NewRelic.disableFeature(FeatureFlag.DefaultInteractions);
        NewRelic.enableFeature(FeatureFlag.CrashReporting);
        NewRelic.withApplicationToken(<NEW_RELIC_TOKEN>).start(this.getApplication());
        ```

        또는

      * `.with` 메서드를 사용하여 에이전트 시작 호출의 일부로 설정을 변경합니다. 예를 들어:

        ```java
        NewRelic.withApplicationToken(<NEW_RELIC_TOKEN>)
                .withDefaultInteractions(false)
                .withCrashReportingEnabled(true)
                .start(this.getApplication());
        ```

      ## 분석 설정

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            이벤트 데이터 수집을 활성화하거나 비활성화합니다. 이러한 이벤트 [는 NRQL로 쿼리할 수 있으며](/docs/insights/explore-data/attributes/mobile-default-attributes-insights) [**충돌 분석** 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes) 에서 사용할 수 있습니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            withAnalyticsEvents(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            이벤트 데이터 보고를 활성화하거나 비활성화합니다. 이벤트 **collection** \[수집은] 계속 발생하지만 이벤트는 수집기로 전송되지 않습니다. 수집을 비활성화하고 싶지만 에이전트가 수집한 내용을 계속 볼 수 있는 경우 `withAnalyticsEvents` 대신 이것을 사용하기로 결정할 수 있습니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.AnalyticsEvents(true)
            ```
          </td>
        </tr>
      </table>

      ## 앱 실행 시간 설정 [#applaunchtime]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            [메트릭](/docs/data-apis/understand-data/new-relic-data-types/#metrics) 에 대한 보고 앱 시작 시간(콜드 및 핫 타임) 활성화 또는 비활성화:

            * 콜드 스타트 시간은 앱이 처음부터 시작되는 것을 의미합니다. 이는 앱의 프로세스가 그때까지 시스템에 의해 생성되지 않았음을 의미합니다.

            * 핫 시작 시간은 앱의 프로세스가 이미 백그라운드에서 실행 중인 시간을 나타냅니다.

              기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.AppStartMetrics(false)
            ```
          </td>
        </tr>
      </table>

      ## 애플리케이션 설정

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            애플리케이션의 버전을 문자열로 설정합니다. 기본값은 `AndroidManifest.xml` 에 설정된 버전 번호입니다.
          </td>

          <td>
            ```java
            withApplicationVersion("MY APP VERSION")
            ```
          </td>
        </tr>

        <tr>
          <td>
            빌드 버전을 문자열로 설정합니다. 기본값은 `AndroidManifest.xml` 에 설정된 빌드 ID입니다.
          </td>

          <td>
            ```java
            withBuildVersion("MY BUILD VERSION")
            ```
          </td>
        </tr>
      </table>

      ## 충돌 및 오류 보고 설정 [#crash-error-reporting-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            [지연된 충돌 보고를](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/android-agent-crash-reporting/#deferred-crash-reporting) 활성화하거나 비활성화합니다. 기본값은 `false` 입니다.
          </td>

          <td>
            ```java
            withCrashReportingEnabled(true)
            ```
          </td>
        </tr>

        <tr>
          <td>
            [처리된 예외 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/handled-exceptions-analyze-trends-prevent-crashes) 에 표시되는 처리된 예외 이벤트 기록을 활성화하거나 비활성화합니다. 기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.HandledExceptions(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            [충돌 분석 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/crashes) 에 표시되는 기본 런타임 충돌, 예외 및 ANR(응용 프로그램 응답 없음) 조건의 기록을 활성화하거나 비활성화합니다. 기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.NativeReporting(false)
            ```
          </td>
        </tr>
      </table>

      ## 분산 추적 [#dt]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            네트워크 요청에 분산 추적 헤더 추가를 활성화하거나 비활성화합니다.

            기본값은 `true` 입니다.

            모바일 앱의 분산 추적에 대한 자세한 내용 [은 New Relic 분산 추적 작동 방식](/docs/distributed-tracing/concepts/how-new-relic-distributed-tracing-works#browser-spans) 을 참조하세요.
          </td>

          <td>
            ```java
            FeatureFlag.DistributedTracing(false)
            ```
          </td>
        </tr>
      </table>

      ## FedRamp 설정 [#fedrampenabled]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            미국 정부 클라이언트에 대해 서로 다른 엔드포인트를 사용하여 보고 데이터를 활성화 또는 비활성화합니다.

            기본값은 `true` 입니다(에이전트 버전 6.11.0 기준).
          </td>

          <td>
            ```java
            FeatureFlag.FedRampEnabled(false)
            ```
          </td>
        </tr>
      </table>

      ## 상호작용 설정 [#interaction-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            상호 작용 추적을 활성화하거나 비활성화합니다. 추적 계측은 계속 발생하지만 추적이 수집되지 않습니다. 이렇게 하면 기본 및 사용자 지정 상호 작용이 비활성화됩니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.InteractionTracing(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            기본 상호 작용을 활성화하거나 비활성화합니다. 추적 계측은 계속 발생하지만 추적이 수집되지 않습니다. 이렇게 하면 사용자 지정 상호 작용이 활성화된 상태로 유지되는 동안에만 기본 상호 작용이 활성화되거나 비활성화됩니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.DefaultInteractions(false)
            ```
          </td>
        </tr>
      </table>

      ## 로깅 설정 [#logging]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            에이전트 로깅을 활성화하거나 비활성화합니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            withLoggingEnabled(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            로그 수준을 지정합니다. 옵션에는 다음이 포함됩니다.

            * `ERROR` (가장 장황하지 않음)

            * `WARNING`

            * `INFO`

            * `VERBOSE`

            * `DEBUG`

            * `AUDIT` (가장 장황한)

              기본값은 `INFO` 입니다.
          </td>

          <td>
            ```java
            withLogLevel("ERROR")
            ```
          </td>
        </tr>
      </table>

      ## 네트워킹 설정 [#networking]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            [MobileRequest](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequest-examples) 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.

            기본값은 `true` 입니다(에이전트 버전 5.15.2 기준).
          </td>

          <td>
            ```java
            FeatureFlag.NetworkRequests(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            [MobileRequestError](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequesterror-examples) 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다. Android 에이전트 버전 5.11.0 이상에서 사용할 수 있습니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.NetworkErrorRequests(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            HTTP 오류 추적 및 [MobileRequestError](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequesterror-examples) 이벤트에 대한 HTTP 응답 본문 캡처를 활성화하거나 비활성화합니다.

            기본값은 `true` 입니다.
          </td>

          <td>
            ```java
            FeatureFlag.HttpResponseBodyCaptureEnabled(false)
            ```
          </td>
        </tr>
      </table>
    </TabsPageItem>

    <TabsPageItem id="ios">
      New Relic의 iOS 에이전트는 에이전트의 기본 동작을 변경하기 위한 구성 설정을 제공합니다. 기본 설정을 변경하는 경우 앱 토큰을 호출한 직후에 기능 플래그를 추가해야 합니다. 예를 들어:

      * 목표-C:

        ```objectivec
        + [NewRelic startWithApplicationToken:]
        ```

      * 빠른:

        ```swift
        NewRelic.start(withApplicationToken:)
        ```

      ## 기능 플래그 활성화 또는 비활성화 [#ios-feature-flags]

      기능 플래그를 구성하려면 에 정의된 다음 방법을 사용하십시오. `NewRelic.h:`

      <table>
        <thead>
          <tr>
            <th width={150}>
              iOS 언어
            </th>

            <th>
              절차
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td id="enable-obj-c">
              오브젝티브-C
            </td>

            <td>
              Objective-C에서 이러한 기능은 비트마스크되어 있으므로 `|` 를 사용하여 동시에 여러 기능을 활성화하거나 비활성화할 수 있습니다.

              기능 플래그 활성화:

              ```objectivec
              +[NewRelic enableFeatures:(NRMAFeatureFlags)flags];
              ```

              기능 플래그 비활성화:

              ```objectivec
              +[NewRelic disableFeatures:(NRMAFeatureFlags)flags];
              ```

              다음은 예입니다.

              * 상호 작용 추적 및 실험적 네트워킹 계측이 활성화됩니다.
              * `NSURLSession` 계측 및 충돌 보고가 비활성화됩니다.

              ```objectivec
              [NewRelic enableFeatures:NRFeatureFlag_SwiftInteractionTracing | NRFeatureFlag_FedRampEnabled];
              [NewRelic disableFeatures:NRFeatureFlag_NSURLSessionInstrumentation | NRFeatureFlag_CrashReporting]; 
              [NewRelic startWithApplicationToken:...];
              ```
            </td>
          </tr>

          <tr>
            <td>
              빠른
            </td>

            <td>
              기능 플래그 활성화:

              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.[NRMAFeatureFlag])
              ```

              기능 플래그 비활성화:

              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.[NRMAFeatureFlag])
              ```

              기본 상호작용 추적을 비활성화하는 예:

              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DefaultInteractions)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## 충돌 및 오류 보고 기능 플래그 [#crashes]

      아래의 충돌 및 오류 보고 설정을 수정하는 경우 New Relic iOS 에이전트 시작 호출 전에 기능 플래그를 호출해야 합니다.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              [**Crash analysis** \[충돌 분석\] 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes) 에 표시되는 충돌 보고를 활성화하거나 비활성화합니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_CrashReporting(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              [처리된 예외 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/handled-exceptions-analyze-trends-prevent-crashes) 에 표시되는 처리된 예외 이벤트 기록을 전역적으로 활성화하거나 비활성화합니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_HandledExceptionEvents(false)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## 데이터 끝점 설정 [#logging]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              수집 데이터 업로드 끝점의 URI 기관 구성 요소를 지정합니다. 이 엔드포인트는 처리된 예외 업로드에도 사용됩니다.

              데이터 수집을 보고할 때 에이전트가 사용할 데이터 끝점을 바꾸려면 `NewRelic.withApplicationToken()` 메서드 호출에 `.usingCollectorAddress()` 추가하세요.

              기본값은 `mobile-collector.newrelic.com` 입니다.
            </td>

            <td>
              ```objectivec
              NewRelic.withApplicationToken("YOUR_APP_TOKEN")
                .usingCollectorAddress("harvest-upload-delegate.domain.com")
                .start(this.getApplication());
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 데이터 업로드 URI의 권한 구성 요소를 지정합니다.

              비정상 종료를 보고할 때 에이전트가 사용할 데이터 엔드포인트를 바꾸려면 `NewRelic.withApplicationToken()` 메서드 호출에 `.usingCrashCollectorAddress()` 추가하세요.

              기본값은 `mobile-crash.newrelic.com` 입니다.
            </td>

            <td>
              ```objectivec
              NewRelic.withApplicationToken("YOUR_APP_TOKEN")
              .usingCrashCollectorAddress("crash-upload-delegate.domain.com")
              .start(this.getApplication());
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## 분산 추적 [#dt]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              네트워크 요청에 대한 분산 추적 헤더 추가를 활성화하거나 비활성화합니다. 모바일 앱의 분산 추적에 대한 자세한 내용은 [New Relic 분산 추적 작동 방식을](/docs/understand-dependencies/distributed-tracing/get-started/how-new-relic-distributed-tracing-works/#no-sampling) 참조하세요.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_DistributedTracing(false)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## 상호작용 기능 플래그 [#interactions]

      아래의 충돌 및 오류 보고 설정을 수정하는 경우 New Relic iOS 에이전트 시작 호출 직전에 기능 플래그를 호출해야 합니다.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              기본 계측 클래스 및 메서드에 대해서만 상호 작용 추적을 활성화하거나 비활성화합니다. 사용자 지정 상호 작용이 보고됩니다. 추적은 [**Interactions** \[상호 작용\] 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page) 에 나타납니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_DefaultInteractions(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              [**Interactions** \[상호 작용\] 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page) 에 표시되는 상호 작용 추적을 전역적으로 활성화하거나 비활성화합니다.

              에이전트가 시작된 후에는 상호 작용 추적이 비활성화됩니다. 에이전트를 시작하기 전에 실행된 활동 또는 방법에 대한 상호 작용은 여전히 **상호 작용** 페이지에 나타날 수 있습니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_InteractionTracing(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              <Callout variant="caution">
                이 기능을 활성화하기 전에 [Swift 상호 작용 추적 활성화](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/enable-swift-interaction-traces) 를 참조하십시오. 적절한 조치를 취하지 않으면 이 기능을 활성화하면 불안정해질 수 있습니다.
              </Callout>

              Swift 코드에 대한 상호 작용 추적을 활성화 또는 비활성화합니다. 추적은 [**Interactions** \[상호 작용\] 페이지](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page) 에 나타납니다. 이 기능은 [`NRFeatureFlag_InteractionTracing`](#interactionTracing) (가) 비활성화되면 항상 비활성화됩니다.

              기본값은 `false` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_SwiftInteractionTracing(true)
              ```
            </td>
          </tr>

          <tr>
            <td>
              자동 WKWebView 계측을 활성화하거나 비활성화합니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_WebViewInstrumentation(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              <Callout variant="caution">
                이 기능은 더 이상 사용되지 않으므로 이 기능 플래그 호출을 활성화하면 애플리케이션의 안정성이 저하될 수 있습니다. New Relic에서 지시하지 않는 한 사용을 피하십시오.
              </Callout>

              자동 제스처 계측을 활성화하거나 비활성화합니다.

              기본값은 `false` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_GestureInstrumentation(true)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## 네트워킹 기능 플래그 [#networking]

      아래 설정을 수정하는 경우 New Relic iOS 에이전트 시작 호출 전에 기능 플래그를 호출해야 합니다.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              <Callout variant="caution">
                이 기능은 더 이상 사용되지 않으므로 이 기능 플래그 호출을 활성화하면 애플리케이션의 안정성이 저하될 수 있습니다. New Relic에서 지시하지 않는 한 사용을 피하십시오.
              </Callout>

              실험적 네트워킹 계측을 활성화하거나 비활성화합니다. 이것은 `NRMAURProtocol` 을 통해 모든 `NSURLConnection` 네트워크 요청을 강제합니다.

              기본값은 `false` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_ExperimentalNetworkingInstrumentation(true)
              ```
            </td>
          </tr>

          <tr>
            <td>
              `NSURLSession` 에 대한 네트워킹 계측을 활성화 또는 비활성화합니다. 이러한 URL 추적은 [HTTP 요청](/docs/mobile-monitoring/mobile-monitoring-ui/network-pages/http-requests-page) 페이지로 전송됩니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_NSURLSessionInstrumentation(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              네트워킹 계측을 활성화하거나 비활성화합니다. 그러면 HTTP 응답 데이터가 [MobileRequest](/docs/insights/insights-data-sources/default-data/mobile-default-event-attributes-insights#mobilerequest-attributes) 이벤트로 전송됩니다.

              기본값은 `true` 입니다(에이전트 버전 6.0.0 기준).
            </td>

            <td>
              ```swift
              NRFeatureFlag_NetworkRequestEvents(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              오류 또는 실패를 초래하는 구성된 네트워크 호출의 HTTP 요청 오류 이벤트 생성을 활성화하거나 비활성화합니다. 이러한 이벤트는 [MobileRequestError](/docs/insights/insights-data-sources/default-data/mobile-default-event-attributes-insights#mobilerequesterror-attributes) 이벤트로 보고됩니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_RequestErrorEvents(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.

              기본값은 `true` 입니다.
            </td>

            <td>
              ```swift
              NRFeatureFlag_HttpResponseBodyCapture(false)
              ```
            </td>
          </tr>

          <tr>
            <td>
              async await URL 세션에 대한 계측을 활성화하거나 비활성화합니다.

              기본값은 `false` 입니다(에이전트 버전 7.4.5 기준).
            </td>

            <td>
              ```swift
              NRFeatureFlag_SwiftAsyncURLSessionSupport(true)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## 앱 실행 시간 설정 [#applaunchtime]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              메트릭으로 보고 앱 시작 시간(콜드 및 핫 타임)을 활성화 또는 비활성화합니다.

              * 콜드 타임은 앱 시작과 첫 번째 추첨 사이의 시간을 나타냅니다.
              * 핫 타임은 앱이 백그라운드에서 실행되는 것부터 첫 번째 그리기까지 다시 시작하는 데 걸리는 시간을 의미합니다. 기본값은 `true` 입니다(에이전트 버전 7.4.0 기준).
            </td>

            <td>
              ```swift
              NRFeatureFlag_AppStartMetrics(false)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## FedRAMP 끝점 설정 [#fedramp]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              FedRAMP 엔드포인트를 사용하여 활성화 또는 비활성화합니다.

              기본값은 `false` 입니다(에이전트 버전 7.4.5 기준).
            </td>

            <td>
              ```swift
              NRFeatureFlag_FedRampEnabled(true)
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      Capacitor 에이전트 SDK를 사용하면 기본 설정을 구성하여 에이전트의 동작을 변경할 수 있습니다.

      기본 설정을 변경하는 경우 `appToken` 호출한 직후에 기능 플래그를 추가해야 합니다.

      샘플 구성은 다음과 같습니다.

      ```typescript
      import { NewRelicCapacitorPlugin, NREnums, AgentConfiguration } from '@newrelic/newrelic-capacitor-plugin';
      import { Capacitor } from '@capacitor/core';

      var appToken;

      if(Capacitor.getPlatform() === 'ios') {
          appToken = '<IOS-APP-TOKEN>';
      } else {
          appToken = '<ANDROID-APP-TOKEN>';
      }

      let agentConfig : AgentConfiguration = {

        analyticsEventEnabled: false,
        crashReportingEnabled: false,
        interactionTracingEnabled: false,
        networkRequestEnabled: false,
        networkErrorRequestEnabled: false,
        httpResponseBodyCaptureEnabled: false,
        loggingEnabled: false,
        logLevel: NREnums.LogLevel.INFO,
        webViewInstrumentation: false,
        collectorAddress: "",
        crashCollectorAddress: "",
        sendConsoleEvents: false,
        fedRampEnabled: false
      }

      NewRelicCapacitorPlugin.start({appKey:appToken, agentConfiguration:agentConfig})
      ```

      ## 사용 가능한 구성 [#configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android 전용) 이벤트 데이터 수집을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              상호 작용 추적을 활성화하거나 비활성화합니다. 추적 계측은 계속 발생하지만 추적이 수집되지 않습니다. 이렇게 하면 기본 및 사용자 지정 상호 작용이 비활성화됩니다.
            </td>

            <td>
              ```typescript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequest` 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequestError` 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              에이전트 로깅을 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              로그 수준을 지정합니다. 기본 로그 수준에 대해서는 이 필드를 생략하십시오.

              옵션에는 `ERROR` (가장 자세한 정보 없음), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (가장 자세한 정보 표시)가 포함됩니다.
            </td>

            <td>
              ```typescript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS 전용) `WebViews` 의 자동 계측을 활성화/비활성화합니다.
            </td>

            <td>
              ```typescript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              데이터 전송을 위한 특정 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```typescript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌을 보내기 위한 특정 충돌 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```typescript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              New Relic에 JavaScript 콘솔 로그 전송을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              미국 정부 클라이언트에 대해 서로 다른 엔드포인트를 사용하여 보고 데이터를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              fedRampEnabled: false
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="cordova">
      Cordova 에이전트 SDK를 사용하면 기본 설정을 구성하여 에이전트의 동작을 변경할 수 있습니다.

      기본 설정을 변경하는 경우 `appToken` 호출한 직후에 기능 플래그를 추가해야 합니다. 

      샘플 구성은 다음과 같습니다.

      ```typescript
      # Disable Crash Reporting
      cordova plugin add https://github.com/newrelic/newrelic-cordova-plugin.git 
      --variable IOS_APP_TOKEN="{ios-app-token}" 
      --variable ANDROID_APP_TOKEN="{android-app-token}" 
      --variable CRASH_REPORTING_ENABLED="false"
      ```

      ## 사용 가능한 구성 [#configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android 전용) 이벤트 데이터 수집을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              상호 작용 추적을 활성화하거나 비활성화합니다. 추적 계측은 계속 발생하지만 추적이 수집되지 않습니다. 이렇게 하면 기본 및 사용자 지정 상호 작용이 비활성화됩니다.
            </td>

            <td>
              ```typescript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequest` 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequestError` 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              에이전트 로깅을 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```typescript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              로그 수준을 지정합니다. 기본 로그 수준에 대해서는 이 필드를 생략하십시오.

              옵션에는 `ERROR` (가장 자세한 정보 없음), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (가장 자세한 정보 표시)가 포함됩니다.
            </td>

            <td>
              ```typescript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS 전용) `WebViews` 의 자동 계측을 활성화/비활성화합니다.
            </td>

            <td>
              ```typescript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              데이터 전송을 위한 특정 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```typescript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌을 보내기 위한 특정 충돌 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```typescript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              New Relic에 JavaScript 콘솔 로그 전송을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              미국 정부 클라이언트에 대해 서로 다른 엔드포인트를 사용하여 보고 데이터를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```typescript
              fedRampEnabled: false
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="maui">
      .NET MAUI 에이전트 SDK를 사용하면 기본 설정을 구성하여 에이전트의 동작을 변경할 수 있습니다.

      기본 설정을 변경하는 경우 `appToken` 호출한 직후에 기능 플래그를 추가해야 합니다.

      샘플 구성은 다음과 같습니다.

      ```csharp
       using NewRelic.MAUI.Plugin;
                  ...
          public App()
              {
                  InitializeComponent();

                  MainPage = new AppShell();

                  CrossNewRelic.Current.HandleUncaughtException();
                  CrossNewRelic.Current.TrackShellNavigatedEvents();

                  // Set optional agent configuration
                  // Options are: crashReportingEnabled, loggingEnabled, logLevel, collectorAddress, crashCollectorAddress
                  AgentStartConfiguration agentConfig = new AgentStartConfiguration(true, true, LogLevel.INFO, "mobile-collector.newrelic.com", "mobile-crash.newrelic.com");

                  if (DeviceInfo.Current.Platform == DevicePlatform.Android)
                  {
                      // CrossNewRelic.Current.Start("<APP-TOKEN-HERE>");
                      // Start with optional agent configuration 
                       CrossNewRelic.Current.Start("<APP-TOKEN-HERE", agentConfig);
                  }
                  else if (DeviceInfo.Current.Platform == DevicePlatform.iOS)
                  {
                      // CrossNewRelic.Current.Start("<APP-TOKEN-HERE>");
                      // Start with optional agent configuration 
                       CrossNewRelic.Current.Start("<APP-TOKEN-HERE", agentConfig);
                  }
              }



          }
      ```

      ## 사용 가능한 구성 [#configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android 전용) 이벤트 데이터 수집을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```csharp
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              에이전트 로깅을 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequest` 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequestError` 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```csharp
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              로그 수준을 지정합니다. 기본 로그 수준에 대해서는 이 필드를 생략하십시오.

              옵션에는 `ERROR` (가장 자세한 정보 없음), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (가장 자세한 정보 표시)가 포함됩니다.
            </td>

            <td>
              ```csharp
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              데이터 전송을 위한 특정 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```csharp
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌을 보내기 위한 특정 충돌 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```csharp
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="flutter">
      Flutter 에이전트 SDK를 사용하면 기본 설정을 구성하여 에이전트의 동작을 변경할 수 있습니다.

      기본 설정을 변경하는 경우 `appToken` 호출한 직후에 기능 플래그를 추가해야 합니다.

      샘플 구성은 다음과 같습니다.

      ```dart
      if (Platform.isAndroid) {
        appToken = AppConfig.androidToken;
      } else if (Platform.isIOS) {
        appToken = AppConfig.iOSToken;
      }

      Config config = Config(
          accessToken: appToken,
          analyticsEventEnabled: false,
          networkErrorRequestEnabled: false,
          networkRequestEnabled: false,
          crashReportingEnabled: false,
          interactionTracingEnabled: false,
          httpResponseBodyCaptureEnabled: false,
          loggingEnabled: false,
          webViewInstrumentation: false,
          printStatementAsEventsEnabled: false,
          httpInstrumentationEnabled:false);

      // NewrelicMobile.instance.start(config, () {
      //   runApp(MyApp());
      // });

      runZonedGuarded(() async {
        WidgetsFlutterBinding.ensureInitialized();
        FlutterError.onError = NewrelicMobile.onError;
        await NewrelicMobile.instance.startAgent(config);
        runApp(MyApp());
      }, (Object error, StackTrace stackTrace) {
        NewrelicMobile.instance.recordError(error, stackTrace);
      });
      ```

      ## 사용 가능한 구성 [#configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android 전용) 이벤트 데이터 수집을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```dart
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```dart
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              상호 작용 추적을 활성화하거나 비활성화합니다. 추적 계측은 계속 발생하지만 추적이 수집되지 않습니다. 이렇게 하면 기본 및 사용자 지정 상호 작용이 비활성화됩니다.
            </td>

            <td>
              ```dart
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequest` 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```dart
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequestError` 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```dart
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```dart
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              에이전트 로깅을 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```dart
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              로그 수준을 지정합니다. 기본 로그 수준에 대해서는 이 필드를 생략하십시오.

              옵션에는 `ERROR` (가장 자세한 정보 없음), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (가장 자세한 정보 표시)가 포함됩니다.
            </td>

            <td>
              ```dart
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS 전용) `WebViews` 의 자동 계측을 활성화/비활성화합니다.
            </td>

            <td>
              ```dart
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              데이터 전송을 위한 특정 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```dart
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌을 보내기 위한 특정 충돌 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```dart
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              New Relic에 JavaScript 콘솔 로그 전송을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```dart
              sendConsoleEvents: true
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="react">
      React Native 에이전트 SDK를 사용하면 기본 설정을 구성하여 에이전트의 동작을 변경할 수 있습니다.

      기본 설정을 변경하는 경우 `appToken` 호출한 직후에 기능 플래그를 추가해야 합니다.

      샘플 구성은 다음과 같습니다.

      ```javascript
      import NewRelic from 'newrelic-react-native-agent';
      import * as appVersion from './package.json';
      import {Platform} from 'react-native';

          let appToken;

          if (Platform.OS === 'ios') {
              appToken = '<IOS-APP-TOKEN>';
          } else {
              appToken = '<ANDROID-APP-TOKEN>';
          }


       const agentConfiguration = {
       Config config = Config(
          analyticsEventEnabled: false,
          crashReportingEnabled: false,
          interactionTracingEnabled: false,
          networkRequestEnabled: false,
          networkErrorRequestEnabled: false,
          httpResponseBodyCaptureEnabled: false,
          loggingEnabled: false,
          logLevel: NREnums.LogLevel.INFO,
          webViewInstrumentation: false,
          collectorAddress: "",
          crashCollectorAddress: "",
          sendConsoleEvents: false,
          fedRampEnabled: false
        };


      NewRelic.startAgent(appToken,agentConfiguration);
      NewRelic.setJSAppVersion(appVersion.version);
      AppRegistry.registerComponent(appName, () => App);
      ```

      ## 사용 가능한 구성 [#configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android 전용) 이벤트 데이터 수집을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```javascript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```javascript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              상호 작용 추적을 활성화하거나 비활성화합니다. 추적 계측은 계속 발생하지만 추적이 수집되지 않습니다. 이렇게 하면 기본 및 사용자 지정 상호 작용이 비활성화됩니다.
            </td>

            <td>
              ```javascript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequest` 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```javascript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequestError` 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```javascript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```javascript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              에이전트 로깅을 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```javascript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              로그 수준을 지정합니다. 기본 로그 수준에 대해서는 이 필드를 생략하십시오.

              옵션에는 `ERROR` (가장 자세한 정보 없음), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (가장 자세한 정보 표시)가 포함됩니다.
            </td>

            <td>
              ```javascript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS 전용) `WebViews` 의 자동 계측을 활성화/비활성화합니다.
            </td>

            <td>
              ```javascript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              데이터 전송을 위한 특정 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```javascript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌을 보내기 위한 특정 충돌 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```javascript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              New Relic에 JavaScript 콘솔 로그 전송을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```javascript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              미국 정부 클라이언트에 대해 서로 다른 엔드포인트를 사용하여 보고 데이터를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```javascript
              fedRampEnabled: false
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="xamarin">
      Xamarin 에이전트 SDK를 사용하면 기본 설정을 구성하여 에이전트의 동작을 변경할 수 있습니다.

      기본 설정을 변경하는 경우 `appToken` 호출한 직후에 기능 플래그를 추가해야 합니다.

      샘플 구성은 다음과 같습니다.

      ```csharp
      public App ()
                  {
                      InitializeComponent();

                      MainPage = new MainPage();
                      Application.Current.PageAppearing += OnPageAppearing;
                      Application.Current.PageDisappearing += PageDisappearing;

                      CrossNewRelicClient.Current.HandleUncaughtException();
                      CrossNewRelicClient.Current.TrackShellNavigatedEvents()
            // Set optional agent configuration
            // Options are: crashReportingEnabled, loggingEnabled, logLevel, collectorAddress, crashCollectorAddress
            AgentStartConfiguration agentConfig = new AgentStartConfiguration(true, true, LogLevel.INFO, "mobile-collector.newrelic.com", "mobile-crash.newrelic.com");
            if (Device.RuntimePlatform == Device.Android)
                      {
                          //CrossNewRelicClient.Current.Start("<APP-TOKEN-HERE>");
                          // Start with optional agent configuration
                          CrossNewRelicClient.Current.Start("<APP-TOKEN-HERE", agentConfig);
                      }
                      else if (Device.RuntimePlatform == Device.iOS)
                      {
                          //CrossNewRelicClient.Current.Start("<APP-TOKEN-HERE>");
                          // Start with optional agent configuration 
                          CrossNewRelicClient.Current.Start("<APP-TOKEN-HERE", agentConfig);
                      }
                  }
              }
      ```

      ## 사용 가능한 구성 [#configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              설명
            </th>

            <th>
              예시
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android 전용) 이벤트 데이터 수집을 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```csharp
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              에이전트 로깅을 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequest` 이벤트 유형에 대한 성공적인 HTTP 요청 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              `MobileRequestError` 이벤트 유형에 대한 네트워크 및 HTTP 요청 오류 보고를 활성화하거나 비활성화합니다.
            </td>

            <td>
              ```csharp
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              HTTP 오류 추적 및 `MobileRequestError` 이벤트에 대한 HTTP 응답 본문 캡처를 활성화 또는 비활성화합니다.
            </td>

            <td>
              ```csharp
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              로그 수준을 지정합니다. 기본 로그 수준에 대해서는 이 필드를 생략하십시오.

              옵션에는 `ERROR` (가장 자세한 정보 없음), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (가장 자세한 정보 표시)가 포함됩니다.
            </td>

            <td>
              ```csharp
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              데이터 전송을 위한 특정 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```csharp
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              충돌을 보내기 위한 특정 충돌 수집기 주소를 설정합니다. 기본 주소에 대해서는 이 필드를 생략하십시오.
            </td>

            <td>
              ```csharp
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>
  </TabsPages>
</Tabs>