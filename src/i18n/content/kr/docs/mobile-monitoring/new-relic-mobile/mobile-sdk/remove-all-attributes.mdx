---
title: 모든 속성 제거
type: apiDoc
shortDescription: 모든 속성을 제거합니다.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to remove all attributes.
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      콘덴서
    </TabsBarItem>

    <TabsBarItem id="cordova">
      코르도바
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET 마우이
    </TabsBarItem>

    <TabsBarItem id="react">
      네이티브 반응
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      자마린
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 통사론 [#syntax]

      ### 자바 [#java]

      ```java
      NewRelic.removeAllAttributes ()
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.removeAllAttributes()
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 반환 값 [#return-values]

      성공적으로 기록된 경우 `true` 을 반환하고 기록되지 않은 경우 `false` 을 반환합니다.

      ## 예시 [#example]

      ### 자바 [#java]

      ```java
      boolean attributesRemoved = NewRelic.removeAllAttributes();
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
          val attributesRemoved = NewRelic.removeAllAttributes()
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## 통사론 [#syntax]

      ### 목표-c [#objc]

      ```
      + (BOOL) removeAllAttributes();
      ```

      ### 빠른 [#swift]

      ```
      NewRelic.removeAllAttributes() -> Bool
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 반환 값 [#return-values]

      성공적으로 기록된 경우 `true` 을 반환하고 기록되지 않은 경우 `false` 을 반환합니다.

      ## 예 [#examples]

      ### 오브젝티브-C [#obj-c]

      ```objectivec
      BOOL attributesRemoved = NewRelic.removeAllAttributes();
      ```

      ### 빠른 [#swift]

      ```swift
      let attributesRemoved = NewRelic.removeAllAttributes()
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## 통사론 [#syntax]

      ```typescript
      removeAllAttributes(options?: {} | undefined) => void
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 예시 [#example]

      ```typescript
          NewRelicCapacitorPlugin.removeAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## 통사론 [#syntax]

      ```csharp
      RemoveAllAttributes() : bool;
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 예시 [#example]

      ```C#
          CrossNewRelic.Current.RemoveAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## 통사론 [#syntax]

      ```typescript
      removeAllAttributes(): void;
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 예시 [#example]

      ```js
      NewRelic.removeAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## 통사론 [#syntax]

      ```js
      removeAllAttributes(): void;
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 예시 [#example]

      ```js
         NewRelic.removeAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="xamarin">
      ## 통사론 [#syntax]

      ```csharp
      RemoveAllAttributes() : bool;
      ```

      ## 설명 [#description]

      이 메소드는 세션에서 모든 속성을 제거합니다. 단일 세션 속성을 제거하려면 [속성 제거를](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes) 참조하십시오.

      ## 예시 [#example]

      ```csharp
          CrossNewRelicClient.Current.RemoveAllAttributes();
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>