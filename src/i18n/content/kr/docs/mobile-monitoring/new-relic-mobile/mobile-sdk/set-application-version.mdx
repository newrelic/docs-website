---
title: 애플리케이션 버전 설정
type: apiDoc
shortDescription: 상호 작용과 연결할 애플리케이션 버전을 설정합니다.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to set an application version to be associated with an interaction.
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 통사론 [#syntax]

      ### 자바 [#java]

      ```java
      NewRelic.withApplicationVersion(string $appVersion)
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.withApplicationVersion(appVersion: String?)
      ```

      ## 설명 [#description]

      기본적으로 New Relic Android 에이전트는 `AndroidManifest.xml` 또는 `build.gradle` 에서 앱 버전을 가져옵니다. 이 메서드를 사용하면 해당 값을 재정의하고 앱 버전을 전달하여 앱 버전을 설정할 수 있습니다. 앱 버전은 New Relic UI에 드롭다운으로 표시되며 이를 사용하여 상호 작용을 정렬할 수 있습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$appVersion`
            </td>

            <td>
              `string`
            </td>

            <td>
              필수의. 애플리케이션 버전을 나타내는 문자열입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ### 자바 [#java]

      ```java
      NewRelic.withApplicationToken("").withApplicationVersion("1.23.4").start(this.getApplication());
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.withApplicationToken("").withApplicationVersion("1.23.4").start(applicationContext)
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## 통사론 [#syntax]

      ### 목표-c [#objc]

      ```objectivec
      setApplicationVersion:(NSString *_Nonnull)versionString;
      ```

      ### 빠른 [#swift]

      ```swift
      NewRelic.setApplicationVersion:(NSString*)versionString;
      ```

      ## 설명 [#description]

      기본적으로 New Relic은 빌드 번호를 보고할 때 `CFBundleShortVersionString` 를 사용합니다. 그러나 `startWithApplicationToken` 호출하기 전에 이 메서드를 호출하여 보고된 빌드 번호를 재정의할 수 있습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `versionString`
            </td>

            <td>
              `NSString`
            </td>

            <td>
              필수의. 이 애플리케이션의 버전으로 표시할 문자열입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ### 오브젝티브-C [#obj-c]

      ```objectivec
      [NewRelic setApplicationVersion:@"1.2.3"];
      ```

      ### 빠른 [#swift]

      ```swift
      NewRelic.setApplicationVersion("1.2.3")
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>