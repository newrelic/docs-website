---
title: 최대 이벤트 풀 크기 설정
type: apiDoc
shortDescription: 이벤트 풀의 최대 크기를 설정합니다.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to set the maximum size of the event pool.
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      콘덴서
    </TabsBarItem>

    <TabsBarItem id="cordova">
      코르도바
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET 마우이
    </TabsBarItem>

    <TabsBarItem id="flutter">
      실룩 거리다
    </TabsBarItem>

    <TabsBarItem id="react">
      네이티브 반응
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      자마린
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 통사론 [#syntax]

      ### 자바 [#java]

      ```java
      NewRelic.setMaxEventPoolSize(int $maxSize)
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.setMaxEventPoolSize(maxSize: Int)
      ```

      ## 설명 [#description]

      이벤트 풀의 최대 크기를 설정합니다.

      기본적으로 모바일 모니터링은 기본적으로 600초인 이벤트 수집 주기당 최대 1,000개의 이벤트를 수집합니다. 이 방법은 다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 제어합니다. 풀 크기 제한에 도달하면 New Relic Android 에이전트는 이벤트 풀을 다음 수확 주기와 함께 전송할 때까지 [이벤트 샘플링을](/docs/agents/manage-apm-agents/agent-data/new-relic-events-limits-sampling)시작하여 이전 및 일부 새 이벤트를 삭제합니다. 이 방법을 사용하면 해당 이벤트 풀의 최대 크기를 재정의할 수 있습니다.

      이벤트 수집 주기의 기본값은 600초입니다. 이벤트 수집 주기의 길이를 변경하려면 [최대 이벤트 버퍼 시간 설정을](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-event-buffer-time) 참조하십시오.

      <Callout variant="important">
        많은 수의 이벤트를 보고하거나 이벤트를 너무 자주 보고하면 앱 성능에 영향을 줄 수 있습니다.
      </Callout>

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$maxSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              필수의. 이벤트 풀의 최대 크기입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      다음은 이벤트 풀의 최대 크기를 1000으로 설정하는 예입니다.

      ### 자바 [#java]

      ```java
      NewRelic.setMaxEventPoolSize(1000);
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.setMaxEventPoolSize(1000)
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## 통사론 [#syntax]

      ### 오브젝티브-C [#objc]

      ```objectivec
      + (void) setMaxEventPoolSize:(unsigned int)size;
      ```

      ### 빠른 [#swift]

      ```swift
      NewRelic.setMaxEventPoolSize(unsigned int $size)
      ```

      ## 설명 [#description]

      이벤트 풀의 최대 크기를 설정합니다.

      기본적으로 iOS 에이전트는 [수확 주기](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#harvest-cycle)당 최대 1000개의 이벤트를 수집합니다. 이 방법은 다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 제어합니다. 풀 크기 제한에 도달하면 New Relic iOS 에이전트는 이벤트 풀을 다음 수확 주기와 함께 전송할 때까지 [이벤트 샘플링을](/docs/agents/manage-apm-agents/agent-data/new-relic-events-limits-sampling)시작하여 이전 및 일부 새 이벤트를 버립니다. 이 방법을 사용하면 해당 이벤트 풀의 최대 크기를 재정의할 수 있습니다. `Agent.start()` 호출된 후에 이 값을 설정해야 합니다.

      이벤트 수집 주기의 기본값은 600초입니다. 이벤트 수집 주기의 길이를 변경하려면 [최대 이벤트 버퍼 시간 설정](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-event-buffer-time/) 도 참조하십시오.

      새 값이 항상 적용되도록 하려면 이 API 호출을 `applicationDidBecomeActive` iOS 수명 주기 메서드에 배치합니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$size`
            </td>

            <td>
              `unsigned int`
            </td>

            <td>
              필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예 [#examples]

      ### 오브젝티브-C [#obj-c]

      ```objectivec
      BOOL poolSizeSet = [NewRelic setMaxEventPoolSize:1000];
      ```

      ### 빠른 [#swift]

      ```swift
      let poolSizeSet = NewRelic.setMaxEventPoolSize(1000)
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## 통사론 [#syntax]

      ```typescript
      setMaxEventPoolSize(options: { maxPoolSize: number; }) => void
      ```

      ## 설명 [#description]

      다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 설정합니다. 기본값은 이벤트 수집 주기당 최대 1000개의 이벤트입니다. 풀 크기 제한에 도달하면 에이전트는 다음 수집 주기에서 이벤트 풀이 전송될 때까지 이벤트 샘플링을 시작하고 새 이벤트와 기존 이벤트를 버립니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxPoolSize`
            </td>

            <td>
              `number`
            </td>

            <td>
              필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```typescript
              NewRelicCapacitorPlugin.setMaxEventPoolSize({ maxPoolSize: 2000 })
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## 통사론 [#syntax]

      ```typescript
      setMaxEventPoolSize(maxSize: number): void;

      ```

      ## 설명 [#description]

      다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 설정합니다. 기본값은 이벤트 수집 주기당 최대 1000개의 이벤트입니다. 풀 크기 제한에 도달하면 에이전트는 다음 수집 주기에서 이벤트 풀이 전송될 때까지 이벤트 샘플링을 시작하고 새 이벤트와 기존 이벤트를 버립니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxSize`
            </td>

            <td>
              `number`
            </td>

            <td>
              필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```js
        NewRelic.setMaxEventPoolSize(2000);
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## 통사론 [#syntax]

      ```csharp
      SetMaxEventPoolSize(int maxPoolSize): void;
      ```

      ## 설명 [#description]

      다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 설정합니다. 기본값은 이벤트 수집 주기당 최대 1000개의 이벤트입니다. 풀 크기 제한에 도달하면 에이전트는 다음 수집 주기에서 이벤트 풀이 전송될 때까지 이벤트 샘플링을 시작하고 새 이벤트와 기존 이벤트를 버립니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxPoolSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```C#
          CrossNewRelic.Current.SetMaxEventPoolSize(1500);
      ```
    </TabsPageItem>
  </TabsPages>

  <TabsPageItem id="flutter">
    ## 통사론 [#syntax]

    ```typescript
    setMaxEventPoolSize(int maxSize): void;

    ```

    ## 설명 [#description]

    다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 설정합니다. 기본값은 이벤트 수집 주기당 최대 1000개의 이벤트입니다. 풀 크기 제한에 도달하면 에이전트는 다음 수집 주기에서 이벤트 풀이 전송될 때까지 이벤트 샘플링을 시작하고 새 이벤트와 기존 이벤트를 버립니다.

    ## 매개변수 [#parameters]

    <table>
      <thead>
        <tr>
          <th width={200}>
            매개변수
          </th>

          <th width={200}>
            유형
          </th>

          <th>
            설명
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `maxSize`
          </td>

          <td>
            `int`
          </td>

          <td>
            필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
          </td>
        </tr>
      </tbody>
    </table>

    ## 예시 [#example]

    ```dart
    NewrelicMobile.instance.setMaxEventPoolSize(10000);
    ```
  </TabsPageItem>

  <TabsPageItem id="react">
    ## 통사론 [#syntax]

    ```js
    setMaxEventPoolSize(maxSize: number): void;
    ```

    ## 설명 [#description]

    다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 설정합니다. 기본값은 이벤트 수집 주기당 최대 1000개의 이벤트입니다. 풀 크기 제한에 도달하면 에이전트는 다음 수집 주기에서 이벤트 풀이 전송될 때까지 이벤트 샘플링을 시작하고 새 이벤트와 기존 이벤트를 버립니다.

    ## 매개변수 [#parameters]

    <table>
      <thead>
        <tr>
          <th width={200}>
            매개변수
          </th>

          <th width={200}>
            유형
          </th>

          <th>
            설명
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `maxSize`
          </td>

          <td>
            `number`
          </td>

          <td>
            필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
          </td>
        </tr>
      </tbody>
    </table>

    ## 예시 [#example]

    ```js
       NewRelic.setMaxEventPoolSize(2000);
    ```
  </TabsPageItem>

  <TabsPageItem id="xamarin">
    ## 통사론 [#syntax]

    ```csharp
    SetMaxEventPoolSize(int maxPoolSize): void;
    ```

    ## 설명 [#description]

    다음 수집 주기까지 메모리에 저장되는 이벤트 풀의 최대 크기를 설정합니다. 기본값은 이벤트 수집 주기당 최대 1000개의 이벤트입니다. 풀 크기 제한에 도달하면 에이전트는 다음 수집 주기에서 이벤트 풀이 전송될 때까지 이벤트 샘플링을 시작하고 새 이벤트와 기존 이벤트를 버립니다.

    ## 매개변수 [#parameters]

    <table>
      <thead>
        <tr>
          <th width={200}>
            매개변수
          </th>

          <th width={200}>
            유형
          </th>

          <th>
            설명
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `maxPoolSize`
          </td>

          <td>
            `int`
          </td>

          <td>
            필수의. New Relic으로 전송되기 전에 메모리에 버퍼링될 수 있는 최대 이벤트 수입니다.
          </td>
        </tr>
      </tbody>
    </table>

    ## 예시 [#example]

    ```csharp
        CrossNewRelicClient.Current.SetMaxEventPoolSize(1500);
    ```
  </TabsPageItem>
</Tabs>