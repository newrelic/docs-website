---
title: 최대 이벤트 버퍼 시간 설정
type: apiDoc
shortDescription: 이벤트 수확 주기 길이를 설정합니다.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to set the event harvest cycle length.
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      콘덴서
    </TabsBarItem>

    <TabsBarItem id="cordova">
      코르도바
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET 마우이
    </TabsBarItem>

    <TabsBarItem id="flutter">
      실룩 거리다
    </TabsBarItem>

    <TabsBarItem id="react">
      네이티브 반응
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      자마린
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 통사론 [#syntax]

      ### 자바 [#java]

      ```java
      NewRelic.setMaxEventBufferTime(int $maxBufferTimeInSec)
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.setMaxEventBufferTime(maxBufferTimeInSec: Int)
      ```

      ## 설명 [#description]

      이벤트 수확 주기 길이를 설정합니다.

      이 메서드는 New Relic Android 에이전트가 다음 [수확 주기](/docs/apm/new-relic-apm/getting-started/glossary#harvest-cycle) 에서 New Relic으로 보낼 버퍼링된 이벤트를 포함하기 전에 메모리에 이벤트를 저장하는 최대 시간(초)을 설정합니다.

      * 기본값은 600초(10분)입니다.
      * 최소값은 60초 이상이어야 합니다.
      * 최대값은 600초를 초과할 수 없습니다.

      즉, 가장 오래된 이벤트 타임스탬프가 이 사용자 지정 구성 시간을 초과하면 에이전트가 New Relic 에이전트에서 수집기로의 각 연결 사이의 수집 주기에서 버퍼링된 콘텐츠를 전송합니다.

      수확 주기 동안 에이전트가 저장하는 최대 이벤트 수를 설정하는 [`setMaxEventPoolSize()`](/docs/mobile-monitoring/new-relic-mobile-android/android-sdk-api/setmaxeventpoolsize-android-sdk-api) 도 참조하세요.

      <Callout variant="important">
        많은 수의 이벤트를 보고하거나 이벤트를 너무 자주 보고하면 앱 성능에 영향을 줄 수 있습니다.
      </Callout>

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$maxBufferTimeInSec`
            </td>

            <td>
              `int`
            </td>

            <td>
              필수의. 에이전트가 이벤트를 메모리에 저장해야 하는 최대 시간(초)입니다. 기본값 수집 주기 길이는 600초입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      다음은 최대 이벤트 버퍼 시간을 300초로 설정하는 예입니다.

      ### 자바 [#java]

      ```java
      boolean NewRelic.setMaxEventBufferTime(300);
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.setMaxEventBufferTime(300)
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## 통사론 [#syntax]

      ### 목표-c [#objc]

      ```objectivec
      + (void) setMaxEventBufferTime:(unsigned int)seconds;
      ```

      ### 빠른 [#swift]

      ```swift
      NewRelic.setMaxEventBufferTime:(unsigned int)seconds;
      ```

      ## 설명 [#description]

      이벤트 수확 주기 길이를 설정합니다. 기본값은 600초입니다.

      이 메서드는 New Relic iOS 에이전트가 다음 [수집 주기](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#harvest-cycle) 에서 New Relic으로 보낼 버퍼링된 이벤트를 포함하기 전에 메모리에 이벤트를 저장하는 최대 시간(초)을 설정합니다. 즉, 가장 오래된 이벤트 타임스탬프가 이 사용자 지정 구성 시간을 초과하면 에이전트가 New Relic 에이전트에서 수집기로의 각 연결 사이의 수집 주기에서 버퍼링된 콘텐츠를 전송합니다. `Agent.start()` 이 호출된 후 이 값을 설정해야 합니다.

      * 기본값은 600초(10분)입니다.
      * 최소값은 60초 이상이어야 합니다.

      이벤트 풀의 최대 크기를 변경할 수 있는 [`setMaxEventPoolSize()`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-event-pool-size/) 도 참조하세요.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `seconds`
            </td>

            <td>
              `unsigned int`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예 [#examples]

      ### 오브젝티브-C [#obj-c]

      ```objectivec
      BOOL fiveMinuteLimitSet = [NewRelic setMaxEventBufferTime:600];
      ```

      ### 빠른 [#swift]

      ```swift
      let fiveMinuteLimitSet = NewRelic.setMaxEventBufferTime(600)
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## 통사론 [#syntax]

      ```typescript
      setMaxEventBufferTime(options: { maxBufferTimeInSeconds: number; }) => void
      ```

      ## 설명 [#description]

      이벤트 수집 주기 길이를 설정합니다. 기본값은 600초(10분)입니다. 최소값은 60초보다 작을 수 없습니다. 최대값은 600초를 초과할 수 없습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `options`
            </td>

            <td>
              `{ maxBufferTimeInSeconds: number; }`
            </td>

            <td>
              필수의. 최대 버퍼 시간(초)을 포함하는 개체입니다.
            </td>
          </tr>

          <tr>
            <td>
              `maxBufferTimeInSeconds`
            </td>

            <td>
              `number`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```typescript
          NewRelicCapacitorPlugin.setMaxEventBufferTime({ maxBufferTimeInSeconds: 60 });
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## 통사론 [#syntax]

      ```typescript
      setMaxEventBufferTime(maxBufferTimeInSeconds: number): void;

      ```

      ## 설명 [#description]

      이벤트 수집 주기 길이를 설정합니다. 기본값은 600초(10분)입니다. 최소값은 60초보다 작을 수 없습니다. 최대값은 600초를 초과할 수 없습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxBufferTimeInSeconds`
            </td>

            <td>
              `number`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```js
      NewRelic.setMaxEventBufferTime(60);
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## 통사론 [#syntax]

      ```csharp
      SetMaxEventBufferTime(int maxBufferTimeInSec) void;
      ```

      ## 설명 [#description]

      이벤트 수집 주기 길이를 설정합니다. 기본값은 600초(10분)입니다. 최소값은 60초보다 작을 수 없습니다. 최대값은 600초를 초과할 수 없습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxBufferTimeInSec`
            </td>

            <td>
              `int`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```C#
          CrossNewRelic.Current.SetMaxEventBufferTime(200);
      ```
    </TabsPageItem>

    <TabsPageItem id="flutter">
      ## 통사론 [#syntax]

      ```typescript
      setMaxEventBufferTime(int maxBufferTimeInSec) void;

      ```

      ## 설명 [#description]

      이벤트 수집 주기 길이를 설정합니다. 기본값은 600초(10분)입니다. 최소값은 60초보다 작을 수 없습니다. 최대값은 600초를 초과할 수 없습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxBufferTimeInSec`
            </td>

            <td>
              `int`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```dart
      NewrelicMobile.instance.setMaxEventBufferTime(200);
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## 통사론 [#syntax]

      ```js
      setMaxEventBufferTime(maxBufferTimeInSeconds: number): void;

      ```

      ## 설명 [#description]

      이벤트 수집 주기 길이를 설정합니다. 기본값은 600초(10분)입니다. 최소값은 60초보다 작을 수 없습니다. 최대값은 600초를 초과할 수 없습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxBufferTimeInSeconds`
            </td>

            <td>
              `number`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```js
         NewRelic.setMaxEventBufferTime(60);
      ```
    </TabsPageItem>

    <TabsPageItem id="xamarin">
      ## 통사론 [#syntax]

      ```csharp
      SetMaxEventBufferTime(int maxBufferTimeInSec) void;
      ```

      ## 설명 [#description]

      이벤트 수집 주기 길이를 설정합니다. 기본값은 600초(10분)입니다. 최소값은 60초보다 작을 수 없습니다. 최대값은 600초를 초과할 수 없습니다.

      ## 매개변수 [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              매개변수
            </th>

            <th width={200}>
              유형
            </th>

            <th>
              설명
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxBufferTimeInSec`
            </td>

            <td>
              `int`
            </td>

            <td>
              필수의. 이벤트가 New Relic으로 전송되기 전에 버퍼링될 수 있는 최대 시간(초)입니다.
            </td>
          </tr>
        </tbody>
      </table>

      ## 예시 [#example]

      ```csharp
          CrossNewRelicClient.Current.SetMaxEventBufferTime(200);
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>