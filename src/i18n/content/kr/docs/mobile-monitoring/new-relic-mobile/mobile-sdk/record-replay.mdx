---
title: 녹화 재생
type: apiDoc
shortDescription: 레코드 세션 리플레이.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Record session replay
freshnessValidatedDate: never
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="react">
      네이티브 반응
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## 통사론 [#syntax]

      ### 자바 [#java]

      ```java
      NewRelic.recordReplay()
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      NewRelic.recordReplay()
      ```

      ## 요구 사항

      * 안드로이드 에이전트 v7.7.0 이상.

      ## 설명

      `NewRelic.recordReplay()` 재생이 녹음을 시작하도록 수동으로 호출할 수 있습니다. 다음 요구 사항을 충족해야 합니다.

      * 계정은 리플레이를 녹화할 수 있는 권한이 있습니다

      재생이 이미 진행 중이거나 위의 기준이 모두 충족되지 않는 경우 이 API 메서드는 효과가 없습니다.

      재생 일시 중지 및 재개에 대한 자세한 내용은 [`NewRelic.pauseReplay()`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/pause-replay)참조하십시오.

      ## 사용 사례

      * 샘플링 설정에 관계없이 특정 사이트 페이지 또는 특정 조건에서 녹화하기 위해 수동으로 재생을 트리거하려고 합니다.

      ## 예시 [#example]

      다음은 재생 중에 강제로 녹화를 시작하는 예시입니다.

      ### 자바 [#java]

      ```java
      public class MainActivity extends Activity {
        ...
        @Override
        public boolean onOptionsItemSelected(MenuItem item) {
          switch (item.getItemId()) {
            case R.id.record_replay:
              NewRelic.recordReplay();
            ...
              return true;
            default:
              return super.onOptionsItemSelected(item);
          }
        }
        ...
      }
      ```

      ### 코틀린 [#kotlin]

      ```kotlin
      class MainActivity : AppCompatActivity() {

        ...
        var client: OkHttpClient = OkHttpClient();

            binding.fab.setOnClickListener { view ->

                NewRelic.recordReplay()
                }
            }
          ...
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## 통사론 [#syntax]

      ### 목표-c [#objc]

      ### 빠른 [#swift]
    </TabsPageItem>

    <TabsPageItem id="react">
      ## 통사론 [#syntax]

      ```
      NewRelic.recordReplay()
      ```

      ## 요구 사항

      * 리액트가 v1.7.0 이상으로 나타났습니다.

      ## 설명

      `NewRelic.recordReplay()` 재생이 녹음을 시작하도록 수동으로 호출할 수 있습니다. 다음 요구 사항을 충족해야 합니다.

      * 계정은 리플레이를 녹화할 수 있는 권한이 있습니다

      재생이 이미 진행 중이거나 위의 기준이 모두 충족되지 않는 경우 이 API 메서드는 효과가 없습니다.

      재생 일시 중지 및 재개에 대한 자세한 내용은 [`NewRelic.pauseReplay()`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/pause-replay)참조하십시오.

      ## 사용 사례

      * 샘플링 설정에 관계없이 특정 사이트 페이지 또는 특정 조건에서 녹화하기 위해 수동으로 재생을 트리거하려고 합니다.

      ## 예시 [#example]

      다음은 재생 중에 강제로 녹화를 시작하는 예시입니다.

      ```
      NewRelic.recordReplay();
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>