---
title: endInteraction(안드로이드 SDK API)
type: apiDoc
shortDescription: 사용자 지정 상호 작용을 종료합니다.
tags:
  - Mobile monitoring
  - New Relic Mobile Android
  - Android SDK API
metaDescription: 'New Relic API for Android mobile app monitoring: end a custom interaction.'
translationType: machine
---

## 통사론

```
NewRelic.endInteraction(string $interactionID)
```

사용자 지정 상호 작용을 종료합니다.

## 요구 사항

모든 에이전트 버전과 호환됩니다.

## 설명

New Relic은 모든 상호작용을 자동으로 종료하지만 `endInteraction()` 를 사용하여 사용자 지정 상호작용을 조기에 종료할 수 있습니다. `startInteraction()` 호출을 사용할 때 문자열 ID가 반환됩니다.

상호 작용이 이미 종료된 경우 이 호출은 효과가 없습니다.

이 API 사용에 대한 일반적인 정보는 [Android SDK API 사용 가이드](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/work-android-sdk-api) 를 참조하세요.

## 매개변수

<table>
  <thead>
    <tr>
      <th width="25%">
        매개변수
      </th>

      <th>
        설명
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$interactionID`

        _끈_
      </td>

      <td>
        필수의. 종료하려는 상호작용의 문자열 ID입니다. 이 문자열은 `startInteraction()` 을 사용할 때 반환됩니다.
      </td>
    </tr>
  </tbody>
</table>

## 예

### 상호작용 종료

다음은 맞춤 상호작용 `RefreshContacts` 을 종료하는 예입니다.

```
public class MainActivity extends Activity {
  ...
  @Override
  public boolean onOptionsItemSelected(MenuItem item) {
    switch (item.getItemId()) {
      case R.id.action_refresh:
        String interactionId = NewRelic.startInteraction("RefreshContacts");
        ... 
        return true;
      default:
        NewRelic.endInteraction(interactionId);
        return super.onOptionsItemSelected(item);
    }
  }
  ...
}
```