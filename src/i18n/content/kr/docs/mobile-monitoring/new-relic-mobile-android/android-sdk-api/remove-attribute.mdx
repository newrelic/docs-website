---
title: removeAttribute(안드로이드 SDK API)
type: apiDoc
shortDescription: 속성을 제거합니다.
tags:
  - Mobile monitoring
  - New Relic Mobile Android
  - Android SDK API
metaDescription: 'New Relic API for Android mobile app monitoring: remove a custom attribute.'
translationType: machine
---

## 통사론

```
NewRelic.removeAttribute(string $name)
```

속성을 제거합니다.

## 요구 사항

에이전트 버전 5.0.0 이상.

## 설명

이 메서드는 이름 문자열로 지정된 속성을 제거합니다.

이 API를 사용하는 방법에 대한 컨텍스트는 [사용자 지정 속성 및 이벤트 보내기](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/work-android-sdk-api#attributes-events-insights) 를 참조하세요.

## 매개변수

<table>
  <thead>
    <tr>
      <th width="25%">
        매개변수
      </th>

      <th>
        설명
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$name`

        _끈_
      </td>

      <td>
        필수의. 제거하려는 속성의 이름입니다.
      </td>
    </tr>
  </tbody>
</table>

## 반환 값

성공하면 `true` , 실패하면 `false` 를 반환합니다.

## 예

### 속성 제거

이름이 `rate` 인 속성을 제거합니다.

```
boolean attributeRemoved = NewRelic.removeAttribute("rate");
```