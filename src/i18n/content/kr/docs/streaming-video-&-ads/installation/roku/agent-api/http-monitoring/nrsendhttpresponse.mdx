---
title: nrSendHttpResponse
type: apiDoc
shortDescription: ConnectedDeviceSystem 유형의 HTTP_RESPONSE 이벤트를 보냅니다.
tags: null
metaDescription: Send an HTTP_RESPONSE event of type ConnectedDeviceSystem.
freshnessValidatedDate: never
translationType: machine
---

## 통사론

```js
  nrSendHttpResponse(nr as Object, _url as String, msg as Object) as Void
```

## 설명

ConnectedDeviceSystem 유형의 HTTP\_RESPONSE 이벤트를 보냅니다.

## 인수

<table>
  <thead>
    <tr>
      <th width="25%">
        인수
      </th>

      <th>
        설명
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `nr`
      </td>

      <td>
        뉴렐릭 에이전트 개체.
      </td>
    </tr>

    <tr>
      <td>
        `_url`
      </td>

      <td>
        요청 URL.
      </td>
    </tr>

    <tr>
      <td>
        `msg`
      </td>

      <td>
        roUrlEvent 유형의 메시지.
      </td>
    </tr>
  </tbody>
</table>

## 반품

아무것도 아님.

## 예시

```js
  	msg = wait(5000, m.port)
	  if type(msg) = "roUrlEvent" then
		        nrSendHttpResponse(m.nr, _url, msg)
	  end if
```