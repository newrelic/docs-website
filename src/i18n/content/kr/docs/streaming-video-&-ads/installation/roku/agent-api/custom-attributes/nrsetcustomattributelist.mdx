---
title: nrSetCustomAttributeList
type: apiDoc
shortDescription: 이벤트에 포함될 사용자 정의 속성 목록을 설정합니다.
tags: null
metaDescription: Set a custom attribute list to be included in the events.
freshnessValidatedDate: never
translationType: machine
---

## 통사론

```js
  nrSetCustomAttributeList(nr as Object, attr as Object, actionName = "" as String) as Void
```

## 설명

이벤트에 포함될 사용자 정의 속성 목록을 설정합니다.

## 인수

<table>
  <thead>
    <tr>
      <th width="25%">
        인수
      </th>

      <th>
        설명
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `nr`
      </td>

      <td>
        뉴렐릭 에이전트 개체.
      </td>
    </tr>

    <tr>
      <td>
        `attr`
      </td>

      <td>
        속성 목록을 연관 항목으로 포함합니다.
      </td>
    </tr>

    <tr>
      <td>
        `actionName`
      </td>

      <td>
        (선택 사항) 속성이 포함될 작업입니다. 모든 작업을 기본값으로 설정합니다.
      </td>
    </tr>
  </tbody>
</table>

## 반품

아무것도 아님.

## 예시

```js
  	attr = {"key0":"val0", "key1":"val1"}
	  nrSetCustomAttributeList(m.nr, attr, "CONTENT_HEARTBEAT")
```