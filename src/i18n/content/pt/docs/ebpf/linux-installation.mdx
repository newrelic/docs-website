---
title: Instale o agente New Relic eBPF para hosts Linux.
metaDescription: Learn how to install and configure the New Relic eBPF agent for your Linux host with Debian package installation.
tags:
  - New Relic integrations with eBPF
  - New Relic eBPF agent
  - eBPF integration
  - eAPM
  - eBPF APM
  - Linux host
  - New Relic eBPF agent for Linux
freshnessValidatedDate: never
translationType: machine
---

Você pode instalar o agente New Relic eBPF no seu host Linux para monitorar a integridade de todo o sistema. O agente eBPF fornece visibilidade profunda do desempenho do aplicativo sem exigir alterações de código ou implantar agente específico de linguagem.

## Instale o agente New Relic eBPF. [#install-linux]

<Steps>
  <Step>
    ### Antes de você começar [#requirements]

    Você deve atender aos pré-requisitos descritos na documentação [de compatibilidade e requisitos do eBPF](/docs/ebpf/requirements#linux) para hosts Linux.
  </Step>

  <Step>
    ### Instalar o agente eBPF [#install]

    Para instalar o agente eBPF:

    1. Login em sua conta New Relic .

    2. Vá para <DNT>**[left navigation pane &gt; + Integration &amp; Agents &gt; eBPF Agent](https://onenr.io/0oR8XWW36RG)**</DNT>.

    3. Na tela <DNT>Select an account</DNT>, selecione a conta na qual deseja instalar o agente eBPF e clique em <DNT>**Continue**</DNT>.

    4. Na página <DNT>Select an installation method</DNT>, selecione <DNT>**On a host**</DNT> e clique em <DNT>**Continue**</DNT>.

    5. Na tela <DNT>Enter your user key</DNT>, selecione uma das seguintes opções e, em seguida, clique em <DNT>**Continue**</DNT>:

       * <DNT>**Use an existing key**</DNT>: Se você já possui uma chave de usuário, forneça a chave de usuário. Para mais informações, consulte [Chaves de usuário](/docs/apis/intro-apis/new-relic-api-keys/#user-key).
       * <DNT>**Create a new key**</DNT>: Se você não tiver uma chave de usuário, clique em <DNT>**Create a new key**</DNT> para criar uma.

    6. Na tela <DNT>Configure the host integration</DNT>, insira o nome da implantação para o host e, em seguida, clique em <DNT>**Continue**</DNT>.

    7. Na tela <DNT>Install the agent</DNT>, execute o comando fornecido no seu terminal para instalar o agente eBPF via pacote Debian.

       1. Copie e cole o comando exibido para instalar o agente eBPF em seu host Debian.

       2. Para verificar a instalação, execute o seguinte comando:

          ```bash
          sudo systemctl status newrelic-ebpf-agent
          ```

       Após a instalação bem-sucedida, o status desses serviços será exibido como ativo (em execução).

       <Callout variant="tip">
         Você pode personalizar a configuração do agente eBPF editando o arquivo <DNT>`newrelic-ebpf-agent.yaml`</DNT> disponível em <DNT>`/etc/newrelic-ebpf-agent/newrelic-ebpf-agent.yaml`</DNT>. Para obter mais informações sobre o parâmetro de configuração, consulte [parâmetro de configuração](#config-params).
       </Callout>
  </Step>

  <Step>
    ### Acesse os dados do eBPF no New Relic [#access-data]

    Depois que o agente eBPF estiver instalado, ele começará automaticamente a coletar dados do seu host Linux. Você pode acessar esses dados na interface de usuário OpenTelemetry do New Relic. Para obter mais informações sobre a interface OpenTelemetry New Relic, consulte [Interface OpenTelemetry APM ](/docs/opentelemetry/get-started/apm-monitoring/opentelemetry-apm-ui).

    **Para visualizar os dados do eBPF no New Relic:**

    1. Vá para <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; APM &amp; Services**</DNT>.
    2. No banner de pesquisa, defina os critérios de pesquisa como <DNT>`instrumentation.name = nr_ebpf`</DNT>:
       <img style={{ align: 'left' }} title="eBPF filters" alt="eBPF filter for eBPF data in New Relic OpenTelemetry UI" src="/images/ebpf_filters.webp" />
  </Step>
</Steps>

## Atualize o agente eBPF [#upgrade]

Para atualizar o agente eBPF em um host Linux, execute novamente o comando de instalação na interface do usuário do New Relic. Este processo instalará a nova versão do agente e removerá automaticamente a versão anterior.

<Callout variant="important">
  A atualização através de um gerenciador de pacotes não é suportada. Para uma atualização bem-sucedida, você deve usar o comando de instalação da interface do usuário do New Relic.
</Callout>

Para obter o comando de instalação mais recente:

1. Vá para <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; + Integration &amp; Agents &gt; eBPF Agent**</DNT>.

2. Siga o fluxo de instalação para obter o comando atualizado.

3. Execute o comando no seu host para atualizar o agente.

4. Reinicie o serviço do agente ebpf usando o comando abaixo:

   ```bash
   sudo systemctl restart newrelic-ebpf-agent
   ```

## Parâmetro de configuração [#config-params]

O arquivo `newrelic-ebpf-agent.yaml` contém o seguinte parâmetro de configuração:

<Callout variant="tip">
  O agente eBPF gera automaticamente nomes de entidades de forma diferente dependendo do ambiente:

  * Em hosts ou Docker, esses nomes são uma combinação do nome do processo, seu diretório ou ID do contêiner e a porta de escuta. Por exemplo, `ruby:/home/ubuntu/app:[5678]` ou `java:f4aead533895:[8080]`.

  * No Kubernetes, esses nomes são derivados do nome do serviço, por exemplo, `mysql-database-service`.

  Atribuindo nome personalizado ao aplicativo:

  * Você pode atribuir um nome personalizado à sua aplicação definindo a variável de ambiente `NEW_RELIC_APP_NAME` tanto para Kubernetes quanto para aplicações on-host.
</Callout>

<CollapserGroup>
  <Collapser id="general-configuration" title="Configuração geral">
    Esses parâmetros controlam a identidade do núcleo e o destino dos dados para o agente eBPF.

    <table>
      <thead>
        <tr>
          <th>
            Nome do parâmetro
          </th>

          <th>
            Descrição
          </th>

          <th>
            Tipo de dados
          </th>

          <th>
            Valor de exemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `licenseKey`
          </td>

          <td>
            Especifica sua chave de licença do New Relic, necessária para enviar dados.
          </td>

          <td>
            Corda
          </td>

          <td>
            `"22387565c...FFFFNRAL"`
          </td>
        </tr>

        <tr>
          <td>
            `deploymentName`
          </td>

          <td>
            Especifica um nome exclusivo para sua implantação para ajudar a identificar seus dados no New Relic.
          </td>

          <td>
            Corda
          </td>

          <td>
            `"my-deployment"`
          </td>
        </tr>

        <tr>
          <td>
            `region`
          </td>

          <td>
            Se usar uma customSecretLicenseKey, você deve fornecer sua região 

            `"US"`

            /

            `"EU"`

            . Caso contrário, deixe este valor como uma string vazia.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `customOtlpEndpoint`
          </td>

          <td>
            URL do endpoint OTLP personalizado. Quando definido, isso tem precedência sobre os endpoints estáticos baseados em região.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `customOtlpEndpointTlsEnabled`
          </td>

          <td>
            Se o TLS está habilitado no endpoint OTLP personalizado. Aplicável somente quando 

            `customOtlpEndpoint`

             está definido.
          </td>

          <td>
            Boleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `customOtlpEndpointTlsCertPath`
          </td>

          <td>
            Caminho para certificados CA personalizados para validação TLS do endpoint OTLP. Se vazio, os certificados CA padrão do sistema são usados.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `logLevel`
          </td>

          <td>
            Define o nível de log do agente. As opções são 

            `OFF`

            , 

            `FATAL`

            , 

            `ERROR`

            , 

            `WARNING`

            , 

            `INFO`

            , 

            `DEBUG`

             em ordem crescente de verbosidade.
          </td>

          <td>
            Corda
          </td>

          <td>
            `"INFO"`
          </td>
        </tr>

        <tr>
          <td>
            `logFilePath`
          </td>

          <td>
            Especifica o caminho do arquivo para logs do agente. Se estiver vazio ou o caminho for inválido, os logs serão enviados para a saída padrão (stdout).
          </td>

          <td>
            Corda
          </td>

          <td>
            `"/var/log/newrelic-ebpf-agent"`
          </td>
        </tr>

        <tr>
          <td>
            `reportApmData`
          </td>

          <td>
            Controla o relatório de dados de APM. Valores aceitos: 

            `"true"`

             (sempre enviar), 

            `"false"`

             (nunca enviar), 

            `"auto"`

             (enviar apenas quando nem o agente APM nem o OTel estiver anexado).
          </td>

          <td>
            Corda
          </td>

          <td>
            `"auto"`
          </td>
        </tr>

        <tr>
          <td>
            `reportNetworkMetrics`
          </td>

          <td>
            Controla o relatório de métricas de rede. Quando habilitado, o agente coleta e relata métricas de rede, incluindo estatísticas TCP. Valores aceitos: 

            `"true"`

             (sempre enviar), 

            `"false"`

             (nunca enviar), 

            `"auto"`

            .
          </td>

          <td>
            Corda
          </td>

          <td>
            `"auto"`
          </td>
        </tr>

        <tr>
          <td>
            `includePortInServerEntityIdentification`
          </td>

          <td>
            A configuração permite incluir ou excluir portas dos nomes de entidades descobertos automaticamente. Normalmente, os nomes aparecem como 

            `processName:cwd/container_name:[port]`

            , por exemplo, 

            `python:frosty_merkle:[80]`

            . Para excluir detalhes da porta, defina esta opção como 

            `false`

             para nomes como 

            `python:frosty_merkle`

            .
          </td>

          <td>
            Boleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `entityLabels`
          </td>

          <td>
            Rótulos personalizados a serem adicionados a todas as entidades reportadas pelo agente eBPF. Esses rótulos aparecem como atributos 

            `tags.*`

             no New Relic (por exemplo, 

            `tags.environment`

            , 

            `tags.datacenter`

            ).
          </td>

          <td>
            Mapa
          </td>

          <td>
            `{datacenter: "us-east-1", environment: "production"}`
          </td>
        </tr>

        <tr>
          <td>
            `downloadedPackagedHeadersPath`
          </td>

          <td>
            Define o caminho absoluto do diretório completo onde os cabeçalhos Linux necessários são baixados manualmente e colocados para uso pelo agente eBPF. Isso é útil em ambientes restritos onde o agente não consegue baixar os cabeçalhos Linux necessários. Os cabeçalhos necessários são identificados pelo agente com base na versão do kernel. Use somente após recomendação de suporte do NR.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `distroKernelHeadersPath`
          </td>

          <td>
            Define o caminho absoluto do diretório completo onde os cabeçalhos do Linux estão presentes para o agente eBPF usar. Isso é útil quando os cabeçalhos Linux necessários não puderam ser instalados ou o caminho não pôde ser determinado. Use somente após recomendação de suporte do NR.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="all-data-filters" title="Todos os filtros de dados">
    Configure filtros para descartar todos os tipos de dados (métricas de rede e dados APM) com base na configuração fornecida.

    <table>
      <thead>
        <tr>
          <th>
            Nome do parâmetro
          </th>

          <th>
            Descrição
          </th>

          <th>
            Tipo de dados
          </th>

          <th>
            Valor de exemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `allDataFilters.dropServiceNameRegex`
          </td>

          <td>
            Padrão de expressão regular para corresponder nomes de entidades para descartar TODOS os dados (métricas de rede + dados APM). Por exemplo: 

            `"java-app|otel-collector|\\bservice1\\b"`

            . Use dropServiceNameRegex ou keepServiceNameRegex para filtrar nomes de serviços, não ambos. Se ambos forem fornecidos, keepServiceNameRegex tem precedência.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `allDataFilters.keepServiceNameRegex`
          </td>

          <td>
            Padrão de expressão regular para corresponder nomes de entidades para sempre manter TODOS os dados. Isso ignora todos os filtros de descarte. Por exemplo: 

            `"critical-service|important-app|\\bmust-monitor\\b"`

            . Use dropServiceNameRegex ou keepServiceNameRegex para filtrar nomes de serviços, não ambos. Se ambos forem fornecidos, keepServiceNameRegex tem precedência.
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `allDataFilters.dropApmAgentEnabledEntity`
          </td>

          <td>
            Eliminar todos os dados de aplicações/entidades que tenham agentes APM NewRelic/OpenTelemetry em execução.
          </td>

          <td>
            Boleano
          </td>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="apm-data-filters" title="Filtros de dados APM">
    Configure filtros para descartar dados eBPF APM com base na configuração fornecida.

    <table>
      <thead>
        <tr>
          <th>
            Nome do parâmetro
          </th>

          <th>
            Descrição
          </th>

          <th>
            Tipo de dados
          </th>

          <th>
            Valor de exemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `apmDataFilters.dropEntityName`
          </td>

          <td>
            Lista de nomes de entidades para descartar dados eBPF APM. Por exemplo: 

            `["kube-dns", "otel-collector", "service1"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>

        <tr>
          <td>
            `apmDataFilters.keepEntityName`
          </td>

          <td>
            Lista de nomes de entidades para sempre manter os dados do eBPF APM. Por padrão, todas as entidades são mantidas/habilitadas. Isso ignora o filtro 

            `apmDataFilters.dropEntityName`

            . Por exemplo: 

            `["critical-service", "important-app"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>

        <tr>
          <td>
            `apmDataFilters.jvmMetricsReporting`
          </td>

          <td>
            Habilite o relatório de métricas da JVM. Quando habilitado, o agente coleta e reporta métricas da JVM para aplicações Java.
          </td>

          <td>
            Boleano
          </td>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="network-metrics-data-filters" title="Filtros de dados de métricas de rede">
    Configure filtros para descartar/manter dados de métricas de rede com base na configuração fornecida.

    <table>
      <thead>
        <tr>
          <th>
            Nome do parâmetro
          </th>

          <th>
            Descrição
          </th>

          <th>
            Tipo de dados
          </th>

          <th>
            Valor de exemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `networkMetricsDataFilter.dropEntityName`
          </td>

          <td>
            Lista de nomes de entidades para descartar dados de métricas de rede. Por exemplo: 

            `["kube-dns", "otel-collector", "service1"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>

        <tr>
          <td>
            `networkMetricsDataFilter.keepEntityName`
          </td>

          <td>
            Lista de nomes de entidades para sempre manter os dados de métricas de rede. Por padrão, todas as entidades são mantidas/habilitadas. Isso ignora o filtro 

            `networkMetricsDataFilter.dropEntityName`

            . Por exemplo: 

            `["critical-service", "important-app"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="protocol-tracing" title="Configuração de rastreamento de protocolo">
    Esta seção permite que você habilite o monitoramento para protocolos de rede específicos e configure como os dados trace (spans) são coletados. Você pode habilitar ou desabilitar o monitoramento para protocolos como HTTP, MySQL e outros, e definir parâmetros para coleta de span com base em latência ou taxa de erros. Os seguintes protocolos são suportados:

    * HTTP
    * Thrift
    * MySQL
    * PostgreSQL
    * MongoDB
    * Apache Cassandra
    * Redis
    * DynamoDB
    * MSSQL
    * Kafka
    * AMQP
    * DNS

    <table>
      <thead>
        <tr>
          <th>
            Nome do parâmetro
          </th>

          <th>
            Descrição
          </th>

          <th>
            Tipo de dados
          </th>

          <th>
            Valor de exemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `protocols.global.max_unlinked_spans`
          </td>

          <td>
            Controla o número máximo de spans não vinculados relatados por protocolo. Defina como 0 para desabilitar o limite. Spans não vinculados são spans que o agente eBPF captura, mas não consegue associar ao seu pai.
          </td>

          <td>
            Inteiro
          </td>

          <td>
            `100`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.global.unlinked_spans_error_quota_percentage`
          </td>

          <td>
            Reserva uma porcentagem configurável da cota de spans não vinculados para spans de erro, garantindo a visibilidade de erros mesmo quando spans normais chegam primeiro.
          </td>

          <td>
            Inteiro
          </td>

          <td>
            `30`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.enabled`
          </td>

          <td>
            Se 

            `true`

            , habilita o monitoramento para o protocolo especificado (por exemplo, 

            `http`

            , 

            `mysql`

             e quaisquer outros).
          </td>

          <td>
            Boleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.spans.enabled`
          </td>

          <td>
            Se 

            `true`

            , exporta intervalos trace para o protocolo habilitado.
          </td>

          <td>
            Boleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.spans.samplingLatency`
          </td>

          <td>
            Define o limite de amostragem baseado em latência para exportação de intervalos. Opções válidas: 

            `p1`

            , 

            `p10`

            , 

            `p50`

            , 

            `p90`

            , 

            `p99`

            .
          </td>

          <td>
            Corda
          </td>

          <td>
            `"p50"`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.spans.samplingErrorRate`
          </td>

          <td>
            Somente para HTTP e Thrift. Exporta spans de qualquer rota onde a taxa de erro excede a porcentagem especificada (1-100).
          </td>

          <td>
            Corda
          </td>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>
</CollapserGroup>

## Desinstale o agente eBPF. [#uninstall]

Para desinstalar o agente eBPF de um host Linux, utilize as ferramentas de gerenciamento de pacotes do seu sistema:

```bash
sudo apt-get purge newrelic-ebpf-agent
```

<Callout variant="tip">
  Não é necessário interromper o serviço antes de executar o comando de desinstalação.
</Callout>

<DocTiles>
  <DocTile title="Instalação eBPF Kubernetes" path="/docs/ebpf/k8s-installation/">
    Aprenda como configurar o agente New Relic eBPF para o seu cluster Kubernetes.
  </DocTile>

  <DocTile title="resolução de problemas eBPF" path="/docs/ebpf/troubleshooting/no-ui-data/">
    Aprenda a solucionar problemas com o agente eBPF da New Relic.
  </DocTile>

  <DocTile title="eBPF práticas recomenda" path="/docs/ebpf/best-practices/">
    Conheça as práticas recomendadas para uso do agente New Relic eBPF.
  </DocTile>
</DocTiles>