---
subject: Ruby agent
releaseDate: '2026-06-25'
version: 10.6.0
downloadLink: 'https://rubygems.org/gems/newrelic_rpm/versions/10.6.0'
features:
  - SpanLink events are now supported for the Hybrid Agent
  - SpanEvent events are now supported for the Hybrid Agent
  - Set Span Kind on all Hybrid Agent spans
  - 'Add support for OpenTelemetry::Tracer#start_root_span'
bugs:
  - 'Fix `instrumentation.rails_event_logger: false` not disabling the instrumentation'
  - Normalize boolean-like values to `disabled` for instrumentation config keys
  - Per-library logging supportability metrics now reflect each library's instrumentation state
security: []
translationType: machine
---

<Callout variant="important">
  Recomendamos atualizar para a versão mais recente do agente assim que estiver disponível. Se você não puder atualizar para a versão mais recente, atualize seu agente para uma versão com no máximo 90 dias. Saiba mais sobre [como manter o agente atualizado](/docs/new-relic-solutions/new-relic-one/install-configure/update-new-relic-agent/).

  Consulte a [política de fim de vida útil](/docs/apm/agents/ruby-agent/getting-started/ruby-agent-eol-policy/) do agente Ruby New Relic para obter informações sobre lançamentos de agentes e datas de suporte.
</Callout>

## v10.6.0

* **Recurso: eventos SpanLink agora são suportados para o agente Hybrid**

  Spans criados por uma API do OpenTelemetry agora podem ter [Span Links](https://opentelemetry.io/docs/concepts/signals/traces/#span-links) associados a eles. Links podem ser adicionados no início de um span, passando-os para o argumento `links`, ou chamando a API `OpenTelemetry::Trace::Span#add_link`. [PR#3586](https://github.com/newrelic/newrelic-ruby-agent/pull/3586)

* **Recurso: eventos SpanEvent agora são suportados para o agente Hybrid**

  Os spans criados por uma API do OpenTelemetry agora podem ter [eventos SpanEvent](https://opentelemetry.io/docs/concepts/signals/traces/#span-events) associados a eles por meio da API `OpenTelemetry::Trace::Span#add_event`. Os eventos SpanEvent capturam anotações com timestamp em um span e são enviados para o New Relic junto com o span pai. [PR#3587](https://github.com/newrelic/newrelic-ruby-agent/pull/3587)

* **Recurso: definir o tipo de span em todos os spans do agente Hybrid**

  Anteriormente, apenas os spans do OpenTelemetry traduzidos em segmentos de solicitação externa ou segmentos de armazenamento de dados adicionavam o tipo de span como um atributo. Agora, o agente adiciona o tipo de span a todos os spans do OpenTelemetry onde o valor está disponível. [PR#3589](https://github.com/newrelic/newrelic-ruby-agent/pull/3589)

* **Recurso: adicionar suporte para OpenTelemetry::Tracer#start\_root\_span**

  A API `OpenTelemetry::Tracer#start_root_span` agora pode ser usada para forçar o início de uma transação para um determinado span, desde que ele tenha um tipo de span `:server` ou `:consumer`. Para quaisquer outros tipos de span, ela não executará nenhuma ação. Esse método é mais comumente usado na instrumentação de jobs em segundo plano. [PR#3588](https://github.com/newrelic/newrelic-ruby-agent/pull/3588)

* **Correção de bug: corrigir `instrumentation.rails_event_logger: false` não desativando a instrumentação**

  Anteriormente, definir `instrumentation.rails_event_logger` como `false` não desativava a instrumentação de `Rails.event` como esperado; ela ainda era instalada durante a inicialização do Rails. O problema já foi resolvido. [PR#3564](https://github.com/newrelic/newrelic-ruby-agent/pull/3564)

* **Correção de bug: normalizar valores semelhantes a booleanos para `disabled` em chaves de configuração de instrumentação**

  Anteriormente, apenas `disabled` desativaria uma chave de configuração `instrumentation.*`. Agora, valores do tipo booleano, como `false`, `no` ou `off`, também são resolvidos como `disabled` e impedem que a instrumentação seja instalada. [PR#3579](https://github.com/newrelic/newrelic-ruby-agent/pull/3579)

* **Correção de bug: métricas de suportabilidade de logging por biblioteca agora refletem o estado de instrumentação de cada biblioteca**

  Anteriormente, as métricas `Supportability/Logging/Ruby/{library}/{enabled|disabled}` relatavam o valor da configuração global `application_logging.enabled` para cada biblioteca, em vez do estado real de cada biblioteca. Como resultado, a métrica relatava `enabled` mesmo quando você havia desativado a instrumentação de log para uma biblioteca específica ou não estava usando a gem dessa biblioteca. Agora, a métrica de cada biblioteca reflete se sua própria instrumentação de log está ativada. [PR#3571](https://github.com/newrelic/newrelic-ruby-agent/pull/3571)