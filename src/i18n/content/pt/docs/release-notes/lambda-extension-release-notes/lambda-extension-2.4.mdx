---
subject: Lambda-Extension
releaseDate: '2025-12-17'
version: 2.4.1
translationType: machine
---

## Notas

* Removido log de rastreamento desnecessário para logs `Function` e `Extension` ignorados.

* Lógica de parsing atualizada para verificar os primeiros 50 caracteres em vez de uma fatia de 100 bytes. Garantindo o manuseio seguro de `UTF-8 characters` multibyte (como emojis) sem erros de truncamento.

## Fixo

* Resolvido um problema em que as requisições ocasionalmente não eram instrumentadas no modo APM.

* Fallback automático de `NEW_RELIC_LICENSE_KEY` habilitado.