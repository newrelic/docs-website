---
subject: Lambda-Extension
releaseDate: '2025-04-08'
version: 2.3.18
translationType: machine
---

## Notas

* Adicionado exemplo para New Relic AI Monitoring, juntamente com instrumentação da função do Lambda com streaming de resposta

* Exemplos `SAM` atualizados para incluir as camadas e pacotes mais recentes

* Exemplo de instrumentação Lambda atualizado `Ruby`

* Leia-me atualizado para incluir a variável de ambiente `NEW_RELIC_COLLECT_TRACE_ID` que pode ser usada para adicionar `trace-id` aos logs do Lambda

## Problemas corrigidos

* Foi resolvido o problema em que a extensão capturava incorretamente as durações de tempo limite para erros de tempo limite da função do Lambda

* Tempo de espera atualizado para resolver problema intermitente com teste de unidade `TestClientSendRetry`