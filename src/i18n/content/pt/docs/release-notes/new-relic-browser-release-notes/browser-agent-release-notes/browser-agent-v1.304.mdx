---
subject: Browser agent
releaseDate: '2025-12-03'
version: 1.304.0
downloadLink: 'https://www.npmjs.com/package/@newrelic/browser-agent'
features:
  - Warn message on session reset
  - Reduce Session Replay snapshot sizes
bugs:
  - Observe document.body after document is interactive
security: []
translationType: machine
---

## v1.304.0

### Recurso

#### Mensagem de aviso ao redefinir a sessão

Adiciona um código de aviso quando ocorre uma reinicialização da sessão do agente, para que possamos depurar melhor o comportamento de transição do navegador.

#### Reduzir o tamanho dos snapshots Session Replay

Por padrão, use `slimDOMOption = 'all'` para reduzir o tamanho dos snapshots de reprodução de sessão.

### Correções de bugs

#### Observe o corpo do documento após o documento se tornar interativo.

Aborda o cenário em que `MutationObserver.observe()` foi emitido antes que `document.body` estivesse disponível, causando erro `Failed to execute 'observe' on 'MutationObserver': parameter 1 is not of type 'Node'.`.

## Declaração de apoio

New Relic recomenda que você atualize o agente regularmente para garantir que esteja obtendo os benefícios mais recentes de recursos e desempenho. Versões mais antigas não terão mais suporte quando chegarem [ao fim de sua vida útil](https://docs.newrelic.com/docs/browser/browser-monitoring/getting-started/browser-agent-eol-policy/). As datas de lançamento refletem a data de publicação original da versão do agente.

Novos lançamentos de agente do browser são disponibilizados aos clientes em pequenas etapas ao longo de um período de tempo. Por esse motivo, a data em que o lançamento fica acessível à sua conta pode não corresponder à data de publicação original. Consulte este [dashboard de status](https://newrelic.github.io/newrelic-browser-agent-release/) para obter mais informações.

Consistente com nossa [política de suporte ao navegador](https://docs.newrelic.com/docs/browser/new-relic-browser/getting-started/compatibility-requirements-browser-monitoring/#browser-types), a versão 1.304.0 do agente do Browser foi criada e testada para os seguintes navegadores e intervalos de versões: Chrome 132-142, Edge 132-142, Safari 17-26 e Firefox 134-144. Para dispositivos móveis, a versão 1.304.0 foi criada e testada para Android OS 16 e iOS Safari 17-26.