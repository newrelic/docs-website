---
subject: Pipeline Control Gateway
releaseDate: '2025-08-05'
version: 1.1.0
metaDescription: Release notes for Pipeline Control gateway 1.1.0
translationType: machine
---

### Notas de versão do Controle de Pipeline Gateway - v1.1.0

#### Suporte para trace SQL e trace de dados de transação

* Implementou uma nova funcionalidade para descartar seletivamente dados e atributos dos eventos `SqlTrace` e `TransactionTrace` usando regras de descarte.

#### Atualizações de segurança

* Foram corrigidas vulnerabilidades no pacote `golang.org/x/net`, incluindo um problema de script entre sites e um desvio de proxy HTTP relacionado a IDs de zona IPv6.

* Atualizado para Go `1.24` e as seguintes dependências foram atualizadas para aumentar a segurança e incorporar correções recentes:

  * `golang.org/x/net` para `v0.38.0`
  * `golang.org/x/sys` para `v0.31.0`
  * `golang.org/x/text` para `v0.23.0`

#### Suporte para novas funções

Adicionado suporte para diversas funções novas na consulta de regra de descarte NRQL. Agora você pode usar as seguintes funções:

* `aparse()`
* `floor()`
* `getField()`
* `hourOf()`
* `numeric()`
* `round()`
* `string()`
* `substring()`
* `weekdayOf()`
* `mod()`
* `dimensions()`