---
subject: iOS agent
releaseDate: '2026-07-02'
version: 7.7.3
downloadLink: 'https://download.newrelic.com/ios_agent/NewRelic_XCFramework_Agent_7.7.3.zip'
translationType: machine
---

### Novo recurso

Funcionalidade de armazenamento offline para Mobile Session Replay.

### Corrigido nesta versão

Substituído o LCG semeado por timestamp no IGuidGenerator por arc4random\_buf.

Adicionada a detecção da região da UE ao upload de Sourcemap do RN.

Bloqueada a recursão para o swizzle de interação padrão do estilo MAUI.

Renomeado appVersionId para appVersion no upload de source map do RN.

Corrigido o loop infinito no EventDeserializer que congelava o agente no lançamento.

Alterado o modo de mascaramento padrão do Session Replay para não permitir o desmascaramento usando APIs programáticas e corrigidas as APIs de substituição do UIKit.

Corrigido o crash que poderia ocorrer no NRMAWKWebViewNavigationDelegate webViewWebContentProcessDidTerminate.

Garantido o upload dos dados do Session Replay no próximo lançamento caso tenha falhado ao fazer o upload quando o aplicativo foi fechado.

Descartados os relatórios de crash permanentemente rejeitados (400/403).

Adicionada a remoção por TTL para armazenamento offline (#786).

Corrigido o crash de inicialização do NRMAHarvestController (#793).