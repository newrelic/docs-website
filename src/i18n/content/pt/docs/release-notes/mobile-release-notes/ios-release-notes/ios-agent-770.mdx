---
subject: iOS agent
releaseDate: '2026-03-25'
version: 7.7.0
downloadLink: 'https://download.newrelic.com/ios_agent/NewRelic_XCFramework_Agent_7.7.0.zip'
translationType: machine
---

### Novidades desta versão

* **Visualizações bloqueadas do Session Replay:** Adicionada nova funcionalidade de visualizações bloqueadas. Para UIKit, use o ID de acessibilidade &quot;nr-block&quot;, o novo UIView `blockView`, ou no SwiftUI `NRConditionalMaskView` com `blockView`=true para bloquear a captura de regiões durante o replay de sessão.

### Corrigido nesta versão

* Corrigido um problema de superaquecimento causado por geração excessiva de logging em casos raros.
* Resolvido um bug em `NewRelic.setMaxOfflineStorageSize`.
* Durante a coleta, tratar o código de status 0.
* Problemas resolvidos em `getAndClearFrames` e `flattenTree`.