---
title: Resolução de problemas
metaDescription: Instructions for troubleshooting SAP systems.
freshnessValidatedDate: never
translationType: machine
---

## Provedores de dados de disponibilidade do servidor não estão funcionando

**Sintoma**: os provedores de dados `NR_SAP_INSTANCE_AVAILABILITY` e `NR_SAP_SERVER_LIST` falham ao coletar dados.

**Causa**: o perfil `DEFAULT.PFL` nos servidores monitorados não está configurado para serviços web <DNT>**SAPCONTROL**</DNT>.

**Solução**:

1. Abra o arquivo `DEFAULT.PFL` e verifique se esta linha existe:

`service/protectedwebmethods = SDEFAULT`

2. Se a linha estiver ausente, adicione o seguinte parâmetro:

`service/protectedwebmethods = SDEFAULT -GetQueueStatistic -ABAPGetWPTable -EnqGetStatistic -GetProcessList -GetEnvironment -GetVersionInfo -GetSystemInstanceList`

3. Reinicie o <DNT>**SAP service**</DNT> (onde `##` é o número da instância):

```yaml
sapcontrol -nr ## -function RestartService
```

<Callout variant="important">
  Um reinício completo do sistema SAP não é necessário. Realizar a reinicialização do serviço SAP com cuidado em sistemas em cluster.
</Callout>

4. Verifique a configuração (onde `##` é o número da instância):

```yaml
sapcontrol -prot GSOAP_HTTP -nr ## -function GetSystemInstanceList
```

**Resultado esperado**: o comando retorna a lista de instâncias do sistema sem erros.