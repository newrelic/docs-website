---
title: Configuração adicional
metaDescription: Additional configuration instructions for SAP systems
freshnessValidatedDate: never
translationType: machine
---

Conclua estas tarefas de configuração para cada sistema monitorado.

## Função de autorização de upload [#upload-authorization-role]

1. Use <DNT>**SAP transaction**</DNT> `PFCG`.

2. Vá para <DNT>**Role**</DNT> &gt; <DNT>**Upload**</DNT> e carregue o arquivo <DNT>**\_NEWR\_DATA\_PROVIDER.SAP**</DNT> do pacote de instalação.

3. Confirme que a função `/NEWR/DATA_PROVIDER` carrega.

   <img title="Authorization Role" alt="A screenshot displaying the authorization role upload" src="/images/authorisation-role.webp" width="600" />

4. Selecione o<img src="/images/change-icon.webp" class="inline" style={{height: "22px", width: "22px"}} alt="change icon in SAP" title="Change icon indicator in SAP" />

   ícone após o carregamento da função.

5. Na aba <DNT>**Authorizations**</DNT>, selecione<img src="/images/change-icon.webp" class="inline" style={{height: "20px", width: "20px"}} alt="change icon in SAP" title="Change icon indicator in SAP" />

   <DNT>**Change Authorization Data**</DNT> e, em seguida, ative-o usando o<img src="/images/activate-option.webp" class="inline" style={{height: "25px", width: "30px"}} alt="activate icon in SAP" title="Activate Icon in SAP" />ícone.

   <img title="Authorizations Tab" alt="A screenshot displaying the authorization tab and Change Authorization Data in SAP upload" src="/images/sap-authorization-1.webp" width="500" />

6. Se um nome de perfil for sugerido, selecione o ícone de check verde.

7. Verifique se <DNT>**Profile(s) were updated**</DNT> aparece na barra de status.

<Callout variant="important">
  Caso necessite de <DNT>**SLT module monitoring**</DNT> no sistema, também será necessário fazer o upload da função `/NEWR/SLT_COLLECTOR` do arquivo `_NEWR_SLT_COLLECTOR.SAP` no pacote de instalação.
</Callout>

## Criar New Relic [#create-rfc-user-id]

Após ativar o <DNT>**authorization profile**</DNT>, use a transação `SU01` para criar um novo <DNT>**RFC user ID**</DNT> (por exemplo, `NR4SAPDP`). Defina o <DNT>**user type**</DNT> como <DNT>**Communication**</DNT> nos sistemas monitorados (provedores de dados) e <DNT>**System**</DNT> no sistema de monitoramento central (CMS). Em seguida, adicione a função `/NEWR/DATA_PROVIDER`.

<img title="Create New RFC User" alt="A screenshot displaying the creation of a new RFC user in SAP" src="/images/rfc-userid.webp" width="600" />

## Destino RFC padrão [#standard-rfc-destination]

Use a transação `SM59` para criar um <DNT>**RFC destination**</DNT> para cada sistema monitorado.

1. Selecione <DNT>**ABAP Connections**</DNT> e, em seguida, selecione o ícone <DNT>**Create**</DNT> para configurar um novo <DNT>**RFC destination**</DNT>.

2. Insira um nome descritivo para <DNT>**RFC Destination**</DNT>. Recomendamos: `NR_<SYSID>CLNT<CLNT#>`

3. Defina <DNT>**Connection Type**</DNT> como `3 - ABAP Connection`.

4. Na aba <DNT>**Technical Settings**</DNT>, insira:

   * <DNT>**Target Host**</DNT> (domínio ou IP totalmente qualificado) e <DNT>**Instance No.**</DNT>
   * <DNT>**Gateway Host**</DNT> e <DNT>**Service**</DNT> se necessário.

   <img title="Create RFC destinations for monitored systems" alt="A screenshot displaying the how to create RFC destinations for monitored systems" src="/images/rfc-destination.webp" width="600" />

   <Callout variant="tip">
     Use <DNT>**load balancing**</DNT> sempre que possível. Isso permite que a coleta de dados continue mesmo quando uma única conexão falha. Para obter etapas detalhadas, consulte [Configuração de um Destino RFC com Balanceamento de Carga](https://help.sap.com/doc/saphelp_nw75/7.5.5/en-US/cd/d9a38c1709414b8bc6a4380c398063/content.htm?no_cache=true) na documentação da SAP.
   </Callout>

5. Na guia <DNT>**Logon &amp; Security**</DNT>, insira o <DNT>**RFC user**</DNT> (`NR4SAPDP`) e a senha.

6. Selecione <DNT>**Save**</DNT>.

7. Vá para <DNT>**Utility &gt; Test &gt; Connection Test**</DNT> e <DNT>**Authorization Test**</DNT>.

   * <DNT>**Expected result**</DNT>: Ambos os testes mostram status de sucesso.

## Definir destino HTTP para a cloud New Relic [#set-http-destination-to-new-relic-cloud]

Use a transação `SM59` para configurar <DNT>**HTTPS connections**</DNT> para o <DNT>**New Relic API endpoints**</DNT>.

<Callout variant="important">
  Crie <DNT>**RFC destinations**</DNT> individuais para <DNT>**Events**</DNT>, <DNT>**Logs**</DNT>, <DNT>**Metrics**</DNT> e <DNT>**Traces**</DNT> separadamente.
</Callout>

1. Insira um <DNT>**Destination Name**</DNT> (por exemplo, `NR_API_EVENT`).

2. Defina <DNT>**Connection Type**</DNT> como `G` (Conexões HTTP para Servidor Externo).

3. Insira <DNT>**Target Host**</DNT>, <DNT>**Service No.**</DNT> como <DNT>**443**</DNT> (consulte [a porta padrão](/docs/sap-solutions/reference/network-accessibility) para <DNT>**HTTPS**</DNT>) e <DNT>**Path Prefix**</DNT> (consulte [a lista de verificação de prontidão para instalação](/docs/sap-solutions/reference/pre-installation-checklist/#installation-readiness-checklist) para obter detalhes).

4. Se você precisar de um proxy, configure-o na guia <DNT>**Technical Settings**</DNT>.

5. Na aba <DNT>**Security Options**</DNT>, selecione a opção <DNT>**Active**</DNT> para <DNT>**SSL**</DNT>.

   <img title="HTTP destination to New Relic" alt="A screenshot displaying the connection setup for HTTP destination to New Relic" src="/images/http-destination-to-new-relic.webp" width="600" />

6. Se o certificado SSL ainda não estiver configurado, faça o download dos <DNT>**root and intermediate certificates**</DNT> do endpoint do New Relic no navegador e importe-os para <DNT>**SAP**</DNT> usando a transação `STRUST`.

7. Selecione <DNT>**Connection Test**</DNT> para verificar a conexão.

   * <DNT>**Expected result**</DNT>: Uma conexão bem-sucedida prompt você a inserir dados de logon.
   * <DNT>**If test times out**</DNT>: é provável que um firewall esteja bloqueando a conexão. Consulte a equipe de rede para garantir que o acesso seja concedido ao <DNT>**New Relic domains**</DNT>.

8. No prompt de logon, selecione <DNT>**Cancel**</DNT> para prosseguir. Uma tela de código de resposta <DNT>**HTTP 4xx**</DNT> confirma que a conexão está funcionando.

9. Repita este procedimento para todos os <DNT>**New Relic API endpoints**</DNT>.

## Destino RFC do Cloud (BTP) [#cloud-btp-rfc-destination]

Selecione a guia para o serviço:

<Tabs>
  <TabsBar>
    <TabsBarItem id="btp-process-integration-runtime">
      <DNT>**BTP process integration runtime**</DNT>
    </TabsBarItem>

    <TabsBarItem id="cloud-alm-api">
      <DNT>**CloudALM API**</DNT>
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="btp-process-integration-runtime">
      1. Abra o <DNT>**Service Key**</DNT> baixado para identificar e registrar o `clientId`, o `clientsecret` e o `tokenurl`.
      2. Navegue até o URL especificado na chave de serviço usando um navegador da web.
      3. Nessa página, obtenha os certificados <DNT>**SAP on demand**</DNT>, <DNT>**Digicert global**</DNT> e <DNT>**Digicert root**</DNT>.

      <img title="BTP process integration runtime" alt="A screenshot displaying the connection setup for BTP process integration runtime" src="/images/btp-process-integration.webp" width="600" />

      4. Navegue até o código de transação `STRUST` e selecione <DNT>**SSL Client (Standard)**</DNT> enquanto estiver no modo de edição.
      5. Faça o upload individual dos três certificados, selecione <DNT>**Add to Certificate List**</DNT> para cada um e, em seguida, salve as alterações.
      6. Navegue até o Tcode `SMICM` e, em seguida, selecione <DNT>**Administration &gt; ICM &gt; Exit Soft &gt; Global**</DNT>. A execução dessa ação aciona a reinicialização de todos os <DNT>**ICM processes**</DNT> no sistema.

      <img title="Administration ICM" alt="A screenshot displaying the Administration ICM" src="/images/administration-icm.webp" width="600" />

      7. Acessar o sistema SAP e navegar até o código de transação `SM59`, em seguida, estabelecer um novo <DNT>**RFC connection**</DNT> na categoria <DNT>**HTTP Connections to External Server**</DNT>.
      8. Configure o destino dividindo o URL do token em seus respectivos componentes de host e prefixo de caminho.
      9. Defina o número do serviço como `443` e modifique o prefixo do caminho anexando `?grant_type=client_credentials`.

      <img title="Service No." alt="A screenshot displaying the Service No." src="/images/service-no.webp" width="600" />

      10. Na guia <DNT>**Login &amp; Security**</DNT>, selecione <DNT>**Basic Authentication**</DNT> e insira `clientId` e `clientsecret` como nome de usuário e senha.
      11. Defina o status de <DNT>**Secure Protocol**</DNT> como <DNT>**Active**</DNT> e salve a configuração.
      12. Selecione <DNT>**Connection Test**</DNT>.
    </TabsPageItem>

    <TabsPageItem id="cloud-alm-api">
      1. Observe `clientId`, `clientsecret` e `url` do <DNT>**Service Key**</DNT> baixado.
      2. Acesse o <DNT>**url**</DNT> em um navegador da web e baixe os certificados <DNT>**Digicert root**</DNT>, <DNT>**global**</DNT> e <DNT>**SAP on demand**</DNT>.

      <img title="CloudALM API" alt="A screenshot displaying the connection setup for CloudALM API" src="/images/cloudalm-api.webp" width="600" />

      3. Navegue até o código de transação `STRUST` e selecione <DNT>**SSL Client (Standard)**</DNT> enquanto estiver no modo de edição.
      4. Fazer o upload individualmente de cada um dos três certificados, em seguida, selecionar <DNT>**Add to Certificate List**</DNT> e salvar as alterações.
      5. Navegue até o Tcode `SMICM` e, em seguida, selecione <DNT>**Administration &gt; ICM &gt; Exit Soft &gt; Global**</DNT>. A execução desta ação aciona uma reinicialização de todos os <DNT>**ICM processes**</DNT> do sistema.

      <img title="Administration ICM" alt="A screenshot displaying the Administration ICM" src="/images/administration-icm.webp" width="600" />

      6. Acesse o código de transação `SM59` após fazer login no sistema SAP e, em seguida, estabeleça um novo <DNT>**RFC connection**</DNT> na categoria <DNT>**HTTP Connections to External Server**</DNT>.
      7. Configure o <DNT>**Target Host**</DNT> com o URL apropriado e defina o <DNT>**Service No.**</DNT> como `443`. Defina o <DNT>**Path Prefix**</DNT> como `/oauth/token?grant_type=client_credentials`.

      <img title="SM59 Configuration" alt="A screenshot displaying the SM59 configuration" src="/images/sm59-configuration.webp" width="600" />

      8. Navegue até a guia <DNT>**Login &amp; Security**</DNT> e escolha <DNT>**Basic Authentication**</DNT> na seção <DNT>**Login with user**</DNT>. Forneça o `clientId` como o nome de usuário e o `clientsecret` como a senha.
      9. Localize o status <DNT>**Secure Protocol**</DNT> abaixo e defina-o como <DNT>**Active**</DNT>.
      10. Aplique as alterações de configuração salvando e, em seguida, selecione <DNT>**Connection Test**</DNT>.
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Criar conexão com o banco de dados [#create-database-connection]

Use a transação `DBACOCKPIT` para definir o <DNT>**HANA Database connections**</DNT> remoto. No painel esquerdo, clique duas vezes em <DNT>**Database Connections**</DNT> e, em seguida, selecione <DNT>**Add**</DNT> para criar uma nova conexão.

<img title="Database Connection" alt="A screenshot displaying the Database Connection" src="/images/database-connection.webp" width="600" />

Em <DNT>**Database Details**</DNT>, execute as seguintes etapas:

1. Insira o <DNT>**Connection Name**</DNT> (recomendamos usar o <DNT>**SAP system ID**</DNT>).

2. Selecione <DNT>**HANA Database**</DNT> como o <DNT>**Database System**</DNT> designado.

3. Forneça um ID de usuário do banco de dados (`NRAGENT` é o ID recomendado), em seguida, insira e confirme a senha.

4. Insira o <DNT>**Database Host**</DNT> e o <DNT>**SQL Port**</DNT>.

   <img title="SAP Connection Name" alt="A screenshot displaying the SAP Connection Name" src="/images/connection-name.webp" width="600" />

5. Após salvar as entradas, selecione <DNT>**Test**</DNT> para verificar a conexão com o banco de dados.

   * <DNT>**Expected result**</DNT>: O teste de conexão mostra status bem-sucedido.