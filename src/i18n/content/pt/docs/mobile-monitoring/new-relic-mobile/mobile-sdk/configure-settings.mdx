---
title: Definir configurações de monitoramento de Mobile
type: apiDoc
shortDescription: API para ativar ou desativar configurações de monitoramento de Mobile.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: New Relic mobile monitoring API to enable or disable mobile monitoring settings.
freshnessValidatedDate: '2024-09-06T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="flutter">
      Flutter
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unidade
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      Xamarin
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      Use os métodos abaixo para alterar as configurações padrão de monitoramento de Mobile. Todas as configurações, incluindo a chamada para invocar o agente, são chamadas no método `onCreate` da classe `MainActivity` .

      Para alterar as configurações, você tem duas opções (se a configuração permitir):

      * Altere a configuração em sua própria linha para cada condição específica. Por exemplo:

      ```java
      NewRelic.disableFeature(FeatureFlag.DefaultInteractions);
      NewRelic.enableFeature(FeatureFlag.CrashReporting);
      NewRelic.withApplicationToken(NEW_RELIC_TOKEN).start(this.getApplication());
      ```

      OU

      * Altere a configuração como parte da chamada inicial do agente usando o método `.with` . Por exemplo:

      ```java
      NewRelic.withApplicationToken(NEW_RELIC_TOKEN)
              .withDefaultInteractions(false)
              .withCrashReportingEnabled(true)
              .start(this.getApplication());
      ```

      ## Configurações de endpoint de dados [#android-data-endpoint-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o relatório de falhas, que aparece na [página<DNT>**Crash analysis**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

              Especifica o componente de autoridade URI do endpoint de upload de dados de coleta. Este endpoint também é usado para uploads de exceções tratadas.

              Para substituir o endpoint de dados que o agente usará ao relatar coletas de dados, adicione `andCollectorAddress:` à chamada de método `NewRelic.withApplicationToken(“<TOKEN>”)` .

              O valor padrão é `mobile-collector.newrelic.com`.
            </td>

            <td>
              ```java
              NewRelic.withApplicationToken(“<TOKEN>”)
                      .usingCollectorAddress("harvest-upload.domain.com")
                      .usingCrashCollectorAddress("crash-upload.domain.com")
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o componente de autoridade do URI de upload de dados de falha.

              Para substituir o endpoint de dados que o agente usará ao relatar falhas, adicione `andCrashCollectorAddress:` à chamada do método `NewRelic.withApplicationToken(“<TOKEN>”)` .

              O valor padrão é `mobile-crash.newrelic.com`.
            </td>

            <td>
              ```java
              NewRelic.withApplicationToken(“<TOKEN>”)
                      .usingCollectorAddress("harvest-upload.domain.com")
                      .usingCrashCollectorAddress("crash-upload.domain.com")
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Configurações de análise [#android-analytics-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Habilite ou desabilite a coleta de dados de eventos. Esses eventos [podem ser consultados no NRQL](/docs/insights/explore-data/attributes/mobile-default-attributes-insights) e utilizados na [página<DNT>**Crash analysis**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            withAnalyticsEvents(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Habilite ou desabilite o relatório de dados de eventos. o evento <DNT>**collection**</DNT> ainda ocorrerá, mas os eventos não serão enviados ao nosso coletor. Você pode decidir usar isso em vez de `withAnalyticsEvents` se quiser desativar a coleta, mas ainda conseguir ver o que o agente estava coletando.

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.AnalyticsEvents)
            NewRelic.disableFeature(FeatureFlag.AnalyticsEvents)
            ```
          </td>
        </tr>
      </table>

      ## Configurações de tempo de lançamento do aplicativo [#android-applaunchtime]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Habilite ou desabilite o relatório do tempo de lançamento do aplicativo (tempo frio e quente) para a [métrica](/docs/data-apis/understand-data/new-relic-data-types/#metrics):

            * O tempo de inicialização a frio refere-se ao início de um aplicativo do zero. Isso significa que o processo do aplicativo não foi criado pelo sistema até então.

            * O horário de inicialização a quente refere-se a quando o processo do seu aplicativo já está em execução em segundo plano.

              O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.AppStartMetrics)
            NewRelic.disableFeature(FeatureFlag.AppStartMetrics)
            ```
          </td>
        </tr>
      </table>

      ## Configurações do aplicativo [#android-application-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Defina a versão do aplicativo como uma string. O padrão é o número de versão definido em `AndroidManifest.xml`.
          </td>

          <td>
            ```java
            withApplicationVersion("MY APP VERSION")
            ```
          </td>
        </tr>

        <tr>
          <td>
            Defina a versão da compilação como uma string. O padrão é o ID de compilação definido em `AndroidManifest.xml`.
          </td>

          <td>
            ```java
            withApplicationBuild("MY BUILD VERSION")
            ```
          </td>
        </tr>
      </table>

      ## Relatórios de histórico [#android-background-reporting]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```java
              NewRelic.enableFeature(FeatureFlag.BackgroundReporting)
              NewRelic.disableFeature(FeatureFlag.BackgroundReporting)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Configurações de relatórios de falhas e erros [#crash-error-reporting-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Ativa ou desativa [relatórios de falhas adiados](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/android-agent-crash-reporting/#deferred-crash-reporting). O valor padrão é `false`.
          </td>

          <td>
            ```java
            withCrashReportingEnabled(true)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Habilite ou desabilite a gravação de eventos de exceções tratadas, que aparecem na [página Exceção tratada](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/handled-exceptions-analyze-trends-prevent-crashes). O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.HandledExceptions)
            NewRelic.disableFeature(FeatureFlag.HandledExceptions)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Ative ou desative a gravação de travamentos de tempo de execução nativo, exceções e condições de aplicativo não respondendo (ANR), que aparecem na [página Análise de travamento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes). O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.NativeReporting)
            NewRelic.disableFeature(FeatureFlag.NativeReporting)
            ```
          </td>
        </tr>
      </table>

      ## Distributed tracing [#dt]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Habilite ou desabilite a adição de cabeçalhos distributed tracing às solicitações de rede.

            O valor padrão é `true`.

            Consulte [Como funciona distributed tracing do New Relic](/docs/distributed-tracing/concepts/how-new-relic-distributed-tracing-works#browser-spans) para obter mais informações sobre distributed tracing em aplicativos móveis.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.DistributedTracing)
            NewRelic.disableFeature(FeatureFlag.DistributedTracing)
            ```
          </td>
        </tr>
      </table>

      ## persistência de eventos [#android-event-persistence]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilitar ou desabilitar persistência de eventos.
            </td>

            <td>
              A persistência de eventos é desabilitada por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```java
              NewRelic.enableFeature(FeatureFlag.EventPersistence)
              NewRelic.disableFeature(FeatureFlag.EventPersistence)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Configurações do FedRamp [#fedrampenabled]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Ative ou desative dados de relatórios usando endpoints diferentes para clientes do governo dos EUA.

            O valor padrão é `true` (a partir da versão 6.11.0 do agente).
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.FedRampEnabled)
            NewRelic.disableFeature(FeatureFlag.FedRampEnabled)
            ```
          </td>
        </tr>
      </table>

      ## Configurações de interação [#interaction-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada.

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.InteractionTracing)
            NewRelic.disableFeature(FeatureFlag.InteractionTracing)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Habilite ou desabilite a interação padrão. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso ativará ou desativará a interação padrão apenas enquanto a interação personalizada permanecer habilitada.

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.DefaultInteractions)
            NewRelic.disableFeature(FeatureFlag.DefaultInteractions)
            ```
          </td>
        </tr>
      </table>

      ## Configurações de registro [#logging]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Habilite ou desabilite o log do agente.

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            withLoggingEnabled(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Especifica o nível de log. As opções incluem:

            * `ERROR` (menos detalhado)

            * `WARN`

            * `INFO`

            * `VERBOSE`

            * `DEBUG`

            * `AUDIT` (mais detalhado)

              O valor padrão é `INFO`.
          </td>

          <td>
            ```java
            withLogLevel(AgentLog.ERROR)
            ```
          </td>
        </tr>
      </table>

      ## Configurações de rede [#networking]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento [MobileRequest](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequest-examples) .

            O valor padrão é `true` (a partir da versão 5.15.2 do agente).
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.NetworkRequests)
            NewRelic.disableFeature(FeatureFlag.NetworkRequests)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento [MobileRequestError](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequesterror-examples) . Disponível para agente Android versão 5.11.0 ou superior.

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.NetworkErrorRequests)
            NewRelic.disableFeature(FeatureFlag.NetworkErrorRequests)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Habilite ou desabilite a captura de corpos de resposta HTTP para rastreamento de erro HTTP e evento [MobileRequestError](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequesterror-examples) .

            O valor padrão é `true`.
          </td>

          <td>
            ```java
            NewRelic.enableFeature(FeatureFlag.HttpResponseBodyCaptureEnabled)
            NewRelic.disableFeature(FeatureFlag.HttpResponseBodyCaptureEnabled)
            ```
          </td>
        </tr>
      </table>

      ## Armazenamento off-line [#android-offline-storage]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível.

              Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage).
            </td>

            <td>
              O armazenamento offline está desabilitado por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```java
              NewRelic.enableFeature(FeatureFlag.OfflineStorage)
              NewRelic.disableFeature(FeatureFlag.OfflineStorage)
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="ios">
      O agente iOS da New Relic fornece definições de configuração para alterar o comportamento padrão do agente. Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar o token do aplicativo. Por exemplo:

      * Objective-C:

      ```objectivec
      + [NewRelic startWithApplicationToken:]
      ```

      * Swift:

      ```swift
      NewRelic.start(withApplicationToken:)
      ```

      ## Habilitar ou desabilitar flags de recurso [#ios-feature-flags]

      Para configurar flags de recurso, utilize o seguinte método definido em `NewRelic.h:`

      <table>
        <thead>
          <tr>
            <th width={150}>
              Idioma iOS
            </th>

            <th>
              Procedimento
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td id="enable-obj-c">
              Objective-C
            </td>

            <td>
              No Objective-C, esses recursos são bitmasked, então você pode usar um `|` para ativar ou desativar vários recursos ao mesmo tempo.

              Habilite um sinalizador de recurso:

              ```objectivec
              +[NewRelic enableFeatures:(NRMAFeatureFlags)flags];
              ```

              Desative um sinalizador de recurso:

              ```objectivec
              +[NewRelic disableFeatures:(NRMAFeatureFlags)flags];
              ```

              Aqui está um exemplo onde:

              * O rastreamento de interação e a instrumentação de rede experimental estão habilitados.
              * `NSURLSession` instrumentação e relatórios de falhas estão desativados.

              ```objectivec
              [NewRelic enableFeatures:NRFeatureFlag_SwiftInteractionTracing | NRFeatureFlag_FedRampEnabled];
              [NewRelic disableFeatures:NRFeatureFlag_NSURLSessionInstrumentation | NRFeatureFlag_CrashReporting];
              [NewRelic startWithApplicationToken:...];
              ```
            </td>
          </tr>

          <tr>
            <td>
              Swift
            </td>

            <td>
              Habilite um sinalizador de recurso:

              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.[NRMAFeatureFlag])
              ```

              Desative um sinalizador de recurso:

              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.[NRMAFeatureFlag])
              ```

              Exemplo para desativar o rastreamento de interação padrão:

              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DefaultInteractions)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Configurações de endpoint de dados [#ios-data-endpoint-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o relatório de falhas, que aparece na [página<DNT>**Crash analysis**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

              Especifica o componente de autoridade URI do endpoint de upload de dados de coleta. Este endpoint também é usado para uploads de exceções tratadas.

              Para substituir o endpoint de dados que o agente usará ao relatar coletas de dados, adicione `andCollectorAddress:` à chamada de método `NewRelic.start(withApplicationToken:)` .

              O valor padrão é `mobile-collector.newrelic.com`.
            </td>

            <td>
              ```swift
              NewRelic.start(withApplicationToken:YOUR_APP_TOKEN
                      andCollectorAddress:"harvest-upload.domain.com"
                      andCrashCollectorAddress:"crash-upload.domain.com");
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o componente de autoridade do URI de upload de dados de falha.

              Para substituir o endpoint de dados que o agente usará ao relatar falhas, adicione `andCrashCollectorAddress:` à chamada do método `NewRelic.start(withApplicationToken:)` .

              O valor padrão é `mobile-crash.newrelic.com`.
            </td>

            <td>
              ```swift
              NewRelic.start(withApplicationToken:YOUR_APP_TOKEN
                    andCollectorAddress:"harvest-upload.domain.com"
                    andCrashCollectorAddress:"crash-upload.domain.com");
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Sinalizador de recurso de relatório de falhas e erros [#ios-crashes]

      Se você modificar qualquer uma das configurações de relatório de falhas e erros abaixo, certifique-se de chamar o sinalizador de recurso antes que o agente iOS inicie a chamada.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o relatório de falhas, que aparece na [página**Análise de falhas** ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

              Especifica o componente de autoridade URI do endpoint de upload de dados de coleta. Este endpoint também é usado para uploads de exceções tratadas.

              Para substituir o endpoint de dados que o agente usará ao relatar coletas de dados, adicione `.usingCollectorAddress()` à chamada de método `NewRelic.withApplicationToken()` .

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_CrashReporting)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite globalmente a gravação de eventos de exceções tratadas, que aparecem na [página Exceção tratada](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/handled-exceptions-analyze-trends-prevent-crashes).

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_HandledExceptionEvents)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Distributed tracing [#ios-dt]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilite ou desabilite a adição de cabeçalhos distributed tracing às solicitações de rede. Consulte [Como funciona distributed tracing do New Relic](/docs/understand-dependencies/distributed-tracing/get-started/how-new-relic-distributed-tracing-works/#no-sampling) para obter mais informações sobre distributed tracing em aplicativos móveis.

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DistributedTracing)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## interação recurso flags [#ios-interactions]

      Se você modificar qualquer uma das configurações de relatório de falhas e erros abaixo, certifique-se de chamar o sinalizador de recurso pouco antes de o agente iOS da New Relic iniciar a chamada.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilite ou desabilite o rastreamento de interação apenas para classes e métodos de instrumento padrão. A interação personalizada será relatada. O rastreamento aparecerá na [página<DNT>**Interactions**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page).

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DefaultInteractions)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite globalmente o trace da interação, que aparece na [página<DNT>**Interactions**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page).

              O rastreamento de interação será desativado após o início do agente. A interação para qualquer atividade ou método executado antes do início do agente ainda poderá aparecer na página <DNT>**Interactions**</DNT> .

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_InteractionTracing)
              ```
            </td>
          </tr>

          <tr>
            <td>
              <Callout variant="caution">
                Antes de habilitar esse recurso, consulte [Habilitar rastreamento de interação do Swift](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/enable-swift-interaction-traces). Se as medidas adequadas não forem tomadas, a ativação deste recurso poderá causar instabilidade.
              </Callout>

              Habilite ou desabilite o rastreamento de interação para código Swift. O rastreamento aparece na [página<DNT>**Interactions**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page). Este recurso estará sempre desativado se [`NRFeatureFlag_InteractionTracing`](#interactionTracing) estiver desativado.

              O valor padrão é `false`.
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_SwiftInteractionTracing)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite a instrumentação automática do WKWebView.

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_WebViewInstrumentation)
              ```
            </td>
          </tr>

          <tr>
            <td>
              <Callout variant="caution">
                Este recurso está obsoleto, habilitar esta chamada de sinalizador de recurso pode diminuir a estabilidade do aplicativo. Evite usar, a menos que seja instruído pela New Relic.
              </Callout>

              Ative ou desative a instrumentação automática de gestos.

              O valor padrão é `false`.
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_GestureInstrumentation)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Sinalizadores de recursos de rede [#ios-networking]

      Se você modificar alguma das configurações abaixo, certifique-se de chamar o sinalizador de recurso antes que o agente iOS da New Relic inicie a chamada.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              <Callout variant="caution">
                Este recurso está obsoleto, habilitar esta chamada de sinalizador de recurso pode diminuir a estabilidade do aplicativo. Evite usar, a menos que seja instruído pela New Relic.
              </Callout>

              Habilite ou desabilite a instrumentação de rede experimental. Isso força todas as `NSURLConnection` solicitações de rede através do `NRMAURProtocol`.

              O valor padrão é `false`.
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_ExperimentalNetworkingInstrumentation)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a instrumentação de rede para `NSURLSession`. Esses rastreamentos de URL são enviados para a página [Solicitações HTTP](/docs/mobile-monitoring/mobile-monitoring-ui/network-pages/http-requests-page) .

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_NSURLSessionInstrumentation)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite a instrumentação de rede. Isso envia os dados de resposta HTTP como evento [MobileRequest](/docs/insights/insights-data-sources/default-data/mobile-default-event-attributes-insights#mobilerequest-attributes) .

              O valor padrão é `true` (a partir da versão 6.0.0 do agente).
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_NetworkRequestEvents)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilitar ou desabilitar erro de solicitação HTTP em caso de geração de chamadas de rede do instrumento que resultem em erro ou falha. Esses eventos são relatados como evento [MobileRequestError](/docs/insights/insights-data-sources/default-data/mobile-default-event-attributes-insights#mobilerequesterror-attributes) .

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_RequestErrorEvents)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a captura de corpos de resposta HTTP para rastreamento de erro HTTP e evento `MobileRequestError` .

              O valor padrão é `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_HttpResponseBodyCapture)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilitar ou desabilitar instrumentação para sessões de URL de espera assíncrona.

              O valor padrão é `false` (a partir da versão 7.4.5 do agente).
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_SwiftAsyncURLSessionSupport)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Configurações de tempo de lançamento do aplicativo [#ios-applaunchtime]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o relatório de horário de início do aplicativo (horário frio e quente) como métrica:

              * O tempo frio refere-se ao tempo entre o início do aplicativo e o primeiro sorteio.

              * O tempo quente refere-se ao tempo que leva para um aplicativo retomar a execução em segundo plano até o primeiro sorteio.

                O valor padrão é `true` (a partir da versão 7.4.0 do agente).
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_AppStartMetrics)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Configurações de endpoint do FedRAMP [#ios-fedramp]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilite ou desabilite usando o endpoint FedRAMP.

              O valor padrão é `false` (a partir da versão 7.4.5 do agente).
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_FedRampEnabled)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Armazenamento off-line [#ios-offline-storage]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#ios).
            </td>

            <td>
              O armazenamento offline está desabilitado por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_OfflineStorage)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Relatórios de histórico [#ios-background-reporting]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```swift
              NewRelic.enableFeatures([NRMAFeatureFlags.NRFeatureFlag_BackgroundReporting])
              ```

              Pré-requisitos no seu projeto de aplicativo iOS para usar instrumentação em segundo plano:

              * Adicione a chave `Permitted background task scheduler identifiers` com uma matriz incluindo um item contendo o ID do pacote de aplicativos.

              * Habilite a **Background Fetch** e **Background Processing** na seção **Background Modes** dos recursos do projeto Xcode.

              Observação: o iOS só garante que o iOS coletará em segundo plano uma vez a cada período de vinte e quatro horas.
            </td>
          </tr>
        </tbody>
      </table>

      ## Coleta automática de logs [#ios-log-collection]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Habilita ou desabilita a coleta de `stdout` e `stderr` para o logging remoto.
            </td>

            <td>
              A coleta de logs é desabilitada por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```swift
              NewRelic.disableFeatures([NRMAFeatureFlags.NRFeatureFlag_AutoCollectLogs])
              ```

              Para obter mais informações, consulte [Logging do agente iOS](/docs/mobile-monitoring/new-relic-mobile-ios/configuration/ios-remote-logging).
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      O SDK do agente Capacitor permite definir configurações padrão para alterar o comportamento do agente.

      Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar `appToken`.

      Aqui está um exemplo de configuração:

      ```typescript
      import { NewRelicCapacitorPlugin, NREnums, AgentConfiguration } from '@newrelic/newrelic-capacitor-plugin';
      import { Capacitor } from '@capacitor/core';

      var appToken;

      if(Capacitor.getPlatform() === 'ios') {
        appToken = 'IOS_APP_TOKEN';
      } else {
        appToken = 'ANDROID_APP_TOKEN';
      }

      let agentConfig : AgentConfiguration = {
        analyticsEventEnabled: false,
        crashReportingEnabled: false,
        interactionTracingEnabled: false,
        networkRequestEnabled: false,
        networkErrorRequestEnabled: false,
        httpResponseBodyCaptureEnabled: false,
        loggingEnabled: false,
        logLevel: NREnums.LogLevel.INFO,
        webViewInstrumentation: false,
        collectorAddress: "",
        crashCollectorAddress: "",
        sendConsoleEvents: false,
        fedRampEnabled: false,
        offlineStorageEnabled: false
      }

      NewRelicCapacitorPlugin.start({appKey:appToken, agentConfiguration:agentConfig})
      ```

      ## Configuração disponível [#capacitor-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Somente Android) Ative ou desative a coleta de dados de eventos.
            </td>

            <td>
              ```typescript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de falhas.
            </td>

            <td>
              ```typescript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada.
            </td>

            <td>
              ```typescript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento `MobileRequest` .
            </td>

            <td>
              ```typescript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento `MobileRequestError` .
            </td>

            <td>
              ```typescript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a captura de corpos de resposta HTTP para rastreamento de erro HTTP e evento `MobileRequestError` .
            </td>

            <td>
              ```typescript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite o log do agente.
            </td>

            <td>
              ```typescript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o nível de log. Omita este campo para o nível de log padrão.

              As opções incluem: `ERROR` (menos detalhado), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (mais detalhado).
            </td>

            <td>
              ```typescript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (somente iOS) Ativar/desativar instrumentação automática de `WebViews`.
            </td>

            <td>
              ```typescript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço de coletor específico para envio de dados. Omita este campo para endereço padrão.
            </td>

            <td>
              ```typescript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço específico do coletor de falhas para enviar falhas. Omita este campo para endereço padrão.
            </td>

            <td>
              ```typescript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o envio de log do console JavaScript para o New Relic.
            </td>

            <td>
              ```typescript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative dados de relatórios usando endpoints diferentes para clientes do governo dos EUA.
            </td>

            <td>
              ```typescript
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#capacitor).
            </td>

            <td>
              O armazenamento offline está ativado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```typescript
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```typescript
              backgroundReportingEnabled: true
              ```

              Para usar a instrumentação em segundo plano, você precisará do seguinte em seu projeto de aplicativo iOS:

              * No arquivo `Info.plist` do seu aplicativo, adicione a chave &quot;Identificador do agendador de tarefas em segundo plano permitido&quot;. Essa chave deve ser uma matriz contendo um único valor de string, que é o identificador do pacote do seu aplicativo.

              * Na seção **Background Modes** dos recursos do projeto Xcode, habilite **Background Fetch** e **Background Processing**.

              Observação: o iOS só garante que o iOS coletará em segundo plano uma vez a cada período de vinte e quatro horas.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="cordova">
      O SDK do agente Cordova permite definir configurações padrão para alterar o comportamento do agente.

      Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar `appToken`. 

      Aqui está um exemplo de configuração:

      ```shell
      # Disable Crash Reporting
      cordova plugin add https://github.com/newrelic/newrelic-cordova-plugin.git
        --variable IOS_APP_TOKEN="YOUR_IOS_APP_TOKEN"
        --variable ANDROID_APP_TOKEN="YOUR_ANDROID_APP_TOKEN"
        --variable CRASH_REPORTING_ENABLED="false"
        --variable OFFLINE_STORAGE_ENABLED="false"
      ```

      ## Configuração disponível [#cordova-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Ative ou desative o relatório de falhas.
            </td>

            <td>
              ```shell
              CRASH_REPORTING_ENABLED = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada.
            </td>

            <td>
              ```shell
              INTERACTION_TRACING_ENABLED = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento `MobileRequest` .
            </td>

            <td>
              ```typescript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento `MobileRequestError` .
            </td>

            <td>
              ```typescript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a captura de corpos de resposta HTTP para rastreamento de erro HTTP e evento `MobileRequestError` .
            </td>

            <td>
              ```typescript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite o log do agente.
            </td>

            <td>
              ```shell
              LOGGING_ENABLED = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o nível de log. Omita este campo para o nível de log padrão.

              As opções incluem: `ERROR` (menos detalhado), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (mais detalhado).
            </td>

            <td>
              ```shell
              LOG_LEVEL = "INFO"
              ```
            </td>
          </tr>

          <tr>
            <td>
              (somente iOS) Ativar/desativar instrumentação automática de `WebViews`.
            </td>

            <td>
              ```shell
              WEB_VIEW_INSTRUMENTATION = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço de coletor específico para envio de dados. Omita este campo para endereço padrão.
            </td>

            <td>
              ```shell
              COLLECTOR_ADDRESS = "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço específico do coletor de falhas para enviar falhas. Omita este campo para endereço padrão.
            </td>

            <td>
              ```shell
              CRASH_COLLECTOR_ADDRESS = "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o envio de log do console JavaScript para o New Relic.
            </td>

            <td>
              ```typescript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative dados de relatórios usando endpoints diferentes para clientes do governo dos EUA.
            </td>

            <td>
              ```shell
              FEDRAMP_ENABLED = "false"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#cordova).
            </td>

            <td>
              O armazenamento offline está ativado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```shell
              OFFLINE_STORAGE_ENABLED = "false"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```shell
              BACKGROUND_REPORTING_ENABLED = "true"
              ```

              Para usar a instrumentação em segundo plano, você precisará do seguinte em seu projeto de aplicativo iOS:

              * No arquivo `Info.plist` do seu aplicativo, adicione a chave &quot;Identificador do agendador de tarefas em segundo plano permitido&quot;. Essa chave deve ser uma matriz contendo um único valor de string, que é o identificador do pacote do seu aplicativo.

              * Na seção **Background Modes** dos recursos do projeto Xcode, habilite **Background Fetch** e **Background Processing**.

              Observação: o iOS só garante que o iOS coletará em segundo plano uma vez a cada período de vinte e quatro horas.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="maui">
      O SDK do agente .NET MAUI permite definir configurações padrão para alterar o comportamento do agente.

      Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar `appToken`.

      Aqui está um exemplo de configuração:

      ```csharp
      using NewRelic.MAUI.Plugin;
      ...

      public static MauiApp CreateMauiApp()
      {
        var builder = MauiApp.CreateBuilder();
        builder
          .UseMauiApp<App>()
          .ConfigureFonts(fonts =>
          {
            fonts.AddFont("OpenSans-Regular.ttf", "OpenSansRegular");
            fonts.AddFont("OpenSans-Semibold.ttf", "OpenSansSemibold");
          });

            builder.ConfigureLifecycleEvents(AppLifecycle => {
            #if ANDROID
                AppLifecycle.AddAndroid(android => android
                    .OnCreate((activity, savedInstanceState) => StartNewRelic()));
            #endif
            #if IOS

                  AppLifecycle.AddiOS(iOS => iOS.WillFinishLaunching((_,__) => {
                    StartNewRelic();
                    return false;
                }));
            #endif
            });
        return builder.Build();
      }

      private static void StartNewRelic()
      {
        CrossNewRelic.Current.HandleUncaughtException();

        // Set optional agent configuration. Options are: 
        // crashReportingEnabled, loggingEnabled, logLevel, collectorAddress, 
        // crashCollectorAddress,analyticsEventEnabled, networkErrorRequestEnabled,
        // networkRequestEnabled, interactionTracingEnabled, webViewInstrumentation,
        // fedRampEnabled, offlineStorageEnabled, newEventSystemEnabled, backgroundReportingEnabled
        // AgentStartConfiguration agentConfig = new AgentStartConfiguration(crashReportingEnabled:false);

        if (DeviceInfo.Current.Platform == DevicePlatform.Android)
        {
          CrossNewRelic.Current.Start("APP_TOKEN_HERE");
          // Start with optional agent configuration
          // CrossNewRelic.Current.Start("APP_TOKEN_HERE", agentConfig);
        } 
        else if (DeviceInfo.Current.Platform == DevicePlatform.iOS)
        {
          CrossNewRelic.Current.Start("APP_TOKEN_HERE");
          // Start with optional agent configuration
          // CrossNewRelic.Current.Start("APP_TOKEN_HERE", agentConfig);
        }
      }
      ```

      ## Configuração disponível [#maui-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Somente Android) Ative ou desative a coleta de dados de eventos.
            </td>

            <td>
              ```csharp
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de falhas.
            </td>

            <td>
              ```csharp
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite o log do agente.
            </td>

            <td>
              ```csharp
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento `MobileRequest` .
            </td>

            <td>
              ```csharp
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento `MobileRequestError` .
            </td>

            <td>
              ```csharp
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada. por padrão falso
            </td>

            <td>
              ```csharp
              interactionTracingEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite/desabilite a instrumentação automática de WebViews.
            </td>

            <td>
              ```csharp
              webViewInstrumentation: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative dados de relatórios usando endpoints diferentes para clientes do governo dos EUA.
            </td>

            <td>
              ```csharp
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o nível de log. Omita este campo para o nível de log padrão.

              As opções incluem: `ERROR` (menos detalhado), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (mais detalhado).
            </td>

            <td>
              ```csharp
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço de coletor específico para envio de dados. Omita este campo para endereço padrão.
            </td>

            <td>
              ```csharp
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço específico do coletor de falhas para enviar falhas. Omita este campo para endereço padrão.
            </td>

            <td>
              ```csharp
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#maui).
            </td>

            <td>
              O armazenamento offline está ativado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```csharp
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```csharp
              backgroundReportingEnabled: true
              ```

              Para usar a instrumentação em segundo plano, você precisará do seguinte em seu projeto de aplicativo iOS:

              * No arquivo `Info.plist` do seu aplicativo, adicione a chave &quot;Identificador do agendador de tarefas em segundo plano permitido&quot;. Essa chave deve ser uma matriz contendo um único valor de string, que é o identificador do pacote do seu aplicativo.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="flutter">
      O SDK do agente Flutter permite definir configurações padrão para alterar o comportamento do agente.

      Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar `appToken`.

      Aqui está um exemplo de configuração:

      ```dart
      if (Platform.isAndroid) {
        appToken = AppConfig.androidToken;
      } else if (Platform.isIOS) {
        appToken = AppConfig.iOSToken;
      }

      Config config = Config(
          accessToken: appToken,
          analyticsEventEnabled: false,
          networkErrorRequestEnabled: false,
          networkRequestEnabled: false,
          crashReportingEnabled: false,
          interactionTracingEnabled: false,
          httpResponseBodyCaptureEnabled: false,
          loggingEnabled: false,
          webViewInstrumentation: false,
          printStatementAsEventsEnabled: false,
          httpInstrumentationEnabled: false,
          offlineStorageEnabled: true);

      // NewrelicMobile.instance.start(config, () {
      //   runApp(MyApp());
      // });

      runZonedGuarded(() async {
        WidgetsFlutterBinding.ensureInitialized();
        FlutterError.onError = NewrelicMobile.onError;
        await NewrelicMobile.instance.startAgent(config);
        runApp(MyApp());
      }, (Object error, StackTrace stackTrace) {
        NewrelicMobile.instance.recordError(error, stackTrace);
      });
      ```

      ## Configuração disponível [#flutter-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Somente Android) Ative ou desative a coleta de dados de eventos.
            </td>

            <td>
              ```dart
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de falhas.
            </td>

            <td>
              ```dart
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada.
            </td>

            <td>
              ```dart
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento `MobileRequest` .
            </td>

            <td>
              ```dart
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento `MobileRequestError` .
            </td>

            <td>
              ```dart
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a captura de corpos de resposta HTTP para rastreamento de erro HTTP e evento `MobileRequestError` .
            </td>

            <td>
              ```dart
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite o log do agente.
            </td>

            <td>
              ```dart
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o nível de log. Omita este campo para o nível de log padrão.

              As opções incluem: `ERROR` (menos detalhado), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (mais detalhado).
            </td>

            <td>
              ```dart
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (somente iOS) Ativar/desativar instrumentação automática de `WebViews`.
            </td>

            <td>
              ```dart
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço de coletor específico para envio de dados. Omita este campo para endereço padrão.
            </td>

            <td>
              ```dart
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço específico do coletor de falhas para enviar falhas. Omita este campo para endereço padrão.
            </td>

            <td>
              ```dart
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o envio de log do console JavaScript para o New Relic.
            </td>

            <td>
              ```dart
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#flutter).
            </td>

            <td>
              O armazenamento offline está ativado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```dart
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite a adição de cabeçalhos distributed tracing às solicitações de rede.

              O valor padrão é `true`.

              Para obter mais informações sobre distributed tracing em aplicativos móveis, consulte [relatórios trace de browser e mobile](/docs/distributed-tracing/concepts/how-new-relic-distributed-tracing-works#browser-spans)
            </td>

            <td>
              o rastreamento distribuído é habilitado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```dart
              distributedTracingEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```typescript
              backgroundReportingEnabled: true
              ```

              Para usar a instrumentação em segundo plano, você precisará do seguinte em seu projeto de aplicativo iOS:

              * No arquivo `Info.plist` do seu aplicativo, adicione a chave &quot;Identificador do agendador de tarefas em segundo plano permitido&quot;. Essa chave deve ser uma matriz contendo um único valor de string, que é o identificador do pacote do seu aplicativo.

              * Na seção **Background Modes** dos recursos do projeto Xcode, habilite **Background Fetch** e **Background Processing**.

              Observação: o iOS só garante que o iOS coletará em segundo plano uma vez a cada período de vinte e quatro horas.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="react">
      O SDK do agente React Native permite definir configurações padrão para alterar o comportamento do agente.

      Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar `appToken`.

      Aqui está um exemplo de configuração:

      ```javascript
      import NewRelic from 'newrelic-react-native-agent';
      import * as appVersion from './package.json';
      import {Platform} from 'react-native';

      let appToken;

      if (Platform.OS === 'ios') {
        appToken = 'IOS_APP_TOKEN';
      } else {
        appToken = 'ANDROID_APP_TOKEN';
      }


      const agentConfiguration = {
        Config config = Config(
          analyticsEventEnabled: false,
          crashReportingEnabled: false,
          interactionTracingEnabled: false,
          networkRequestEnabled: false,
          networkErrorRequestEnabled: false,
          httpResponseBodyCaptureEnabled: false,
          loggingEnabled: false,
          logLevel: NREnums.LogLevel.INFO,
          webViewInstrumentation: false,
          collectorAddress: "",
          crashCollectorAddress: "",
          sendConsoleEvents: false,
          fedRampEnabled: false,
          offlineStorageEnabled:false
        )
      };

      NewRelic.startAgent(appToken,agentConfiguration);
      NewRelic.setJSAppVersion(appVersion.version);
      AppRegistry.registerComponent(appName, () => App);
      ```

      ## Configuração disponível [#react-native-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Somente Android) Ative ou desative a coleta de dados de eventos.
            </td>

            <td>
              ```javascript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de falhas.
            </td>

            <td>
              ```javascript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilitar ou desabilitar relatórios de falhas nativos.
            </td>

            <td>
              ```javascript
              nativeCrashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada.
            </td>

            <td>
              ```javascript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento `MobileRequest` .
            </td>

            <td>
              ```javascript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento `MobileRequestError` .
            </td>

            <td>
              ```javascript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a captura de corpos de resposta HTTP para rastreamento de erro HTTP e evento `MobileRequestError` .
            </td>

            <td>
              ```javascript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite o log do agente.
            </td>

            <td>
              ```javascript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o nível de log. Omita este campo para o nível de log padrão.

              As opções incluem: `ERROR` (menos detalhado), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (mais detalhado).
            </td>

            <td>
              ```javascript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (somente iOS) Ativar/desativar instrumentação automática de `WebViews`.
            </td>

            <td>
              ```javascript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço de coletor específico para envio de dados. Omita este campo para endereço padrão.
            </td>

            <td>
              ```javascript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço específico do coletor de falhas para enviar falhas. Omita este campo para endereço padrão.
            </td>

            <td>
              ```javascript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o envio de log do console JavaScript para o New Relic.
            </td>

            <td>
              ```javascript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative dados de relatórios usando endpoints diferentes para clientes do governo dos EUA.
            </td>

            <td>
              ```javascript
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#react).
            </td>

            <td>
              O armazenamento offline está ativado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```typescript
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```typescript
              backgroundReportingEnabled: true
              ```

              Para usar a instrumentação em segundo plano, você precisará do seguinte em seu projeto de aplicativo iOS:

              * No arquivo `Info.plist` do seu aplicativo, adicione a chave &quot;Identificador do agendador de tarefas em segundo plano permitido&quot;. Essa chave deve ser uma matriz contendo um único valor de string, que é o identificador do pacote do seu aplicativo.

              * Na seção **Background Modes** dos recursos do projeto Xcode, habilite **Background Fetch** e **Background Processing**.

              Observação: o iOS só garante que o iOS coletará em segundo plano uma vez a cada período de vinte e quatro horas.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="unity">
      A New Relic oferece configurações padrão para monitoramento de aplicativos móveis no Unity. Você pode ajustar facilmente essas configurações no editor do Unity para atender às suas necessidades específicas.

      Para definir essas configurações:

      1. Lance o Unity Editor e abra seu projeto Unity.

      2. Na barra de menu, selecione **Tools &gt; New Relic &gt; Getting Started &gt; New Relic Configuration**.

      3. A janela esquerda do **Inspector** exibe uma lista de configurações padrão. Basta marcar a caixa ao lado de uma configuração para ativá-la ou desmarcar a caixa para desativá-la.

         <img title="Unity editor" alt="Screenshot of the Unity editor to configure settings" src="/images/mobile_screenshot-crop_Unity-editor-UI.webp" />

      4. Clique em **Add component**.
    </TabsPageItem>

    <TabsPageItem id="unreal">
      New Relic oferece configurações padrão para monitoramento de aplicativos móveis no Unreal Engine. Você pode ajustar facilmente essas configurações no editor Unreal para atender às suas necessidades específicas.

      Para definir essas configurações:

      1. lance o Unreal Editor e abra seu projeto Unreal.

      2. Na barra de menu, selecione \*\*Plugins &gt; New Relic &gt; \*\*.

      <img title="Unreal editor" alt="Screenshot of the Unreal editor to configure settings" src="/images/newrelic_unreal_sdk_configuration.webp" />
    </TabsPageItem>

    <TabsPageItem id="xamarin">
      O SDK do agente Xamarin permite definir configurações padrão para alterar o comportamento do agente.

      Se você fizer alguma alteração nas configurações padrão, adicione os sinalizadores de recurso logo após chamar `appToken`.

      Aqui está um exemplo de configuração:

      ```csharp
      public App ()
      {
          InitializeComponent();

          MainPage = new MainPage();
          Application.Current.PageAppearing += OnPageAppearing;
          Application.Current.PageDisappearing += PageDisappearing;

          CrossNewRelicClient.Current.HandleUncaughtException();
          CrossNewRelicClient.Current.TrackShellNavigatedEvents();

          // Set optional agent configuration. Options are: 
          // crashReportingEnabled, loggingEnabled, logLevel, collectorAddress, 
          // crashCollectorAddress, analyticsEventEnabled, networkErrorRequestEnabled,
          // networkRequestEnabled, interactionTracingEnabled,webViewInstrumentation,
          // fedRampEnabled, offlineStorageEnabled
          AgentStartConfiguration agentConfig = new AgentStartConfiguration(crashReportingEnabled: false, offlineStorageEnabled: false);

          if (Device.RuntimePlatform == Device.Android)
          {
              //CrossNewRelicClient.Current.Start("APP_TOKEN_HERE");
              // Start with optional agent configuration
              CrossNewRelicClient.Current.Start("APP_TOKEN_HERE", agentConfig);
          }
          else if (Device.RuntimePlatform == Device.iOS)
          {
              //CrossNewRelicClient.Current.Start("APP_TOKEN_HERE");
              // Start with optional agent configuration
              CrossNewRelicClient.Current.Start("APP_TOKEN_HERE", agentConfig);
          }
      }
      ```

      ## Configuração disponível [#xamarain-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Descrição
            </th>

            <th>
              Exemplo
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Somente Android) Ative ou desative a coleta de dados de eventos.
            </td>

            <td>
              ```csharp
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de falhas.
            </td>

            <td>
              ```csharp
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite o log do agente.
            </td>

            <td>
              ```csharp
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o relatório de solicitações HTTP bem-sucedidas para o tipo de evento `MobileRequest` .
            </td>

            <td>
              ```csharp
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative a rede de relatórios e erros de solicitação HTTP para o tipo de evento `MobileRequestError` .
            </td>

            <td>
              ```csharp
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Especifica o nível de log. Omita este campo para o nível de log padrão.

              As opções incluem: `ERROR` (menos detalhado), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (mais detalhado).
            </td>

            <td>
              ```csharp
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço de coletor específico para envio de dados. Omita este campo para endereço padrão.
            </td>

            <td>
              ```csharp
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Defina um endereço específico do coletor de falhas para enviar falhas. Omita este campo para endereço padrão.
            </td>

            <td>
              ```csharp
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o rastreamento de interação. a instrumentação trace ainda ocorre, mas nenhum traço é coletado. Isso desativará a interação padrão e personalizada. por padrão falso
            </td>

            <td>
              ```csharp
              interactionTracingEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite/desabilite a instrumentação automática de WebViews.
            </td>

            <td>
              ```csharp
              webViewInstrumentation: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative dados de relatórios usando endpoints diferentes para clientes do governo dos EUA.
            </td>

            <td>
              ```csharp
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Ative ou desative o armazenamento de dados offline quando não houver conexão com a Internet disponível. Para configurar a quantidade de armazenamento offline, consulte [Definir tamanho máximo de armazenamento offline](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#xamarin).
            </td>

            <td>
              O armazenamento offline está ativado por padrão. Para desativá-lo, adicione o seguinte sinalizador de recurso:

              ```csharp
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Habilite ou desabilite relatórios em segundo plano quando o aplicativo passar para o estado de segundo plano.
            </td>

            <td>
              Os relatórios em segundo plano estão desativados por padrão. Para habilitá-lo, adicione o seguinte sinalizador de recurso:

              ```csharp
              backgroundReportingEnabled: true
              ```

              Para usar a instrumentação em segundo plano, você precisará do seguinte em seu projeto de aplicativo iOS:

              * Adicione a chave `Permitted background task scheduler identifiers` com uma matriz incluindo um item contendo o ID do pacote de aplicativos.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>
  </TabsPages>
</Tabs>