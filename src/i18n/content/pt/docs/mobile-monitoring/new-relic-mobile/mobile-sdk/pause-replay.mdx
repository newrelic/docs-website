---
title: Pausar replay
type: apiDoc
shortDescription: Pausar a reprodução da sessão.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Record session replay
freshnessValidatedDate: never
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxe [#syntax]

      ### Java [#java]

      ```java
      NewRelic.pauseReplay()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.pauseReplay()
      ```

      ## Requisitos

      * Agente Android v7.7.0 ou superior.

      ## Descrição

      `newrelic.pauseReplay()` pode ser chamado para forçar manualmente um replay para pausar a gravação.

      Se uma repetição ainda não estiver em andamento, este método de API não terá efeito.

      Se a sessão móvel ainda for válida, você pode retomar um replay pausado chamando o método de API [`newrelic.recordReplay()`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/record-replay).

      ## Casos de uso

      * Você deseja acionar manualmente uma repetição para interromper a gravação em determinadas páginas do site ou sob determinadas condições, como uma página de saúde, página de informações de cartão de crédito, etc.

      ## Exemplo [#example]

      Aqui está um exemplo de como forçar um replay a pausar a gravação.

      ### Java [#java]

      ```java
      public class MainActivity extends Activity {
        ...
        @Override
        public boolean onOptionsItemSelected(MenuItem item) {
          switch (item.getItemId()) {
            case R.id.pause_replay:
              NewRelic.pauseReplay();
            ...
              return true;
            default:
              return super.onOptionsItemSelected(item);
          }
        }
        ...
      }
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      class MainActivity : AppCompatActivity() {

        ...
        var client: OkHttpClient = OkHttpClient();

            binding.fab.setOnClickListener { view ->

                NewRelic.pauseReplay()
                }
            }
          ...
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxe [#syntax]

      ### Objetivo-c [#objc]

      ### Swift [#swift]
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxe [#syntax]

      ```
      NewRelic.pauseReplay()
      ```

      ## Requisitos

      * React native v1.7.0 ou superior.

      ## Descrição

      `newrelic.pauseReplay()` pode ser chamado para forçar manualmente um replay para pausar a gravação.

      Se uma repetição ainda não estiver em andamento, este método de API não terá efeito.

      Se a sessão móvel ainda for válida, você pode retomar um replay pausado chamando o método de API [`newrelic.recordReplay()`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/record-replay).

      ## Casos de uso

      * Você deseja acionar manualmente uma repetição para interromper a gravação em determinadas páginas do site ou sob determinadas condições, como uma página de saúde, página de informações de cartão de crédito, etc.

      ## Exemplo [#example]

      Aqui está um exemplo de como forçar um replay a pausar a gravação.

      ```
      NewRelic.pauseReplay()
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>