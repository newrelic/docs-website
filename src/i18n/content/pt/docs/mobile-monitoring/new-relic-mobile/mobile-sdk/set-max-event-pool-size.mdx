---
title: Definir tamanho máximo do pool de eventos
type: apiDoc
shortDescription: Define o tamanho máximo do pool de eventos.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to set the maximum size of the event pool.
freshnessValidatedDate: '2023-07-20T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="flutter">
      Flutter
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unidade
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxe [#syntax]

      ### Java [#java]

      ```java
      NewRelic.setMaxEventPoolSize(int $maxSize)
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.setMaxEventPoolSize(maxSize: Int)
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos.

      Por padrão, <InlinePopover type="mobile" />coleta no máximo 1.000 eventos por ciclo de coleta de eventos, que dura 600 segundos por padrão. Este método controla o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. Quando o limite de tamanho do pool for atingido, o agente Android do New Relic começará [a amostrar evento](/docs/agents/manage-apm-agents/agent-data/new-relic-events-limits-sampling), descartando alguns eventos antigos e alguns novos, até que o pool de evento seja transmitido com o próximo ciclo de colheita. Este método permite substituir o tamanho máximo desse pool de eventos.

      O valor padrão para o ciclo de coleta de eventos é 600 segundos. Consulte [Definir tempo máximo de buffer de eventos](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-event-buffer-time) para alterar a duração do ciclo de coleta de eventos.

      <Callout variant="important">
        Esteja ciente de que relatar um grande número de eventos ou relatar eventos com muita frequência pode afetar o desempenho do aplicativo.
      </Callout>

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$maxSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              Obrigatório. Tamanho máximo do pool de eventos.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      Aqui está um exemplo para definir o tamanho máximo do pool de eventos como 1000:

      ### Java [#java]

      ```java
      NewRelic.setMaxEventPoolSize(1000);
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.setMaxEventPoolSize(1000)
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxe [#syntax]

      ### Objective-C [#objc]

      ```objectivec
      + (void) setMaxEventPoolSize:(unsigned int)size;
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.setMaxEventPoolSize(unsigned int $size)
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos.

      Por padrão, o agente iOS coleta no máximo 1.000 eventos por [ciclo de coleta](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#harvest-cycle). Este método controla o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. Quando o limite de tamanho do pool for atingido, o agente iOS do New Relic começará [a amostrar evento](/docs/agents/manage-apm-agents/agent-data/new-relic-events-limits-sampling), descartando alguns eventos antigos e alguns novos, até que o pool de eventos seja transmitido com o próximo ciclo de coleta. Este método permite substituir o tamanho máximo desse pool de eventos. Você deve definir esse valor depois que `Agent.start()` for chamado.

      O valor padrão para o ciclo de coleta de eventos é 600 segundos. Consulte também [Definir tempo máximo de buffer de eventos](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-event-buffer-time/) para alterar a duração do ciclo de coleta de eventos.

      Para garantir que o novo valor seja sempre aplicado, coloque esta chamada de API no método de ciclo de vida do iOS `applicationDidBecomeActive` .

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$size`
            </td>

            <td>
              `unsigned int`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplos [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      BOOL poolSizeSet = [NewRelic setMaxEventPoolSize:1000];
      ```

      ### Swift [#swift]

      ```swift
      let poolSizeSet = NewRelic.setMaxEventPoolSize(1000)
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Sintaxe [#syntax]

      ```typescript
      setMaxEventPoolSize(options: { maxPoolSize: number; }) => void
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxPoolSize`
            </td>

            <td>
              `number`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```typescript
      NewRelicCapacitorPlugin.setMaxEventPoolSize({ maxPoolSize: 2000 })
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Sintaxe [#syntax]

      ```typescript
      setMaxEventPoolSize(maxSize: number): void;
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxSize`
            </td>

            <td>
              `number`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```js
      NewRelic.setMaxEventPoolSize(2000);
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Sintaxe [#syntax]

      ```csharp
      SetMaxEventPoolSize(int maxPoolSize): void;
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxPoolSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```csharp
      CrossNewRelic.Current.SetMaxEventPoolSize(1500);
      ```
    </TabsPageItem>

    <TabsPageItem id="flutter">
      ## Sintaxe [#syntax]

      ```typescript
      setMaxEventPoolSize(int maxSize): void;
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```dart
      NewrelicMobile.instance.setMaxEventPoolSize(10000);
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxe [#syntax]

      ```js
      setMaxEventPoolSize(maxSize: number): void;
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxSize`
            </td>

            <td>
              `number`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```js
      NewRelic.setMaxEventPoolSize(2000);
      ```
    </TabsPageItem>

    <TabsPageItem id="unity">
      ## Sintaxe [#syntax]

      ```csharp
      SetMaxEventPoolSize(int maxPoolSize): void;
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxPoolSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```csharp
      NewRelicAgent.SetMaxEventPoolSize(1500);
      ```
    </TabsPageItem>

    <TabsPageItem id="unity">
      ## Sintaxe [#syntax]

      ```cpp
      setMaxEventPoolSize(int64 maxSize): void;
      ```

      ## Descrição [#description]

      Define o tamanho máximo do pool de eventos armazenado na memória até o próximo ciclo de coleta. O padrão é um máximo de 1.000 eventos por ciclo de coleta de eventos. Quando o limite de tamanho do pool for atingido, o agente iniciará a amostragem do evento, descartando alguns novos e antigos, até que o pool do evento seja enviado no próximo ciclo de coleta.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `maxSize`
            </td>

            <td>
              `int`
            </td>

            <td>
              Obrigatório. O número máximo de eventos que podem ser armazenados em buffer na memória antes de serem enviados para o New Relic.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```cpp
      #include "NewRelicBPLibrary.h"

      UNewRelicBPLibrary::setMaxEventPoolSize(2000);
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>