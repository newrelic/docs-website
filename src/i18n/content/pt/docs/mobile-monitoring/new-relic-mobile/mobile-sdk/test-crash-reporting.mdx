---
title: Relatório de falhas de teste
type: apiDoc
shortDescription: Lança uma exceção de tempo de execução de demonstração para testar o relatório de falhas do New Relic.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to throw a demo run-time exception to test New Relic crash reporting.
freshnessValidatedDate: '2023-07-20T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="flutter">
      Flutter
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unidade
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxe [#syntax]

      ### Java [#java]

      ```java
      NewRelic.crashNow(string $message)
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.crashNow(message:String!)
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração chamada java.lang.RuntimeException para testar o relatório de falhas do New Relic.

      Com esse método, você pode lançar uma exceção de tempo de execução de demonstração chamada `java.lang.RuntimeException` com uma mensagem. Esta falha aparecerá na [página Análise de falhas](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$message`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ### Java [#java]

      ```java
      NewRelic.crashNow("This is my test message");
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      dNewRelic.crashNow("This is my test message")
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxe [#syntax]

      ### Objetivo-c [#objc]

      ```objectivec
      + (void) crashNow:(NSString* __nullable)message;
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.crashNow()
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração chamada `NewRelicDemoException` para testar o relatório de falhas do New Relic.

      Com esse método, você pode lançar uma exceção de tempo de execução de demonstração chamada `NewRelicDemoException` com uma mensagem. Esta falha aparecerá [na página de análise de falha](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `message`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplos [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      [NewRelic crashNow:@"This is a test crash"];
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.crashNow("This is a test crash")
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Sintaxe [#syntax]

      ```typescript
      crashNow(options?: { message: string; } | undefined) => void
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `options`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```typescript
      NewRelicCapacitorPlugin.crashNow();
      NewRelicCapacitorPlugin.crashNow({ message: "A demo crash message" });
      ```
    </TabsPageItem>

    <TabsPageItem id="flutter">
      ## Sintaxe [#syntax]

      ```dart
      crashNow({String name}) : void ;
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração no Android/iOS para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```dart
            NewrelicMobile.instance.crashNow(name: "This is a crash");
            NewrelicMobile.instance.crashNow();

      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Sintaxe [#syntax]

      ```typescript
      crashNow(message?: string): void;
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `message`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```js
      NewRelic.crashNow();
      NewRelic.crashNow("New Relic example crash message");
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Sintaxe [#syntax]

      ```csharp
      CrashNow(string message = "") : void;
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração no Android/iOS para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `message`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```csharp
      CrossNewRelic.Current.CrashNow();
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxe [#syntax]

      ```ts
      crashNow(message?: string): void;
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `message`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```js
      NewRelic.crashNow();
      NewRelic.crashNow("New Relic example crash message");
      ```
    </TabsPageItem>

    <TabsPageItem id="unity">
      ## Sintaxe [#syntax]

      ```csharp
      CrashNow(string message = "") : void;
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração no Unity para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `message`
            </td>

            <td>
              `string`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```csharp
      NewRelicAgent.CrashNow("this is crash");
      ```
    </TabsPageItem>

    <TabsPageItem id="unreal">
      ## Sintaxe [#syntax]

      ```cpp
      crashNow(FString message): void;
      ```

      ## Descrição [#description]

      Lança uma exceção de tempo de execução de demonstração no Unreal para testar o relatório de falhas do New Relic.

      ## Parâmetro [#parameters]

      <table>
        <thead>
          <tr>
            <th width={200}>
              Parâmetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descrição
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `message`
            </td>

            <td>
              `FString`
            </td>

            <td>
              Opcional. Uma mensagem anexada à exceção.
            </td>
          </tr>
        </tbody>
      </table>

      ## Exemplo [#example]

      ```cpp
      #include "NewRelicBPLibrary.h"

      UNewRelicBPLibrary::crashNow("This is test");
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>