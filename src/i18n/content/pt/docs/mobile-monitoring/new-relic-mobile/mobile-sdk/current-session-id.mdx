---
title: ID da sessão atual
type: apiDoc
shortDescription: Retorna o ID da sessão atual.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to return the ID for current session.
freshnessValidatedDate: '2023-07-20T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="flutter">
      Flutter
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unidade
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxe [#syntax]

      ### Java [#java]

      ```java
      NewRelic.currentSessionId()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.currentSessionId()
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual.

      Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão. Por exemplo, você pode querer usar o mesmo identificador para análise de marketing ou análise de usuário.

      Compatível com todas as versões do agente.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ### Java [#java]

      ```java
      String sessionId = NewRelic.currentSessionId();
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      val sessionId = NewRelic.currentSessionId()
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxe [#syntax]

      ### Objetivo-c [#objc]

      ```objectivec
      + (NSString* _Null_unspecified) currentSessionId;
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.currentSessionId()
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão. Por exemplo, você pode querer usar o mesmo identificador para análise de marketing ou análise de usuário. Este ID de sessão mudará sempre que o aplicativo entrar em primeiro plano.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplos [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      NSString* sessionId = [NewRelic currentSessionId()];
      ```

      ### Swift [#swift]

      ```swift
      let sessionId = NewRelic.currentSessionId()
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Sintaxe [#syntax]

      ```typescript
      currentSessionId(options?: {} | undefined) => Promise<{ sessionId: string; }>
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```typescript
      let { sessionId } = await NewRelicCapacitorPlugin.currentSessionId();
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Sintaxe [#syntax]

      ```typescript
      currentSessionId(): Promise<sessionId>;
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```js
      let sessionId = await NewRelic.currentSessionId();
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Sintaxe [#syntax]

      ```csharp
      CurrentSessionId() : string;
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```csharp
      string sessionId = CrossNewRelic.Current.CurrentSessionId();
      ```
    </TabsPageItem>

    <TabsPageItem id="flutter">
      ## Sintaxe [#syntax]

      ```dart
      currentSessionId(): Promise;
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```dart
      var sessionId = await NewrelicMobile.instance.currentSessionId();
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxe [#syntax]

      ```js
      currentSessionId(): Promise;
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```js
      let sessionId = await NewRelic.currentSessionId();
      ```
    </TabsPageItem>

    <TabsPageItem id="unity">
      ## Sintaxe [#syntax]

      ```csharp
      currentSessionId() : string;
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```csharp
      string sessionId =  NewRelicAgent.CurrentSessionId();
      ```
    </TabsPageItem>

    <TabsPageItem id="unreal">
      ## Sintaxe [#syntax]

      ```cpp
      currentSessionId() : FString;
      ```

      ## Descrição [#description]

      Retorna o ID da sessão atual. Este método é útil para consolidar o monitoramento de dados de aplicativos (não apenas dados do New Relic) com base em uma única definição e identificador de sessão.

      ## Valores de retorno [#return-values]

      Retorna a string de ID da sessão atual.

      ## Exemplo [#example]

      ```cpp
      #include "NewRelicBPLibrary.h"

      FString id =  UNewRelicBPLibrary::currentSessionId();
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>