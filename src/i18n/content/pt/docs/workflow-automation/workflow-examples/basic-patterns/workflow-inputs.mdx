---
title: Utilizando entradas de fluxo de trabalho
tags:
  - workflow automation
  - workflow
  - inputs
  - variables
metaDescription: Pass dynamic values at runtime using workflow inputs in endpoint URLs and other input fields.
freshnessValidatedDate: never
translationType: machine
---

Use `${{ .workflowInputs.variableName }}` para passar valores dinâmicos em tempo de execução em URLs de endpoint e outros campos de entrada.

**O que essa abordagem faz:**

* Aceitar valores de entrada dinâmicos quando o fluxo de trabalho é acionado
* Faça referência aos valores de entrada usando a sintaxe de modelo em todas as etapas do fluxo de trabalho
* Habilite a reutilização do fluxo de trabalho com parâmetros diferentes

## Exemplo

```json
  {
    "inputs": [
      {
        "key": "urlParams",
        "value": "{\"filter\": \"active\"}"
      },
      {
        "key": "headers",
        "value": "{\"Api-Key\": \"your-api-key\"}"
      }
    ]
  }
```

## Tópicos relacionados [#related-topics]

<DocTiles>
  <DocTile title="Passagem de dados" path="/docs/workflow-automation/workflow-examples/basic-patterns/data-passing">
    Passar entradas entre etapas do fluxo de trabalho
  </DocTile>

  <DocTile title="API de início de execução de fluxo de trabalho" path="/docs/workflow-automation/workflow-automation-apis/start-workflow-run">
    Fornecer entradas ao iniciar fluxos de trabalho via API
  </DocTile>

  <DocTile title="Esquema de definição: Entradas do fluxo de trabalho" path="/docs/workflow-automation/workflow-automation-apis/definition-schema#workflowinputs">
    Referência completa de validação de entrada
  </DocTile>
</DocTiles>