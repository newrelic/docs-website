---
title: Ações do AWS CloudWatch
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS CloudWatch actions
metaDescription: A reference for AWS CloudWatch actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

Esta página fornece uma referência para as ações do AWS CloudWatch disponíveis no catálogo de ações de automação de fluxo de trabalho. Use estas ações para recuperar e fazer upload de dados de log no AWS CloudWatch.

## Pré-requisitos

Antes de usar as ações AWS na automação do fluxo de trabalho, certifique-se de ter:

* Uma conta da AWS com as permissões apropriadas.
* Credenciais da AWS configuradas (credenciais de usuário do IAM, ARN da função do IAM ou credenciais de sessão).
* As permissões IAM necessárias para os serviços específicos da AWS que você planeja usar.

Consulte [Configurar credenciais da AWS](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) para obter informações sobre como criar usuários e funções do IAM, e configurar credenciais da AWS estáticas e de sessão para integração com ações da AWS de automação de fluxo de trabalho.

## Obter evento de log

O identificador da ação é `aws.cloudwatch.getLogEvents`.

Recupera um lote de eventos de log de um fluxo de logs especificado no AWS CloudWatch Logs.

A tabela a seguir descreve os campos de entrada para esta ação.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Entrada**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descrição**
      </th>

      <th>
        **Exemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ARN da função do IAM a ser assumida para chamadas de API da AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ID da chave de acesso da AWS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Chave de acesso secreta da AWS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Token de sessão temporário para autenticação STS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. Região da AWS do grupo de logs de destino.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `logStreamName`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. Nome do fluxo de logs do qual recuperar eventos.
      </td>

      <td>
        `2023/10/27/[$LATEST]abcdef123456`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupName`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Nome do grupo de logs que contém o fluxo de logs.
      </td>

      <td>
        `/aws/lambda/my-function`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupIdentifier`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ARN ou nome do grupo de logs. Use em vez de 

        `logGroupName`

         ao referenciar um grupo de logs por ARN.
      </td>

      <td>
        `arn:partition:service:region:account-id:resource`
      </td>
    </tr>

    <tr>
      <td>
        `startTime`
      </td>

      <td>
        Interno
      </td>

      <td>
        Opcional. Início do intervalo de tempo, expresso como um timestamp Unix em milissegundos.
      </td>

      <td>
        `1759296000000`
      </td>
    </tr>

    <tr>
      <td>
        `endTime`
      </td>

      <td>
        Interno
      </td>

      <td>
        Opcional. Fim do intervalo de tempo, expresso como um timestamp Unix em milissegundos.
      </td>

      <td>
        `1759296000000`
      </td>
    </tr>

    <tr>
      <td>
        `limit`
      </td>

      <td>
        Interno
      </td>

      <td>
        Opcional. Número máximo de eventos de log a retornar.
      </td>

      <td>
        `50`
      </td>
    </tr>

    <tr>
      <td>
        `startFromHead`
      </td>

      <td>
        Boleano
      </td>

      <td>
        Opcional. Se 

        `true`

        , retorna os eventos de log mais antigos primeiro. O padrão é 

        `false`

        .
      </td>

      <td>
        `true`
      </td>
    </tr>

    <tr>
      <td>
        `unmask`
      </td>

      <td>
        Boleano
      </td>

      <td>
        Opcional. Se 

        `true`

        , retorna dados de evento de log não mascarados. O padrão é 

        `false`

        .
      </td>

      <td>
        `false`
      </td>
    </tr>

    <tr>
      <td>
        `nextToken`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Token de paginação de uma resposta anterior para recuperar a próxima página de resultados.
      </td>

      <td>
        `f/39218833627378687642013305455131706539523449361490509828/s`
      </td>
    </tr>
  </tbody>
</table>

A tabela a seguir descreve os campos de saída para esta ação.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saída**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Exemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        `{"events": [{"timestamp": 123, "message": "string", "ingestionTime": 123}], "nextForwardToken": "string", "nextBackwardToken": "string"}`
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Boleano
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Corda
      </td>

      <td>
        `"An error occurred (ExpiredTokenException) when calling the GetLogEvents operation: The security token included in the request is expired"`
      </td>
    </tr>
  </tbody>
</table>

O fluxo de trabalho de exemplo a seguir mostra como usar esta ação.

```yaml
name: get-lambda-logs
description: 'Retrieve log events from an AWS Lambda function'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: /aws/lambda/my-function
  logStreamName:
    type: String
    defaultValue: 2023/10/27/[$LATEST]abcdef123456
steps:
  - name: get_events_step
    type: action
    action: aws.cloudwatch.getLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      limit: 100
    next: end
```

## Fazer upload de eventos de log

O identificador da ação é `aws.cloudwatch.putLogEvents`.

Carrega um lote de eventos de log para o fluxo de logs especificado no AWS CloudWatch Logs.

A tabela a seguir descreve os campos de entrada para esta ação.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Entrada**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descrição**
      </th>

      <th>
        **Exemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ARN da função do IAM a ser assumida para chamadas de API da AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ID da chave de acesso da AWS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Chave de acesso secreta da AWS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Token de sessão temporário para autenticação STS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. Região da AWS do grupo de logs de destino.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupName`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. Nome do grupo de logs para o qual fazer o upload de eventos.
      </td>

      <td>
        `/aws/lambda/hello-you`
      </td>
    </tr>

    <tr>
      <td>
        `logStreamName`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. Nome do fluxo de logs para o qual carregar eventos.
      </td>

      <td>
        `2025/09/24/[$LATEST]09f7ca9e9ab044f389419ce60305f594`
      </td>
    </tr>

    <tr>
      <td>
        `logEvents`
      </td>

      <td>
        Lista
      </td>

      <td>
        Obrigatório. Lista de eventos de log para upload. Cada evento deve incluir um 

        `timestamp`

         (milissegundos Unix) e uma string 

        `message`

        .
      </td>

      <td>
        `[{"timestamp": 1698384000000, "message": "Workflow task started."}]`
      </td>
    </tr>

    <tr>
      <td>
        `entity`
      </td>

      <td>
        Dict
      </td>

      <td>
        Opcional. Metadados de entidade para associar aos eventos de log para correlação de entidade do New Relic.
      </td>

      <td>
        `{"keyAttributes": {"ResourceType": "AWS::ElasticLoadBalancingV2::LoadBalancer", "Identifier": "app/my-web-lb/12345", "Environment": "Production"}, "attributes": {"MonitoringTier": "Gold"}}`
      </td>
    </tr>
  </tbody>
</table>

A tabela a seguir descreve os campos de saída para esta ação.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saída**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Exemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        `{"nextSequenceToken": "string", "rejectedLogEventsInfo": {"tooNewLogEventStartIndex": 123, "tooOldLogEventEndIndex": 123, "expiredLogEventEndIndex": 123}}`
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Boleano
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Corda
      </td>

      <td>
        `"An error occurred (ExpiredTokenException) when calling the PutLogEvents operation: The security token included in the request is expired"`
      </td>
    </tr>
  </tbody>
</table>

O fluxo de trabalho de exemplo a seguir mostra como usar esta ação.

```yaml
name: put-custom-logs
description: 'Upload a batch of custom log events to CloudWatch'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: my-custom-app-logs
  logStreamName:
    type: String
    defaultValue: instance-01-thread-01
steps:
  - name: upload_logs
    type: action
    action: aws.cloudwatch.putLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      logEvents:
        - timestamp: 1698384000000
          message: "Workflow started successfully"
        - timestamp: 1698384005000
          message: "Step 1 completed"
    next: end
```