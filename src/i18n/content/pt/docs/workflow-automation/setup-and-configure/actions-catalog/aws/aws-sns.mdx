---
title: Ações do AWS SNS
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS SNS actions
metaDescription: A reference for AWS SNS actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

Esta página fornece uma referência para as ações do AWS SNS disponíveis no catálogo de ações de automação de fluxo de trabalho. Use essas ações para publicar mensagens em tópicos do Amazon SNS.

## Pré-requisitos

Antes de usar as ações AWS na automação do fluxo de trabalho, certifique-se de ter:

* Uma conta da AWS com as permissões apropriadas.
* Credenciais da AWS configuradas (credenciais de usuário do IAM, ARN da função do IAM ou credenciais de sessão).
* As permissões IAM necessárias para os serviços específicos da AWS que você planeja usar.

Consulte [Configurar credenciais da AWS](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) para obter informações sobre como criar usuários e funções do IAM, e configurar credenciais da AWS estáticas e de sessão para integração com ações da AWS de automação de fluxo de trabalho.

## Publicar uma mensagem em um tópico SNS

O identificador da ação é `aws.sns.publish`.

Envia uma mensagem para um tópico do Amazon SNS. Todos os assinantes do tópico receberão a mensagem.

A tabela a seguir descreve os campos de entrada para esta ação.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Entrada**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descrição**
      </th>

      <th>
        **Exemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ARN da função do IAM a ser assumida para chamadas de API da AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ID da chave de acesso da AWS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Chave de acesso secreta da AWS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Token de sessão temporário para autenticação STS. Passe como um segredo.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. Região da AWS do tópico do SNS de destino.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `message`
      </td>

      <td>
        Corda
      </td>

      <td>
        Obrigatório. A mensagem a ser publicada. As mensagens devem ser strings codificadas em UTF-8 e ter no máximo 256 KB de tamanho. 

        `PhoneNumber`

         é intencionalmente excluído desta ação porque é uma informação de identificação pessoal (PII). Para enviar um SMS direto, use o SDK da AWS diretamente.
      </td>

      <td>
        `"Workflow failed at step 3"`
      </td>
    </tr>

    <tr>
      <td>
        `topicArn`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ARN do tópico no qual publicar. É necessário especificar 

        `topicArn`

         ou 

        `targetArn`

        .
      </td>

      <td>
        `arn:aws:sns:us-east-2:123456789012:my-topic`
      </td>
    </tr>

    <tr>
      <td>
        `targetArn`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. ARN de destino para publicação. É necessário especificar 

        `topicArn`

         ou 

        `targetArn`

        .
      </td>

      <td>
        `arn:aws:sns:us-east-2:123456789012:MySNSTopic`
      </td>
    </tr>

    <tr>
      <td>
        `subject`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Assunto para assinaturas por e-mail.
      </td>

      <td>
        `"Workflow Update"`
      </td>
    </tr>

    <tr>
      <td>
        `MessageStructure`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Defina como 

        `json`

         para enviar mensagens diferentes para protocolos diferentes.
      </td>

      <td>
        `"json"`
      </td>
    </tr>

    <tr>
      <td>
        `messageAttributes`
      </td>

      <td>
        Mapa
      </td>

      <td>
        Opcional. Atributos da mensagem a serem anexados à mensagem.
      </td>

      <td>
        `{"string": {"DataType": "string", "StringValue": "string"}}`
      </td>
    </tr>

    <tr>
      <td>
        `messageDeduplicationId`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Token usado para desduplicação de mensagens em tópicos FIFO.
      </td>

      <td>
        `"abc123deduplicationId5678"`
      </td>
    </tr>

    <tr>
      <td>
        `messageGroupId`
      </td>

      <td>
        Corda
      </td>

      <td>
        Opcional. Tag que especifica o grupo de mensagens em um tópico FIFO.
      </td>

      <td>
        `"order-processing-group-2023_A"`
      </td>
    </tr>
  </tbody>
</table>

A tabela a seguir descreve os campos de saída para esta ação.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saída**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Exemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        `{"MessageId": "2333ededwedwed-52f5-a716-e10355e3e2ff"}`

        . Consulte a 

        [referência do publish do boto3](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/sns/client/publish.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Boleano
      </td>

      <td>
        `true | false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Corda
      </td>

      <td>
        `"An error occurred (InvalidParameter) when calling the Publish operation: Invalid parameter: TopicArn or TargetArn Reason: no value for required parameter"`
      </td>
    </tr>
  </tbody>
</table>

```yaml
name: sns-publish-test
description: Publishes a notification to an SNS topic
workflowInputs:
  arnRole:
    type: String
steps:
  - name: aws_sns_publish_1
    type: action
    action: aws.sns.publish
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      topicArn: arn:aws:sns:us-east-1:123456789012:workflow-test-topic
      subject: "Workflow Update"
      message: "The data processing workflow has completed successfully."
    next: end
```