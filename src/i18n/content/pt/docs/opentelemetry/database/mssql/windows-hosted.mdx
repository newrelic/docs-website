---
title: Monitoramento de MSSQL auto-hospedado no Windows com NRDOT
tags:
  - OpenTelemetry
  - NRDOT
  - SQL Server
  - Windows
metaDescription: Set up Microsoft SQL Server monitoring with NRDOT Collector on Windows self-hosted environments
freshnessValidatedDate: never
hideTOC: true
contentType: onboarding-intro
audience:
  - human
  - ai
surface:
  - concept
integration: Microsoft-SQL-Server-with-NRDOT
flow:
  prev: /docs/opentelemetry/database/mssql/introduction
  next: null
translationType: machine
---

<Callout title="Visualização">
  Ainda estamos trabalhando nesse recurso, mas adoraríamos que você experimentasse!

  Atualmente, esse recurso é fornecido como parte de uma prévia, de acordo com nossas [políticas de pré-lançamento](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

Configure o monitoramento do Microsoft SQL Server usando o NRDOT Collector em ambientes auto-hospedados do Windows, incluindo servidores físicos, máquinas virtuais e instalações autônomas do Windows. Para monitorar em um ambiente Windows RDS, consulte a [instrumentação do Windows RDS](/docs/opentelemetry/database/mssql/windows-rds).

Para instalar isso por meio da interface da New Relic, vá para <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; Integrations &amp; Agents &gt; MSSQL (OpenTelemetry)** </DNT>. Siga as instruções na tela para configurar a integração. Esta página abrange apenas o caminho de instalação da CLI.

Com base na sua configuração de segurança do SQL Server, você pode escolher entre três métodos de autenticação para conectar o NRDOT Collector à sua instância do SQL Server:

<Tabs>
  <TabsBar>
    <TabsBarItem id="sql">Autenticação do SQL Server</TabsBarItem> <TabsBarItem id="auth">Autenticação de domínio do Windows</TabsBarItem> <TabsBarItem id="gmsa">gMSA</TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="sql">
      <Steps>
        <Step>
          ## Pré-requisitos [#prerequisites]

          Antes de monitorar seu Microsoft SQL Server com o NRDOT, certifique-se de que seu ambiente atenda a estes requisitos:

          * Conta do New Relic com uma [chave de licença](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)válida

          * [Endpoint OTLP](/docs/opentelemetry/best-practices/opentelemetry-otlp) da New Relic para sua região

          * Requisitos do SQL Server, será necessário:

            * SQL Server 2017 ou posterior
            * Acesso administrativo ao SQL Server (função `sysadmin` ou equivalente)
            * Conectividade de rede entre o coletor e o SQL Server na porta `1433` ou porta personalizada
            * SQL Server Management Studio (SSMS) ou utilitário `sqlcmd`
            * Autenticação de domínio do Windows ou do SQL Server

          * Conectividade de rede para o [endpoint OTLP da New Relic](/docs/opentelemetry/best-practices/opentelemetry-otlp)
        </Step>

        <Step>
          ## Inscreva-se para visualizar [#enroll]

          Esta integração está disponível como parte do programa de visualização pública da New Relic. Entre em contato com o gerente da sua organização para aceitar na página [Previews &amp; Trials](https://one.newrelic.com/admin-portal/promotion-management/home).
        </Step>

        <Step>
          ## Instalar o NRDOT Collector [#setup-sql]

          Baixe e instale o NRDOT Collector usando o PowerShell:

          ```bash
          [Net.ServicePointManager]::SecurityProtocol = 'tls12, tls'; $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "$env:TEMP\nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i `"$env:TEMP\nrdot-collector.msi`" /qn /norestart /L*V `"$env:TEMP\nrdot_install.log`"" -Wait; Get-Service nrdot-collector -ErrorAction SilentlyContinue
          ```
        </Step>

        <Step>
          ## Criar usuário de monitoramento [#user-sql]

          Execute este script como usuário root `user/sysadmin` para criar o usuário de monitoramento `newrelic` e conceder as permissões necessárias para coletar métricas do SQL Server.

          Em seu SQL Server Management Studio (SSMS), execute o seguinte script para criar o usuário de monitoramento `newrelic`. Substitua `<YOUR_PASSWORD>` pela senha desejada:

          ```sql
          USE [master];
          GO
          CREATE LOGIN [newrelic] WITH PASSWORD = '<YOUR_PASSWORD>';
          GO

          GRANT VIEW SERVER STATE TO [newrelic];
          GRANT VIEW ANY DEFINITION TO [newrelic];
          GRANT VIEW ANY DATABASE TO [newrelic];
          GO

          DECLARE @name SYSNAME;
          DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
          SELECT [name]
          FROM [master].[sys].[databases]
          WHERE [name] NOT IN ('master', 'msdb', 'model', 'rdsadmin', 'distribution')
          AND [state] = 0;
          OPEN db_cursor;
          FETCH NEXT FROM db_cursor INTO @name;
          WHILE @@FETCH_STATUS = 0
          BEGIN
            BEGIN TRY
              EXEC('USE [' + @name + '];
              IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''newrelic'')
              BEGIN
                CREATE USER [newrelic] FOR LOGIN [newrelic];
              END;
              GRANT VIEW DATABASE STATE TO [newrelic];');
            END TRY
            BEGIN CATCH
              PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
            END CATCH
            FETCH NEXT FROM db_cursor INTO @name;
          END
          CLOSE db_cursor;
          DEALLOCATE db_cursor;
          GO
          ```

          <Callout variant="tip">
            Para gerenciar informações confidenciais com segurança, como credenciais de banco de dados, armazene-as em ferramentas de [gerenciamento de segredos](/docs/opentelemetry/database/capabilities/db-apm/#secret-management).
          </Callout>
        </Step>

        <Step>
          ## Configurar o NRDOT Collector [#configure-sql]

          1. Crie um arquivo de configuração nomeado como `mssql-config.yaml` no PowerShell como administrador:

          ```bash
          New-Item -Path "C:\Program Files\nrdot-collector\mssql-config.yaml" -ItemType File
          ```

          2. Adicione seus valores específicos do ambiente à seguinte configuração e copie-a para o arquivo `mssql-config.yaml` criado na etapa anterior:

          <Callout variant="tip" title="configuração completa">
            Esta configuração foca no monitoramento essencial do banco de dados para o seu ambiente SQL Server. Para habilitar o monitoramento abrangente com todas as métricas disponíveis, consulte a [referência de configuração](/docs/opentelemetry/database/mssql/config-reference/#hosted-sql).
          </Callout>

          <OtelConfig
            inputOptions={[{"name":"username","label":"Nome de usuário do SQL Server","codeLine":208,"defaultValue":"newrelic","toolTip":"Insira seu nome de usuário do SQL Server"},{"name":"password","label":"Senha do SQL Server","codeLine":209,"defaultValue":"YOUR_PASSWORD","toolTip":"Insira a senha do seu SQL Server"},{"name":"server","label":"Nome do host/IP do servidor","codeLine":210,"defaultValue":"sqlserver.example.com","toolTip":"Insira o nome do host ou endereço IP do SQL Server"},{"name":"port","label":"Porta do servidor","codeLine":211,"defaultValue":"1433","toolTip":"Insira o número da porta do SQL Server. O valor padrão é 1433."},{"name":"endpoint","label":"Endpoint New Relic OTLP","codeLine":269,"defaultValue":"https://otlp.nr-data.net","toolTip":"Insira o seu endpoint OTLP da New Relic. Para obter mais informações, consulte https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp/"},{"name":"licenseKey","label":"Chave de licença da New Relic","codeLine":271,"defaultValue":"YOUR_LICENSE_KEY","toolTip":"Insira sua chave de licença de ingestão da New Relic"}]}
            config={`receivers:
            nrsqlserver:
             collection_interval: 15s
             username: <input1>
             password: <input2>
             server: <input3>
             port: <input4>

             # Enable comprehensive database metrics
             metrics:
               sqlserver.database.count:
                 enabled: true
               sqlserver.database.io:
                 enabled: true
               sqlserver.database.latency:
                 enabled: true
               sqlserver.database.operations:
                 enabled: true
               sqlserver.database.tempdb.space:
                 enabled: true
               sqlserver.database.tempdb.version_store.size:
                 enabled: true
               sqlserver.deadlock.rate:
                 enabled: true
               sqlserver.os.wait.duration:
                 enabled: true
               sqlserver.processes.blocked:
                 enabled: true
               sqlserver.memory.grants.pending.count:
                 enabled: true
               sqlserver.database.file.size:
                 enabled: true
               sqlserver.memory.area:
                 enabled: true

             # Enable top query and query sample log collection
             events:
               db.server.query_sample:
                 enabled: true
               db.server.top_query:
                 enabled: true

             # Top query collection configuration
             top_query_collection:
               lookback_time: 60s
               max_query_sample_count: 1000
               top_query_count: 250
               collection_interval: 60s

             collect_full_query_text: true
             allowed_comment_keys:
               - nr_service_guid

             # Query sample collection configuration
             query_sample_collection:
               max_rows_per_query: 100

            processors:
            memory_limiter:
             check_interval: \${env:NR_MEM_LIMITER_CHECK_INTERVAL:-1s}
             limit_mib:       \${env:NR_MEM_LIMITER_LIMIT_MIB:-200}
             spike_limit_mib: \${env:NR_MEM_LIMITER_SPIKE_MIB:-50}

            batch:

            exporters:
            otlp:
             endpoint: <input5>:4318
             headers:
               api-key: <input6>
             tls:
               insecure: false
             compression: gzip

            service:
            telemetry:
             metrics:
               level: none

            pipelines:
             metrics:
               receivers: [nrsqlserver]
               processors: [memory_limiter, batch]
               exporters: [otlp]

             logs:
               receivers: [nrsqlserver]
               processors: [memory_limiter, batch]
               exporters: [otlp]`}
            fileName="mssql-config.yaml"
            tipMdx={{ body: null }}
            onChange={() => {}}
          />
        </Step>

        <Step>
          ## Atualizar Registro de Serviço do Windows [#update-sql]

          1. Atualize o Registro de Serviço do Windows para usar o novo arquivo `mssql-config.yaml` usando um dos seguintes métodos:

             * **Para PowerShell:** execute o seguinte comando como administrador:

               ```bash
               Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\nrdot-collector" -Name "ImagePath" -Value '"C:\Program Files\nrdot-collector\nrdot-collector.exe" --config "C:\Program Files\nrdot-collector\mssql-config.yaml"'
               ```

             * **Para prompt de comando:** se o PowerShell não estiver disponível, use o prompt de comando e execute o seguinte comando como administrador:

             ```bash
             sc config "nrdot-collector" binPath= "\"C:\Program Files\nrdot-collector\nrdot-collector.exe\" --config \"C:\Program Files\nrdot-collector\mssql-config.yaml\""
             ```

          2. Valide a configuração do Collector NRDOT para garantir que esteja formatada corretamente e funcione de maneira adequada.

             ```bash
             & "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\mssql-config.yaml"
             ```

             <Callout variant="tip">
               Para correlacionar o desempenho do aplicativo com as operações de banco de dados, é possível configurar a identificação de serviço de banco de dados. Para mais informações, consulte [configurar a identificação de serviço de banco de dados](/docs/opentelemetry/database/capabilities/db-apm/).
             </Callout>
        </Step>

        <Step>
          ## Reiniciar o NRDOT Collector [#restart-sql]

          Após atualizar sua configuração, reinicie o serviço NRDOT Collector:

          ```bash
          net stop nrdot-collector
          net start nrdot-collector
          ```

          <Callout variant="tip">
            Sempre reinicie o serviço do NRDOT Collector após fazer alterações na configuração para garantir que as novas configurações entrem em vigor.
          </Callout>
        </Step>

        <Step>
          ## Encontre e use seus dados [#find]

          Assim que seus dados estiverem sendo coletados, você poderá acessar o monitoramento abrangente do banco de dados SQL Server por meio da interface do New Relic.

          Para encontrar sua entidade de banco de dados SQL Server no New Relic:

          1. Vá para <DNT>**[https://one.newrelic.com](https://one.newrelic.com) &gt; All Capabilities &gt; Databases**</DNT>.

          2. Na dropdown **<DNT>Entity type</DNT>**, selecione <DNT>**MSSQL instance**</DNT> e clique em <DNT>**Apply**</DNT>.

          3. Selecione seu banco de dados SQL Server na lista de entidades.

             Após configurar o monitoramento do SQL Server com o NRDOT, você pode:

             * [Crie dashboards personalizados](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) para visualizar suas métricas de banco de dados
             * [Configure alertas](/docs/alerts/create-alert/create-alert-condition/alert-conditions/) para limites críticos de desempenho do banco de dados
             * [Explore seus dados](/docs/query-your-data/explore-query-data/browse-data/introduction-data-explorer/) usando os recursos de consulta do New Relic
        </Step>
      </Steps>
    </TabsPageItem>

    <TabsPageItem id="auth">
      <Steps>
        <Step>
          ## Pré-requisitos [#prerequisites-domain]

          Antes de monitorar seu Microsoft SQL Server com NRDOT usando a Autenticação do Windows, certifique-se de que seu ambiente atenda a estes requisitos:

          * Conta do New Relic com uma [chave de licença](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)válida

          * [Endpoint OTLP](/docs/opentelemetry/best-practices/opentelemetry-otlp) da New Relic para sua região

          * Requisitos do SQL Server, será necessário:

            * SQL Server 2017 ou posterior
            * Acesso administrativo ao SQL Server (função `sysadmin` ou equivalente)
            * Conectividade de rede entre o coletor e o SQL Server na porta `1433` ou porta personalizada
            * SQL Server Management Studio (SSMS) ou utilitário `sqlcmd`

          * Ambiente de domínio do Windows com Active Directory

          * SQL Server configurado para aceitar a autenticação do Windows

          * Conta de usuário de domínio com as permissões apropriadas do SQL Server

          * Conectividade de rede para o [endpoint OTLP da New Relic](/docs/opentelemetry/best-practices/opentelemetry-otlp)
        </Step>

        <Step>
          ## Inscreva-se para visualizar [#enroll-domain]

          Esta integração está disponível como parte do programa de visualização pública da New Relic. Entre em contato com o gerente da sua organização para aceitar na página [Previews &amp; Trials](https://one.newrelic.com/admin-portal/promotion-management/home).
        </Step>

        <Step>
          ## Instalar o NRDOT Collector [#setup-domain]

          Baixe e instale o NRDOT Collector usando o PowerShell:

          ```bash
          [Net.ServicePointManager]::SecurityProtocol = 'tls12, tls'; $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "$env:TEMP\nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i `"$env:TEMP\nrdot-collector.msi`" /qn /norestart /L*V `"$env:TEMP\nrdot_install.log`"" -Wait; Get-Service nrdot-collector -ErrorAction SilentlyContinue
          ```
        </Step>

        <Step>
          ## Criar usuário de monitoramento [#user-domain]

          Certifique-se de que seu SQL Server esteja configurado para Autenticação do Windows e verifique a conectividade.

          <CollapserGroup>
            <Collapser id="same-host" title="Collector no mesmo host do Windows">
              1. Recupere o seu nome de usuário do Windows:

                 ```sql
                 SELECT SYSTEM_USER;
                 ```

              2. Execute o seguinte comando para verificar se as permissões necessárias foram concedidas ao seu usuário do Windows. Substitua todas as ocorrências de `<ACCOUNT_USERNAME>` pelo nome de usuário recuperado da etapa 1:

                 ```sql
                 DECLARE @TargetUser SYSNAME = '<ACCOUNT_USERNAME>'; -- <-- Set your Windows user here

                 -- 1. Create the checklist of required permissions from your script
                 DECLARE @Checklist TABLE (Scope NVARCHAR(50), [Database] SYSNAME, [Permission] NVARCHAR(128));

                 INSERT INTO @Checklist VALUES
                 ('Server', 'ALL', 'VIEW SERVER STATE'),
                 ('Server', 'ALL', 'VIEW ANY DEFINITION'),
                 ('Server', 'ALL', 'VIEW ANY DATABASE');

                 INSERT INTO @Checklist
                 SELECT 'Database Role', [name], 'db_datareader' FROM sys.databases WHERE [name] NOT IN ('master', 'msdb', 'model', 'rdsadmin', 'distribution') AND [state] = 0
                 UNION ALL
                 SELECT 'Database Explicit', [name], 'VIEW DATABASE STATE' FROM sys.databases WHERE [name] NOT IN ('master', 'msdb', 'model', 'rdsadmin', 'distribution') AND [state] = 0
                 UNION ALL
                 SELECT 'Database Explicit', [name], 'VIEW DEFINITION' FROM sys.databases WHERE [name] NOT IN ('master', 'msdb', 'model', 'rdsadmin', 'distribution') AND [state] = 0;

                 -- 2. Gather existing explicit permissions
                 WITH ExplicitPerms AS (
                     SELECT 'Server' AS Scope, CAST('ALL' AS NVARCHAR(128)) COLLATE DATABASE_DEFAULT AS [Database], p.permission_name COLLATE DATABASE_DEFAULT AS [Permission]
                     FROM sys.server_permissions p JOIN sys.server_principals l ON p.grantee_principal_id = l.principal_id WHERE l.name = @TargetUser
                     UNION ALL
                     SELECT 'Database Role', d.name COLLATE DATABASE_DEFAULT, r.name COLLATE DATABASE_DEFAULT
                     FROM sys.databases d CROSS APPLY (SELECT dp_role.name FROM sys.database_role_members drm JOIN sys.database_principals dp_role ON drm.role_principal_id = dp_role.principal_id JOIN sys.database_principals dp_user ON drm.member_principal_id = dp_user.principal_id JOIN sys.server_principals sp ON dp_user.sid = sp.sid WHERE sp.name = @TargetUser) r WHERE d.state = 0
                     UNION ALL
                     SELECT 'Database Explicit', d.name COLLATE DATABASE_DEFAULT, p.permission_name COLLATE DATABASE_DEFAULT
                     FROM sys.databases d CROSS APPLY (SELECT db_p.permission_name FROM sys.database_permissions db_p JOIN sys.database_principals dp_user ON db_p.grantee_principal_id = dp_user.principal_id JOIN sys.server_principals sp ON dp_user.sid = sp.sid WHERE sp.name = @TargetUser) p WHERE d.state = 0
                 )
                 -- 3. Output the exact verification matrix
                 SELECT
                     chk.Scope,
                     chk.[Database],
                     chk.[Permission] AS [Required Permission],
                     CASE
                         WHEN exp.[Permission] IS NOT NULL THEN 'GRANTED'
                         WHEN IS_SRVROLEMEMBER('sysadmin', @TargetUser) = 1 THEN 'PASSED (via SysAdmin Role)'
                         ELSE 'MISSING'
                     END AS [Verification Status]
                 FROM @Checklist chk
                 LEFT JOIN ExplicitPerms exp ON chk.Scope = exp.Scope AND chk.[Database] = exp.[Database] AND chk.[Permission] = exp.[Permission]
                 ORDER BY chk.Scope, chk.[Database], chk.[Permission];
                 ```

              3. (Opcional) Se você não tiver as permissões necessárias, no seu SSMS ou utilitário `sqlcmd`, execute o seguinte comando para conceder as permissões necessárias à sua conta do Windows. Substitua todas as ocorrências de `<ACCOUNT_USERNAME>` pelo seu nome de usuário do Windows recuperado na etapa 1:

                 ```sql
                 USE [master];
                 GO

                 -- 1. Grant Instance-level permissions to your Windows Account
                 GRANT VIEW SERVER STATE TO [<ACCOUNT_USERNAME>];
                 GRANT VIEW ANY DEFINITION TO [<ACCOUNT_USERNAME>];
                 GRANT VIEW ANY DATABASE TO [<ACCOUNT_USERNAME>];
                 GO

                 -- 2. Loop through all user databases and grant read/visibility permissions
                 DECLARE @name SYSNAME;
                 DECLARE @sql NVARCHAR(MAX);

                 DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
                 SELECT [name]
                 FROM [master].[sys].[databases]
                 WHERE [name] NOT IN ('master', 'msdb', 'model', 'rdsadmin', 'distribution')
                   AND [state] = 0; -- Only online databases

                 OPEN db_cursor;
                 FETCH NEXT FROM db_cursor INTO @name;

                 WHILE @@FETCH_STATUS = 0
                 BEGIN
                     BEGIN TRY
                         PRINT 'Granting permissions on database: ' + @name;

                         -- Create the database user mapping if it doesn't exist, then grant roles
                         SET @sql = '
                             USE [' + @name + '];
                             IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''<ACCOUNT_USERNAME>'')
                             BEGIN
                                 CREATE USER [<ACCOUNT_USERNAME>] FOR LOGIN [<ACCOUNT_USERNAME>];
                             END;
                             GRANT VIEW DATABASE STATE TO [<ACCOUNT_USERNAME>];
                             GRANT VIEW DEFINITION TO [<ACCOUNT_USERNAME>];
                             ALTER ROLE db_datareader ADD MEMBER [<ACCOUNT_USERNAME>];';

                         EXEC sp_executesql @sql;
                         PRINT 'Success: ' + @name;
                     END TRY
                     BEGIN CATCH
                         PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
                     END CATCH

                     FETCH NEXT FROM db_cursor INTO @name;
                 END

                 CLOSE db_cursor;
                 DEALLOCATE db_cursor;
                 GO
                 ```
            </Collapser>

            <Collapser id="different-host" title="Collector em um host diferente do Windows">
              1. No seu SQL Server Management Studio (SSMS), execute o seguinte script para criar um login para a sua conta gMSA e conceder as permissões necessárias. Substitua todas as ocorrências de `<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>` pelo seu domínio e nome de conta reais.

              ```sql
              USE [master];
              GO

              CREATE LOGIN [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>] FROM WINDOWS;
              GO

              GRANT VIEW ANY DATABASE   TO [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>];
              GRANT VIEW SERVER STATE TO [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>];
              GRANT VIEW ANY DEFINITION TO [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>];
              GO

              -- Grant access to all databases
              DECLARE @name SYSNAME;
              DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
                SELECT [name] FROM [master].[sys].[databases]
                WHERE [name] NOT IN ('master','msdb','model','rdsadmin','distribution')
                  AND [state] = 0;
              OPEN db_cursor;
              FETCH NEXT FROM db_cursor INTO @name;
              WHILE @@FETCH_STATUS = 0
              BEGIN
                BEGIN TRY
                  EXEC('USE [' + @name + '];
                  IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>'')
                  BEGIN
                    CREATE USER [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>] FOR LOGIN [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>];
                  END;
                  GRANT VIEW DATABASE STATE TO [<YOUR_DOMAIN>\<YOUR_DOMAIN_USER>];');
                END TRY
                BEGIN CATCH
                  PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
                END CATCH
                FETCH NEXT FROM db_cursor INTO @name;
              END
              CLOSE db_cursor;
              DEALLOCATE db_cursor;
              GO
              ```

              2. Configure o serviço NRDOT Collector para ser executado como seu usuário de domínio usando o PowerShell. Substitua `YOUR_DOMAIN\YOUR_USER_ACCOUNT` pelo seu domínio e nome de conta:

                 ```bash
                 # Configure service to use domain account
                 # The space after obj= and password= is required syntax for sc.exe
                 sc.exe config "nrdot-collector" obj= "YOUR_DOMAIN\YOUR_USER_ACCOUNT" password= "<YOUR_PASSWORD>"

                 # Restart the service
                 Restart-Service nrdot-collector
                 ```

              3. Valide a identidade do serviço:

                 ```bash
                 Get-WmiObject Win32_Service -Filter "Name='nrdot-collector'" | Select Name, State, StartName
                 # Expected: StartName = YOUR_DOMAIN\YOUR_USER_ACCOUNT
                 ```
            </Collapser>
          </CollapserGroup>

          <Callout variant="tip">
            Para gerenciar informações confidenciais com segurança, como credenciais de banco de dados, armazene-as em ferramentas de [gerenciamento de segredos](/docs/opentelemetry/database/capabilities/db-apm/#secret-management).
          </Callout>
        </Step>

        <Step>
          ## Configurar o NRDOT Collector [#configure-domain]

          1. Crie um arquivo de configuração nomeado como `mssql-config.yaml` no PowerShell como administrador:

          ```bash
          New-Item -Path "C:\Program Files\nrdot-collector\mssql-config.yaml" -ItemType File
          ```

          2. Adicione a seguinte configuração ao arquivo `mssql-config.yaml` criado na etapa anterior:

          <Callout variant="tip" title="configuração completa">
            Esta configuração se concentra no monitoramento essencial de banco de dados para o seu ambiente do Windows SQL Server. Para habilitar o monitoramento abrangente com toda a métrica disponível, consulte a [referência de configuração](/docs/opentelemetry/database/mssql/config-reference/#hosted-domain).
          </Callout>

          <OtelConfig
            inputOptions={[{"name":"hostname","label":"Nome do host/IP do SQL Server","codeLine":589,"defaultValue":"sqlserver.example.com","toolTip":"Insira o nome do host ou endereço IP do SQL Server"},{"name":"port","label":"Porta do SQL Server","codeLine":589,"defaultValue":"1433","toolTip":"Insira o número da porta do SQL Server. O valor padrão é 1433."},{"name":"endpoint","label":"Endpoint New Relic OTLP","codeLine":650,"defaultValue":"https://otlp.nr-data.net","toolTip":"Insira o seu endpoint OTLP da New Relic. Para obter mais informações, consulte https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp/"},{"name":"licenseKey","label":"Chave de licença da New Relic","codeLine":652,"defaultValue":"YOUR_LICENSE_KEY","toolTip":"Insira sua chave de licença de ingestão da New Relic"}]}
            config={`receivers:
            nrsqlserver:
             collection_interval: 15s
             datasource: "server=<input1>;port=<input2>;integrated security=true;encrypt=true;TrustServerCertificate=true;"

             # Enable comprehensive database metrics
             metrics:
               sqlserver.database.count:
                 enabled: true
               sqlserver.database.io:
                 enabled: true
               sqlserver.database.latency:
                 enabled: true
               sqlserver.database.operations:
                 enabled: true
               sqlserver.database.tempdb.space:
                 enabled: true
               sqlserver.database.tempdb.version_store.size:
                 enabled: true
               sqlserver.deadlock.rate:
                 enabled: true
               sqlserver.os.wait.duration:
                 enabled: true
               sqlserver.processes.blocked:
                 enabled: true
               sqlserver.memory.grants.pending.count:
                 enabled: true
               sqlserver.database.file.size:
                 enabled: true
               sqlserver.memory.area:
                 enabled: true

             # Enable top query and query sample log collection
             events:
               db.server.query_sample:
                 enabled: true
               db.server.top_query:
                 enabled: true

             # Top query collection configuration
             top_query_collection:
               lookback_time: 60s
               max_query_sample_count: 1000
               top_query_count: 250
               collection_interval: 60s

             collect_full_query_text: true
             allowed_comment_keys:
               - nr_service_guid

             # Query sample collection configuration
             query_sample_collection:
               max_rows_per_query: 100

            processors:
            memory_limiter:
             check_interval: \${env:NR_MEM_LIMITER_CHECK_INTERVAL:-1s}
             limit_mib:       \${env:NR_MEM_LIMITER_LIMIT_MIB:-200}
             spike_limit_mib: \${env:NR_MEM_LIMITER_SPIKE_MIB:-50}

            batch:

            exporters:
            otlp:
             endpoint: <input3>:4318
             headers:
               api-key: <input4>
             tls:
               insecure: false
             compression: gzip

            service:
            telemetry:
             metrics:
               level: none

            pipelines:
             metrics:
               receivers: [nrsqlserver]
               processors: [memory_limiter, batch]
               exporters: [otlp]

             logs:
               receivers: [nrsqlserver]
               processors: [memory_limiter, batch]
               exporters: [otlp]`}
            fileName="mssql-config.yaml"
            tipMdx={{ body: null }}
            onChange={() => {}}
          />

          <Callout variant="tip">
            Esta configuração se concentra no monitoramento essencial de banco de dados para o seu ambiente de autenticação do Windows. Para habilitar o monitoramento abrangente com todas as métricas disponíveis, consulte a [referência de configuração](/docs/opentelemetry/database/mssql/config-reference/#hosted-gmsa).
          </Callout>
        </Step>

        <Step>
          ## Atualizar Registro de Serviço do Windows [#update-registry-domain]

          1. Atualize o Registro de Serviço do Windows para usar o novo arquivo `mssql-config.yaml` usando um dos seguintes métodos:

             * **Para PowerShell:** execute o seguinte comando como administrador:

               ```bash
               Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\nrdot-collector" -Name "ImagePath" -Value '"C:\Program Files\nrdot-collector\nrdot-collector.exe" --config "C:\Program Files\nrdot-collector\mssql-config.yaml"'
               ```

             * **Para prompt de comando:** se o PowerShell não estiver disponível, use o prompt de comando e execute o seguinte comando como administrador:

             ```bash
             sc config "nrdot-collector" binPath= "\"C:\Program Files\nrdot-collector\nrdot-collector.exe\" --config \"C:\Program Files\nrdot-collector\mssql-config.yaml\""
             ```

          2. Valide a configuração do Collector NRDOT para garantir que esteja formatada corretamente e funcione adequadamente:

             ```bash
             & "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\mssql-config.yaml"
             ```

             <Callout variant="tip">
               Para correlacionar o desempenho do aplicativo com as operações de banco de dados, é possível configurar a identificação de serviço de banco de dados. Para mais informações, consulte [configurar a identificação de serviço de banco de dados](/docs/opentelemetry/database/capabilities/db-apm/).
             </Callout>
        </Step>

        <Step>
          ## Reiniciar o NRDOT Collector [#restart-domain]

          Após atualizar sua configuração, reinicie o serviço NRDOT Collector:

          ```bash
          net stop nrdot-collector
          net start nrdot-collector
          ```

          <Callout variant="tip">
            Sempre reinicie o serviço do NRDOT Collector após fazer alterações na configuração para garantir que as novas configurações entrem em vigor.
          </Callout>
        </Step>

        <Step>
          ## Encontre e use seus dados [#find-domain]

          Assim que seus dados estiverem sendo coletados, você poderá acessar o monitoramento abrangente do banco de dados SQL Server por meio da interface do New Relic.

          Para encontrar sua entidade de banco de dados SQL Server no New Relic:

          1. Vá para <DNT>**[https://one.newrelic.com](https://one.newrelic.com) &gt; All Capabilities &gt; Databases**</DNT>.

          2. Na dropdown **<DNT>Entity type</DNT>**, selecione <DNT>**MSSQL instance**</DNT> e clique em <DNT>**Apply**</DNT>.

          3. Selecione seu banco de dados SQL Server na lista de entidades.

             Após configurar o monitoramento do SQL Server com o NRDOT, você pode:

             * [Crie dashboards personalizados](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) para visualizar suas métricas de banco de dados
             * [Configure alertas](/docs/alerts/create-alert/create-alert-condition/alert-conditions/) para limites críticos de desempenho do banco de dados
             * [Explore seus dados](/docs/query-your-data/explore-query-data/browse-data/introduction-data-explorer/) usando os recursos de consulta do New Relic
        </Step>
      </Steps>
    </TabsPageItem>

    <TabsPageItem id="gmsa">
      <Steps>
        <Step>
          ## Pré-requisitos [#prerequisites-gmsa]

          Antes de monitorar seu Microsoft SQL Server com o NRDOT usando a Autenticação gMSA, certifique-se de que seu ambiente atenda a estes requisitos:

          * Conta do New Relic com uma [chave de licença](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)válida

          * [Endpoint OTLP](/docs/opentelemetry/best-practices/opentelemetry-otlp) da New Relic para sua região

          * Requisitos do SQL Server, será necessário:

            * SQL Server 2017 ou posterior
            * Acesso administrativo ao SQL Server (função `sysadmin` ou equivalente)
            * Conectividade de rede entre o coletor e o SQL Server para a porta 1433 ou porta personalizada
            * SQL Server Management Studio (SSMS) ou utilitário `sqlcmd`

          * Ambiente do Active Directory com suporte a gMSA

          * O host do Windows deve estar associado ao domínio e autorizado a usar a conta gMSA

          * Conectividade de rede para o [endpoint OTLP da New Relic](/docs/opentelemetry/best-practices/opentelemetry-otlp)
        </Step>

        <Step>
          ## Inscreva-se para visualizar [#enroll-gmsa]

          Esta integração está disponível como parte do programa de visualização pública da New Relic. Entre em contato com o gerente da sua organização para aceitar na página [Previews &amp; Trials](https://one.newrelic.com/admin-portal/promotion-management/home).
        </Step>

        <Step>
          ## Instalar o NRDOT Collector [#setup-gmsa]

          Baixe e instale o NRDOT Collector usando o PowerShell:

          ```bash
          [Net.ServicePointManager]::SecurityProtocol = 'tls12, tls'; $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "$env:TEMP\nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i `"$env:TEMP\nrdot-collector.msi`" /qn /norestart /L*V `"$env:TEMP\nrdot_install.log`"" -Wait; Get-Service nrdot-collector -ErrorAction SilentlyContinue
          ```
        </Step>

        <Step>
          ## Criar usuário de monitoramento [#user-gmsa]

          No seu SQL Server Management Studio (SSMS), execute o seguinte script para criar um login para a sua conta gMSA e conceder as permissões necessárias. Substitua todas as ocorrências de `<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>` pelo seu domínio real e nome da conta gMSA.

          ```sql
          USE [master];
          GO

          -- Provision an engine login identity for the gMSA
          CREATE LOGIN [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$] FROM WINDOWS;
          GO

          -- Grant monitoring permissions
          GRANT VIEW SERVER STATE TO [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$];
          GRANT VIEW ANY DEFINITION TO [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$];
          GRANT VIEW ANY DATABASE TO [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$];
          GO

          -- Grant access to all databases (required for query monitoring and tempdb metrics)
          DECLARE @name SYSNAME;
          DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
          SELECT [name] FROM [master].[sys].[databases]
          WHERE [name] NOT IN ('master','msdb','model','rdsadmin','distribution')
          AND [state] = 0;
          OPEN db_cursor;
          FETCH NEXT FROM db_cursor INTO @name;
          WHILE @@FETCH_STATUS = 0
          BEGIN
            BEGIN TRY
              EXEC('USE [' + @name + '];
              IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$'')
              BEGIN
                CREATE USER [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$] FOR LOGIN [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$];
              END;
              GRANT VIEW DATABASE STATE TO [<YOUR_DOMAIN>\<YOUR_GMSA_USERNAME>$];');
            END TRY
            BEGIN CATCH
              PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
            END CATCH
            FETCH NEXT FROM db_cursor INTO @name;
          END
          CLOSE db_cursor;
          DEALLOCATE db_cursor;
          GO
          ```

          <Callout variant="tip">
            Para gerenciar informações confidenciais com segurança, como credenciais de banco de dados, armazene-as em ferramentas de [gerenciamento de segredos](/docs/opentelemetry/database/capabilities/db-apm/#secret-management).
          </Callout>
        </Step>

        <Step>
          ## Executar como conta gMSA [#service-gmsa]

          Após instalar o NRDOT Collector, configure o serviço para ser executado como sua conta gMSA usando o PowerShell. Substitua `YOUR_DOMAIN\YOUR_gMSA_ACCOUNT$` pelo seu domínio e nome da conta gMSA. Você deve incluir o `$` no final, pois é necessário para contas gMSA.

          ```bash
          # Stop the NRDOT Collector service
          Stop-Service nrdot-collector

          # Configure service to use gMSA account
          # Use cmd /c to avoid PowerShell treating $ as a variable
          cmd /c 'sc config nrdot-collector obj= "YOUR_DOMAIN\YOUR_gMSA_ACCOUNT$" password= ""'

          # Verify the configuration
          Get-WmiObject Win32_Service -Filter "Name='nrdot-collector'" | Select Name, StartName

          # start the NRDOT Collector service
          Start-Service nrdot-collector
          ```
        </Step>

        <Step>
          ## Configurar o NRDOT Collector [#configure-gmsa]

          1. Crie um arquivo de configuração nomeado como `mssql-config.yaml` no PowerShell como administrador:

          ```bash
          New-Item -Path "C:\Program Files\nrdot-collector\mssql-config.yaml" -ItemType File
          ```

          2. Adicione a seguinte configuração ao arquivo `mssql-config.yaml` criado na etapa anterior:

          <Callout variant="tip" title="configuração completa">
            Esta configuração se concentra no monitoramento essencial de banco de dados para o seu ambiente do Windows SQL Server. Para habilitar o monitoramento abrangente com todas as métricas disponíveis, consulte a [referência de configuração](/docs/opentelemetry/database/mssql/config-reference/#hosted-gmsa).
          </Callout>

          <OtelConfig
            inputOptions={[{"name":"hostname","label":"Nome do host/IP do SQL Server","codeLine":4,"defaultValue":"sqlserver.example.com","toolTip":"Insira o nome do host ou endereço IP do SQL Server"},{"name":"port","label":"Porta do SQL Server","codeLine":4,"defaultValue":"1433","toolTip":"Insira o número da porta do SQL Server. O valor padrão é 1433."},{"name":"endpoint","label":"Endpoint New Relic OTLP","codeLine":65,"defaultValue":"https://otlp.nr-data.net","toolTip":"Insira o seu endpoint OTLP da New Relic. Para obter mais informações, consulte https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp/"},{"name":"licenseKey","label":"Chave de licença da New Relic","codeLine":67,"defaultValue":"YOUR_LICENSE_KEY","toolTip":"Insira sua chave de licença de ingestão da New Relic"}]}
            config={`receivers:
            nrsqlserver:
             collection_interval: 15s
             datasource: "server=<input1>;port=<input2>;integrated security=true;encrypt=true;TrustServerCertificate=true;"

             # Enable comprehensive database metrics
             metrics:
               sqlserver.database.count:
                 enabled: true
               sqlserver.database.io:
                 enabled: true
               sqlserver.database.latency:
                 enabled: true
               sqlserver.database.operations:
                 enabled: true
               sqlserver.database.tempdb.space:
                 enabled: true
               sqlserver.database.tempdb.version_store.size:
                 enabled: true
               sqlserver.deadlock.rate:
                 enabled: true
               sqlserver.os.wait.duration:
                 enabled: true
               sqlserver.processes.blocked:
                 enabled: true
               sqlserver.memory.grants.pending.count:
                 enabled: true
               sqlserver.database.file.size:
                 enabled: true
               sqlserver.memory.area:
                 enabled: true

             # Enable top query and query sample log collection
             events:
               db.server.query_sample:
                 enabled: true
               db.server.top_query:
                 enabled: true

             # Top query collection configuration
             top_query_collection:
               lookback_time: 60s
               max_query_sample_count: 1000
               top_query_count: 250
               collection_interval: 60s

             collect_full_query_text: true
             allowed_comment_keys:
               - nr_service_guid

             # Query sample collection configuration
             query_sample_collection:
               max_rows_per_query: 100

            processors:
            memory_limiter:
             check_interval: \${env:NR_MEM_LIMITER_CHECK_INTERVAL:-1s}
             limit_mib:       \${env:NR_MEM_LIMITER_LIMIT_MIB:-200}
             spike_limit_mib: \${env:NR_MEM_LIMITER_SPIKE_MIB:-50}

            batch:

            exporters:
            otlp:
             endpoint: <input3>:4318
             headers:
               api-key: <input4>
             tls:
               insecure: false
             compression: gzip

            service:
            telemetry:
             metrics:
               level: none

            pipelines:
             metrics:
               receivers: [nrsqlserver]
               processors: [memory_limiter, batch]
               exporters: [otlp]

             logs:
               receivers: [nrsqlserver]
               processors: [memory_limiter, batch]
               exporters: [otlp]`}
            fileName="mssql-config.yaml"
            tipMdx={{ body: null }}
            onChange={() => {}}
          />

          <Callout variant="tip">
            Para correlacionar o desempenho do aplicativo com as operações de banco de dados, é possível configurar a identificação de serviço de banco de dados. Para mais informações, consulte [configurar a identificação de serviço de banco de dados](/docs/opentelemetry/database/capabilities/db-apm/).
          </Callout>
        </Step>

        <Step>
          ## Atualizar Registro de Serviço do Windows [#update-registry-gmsa]

          1. Atualize o Registro de Serviço do Windows para usar o novo arquivo `mssql-config.yaml` usando um dos seguintes métodos:

             * **Para PowerShell:** execute o seguinte comando como administrador:

               ```bash
               Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\nrdot-collector" -Name "ImagePath" -Value '"C:\Program Files\nrdot-collector\nrdot-collector.exe" --config "C:\Program Files\nrdot-collector\mssql-config.yaml"'
               ```

             * **Para prompt de comando:** se o PowerShell não estiver disponível, use o prompt de comando e execute o seguinte comando como administrador:

             ```bash
             sc config "nrdot-collector" binPath= "\"C:\Program Files\nrdot-collector\nrdot-collector.exe\" --config \"C:\Program Files\nrdot-collector\mssql-config.yaml\""
             ```

          2. Valide a configuração do Collector NRDOT para garantir que esteja formatada corretamente e funcione adequadamente:

             ```bash
             & "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\mssql-config.yaml"
             ```
        </Step>

        <Step>
          ## Reiniciar o NRDOT Collector [#restart-gmsa]

          Após atualizar sua configuração, reinicie o serviço NRDOT Collector:

          ```bash
          net stop nrdot-collector
          net start nrdot-collector
          ```

          <Callout variant="tip">
            Sempre reinicie o serviço do NRDOT Collector após fazer alterações na configuração para garantir que as novas configurações entrem em vigor.
          </Callout>
        </Step>

        <Step>
          ## Encontre e use seus dados [#find-gmsa]

          Assim que seus dados estiverem sendo coletados, você poderá acessar o monitoramento abrangente do banco de dados SQL Server por meio da interface do New Relic.

          Para encontrar sua entidade de banco de dados SQL Server no New Relic:

          1. Vá para <DNT>**[https://one.newrelic.com](https://one.newrelic.com) &gt; All Capabilities &gt; Databases**</DNT>.

          2. Na dropdown **<DNT>Entity type</DNT>**, selecione <DNT>**MSSQL instance**</DNT> e clique em <DNT>**Apply**</DNT>.

          3. Selecione seu banco de dados SQL Server na lista de entidades.

             Após configurar o monitoramento do SQL Server com o NRDOT, você pode:

             * [Crie dashboards personalizados](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) para visualizar suas métricas de banco de dados
             * [Configure alertas](/docs/alerts/create-alert/create-alert-condition/alert-conditions/) para limites críticos de desempenho do banco de dados
             * [Explore seus dados](/docs/query-your-data/explore-query-data/browse-data/introduction-data-explorer/) usando os recursos de consulta do New Relic
        </Step>
      </Steps>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Documentação relacionada [#related-docs]

<DocTiles>
  <DocTile title="Configurar a correlação entre APM e banco de dados" path="/docs/opentelemetry/database/capabilities/db-apm">
    Aprenda a correlacionar o desempenho do seu aplicativo com as operações de banco de dados no New Relic.
  </DocTile>

  <DocTile title="Resolução de problemas" path="/docs/opentelemetry/database/mssql/windows-hosted">
    Aprenda a solucionar problemas na sua configuração de monitoramento do MSSQL Windows no New Relic.
  </DocTile>

  <DocTile title="Referência de métricas" path="/docs/opentelemetry/database/mssql/metrics-reference">
    Saiba mais sobre as métricas disponíveis coletadas pelo NRDOT Collector.
  </DocTile>
</DocTiles>