---
title: Configuração de múltiplos receptores com o NRDOT Collector
tags:
  - OpenTelemetry
  - NRDOT
  - PostgreSQL
  - Self-hosted
metaDescription: Set up a multi-receiver configuration with NRDOT Collector
freshnessValidatedDate: never
translationType: machine
---

<Callout title="Visualização">
  Ainda estamos trabalhando nesse recurso, mas adoraríamos que você experimentasse!

  Atualmente, esse recurso é fornecido como parte de uma prévia, de acordo com nossas [políticas de pré-lançamento](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

É possível configurar o NRDOT Collector para monitorar várias instâncias do PostgreSQL a partir de um coletor. Isso é útil se houver vários bancos de dados PostgreSQL em execução em hosts ou portas diferentes e se desejar coletar métricas de todos eles usando uma única instância do NRDOT Collector.

<Steps>
  <Step>
    ## Pré-requisitos [#prerequisites]

    * O NRDOT está instalado e configurado para as instâncias do PostgreSQL:
      * [PostgreSQL auto-hospedado](/docs/opentelemetry/database/postgresql/hosted)
  </Step>

  <Step>
    ## Adicionar configuração de multirreceptor [#multi]

    O exemplo a seguir mostra como configurar o NRDOT Collector para monitorar duas instâncias do PostgreSQL usando uma configuração de vários receptores. É possível adicionar mais receptores conforme necessário para instâncias adicionais do PostgreSQL.

    ```yaml
    receivers:
      nrpostgresql/db1: &nrpostgresql-common
        endpoint: "<YOUR_DB1_HOST>:<YOUR_DB1_PORT>"
        username: "<YOUR_DB_USERNAME>"
        password: "<YOUR_DB_PASSWORD>"
        databases:
          - <YOUR_DB1_DATABASE_NAME>
        collection_interval: 10s
      nrpostgresql/db2:
        <<: *nrpostgresql-common
        endpoint: "<YOUR_DB2_HOST>:<YOUR_DB2_PORT>"
        databases:
          - <YOUR_DB2_DATABASE_NAME>

    service:
      pipelines:
        metrics:
          receivers: [nrpostgresql/db1, nrpostgresql/db2]
          exporters: [otlp/newrelic]
    ```
  </Step>

  <Step>
    ## Reiniciar o NRDOT Collector [#restart]

    A reinicialização é obrigatória, não opcional — o coletor carrega a configuração uma vez na inicialização e não recarrega a quente. Qualquer alteração de configuração não tem efeito até que o processo seja reiniciado.

    ```bash
    sudo systemctl restart nrdot-collector
    ```
  </Step>
</Steps>

## Documentação relacionada [#related]

<DocTiles>
  <DocTile title="Compatibilidade e pré-requisitos" path="/docs/opentelemetry/database/postgresql/compatibility">
    Saiba mais sobre as versões suportadas do PostgreSQL, as concessões necessárias e os requisitos de rede.
  </DocTile>

  <DocTile title="Referência de métricas" path="/docs/opentelemetry/database/postgresql/metrics-reference">
    Saiba mais sobre as métricas disponíveis coletadas pelo NRDOT Collector.
  </DocTile>

  <DocTile title="Guia de solução de problemas" path="/docs/opentelemetry/database/postgresql/troubleshooting">
    Saiba como solucionar problemas comuns com o monitoramento do PostgreSQL.
  </DocTile>
</DocTiles>