---
title: Monitoramento do OpenTelemetry Collector
metaDescription: Monitor your OpenTelemetry collector's health and performance using its internal telemetry in a dedicated APM UI experience.
freshnessValidatedDate: '2025-05-27T00:00:00.000Z'
tags:
  - Integrations
  - Open source telemetry integrations
  - OpenTelemetry
translationType: machine
---

<Callout title="Visualização">
  Ainda estamos trabalhando nesse recurso, mas adoraríamos que você experimentasse!

  Atualmente, esse recurso é fornecido como parte de uma prévia, de acordo com nossas [políticas de pré-lançamento](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

Monitore a integridade e o desempenho do seu [coletor OpenTelemetry](https://opentelemetry.io/docs/collector/) com uma experiência de UI de APM dedicada. Quando seu coletor falha ou apresenta mau funcionamento, isso pode causar um apagão de dados de observabilidade, perda permanente de dados ou insights distorcidos. É por isso que criamos o Collector Observability — uma experiência de APM feita sob medida para o trabalho de streaming que os coletores realizam. Você pode aproveitar a [telemetria interna](https://opentelemetry.io/docs/collector/internal-telemetry/) do coletor para ver rapidamente como cada componente do seu coletor está operando, para que você possa identificar problemas antes que eles impactem seu pipeline de observabilidade.

## Configurar o monitoramento do coletor [#setup]

<Callout variant="important" title="Cobrança">
  O uso do Collector Observability é faturável durante o preview de acordo com o seu Pedido, conforme aplicável ao modelo de preços associado à sua Conta e conforme definido abaixo.

  Os custos associados a este recurso são determinados pelos seguintes fatores, conforme aplicável ao modelo de preços associado à sua Conta:

  **Core Compute**: A página de Resumo, medida em Core CCU, é faturável durante o preview. A página de Processos e a página de HTTP/RPC não são faturáveis durante o preview.

  **Ingestão de dados**: Dados adicionais da telemetria interna, medidos em GB ingeridos, são faturáveis durante o preview.

  Se este recurso se tornar disponível de forma geral, seu uso será faturável de acordo com o seu Pedido.
</Callout>

### Habilite a telemetria interna para o seu coletor [#enable-telemetry]

Por padrão, o coletor não emite sua [telemetria interna](https://opentelemetry.io/docs/collector/internal-telemetry/), então você precisará habilitá-la primeiro.

#### Baixe o arquivo de configuração

```bash
curl -L https://raw.githubusercontent.com/newrelic/nrdot-collector-releases/refs/tags/1.10.0/examples/internal-telemetry-config.yaml \
  --silent --output internal-telemetry-config.yaml
```

#### Definir variáveis de ambiente

* <DNT>`INTERNAL_TELEMETRY_NEW_RELIC_LICENSE_KEY`</DNT>: Chave de licença de ingestão para a conta para a qual a telemetria interna deve ser enviada. Essa chave pode ser diferente da chave que o coletor usa para enviar dados regulares para a New Relic, ou seja, <DNT>`NEW_RELIC_LICENSE_KEY`</DNT> no exemplo abaixo.
* <DNT>`INTERNAL_TELEMETRY_SERVICE_NAME`</DNT>: O padrão é `otel-collector`, determina o nome da entidade no New Relic
* <DNT>`INTERNAL_TELEMETRY_OTLP_ENDPOINT`</DNT>: O padrão é EUA `https://otlp.nr-data.net`; se estiver na UE, defina como `https://otlp.eu01.nr-data.net`

#### Execute o coletor com a configuração mesclada

Além da configuração normal para componentes e pipelines (no exemplo a seguir `--config=/etc/nrdot-collector/config.yaml`), adicione um segundo argumento `--config` que mesclará ambas as configurações:

```bash
docker run \
  -e INTERNAL_TELEMETRY_NEW_RELIC_LICENSE_KEY='...' \
  -e NEW_RELIC_LICENSE_KEY='...' \
  -e INTERNAL_TELEMETRY_SERVICE_NAME='demo-collector' \
  -v './internal-telemetry-config.yaml:/etc/nrdot-collector/config-internal.yaml' \
  newrelic/nrdot-collector:1.10.0 --config=/etc/nrdot-collector/config.yaml \
  --config='/etc/nrdot-collector/config-internal.yaml'
```

<Callout variant="important">
  A ordem dos argumentos `--config` importa se você tiver uma configuração pré-existente sob o nó `service::telemetry`. O coletor usa uma estratégia de &apos;o último vence&apos; em nível de nó ao mesclar configurações e certas partes da configuração (por exemplo, listas, nós folha) não podem ser mesclados, então são sobrescritos pelo último argumento `--config`.
</Callout>

#### Alternativa (não recomendada para produção)

Não recomendamos isso por motivos de confiabilidade, mas, para fins de teste, você também pode referenciar a configuração diretamente e o coletor a carregará na inicialização:

```bash
docker run \
  -e INTERNAL_TELEMETRY_NEW_RELIC_LICENSE_KEY='...' \
  -e NEW_RELIC_LICENSE_KEY='...' \
  -e INTERNAL_TELEMETRY_SERVICE_NAME='demo-collector' \
  newrelic/nrdot-collector:1.10.0 --config=/etc/nrdot-collector/config.yaml \
  --config='https://raw.githubusercontent.com/newrelic/nrdot-collector-releases/refs/tags/1.10.0/examples/internal-telemetry-config.yaml'
```

### Adicionar tag de entidade [#add-tag]

A tag <DNT>`newrelic.service.type: otel_collector`</DNT> atua como uma adesão à experiência no nível da interface do usuário. Escolha uma das seguintes opções:

* **Opção 1**: Use a configuração de exemplo fornecida acima, que contém a configuração da Opção 2.
* **Opção 2**: Adicione o argumento <DNT>`--config=yaml:service::telemetry::resource::newrelic.service.type: otel_collector`</DNT> ao coletor. Isso adiciona o atributo como um atributo de recurso e a New Relic faz a etiquetagem para você na ingestão. Se você remover esta opção, levará um dia para a tag expirar.
* **Opção 3**: Adicionar tag via interface do APM (topo da página, ao lado do nome da entidade). Você também pode remover isso pela UI para alternar de volta.

### Personalizando a configuração [#customize]

A configuração padrão expõe variáveis de ambiente adicionais da forma `INTERNAL_TELEMETRY_...` para ajustar opções comuns, como níveis de detalhe e amostragem. Consulte a [própria configuração](https://github.com/newrelic/nrdot-collector-releases/blob/main/examples/internal-telemetry-config.yaml) para obter mais detalhes.

## Visualize seu coletor na UI [#view-ui]

### Explore a telemetria interna na interface do APM [#explore-ui]

Para visualizar a telemetria interna do seu coletor, navegue até <DNT>**APM &amp; Services &gt; Services - OpenTelemetry &gt; your\_collector\_name**</DNT> para explorar a entidade do coletor.

Dependendo dos componentes que seu coletor utiliza, alguns gráficos podem não ser preenchidos. Por exemplo, se você não receber ou exportar dados OTLP via gRPC, os gráficos de RPC ficarão vazios.

### Página de resumo [#summary-page]

A página de Resumo fornece uma visão geral da saúde e do desempenho do seu coletor:

* Métricas gerais de integridade do coletor
* Gráficos para o comportamento de receptores, processadores, exportadores e processamento em lote (requer [batchprocessor](https://github.com/open-telemetry/opentelemetry-collector/tree/v0.128.0/processor/batchprocessor))
* Gráfico dedicado para o [memorylimiter](https://github.com/open-telemetry/opentelemetry-collector/tree/v0.128.0/processor/memorylimiterprocessor) devido ao seu modo de falha único

<img title="Screenshot showing new Summary Page" alt="Screenshot showing new Summary Page" src="/images/otel_collector_o11y-summary.webp" />

* Relacionamentos e métricas de infraestrutura (se configurado, consulte [exemplos de configuração](#examples))

<img title="Screenshot showing related infrastructure telemetry in the new Summary Page" alt="Screenshot showing related infrastructure telemetry in the new Summary Page" src="/images/otel_collector_o11y-infra.webp" />

### Página de processos [#process-page]

Acompanhe o consumo de recursos em nível de sistema com a página Processos:

* Utilização e tendências da CPU
* Uso e padrões de memória
* Indicadores de desempenho em nível de processo

<img title="Screenshot showing new Process Page" alt="Screenshot showing new Process Page" src="/images/otel_collector_o11y-process.webp" />

### Página HTTP/RPC [#http-rpc-page]

Monitore a comunicação de rede com a página HTTP/RPC:

* Métricas de requisições de cliente e servidor HTTP
* Estatísticas de comunicação gRPC
* Latência de rede e taxas de erro

<img title="Screenshot showing new HTTP/RPC Page" alt="Screenshot showing new HTTP/RPC Page" src="/images/otel_collector_o11y-httprpc.webp" />

Nota: Estes gráficos são preenchidos apenas se o seu coletor usar componentes que se comuniquem via HTTP ou gRPC (como receptores ou exportadores OTLP).

## Exemplos de configuração [#examples]

Para exemplos detalhados de configuração e opções de personalização, consulte a [configuração de telemetria interna](https://github.com/newrelic/nrdot-collector-releases/blob/main/examples/internal-telemetry-config.yaml) no repositório de releases do NRDOT Collector.

A configuração expõe variáveis de ambiente no formato `INTERNAL_TELEMETRY_...` para personalizar opções como níveis de detalhe e amostragem.

### Exemplos de implantação [#deployment-examples]

Para cenários de implantação do mundo real, consulte estes exemplos de configuração:

* **[Implantação do Kubernetes](https://github.com/newrelic/helm-charts/tree/master/charts/nr-k8s-otel-collector)**: Exemplo de execução de um coletor no Kubernetes com telemetria interna habilitada
* **[Implantação do Docker](https://github.com/newrelic/nrdot-collector-releases/tree/main/examples)**: Exemplos de configurações do Docker para várias instalações de coletor com monitoramento ativado

## Faturamento e sobrecarga de dados [#billing-overhead]

A Observabilidade do Coletor gera dados de telemetria adicionais que contam para a sua ingestão de dados. Entender o volume de dados ajuda a planejar os custos e otimizar sua configuração.

### Dados gerados [#data-generated]

A Observabilidade do Coletor gera os seguintes tipos de telemetria:

* **Métricas**: Insights em nível de componente sobre receptores, processadores e exportadores
* **Logs**: Logs amostrados com sobrecarga mínima durante a operação normal
* **Traces**: Suporte experimental opcional para análise aprofundada de pipeline

### Volume de dados esperado [#expected-volume]

A quantidade de dados gerados depende da configuração do seu coletor:

* **Métricas de linha de base**: Um coletor com componentes padrão (receptor, processador, exportador) gera aproximadamente **1-2 KB/minuto** de dados de métricas
* **Logs**: Com amostragem padrão (1% das instruções de log), espere **0,5-1 KB/minuto** durante a operação normal. Durante erros ou alta verbosidade, isso pode aumentar significativamente
* **Traces** (opcional): Se ativado, espere **5-10 KB/minuto** dependendo da complexidade do pipeline

Para um coletor típico processando telemetria 24/7:

* **Ingestão mensal de métricas**: -2,5-5 MB
* **Ingestão mensal de logs**: -1,5-3 MB (operação normal)
* **Ingestão mensal de traces**: -10-20 MB (se ativado)

### Controlando a sobrecarga de dados [#controlling-overhead]

Você pode ajustar a geração de telemetria usando variáveis de ambiente:

* <DNT>**`INTERNAL_TELEMETRY_METRICS_LEVEL`**</DNT>: Defina como `none` para desabilitar métricas, `normal` (padrão), ou `detailed`
* <DNT>**`INTERNAL_TELEMETRY_LOGS_LEVEL`**</DNT>: Defina como `info` (padrão), `warn` ou `error` para reduzir o volume de logs
* <DNT>**`INTERNAL_TELEMETRY_LOGS_SAMPLING`**</DNT>: Ajustar taxa de amostragem (padrão: 1%)
* <DNT>**`INTERNAL_TELEMETRY_TRACES_ENABLED`**</DNT>: Defina como `false` para desabilitar rastreamentos (desabilitado por padrão)

Para obter detalhes de configuração, consulte a seção [personalização da configuração](#customize) acima.

## Limitações [#limitations]

* **Certos recursos comuns de APM** não se adaptam perfeitamente ao coletor e sua natureza de processamento de stream e foram ocultados da página de Resumo e da navegação lateral. Analisaremos cada um deles para a Disponibilidade Geral para determinar se e como podemos integrá-los da melhor forma. Exemplos são:

  * Pontuação Apdex
  * Métricas `apm.%` significativas
  * Taxa de erro
  * Transação

* **A telemetria do Coletor ainda não está estável**:

  * A versão mais recente suportada da telemetria emitida pelos componentes do coletor é a versão do componente em nossas distribuições NRDOT (consulte o [manifesto](https://github.com/newrelic/nrdot-collector-releases/blob/main/distributions/nrdot-collector/manifest.yaml) para referência).
  * Se a telemetria emitida mudar durante a Public Preview, reservamo-nos o direito de suportar apenas a versão mais recente. Por exemplo, suportamos <DNT>`http.client.request.duration`</DNT> (versão do coletor &gt;=0.128.0, versão do NRDOT &gt;=1.2.0, nr-k8s-otel-collector &gt;=0.8.37) mas não <DNT>`http.request.duration`</DNT>.
  * Com a Disponibilidade Geral, começaremos a fornecer retrocompatibilidade se os nomes das métricas continuarem a mudar.

* **Requisitos de formato de exportação**: A interface do coletor espera telemetria no formato exportado pelo <DNT>`otlpexporter`</DNT>. Não suporta métricas exportadas via Prometheus. Por exemplo, <DNT>`http.client.request.duration`</DNT> é suportado, mas <DNT>`http_client_request_duration`</DNT> não é.

* **Métricas de componentes personalizados**: A telemetria interna que não está listada na [documentação de telemetria interna](https://opentelemetry.io/docs/collector/internal-telemetry/) ainda não é suportada. Componentes personalizados ou [componentes contrib](https://github.com/open-telemetry/opentelemetry-collector-contrib) emitem métricas padrão, mas também podem definir suas próprias métricas. Ainda estamos trabalhando em uma maneira de ajudar você a obter insights a partir deles sem criar dashboards personalizados.

* **Métricas douradas para contêineres OTel**: Ainda não totalmente suportadas, o que significa que algumas colunas no painel de infraestrutura podem não ser preenchidas para contêineres.