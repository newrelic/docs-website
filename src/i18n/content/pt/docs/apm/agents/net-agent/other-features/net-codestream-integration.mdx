---
title: Métrica al nível do código
tags:
  - Agents
  - NET agent
  - Configuration
metaDescription: Code-level metrics for .NET can be displayed in your IDE using the New Relic CodeStream extension.
freshnessValidatedDate: never
translationType: machine
---

O nível do código métrico permite que os desenvolvedores que usam a [extensão New Relic CodeStream](/docs/codestream/observability/code-level-metrics) vejam os dados APM exibidos contextualmente em seu IDE, juntamente com métodos individuais no código. Isso permite que os desenvolvedores sejam mais proativos na abordagem de problemas de desempenho à medida que escrevem e revisam o código.

## Iniciar [#get-started]

Primeiro, [instale](/docs/codestream/start-here/install-codestream) a extensão New Relic CodeStream no IDE compatível de sua escolha e login.

<Callout variant="important">
  Com o lançamento do agente .NET versão 10.2.0, nível do código métrico habilitado por padrão.

  Nível do código métrica estão disponíveis no [agente .NET versão 9.9.0 e superior](/docs/release-notes/agent-release-notes/net-release-notes) e estão desabilitados por padrão. Para alterar esta configuração, consulte nossa [documentação](/docs/apm/agents/net-agent/configuration/net-agent-configuration#code_level_metrics).
</Callout>

## Atributo do agente [#attributes]

O agente .NET relata e anexa o seguinte atributo aos spans produzidos pelo seu aplicativo.

<Callout variant="important">
  Em alguns casos, o agente encapsulará diretamente o código framework base (armazenamentos de dados, chamadas externas, etc.) que é invocado em seu aplicativo e não será exibido em seu IDE.
</Callout>

<table>
  <thead>
    <tr>
      <th style={{ width: "300px" }}>
        Nome
      </th>

      <th>
        Descrição
      </th>

      <th>
        Exemplo
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `code.function`
      </td>

      <td>
        O nome da função instrumentada.
      </td>

      <td>
        Índice
      </td>
    </tr>

    <tr>
      <td>
        `code.namespace`
      </td>

      <td>
        O namespace (nome completo do tipo de classe) no qual `code.function` é definido.
      </td>

      <td>
        MyService.Controllers.HomeController
      </td>
    </tr>
  </tbody>
</table>
