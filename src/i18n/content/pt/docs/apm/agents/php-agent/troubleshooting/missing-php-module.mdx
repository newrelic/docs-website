---
title: Módulo PHP ausente
type: troubleshooting
tags:
  - Agents
  - PHP agent
  - Troubleshooting
metaDescription: Check the PHP extensions directory to ensure you do not have a non-standard installation for the New Relic PHP agent .
freshnessValidatedDate: never
translationType: machine
---

## Problema

<DNT>**newrelic**</DNT> a seção do módulo não aparece em `phpinfo()`.

## Solução

Uma seção de descrição do módulo `newrelic` ausente na página `phpinfo()` indica que o arquivo <DNT>**`newrelic.so`**</DNT> pode não ter sido instalado no diretório de extensões. Para determinar se o módulo New Relic PHP existe:

1. Na sua [página`phpinfo()` ](/docs/php/php-kb-103), procure a seção

   <DNT>
     **PHP Core**
   </DNT>

   .

2. Para identificar o diretório de onde suas extensões PHP são carregadas, encontre o valor local da configuração `extension_dir` .

3. Verifique se `newrelic.so` está presente no diretório especificado por `extension_dir`.

Se o arquivo `newrelic.so` não estiver presente em seu diretório de extensões:

* Siga [os procedimentos de instalação PHP não padrão](/docs/php/php-agent-installation-non-standard-php) da New Relic.

  OU

* Vincule manualmente a versão correta de `/usr/lib/newrelic-php5/agent/x32` para 32 bits ou `/usr/lib/newrelic-php5/agent/x64` para 64 bits. Ou, se você instalou a partir de um arquivo tar, copie ou crie um link do diretório <DNT>**agent**</DNT> onde você extraiu o arquivo tar.

<img
  title="doc_example_phpinfo_core_0.png"
  alt="Screenshot showing the phpinfo() page."
  src="/images/apm_diagram_php-info-extension-dir.webp"
/>

<figcaption>
  <DNT>**Sample phpinfo FIle, Core section**</DNT>: Verifique o diretório especificado por <DNT>**extension_dir**</DNT> e verifique se `newrelic.so` está presente. Se os dados não aparecerem em sua [página APM <DNT>**Summary**</DNT> ](/docs/apm/applications-menu/monitoring/apm-overview-page)alguns minutos após configurar e reiniciar seu servidor web e PHP.
</figcaption>
