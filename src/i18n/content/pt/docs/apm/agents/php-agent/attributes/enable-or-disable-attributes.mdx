---
title: Habilitar ou desabilitar atributo
tags:
  - Agents
  - PHP agent
  - Attributes
metaDescription: 'How to include and exclude specific attributes for your New Relic PHP agent, including properties and rules.'
freshnessValidatedDate: never
translationType: machine
---

Aprenda sobre as propriedades para ativar ou desativar o atributo e as regras que a New Relic usa ao determinar qual atributo incluir ou excluir para um [destino](/docs/features/agent-attributes#destinations). Isso também inclui um resumo das propriedades do agente PHP que foram descontinuadas com o lançamento do atributo do agente New Relic.

## Propriedades

Use as seguintes propriedades de destino para abrir ou fechar o destino para qualquer coleção de atributos:

<CollapserGroup>
  <Collapser
    id="cfg-attributes-enabled"
    title={<InlineCode>newrelic.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo
          </th>

          <td>
            Boleano
          </td>
        </tr>

        <tr>
          <th>
            Padrão
          </th>

          <td>
            Verdadeiro
          </td>
        </tr>
      </tbody>
    </table>

    Ativa ou desativa todos os atributos em todos os destinos.
  </Collapser>

  <Collapser
    id="cfg-browser-attributes-enabled"
    title={<InlineCode>newrelic.browser_monitoring.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Boleano
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Falso
          </td>
        </tr>
      </tbody>
    </table>

    Ativa ou desativa todos os atributos de <InlinePopover type="browser"/>. Estes são os dados que são anexados ao evento de visualização de página. Se `newrelic.attributes.enabled` for `false`, nenhum atributo será enviado ao monitoramento do Browser independente de como esta propriedade esteja configurada.
  </Collapser>

  <Collapser
    id="cfg-error-attributes-enabled"
    title={<InlineCode>newrelic.error_collector.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Boleano
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Verdadeiro
          </td>
        </tr>
      </tbody>
    </table>

    Ativa ou desativa todos os atributos para erros de rastreamento. Se `newrelic.attributes.enabled` for `false`, nenhum atributo será enviado para erros de rastreamento, independentemente de como essa propriedade estiver configurada.
  </Collapser>

  <Collapser
    id="cfg-events-attributes-enabled"
    title={<InlineCode>newrelic.transaction_events.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Boleano
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Verdadeiro
          </td>
        </tr>
      </tbody>
    </table>

    Ativa ou desativa todos os atributos do evento de transação. Se `newrelic.attributes.enabled` for `false`, nenhum atributo será enviado para o evento de transação, independentemente de como esta propriedade estiver configurada.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-enabled"
    title={<InlineCode>newrelic.transaction_tracer.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo
          </th>

          <td>
            Boleano
          </td>
        </tr>

        <tr>
          <th>
            Padrão
          </th>

          <td>
            Verdadeiro
          </td>
        </tr>
      </tbody>
    </table>

    Ativa ou desativa todos os atributos para rastreamento da transação. Se `newrelic.attributes.enabled` for `false`, nenhum atributo será enviado para o rastreamento da transação, independentemente de como esta propriedade esteja configurada.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-enabled"
    title={<InlineCode>newrelic.span_events.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo
          </th>

          <td>
            Boleano
          </td>
        </tr>

        <tr>
          <th>
            Padrão
          </th>

          <td>
            Verdadeiro
          </td>
        </tr>
      </tbody>
    </table>

    Ativa ou desativa todos os atributos para dados `Span` . Se `newrelic.attributes.enabled` for `false`, nenhum atributo será enviado para spans, independentemente de como essa propriedade estiver configurada.
  </Collapser>
</CollapserGroup>

Use as seguintes propriedades `.include` ou `.exclude` específicas de atributo/destino para adicionar ou remover atributos específicos em destinos específicos:

<CollapserGroup>
  <Collapser
    id="cfg-attributes-include"
    title={<InlineCode>newrelic.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Se atributo estiver habilitado, todas as chaves de atributo nesta lista serão enviadas para New Relic.
  </Collapser>

  <Collapser
    id="cfg-attributes-exclude"
    title={<InlineCode>newrelic.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Todas as chaves de atributo nesta lista **não** serão enviadas para a New Relic.
  </Collapser>

  <Collapser
    id="cfg-bm-attributes-include"
    title={<InlineCode>newrelic.browser_monitoring.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Se atributo estiver ativado para `browser_monitoring`, todas as chaves de atributo nesta lista serão enviadas em dados de visualização de página.
  </Collapser>

  <Collapser
    id="cfg-bm-attributes-exclude"
    title={<InlineCode>newrelic.browser_monitoring.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Todas as chaves de atributo encontradas nesta lista **não** serão enviadas para a New Relic nas visualizações de página.
  </Collapser>

  <Collapser
    id="cfg-ec-attributes-include"
    title={<InlineCode>newrelic.error_collector.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Se atributo estiver habilitado para erros de rastreamento, todas as chaves de atributo nesta lista serão enviadas para New Relic em erros de rastreamento.
  </Collapser>

  <Collapser
    id="cfg-ec-attributes-exclude"
    title={<InlineCode>newrelic.error_collector.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Todas as chaves de atributo nesta lista **não** serão enviadas para New Relic em caso de erros de rastreamento.
  </Collapser>

  <Collapser
    id="cfg-te-attributes-include"
    title={<InlineCode>newrelic.transaction_events.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Se o atributo estiver habilitado para evento de transação, todas as chaves de atributo desta lista serão enviadas para New Relic no evento de transação.
  </Collapser>

  <Collapser
    id="cfg-te-attributes-exclude"
    title={<InlineCode>newrelic.transaction_events.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Todas as chaves de atributo desta lista **não** serão enviadas para New Relic em evento de transação.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-include"
    title={<InlineCode>newrelic.transaction_tracer.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Se os atributo estiverem habilitados para rastreamento da transação, todas as chaves de atributo nesta lista serão enviadas para a New Relic no rastreamento da transação.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-exclude"
    title={<InlineCode>newrelic.transaction_tracer.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Todas as chaves de atributo nesta lista **não** serão enviadas à New Relic no rastreamento da transação.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-include"
    title={<InlineCode>newrelic.span_events.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Se atributo estiver habilitado para evento span, todas as chaves de atributo nesta lista serão enviadas para New Relic no evento span.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-exclude"
    title={<InlineCode>newrelic.span_events.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de strings delimitada por vírgulas
          </td>
        </tr>

        <tr>
          <th>
            Padrão:
          </th>

          <td>
            Vazio
          </td>
        </tr>
      </tbody>
    </table>

    Todas as chaves de atributo nesta lista **não** serão enviadas para New Relic no evento span.
  </Collapser>
</CollapserGroup>

## Regras de atributo [#attruls]

A New Relic segue essas regras ao determinar qual atributo incluir ou excluir para um destino.

<CollapserGroup>
  <Collapser
    id="rule-root-precedence"
    title="O nível raiz tem precedência para ativado."
  >
    O campo `newrelic.attributes.enabled` supera todas as outras configurações. Quando `false`, nenhum atributo será reportado à New Relic.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.enabled = false
    newrelic.attributes.include = foo,bar
    newrelic.transaction_tracer.attributes.enabled = true
    ```

    Exemplo de saída:

    ```
    Keys passed in: password, foo, bar, bat
    Keys included for all destinations:
    Keys excluded for all destinations: password, foo, bar, bat
    ```
  </Collapser>

  <Collapser
    id="rule-destination-precedence"
    title="O destino ativado tem precedência sobre inclusão e exclusão."
  >
    `newrelic.{destination}.attributes.enabled` sinalizadores têm precedência sobre chaves de inclusão e exclusão.

    Exemplo de configuração:

    ```ini
    newrelic.transaction_tracer.attributes.enabled = false
    newrelic.attributes.include = one,two
    newrelic.transaction_tracer.attributes.include = three,four
    ```

    Exemplo de saída:

    ```
    Keys passed in: one, two, three, four
    Keys included for transaction traces:
    Keys excluded for transaction traces: one, two, three, four
    ```
  </Collapser>

  <Collapser
    id="rule-included-destination"
    title="O atributo será incluído se o destino estiver habilitado."
  >
    Se um destino estiver habilitado, todos os atributos do usuário serão enviados para esse destino por padrão.

    Todos os atributos do usuário são padronizados como `true`. Porém, por padrão, request atributo e message parâmetro estão desabilitados para todos os destinos.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.exclude = myAttKey
    ```

    Exemplo de saída:

    ```
    Keys passed in: foo, bar, myAttKey
    Keys included: foo, bar
    Keys excluded: myAttKey
    ```
  </Collapser>

  <Collapser
    id="rule-exclude-wins"
    title="Excluir sempre substitui incluir."
  >
    Se a mesma chave estiver listada nas listas de inclusão e exclusão, então o atributo com a chave especificada será excluído.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.include = foo,myCustomAtt
    newrelic.attributes.exclude = password,myCustomAtt
    ```

    Exemplo de saída:

    ```
    Keys passed in: foo, myCustomAtt, password
    Keys included: foo
    Keys excluded: password, myCustomAtt
    ```
  </Collapser>

  <Collapser
    id="rule-keys-case-sensitive"
    title="As chaves diferenciam maiúsculas de minúsculas."
  >
    As chaves diferenciam maiúsculas de minúsculas.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.exclude = password,PaSsWoRd
    ```

    Exemplo de saída:

    ```
    Keys passed in: password, Password, PASSWORD, PaSsWoRd, PassWORD
    Keys included: Password, PASSWORD, PassWORD
    Keys excluded: password, PaSsWoRd
    ```
  </Collapser>

  <Collapser
    id="rule-star-wildcard"
    title={<>Use uma estrela (<InlineCode>\*</InlineCode>) para curingas.</>}
  >
    Você pode usar um asterisco ou estrela (`*`) no final de uma chave como curinga. Isso corresponderá a um conjunto de atributo com o mesmo prefixo.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.include = custom*
    newrelic.attributes.exclude = request.parameters.*
    ```

    Exemplo de saída:

    ```
    Keys passed in: custom, custom.key1, custom.key2, request.parameters., request.parameters.foo, request.parameters.bar
    Keys included: custom, custom.key1, custom.key2
    Keys excluded: request.parameters., request.parameters.foo, request.parameters.bar
    ```
  </Collapser>

  <Collapser
    id="rule-specific-wins"
    title="A configuração mais específica de uma chave tem prioridade."
  >
    Se vários atributos de inclusão ou exclusão afetarem a mesma chave, a configuração mais específica terá prioridade.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.include = request.parameters.foo
    newrelic.attributes.exclude = request.parameters.*
    ```

    Exemplo de saída:

    ```
    Keys passed in: request.parameters., request.parameters.foo, request.parameters.bar
    Keys included: request.parameters.foo
    Keys excluded: request.parameters., request.parameters.bar
    ```
  </Collapser>

  <Collapser
    id="rule-destination-include-exclude"
    title="Incluir ou excluir afeta o destino específico."
  >
    Se o atributo include ou exclude for especificado em um destino, ele afetará apenas esse destino.

    Exemplo de configuração:

    ```ini
    newrelic.attributes.include = foo
    newrelic.transaction_events.attributes.exclude = foo
    ```

    Exemplo de saída:

    ```
    Keys passed in: foo
    Keys included for transaction events: 
    Keys included for other destinations: foo
    Keys excluded for transaction events: foo
    ```
  </Collapser>
</CollapserGroup>

## Propriedades obsoletas [#deprecated]

As propriedades a seguir foram descontinuadas. Mude para a nova configuração de atributo para essas propriedades ao atualizar seu agente PHP.

<table>
  <thead>
    <tr>
      <th width={200}>
        <DNT>
          **Deprecated property**
        </DNT>
      </th>

      <th>
        <DNT>
          **New property**
        </DNT>
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `newrelic.capture_params`
      </td>

      <td>
        `newrelic.attributes.include = request.parameters.*`

        Por padrão, os parâmetros de solicitação não são enviados para o New Relic. Adicione `request.parameters.*` à lista `newrelic.attributes.include` para ativar todos os parâmetros de solicitação. A propriedade `newrelic.capture_params` foi descontinuada.
      </td>
    </tr>

    <tr>
      <td>
        `newrelic.ignored_params`
      </td>

      <td>
        `newrelic.attributes.exclude = request.parameters.{name}`

        Adicione cada chave de parâmetro de solicitação à lista `newrelic.attributes.exclude` , delimitada por vírgulas. Certifique-se de anexar a chave com `request.parameters`. A propriedade `newrelic.ignored_params` foi descontinuada.
      </td>
    </tr>

    <tr>
      <td>
        `newrelic.{destination}.capture_attributes`
      </td>

      <td>
        `newrelic.{destination}.attributes.enabled`

        A antiga sinalização `newrelic.{destination}.capture_attributes` desativaria a coleta de atributos do usuário. Isso foi descontinuado. Use `newrelic.{destination}.attributes.enabled` em vez disso.
      </td>
    </tr>
  </tbody>
</table>
