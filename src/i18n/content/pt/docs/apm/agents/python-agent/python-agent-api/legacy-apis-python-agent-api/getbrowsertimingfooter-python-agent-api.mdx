---
title: get_browser_timing_footer (API do agente Python)
type: apiDoc
shortDescription: Gera um rodapé do agente browser para injetar o agente JavaScript.
tags:
  - Agents
  - Python agent
  - Python agent API
metaDescription: 'Python API: This call produces the JavaScript footer snippet used for setting up browser monitoring.'
freshnessValidatedDate: never
translationType: machine
---

## Sintaxe [#syntax]

```py
newrelic.agent.get_browser_timing_footer()
```

<Callout variant="caution">
  Esta API foi descontinuada na versão 9.4.0 e completamente removida na versão 11.0.0. A partir da versão 9.4.0 do agente Python, a funcionalidade anterior de `get_browser_timing_footer()` foi incorporada ao [`get_browser_timing_header()`](/docs/apm/agents/python-agent/python-agent-api/getbrowsertimingheader-python-agent-api).

  Para obter mais informações sobre o uso adequado, consulte [Manually instrumento monitoramento de Browser](/docs/agents/python-agent/supported-features/browser-monitoring-python-agent#manual_instrumentation).
</Callout>

Gera um rodapé de agente <InlinePopover type="browser" />para injetar o agente JavaScript.

## Descrição [#description]

Esta chamada retorna o rodapé JavaScript do agente browser trecho que, junto com o [cabeçalho JavaScript trecho](/docs/agents/python-agent/python-agent-api/get_browser_timing_header), é utilizado para habilitar o monitoramento do Browser. Para saber como habilitar o monitoramento de Browser usando esta chamada junto com `get_browser_timing_header`, consulte [Manualmente instrumento de monitoramento de Browser](/docs/agents/python-agent/supported-features/page-load-timing-python#manual_instrumentation).

Esta chamada deve ser chamada para cada solicitação da web onde você deseja injetar o trecho JavaScript. Ele <DNT>**cannot**</DNT> será chamado em escopo global e armazenado em cache.

## Valores de retorno [#return-values]

Retorna o JavaScript do rodapé do agente browser.

## Exemplos [#examples]

### Manual de monitoramento de instrumentação do Browser [#browser-example]

Consulte [Manualmente Instrumento de Monitoramento do Browser](/docs/agents/python-agent/supported-features/page-load-timing-python#manual_instrumentation) para obter instruções sobre como usar as chamadas de cabeçalho e rodapé.