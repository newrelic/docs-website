---
title: Adicionar e editar monitor
tags:
  - Synthetics
  - Synthetic monitoring
  - Using monitors
metaDescription: 'Synthetic monitoring: how to add or edit ping monitors, simple browser monitors, scripted browser monitors, and API test monitors.'
freshnessValidatedDate: never
translationType: machine
---

Monitor Sintético são verificações de API ou instância de browser virtual que monitor seu site, registrando cada verificação detalhadamente. monitor pode:

* Capture números agregados, incluindo uma visão geral ou resumo.
* Fornece estatísticas detalhadas sobre o período de inatividade incidente e os recursos da página.
* Colete códigos de resposta personalizados que detalham o desempenho do seu monitor.

Você pode criar monitor Sintético sem instalação. Para começar:

1. Crie uma [contaNew Relic ](https://newrelic.com/signup).
2. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT> e clique em **Criar um monitor**.
3. Selecione o monitor que você deseja criar. Se for sua primeira vez, recomendamos criar um monitor de ping ou de passos para começar.

## Crie um monitor [#adding-monitors]

Pronto para monitor suas aplicações web com um (ou vários!) de nossos [monitores Sintético](/docs/synthetics/synthetic-monitoring/using-monitors/intro-synthetic-monitoring/#types-of-synthetic-monitors)? As etapas abaixo orientam você no processo para todos os nossos monitores.

<img title="Create your first synthetic monitor" alt="A screenshot that displays the menu options when you go to create synthetic monitors" src="/images/synthetic_screenshot-crop_create-monitor.webp" />

<figcaption>
  Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT> e clique em **Criar um monitor**.
</figcaption>

<CollapserGroup>
  <Collapser id="simple" title="Adicione um ping ou um monitor de browser simples">
    1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring &gt; Create monitor](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT>
    2. Clique no bloco <DNT>**Availability**</DNT> para monitor de ping ou <DNT>**Page load performance**</DNT> para monitor de browser simples.
    3. Opcional: Configure [a notificação de alerta](/docs/synthetics/new-relic-synthetics/using-monitors/alerting-synthetics).

    Aguarde alguns minutos e verifique seu monitor no [índice<DNT>**Monitors**</DNT> ](/docs/new-relic-one-monitors-index).

    Você tem algumas opções de configuração adicionais para o ping ou monitor simples do browser. Para ambos os tipos de monitor, você pode configurar:

    * Um <DNT>**validation string**</DNT> que permite o monitoramento de substring para [validação de resposta](#response-validation).

    * <DNT>**Custom Headers**</DNT> que acompanhará as solicitações enviadas pelo seu monitor.

    * <DNT>**Verify SSL**</DNT> para validar a cadeia de certificados SSL. Ele pode ser duplicado executando a seguinte sintaxe:

      ```sh
      openssl s_client -servername YOUR_HOSTNAME -connect YOUR_HOSTNAME:443 -CApath /etc/ssl/certs -verify_hostname YOUR_HOSTNAME > /dev/null
      ```

      Se um código de saída diferente de zero for retornado, o monitor falhará.

    Com apenas o monitor de ping, você pode optar por configurar:

    * A opção <DNT>**Bypass HEAD request**</DNT> para ignorar a solicitação `HEAD` padrão e usar o verbo `GET` .
    * <DNT>**Redirect is Failure**</DNT>, que categorizará o redirecionamento como uma falha, em vez de seguir o redirecionamento para o novo URL.
    * <DNT>**Custom Headers**</DNT> pode ser adicionado ao ping e ao monitor simples do browser. Esses cabeçalhos serão adicionados às solicitações enviadas pelo monitor.

    Com apenas um monitor de navegador simples, você pode selecionar entre Chrome ou Firefox e habilitar as configurações [de emulação do dispositivo](/docs/synthetics/synthetic-monitoring/using-monitors/device-emulation) , incluindo o tipo de dispositivo e a orientação da tela.
  </Collapser>

  <Collapser id="complex" title="Adicione um browser com script ou monitor de teste de API">
    1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring &gt; Create monitor](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT>.

    2. Especifique um [tipo](#setting-type) e [nome](#setting-name) de monitor.

    3. Escolha um [período](#setting-frequency) para determinar com que frequência cada local executará seu monitor.

    4. Opcional: Selecione um ou mais tipos de navegador e ative as configurações [de emulação de dispositivo](/docs/synthetics/synthetic-monitoring/using-monitors/device-emulation) . Os tipos de navegador, incluindo o tipo de dispositivo e a orientação da tela, estão disponíveis para o monitor de browser com script.

    5. Selecione os [locais](#setting-location) onde você deseja que seu monitor seja executado.

    6. Opcional: Configure [a notificação de alerta](/docs/synthetics/new-relic-synthetics/using-monitors/alerting-synthetics).

    7. Selecione <DNT>**Next: Write your script**</DNT> para criar um script para [<DNT>**scripted browser**</DNT>](/docs/synthetics/new-relic-synthetics/scripting-monitors/writing-synthetic-scripts) ou [<DNT>**API test**</DNT>](/docs/synthetics/new-relic-synthetics/scripting-monitors/writing-api-tests) e selecione <DNT>**Validate**</DNT> para verificar sua sintaxe.

       <Callout variant="tip">
         Para scripts complexos, a validação pode levar até um minuto.
       </Callout>

    8. Selecione <DNT>**Create my monitor**</DNT> para confirmar.

       Aguarde alguns minutos e verifique seu monitor no [índice<DNT>**Monitors**</DNT> ](/docs/synthetics/new-relic-synthetics/dashboards/synthetics-monitors-dashboard-access-your-monitors).
  </Collapser>

  <Collapser id="stepmonitor" title="Adicione um monitor de passos">
    1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring &gt; Create monitor](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT>.

    2. Selecione monitor de etapas como o [tipo de monitor](#setting-type).

    3. Especifique um [nome](#setting-name) e escolha um [período](#setting-frequency) para determinar com que frequência cada local executará seu monitor.

    4. Opcional: Selecione um ou mais tipos de navegador e ative as configurações [de emulação de dispositivo](/docs/synthetics/synthetic-monitoring/using-monitors/device-emulation) . Os tipos de navegador, incluindo o tipo de dispositivo e a orientação da tela, estão disponíveis para o monitor de browser com script.

    5. Selecione os [locais](#setting-location) onde você deseja que seu monitor seja executado.

    6. Construa seu monitor selecionando uma das etapas pré-configuradas na parte inferior da interface:

       * Navegue até um URL
       * Digite o texto
       * Clique em um elemento
       * Afirmar texto
       * Afirmar um elemento
       * Garanta uma credencial

       Use as instruções no lado direito da interface para ajudar a localizar elementos por classe CSS, ID HTML, texto vinculado ou XPath.

    7. Selecione <DNT>**Save monitor**</DNT> para confirmar.

       Gere algum tráfego, aguarde alguns minutos e verifique seu monitor no [índice<DNT>**Monitors**</DNT> ](/docs/synthetics/new-relic-synthetics/dashboards/synthetics-monitors-dashboard-access-your-monitors).
  </Collapser>

  <Collapser className="freq-link" id="certificate" title="Adicione um monitor de verificação de certificado">
    1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring &gt; Create monitor](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT>.

    2. Selecione o tipo de monitor de verificação de certificado.

    3. Especifique um [nome](#setting-name) e insira o domínio que deseja monitor.

    4. Insira o número de dias que leva para o seu certificado expirar.

    5. Selecione o período para determinar [a frequência](#setting-frequency) do seu monitor.

    6. Opcional: adicione uma tag para ajudá-lo a encontrar esse monitor posteriormente.

    7. Selecione os [locais](#setting-location) onde deseja que seu monitor seja executado e clique em <DNT>**Save monitor**</DNT> para confirmar.

       Gere algum tráfego, aguarde alguns minutos e verifique seu monitor no [índice<DNT>**Monitors**</DNT> ](/docs/synthetics/new-relic-synthetics/dashboards/synthetics-monitors-dashboard-access-your-monitors).
  </Collapser>

  <Collapser className="freq-link" id="broken-links" title="Adicione um monitor de links quebrados">
    1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring &gt; Create monitor](https://one.newrelic.com/synthetics-nerdlets/monitor-create)**</DNT>.

    2. Selecione o tipo de monitor de verificação de links quebrados.

    3. Especifique um [nome](#setting-name) e insira o URL que você deseja monitor (pode ser qualquer URL HTTP ou HTTPS válido).

    4. Selecione o período para determinar [a frequência](#setting-frequency) do seu monitor.

    5. Opcional: adicione uma tag para ajudá-lo a encontrar esse monitor posteriormente.

    6. Selecione os [locais](#setting-location) onde deseja que seu monitor seja executado e clique em <DNT>**Save monitor**</DNT> para confirmar.

       Gere algum tráfego, aguarde alguns minutos e verifique seu monitor no [índice<DNT>**Monitors**</DNT> ](/docs/synthetics/new-relic-synthetics/dashboards/synthetics-monitors-dashboard-access-your-monitors).
  </Collapser>
</CollapserGroup>

Quer implantar mais de um monitor por vez? Recomendamos verificar o [tutorial de monitoramento sintético da API NerdGraph](/docs/apis/nerdgraph/examples/nerdgraph-synthetics-tutorial). Você pode automatizar a criação monitor com chamada de API.

Você também pode criar monitores com nossa [API REST de monitoramento sintético](/docs/apis/synthetics-rest-api/monitor-examples/manage-synthetics-monitors-via-rest-api). Para REST API, faça uma solicitação `GET` para recuperar os detalhes de configuração do monitor Sintético escolhido. Depois de recuperar esses dados, use `POST` para criar uma &quot;cópia&quot; do monitor escolhido.

## Editar um monitor [#editing-monitors]

<img src="/images/synthetic_screenshot-full_edit-monitor-from-general.webp" title="Edit a monitor page" alt="A screenshot that shows the New Relic UI when you're editing your monitor." />

<figcaption>
  <DNT>**[one.newrelic.com &gt; Synthetic monitoring](https://one.newrelic.com/synthetics-nerdlets) &gt; General**</DNT>: Mantenha seu monitor atualizado para que a New Relic receba sempre os dados mais pertinentes.
</figcaption>

Você não pode alterar monitor o [tipo de um após](#setting-type) monitor ser criado, mas pode editar outras monitor configurações .

1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring](https://one.newrelic.com/synthetics-nerdlets)**</DNT> e selecione o monitor que deseja editar.

2. No menu lateral, selecione um link para alterar as seguintes [configurações](#settings):

   * Selecione <DNT>**Settings &gt; General**</DNT> para editar [nome](#setting-name), URL, [locais](#setting-location), [frequência](#setting-frequency) e opções avançadas.
   * Para monitores <DNT>**Scripted browser**</DNT> e <DNT>**API test**</DNT>, selecione <DNT>**Settings &gt; Script**</DNT> para editar o script do monitor .
   * Para alertas de monitoramento sintético, clique em <DNT>**Manage alerts**</DNT>.

3. Selecione <DNT>**Save changes**</DNT> para confirmar.

<Callout variant="important">
  Converta para o [novo tempo de execução do Synthetics](/docs/synthetics/synthetic-monitoring/getting-started/new-runtime) ao editar seu monitor. Certifique-se de atualizar sua lista de permissões para aceitar solicitações de rede de [novos intervalos de IP públicos](/docs/synthetics/synthetic-monitoring/administration/new-synthetic-public-minion-ips).
</Callout>

## Excluir um monitor [#deleting-monitors]

<img src="/images/synthetic_screenshot-crop_delete-monitor.webp" title="Edit a monitor page to delete monitor" alt="A screenshot of the Edit a monitor page with a red arrow that shows where someone should look to delete a monitor." />

<figcaption>
  <DNT>**[one.newrelic.com &gt; Synthetic monitoring](https://one.newrelic.com/synthetics-nerdlets) &gt; General**</DNT>: exclua seus monitores Sintético da página Editar monitor .
</figcaption>

Você deve ter [privilégios de administrador](/docs/synthetics/synthetic-monitoring/administration/user-roles-synthetic-monitoring/) para excluir um monitor.

Para excluir um monitor:

1. Vá para <DNT>**[one.newrelic.com &gt; Synthetic monitoring](https://one.newrelic.com/synthetics-nerdlets)**</DNT> e selecione o monitor que deseja editar.
2. Clique em <DNT>**General**</DNT> e, em seguida, clique no botão excluir monitor.

Você também pode excluir um monitor com nossa [API NerdGraph](/docs/apis/nerdgraph/examples/nerdgraph-synthetics-tutorial) ou a [API REST de monitoramento sintético](/docs/apis/synthetics-rest-api/monitor-examples/manage-synthetics-monitors-via-rest-api#delete-monitor).

## Configurações do monitor [#settings]

Ao configurar o monitor, as seguintes configurações estão disponíveis:

<CollapserGroup>
  <Collapser id="setting-type" title="Tipo">
    Selecione o tipo de monitor que deseja criar. O [tipo](/docs/synthetics/new-relic-synthetics/getting-started/types-synthetics-monitors#types-monitors) de um monitor não pode ser alterado após a criação do monitor.

    * <DNT>**Ping**</DNT>: especifique um único URL para monitor a disponibilidade. A New Relic verificará esta URL via HEAD ou solicitação GET. O [tempo limite](/docs/synthetics/new-relic-synthetics/scripting-monitors/write-scripted-browsers#waiting-elements) não configurável para este monitor é de 60 segundos.
    * <DNT>**Simple browser**</DNT>: Especifique uma única URL para monitor via navegador real. A cada [intervalo de frequência](#setting-frequency), o New Relic verificará esta URL por meio de um navegador Chrome ou Firefox com tecnologia Selenium. O [tempo limite](/docs/synthetics/new-relic-synthetics/scripting-monitors/write-scripted-browsers/#waiting-elements) não configurável para este monitor é de 60 segundos.
    * <DNT>**Scripted browser**</DNT>: [Crie um script](/docs/synthetics/new-relic-synthetics/scripting-monitors/writing-synthetic-scripts) para controlar um navegador Chrome ou Firefox com Selenium. O navegador segue cada etapa do script para verificar se o comportamento complexo está funcionando conforme o esperado (por exemplo, pesquisar em um site e, em seguida, clicar em um dos resultados da pesquisa). O [tempo limite](/docs/synthetics/new-relic-synthetics/scripting-monitors/write-scripted-browsers#waiting-elements) não configurável para este monitor é de 180 segundos.
    * <DNT>**API test**</DNT>: Crie um script de API para garantir que seus endpoints de API estejam funcionando corretamente. Para obter mais informações, consulte [Escrever testes de API](/docs/synthetics/new-relic-synthetics/scripting-monitors/writing-api-tests). O tempo limite não configurável para este monitor é de 180 segundos.
    * <DNT>**Step monitor**</DNT>: Uma opção sem código para configurar uma ou mais etapas para conduzir um navegador Chrome ou Firefox com Selenium. O navegador segue cada etapa para verificar se o comportamento complexo está funcionando conforme o esperado (por exemplo, pesquisar em um site e, em seguida, clicar em um dos resultados da pesquisa). O [tempo limite](/docs/synthetics/new-relic-synthetics/scripting-monitors/write-scripted-browsers#waiting-elements) não configurável para este monitor é de 180 segundos.
    * <DNT>**Certificate check**</DNT>: Verifique se um certificado SSL irá expirar em um número configurável de dias ou menos.
    * <DNT>**Broken links**</DNT>: Teste todos os links encontrados em uma URL para garantir que eles respondam com códigos de resposta HTTP bem-sucedidos.
  </Collapser>

  <Collapser id="setting-name" title="Nome do monitor">
    Define um nome para o monitor. os nomes monitor não podem conter colchetes angulares não codificados (`<` e `>`). Para incluir colchetes angulares em um nome de monitor, codifique-os como entidade de colchetes HTML (`&lt;` para `<` e `&gt;` para `>`) na interface ou API.
  </Collapser>

  <Collapser id="setting-location" title="Localização">
    Selecione os locais onde deseja que seu monitor seja executado. Selecione mais locais para garantir que seu aplicativo esteja disponível para usuários em todo o mundo. Se você tiver alguma [localização privada](/docs/synthetics/new-relic-synthetics/private-locations/private-locations-overview-monitor-internal-sites-add-new-locations), ela também estará listada aqui. Você pode usar o [endpoint `location` da API Sintético](/docs/apis/synthetics-rest-api/monitor-examples/manage-synthetics-monitors-via-rest-api#list-locations) para recuperar uma lista de locais válidos para sua conta.

    Seu monitor executará uma verificação em cada local selecionado durante cada [intervalo de frequência](#setting-frequency). Por exemplo, se você selecionar três locais e definir uma frequência de 15 minutos, seu monitor executará três verificações em cada período de 15 minutos (ou 8.640 verificações por mês).
  </Collapser>

  <Collapser id="setting-frequency" title="Frequência">
    Selecione a frequência de execução do monitor, em incrementos de minutos, horas ou 1 dia. Esta frequência se aplica a cada [local](#setting-location). Por exemplo, se você selecionar três locais e uma frequência de <DNT>**15 minutes**</DNT>, seu monitor executará três verificações, em média, a cada 5 minutos, em cada período de 15 minutos (ou 8.640 verificações por mês).
  </Collapser>

  <Collapser id="response-validation" title="Validação de resposta (opcional)">
    Especifique o texto a ser pesquisado na página DOM. Ao usar browser simples ou [tipos de monitor](/docs/synthetics/synthetic-monitoring/getting-started/types-synthetic-monitors/#types-monitors) de ping, há um limite de 1 MB (10 ^ 6 bytes) no carregamento da página.
  </Collapser>

  <Collapser id="apdex-t" title="Apdex T (opcional)">
    Especifique o limite para o <DNT>**tolerable**</DNT> tempo de resposta do seu monitor. O valor padrão é 7 segundos (7.000 ms). Para obter mais informações, consulte [Entender a métrica do relatório de SLA](/docs/synthetics/synthetic-monitoring/pages/synthetic-monitoring-aggregate-monitor-metrics/#understanding).
  </Collapser>
</CollapserGroup>

## Veja um histórico de alterações no monitor [#track-changes]

Você pode usar a interface do New Relic para ver um [histórico de alterações recentes no monitor Sintético](/docs/synthetics/new-relic-synthetics/administration/audit-synthetics-account-changes) e quais usuários as alteraram.