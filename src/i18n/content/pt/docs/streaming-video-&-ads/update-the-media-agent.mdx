---
title: Migrando o agente de vídeo para a versão 3.xx
metaDescription: How to update your existing media agent to the latest version.
freshnessValidatedDate: never
translationType: machine
---

To make migrating from earlier versions as easy as possible, we have developed a compatibility layer that translates most of the configurable options chart to their new counterparts. This compatibility layer is temporary and will be removed in the future. We encourage you to read this guide carefully and migrate the configuration under human supervision.

## Migração para a versão 3.xx

Para experimentar o novo vista customizada, você deve migrar para a versão 3.xx do agente de mídia.

### Complicações do tipo evento duplo

Existe a possibilidade de que, após a migração, alguns dos seus usuários finais não tenham optado pela atualização do aplicativo, ou muito poucos o tenham feito. Durante esse período intermediário, você poderá experimentar tipos de eventos duplos, o que pode ser complicado. Para gerenciar isso:

* Reúna todas as consultas e mapeamentos de ações.
* Informe ao seu usuário final que durante esse período intermediário, ele poderá ver dados em vários eventos e como eles estão relacionados.

<Callout variant="important">
  Isso se aplica ao agente móvel. O browser será atualizado automaticamente.
</Callout>

### Visualizando dados pós-migração

Após a migração, se você quiser ver os dados do mês anterior, recomendamos que você use algum dashboard personalizado ou consulte:

```sql
SELECT * FROM pageAction
```

<Callout variant="important">
  With named event definitions, they loose the dashboard capability for querying the old data.
</Callout>

### evento Definição de mapeamento

Um mapeamento detalhado de cada tipo de evento antigo com o novo é fornecido para ajudar você a migrar qualquer script ou fluxo de trabalho. Esse mapeamento é crucial para garantir uma transição tranquila e manter a integridade dos dados.

We recommend you carefully follow this guide and ensure that all configurations are migrated under supervision. The compatibility layer is a temporary solution, and it is important to transition to the new configuration as soon as possible.

Para informações mais detalhadas, entre em contato com o suporte ao cliente New Relic .