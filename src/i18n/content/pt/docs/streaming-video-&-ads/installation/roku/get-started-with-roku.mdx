---
title: Comece com o agente Roku
tags: null
metaDescription: 'How to deploy roku agent monitoring in New Relic for your apps, including UI, copy/paste, and API options.'
freshnessValidatedDate: never
translationType: machine
---

O agente Roku New Relic foi projetado para rastrear o comportamento do aplicativo Roku, com foco em eventos relacionados ao sistema e a vídeo. Ele usa a API de eventos para enviar dados por meio de uma interface REST, suportando cinco tipos de eventos:

* **ConnectedDeviceSystem**: Sistema de eventos
* **VideoAction**: Evento de vídeo
* **VideoErrorAction**: Evento de erro
* **VideoAdAction**: Anúncio de evento
* **VideoCustomAction**: evento personalizado

## Pré-requisitos

Para inicializar o agente New Relic Roku, você precisa:

* [ID da sua conta](/docs/accounts-partnerships/accounts/account-setup/account-id)New Relic
* Sua New Relic <InlinePopover type="licenseKey" />.

## Instalar e configurar o agente Roku

<Steps>
  <Step>
    Baixe o pacote Roku Video agente e descompacte-o para acessar a seguinte estrutura de arquivo:

    ```
    components/NewRelicAgent/
        NRAgent.brs
        NRAgent.xml
        NRTask.brs
        NRTask.xml
    source/
        NewRelicAgent.brs
    ```
  </Step>

  <Step>
    Copie a pasta `NewRelicAgent` para o diretório `components` e o arquivo `NewRelicAgent.brs` para o diretório `source` do seu projeto de aplicativo Roku.
  </Step>

  <Step>
    **Inicialize o agente**: abra seu arquivo principal BrightScript (por exemplo, `Main.brs`). Inicialize o agente New Relic chamando a função `NewRelic` com seus `ACCOUNT ID`, <InlinePopover type="licenseKey" />, `APP NAME`, `APPLICATION TOKEN` e `ENDPOINT`. \*\*Para configurar o agente para usar endpoints diferentes com base no ambiente, você precisa modificar o arquivo main.brs. O valor de configuração padrão é US, e pode ser alterado para EU ou preparação dependendo do ambiente que você deseja usar.

    ```brightscript
    m.nr = NewRelic("ACCOUNT ID", "LICENSE KEY", "APP NAME", "APPLICATION TOKEN", "US", true)

    ```
  </Step>
</Steps>