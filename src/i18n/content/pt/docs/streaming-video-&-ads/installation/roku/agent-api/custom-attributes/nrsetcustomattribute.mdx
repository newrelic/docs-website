---
title: nrSetCustomAttribute
type: apiDoc
shortDescription: Defina um atributo personalizado a ser incluído no evento.
tags: null
metaDescription: Set a custom attribute to be included in the events.
freshnessValidatedDate: never
translationType: machine
---

## Sintaxe

```js
  nrSetCustomAttribute(nr as Object, key as String, value as Object, actionName = "" as String) as Void
```

## Descrição

Defina um atributo personalizado a ser incluído no evento.

## Argumentos

<table>
  <thead>
    <tr>
      <th width="25%">
        Argumentos
      </th>

      <th>
        Descrição
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `nr`
      </td>

      <td>
        Objeto agente New Relic .
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Nome do atributo.
      </td>
    </tr>

    <tr>
      <td>
        `value`
      </td>

      <td>
        Valor do atributo.
      </td>
    </tr>

    <tr>
      <td>
        `actionName`
      </td>

      <td>
        (opcional) Ação onde o atributo será incluído. Definir todas as ações como padrão.
      </td>
    </tr>
  </tbody>
</table>

## Retornar

Nada.

## Exemplo

```js
  	nrSetCustomAttribute(m.nr, "myNum", 123, "CONTENT_START")
	  nrSetCustomAttribute(m.nr, "myString", "hello")
```