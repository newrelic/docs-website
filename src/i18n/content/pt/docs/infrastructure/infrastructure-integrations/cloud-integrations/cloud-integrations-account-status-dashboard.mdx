---
title: 'Integração na nuvem: dashboard de status da conta'
tags:
  - Integrations
  - Infrastructure integrations
  - Cloud integrations
metaDescription: 'For New Relic cloud integrations: use the account status dashboard to understand how your cloud service is using APIs and transmitting data.'
freshnessValidatedDate: never
translationType: machine
---

Cada conta de nuvem vinculada ao monitoramento de infraestrutura New Relic (por exemplo, [AWS](/docs/integrations/amazon-integrations/get-started/connect-aws-infrastructure) ou [Azure](/docs/integrations/microsoft-azure-integrations/getting-started/introduction-azure-monitoring-integrations)) tem um <DoNotTranslate>**account status dashboard**</DoNotTranslate> que mostra como nossa plataforma está lidando com dados desse provedor de serviço de nuvem.

## Por que isso importa [#reasons-to-use]

Os motivos para monitor este dashboard incluem:

* <DoNotTranslate>**Understand API call usage.**</DoNotTranslate>

  Utilizamos API em nuvem para coletar métricas e dados de inventário. O painel mostra contagens e frequências para essas chamadas de API. Você pode querer monitor -los por dois motivos: a chamada de API pode custar dinheiro e o uso API pode ser limitado em determinados níveis de uso. Para diminuir o uso, você pode [definir configurações de integração](/docs/integrations/new-relic-integrations/getting-started/configure-polling-frequency-data-collection-cloud-integrations) para reduzir o número de componentes monitor ou a frequência com que eles são monitorados.

* <DoNotTranslate>**Troubleshoot missing data or other data issues.**</DoNotTranslate>

  O dashboard exibe informações que afetam a forma como a integração reporta os dados, incluindo:

  * Erros que afetam a coleta de dados do New Relic, como erros de permissão ou erros de esgotamento de cota.
  * Alterações na configuração de integração, como alterações no intervalo de sondagem ou coleta de tagsendo desativada ou ativada.

## Entenda os dados dashboard [#understand-data]

Os dados específicos exibidos no dashboard de status da conta serão diferentes de acordo com o provedor de serviço de nuvem. Os gráficos comuns incluem:

* <DoNotTranslate>**Data updates**</DoNotTranslate>

  : Mostra atualizações de dados métricos ou dados de inventário (atualizações mostradas como um valor `1` ).

* <DoNotTranslate>**Account changes**</DoNotTranslate>

  : Ações que afetam o funcionamento da integração. Por exemplo: renomear ou desvincular uma conta na nuvem, alterar intervalo de sondagem e outras opções de configuração.

* <DoNotTranslate>**Data freshness**</DoNotTranslate>

  : timestamp do último dado coletado para cada integração.

* <DoNotTranslate>**Fetching errors**</DoNotTranslate>

  : indicam problemas na coleta de dados. (Isso pode ser um problema do lado do provedor de nuvem.)

As opções para melhor compreensão dos dados do gráfico incluem:

* Passe o mouse sobre um gráfico

  <Icon name="fe-help-circle"/>

  ícone para ver uma descrição do gráfico (se disponível).

* [Veja a consulta NRQL subjacente do gráfico](/docs/new-relic-solutions/new-relic-one/introduction-new-relic-platform).

## Encontre dashboard de status da conta [#find-dashboard]

Para encontrar o dashboard de status da conta de um provedor de serviço em nuvem:

1. Em

   <DoNotTranslate>**[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities) > Infrastructure**</DoNotTranslate>

   e selecione um provedor de serviço de nuvem (por exemplo,

   <DoNotTranslate>**AWS**</DoNotTranslate>

   ).

2. Selecione

   <DoNotTranslate>**Account status dashboard**</DoNotTranslate>

   para a conta na nuvem.