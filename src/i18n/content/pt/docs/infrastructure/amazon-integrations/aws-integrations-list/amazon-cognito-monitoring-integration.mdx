---
title: Amazon Cognito monitoramento integração
tags:
  - Integrations
  - Amazon integrations
  - AWS integrations list
metaDescription: 'New Relic''s Amazon Cognito monitoring integration: what data it reports, and how to enable it.'
freshnessValidatedDate: never
translationType: machine
---

New Relic integra-se com Amazon Web Services (AWS) para relatar seus dados [do Amazon Cognito](https://aws.amazon.com/cognito/) métrica e outros dados ao New Relic.

Este documento explica como ativar a integração e descreve os dados relatados.

## Recurso [#features]

Colete e envie dados de telemetria para o New Relic a partir dos seus serviços do Amazon Cognito usando nossa integração. monitore seus serviços, consulte dados recebidos e crie um painel para observar tudo rapidamente.

## Ativar integração [#activate]

Essa integração está disponível por meio do CloudWatch MetricStreams (recomendado).

Para habilitar essa integração, veja como [conectar serviços da AWS ao New Relic via CloudWatch Metric Streams](/docs/infrastructure/amazon-integrations/connect/aws-metric-stream-setup/).

### Ative usando a API herdada Polling integração [#polling]

Para habilitar essa integração usando nossa integração de polling de legado, veja como habilitar a [AWS API integração de polling](/docs/infrastructure/amazon-integrations/connect/connect-aws-new-relic-infrastructure-monitoring/).

<Callout variant="tip">
  Você pode alterar a frequência de pesquisa e filtrar dados usando [opções de configuração](/docs/integrations/new-relic-integrations/getting-started/configure-polling-frequency-data-collection-cloud-integrations).

  Informações [de pesquisa](/docs/infrastructure/amazon-integrations/aws-integrations-list/aws-polling-intervals-infrastructure-integrations) padrão para a integração do AWS Cognito:

  * Intervalo de sondagem New Relic : 5 minutos
  * Intervalo de dados do AWS CloudWatch: 1 minuto
</Callout>

## Encontre e use dados [#find-data]

Para encontrar seus dados de integração, vá para <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Metrics and events**</DNT> e filtre por `aws.cognito`.

## Dados métricos [#metrics]

Essa integração coleta dados do AWS Cognito para UserPool.

### Dados do UserPool do Cognito

<table>
  <thead>
    <tr>
      <th style={{ width: "275px" }}>
        Métrica
      </th>

      <th style={{ width: "150px" }}>
        Unidade
      </th>

      <th>
        Descrição
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `signUpSuccesses`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações de registro de usuário bem-sucedidas feitas ao pool de usuários do Cognito. Para encontrar a porcentagem de solicitações de cadastro de usuários bem-sucedidas, utilize o atributo Média nesta métrica. Para contabilizar o número total de solicitações de cadastro de usuários, utilize o atributo SampleCount nesta métrica. Para contar o número total de solicitações de registro de usuário bem-sucedidas, use o atributo Soma nesta métrica. Para contar o número total de solicitações de registro de usuário com falha, subtraia o atributo Sum do atributo SampleCount.
      </td>
    </tr>

    <tr>
      <td>
        `signUpThrottles`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações de registro de usuário limitadas. Para contar o número total de solicitações de registro de usuário limitadas, use o atributo Sum para esta métrica.
      </td>
    </tr>

    <tr>
      <td>
        `signInSuccesses`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações de autenticação de usuário bem-sucedidas para o pool de usuários. Para encontrar a porcentagem de solicitações de autenticação de usuário bem-sucedidas, use o atributo Média nesta métrica. Para contar o número total de solicitações de autenticação de usuário, use o atributo SampleCount nesta métrica. Para contar o número total de solicitações de autenticação de usuário bem-sucedidas, use o atributo Sum nesta métrica. Para contar o número total de solicitações de autenticação de usuário com falha, subtraia o atributo Sum do atributo SampleCount.
      </td>
    </tr>

    <tr>
      <td>
        `signInThrottles`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações de autenticação de usuário limitadas feitas ao pool de usuários do Cognito. Para contar o número total de solicitações de autenticação de usuário limitadas, use o atributo Sum para esta métrica.
      </td>
    </tr>

    <tr>
      <td>
        `tokenRefreshSuccesses`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações bem-sucedidas para atualizar um token do Cognito para o pool de usuários. Para encontrar a porcentagem de solicitações bem-sucedidas para atualizar um token Cognito, use o atributo Average nesta métrica. Para contar o número total de solicitações para atualizar um token Cognito, use o atributo SampleCount nesta métrica. Para contar o número total de solicitações bem-sucedidas para atualizar um token Cognito, use o atributo Sum nesta métrica. Para contar o número total de solicitações com falha para atualizar um token do Cognito, subtraia o atributo Sum do atributo SampleCount.
      </td>
    </tr>

    <tr>
      <td>
        `tokenRefreshThrottles`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações limitadas para atualizar um token do Cognito para o pool de usuários. Para contar o número total de solicitações limitadas para atualizar um token Cognito, use o atributo Sum para esta métrica.
      </td>
    </tr>

    <tr>
      <td>
        `federationSuccesses`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações de federação de identidade bem-sucedidas para o pool de usuários do Cognito. Para encontrar a porcentagem de solicitações bem-sucedidas de federação de identidades, use o atributo Média nesta métrica. Para contar o número total de solicitações de federação de identidade, use o atributo SampleCount nesta métrica. Para contar o número total de solicitações de federação de identidade bem-sucedidas, use o atributo Sum nesta métrica. Para contar o número total de solicitações de federação de identidade com falha, subtraia o atributo Sum do atributo SampleCount.
      </td>
    </tr>

    <tr>
      <td>
        `federationThrottles`
      </td>

      <td>
        Contar
      </td>

      <td>
        O número total de solicitações de federação de identidade limitadas para o pool de usuários do Cognito. Para contar o número total de solicitações de federação de identidade limitadas, use o atributo Sum para esta métrica.
      </td>
    </tr>
  </tbody>
</table>

### Criar alerta

Você pode configurar <InlinePopover type="alerts" />para notificá-lo se houver alguma alteração. Por exemplo, você pode configurar um alerta para notificar as partes relevantes sobre erros críticos ou fatais.

Saiba mais sobre como criar alertas [aqui](/docs/alerts-applied-intelligence/new-relic-alerts/learn-alerts/introduction-alerts/).