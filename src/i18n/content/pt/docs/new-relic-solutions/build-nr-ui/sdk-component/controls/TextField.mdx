---
title: Campo de texto
metaDescription: Learn how to work the TextField component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Os formulários permitem que um usuário forneça informações para concluir uma tarefa.

### Uso

```js
import { TextField } from 'nr1'
```

### Exemplos

#### Básico

```js
<TextField label="Username" placeholder="e.g. John Doe" />
```

#### Com etiqueta embutida

```js
<TextField label="Username" labelInline placeholder="e.g. John Doe" />
```

#### Procurar

```js
<TextField type={TextField.TYPE.SEARCH} placeholder="e.g. Placeholder" />
```

#### Senha

```js
<TextField type={TextField.TYPE.PASSWORD} placeholder="e.g. Placeholder" />
```

#### Pré-formatado

```js
<TextField type={TextField.TYPE.PREFORMATTED} placeholder="npm run build" />
```

#### Carregando

```js
<TextField loading placeholder="e.g Placeholder" />
```

#### Desabilitado

```js
<TextField defaultValue="Some value" disabled placeholder="e.g Placeholder" />
```

#### Somente leitura

```js
<TextField defaultValue="Some value" readOnly placeholder="e.g Placeholder" />
```

#### Com descrição

```js
<TextField description="Description value" placeholder="e.g Placeholder" />
```

#### Com mensagem inválida

```js
<TextField
  invalid="Not enough text, needs more"
  placeholder="e.g Placeholder"
/>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>corda</h5>
      </td>

      <td>
        Forneça um rótulo para leitores de tela para descrever essa entrada. Isso só será necessário se o rótulo do leitor de tela for diferente do adereço `label` ou se você não estiver fornecendo um adereço `label` .
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabelledBy` <h5>corda</h5>
      </td>

      <td>
        Forneça o `id` exclusivo de um elemento cujo conteúdo descreva essa entrada para leitores de tela. Isso só será necessário se o rótulo do leitor de tela for diferente do adereço `label` ou se você não estiver fornecendo um adereço `label` . Use isto ou `ariaLabel`, não ambos.
      </td>
    </tr>

    <tr>
      <td>
        `autoFocus` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, o campo de texto terá foco automático na montagem.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>corda</h5>
      </td>

      <td>
        Valor padrão do campo de texto.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>corda</h5>
      </td>

      <td>
        Mensagem com instruções sobre como preencher o campo do formulário.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>boleano</h5>
      </td>

      <td>
        Estabelece se o campo pode ser editável ou não.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>corda</h5>
      </td>

      <td>
        Informações adicionais podem ser exibidas em uma dica de informação ao lado do rótulo.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>boolean|string</h5>
      </td>

      <td>
        Quando verdadeiro, define o campo em um estado inválido, para notificar o usuário que é necessária atenção sobre este campo específico. Esta propriedade pode ser um campo `boolean` ou um `string`. Quando for um `string`, assim como o estado inválido sendo mostrado, o texto será mostrado abaixo.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>corda</h5>
      </td>

      <td>
        Texto a ser exibido como rótulo.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>boleano</h5>
      </td>

      <td>
        Exiba o rótulo embutido no controle do formulário. Use somente quando o componente não estiver dentro de um `Form`. Nesse caso, defina `layoutType` como `Form.LAYOUT_TYPE.SPLIT` no componente `Form` .
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>boleano</h5>
      </td>

      <td>
        Se estiver carregando uma lista de dados ou algo mais em um campo de formulário, indique ao usuário que algo está sendo carregado usando o formulário de estado de carregamento. Usado para informar que o campo pode carregar valores dinamicamente. Quando definido como `true`, mostra um botão giratório de carregamento; e quando definido como `false`, reserva algum espaço na interface para que o controle giratório possa ser potencialmente mostrado. Não defina a propriedade (ou defina-a como `undefined`) se não for esperado que o campo seja carregado em um determinado ponto em tempo.
      </td>
    </tr>

    <tr>
      <td>
        `multiline` <h5>DEPRECADO</h5> <h5>booleano</h5>
      </td>

      <td>
        <Callout
          variant="caution"
          title="Vencimento em 1º de janeiro de 2022"
        >
          A propriedade "multiline" está obsoleta, use o componente MultilineTextField para fins de multilinha.
        </Callout>

        Se a entrada aceita uma ou mais linhas. Isso também afeta o comportamento da tecla \[Enter], que criará uma nova linha, em oposição a um campo autônomo em um campo de linha única.
      </td>
    </tr>

    <tr>
      <td>
        `onBlur` <h5>função</h5>
      </td>

      <td>
        retorno de chamada que é acionado quando o campo de texto perde o foco.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>função</h5>
      </td>

      <td>
        retorno de chamada que é disparado quando o valor do campo de texto muda.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback. You can pull out the new value by accessing `event.target.value`."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onFocus` <h5>função</h5>
      </td>

      <td>
        retorno de chamada que é disparado quando o campo de texto recebe foco.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyDown` <h5>função</h5>
      </td>

      <td>
        retorno de chamada que é disparado quando uma tecla é pressionada.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyUp` <h5>função</h5>
      </td>

      <td>
        retorno de chamada que é disparado quando uma tecla é liberada.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `placeholder` <h5>corda</h5>
      </td>

      <td>
        Dica exibida no campo de texto quando não possui valor.
      </td>
    </tr>

    <tr>
      <td>
        `readOnly` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, torna o campo de texto somente leitura.
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, denota o campo do formulário conforme obrigatório.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propriedade de espaçamento. O espaçamento é definido como uma tupla de zero a quatro valores, que segue as mesmas convenções das propriedades CSS como `margin` ou `padding`. Para omitir um valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          TextField.SPACING_TYPE.EXTRA_LARGE, TextField.SPACING_TYPE.LARGE, TextField.SPACING_TYPE.MEDIUM, TextField.SPACING_TYPE.NONE, TextField.SPACING_TYPE.OMIT, TextField.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spellCheck` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, ativa a verificação do navegador em busca de erros ortográficos.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`. **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeração</h5>
      </td>

      <td>
        * Texto — quando um usuário insere uma linha de texto em um campo

        * E-mail – quando você precisa que um usuário insira o endereço de e-mail

        * Pesquisa – quando um usuário precisa pesquisar

        * Pré-formatado — quando um usuário precisa inserir conteúdo de código

        * Senha — quando um usuário insere uma senha

        * URL – quando um usuário insere um URL

          <OptionReference>
            TextField.TYPE.EMAIL, TextField.TYPE.PASSWORD, TextField.TYPE.PREFORMATTED, TextField.TYPE.SEARCH, TextField.TYPE.TEXT, TextField.TYPE.URL,
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>corda</h5>
      </td>

      <td>
        Valor do campo de texto.Isso é necessário para um componente controlado.
      </td>
    </tr>
  </tbody>
</table>