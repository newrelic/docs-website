---
title: Item suspenso
metaDescription: Learn how to work the DropdownItem component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Elemento filho do componente `<Dropdown>` .

Suporta evento de clique através da propriedade `onClick` e a possibilidade de redirecionar para outras páginas através da propriedade `to` .

Suporta subitens por meio da propriedade `items` e passando uma função de renderização para a propriedade `children` .

### Uso

```js
import { DropdownItem } from 'nr1'
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` Nó <h5>REQUERIDO</h5> <h5>| (nó|função)\[]</h5>
      </td>

      <td>
        Conteúdo a ser exibido dentro do item. Ele também aceita um método render como último filho ao renderizar listas aninhadas. Requer passar uma matriz para a propriedade `items` .

        ```js
        import { Dropdown, DropdownItem } from 'nr1';

        const nestedItems = [
          { id: '3.1', text: 'Item 3.1' },
          { id: '3.2', text: 'Item 3.2' },
          { id: '3.3', text: 'Item 3.3' },
          { id: '3.4', text: 'Item 3.4' },
        ];

        const nestedDropdown = (
          <Dropdown title="Nested Dropdown">
            <DropdownItem>Item 1</DropdownItem>
            <DropdownItem>Item 2</DropdownItem>
            <DropdownItem items={nestedItems}>
              Item 3
              {({ item }) => <DropdownItem key={item.id}>{item.text}</DropdownItem>}
            </DropdownItem>
            <DropdownItem>Item 4</DropdownItem>
          </Dropdown>
        );
        ```
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, o item não estará disponível para interação e seu retorno de chamada onClick não será acionado.
      </td>
    </tr>

    <tr>
      <td>
        `items` <h5>matriz</h5>
      </td>

      <td>
        Itens para renderizar uma sublista do item atual. Esta propriedade também requer a passagem de uma função como último `children` após o conteúdo do item funcionar.
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>função</h5>
      </td>

      <td>
        O retorno de chamada é acionado sempre que o usuário clica no item.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selected` <h5>boleano</h5>
      </td>

      <td>
        Mostra a opção conforme selecionada.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`.**Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>shape|string</h5>
      </td>

      <td>
        Objeto de localização ou string de URL ao qual vincular.

        <h3>forma</h3>

        `pathname` <h5>Sequência</h5><h5>OBRIGATÓRIA</h5>

        `search` <h5>corda</h5>

        `hash` <h5>corda</h5>
      </td>
    </tr>
  </tbody>
</table>