---
title: FavoriteTableRowCell
metaDescription: Learn how to work the FavoriteTableRowCell component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Renderiza uma célula da tabela de linhas mostrando o status favorito de uma entidade específica. O status de favorito pode ser controlado por meio da propriedade `checked` e as alterações de status podem ser ouvidas por meio da propriedade `onChange` .

### Uso

```js
import { FavoriteTableRowCell } from 'nr1'
```

### Exemplos

#### Exemplo 1

```js
function render() {
  const items = [
    {
      name: 'Login service',
      alertSeverity: 'CRITICAL',
      reporting: true,
      favorite: true,
    },
    {
      name: 'Service Gateway',
      alertSeverity: 'NOT_ALERTING',
      reporting: true,
      favorite: false,
    },
  ];
  return (
    <Table items={items}>
      <TableHeader>
        <TableHeaderCell width="56px" />
        <TableHeaderCell>Entity</TableHeaderCell>
      </TableHeader>
      {({ item }) => (
        <TableRow>
          <FavoriteTableRowCell checked={item.favorite} />
          <EntityTitleTableRowCell value={item} />
        </TableRow>
      )}
    </Table>
  );
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `checked` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, indica que o usuário marcou a entidade como favorita.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>função</h5>
      </td>

      <td>
        O retorno de chamada é acionado sempre que o status do favorito muda. O novo status de favorito será passado como segundo parâmetro.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""},{"name":"checked","type":"boolean","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`.

        **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>shape|string</h5>
      </td>

      <td>
        Objeto de localização ou string de URL para vincular. `TableRowCell`s vinculados não têm estilo e não mostrarão ícones para links externos. Se o mesmo estilo do componente Link for o desejado, use um `Link` como componente filho dentro da célula.<h3>forma</h3> `pathname` <h5>string</h5><h5>OBRIGATÓRIA</h5>

        `search` <h5>corda</h5>

        `hash` <h5>corda</h5>
      </td>
    </tr>
  </tbody>
</table>