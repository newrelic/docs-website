---
title: TableChart
metaDescription: Learn how to work the TableChart component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Cria um gráfico de tabela. Os dados podem ser obtidos executando uma consulta NRQL em uma conta específica ou podem ser transmitidos por meio da propriedade `data` .

O formato `data` é uma série de objetos, cada um contendo `metadata` e `data`. Cada `data` contém ambos os valores deste ponto e as chaves `x` e `y` usadas para traçar o gráfico.

### Uso

```js
import { TableChart } from 'nr1'
```

### Exemplos

#### Básico

```js
<TableChart accountIds={[1]} query="SELECT count(*) FROM `Synthetics` SINCE 1 DAY AGO TIMESERIES AUTO FACET jobType" />
```

#### Com várias contas

```js
<TableChart accountIds={[1, 1067061]} query="SELECT count(*) FROM `Synthetics` SINCE 1 DAY AGO TIMESERIES AUTO FACET jobType" />
```

#### Preencher contêiner

```js
<TableChart
  accountIds={[1]}
  query="SELECT count(*) FROM `Synthetics` SINCE 1 DAY AGO TIMESERIES AUTO FACET jobType"
  fullWidth
  fullHeight
/>
```

#### Com dados personalizados

```js
function render() {
  const data = [
     {
       metadata: {
         id: 'series-1',
         name: 'Serie 1',
         color: '#008c99',
         viz: 'main',
         columns: ['jobType', 'count'],
       },
       data: [
         {
           jobType: 'SIMPLE',
           count: 18,
         },
         {
           jobType: 'SCRIPT_BROWSER',
           count: 27,
         },
         {
           jobType: 'SCRIPT_API',
           count: 33,
         },
         {
           jobType: 'BROWSER',
           count: 29,
         },
         {
           jobType: 'SELENIUM_JSON',
           count: 22,
         },
         {
           jobType: 'SCRIPT',
           count: 31,
         },
       ],
     },
   ];;


  return <TableChart data={data} fullWidth />;
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `accountId` <h5>Número</h5> <h5>obsoleto</h5>
      </td>

      <td>
        <Callout
          variant="caution"
          title="Vencimento em 1º de novembro de 2022"
        >
          O accountId está obsoleto. Em vez disso, use accountIds
        </Callout>

        Define o ID da conta para realizar a consulta.
      </td>
    </tr>

    <tr>
      <td>
        `accountIds` <h5>number\[]</h5>
      </td>

      <td>
        Define os IDs de conta para realizar a consulta.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente.
      </td>
    </tr>

    <tr>
      <td>
        `data` <h5>object\[]</h5>
      </td>

      <td>
        Dados são uma matriz de objetos onde cada objeto representa uma série a ser desenhada. Cada série compreende metadados de visualização e uma matriz de pontos de dados.

        ```
        const data = [
          {
            metadata: {
              id: 'series-1',
              name: 'Serie 1',
              color: '#008c99',
              viz: 'main',
              columns: ['jobType', 'count'],
            },
            data: [
              {
                jobType: 'SIMPLE',
                count: 18,
              },
              {
                jobType: 'SCRIPT_BROWSER',
                count: 27,
              },
              {
                jobType: 'SCRIPT_API',
                count: 33,
              },
              {
                jobType: 'BROWSER',
                count: 29,
              },
              {
                jobType: 'SELENIUM_JSON',
                count: 22,
              },
              {
                jobType: 'SCRIPT',
                count: 31,
              },
            ],
          },
        ]; 
        ```
      </td>
    </tr>

    <tr>
      <td>
        `fullHeight` <h5>boleano</h5>
      </td>

      <td>
        Expande o gráfico para ocupar toda a altura disponível.
      </td>
    </tr>

    <tr>
      <td>
        `fullWidth` <h5>boleano</h5>
      </td>

      <td>
        Expande o gráfico para ocupar toda a largura disponível.
      </td>
    </tr>

    <tr>
      <td>
        `onClickTable` <h5>função</h5>
      </td>

      <td>
        Adiciona um ouvinte de clique que é acionado quando o usuário clica na tabela correspondente.
      </td>
    </tr>

    <tr>
      <td>
        `onHoverTable` <h5>função</h5>
      </td>

      <td>
        Adiciona um ouvinte instantâneo que é acionado quando o cursor passa sobre a tabela correspondente.
      </td>
    </tr>

    <tr>
      <td>
        `query` <h5>corda</h5>
      </td>

      <td>
        Consulta NRQL usada para busca de dados. A consulta é executada no `accountIds` fornecido.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado.
      </td>
    </tr>
  </tbody>
</table>