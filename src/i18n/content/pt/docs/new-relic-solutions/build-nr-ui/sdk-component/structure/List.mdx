---
title: Lista
metaDescription: Learn how to work the List component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<List>` permite renderizar com eficiência uma grande lista de itens.

O `<List>` pode renderizar os itens de duas maneiras: como uma matriz de elementos `<ListItem>` ou como um retorno de chamada de renderização (função como filhos). Recomenda-se utilizar o retorno de chamada de renderização, pois ele virtualizará todos os itens, tornando-o muito rápido quando um grande número de itens for fornecido.

Se a largura e a altura não forem especificadas por meio dos adereços `style` ou `className` , ele preencherá a altura e a largura do contêiner, a menos que `width="fit-content"` seja fornecido como adereço. Em seguida, ele dimensionará o contêiner `<List>` para ter a largura do componente `<ListItem>` mais largo medido.

### Uso

```js
import { List } from 'nr1'
```

### Exemplos

#### Básico

```js
function render() {
  return (
    <List rowHeight={20}>
      <ListItem>Item 1</ListItem>
      <ListItem>Item 2</ListItem>
      <ListItem>Item 3</ListItem>
      <ListItem>Item 4</ListItem>
    </List>
  );
}
```

#### Renderizar retorno de chamada

```js
function render() {
  const items = new Array(10000).fill().map((_, i) => {
    return { key: `foo-${i}`, name: `Item ${i}` };
  });


  const style = { width: 200, height: 300 };


  return (
    <List style={style} rowHeight={20} items={items}>
      {({ item }) => <ListItem key={item.key}>{item.name}</ListItem>}
    </List>
  );
}
```

#### Carregamento lento

```js
function render() {
  // This example assumes you have a way to know/load this information.
  const remoteTotalNumberOfItems = 9000;


  const items = [
    { id: 1, text: 'Item 1' },
    { id: 2, text: 'Item 2' },
    { id: 3, text: 'Item 3' },
    { id: 4, text: 'Item 4' },
  ];


  function loadMoreItems({ startIndex, stopIndex }) {
    return fetch(`path/to/api?start=${startIndex}&stop=${stopIndex}`).then(
      () => {
        // Store items in list...
      },
    );
  }


  const style = { width: 200, height: 300 };


  return (
    <div style={style}>
      <List
        items={items}
        rowCount={remoteTotalNumberOfItems}
        onLoadMore={loadMoreItems}
      >
        {({ item }) => <ListItem key={item.id}>{item.text}</ListItem>}
      </List>
    </div>
  );
}
```

#### Dimensionamento automático para largura do conteúdo

```js
class AutoSizingList extends React.Component {
  constructor() {
    super(...arguments);
    this.state = {
      items: this._generateItems(0),
    };
    this._maxItems = 1000;
    this._onLoadMore = this._onLoadMore.bind(this);
  }


  _onLoadMore(cursor) {
    return (
      Promise.resolve()
        // Fetch more items to load into <List>
        .then(() => this._generateItems(this.state.items.length))
        .then((moreItems) =>
          // Add retrieved items to the stored list
          this.setState(({ items }) => ({ items: items.concat(moreItems) })),
        )
    );
  }


  _generateString(num, extra = 0) {
    return `Entity ${num.toString(10)}: 1${'00'.repeat(
      Math.floor(Math.random() * 10) + 1 + extra,
    )} bytes`;
  }


  _generateItems(start) {
    return new Array(200).fill().map((_, i) => ({
      key: `foo-${start + i}`,
      name: this._generateString(start + i, start / 100),
    }));
  }


  render() {
    const style = { 'height': 300, 'background-color': 'white' };
    const parentStyle = { width: 260 };


    return (
      <div style={parentStyle}>
        <List
          rowCount={this._maxItems}
          onLoadMore={this._onLoadMore}
          style={{ ...style }}
          rowHeight={20}
          items={this.state.items}
          width="fit-content"
        >
          {({ item }) => <ListItem key={item.key}>{item.name}</ListItem>}
        </List>
      </div>
    );
  }
}
```

#### integração com componentes de consulta

```js
function render() {
  const style = { width: 200, height: 300 };


  const list = (
    <div style={style}>
      <EntitiesByDomainTypeQuery entityDomain="APM" entityType="APPLICATION">
        {({ fetchMore, loading, data }) => {
          const { results, count } = data.actor.entitySearch;


          return (
            <List
              rowHeight={40}
              items={results.entities}
              onLoadMore={fetchMore}
              rowCount={count}
            >
              {({ item }) => <ListItem key={item.id}>{item.text}</ListItem>}
            </List>
          );
        }}
      </EntitiesByDomainTypeQuery>
    </div>
  );
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` Nó <h5>REQUERIDO</h5> <h5>| função</h5>
      </td>

      <td>
        Pode ser uma matriz de elementos `<ListItem>` ou um retorno de chamada de renderização (função como filho). A recomendação é usar o retorno de chamada de renderização, pois ele virtualizará todos os itens, tornando-o muito rápido quando um grande número de itens for fornecido. Ao usar o retorno de chamada de renderização, os itens precisam ser fornecidos por meio da propriedade `items` .
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Nome da classe para estilo personalizado.
      </td>
    </tr>

    <tr>
      <td>
        `fullHeight` <h5>boleano</h5>
      </td>

      <td>
        Expande a stack para ocupar toda a altura disponível.
      </td>
    </tr>

    <tr>
      <td>
        `fullWidth` <h5>boleano</h5>
      </td>

      <td>
        Expande a stack para ocupar toda a largura disponível.
      </td>
    </tr>

    <tr>
      <td>
        `items` <h5>qualquer\[]</h5>
      </td>

      <td>
        Os itens a serem usados durante a renderização. Eles são necessários ao renderizar itens com um retorno de chamada de renderização. Cada item pode ter qualquer estrutura e tipo possível, e o correspondente será fornecido ao renderizar cada lista de elementos.

        ```js
        function render() {
          const style = { width: 200, height: 300 };

          const items = [
            { foo: 'a', bar: 'x' },
            { foo: 'b', bar: 'y' },
          ];

          return (
            <div style={style}>
              <List items={items}>
                {({ item, index }) => (
                  <ListItem key={index}>{`${item.foo} - ${item.bar}`}</ListItem>
                )}
              </List>
            </div>
          );
        }
        ```
      </td>
    </tr>

    <tr>
      <td>
        `onLoadMore` <h5>função</h5>
      </td>

      <td>
        retorno de chamada disparado quando mais itens devem ser carregados. Isso acontece quando você está carregando lentamente os itens e os itens que estão prestes a renderizar não podem ser encontrados na matriz `items`. Este retorno de chamada deve ser usado para buscar/carregar os itens ausentes do backend ou de outras fontes. A promessa retornada deve ser resolvida assim que os dados do item terminarem de carregar. Será usado para determinar quando atualizar a lista com os dados recém-carregados. Esse retorno de chamada pode ser chamado diversas vezes em reação a um único evento de rolagem.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"cursor","type":"Cursor","description":"Items to load."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `rowCount` <h5>número</h5>
      </td>

      <td>
        Número de linhas. Por padrão, é igual ao comprimento da matriz passada na propriedade items. Você deve especificar `rowCount` quando souber o número total de itens, mas quiser carregá-los lentamente durante a rolagem.
      </td>
    </tr>

    <tr>
      <td>
        `rowHeight` <h5>Número</h5> <h5>OBRIGATÓRIO</h5>
      </td>

      <td>
        Altura da linha da lista. Obrigatório ao renderizar itens com o retorno de chamada de renderização (função como filhos). Esta é uma altura provisória até que o conteúdo da célula seja medido, renderizando-o temporariamente de uma forma que não seja visível para o usuário.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propriedade de espaçamento. O espaçamento é definido como uma tupla de zero a quatro valores, que segue as mesmas convenções das propriedades CSS como `margin` ou `padding`. Para omitir um valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Lista.SPACING_TYPE.EXTRA_LARGE, Lista.SPACING_TYPE.LARGE, Lista.SPACING_TYPE.MEDIUM, Lista.SPACING_TYPE.NONE, Lista.SPACING_TYPE.OMIT, Lista.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`.

        **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `width` <h5>corda</h5>
      </td>

      <td>
        Representa a largura da lista. É sempre uma string e pode ser de um dos seguintes tipos:

        * `'fit-content'`: ativa o dimensionamento automático do contêiner `<List>` . Destinado ao uso com `<List>`s virtualizados para garantir que o contêiner se ajuste à largura do `<ListItem>` mais largo. Redimensiona à medida que mais itens são carregados pelo componente `<List>` . Não afeta a altura do componente `<List>` , que continuará a se ajustar à altura do contêiner em que o componente `<List>` está.
        * `'auto'`: o valor padrão para `<List>`. Isso solicita que o componente `<List>` seja dimensionado de acordo com seu estilo padrão, permitindo que ele se expanda para preencher a largura e a altura de seu contêiner. O conteúdo é então dimensionado para caber na largura resultante do componente `<List>` .
      </td>
    </tr>
  </tbody>
</table>

### Definições de tipo

<TypeDefReference typeDef={{"name":"RenderCallbackArguments","properties":[{"description":"Item to render.","name":"item","type":"any"},{"description":"Index of the item in the items array.","name":"index","type":"number"},{"description":"Array of items which we're iterating on.","name":"items","type":"any[]"}]}}/><TypeDefReference typeDef={{"name":"Cursor","properties":[{"description":"First index of the range of items to load.","name":"startIndex","type":"number"},{"description":"Last index of the range of items to load.","name":"stopIndex","type":"number"}]}}/>