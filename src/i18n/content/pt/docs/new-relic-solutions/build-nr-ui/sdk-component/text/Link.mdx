---
title: Link
metaDescription: Learn how to work the Link component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Fornece navegação declarativa e acessível em torno do aplicativo e links externos.

### Uso

```js
import { Link } from 'nr1'
```

### Exemplos

#### Básico

```js
<Link to="https://www.google.com">Click me</Link>
```

#### Com objeto de localização

```js
function render() {
  const launcher = { id: 'nr1-core.explorer' };
  const location = navigation.getOpenLauncherLocation(launcher);


  return <Link to={location}>Click me</Link>;
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>corda</h5>
      </td>

      <td>
        Utilize-o para descrever melhor o contexto de ação do componente para o usuário nos leitores de tela.

        ```js
        <Link to="/dashboards" ariaLabel="Go to your dashboard">Go</Link> 
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>Nó</h5> <h5>OBRIGATÓRIO</h5>
      </td>

      <td>
        Conteúdo do link.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>função</h5>
      </td>

      <td>
        O retorno de chamada é acionado sempre que o usuário clica no link.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `role` <h5>corda</h5>
      </td>

      <td>
        O atributo ARIA é usado para descrever a função de `Link` para acessibilidade, como `role="menuitem"`. Por padrão, a função de `Link` é "link", portanto, uma função explícita é para uso em outros elementos acessíveis, como elementos `role="menu"` e outros. [Consulte as práticas recomendadas de ARIA](https://www.w3.org/TR/wai-aria-practices/) para ver quais `role`s podem ser aplicados `Link` e em quais contextos.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propriedade de espaçamento. O espaçamento é definido como uma tupla de zero a quatro valores, que segue as mesmas convenções das propriedades CSS como `margin` ou `padding`. Para omitir um valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Link.SPACING_TYPE.EXTRA_LARGE, Link.SPACING_TYPE.LARGE, Link.SPACING_TYPE.MEDIUM, Link.SPACING_TYPE.NONE, Link.SPACING_TYPE.OMIT, Link.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`.

        **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>shape|string</h5>
      </td>

      <td>
        Objeto de localização ou string de URL ao qual vincular. <h3>forma</h3>

        `pathname` <h5>Sequência</h5><h5>OBRIGATÓRIA</h5>

        `search` <h5>corda</h5>

        `hash` <h5>corda</h5>
      </td>
    </tr>
  </tbody>
</table>