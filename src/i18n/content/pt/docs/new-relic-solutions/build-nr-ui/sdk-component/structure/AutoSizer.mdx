---
title: AutoSizer
metaDescription: Learn how to work the AutoSizer component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

O componente `<AutoSizer>` fornece funcionalidade para reagir a mudanças no tamanho do elemento em que estão contidos, medindo-o. Quando o `width` ou o `height` desse elemento externo muda, os filhos são renderizados novamente e receberão as novas medidas como um objeto.

### Uso

```js
import { AutoSizer } from 'nr1'
```

### Exemplos

#### Básico

```js
function render() {
  const style = {
    backgroundColor: '#d8211a1a',
    border: 'dashed 1px #d8211a4d',
  };


  return (
    <AutoSizer>
      {({ width, height }) => (
        <div style={{ ...style, height, width }}>
          width: {width}, height: {height}
        </div>
      )}
    </AutoSizer>
  );
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>Função</h5><h5>NECESSÁRIA</h5>
      </td>

      <td>
        Função filho que será chamada toda vez que o pai for redimensionado. Ele é chamado com um objeto contendo as chaves `width` e `height` e deve retornar o elemento React a ser renderizado.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Nome da classe para estilo personalizado.
      </td>
    </tr>

    <tr>
      <td>
        `onResize` <h5>função</h5>
      </td>

      <td>
        retorno de chamada invocado quando o contêiner é redimensionado.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"args","type":"OnResizeArguments","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propriedade de espaçamento. O espaçamento é definido como uma tupla de zero a quatro valores, que segue as mesmas convenções das propriedades CSS como `margin` ou `padding`. Para omitir um valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          AutoSizer.SPACING_TYPE.EXTRA_LARGE, AutoSizer.SPACING_TYPE.LARGE, AutoSizer.SPACING_TYPE.MEDIUM, AutoSizer.SPACING_TYPE.NONE, AutoSizer.SPACING_TYPE.OMIT, AutoSizer.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado.
      </td>
    </tr>
  </tbody>
</table>

### Definições de tipo

<TypeDefReference typeDef={{"name":"OnResizeArguments","properties":[{"description":"Height of the container","name":"height","type":"number"},{"description":"Width of the container","name":"width","type":"number"}]}}/>