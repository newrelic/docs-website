---
title: Trocar
metaDescription: Learn how to work the Switch component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

As opções são usadas como alternância em filtros de conteúdo ou para fornecer uma maneira de um usuário selecionar uma preferência binária.

### Uso

```js
import { Switch } from 'nr1'
```

### Exemplos

#### Básico

```js
<div className="nr1-Docs-prettify">
  <Switch onChange={(e) => alert(`Toggle to: ${e.target.checked}`)} />
  <Switch
    defaultChecked
    onChange={(e) => alert(`Toggle to: ${e.target.checked}`)}
  />
</div>
```

#### Desabilitado

```js
<div className="nr1-Docs-prettify">
  <Switch disabled />
  <Switch defaultChecked disabled />
</div>
```

#### Carregando estado

```js
<div className="nr1-Docs-prettify">
  <Switch loading />
  <Switch defaultChecked loading />
</div>
```

#### Com etiqueta

```js
<div className="nr1-Docs-prettify">
  <Switch label="This is a label" />
  <Switch disabled label="This is a label" />
</div>
```

#### Com etiqueta e informações

```js
<Switch label="This is a label" info="Info value" />
```

#### Com descrição

```js
<Switch label="This is a label" description="Description value" />
```

#### Com mensagem inválida

```js
<Switch label="This is a label" invalid="Invalid message value" />
```

#### Componente controlado definido para carregamento durante a execução de uma solicitação

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(resolve, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

#### Componente controlado definido para carregamento durante a execução de uma solicitação que volta para desmarcado se a solicitação falhar

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(reject, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>corda</h5>
      </td>

      <td>
        Forneça um rótulo descritivo para este controle, por exemplo "Alterar visualização".
      </td>
    </tr>

    <tr>
      <td>
        `checked` <h5>boleano</h5>
      </td>

      <td>
        Determina o estado verificado do switch, onde `true` corresponde ao switch que parece estar "ligado" e um valor `false` para o switch que parece estar "desligado". Quando `checked` é definido, o componente Switch se comportará como um [componente controlado](https://reactjs.org/docs/forms.html#controlled-components).
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `defaultChecked` <h5>boleano</h5>
      </td>

      <td>
        Define o valor padrão verificado para a chave ao operar como um [componente não controlado](https://reactjs.org/docs/uncontrolled-components.html). ou seja. Onde a proposta `checked` ainda não está estipulada.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>corda</h5>
      </td>

      <td>
        Mensagem com instruções sobre como preencher o campo do formulário.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>boleano</h5>
      </td>

      <td>
        Determina se o usuário pode interagir com o switch ou se ele deve ser renderizado em um estado desabilitado.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>corda</h5>
      </td>

      <td>
        Informações adicionais podem ser exibidas em uma dica de informação ao lado do rótulo.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>boolean|string</h5>
      </td>

      <td>
        Quando verdadeiro, define o campo em um estado inválido, para notificar o usuário que é necessária atenção sobre este campo específico. Esta propriedade pode ser um campo `boolean` ou um `string`. Quando for um `string`, assim como o estado inválido sendo mostrado, o texto será mostrado abaixo.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>corda</h5>
      </td>

      <td>
        Associe um rótulo de texto para fornecer ao usuário um breve título ou explicação para a configuração controlada pelo switch. A etiqueta será posicionada no lado esquerdo do switch.
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>boleano</h5>
      </td>

      <td>
        Para indicar se uma ação está em andamento, principalmente no caso de demorar mais de 1 segundo para ser concluída, você deve exibir o estado de carregamento. A opção não é interativa durante o carregamento. Se a ação associada ao estado de carregamento falhar, a propriedade verificada deverá ser redefinida para seu valor anterior.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>função</h5>
      </td>

      <td>
        O retorno de chamada é acionado sempre que o usuário alterna a opção. O estado verificado atual pode ser avaliado no retorno de chamada por meio de `event.target.checked`.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propriedade de espaçamento. O espaçamento é definido como uma tupla de zero a quatro valores, que segue as mesmas convenções das propriedades CSS como `margin` ou `padding`. Para omitir um valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Switch.SPACING_TYPE.EXTRA_LARGE, Switch.SPACING_TYPE.LARGE, Switch.SPACING_TYPE.MEDIUM, Switch.SPACING_TYPE.NONE, Switch.SPACING_TYPE.OMIT, Switch.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`. **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>
  </tbody>
</table>