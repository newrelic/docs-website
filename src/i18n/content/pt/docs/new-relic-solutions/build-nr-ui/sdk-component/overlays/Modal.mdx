---
title: Modal
metaDescription: Learn how to work the Modal component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Os modais são usados para ações de criação, adição, edição ou exclusão em uma única etapa. Eles também são usados para exibir metadados adicionais sobre uma tela ou objeto específico na tela.

### Uso

```js
import { Modal } from 'nr1'
```

### Exemplos

#### Básico

```js
class MyNerdlet extends React.PureComponent {
  constructor() {
    super(...arguments);


    this._onClose = this._onClose.bind(this);


    this.state = {
      hidden: true,
    };
  }


  _onClose() {
    this.setState({ hidden: true });
  }


  render() {
    return (
      <>
        <Button onClick={() => this.setState({ hidden: false })}>
          Open modal
        </Button>


        <Modal hidden={this.state.hidden} onClose={this._onClose}>
          <HeadingText type={HeadingText.TYPE.HEADING_3}>Modal</HeadingText>


          <BlockText
            spacingType={[
              BlockText.SPACING_TYPE.EXTRA_LARGE,
              BlockText.SPACING_TYPE.OMIT,
            ]}
          >
            Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
            eiusmod tempor incididunt ut labore et dolore magna aliqua. Dictumst
            quisque sagittis purus sit amet.
          </BlockText>


          <Button onClick={this._onClose}>Close</Button>
        </Modal>
      </>
    );
  }
}
```

#### Desmontando

```js
class MyNerdlet extends React.PureComponent {
  constructor() {
    super(...arguments);


    this._onClick = this._onClick.bind(this);
    this._onClose = this._onClose.bind(this);
    this._onHideEnd = this._onHideEnd.bind(this);


    this.state = {
      hidden: true,
      mounted: false,
    };
  }


  _onClick() {
    this.setState({
      hidden: false,
      mounted: true,
    });
  }


  _onClose() {
    this.setState({ hidden: true });
  }


  _onHideEnd() {
    this.setState({ mounted: false });
  }


  render() {
    return (
      <>
        <Button onClick={this._onClick}>Open modal</Button>


        {this.state.mounted && (
          <Modal
            hidden={this.state.hidden}
            onClose={this._onClose}
            onHideEnd={this._onHideEnd}
          >
            <HeadingText type={HeadingText.TYPE.HEADING_3}>Modal</HeadingText>


            <BlockText
              spacingType={[
                BlockText.SPACING_TYPE.EXTRA_LARGE,
                BlockText.SPACING_TYPE.OMIT,
              ]}
            >
              Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
              eiusmod tempor incididunt ut labore et dolore magna aliqua.
              Dictumst quisque sagittis purus sit amet.
            </BlockText>


            <Button onClick={this._onClose}>Close</Button>
          </Modal>
        )}
      </>
    );
  }
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabelledBy` <h5>corda</h5>
      </td>

      <td>
        Passe a sequência do conteúdo do texto que deve descrever melhor a finalidade do modal para ser corretamente anunciado pelos leitores de tela.

        ```js
        const modalLabelId = 'my-nerdlet.my-modal';

        <Modal ariaLabelledBy={modalLabelId}>
          <span id={modalLabelId}>
            <HeadingText>My modal</HeadingText>
          </span>
        </Modal>;
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>nó</h5>
      </td>

      <td>
        Conteúdo a ser renderizado dentro do modal.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente.
      </td>
    </tr>

    <tr>
      <td>
        `hidden` <h5>boleano</h5>
      </td>

      <td>
        Se `true`, o modal está oculto.
      </td>
    </tr>

    <tr>
      <td>
        `onClose` <h5>Função</h5><h5>NECESSÁRIA</h5>
      </td>

      <td>
        retorno de chamada disparado ao clicar no botão fechar do Modal.
      </td>
    </tr>

    <tr>
      <td>
        `onHideEnd` <h5>função</h5>
      </td>

      <td>
        retorno de chamada disparado quando o Modal termina a animação de fechamento. Use isto para desmontar o componente Modal. Isso garante que a animação de fechamento funcione corretamente.

        ```js
        function render() {
          <>
            {this.state.mounted && (
              <Modal
                hidden={this.state.hidden}
                onHideEnd={() => this.setState({ mounted: false })}
                onClose={() => this.setState({ hidden: true })}
              >
                <h1>Modal</h1>
                <p>
                  Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
                  eiusmod tempor incididunt ut labore et dolore magna aliqua.
                </p>
              </Modal>
            )}
          </>;
        }
        ```
      </td>
    </tr>

    <tr>
      <td>
        `onHideStart` <h5>função</h5>
      </td>

      <td>
        retorno de chamada disparado quando o Modal inicia a animação de fechamento.
      </td>
    </tr>

    <tr>
      <td>
        `onShowEnd` <h5>função</h5>
      </td>

      <td>
        retorno de chamada disparado quando o Modal finaliza a animação de abertura.
      </td>
    </tr>

    <tr>
      <td>
        `onShowStart` <h5>função</h5>
      </td>

      <td>
        retorno de chamada disparado quando o Modal inicia a animação de abertura.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`.

        **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>
  </tbody>
</table>