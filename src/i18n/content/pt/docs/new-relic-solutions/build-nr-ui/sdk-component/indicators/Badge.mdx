---
title: Distintivo
metaDescription: Learn how to work the Badge component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Os emblemas indicam o status de um objeto.

### Uso

```js
import { Badge } from 'nr1'
```

### Exemplos

#### Básico

```js
<div className="nr1-Docs-prettify">
  <Badge>Normal</Badge>
</div>
```

#### Tipos

```js
<div className="nr1-Docs-prettify">
  <Badge>Normal</Badge>
  <Badge type={Badge.TYPE.INFO}>New</Badge>
  <Badge type={Badge.TYPE.SUCCESS}>Success</Badge>
  <Badge type={Badge.TYPE.WARNING}>Warning</Badge>
  <Badge type={Badge.TYPE.CRITICAL}>Critical</Badge>
</div>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` String <h5>OBRIGATÓRIA</h5><h5>|número</h5>
      </td>

      <td>
        String a ser renderizada dentro do emblema. As reticências serão aplicadas se o conteúdo tiver mais de 24 caracteres.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propriedade de espaçamento. O espaçamento é definido como uma tupla de zero a quatro valores, que segue as mesmas convenções das propriedades CSS como `margin` ou `padding`. Para omitir um valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Crachá.SPACING_TYPE.EXTRA_LARGE, Crachá.SPACING_TYPE.LARGE, Crachá.SPACING_TYPE.MEDIUM, Crachá.SPACING_TYPE.NONE, Crachá.SPACING_TYPE.OMIT, Crachá.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`. **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeração</h5>
      </td>

      <td>
        O tipo pode ser:

        * Normal — indica status geral ou em andamento, por exemplo. 'Em breve' ou 'Menor'.

        * Informações - indica um status novo, criado ou de ajuda, por exemplo. 'Novo', 'Beta' ou 'Informações'.

        * Sucesso - indica um estado de sucesso.

        * Aviso - indica itens que requerem estado de aconselhamento.

        * Crítico – indica itens problemáticos.

          <OptionReference>
            Crachá.TYPE.CRITICAL, Crachá.TYPE.INFOBadge.TYPE.NORMAL, Crachá.TYPE.SUCCESS, Crachá.TYPE.WARNING,
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>