---
title: Mensagem embutida
metaDescription: Learn how to work the InlineMessage component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Use as mensagens embutidas quando um elemento exigir atenção. Eles alertam o usuário que informações importantes estão disponíveis ou que uma ação é necessária e desaparecem assim que o usuário toma as medidas apropriadas.

A mensagem embutida usa um ícone como elemento principal para fornecer um indicador visual do tipo e da urgência da mensagem. O ícone pode ser combinado com um rótulo opcional para destacar sua mensagem.

A mensagem embutida revela uma caixa de diálogo que fornece um resumo do problema e links para informações ou ações quando o usuário passa o mouse sobre o ícone ou a combinação de ícone e rótulo.

### Uso

```js
import { InlineMessage } from 'nr1'
```

### Exemplos

#### Tipos

```js
<Stack>
  <StackItem>
    <InlineMessage label="We have updated the data for this chart." />
  </StackItem>
  <StackItem>
    <InlineMessage
      type={InlineMessage.TYPE.SUCCESS}
      label="The entity has been successfully created"
    />
  </StackItem>
  <StackItem>
    <InlineMessage
      type={InlineMessage.TYPE.WARNING}
      label="The edition is restricted for this account."
    />
  </StackItem>
  <StackItem>
    <InlineMessage
      type={InlineMessage.TYPE.CRITICAL}
      label="We couldn't fetch the data."
    />
  </StackItem>
</Stack>
```

#### Rótulo e ação

```js
<InlineMessage
  type={InlineMessage.TYPE.CRITICAL}
  label="We couldn't transform the query."
  action={{ label: 'See the documentation', onClick: console.log }}
/>
```

#### Diálogo com detalhes

```js
<InlineMessage
  type={InlineMessage.TYPE.SUCCESS}
  label="The data has been added."
  description="You have successfully added 3 new hosts."
  action={{ label: 'See the hosts', onClick: console.log }}
/>
```

#### Somente ícone

```js
<InlineMessage
  type={InlineMessage.TYPE.CRITICAL}
  title="We can't render this query."
  description="We tried to transform your query into a query that is compatible with both Metric and previous Infrastructure methods, but some expressions weren’t available."
  action={{ label: 'See NRQL documentation', to: '//docs.newrelic.com' }}
/>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `action` <h5>forma</h5>
      </td>

      <td>
        Você pode especificar uma ação para fornecer ao usuário algumas próximas etapas acionáveis para evitar levá-lo a um beco sem saída.

        <h3>forma</h3>

        `ariaLabel` <h5>corda</h5>

        `label` <h5>Sequência</h5><h5>OBRIGATÓRIA</h5>

        <SideBySide>
          <Side>
            `onClick` <h5>função</h5>
          </Side>

          <Side>
            O retorno de chamada é acionado sempre que o usuário clica no botão.

            <FunctionDefinition
              returnValue={[]}
              arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
            />
          </Side>
        </SideBySide>

        <SideBySide>
          <Side>
            `to` <h5>shape|string</h5>
          </Side>

          <Side>
            Objeto de localização ou string de URL ao qual vincular. <h4>forma</h4> `pathname` <h5>string</h5><h5>OBRIGATÓRIA</h5>

            `search` <h5>corda</h5>

            `hash` <h5>corda</h5>
          </Side>
        </SideBySide>
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabel` <h5>corda</h5>
      </td>

      <td>
        Forneça um rótulo para leitores de tela para descrever a mensagem de status. Isso só será necessário se o rótulo do leitor de tela for diferente do adereço `label` ou se você não estiver fornecendo um adereço `label` .
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>corda</h5>
      </td>

      <td>
        Acrescenta nomes de classes ao componente. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>corda</h5>
      </td>

      <td>
        Deve ser usado para fornecer mais detalhes na caixa de diálogo e complementar o título. Observação: os adereços `title`, `label` e `description` são opcionais, mas pelo menos um deles deve ser fornecido.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>corda</h5>
      </td>

      <td>
        Breve resumo do status. Use o rótulo quando possível para descrever resumidamente o status do componente e quando não houver limitações de espaçamento. Observação: os adereços `title`, `label` e `description` são opcionais, mas pelo menos um deles deve ser fornecido.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo embutido para estilo personalizado. Deve ser usado apenas para fins de posicionamento e espaçamento.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>corda</h5>
      </td>

      <td>
        Adiciona um atributo `data-test-id` . Use-o para destino do componente em testes unitários e E2E. Para que um ID de teste seja válido, prefixe-o com seu ID nerdpack, seguido por um ponto. Por exemplo, `my-nerdpack.some-element`.

        **Nota:** Você pode não ver `data-test-id` atributo pois eles são removidos do DOM, para depurá-los passe um parâmetro de consulta `e2e-test` para a URL.
      </td>
    </tr>

    <tr>
      <td>
        `title` <h5>corda</h5>
      </td>

      <td>
        Título exibido na caixa de diálogo. Observação: os adereços `title`, `label` e `description` são opcionais, mas pelo menos um deles deve ser fornecido.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeração</h5>
      </td>

      <td>
        O tipo pode ser:

        * Informações (padrão) — use-as para indicar o status geral ou em andamento.

        * Sucesso – use-o para comunicar mudanças positivas.

        * Aviso – use-o para comunicar um possível problema, situação indesejada, conselho preventivo ou aviso prévio de algum tipo.

        * Crítico – use-o para comunicar questões problemáticas.

          <OptionReference>
            InlineMessage.TYPE.CRITICAL, InlineMessage.TYPE.INFO, InlineMessage.TYPE.SUCCESS, InlineMessage.TYPE.WARNING
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>