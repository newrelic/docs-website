---
title: 'Tutorial do NerdGraph: listar e revogar URLs de gráficos ao vivo'
tags:
  - APIs
  - NerdGraph
  - Examples
metaDescription: Use New Relic NerdGraph to list and revoke live chart URLs
freshnessValidatedDate: never
translationType: machine
---

Você pode listar e revogar URLs de gráficos ao vivo acessíveis ao público usando consultas e mutações no [NerdGraph](/docs/apis/nerdgraph/get-started/introduction-new-relic-nerdgraph).

Para fazer isso:

1. Vá para o [explorador GraphiQL](https://api.newrelic.com/graphiql).
2. [Liste todos os URLs de gráficos ao vivo](#list) criados em sua conta New Relic.
3. [Revogue qualquer URL de gráfico ao vivo que você desejar](#revoke).

<Callout variant="tip">
  Consulte nossos documentos [para criar gráficos públicos ao vivo](/docs/query-your-data/explore-query-data/dashboards/manage-your-dashboard/#export-share).
</Callout>

## Listar todos os URLs de gráficos ao vivo [#list]

Use a seguinte consulta para recuperar uma lista de URLs de gráficos dinâmicos existentes:

```
{
  actor {
    dashboard {
      liveUrls {
        liveUrls {
          title
          url
          createdAt
          type
        }
        errors {
          description
        }
      }
    }
  }
}
```

## Revogar qualquer URL de gráfico ao vivo [#revoke]

Use a seguinte consulta para revogar o URL do gráfico ao vivo especificado:

```
mutation {
  dashboardWidgetRevokeLiveUrl(uuid: "<enter-the-UUID-of-the-live-chart>") {
    uuid
    errors {
      description
      type
    }
  }
}
```
