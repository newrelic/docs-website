---
subject: Lambda-Extension
releaseDate: '2026-08-19'
version: 2.7.0
features:
  - Added LIM support
  - Added 'entity.name' for logs
enhancements:
  - Updated NEW_RELIC_LABELS configuration to support Transaction and Span attribute propagation.
bugs:
  - Fixed telemetry dispatch timing to send agent payloads immediately upon arrival instead of waiting to pair with platform.report events.
security: []
translationType: machine
---

## メモ

* AWS Lambdaマネージドインスタンス（LMI）のサポートを追加しました。
* `Updated NEW_RELIC_LABELS` トランザクションおよびスパンの属性伝播をサポートする設定。
* サーバーレスモードでの`NEW_RELIC_DATA_COLLECTION_TIMEOUT`および`NEW_RELIC_HTTP_TIMEOUT`のサポートを追加しました
* ログ用の`entity.name`を追加しました。

## 修理済み

* ペアリングを待つのではなく、到着時に直ちにエージェントのペイロードを送信するように、テレメトリーのディスパッチタイミングを修正しました。 `platform.report`
* レガシーなsmithy rustlsのデフォルトを無効にしました