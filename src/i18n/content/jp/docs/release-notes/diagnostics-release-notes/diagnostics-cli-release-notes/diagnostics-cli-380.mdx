---
subject: Diagnostics CLI (nrdiag)
releaseDate: '2026-07-13'
version: 3.8.0
downloadLink: 'https://download.newrelic.com/nrdiag/nrdiag_3.8.0.zip'
translationType: machine
---

## 新機能

* Diagnostics CLIは、New Relicの日本データリージョンをサポートするようになりました。アカウントが日本リージョンにある場合は、CLIの実行時に`region`フラグを指定します（[#263](https://github.com/newrelic/newrelic-diagnostics-cli/pull/263)）：

  ```bash
  ./nrdiag -region JP
  ```

  `region`フラグは`US`、`EU`、または`JP`（大文字と小文字は区別されません）を受け入れ、デフォルトは`US`です。

## 依存関係の更新

* `golang.org/x/net`を0.33.0から0.55.0に更新しました（[#264](https://github.com/newrelic/newrelic-diagnostics-cli/pull/264)）。