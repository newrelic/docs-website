---
title: pauseReplay
type: apiDoc
shortDescription: リプレイの録画を強制的に一時停止する方法
tags:
  - Browser
  - Browser monitoring
  - Browser Agent
metaDescription: A method used to force a replay to pause recording
freshnessValidatedDate: '2024-04-22T00:00:00.000Z'
translationType: machine
---

## 構文

```js
newrelic.pauseReplay()
```

リプレイを強制的に録画を一時停止するために使用されるbrowser API 。

## 要件

* browser Pro、または Pro+SPA エージェント (v1.240.0 以上)

## 説明

`newrelic.pauseReplay()` 手動でリプレイの録画を一時停止するために呼び出すことができます。

再生がまだ進行中でない場合、この API メソッドは効果がありません。

ブラウザ セッションがまだ有効な場合は、 [`newrelic.recordReplay()`](/docs/browser/new-relic-browser/browser-apis/recordReplay) API メソッドを呼び出して一時停止した再生を再開できます。

## 使用例

* ヘルスケア ページ、クレジットカード情報ページなど、特定のサイト ページまたは特定の条件下で、手動でリプレイをトリガーして録画を停止したいとします。

## 例

### リプレイの録画を強制的に一時停止する

```js
newrelic.pauseReplay()
```

### 一時停止した再生を再開する

```js
  newrelic.pauseReplay()
  newrelic.recordReplay()
```