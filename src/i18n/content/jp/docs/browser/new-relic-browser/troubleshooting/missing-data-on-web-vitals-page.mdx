---
title: Web Vitals ページのデータが欠落している
type: troubleshooting
tags:
  - Browser
  - Browser monitoring
  - Troubleshooting
metaDescription: How to troubleshoot missing data in web vitals page
translationType: machine
---

## 問題

**Web Vitals**にデータが表示されません。

## 解決

データが欠落するいくつかの原因を次に示します。

* フィルタが狭すぎます。一部の重要な属性は、特定の Web バイタルにのみ関連しています。たとえば、 `elementID`でフィルタリングした場合、 `elementID`は LCP の測定にのみ適用されるため、データは最大コンテンツ ペイント (LCP) に対してのみ入力されます。
* お使いのブラウザー エージェントまたはエディションは、特定のデータを収集しません。New Relic ブラウザー エージェントが収集する主要な属性については、[ブラウザー エージェント データ ディクショナリ](https://docs.newrelic.com/attribute-dictionary/?dataSource=Browser+agent)を参照してください。
* アプリは`PageViewTiming`データを New Relic に送信できません。Web Vitals は`PageViewTiming`データに基づいて構築されているため、何らかの理由でアプリがインジェストをコレクターに送信できない場合、データが表示されないことがあります。