---
title: IASTをインストールする
tags:
  - New Relic IAST
  - IAST
  - Installation
metaDescription: Learn how to install New Relic IAST.
freshnessValidatedDate: never
translationType: machine
---

import iastSetup from 'images/iast_screenshot-full_install.webp'

import iastInstall from 'images/iast_screenshot-crop_install.webp'

<Callout variant="important">
  IAST は非実稼働デプロイメントのみで実行します。 IAST は、エクスプロイト ペイロードを含む HTTP リクエストを呼び出してテストします。 データの破損とライブコードへのエクスプロイトの導入を避けるために、本番環境以外の環境ではシミュレートされたデータのみを使用して IAST を使用する必要があります。 さらに、実稼働前に新しいコードに対して IAST を実行すると、本番稼働前に潜在的な脆弱性を発見できます。
</Callout>

New Relic IAST をインストールするには:

<Steps>
  <Step>
    **[one.newrelic.com](https://one.newrelic.com) > All capabilities > IAST**に移動し、**Set up IAST with applications** \[アプリケーションで IAST をセットアップする を]クリックします。

    <img
      title="Set up IAST with applications"
      alt="Set up IAST with applications"
      src={iastSetup}
    />
  </Step>

  <Step>
    IAST が本番環境で実行されていないことを確認し、 **Continue** \[続行]をクリックします。  

  </Step>

  <Step>
    アプリケーションの言語を選択し、手順を完了します。

    <img
      title="Install New Relic IAST"
      alt="Install New Relic IAST"
      src={iastInstall}
    />
  </Step>

  <Step>
    `newrelic.yml`構成ファイルが次のように更新されていることを確認してください。

    <CollapserGroup>
      <Collapser
        id="config-file-example"
        title={<><InlineCode>newrelic.yml</InlineCode> 設定ファイル</>}
      >
        ```yml
        host: staging-collector.newrelic.com
        security:
            enabled: true
        agent:
            enabled: true
        validator_service_url: wss://csec-staging.nr-data.net
        ```
      </Collapser>
    </CollapserGroup>
  </Step>

  <Step>
    すべての手順を完了したら、アプリケーションを再起動し、アプリケーションの API に対してトラフィックを生成します。  

  </Step>

  <Step>
    **See your data** \[データを表示]をクリックして、テストしたアプリケーションの概要を表示します。  

  </Step>
</Steps>

詳しい手順については、以下のデモ例をご覧ください。

<CollapserGroup>
  <Collapser
    id="demo-install-java"
    title="Java で IAST をセットアップする方法"
  >
    Java を使用して IAST をセットアップする方法については、このデモ例を参照してください (約 2:48 分):

    <Video
      type="wistia"
      id="dbipyzuyok"
    />
  </Collapser>

  <Collapser
    id="demo-install-nodejs"
    title="Node.js を使用して IAST をセットアップする方法"
  >
    Node.js を使用して IAST をセットアップする方法については、このデモ例を参照してください (約 2:49 分):

    <Video
      type="wistia"
      id="1m2suxuvuz"
    />
  </Collapser>
</CollapserGroup>