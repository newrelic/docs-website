---
title: アプリのメトリックタイムスライスデータを取得する（エクスプローラー）
tags:
  - APIs
  - REST API v2
  - API Explorer v2
metaDescription: How to use the New Relic REST API Explorer (v2) to get metric data for your application.
translationType: machine
---

New Relic の REST API Explorer (v2) を使用して、アプリケーションの [メトリック・タイムスライス・データ](/docs/telemetry-data-platform/ingest-manage-data/understand-data/new-relic-data-types#timeslice-data) を取得する場合、必要となります。

* [APIキー](/docs/apis/rest-api-v2/getting-started/introduction-new-relic-rest-api-v2#api_key)
* [アプリのID](#app_id)
* アプリで利用できるメトリック・タイムスライス・データの種類。 [メトリック名](#view_metrics) と値を含む。

## 自分のアプリのIDを見る [#app_id]

[アプリのIDを見るには](/docs/apm/apis/requirements/identification-code):

1. New Relic REST [API Explorer](https://rpm.newrelic.com/api/explore "リンクが新しいウィンドウで開きます") から、 **Applications> GET List** を選択します。
2. New Relic にサインインしていない場合は、アプリの [API キー](/docs/apis/rest-api-v2/requirements/rest-api-key#viewing) を提供してください。
3. オプション: **Applications > List**から、 `name` 、 `ids` 、または`language`フィルターの値を入力します。
4. Select **Send Request**.
5. **Response**セクションから、アプリの`id`をコピーします。

続けて、 [メトリック名](#view_metrics) を表示する手順を行います。

## アプリのメトリック名の表示 [#view_metrics]

アプリで利用可能なメトリックタイムスライスデータを表示するには

1. New Relic REST API Explorer から **Applications> GET Metric Names** を選択します。
2. From **Applications> Metric Names**, type or paste your [application ID](/docs/apm/apis/requirements/identification-code).
3. 特定の指標または部分的な指標`name`を入力するか、空白のままにして、使用可能な指標の名前と値のリストを表示します。
4. Select **Send Request**.
5. **レスポンス**セクションから、アプリの利用可能な`metrics`を確認します。

続けて、 [メトリック](#app_metric_data) タイムスライス [データ](#app_metric_data) を取得します。

## アプリのタイムスライスデータの取得 [#app_metric_data]

アプリのメトリック値を取得するには、 **Metric Names** の結果から値をコピーして、 **Metric Timeslice Data** のフィールドに貼り付けます。

1. New Relic REST API Explorer から **Applications> GET Metric Data** を選択します。
2. **Applications> Metric Data** から、 [Application ID](/docs/apm/apis/requirements/identification-code) を入力または貼り付けてください。
3. アプリの 1 つ以上の`names` ( **GET Metric Names**から) を入力するか貼り付けます。それぞれの名前を新しい行で開始します。
4. Select **Send Request**.
5. **レスポンス**セクションから、アプリの利用可能な`metric_data`を確認します。

**Response** のセクションを確認した後、引き続き調整を行い、再度 **Send Request** を選択してください。例えば、以下のようになります。

* メトリック タイムスライス データの[時間範囲](/docs/apm/apis/api-v2-examples/specifying-time-range-api-v2)を指定するには、 `from`と`to`の値を入力します。
* 時間範囲にわたるデータの[平均](/docs/apm/apis/requirements/calculating-average-metric-values-summarize)を作成するには、 `summarize`を`true`に設定します。または、指定した範囲の各期間のデータを保持するには、 `summarize`を空白のままにします。

## 他のREST APIデータを見る [#synthetics]

API Explorer を他の New Relic の機能と一緒に使用するには、対応する API キーを選択します。これには以下のものがあります。

* 合成モニタリング
* パートナー