---
title: Security Lake ログを送信するための AWS Lambda
tags:
  - Logs
  - Enable log management in New Relic
  - Enable log monitoring in New Relic
  - AWS Security Lake
metaDescription: Install and configure New Relic logging for AWS Security Lake
freshnessValidatedDate: never
translationType: machine
---

import serverlessAWSLambdaSelectRegion from 'images/serverless_screenshot-crop_AWS-Lambda-select-region.webp'

[AWS サーバーレス アプリケーション リポジトリ](https://serverlessrepo.aws.amazon.com/applications) から簡単にデプロイできるサーバーレス アプリ `newrelic-securitylake-s3-processor-LogForwarder`を使用して、 [Amazon Security Lake ログを](https://docs.aws.amazon.com/security-lake/latest/userguide/internal-sources.html)New Relic に送信できます。

Security Lake からNew Relicにログを転送すると、ログ データの収集、処理、探索、書き込み、集計の機能が強化されます。<InlinePopover type="logs"/>

## サーバーレス アプリケーションをインストールする [#install-app]

Serverless アプリをインストールして Security Lake ログを New Relic に転送するには:

1. <InlinePopover type="licenseKey"/>

   があることを確認してください。

2. ブラウザで[AWS サーバーレス アプリケーション リポジトリ](https://serverlessrepo.aws.amazon.com/applications)を開きます。

   <img title="AWS Lambda - Select region" alt="AWS Lambda - Select region" src={serverlessAWSLambdaSelectRegion}/>

3. `newrelic`を検索し、

   <DoNotTranslate>**Show apps that create custom IAM roles or resource policies**</DoNotTranslate>

   をチェックして`newrelic-securitylake-s3-processor-LogForwarder`を見つけます。

4. `newrelic-securitylake-s3-processor-LogForwarder`の詳細をクリックし、

   <DoNotTranslate>**Deploy**</DoNotTranslate>

   をクリックします。

5. アプリのガイダンスに従って、必要なパラメーターを設定します。

   * `SecurityLakeSQSArn`
   * `SecurityLakeSubscriberRoleArn`
   * `ExternalID`
   * `NRLicenseKey`

セキュリティ レイク プロバイダから `SecurityLakeSQSArn` と `SecurityLakeSubscriberRoleArn` を取得します。

イベント ソース リージョンは Lambda リージョンと一致する必要があることに注意してください。