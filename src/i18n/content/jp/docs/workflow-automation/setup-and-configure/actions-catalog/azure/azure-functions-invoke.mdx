---
title: Azure Functionを呼び出す
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - Azure Functions actions
metaDescription: Documentation for the azure.functions.invoke action in the New Relic workflow automation actions catalog
freshnessValidatedDate: '2026-08-24T00:00:00.000Z'
translationType: machine
---

Azure Functions Invokeアクションを使用すると、HTTPでトリガーされる任意のAzure Functionをワークフローから直接呼び出すことができます。生のHTTP呼び出しを構築するのではなく、Azure Functionsを呼び出すためのサポートおよび検証済みのインターフェイスを提供します — これには型付き入力、URL検証、およびシークレットベースの認証が含まれます。

## Azure Functionを呼び出す [#azure-functions-invoke]

このアクションは、Azure FunctionのエンドポイントにHTTPリクエストを送信し、レスポンスを返します。認証にはAzureの関数キーメカニズムを使用します；ワークフローはそれを`x-functions-key`ヘッダーとして渡します。

<Tabs>
  <TabsBar>
    <TabsBarItem id="azurefunctionsinvokeinputs">
      入力
    </TabsBarItem>

    <TabsBarItem id="azurefunctionsinvokeoutputs">
      出力
    </TabsBarItem>

    <TabsBarItem id="azurefunctionsinvokeexample">
      例
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="azurefunctionsinvokeinputs">
      以下のフィールドでAzure Functionリクエストを構成します。

      <table>
        <thead>
          <tr>
            <th>
              入力フィールド
            </th>

            <th>
              オプション性
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `function`
            </td>

            <td>
              必須
            </td>

            <td>
              弦
            </td>

            <td>
              Azure Function HTTPトリガーエンドポイントの完全なHTTPS URL。例：

              `"https://myapp.azurewebsites.net/api/MyFunction"`

              。
            </td>
          </tr>

          <tr>
            <td>
              `key`
            </td>

            <td>
              オプション
            </td>

            <td>
              弦
            </td>

            <td>
              認証用の関数レベルまたはホストレベルのAPIキー。ワークフローはそれを

              `x-functions-key`

              ヘッダーとして送信します。シークレットとして渡します。
            </td>
          </tr>

          <tr>
            <td>
              `method`
            </td>

            <td>
              必須
            </td>

            <td>
              弦
            </td>

            <td>
              使用するHTTPメソッド。

              `"GET"`

              、

              `"POST"`

              、

              `"PUT"`

              、

              `"DELETE"`

              、

              `"PATCH"`

              のいずれかです。デフォルトは

              `"POST"`

              です。
            </td>
          </tr>

          <tr>
            <td>
              `body`
            </td>

            <td>
              オプション
            </td>

            <td>
              弦
            </td>

            <td>
              JSON文字列としてのリクエスト本文。

              `method`

              が

              `GET`

              または

              `HEAD`

              ではない場合にのみ適用されます。シークレット参照を受け入れます。
            </td>
          </tr>

          <tr>
            <td>
              `headers`
            </td>

            <td>
              オプション
            </td>

            <td>
              地図
            </td>

            <td>
              JSON形式の追加のHTTPヘッダー。例：

              `{"X-Custom-Header": "value", "Content-Type": "application/json"}`

              。シークレット参照を受け入れます。
            </td>
          </tr>

          <tr>
            <td>
              `queryParameters`
            </td>

            <td>
              オプション
            </td>

            <td>
              地図
            </td>

            <td>
              関数URLに追加するクエリパラメーター。

              `?`

              または

              `&`

              で始まってはいけません。例：

              `param1=value1&param2=value2`

              。
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="azurefunctionsinvokeoutputs">
      アクションは以下のフィールドを返します。

      <table>
        <thead>
          <tr>
            <th>
              出力フィールド
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `statusCode`
            </td>

            <td>
              整数
            </td>

            <td>
              Azure Functionから返されるHTTPステータスコード。例：

              `200`

              、

              `404`

              、

              `500`

              。
            </td>
          </tr>

          <tr>
            <td>
              `response`
            </td>

            <td>
              弦
            </td>

            <td>
              Azure Functionからのレスポンス本文。JSON、プレーンテキスト、HTML、またはその他の形式が可能です。
            </td>
          </tr>

          <tr>
            <td>
              `success`
            </td>

            <td>
              ブール値
            </td>

            <td>
              関数呼び出しが成功したかどうかを示します。2xxレスポンス（200～299）の場合は

              `true`

              、それ以外の場合は

              `false`

              。
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="azurefunctionsinvokeexample">
      ### 例：修復機能をトリガーする

      ```yaml
      name: azure-function-invoke-remediation

      steps:
        - name: invokeRemediationFunction
          type: action
          action: azure.functions.invoke
          version: 1
          inputs:
            function: "https://remediation.azurewebsites.net/api/fix-incident"
            key: ${{ :secrets:azure:functionKey }}
            method: "POST"
            body: '{"incidentId":"${{ .workflowInputs.incidentId }}"}'
      ```

      ### 例：クエリパラメーターを含むGETリクエスト

      ```yaml
      name: azure-function-invoke-get

      steps:
        - name: getFromFunction
          type: action
          action: azure.functions.invoke
          version: 1
          inputs:
            function: "https://myapp.azurewebsites.net/api/GetStatus"
            key: ${{ :secrets:azure:functionKey }}
            method: "GET"
            queryParameters: "env=prod&region=eastus"
      ```

      ### 例：関数を呼び出して結果をログに記録する

      ```yaml
      name: azure-function-invoke-and-log

      steps:
        - name: invokeFunction
          type: action
          action: azure.functions.invoke
          version: 1
          inputs:
            function: "https://myapp.azurewebsites.net/api/RunCheck"
            key: ${{ :secrets:azure:functionKey }}
            method: "POST"
            body: '{"target":"${{ .workflowInputs.target }}"}'
            headers:
              Content-Type: "application/json"

        - name: logFunctionResult
          type: action
          action: newrelic.ingest.sendLogs
          version: 1
          inputs:
            logs:
              - message: "Azure Function invocation complete"
                attributes:
                  success: ${{ .steps.invokeFunction.outputs.success }}
                  statusCode: ${{ .steps.invokeFunction.outputs.statusCode }}
                  response: ${{ .steps.invokeFunction.outputs.response }}
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>