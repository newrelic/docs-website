---
title: Jenkinsキューアクション
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - Jenkins actions
  - Jenkins queue actions
metaDescription: 'Reference for Jenkins queue actions in the workflow automation actions catalog: get queue item, cancel, and list.'
freshnessValidatedDate: never
translationType: machine
---

このページでは、ワークフロー自動化アクションカタログで利用可能なJenkinsキューアクションのリファレンスを提供します。これらのアクションを使用して、Jenkinsビルドキューで待機しているビルドを検査および管理してください。

## 前提条件

ワークフロー自動化でJenkinsキューアクションを使用する前に、以下が必要です：

* HTTPS経由でNew Relicワークフローオートメーションから到達可能なJenkinsサーバー。Jenkinsインスタンスがファイアウォールの背後またはプライベートネットワーク内にある場合は、New Relicワークフローオートメーションからのインバウンドアクセスを許可してください。
* ワークフローを実行するアカウントのJenkins APIトークン。Jenkinsユーザープロファイルから生成します：**Configure &gt; API Token &gt; Add new Token**。
* `username:apiToken`のBase64エンコードされた値を含むNew Relic Secrets Serviceのシークレット。ワークフロー内で`${{ :secrets:<keyName> }}`として参照します。

## Jenkinsキューアイテムを取得します

アクション識別子は`jenkins.queue.getItem`です。

実行が開始されると、ビルド番号を含むキューアイテムの詳細を取得します。[`jenkins.job.trigger`](/docs/workflow-automation/setup-and-configure/actions-catalog/jenkins/jenkins-job#trigger-a-jenkins-job) でビルドをトリガーした後にキューをポーリングし、他のジョブアクションで必要となるビルド番号を取得するには、このアクションを使用します。

ビルドの実行が開始されると、レスポンスの `executable.number` フィールドにビルド番号が含まれます。アイテムがまだキューに入っている間は `null` です。

<Tabs>
  <TabsBar>
    <TabsBarItem id="jenkins-queue-getitem-inputs">
      入力
    </TabsBarItem>

    <TabsBarItem id="jenkins-queue-getitem-outputs">
      出力
    </TabsBarItem>

    <TabsBarItem id="jenkins-queue-getitem-example">
      例
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="jenkins-queue-getitem-inputs">
      次の表は、このアクションで利用可能なすべての入力フィールドについて説明しています。

      <table>
        <thead>
          <tr>
            <th>
              入力
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>

            <th>
              例
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              **authToken**
            </td>

            <td>
              文字列（シークレット）
            </td>

            <td>
              **必須。**

              HTTP Basic認証用のBase64エンコードされた

              `username:apiToken`

              です。
            </td>

            <td>
              `${{ :secrets:jenkinsAuthToken }}`
            </td>
          </tr>

          <tr>
            <td>
              **url**
            </td>

            <td>
              弦
            </td>

            <td>
              **必須。**

              JenkinsサーバーのURL。
            </td>

            <td>
              `https://jenkins.example.com`
            </td>
          </tr>

          <tr>
            <td>
              **queueId**
            </td>

            <td>
              Int
            </td>

            <td>
              **必須。**

              `jenkins.job.trigger`

              から返されたキューアイテムID。実行が開始されたら、これを使用してビルドを追跡し、ビルド番号を取得します。
            </td>

            <td>
              `123`
            </td>
          </tr>

          <tr>
            <td>
              **selectors**
            </td>

            <td>
              リスト
            </td>

            <td>
              **オプション。**

              HTTPレスポンスから特定のフィールドを抽出するためのJQセレクタ。
            </td>

            <td>
              `[{"name": "buildNumber", "expression": ".responseBody | fromjson.executable.number // null"}]`
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="jenkins-queue-getitem-outputs">
      次の表は、このアクションによって返されるすべての出力フィールドについて説明しています。

      <table>
        <thead>
          <tr>
            <th>
              出力
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              **response**
            </td>

            <td>
              オブジェクト
            </td>

            <td>
              キューアイテムの詳細。ビルドの実行が開始されると、`executable.number`にはビルド番号が含まれます — アイテムがまだキューにある間は`null`です。

              ```json
              {
                "id": 123,
                "blocked": false,
                "buildable": false,
                "cancelled": false,
                "why": null,
                "executable": {
                  "number": 42,
                  "url": "http://jenkins.example.com/job/my-job/42/"
                },
                "task": {
                  "name": "my-job",
                  "url": "http://jenkins.example.com/job/my-job/"
                },
                "inQueueSince": 1625234567000
              }
              ```
            </td>
          </tr>

          <tr>
            <td>
              **success**
            </td>

            <td>
              ブール値
            </td>

            <td>
              `true`

               成功時は、失敗時は

              `false`

              。
            </td>
          </tr>

          <tr>
            <td>
              **errorMessage**
            </td>

            <td>
              弦
            </td>

            <td>
              リクエストが失敗した場合のエラーメッセージです。成功した場合は

              `null`

              。
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="jenkins-queue-getitem-example">
      以下の例では、ビルドをトリガーし、ビルド番号が利用可能になるまでキューをポーリングします。

      <table>
        <thead>
          <tr>
            <th>
              ワークフローの例
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              ```yaml
              name: poll-jenkins-queue
              description: 'Trigger a build and poll the queue until the build number is available'
              steps:
                - name: trigger_build
                  type: action
                  action: jenkins.job.trigger
                  version: 1
                  inputs:
                    authToken: ${{ :secrets:jenkinsAuthToken }}
                    url: "https://jenkins.example.com"
                    jobName: "my-application-build"
                    selectors:
                      - name: "queueId"
                        expression: ".queueId"
                  next: poll_queue

                - name: poll_queue
                  type: loop
                  for:
                    in: ${{ [range(0; 10)] }}
                    steps:
                      - name: get_queue_item
                        type: action
                        action: jenkins.queue.getItem
                        version: 1
                        inputs:
                          authToken: ${{ :secrets:jenkinsAuthToken }}
                          url: "https://jenkins.example.com"
                          queueId: ${{ .steps.trigger_build.outputs.queueId }}
                          selectors:
                            - name: "buildNumber"
                              expression: ".responseBody | fromjson.executable.number // null"

                      - name: check_if_started
                        type: switch
                        switch:
                          - condition: ${{ .steps.get_queue_item.outputs.buildNumber != null }}
                            next: end
                        next: wait_and_retry

                      - name: wait_and_retry
                        type: wait
                        seconds: 2
                        next: continue
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## キューに入っているJenkinsビルドをキャンセルする

アクション識別子は`jenkins.queue.cancel`です。

実行が開始される前に、キューに入れられたJenkinsビルドをキャンセルします。以前の`jenkins.job.trigger`呼び出しから取得した`queueId`が必要です。

ビルドの実行がすでに開始されている場合、このアクションは失敗します。すでに実行中のビルドを停止するには、代わりに [`jenkins.job.stop`](/docs/workflow-automation/setup-and-configure/actions-catalog/jenkins/jenkins-job#stop-a-running-jenkins-build) を使用してください。

<Tabs>
  <TabsBar>
    <TabsBarItem id="jenkins-queue-cancel-inputs">
      入力
    </TabsBarItem>

    <TabsBarItem id="jenkins-queue-cancel-outputs">
      出力
    </TabsBarItem>

    <TabsBarItem id="jenkins-queue-cancel-example">
      例
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="jenkins-queue-cancel-inputs">
      次の表は、このアクションで利用可能なすべての入力フィールドについて説明しています。

      <table>
        <thead>
          <tr>
            <th>
              入力
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>

            <th>
              例
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              **authToken**
            </td>

            <td>
              文字列（シークレット）
            </td>

            <td>
              **必須。**

              HTTP Basic認証用のBase64エンコードされた

              `username:apiToken`

              です。
            </td>

            <td>
              `${{ :secrets:jenkinsAuthToken }}`
            </td>
          </tr>

          <tr>
            <td>
              **url**
            </td>

            <td>
              弦
            </td>

            <td>
              **必須。**

              JenkinsサーバーのURL。
            </td>

            <td>
              `https://jenkins.example.com`
            </td>
          </tr>

          <tr>
            <td>
              **queueId**
            </td>

            <td>
              Int
            </td>

            <td>
              **必須。**

              キャンセルするキューアイテムID。アイテムはまだキュー内にあり、実行されていない必要があります。
            </td>

            <td>
              `123`
            </td>
          </tr>

          <tr>
            <td>
              **selectors**
            </td>

            <td>
              リスト
            </td>

            <td>
              **オプション。**

              HTTPレスポンスから特定のフィールドを抽出するためのJQセレクタ。
            </td>

            <td>
              `[{"name": "statusCode", "expression": ".statusCode"}]`
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="jenkins-queue-cancel-outputs">
      次の表は、このアクションによって返されるすべての出力フィールドについて説明しています。

      <table>
        <thead>
          <tr>
            <th>
              出力
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              **response**
            </td>

            <td>
              オブジェクト
            </td>

            <td>
              キャンセルが成功すると、JenkinsはHTTP 204（No Content）を返します。キューアイテムが見つからない場合、またはすでに実行が開始されている場合、Jenkinsは404を返します。例： 

              `{ "statusCode": 204, "body": "" }`
            </td>
          </tr>

          <tr>
            <td>
              **success**
            </td>

            <td>
              ブール値
            </td>

            <td>
              `true`

               成功時は、失敗時は

              `false`

              。
            </td>
          </tr>

          <tr>
            <td>
              **errorMessage**
            </td>

            <td>
              弦
            </td>

            <td>
              キャンセルに失敗した場合のエラーメッセージ。成功した場合は

              `null`

              。
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="jenkins-queue-cancel-example">
      次の例では、Jenkinsビルドをトリガーし、実行が開始される前にキャンセルします。

      <table>
        <thead>
          <tr>
            <th>
              ワークフローの例
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              ```yaml
              name: cancel-queued-jenkins-build
              description: 'Trigger a Jenkins build then cancel it before it starts executing'
              steps:
                - name: trigger_build
                  type: action
                  action: jenkins.job.trigger
                  version: 1
                  inputs:
                    authToken: ${{ :secrets:jenkinsAuthToken }}
                    url: "https://jenkins.example.com"
                    jobName: "my-application-build"
                    selectors:
                      - name: "queueId"
                        expression: ".queueId"
                  next: cancel_build

                - name: cancel_build
                  type: action
                  action: jenkins.queue.cancel
                  version: 1
                  inputs:
                    authToken: ${{ :secrets:jenkinsAuthToken }}
                    url: "https://jenkins.example.com"
                    queueId: ${{ .steps.trigger_build.outputs.queueId }}
                  next: end
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Jenkinsビルドキュー内のすべてのアイテムを一覧表示します。

アクション識別子は`jenkins.queue.list`です。

現在Jenkinsビルドキューにあるすべてのアイテムを一覧表示します。

<Tabs>
  <TabsBar>
    <TabsBarItem id="jenkins-queue-list-inputs">
      入力
    </TabsBarItem>

    <TabsBarItem id="jenkins-queue-list-outputs">
      出力
    </TabsBarItem>

    <TabsBarItem id="jenkins-queue-list-example">
      例
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="jenkins-queue-list-inputs">
      次の表は、このアクションで利用可能なすべての入力フィールドについて説明しています。

      <table>
        <thead>
          <tr>
            <th>
              入力
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>

            <th>
              例
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              **authToken**
            </td>

            <td>
              文字列（シークレット）
            </td>

            <td>
              **必須。**

              HTTP Basic認証用のBase64エンコードされた

              `username:apiToken`

              です。
            </td>

            <td>
              `${{ :secrets:jenkinsAuthToken }}`
            </td>
          </tr>

          <tr>
            <td>
              **url**
            </td>

            <td>
              弦
            </td>

            <td>
              **必須。**

              JenkinsサーバーのURL。
            </td>

            <td>
              `https://jenkins.example.com`
            </td>
          </tr>

          <tr>
            <td>
              **selectors**
            </td>

            <td>
              リスト
            </td>

            <td>
              **オプション。**

              HTTPレスポンスから特定のフィールドを抽出するためのJQセレクタ。
            </td>

            <td>
              `[{"name": "queueCount", "expression": ".responseBody | fromjson.items | length"}]`
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="jenkins-queue-list-outputs">
      次の表は、このアクションによって返されるすべての出力フィールドについて説明しています。

      <table>
        <thead>
          <tr>
            <th>
              出力
            </th>

            <th>
              タイプ
            </th>

            <th>
              説明
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              **response**
            </td>

            <td>
              オブジェクト
            </td>

            <td>
              すべてのキューアイテム。各アイテムには、`id`、`task`（ジョブ情報）、および`executable`（ビルド開始後のビルド情報）が含まれます。

              ```json
              {
                "items": [
                  {
                    "id": 123,
                    "blocked": false,
                    "buildable": true,
                    "cancelled": false,
                    "why": "Waiting for next available executor",
                    "task": {
                      "name": "job1",
                      "url": "http://jenkins.example.com/job/job1/"
                    },
                    "inQueueSince": 1625234567000
                  }
                ]
              }
              ```
            </td>
          </tr>

          <tr>
            <td>
              **success**
            </td>

            <td>
              ブール値
            </td>

            <td>
              `true`

               成功時は、失敗時は

              `false`

              。
            </td>
          </tr>

          <tr>
            <td>
              **errorMessage**
            </td>

            <td>
              弦
            </td>

            <td>
              リクエストが失敗した場合のエラーメッセージです。成功した場合は

              `null`

              。
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="jenkins-queue-list-example">
      以下の例では、Jenkinsビルドキュー内のすべてのアイテムをリストし、カウントを抽出します。

      <table>
        <thead>
          <tr>
            <th>
              ワークフローの例
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              ```yaml
              name: list-jenkins-queue
              description: 'List all items currently in the Jenkins build queue'
              steps:
                - name: list_queue
                  type: action
                  action: jenkins.queue.list
                  version: 1
                  inputs:
                    authToken: ${{ :secrets:jenkinsAuthToken }}
                    url: "https://jenkins.example.com"
                    selectors:
                      - name: "queueItems"
                        expression: ".responseBody | fromjson.items"
                      - name: "queueCount"
                        expression: ".responseBody | fromjson.items | length"
                  next: end
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>
  </TabsPages>
</Tabs>