---
title: ワークフロー入力の使用
tags:
  - workflow automation
  - workflow
  - inputs
  - variables
metaDescription: Pass dynamic values at runtime using workflow inputs in endpoint URLs and other input fields.
freshnessValidatedDate: never
translationType: machine
---

エンドポイント URL やその他の入力フィールドで実行時に動的な値を渡すには、 `${{ .workflowInputs.variableName }}`使用します。

**このアプローチでできること:**

* ワークフローがトリガーされたときに動的な入力値を受け入れる
* ワークフローステップ全体でテンプレート構文を使用して入力値を参照する
* さまざまな論点でワークフローの再利用を可能にする

## 例

```json
  {
    "inputs": [
      {
        "key": "urlParams",
        "value": "{\"filter\": \"active\"}"
      },
      {
        "key": "headers",
        "value": "{\"Api-Key\": \"your-api-key\"}"
      }
    ]
  }
```

## 関連トピック [#related-topics]

<DocTiles>
  <DocTile title="データの受け渡し" path="/docs/workflow-automation/workflow-examples/basic-patterns/data-passing">
    ワークフローステップ間で入力を渡す
  </DocTile>

  <DocTile title="ワークフロー実行APIの開始" path="/docs/workflow-automation/workflow-automation-apis/start-workflow-run">
    API経由でワークフローを開始するときに入力を提供する
  </DocTile>

  <DocTile title="定義スキーマ: ワークフロー入力" path="/docs/workflow-automation/workflow-automation-apis/definition-schema#workflowinputs">
    完全な入力検証リファレンス
  </DocTile>
</DocTiles>