---
title: お気に入りテーブル行セル
metaDescription: Learn how to work the FavoriteTableRowCell component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

特定のエンティティのお気に入りステータスを表示する行テーブル セルをレンダリングします。 お気に入りのステータスは`checked`プロパティを通じて制御でき、ステータスの変更は`onChange`プロパティを通じてリッスンできます。

### 使用方法

```js
import { FavoriteTableRowCell } from 'nr1'
```

### 例

#### 例1

```js
function render() {
  const items = [
    {
      name: 'Login service',
      alertSeverity: 'CRITICAL',
      reporting: true,
      favorite: true,
    },
    {
      name: 'Service Gateway',
      alertSeverity: 'NOT_ALERTING',
      reporting: true,
      favorite: false,
    },
  ];
  return (
    <Table items={items}>
      <TableHeader>
        <TableHeaderCell width="56px" />
        <TableHeaderCell>Entity</TableHeaderCell>
      </TableHeader>
      {({ item }) => (
        <TableRow>
          <FavoriteTableRowCell checked={item.favorite} />
          <EntityTitleTableRowCell value={item} />
        </TableRow>
      )}
    </Table>
  );
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `checked` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、ユーザーがエンティティをお気に入りとしてマークしたことを示します。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        お気に入りのステータスが変更されるたびにコールバックが起動されます。 新しいお気に入りステータスは 2 番目として渡されます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""},{"name":"checked","type":"boolean","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>形状|文字列</h5>
      </td>

      <td>
        リンク先の場所オブジェクトまたは URL 文字列。リンクされた`TableRowCell`はスタイル設定されておらず、外部リンクのアイコンは表示されません。 リンクコンポーネントと同じスタイルが必要な場合は、セル内の子コンポーネントとして代わりに`Link`を使用します。shape `pathname`<h3>必須</h3><h5>文字</h5><h5>列</h5>

        `search` <h5>ストリング</h5>

        `hash` <h5>ストリング</h5>
      </td>
    </tr>
  </tbody>
</table>