---
title: セクションメッセージ
metaDescription: Learn how to work the SectionMessage component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

## セクションメッセージ

セクション メッセージを使用して、セクションまたはアイテムのグループのステータスを通知します。

セクション メッセージはユーザーが閉じることはできません。 状況が解決されたら、またはユーザーが適切なアクションを実行したら、それらを削除します。

### 使用方法

```js
import { SectionMessage } from 'nr1'
```

### 例

#### 種類

```js
<Stack
  directionType={Stack.DIRECTION_TYPE.VERTICAL}
  horizontalType={Stack.HORIZONTAL_TYPE.FILL}
>
  <StackItem>
    <SectionMessage
      description="Your Metric query has been transformed into a compatible query with the previous Infrastructure metric format."
      actions={[{ label: 'See our docs', to: 'https://support.newrelic.com/' }]}
    />
  </StackItem>
  <StackItem>
    <SectionMessage
      type={SectionMessage.TYPE.SUCCESS}
      title="File has been uploaded"
      description="We are unable to save any progress at this time."
      actions={[
        { label: 'Primary action', onClick: console.log },
        { label: 'Secondary action', onClick: console.log },
      ]}
    />
  </StackItem>
  <StackItem>
    <SectionMessage
      type={SectionMessage.TYPE.WARNING}
      title="Cannot connect to the database"
      description="We are unable to save any progress at this time."
      actions={[
        { label: 'Primary action', onClick: console.log },
        { label: 'Secondary action', onClick: console.log },
      ]}
    />
  </StackItem>
  <StackItem>
    <SectionMessage
      type={SectionMessage.TYPE.CRITICAL}
      title="Error on the request"
      description="We tried to transform your query into a query that is compatible with both new Metric and previous Infrastructure metric formats, but the transformation process failed."
      actions={[
        { label: 'Contact support', to: 'https://support.newrelic.com/' },
      ]}
    />
  </StackItem>
</Stack>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `actions` <h5>形\[]</h5>
      </td>

      <td>
        最大 2 つのアクションを使用して、ユーザーが別のビューに移動したり、バックグラウンド イベントをトリガーしたり、ナードレット/セクションの動作を変更したりできるようにします。

        <h3>形</h3>

        `ariaLabel` <h5>ストリング</h5>

        `label` <h5>必須</h5><h5>文字列</h5>

        <SideBySide>
          <Side>
            `onClick` <h5>機能</h5>
          </Side>

          <Side>
            ユーザーがボタンをクリックするたびにコールバックが起動されます。

            <FunctionDefinition
              returnValue={[]}
              arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
            />
          </Side>
        </SideBySide>

        <SideBySide>
          <Side>
            `to` <h5>形状|文字列</h5>
          </Side>

          <Side>
            リンク先の場所オブジェクトまたは URL 文字列。

            <h3>形</h3>

            `pathname` <h5>必須</h5><h5>文字列</h5>

            `search` <h5>ストリング</h5>

            `hash` <h5>ストリング</h5>
          </Side>
        </SideBySide>
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        これを使用して、さらに詳しい情報を提供し、ユーザーが実行すべき手順があれば説明します。

        注: `title`および`description`プロパティはオプションですが、少なくとも 1 つは指定する必要があります。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `title` <h5>ストリング</h5>
      </td>

      <td>
        メイン メッセージを提供するために使用します。注: `title`および`description`プロパティはオプションですが、少なくとも 1 つは指定する必要があります。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        タイプは次のようになります:

        * 情報 (デフォルト) - 何かが新しいこと、アイテムが作成されたばかりであること、またはヘルプを提供することを示すために使用します。

        * 成功 - 肯定的な変化や状況を伝えるために使用します。

        * 警告 - 起こりうる問題、望ましくない状況、注意すべきアドバイス、または何らかの事前通知を伝えるために使用します。

        * 重要 - 問題を伝えるために使用します。

          <OptionReference>
            セクションメッセージ.TYPE.CRITICAL、セクションメッセージ.TYPE.INFO、セクションメッセージ.TYPE.SUCCESS、セクションメッセージ.TYPE.WARNING、
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>