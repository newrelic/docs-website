---
title: インラインメッセージ
metaDescription: Learn how to work the InlineMessage component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

要素に注意が必要な場合は、インライン メッセージを使用します。 これらは、重要な情報が利用可能であること、またはアクションが必要であることをユーザーに通知し、ユーザーが適切なアクションを実行すると消えます。

インライン メッセージでは、アイコンをメイン要素として使用し、メッセージの種類と緊急性を視覚的に示します。 アイコンをオプションのラベルと組み合わせて、メッセージを強調表示できます。

インライン メッセージでは、ユーザーがアイコンまたはアイコンとラベルの組み合わせにマウスを移動したときに、問題の概要と情報またはアクションへのリンクを提供するダイアログが表示されます。

### 使用方法

```js
import { InlineMessage } from 'nr1'
```

### 例

#### 種類

```js
<Stack>
  <StackItem>
    <InlineMessage label="We have updated the data for this chart." />
  </StackItem>
  <StackItem>
    <InlineMessage
      type={InlineMessage.TYPE.SUCCESS}
      label="The entity has been successfully created"
    />
  </StackItem>
  <StackItem>
    <InlineMessage
      type={InlineMessage.TYPE.WARNING}
      label="The edition is restricted for this account."
    />
  </StackItem>
  <StackItem>
    <InlineMessage
      type={InlineMessage.TYPE.CRITICAL}
      label="We couldn't fetch the data."
    />
  </StackItem>
</Stack>
```

#### ラベルとアクション

```js
<InlineMessage
  type={InlineMessage.TYPE.CRITICAL}
  label="We couldn't transform the query."
  action={{ label: 'See the documentation', onClick: console.log }}
/>
```

#### 詳細ダイアログ

```js
<InlineMessage
  type={InlineMessage.TYPE.SUCCESS}
  label="The data has been added."
  description="You have successfully added 3 new hosts."
  action={{ label: 'See the hosts', onClick: console.log }}
/>
```

#### アイコンのみ

```js
<InlineMessage
  type={InlineMessage.TYPE.CRITICAL}
  title="We can't render this query."
  description="We tried to transform your query into a query that is compatible with both Metric and previous Infrastructure methods, but some expressions weren’t available."
  action={{ label: 'See NRQL documentation', to: '//docs.newrelic.com' }}
/>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `action` <h5>形</h5>
      </td>

      <td>
        ユーザーが行き詰まるのを避けるために、実行可能な次のステップをユーザーに提供するアクションを指定できます。

        <h3>形</h3>

        `ariaLabel` <h5>ストリング</h5>

        `label` <h5>必須</h5><h5>文字列</h5>

        <SideBySide>
          <Side>
            `onClick` <h5>機能</h5>
          </Side>

          <Side>
            ユーザーがボタンをクリックするたびにコールバックが起動されます。

            <FunctionDefinition
              returnValue={[]}
              arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
            />
          </Side>
        </SideBySide>

        <SideBySide>
          <Side>
            `to` <h5>形状|文字列</h5>
          </Side>

          <Side>
            リンク先の場所オブジェクトまたは URL 文字列。 <h4>形状</h4>`pathname`<h5>必須</h5><h5>文字列</h5>

            `search` <h5>ストリング</h5>

            `hash` <h5>ストリング</h5>
          </Side>
        </SideBySide>
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        スクリーン リーダーにステータス メッセージを説明するラベルを提供します。 これは、スクリーン リーダーのラベルが`label`プロパティと異なる場合、または`label`プロパティをまったく提供していない場合にのみ必要です。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        ダイアログに詳細を追加し、タイトルを補足するために使用する必要があります。注: `title` 、 `label` 、および`description`プロパティはオプションですが、少なくとも 1 つは指定する必要があります。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>ストリング</h5>
      </td>

      <td>
        ステータスの簡単な要約。 コンポーネントのステータスを簡単に説明でき、スペース制限がない場合には、ラベルを使用します。注: `title` 、 `label` 、および`description`プロパティはオプションですが、少なくとも 1 つは指定する必要があります。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `title` <h5>ストリング</h5>
      </td>

      <td>
        ダイアログに表示されるタイトル。注: `title` 、 `label` 、および`description`プロパティはオプションですが、少なくとも 1 つは指定する必要があります。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        タイプは次のようになります:

        * 情報 (デフォルト) - 一般的なステータスまたは進行中のステータスを示すために使用します。

        * 成功 — それを活用して前向きな変化を伝えます。

        * 警告 — 起こりうる問題、望ましくない状況、注意を促すアドバイス、または何らかの事前通知を伝えるために使用します。

        * 重要 — 問題のある事項を伝えるために使用します。

          <OptionReference>
            InlineMessage.TYPE.CRITICAL、InlineMessage.TYPE.INFO、InlineMessage.TYPE.SUCCESS、InlineMessage.TYPE.WARNING
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>