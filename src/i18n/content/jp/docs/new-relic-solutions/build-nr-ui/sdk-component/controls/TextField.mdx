---
title: テキストフィールド
metaDescription: Learn how to work the TextField component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

フォームを使用すると、ユーザーは入力を行ってタスクを完了できます。

### 使用方法

```js
import { TextField } from 'nr1'
```

### 例

#### ベーシック

```js
<TextField label="Username" placeholder="e.g. John Doe" />
```

#### インラインラベル付き

```js
<TextField label="Username" labelInline placeholder="e.g. John Doe" />
```

#### 検索

```js
<TextField type={TextField.TYPE.SEARCH} placeholder="e.g. Placeholder" />
```

#### パスワード

```js
<TextField type={TextField.TYPE.PASSWORD} placeholder="e.g. Placeholder" />
```

#### フォーマット済み

```js
<TextField type={TextField.TYPE.PREFORMATTED} placeholder="npm run build" />
```

#### 読み込み中

```js
<TextField loading placeholder="e.g Placeholder" />
```

#### 無効

```js
<TextField defaultValue="Some value" disabled placeholder="e.g Placeholder" />
```

#### 読み取り専用

```js
<TextField defaultValue="Some value" readOnly placeholder="e.g Placeholder" />
```

#### 説明付き

```js
<TextField description="Description value" placeholder="e.g Placeholder" />
```

#### 無効なメッセージ

```js
<TextField
  invalid="Not enough text, needs more"
  placeholder="e.g Placeholder"
/>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        この入力を説明するためにスクリーン リーダー用のラベルを指定します。 これは、スクリーン リーダーのラベルが`label`プロパティと異なる場合、または`label`プロパティをまったく提供していない場合にのみ必要です。
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabelledBy` <h5>ストリング</h5>
      </td>

      <td>
        スクリーン リーダー用にこの入力を説明する内容を持つ要素の一意の`id`を指定します。 これは、スクリーン リーダーのラベルが`label`プロパティと異なる場合、または`label`プロパティをまったく提供していない場合にのみ必要です。 これか`ariaLabel`のどちらか一方を使用してください。両方使用しないでください。
      </td>
    </tr>

    <tr>
      <td>
        `autoFocus` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、テキスト フィールドはマウント時に自動的にフォーカスされます。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>ストリング</h5>
      </td>

      <td>
        テキスト フィールドのデフォルト値。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        フォーム フィールドに入力する方法を説明するメッセージ。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        フィールドが編集可能かどうかを決定します。
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>ストリング</h5>
      </td>

      <td>
        追加情報は、ラベルの横にある情報ツールチップに表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>ブール値|文字列</h5>
      </td>

      <td>
        true の場合、この特定のフィールドに対してユーザーの注意が必要であることを通知するために、フィールドを無効な状態に設定しています。 このプロパティは、 `boolean`フィールドまたは`string`になります。 `string`の場合は、無効な状態が表示されるとともに、下にテキストが表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>ストリング</h5>
      </td>

      <td>
        ラベルとして表示するテキスト。
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>ブール値</h5>
      </td>

      <td>
        フォーム コントロール内にラベルを表示します。コンポーネントが`Form`内にない場合にのみ使用します。 その場合は、 `Form`コンポーネントで`layoutType`を`Form.LAYOUT_TYPE.SPLIT`に設定します。
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>ブール値</h5>
      </td>

      <td>
        フォーム フィールドにデータのリストまたは他の何かを読み込む場合は、読み込み状態フォームを使用して、何かが読み込まれていることをユーザーに示します。フィールドが値を動的に読み込む可能性があることを通知するために使用されます。 `true`に設定すると、読み込みスピナーが表示されます。 `false`に設定すると、スピナーを表示できるように UI 内にスペースが確保されます。フィールドが特定の時点で読み込まれることが予想されない場合は、このプロパティを設定しないでください (または`undefined`に設定してください)。
      </td>
    </tr>

    <tr>
      <td>
        `multiline` <h5>非推奨の</h5><h5>ブール値</h5>
      </td>

      <td>
        <Callout
          variant="caution"
          title="2022年1月1日締切"
        >
          「multiline」プロパティは非推奨です。複数行の場合は、代わりに MultilineTextField コンポーネントを使用してください。
        </Callout>

        入力が 1 行以上を受け入れるかどうか。 これは \[Enter] キーの動作にも影響し、単一行フィールドでは何も実行されず、新しい行が作成されます。
      </td>
    </tr>

    <tr>
      <td>
        `onBlur` <h5>機能</h5>
      </td>

      <td>
        テキスト フィールドがフォーカスを失ったときに発生するコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        テキスト フィールドの値が変更されたときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback. You can pull out the new value by accessing `event.target.value`."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onFocus` <h5>機能</h5>
      </td>

      <td>
        テキスト フィールドにフォーカスが当たったときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyDown` <h5>機能</h5>
      </td>

      <td>
        キーが押されたときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyUp` <h5>機能</h5>
      </td>

      <td>
        キーが放されたときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `placeholder` <h5>ストリング</h5>
      </td>

      <td>
        テキスト フィールドに値がない場合に表示されるヒント。
      </td>
    </tr>

    <tr>
      <td>
        `readOnly` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、テキスト フィールドは読み取り専用になります。
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、フォーム フィールドが必須であることを示します。
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          TextField.SPACING_TYPE.EXTRA_LARGE、TextField.SPACING_TYPE.LARGE、TextField.SPACING_TYPE.MEDIUM、TextField.SPACING_TYPE.NONE、TextField.SPACING_TYPE.OMIT、TextField.SPACING_TYPE.SMALL、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spellCheck` <h5>ブール値</h5>
      </td>

      <td>
        `true` の場合、 browserのスペルエラー チェックが有効になります。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。 **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        * テキスト - ユーザーがフィールドに1行のテキストを入力するとき

        * メール - ユーザーにメールアドレスを入力させる必要がある場合

        * 検索 — ユーザーが検索する必要があるとき

        * フォーマット済み - ユーザーがコードコンテンツを入力する必要がある場合

        * パスワード - ユーザーがパスワードを入力するとき

        * URL — ユーザーがURLを入力したとき

          <OptionReference>
            TextField.TYPE.EMAIL、TextField.TYPE.PASSWORD、TextField.TYPE.PREFORMATTED、TextField.TYPE.SEARCH、TextField.TYPE.TEXT、TextField.TYPE.URL、
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>ストリング</h5>
      </td>

      <td>
        テキスト フィールドの値。これは制御されたコンポーネントに必須です。
      </td>
    </tr>
  </tbody>
</table>