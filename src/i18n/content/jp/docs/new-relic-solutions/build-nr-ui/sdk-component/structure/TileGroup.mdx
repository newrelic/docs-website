---
title: タイルグループ
metaDescription: Learn how to work the TileGroup component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

### 使用方法

```js
import { TileGroup } from 'nr1'
```

### 例

#### 基本タイルグループ

```js
<TileGroup>
  <Tile onClick={console.log}>Tile 1</Tile>
  <Tile onClick={console.log}>Tile 2</Tile>
  <Tile onClick={console.log}>Tile 3</Tile>
</TileGroup>
```

#### 隙間の小さいタイルグループ

```js
<TileGroup gapType={TileGroup.GAP_TYPE.SMALL}>
  <Tile onClick={console.log}>Tile 1</Tile>
  <Tile onClick={console.log}>Tile 2</Tile>
  <Tile onClick={console.log}>Tile 3</Tile>
</TileGroup>
```

#### 3列のタイルグループ

```js
<TileGroup tileWidth="4fr">
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### カスタムタイル幅のタイルグループ

```js
<TileGroup tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### 選択可能なタイル

```js
<TileGroup selectionType={TileGroup.SELECTION_TYPE.SINGLE} tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### デフォルトのタイルがチェックされている選択可能なタイル

```js
<TileGroup
  defaultValue="tile-0"
  selectionType={TileGroup.SELECTION_TYPE.SINGLE}
  tileWidth={120}
>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile value={`tile-${index}`} onClick={console.log}>
      Tile {index + 1}
    </Tile>
  ))}
</TileGroup>
```

#### 制御された選択可能なタイル

```js
class MyComponent extends React.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      checkedTile: 'tile-0',
    };
    this._onChange = this._onChange.bind(this);
  }
  _onChange(evt, tileValue) {
    this.setState({ checkedTile: tileValue });
  }
  render() {
    const { checkedTile } = this.state;
    return (
      <TileGroup
        value={checkedTile}
        onChange={this._onChange}
        tileWidth={120}
        selectionType={TileGroup.SELECTION_TYPE.SINGLE}
      >
        {Array.from({ length: 12 }).map((_, index) => (
          <Tile
            sizeType={Tile.SIZE_TYPE.SMALL}
            value={`tile-${index}`}
            key={index}
            onClick={console.log}
          >
            Tile {index + 1}
          </Tile>
        ))}
      </TileGroup>
    );
  }
}
```

#### 複数選択タイル

```js
<TileGroup selectionType={TileGroup.SELECTION_TYPE.MULTIPLE} tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### 制御された複数選択タイル

```js
class MyComponent extends React.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      checkedTiles: [],
    };
    this._onChange = this._onChange.bind(this);
  }
  _onChange(evt, tileValue, checked) {
    this.setState((state) => {
      return {
        checkedTiles: checked
          ? [...state.checkedTiles, tileValue]
          : state.checkedTiles.filter((value) => value !== tileValue),
      };
    });
  }
  render() {
    const { checkedTiles } = this.state;
    return (
      <TileGroup
        value={checkedTiles}
        onChange={this._onChange}
        tileWidth={120}
        selectionType={TileGroup.SELECTION_TYPE.MULTIPLE}
      >
        {Array.from({ length: 12 }).map((_, index) => (
          <Tile
            sizeType={Tile.SIZE_TYPE.SMALL}
            value={`tile-${index}`}
            key={index}
            onClick={console.log}
          >
            Tile {index + 1}
          </Tile>
        ))}
      </TileGroup>
    );
  }
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>必須</h5><h5>ノード</h5>
      </td>

      <td>
        表示するタイル。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>文字列|数値|文字列\[]|数値\[]</h5>
      </td>

      <td>
        チェックするべき最初のタイル。
      </td>
    </tr>

    <tr>
      <td>
        `gapType` <h5>enum</h5>
      </td>

      <td>
        <OptionReference>タイル</OptionReference>間の隙間のサイズ。TileGroup.GAP_TYPE.MEDIUMTileGroup.GAP_TYPE.SMALL
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        タイルがチェックまたはチェック解除されたときに呼び出される関数。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":"Event source of the callback."},{"name":"tileValue","type":"any","description":"The value of the tile."},{"name":"checked","type":"boolean","description":"The new checked state."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selectionType` <h5>enum</h5>
      </td>

      <td>
        <OptionReference>
          TileGroup.SELECTION_TYPE.MULTIPLE、TileGroup.SELECTION_TYPE.NONE、TileGroup.SELECTION_TYPE.SINGLE、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          TileGroup.SPACING_TYPE.EXTRA_LARGE、TileGroup.SPACING_TYPE.LARGE、TileGroup.SPACING_TYPE.MEDIUM、TileGroup.SPACING_TYPE.NONE、TileGroup.SPACING_TYPE.OMIT、TileGroup.SPACING_TYPE.SMALL、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `tileWidth` <h5>文字列|数値</h5>
      </td>

      <td>
        列の幅を表します。 列の幅は常に同じです。 次のいずれかのタイプになります。

        * `'YYpx'`: `YY`は数値で、列の最小幅をピクセル単位で表します。 タイルが利用可能な幅に収まらない場合は、新しい行に折り返されます。
        * `'YYfr'`: `YY`は数値で、12 分数グリッドで使用可能な幅の割合を表します。インスタンスの場合、タイルの幅が`'6fr'`のタイルのグループは常に 2 つの列 ( `12fr/6fr = 2` ) をレンダリングし、各タイルは使用可能な幅の 50% を占めます。
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>文字列|数値|文字列\[]|数値\[]</h5>
      </td>

      <td>
        一致する値を持つタイルがチェックされます。 選択タイプが`multiple`の場合、単一の値または値の配列を指定できます。定義されている場合、コンポーネントは[制御されたコンポーネント](https://facebook.github.io/react/docs/forms.html)になります。
      </td>
    </tr>
  </tbody>
</table>