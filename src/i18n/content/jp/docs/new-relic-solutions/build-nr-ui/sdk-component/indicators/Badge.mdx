---
title: バッジ
metaDescription: Learn how to work the Badge component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

バッジはオブジェクトのステータスを示します。

### 使用方法

```js
import { Badge } from 'nr1'
```

### 例

#### ベーシック

```js
<div className="nr1-Docs-prettify">
  <Badge>Normal</Badge>
</div>
```

#### 種類

```js
<div className="nr1-Docs-prettify">
  <Badge>Normal</Badge>
  <Badge type={Badge.TYPE.INFO}>New</Badge>
  <Badge type={Badge.TYPE.SUCCESS}>Success</Badge>
  <Badge type={Badge.TYPE.WARNING}>Warning</Badge>
  <Badge type={Badge.TYPE.CRITICAL}>Critical</Badge>
</div>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>必須の</h5><h5>文字列|数値</h5>
      </td>

      <td>
        バッジ内にレンダリングする文字列。 コンテンツが 24 文字を超える場合は省略記号が適用されます。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          Badge.SPACING_TYPE.EXTRA_LARGE、Badge.SPACING_TYPE.LARGE、Badge.SPACING_TYPE.MEDIUM、Badge.SPACING_TYPE.NONE、Badge.SPACING_TYPE.OMIT、Badge.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。 **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        タイプは次のようになります:

        * 正常 — 一般的なステータスまたは進行中のステータスを示します。例: 「近日公開」または「マイナー」。

        * 情報 - 新規、作成済み、またはヘルプのステータスを示します。例: 「新規」、「ベータ版」、または「情報」。

        * 成功 - 成功した状態を示します。

        * 警告 - アドバイス状態を必要とする項目を示します。

        * 重大 - 問題のある項目を示します。

          <OptionReference>
            Badge.TYPE.CRITICAL、Badge.TYPE.INFOBadge.TYPE.NORMAL、Badge.TYPE.SUCCESS、Badge.TYPE.WARNING、
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>