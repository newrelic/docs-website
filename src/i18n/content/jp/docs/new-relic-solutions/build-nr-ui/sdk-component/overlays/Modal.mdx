---
title: モーダル
metaDescription: Learn how to work the Modal component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

モーダルは、単一ステップの作成、追加、編集、または削除アクションに使用されます。 また、画面または画面上の特定のオブジェクトに関する追加のメタデータを表示するためにも使用されます。

### 使用方法

```js
import { Modal } from 'nr1'
```

### 例

#### ベーシック

```js
class MyNerdlet extends React.PureComponent {
  constructor() {
    super(...arguments);


    this._onClose = this._onClose.bind(this);


    this.state = {
      hidden: true,
    };
  }


  _onClose() {
    this.setState({ hidden: true });
  }


  render() {
    return (
      <>
        <Button onClick={() => this.setState({ hidden: false })}>
          Open modal
        </Button>


        <Modal hidden={this.state.hidden} onClose={this._onClose}>
          <HeadingText type={HeadingText.TYPE.HEADING_3}>Modal</HeadingText>


          <BlockText
            spacingType={[
              BlockText.SPACING_TYPE.EXTRA_LARGE,
              BlockText.SPACING_TYPE.OMIT,
            ]}
          >
            Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
            eiusmod tempor incididunt ut labore et dolore magna aliqua. Dictumst
            quisque sagittis purus sit amet.
          </BlockText>


          <Button onClick={this._onClose}>Close</Button>
        </Modal>
      </>
    );
  }
}
```

#### アンマウント

```js
class MyNerdlet extends React.PureComponent {
  constructor() {
    super(...arguments);


    this._onClick = this._onClick.bind(this);
    this._onClose = this._onClose.bind(this);
    this._onHideEnd = this._onHideEnd.bind(this);


    this.state = {
      hidden: true,
      mounted: false,
    };
  }


  _onClick() {
    this.setState({
      hidden: false,
      mounted: true,
    });
  }


  _onClose() {
    this.setState({ hidden: true });
  }


  _onHideEnd() {
    this.setState({ mounted: false });
  }


  render() {
    return (
      <>
        <Button onClick={this._onClick}>Open modal</Button>


        {this.state.mounted && (
          <Modal
            hidden={this.state.hidden}
            onClose={this._onClose}
            onHideEnd={this._onHideEnd}
          >
            <HeadingText type={HeadingText.TYPE.HEADING_3}>Modal</HeadingText>


            <BlockText
              spacingType={[
                BlockText.SPACING_TYPE.EXTRA_LARGE,
                BlockText.SPACING_TYPE.OMIT,
              ]}
            >
              Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
              eiusmod tempor incididunt ut labore et dolore magna aliqua.
              Dictumst quisque sagittis purus sit amet.
            </BlockText>


            <Button onClick={this._onClose}>Close</Button>
          </Modal>
        )}
      </>
    );
  }
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabelledBy` <h5>ストリング</h5>
      </td>

      <td>
        スクリーン リーダーによって正しく読み上げられるよう、モーダルの目的をより適切に説明するテキスト コンテンツの文字列を渡します。

        ```js
        const modalLabelId = 'my-nerdlet.my-modal';

        <Modal ariaLabelledBy={modalLabelId}>
          <span id={modalLabelId}>
            <HeadingText>My modal</HeadingText>
          </span>
        </Modal>;
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>ノード</h5>
      </td>

      <td>
        モーダル内でレンダリングするコンテンツ。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。
      </td>
    </tr>

    <tr>
      <td>
        `hidden` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、モーダルは非表示になります。
      </td>
    </tr>

    <tr>
      <td>
        `onClose` <h5>必須</h5><h5>機能</h5>
      </td>

      <td>
        モーダルの閉じるボタンをクリックしたときに呼び出されるコールバック。
      </td>
    </tr>

    <tr>
      <td>
        `onHideEnd` <h5>機能</h5>
      </td>

      <td>
        モーダルが閉じるアニメーションを完了したときに呼び出されるコールバック。これを使用してモーダル コンポーネントをアンマウントします。 これにより、閉じるアニメーションが適切に機能するようになります。

        ```js
        function render() {
          <>
            {this.state.mounted && (
              <Modal
                hidden={this.state.hidden}
                onHideEnd={() => this.setState({ mounted: false })}
                onClose={() => this.setState({ hidden: true })}
              >
                <h1>Modal</h1>
                <p>
                  Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
                  eiusmod tempor incididunt ut labore et dolore magna aliqua.
                </p>
              </Modal>
            )}
          </>;
        }
        ```
      </td>
    </tr>

    <tr>
      <td>
        `onHideStart` <h5>機能</h5>
      </td>

      <td>
        モーダルが閉じるアニメーションを開始するときに呼び出されるコールバック。
      </td>
    </tr>

    <tr>
      <td>
        `onShowEnd` <h5>機能</h5>
      </td>

      <td>
        モーダルがオープニングアニメーションを終了したときに呼び出されるコールバック。
      </td>
    </tr>

    <tr>
      <td>
        `onShowStart` <h5>機能</h5>
      </td>

      <td>
        モーダルがオープニングアニメーションを開始するときに呼び出されるコールバック。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル用のインライン スタイル。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>
  </tbody>
</table>