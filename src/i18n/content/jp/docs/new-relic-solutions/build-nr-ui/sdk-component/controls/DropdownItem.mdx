---
title: ドロップダウン項目
metaDescription: Learn how to work the DropdownItem component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Dropdown>`コンポーネントの子要素。

`onClick`プロパティを通じてクリック イベントをサポートし、 `to`プロパティを通じて他のページにリダイレクトする可能性もサポートします。

`items`プロパティを通じて、またレンダリング関数を`children`プロパティに渡すことによってサブアイテムをサポートします。

### 使用方法

```js
import { DropdownItem } from 'nr1'
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>必須</h5><h5>ノード | (ノード|関数)\[]</h5>
      </td>

      <td>
        アイテム内に表示するコンテンツ。ネストされたリストをレンダリングするときに、最後の子としてレンダリング メソッドも受け入れます。 `items`プロパティに配列を渡す必要があります。

        ```js
        import { Dropdown, DropdownItem } from 'nr1';

        const nestedItems = [
          { id: '3.1', text: 'Item 3.1' },
          { id: '3.2', text: 'Item 3.2' },
          { id: '3.3', text: 'Item 3.3' },
          { id: '3.4', text: 'Item 3.4' },
        ];

        const nestedDropdown = (
          <Dropdown title="Nested Dropdown">
            <DropdownItem>Item 1</DropdownItem>
            <DropdownItem>Item 2</DropdownItem>
            <DropdownItem items={nestedItems}>
              Item 3
              {({ item }) => <DropdownItem key={item.id}>{item.text}</DropdownItem>}
            </DropdownItem>
            <DropdownItem>Item 4</DropdownItem>
          </Dropdown>
        );
        ```
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、アイテムはインタラクションでは使用できず、onClick コールバックは発生しません。
      </td>
    </tr>

    <tr>
      <td>
        `items` <h5>アレイ</h5>
      </td>

      <td>
        現在のアイテムのサブリストをレンダリングするアイテム。このプロパティが機能するには、アイテムのコンテンツの後の最後の`children`として関数を渡す必要もあります。
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>機能</h5>
      </td>

      <td>
        ユーザーがアイテムをクリックするたびにコールバックが起動されます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selected` <h5>ブール値</h5>
      </td>

      <td>
        選択されたオプションを表示します。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストおよび E2E テストでコンポーネントを ターゲット するために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、`my-nerdpack.some-element` のようになります。**注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>形状|文字列</h5>
      </td>

      <td>
        リンク先の場所オブジェクトまたは URL 文字列。

        <h3>形</h3>

        `pathname` <h5>必須</h5><h5>文字列</h5>

        `search` <h5>ストリング</h5>

        `hash` <h5>ストリング</h5>
      </td>
    </tr>
  </tbody>
</table>