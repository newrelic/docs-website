---
title: リンク
metaDescription: Learn how to work the Link component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

アプリケーションおよび外部リンクの周囲に宣言的でアクセス可能なナビゲーションを提供します。

### 使用方法

```js
import { Link } from 'nr1'
```

### 例

#### ベーシック

```js
<Link to="https://www.google.com">Click me</Link>
```

#### 位置オブジェクト付き

```js
function render() {
  const launcher = { id: 'nr1-core.explorer' };
  const location = navigation.getOpenLauncherLocation(launcher);


  return <Link to={location}>Click me</Link>;
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        これを使用して、スクリーン リーダーを使用するユーザーに対してコンポーネントのアクションのコンテキストをより適切に説明します。

        ```js
        <Link to="/dashboards" ariaLabel="Go to your dashboard">Go</Link> 
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>必須</h5><h5>ノード</h5>
      </td>

      <td>
        リンクの内容。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>機能</h5>
      </td>

      <td>
        ユーザーがリンクをクリックするたびにコールバックが起動されます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `role` <h5>ストリング</h5>
      </td>

      <td>
        ARIA 属性は、 `role="menuitem"`のように、アクセシビリティに関する`Link`の役割を記述するために使用されます。 デフォルトでは、 `Link`の役割は「リンク」であるため、明示的な役割は、 `role="menu"`要素などの他のアクセス可能な要素内で使用されます。 `Link`にどの`role`を適用できるか、またどのコンテキストに適用できるかについては[、ARIA ベストプラクティスを参照してください](https://www.w3.org/TR/wai-aria-practices/)。
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          Link.SPACING_TYPE.EXTRA_LARGE、Link.SPACING_TYPE.LARGE、Link.SPACING_TYPE.MEDIUM、Link.SPACING_TYPE.NONE、Link.SPACING_TYPE.OMIT、Link.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>形状|文字列</h5>
      </td>

      <td>
        リンク先の場所オブジェクトまたは URL 文字列。 <h3>形</h3>

        `pathname` <h5>必須</h5><h5>文字列</h5>

        `search` <h5>ストリング</h5>

        `hash` <h5>ストリング</h5>
      </td>
    </tr>
  </tbody>
</table>