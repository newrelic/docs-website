---
title: 複数行テキストフィールド
metaDescription: Learn how to work the MultilineTextField component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

フォームでの複数行入力に使用されるコンポーネント。

### 使用方法

```js
import { MultilineTextField } from 'nr1'
```

### 例

#### ベーシック

```js
<MultilineTextField label="Summary" placeholder="Enter a short description" />
```

#### インラインラベル付き

```js
<MultilineTextField
  label="Summary"
  labelInline
  placeholder="Enter a short description"
/>
```

#### フォーマット済み

```js
<MultilineTextField
  type={MultilineTextField.TYPE.PREFORMATTED}
  label="Build command"
  placeholder="npm run build"
/>
```

#### 読み込み中

```js
<MultilineTextField loading placeholder="Enter a short description" />
```

#### 無効

```js
<MultilineTextField disabled placeholder="Enter a short description" />
```

#### 読み取り専用

```js
<MultilineTextField
  readOnly
  defaultValue="Some value"
  placeholder="Enter a short description"
/>
```

#### 説明付き

```js
<MultilineTextField
  description="Description value"
  placeholder="Enter a short description"
/>
```

#### 無効なメッセージ

```js
<MultilineTextField
  invalid="Not enough text, needs more"
  placeholder="Enter a short description"
/>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        この入力を説明するためにスクリーン リーダー用のラベルを指定します。 これは、スクリーン リーダーのラベルが`label`プロパティと異なる場合、または`label`プロパティをまったく提供していない場合にのみ必要です。
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabelledBy` <h5>ストリング</h5>
      </td>

      <td>
        スクリーン リーダー用にこの入力を説明する内容を持つ要素の一意の`id`を指定します。 これは、スクリーン リーダーのラベルが`label`プロパティと異なる場合、または`label`プロパティをまったく提供していない場合にのみ必要です。 これか`ariaLabel`のどちらか一方を使用してください。両方使用しないでください。
      </td>
    </tr>

    <tr>
      <td>
        `autoFocus` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、複数行テキスト フィールドはマウント時に自動的にフォーカスされます。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>ストリング</h5>
      </td>

      <td>
        複数行テキスト フィールドのデフォルト値。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        フォーム フィールドに入力する方法を説明するメッセージ。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        フィールドが編集可能かどうかを決定します。
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>ストリング</h5>
      </td>

      <td>
        追加情報は、ラベルの横にある情報ツールチップに表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>ブール値|文字列</h5>
      </td>

      <td>
        true の場合、この特定のフィールドに対してユーザーの注意が必要であることを通知するために、フィールドを無効な状態に設定しています。 このプロパティは、 `boolean`フィールドまたは`string`になります。 `string`の場合は、無効な状態が表示されるとともに、下にテキストが表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>ストリング</h5>
      </td>

      <td>
        ラベルとして表示するテキスト。
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>ブール値</h5>
      </td>

      <td>
        フォーム コントロール内にラベルを表示します。コンポーネントが`Form`内にない場合にのみ使用します。 その場合は、 `Form`コンポーネントで`layoutType`を`Form.LAYOUT_TYPE.SPLIT`に設定します。
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>ブール値</h5>
      </td>

      <td>
        フォーム フィールドにデータのリストまたは他の何かを読み込む場合は、読み込み状態フォームを使用して、何かが読み込まれていることをユーザーに示します。フィールドが値を動的に読み込む可能性があることを通知するために使用されます。 `true`に設定すると、読み込みスピナーが表示されます。 `false`に設定すると、スピナーを表示できるように UI 内にスペースが確保されます。フィールドが特定の時点で読み込まれることが予想されない場合は、このプロパティを設定しないでください (または`undefined`に設定してください)。
      </td>
    </tr>

    <tr>
      <td>
        `onBlur` <h5>機能</h5>
      </td>

      <td>
        複数行テキスト フィールドがフォーカスを失ったときに起動されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        複数行テキスト フィールドの値が変更されたときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback. You can pull out the new value by accessing `event.target.value`."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onFocus` <h5>機能</h5>
      </td>

      <td>
        複数行テキスト フィールドにフォーカスが当たったときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyDown` <h5>機能</h5>
      </td>

      <td>
        キーが押されたときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyUp` <h5>機能</h5>
      </td>

      <td>
        キーが放されたときに呼び出されるコールバック。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `placeholder` <h5>ストリング</h5>
      </td>

      <td>
        複数行テキスト フィールドに値がない場合に表示されるヒント。
      </td>
    </tr>

    <tr>
      <td>
        `readOnly` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、複数行テキスト フィールドは読み取り専用になります。
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、フォーム フィールドが必須であることを示します。
      </td>
    </tr>

    <tr>
      <td>
        `rows` <h5>ナンバー</h5>
      </td>

      <td>
        テキスト フィールドの行数。最低限必要なのは 5 行です。
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          MultilineTextField.SPACING_TYPE.EXTRA_LARGE、MultilineTextField.SPACING_TYPE.LARGE、MultilineTextField.SPACING_TYPE.MEDIUM、MultilineTextField.SPACING_TYPE.NONE、MultilineTextField.SPACING_TYPE.OMIT、MultilineTextField.SPACING_TYPE.SMALL、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spellCheck` <h5>ブール値</h5>
      </td>

      <td>
        `true` の場合、 browserのスペルエラー チェックが有効になります。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        * NORMAL — ユーザーがフィールドにテキストを入力するとき \* PERFORMATTED — ユーザーが等幅コンテンツを入力する必要がある場合

          <OptionReference>
            マルチラインテキストフィールド.TYPE.NORMAL、マルチラインテキストフィールド.TYPE.PREFORMATTED、
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>ストリング</h5>
      </td>

      <td>
        複数行テキスト フィールドの値。これは制御されたコンポーネントに必須です。
      </td>
    </tr>
  </tbody>
</table>