---
title: ブロックテキスト
metaDescription: Learn how to work the BlockText component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

タイポグラフィ スタイルは UI 全体で使用されます。 一貫して使用すると、ユーザーにとって明確な階層を維持するのに役立ちます。

### 使用方法

```js
import { BlockText } from 'nr1'
```

### 例

#### ベーシック

```js
<>
  <BlockText>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
</>
```

#### 種類

```js
<>
  <BlockText type={BlockText.TYPE.NORMAL}>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
  <BlockText type={BlockText.TYPE.PARAGRAPH}>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
</>
```

#### 意味の変化

```js
<BlockText type={BlockText.TYPE.PARAGRAPH} tagType={BlockText.TYPE.DIV}>
  This is a div element styled as a paragraph
</BlockText>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>ノード</h5>
      </td>

      <td>
        表示するテキスト。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          BlockText.SPACING_TYPE.EXTRA_LARGE、BlockText.SPACING_TYPE.LARGE、BlockText.SPACING_TYPE.MEDIUM、BlockText.SPACING_TYPE.NONE、BlockText.SPACING_TYPE.OMIT、BlockText.SPACING_TYPE.SMALL、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `tagType` <h5>enum</h5>
      </td>

      <td>
        テキストを囲むタグ。テキストの意味を変更する場合に便利です。 これにはスタイルは適用されません。

        <OptionReference>
          BlockText.タグ.DIV、BlockText.タグ.P
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        表示するテキストの種類。種類に応じてスタイルが適用されます。

        <OptionReference>
          ブロックテキスト.TYPE.NORMAL、ブロックテキスト.TYPE.PARAGRAPH
        </OptionReference>
      </td>
    </tr>
  </tbody>
</table>