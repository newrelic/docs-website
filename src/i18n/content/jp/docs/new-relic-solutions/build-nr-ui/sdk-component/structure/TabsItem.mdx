---
title: TabsItem
metaDescription: Learn how to work the TabsItem component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Tabs>`コンポーネントの子要素。

タブのタイトルが`label`プロパティによって定義され、タブのコンテンツが`children`プロパティによって定義されるタブを作成します。

### 使用方法

```js
import { TabsItem } from 'nr1'
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>必須</h5><h5>ノード</h5>
      </td>

      <td>
        タブが選択されたときに表示されるコンテンツ。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、タブはインタラクションでは使用できません。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>必須</h5><h5>文字列</h5>
      </td>

      <td>
        タブのタイトル。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。タブ コンテンツに適用されます。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>必須</h5><h5>任意</h5>
      </td>

      <td>
        タブの一意の識別子。アクティブなタブを選択するときに使用されます。
      </td>
    </tr>
  </tbody>
</table>