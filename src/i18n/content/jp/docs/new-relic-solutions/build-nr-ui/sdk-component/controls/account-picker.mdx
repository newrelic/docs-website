---
title: アカウントピッカー
tags:
  - SDK components
  - New Relic apps
  - UI components
  - chart components
  - query and storage components
  - Platform APIs
  - AccountPicker
metaDescription: Learn how to work the AccountPicker component
freshnessValidatedDate: '2024-05-13T00:00:00.000Z'
translationType: machine
---

## 使用方法 [#usage]

```js
import { AccountPicker } from 'nr1'
```

## 例 [#examples]

### ベーシック [#basic]

```jsx
class Example extends React.Component {
  constructor() {
    super(...arguments);

    this.state = { accountId: null };

    this.onChangeAccount = this.onChangeAccount.bind(this);
  }

  onChangeAccount(_, value) {
    alert(`Selected account: ${value}`);

    this.setState({ accountId: value });
  }

  render() {
    return (
      <AccountPicker
        value={this.state.accountId}
        onChange={this.onChangeAccount}
      />
    );
  }
}
```

### インラインラベル付き [#inline-label]

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);

    this.state = { accountId: null };

    this.onChangeAccount = this.onChangeAccount.bind(this);
  }

  onChangeAccount(_, value) {
    alert(`Selected account: ${value}`);

    this.setState({ accountId: value });
  }

  render() {
    return (
      <AccountPicker
        label="Account"
        labelInline
        value={this.state.accountId}
        onChange={this.onChangeAccount}
      />
    );
  }
}
```

## 小道具 [#props]

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel`
      </td>

      <td>
        ストリング
      </td>

      <td>
        このコントロールに、わかりやすいラベル 例: **Accounts**を指定します。
      </td>
    </tr>

    <tr>
      <td>
        `className`
      </td>

      <td>
        ストリング
      </td>

      <td>
        コンポーネントにクラス名を追加します。 位置決めと間隔調整の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `crossAccount`
      </td>

      <td>
        ブール値
      </td>

      <td>
        デフォルトでは、その値は`false`です。 上部に**All accounts** \[すべてのアカウント]というオプションが表示されます。 選択すると、 `onChange`コールバックによって提供される値は`AccountPicker.CROSS_ACCOUNT`になります。 これは、複数のアカウントで操作できる機能がある場合に便利です。
      </td>
    </tr>

    <tr>
      <td>
        `disabled`
      </td>

      <td>
        ブール値
      </td>

      <td>
        `true`の場合、 指示 は インタラクションに使用できません。
      </td>
    </tr>

    <tr>
      <td>
        `label`
      </td>

      <td>
        ストリング
      </td>

      <td>
        ラベルとして表示するテキスト。
      </td>
    </tr>

    <tr>
      <td>
        `labelInline`
      </td>

      <td>
        ブール値
      </td>

      <td>
        フォーム コントロール内にラベルを表示します。コンポーネントが`Form`内にない場合にのみ使用します。 その場合は、 `Form`コンポーネントで`layoutType`を`Form.LAYOUT_TYPE.SPLIT`に設定します。
      </td>
    </tr>

    <tr>
      <td>
        `onChange`
      </td>

      <td>
        機能
      </td>

      <td>
        ユーザーがリストからアカウントをクリックするたびにコールバックが実行されます。

        ```
        function (
            event: React.MouseEvent, 
            value: number 
            Id of the account selected.

        )
        ```
      </td>
    </tr>

    <tr>
      <td>
        `spacingType`
      </td>

      <td>
        列挙型\[]
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        ```js
        <Array of
            <One of
                AccountPicker.SPACING_TYPE.EXTRA_LARGE,
                AccountPicker.SPACING_TYPE.LARGE,
                AccountPicker.SPACING_TYPE.MEDIUM,
                AccountPicker.SPACING_TYPE.NONE,
                AccountPicker.SPACING_TYPE.OMIT,
                AccountPicker.SPACING_TYPE.SMALL,
            >
        >
        ```
      </td>
    </tr>

    <tr>
      <td>
        `style`
      </td>

      <td>
        物体
      </td>

      <td>
        カスタム スタイル用のインライン スタイル。 位置決めと間隔調整の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId`
      </td>

      <td>
        ストリング
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストおよび E2E テストでコンポーネントをターゲットにするために使用します。

        テスト ID を有効にするには、テスト ID の前に nerdpack ID を付け、その後にドットを付けます。 たとえば、 `my-nerdpack.some-element` 。

        **注**: `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `value`
      </td>

      <td>
        番号|列挙型
      </td>

      <td>
        選択したアカウントの ID。
      </td>
    </tr>
  </tbody>
</table>