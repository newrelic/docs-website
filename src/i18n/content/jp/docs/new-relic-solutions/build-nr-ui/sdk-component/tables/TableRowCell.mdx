---
title: テーブル行セル
metaDescription: Learn how to work the TableRowCell component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

表の本体にある表セルの内容を折り返します。 このコンポーネントは、 `<TableRow>`の子としてのみ使用されることが想定されています。 セルは、収まらないコンテンツを自動的に省略し、垂直方向に中央揃えします。

このコンポーネントはベース セルです。 一般的なケースでは、SDKは追加のセルのセットも提供し、UI全体で常に一貫して描画されるようにします（例： ステータスまたはユーザーを持つエンティティ名)。

### 使用方法

```js
import { TableRowCell } from 'nr1'
```

### 例

#### 外部リンクと追加情報を含む行セル

```js
function render() {
  const items = [
    {
      dashboardName: 'Staging service - Errors',
      createdBy: 'john@doe.com',
      url: '//fake-url.com/dashboard1',
    },
    {
      dashboardName: 'Package Registry - Performance',
      createdBy: 'jane@doe.com',
      url: '//fake-url.com/dashboard2',
    },
  ];
  return (
    <Table items={items} multivalue>
      <TableHeader>
        <TableHeaderCell>Name</TableHeaderCell>
        <TableHeaderCell>Url</TableHeaderCell>
      </TableHeader>
      {({ item }) => (
        <TableRow>
          <TableRowCell additionalValue={`Created by ${item.createdBy}`}>
            {item.dashboardName}
          </TableRowCell>
          <TableRowCell>
            <Link to={item.url}>Go to the dashboard</Link>
          </TableRowCell>
        </TableRow>
      )}
    </Table>
  );
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `additionalValue` <h5>ストリング</h5>
      </td>

      <td>
        セル内のメインデータに沿った追加情報。

        **注:**現時点では、このコンテンツは、 `multivalue`プロパティが親の`Table`コンポーネントに渡された場合にのみ表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `alignmentType` <h5>enum</h5>
      </td>

      <td>
        セル内のテキストの配置を定義します。

        <OptionReference>
          テーブル行セル.ALIGNMENT_TYPE.CENTER、テーブル行セル.ALIGNMENT_TYPE.LEFT、テーブル行セル.ALIGNMENT_TYPE.RIGHT、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        これを使用して、スクリーン リーダーのユーザーに対してセルのアクションのコンテキストをより適切に説明します。

        ```js
        <TableRowCell to="/dashboards" ariaLabel="Go to your dashboard">
           Go 
        </TableRowCell> 
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>ノード</h5>
      </td>

      <td>
        表セルの内容。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `ellipsisType` <h5>enum</h5>
      </td>

      <td>
        セル内で値がどのように省略されるかを定義します。 省略記号が置かれる場所:

        * `RIGHT`: テキストの始めはそのままにして、テキストの最後に置きます。

        * `LEFT`: テキストの先頭に、テキストの末尾を保持します。

          <OptionReference>
            テーブル行セル.ELLIPSIS_TYPE.LEFT、テーブル行セル.ELLIPSIS_TYPE.RIGHT、
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>機能</h5>
      </td>

      <td>
        ユーザーがテーブル セルをクリックするたびにコールバックが起動されます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>形状|文字列</h5>
      </td>

      <td>
        リンク先の場所オブジェクトまたは URL 文字列。リンクされた`TableRowCell`はスタイル設定されておらず、外部リンクのアイコンは表示されません。 リンク コンポーネントと同じスタイルが必要な場合は、<h3>代わり</h3>にセル内の子コンポーネントとして`Link`を使用します。

        `pathname` <h5>必須</h5><h5>文字列</h5>

        `search` <h5>ストリング</h5>

        `hash` <h5>ストリング</h5>
      </td>
    </tr>
  </tbody>
</table>