---
title: チェックボックス
metaDescription: Learn how to work the Checkbox component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

ユーザーが1つ以上のオプションを選択すると

チェックボックスの状態は、 `checked`および`indeterminate`プロパティを通じて制御できます。 どちらも指定されていない場合は、チェックボックスは独自の状態を管理します。

`onChange`プロパティを通じてチェックボックスの状態の変化を監視できます。

**注意**: `checked`を設定すると、コンポーネントが制御された状態になるため、 `defaultChecked`がオーバーライドされます。 `defaultChecked`制御されていないチェックボックスにのみ使用してください。

### 使用方法

```js
import { Checkbox } from 'nr1'
```

### 例

#### ベーシック

```js
<div className="nr1-Docs-prettify">
  <Checkbox onChange={(event) => alert('Foo')} label="Foo" />
  <Checkbox indeterminate label="Bar" />
  <Checkbox checked disabled label="Baz" />
</div>
```

#### 情報あり

```js
<Checkbox onChange={(event) => alert('Foo')} info="Info value" label="Foo" />
```

#### 説明付き

```js
<Checkbox
  onChange={(event) => alert('Foo')}
  description="Description value"
  label="Foo"
/>
```

#### 無効なメッセージ

```js
<Checkbox
  onChange={(event) => alert('Foo')}
  invalid="Invalid message value"
  label="Foo"
/>
```

#### 制御対象コンポーネント

```js
class MyNerdlet extends React.PureComponent {
  constructor(props) {
    super(props);


    this.state = {
      isChecked: false,
    };


    this.onChange = this.onChange.bind(this);
  }


  onChange(event) {
    console.log(event);
    const isChecked = event.target.checked;


    this.setState({ isChecked });
  }


  render() {
    return (
      <Checkbox
        checked={this.state.isChecked}
        onChange={this.onChange}
        label="Foo"
      />
    );
  }
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `checked` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、チェックボックスがオンになります。定義されている場合、コンポーネントは[制御されたコンポーネント](https://facebook.github.io/react/docs/forms.html)になります。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `defaultChecked` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、チェックボックスの初期状態は`checked`です。[制御されたコンポーネント](https://facebook.github.io/react/docs/forms.html)を使用しない場合に便利です。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        フォーム フィールドに入力する方法を説明するメッセージ。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、インタラクションのチェックボックスは使用できません。
      </td>
    </tr>

    <tr>
      <td>
        `indeterminate` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、チェックボックスは不確定な状態になります。 `checked`プロパティと`indeterminate`プロパティの両方が指定されている場合は、 `indeterminate`プロパティが優先されます。
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>ストリング</h5>
      </td>

      <td>
        追加情報は、ラベルの横にある情報ツールチップに表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>ブール値|文字列</h5>
      </td>

      <td>
        true の場合、この特定のフィールドに対してユーザーの注意が必要であることを通知するために、フィールドを無効な状態に設定しています。 このプロパティは、 `boolean`フィールドまたは`string`になります。 `string`の場合は、無効な状態が表示されるとともに、下にテキストが表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>ストリング</h5>
      </td>

      <td>
        ラベルとして表示するテキスト。
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        チェックボックスの選択状態が変化するたびにコールバックが起動されます。<FunctionDefinition returnValue={[]} arguments={[{"name":"event","type":"React.ChangeEvent","description":"The event source of the callback. You can access the new value with `event.target.checked`."}]}/>
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。 <OptionReference array>Checkbox.SPACING_TYPE.EXTRA_LARGE、Checkbox.SPACING_TYPE.LARGE、Checkbox.SPACING_TYPE.MEDIUM、Checkbox.SPACING_TYPE.NONE、Checkbox.SPACING_TYPE.OMIT、Checkbox.SPACING_TYPE.SMALL</OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストおよび E2E テストでコンポーネントを ターゲット するために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、`my-nerdpack.some-element` のようになります。**注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントの値。 これはコンポーネント自体ではなく、内部のチェックボックス入力に入り、 `onChange`イベント ターゲット要素の一部として返されます。
      </td>
    </tr>
  </tbody>
</table>