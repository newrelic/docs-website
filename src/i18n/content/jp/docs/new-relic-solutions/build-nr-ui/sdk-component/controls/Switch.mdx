---
title: スイッチ
metaDescription: Learn how to work the Switch component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

スイッチは、コンテンツ フィルターのトグルとして、またはユーザーがバイナリ設定を選択する方法を提供するために使用されます。

### 使用方法

```js
import { Switch } from 'nr1'
```

### 例

#### ベーシック

```js
<div className="nr1-Docs-prettify">
  <Switch onChange={(e) => alert(`Toggle to: ${e.target.checked}`)} />
  <Switch
    defaultChecked
    onChange={(e) => alert(`Toggle to: ${e.target.checked}`)}
  />
</div>
```

#### 無効

```js
<div className="nr1-Docs-prettify">
  <Switch disabled />
  <Switch defaultChecked disabled />
</div>
```

#### 読み込み状態

```js
<div className="nr1-Docs-prettify">
  <Switch loading />
  <Switch defaultChecked loading />
</div>
```

#### ラベル付き

```js
<div className="nr1-Docs-prettify">
  <Switch label="This is a label" />
  <Switch disabled label="This is a label" />
</div>
```

#### ラベルと情報付き

```js
<Switch label="This is a label" info="Info value" />
```

#### 説明付き

```js
<Switch label="This is a label" description="Description value" />
```

#### 無効なメッセージ

```js
<Switch label="This is a label" invalid="Invalid message value" />
```

#### リクエストの実行中に制御コンポーネントがロードに設定されました

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(resolve, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

#### 制御されたコンポーネントは、リクエストの実行中にロードするように設定され、リクエストが失敗するとチェックなしに戻ります。

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(reject, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        このコントロールにわかりやすいラベルを付けます。例: 「ビューを切り替える」。
      </td>
    </tr>

    <tr>
      <td>
        `checked` <h5>ブール値</h5>
      </td>

      <td>
        スイッチのチェック状態を決定します。ここで、 `true`スイッチが「オン」になっている状態、 `false`値はスイッチが「オフ」になっている状態を表します。 `checked`が定義されている場合、スイッチ コンポーネントは[制御されたコンポーネント](https://reactjs.org/docs/forms.html#controlled-components)のように動作します。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。 位置決めと間隔調整の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `defaultChecked` <h5>ブール値</h5>
      </td>

      <td>
        [制御されていないコンポーネント](https://reactjs.org/docs/uncontrolled-components.html)として動作する場合のスイッチのデフォルトのチェック値を設定します。 すなわち。 `checked`プロパティがまだ規定されていない場合。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        フォーム フィールドに入力する方法を説明するメッセージ。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        ユーザーがスイッチを操作できるかどうか、またはスイッチを無効な状態でレンダリングするかどうかを決定します。
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>ストリング</h5>
      </td>

      <td>
        追加情報は、ラベルの横にある情報ツールチップに表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>ブール値|文字列</h5>
      </td>

      <td>
        true の場合、この特定のフィールドに対してユーザーの注意が必要であることを通知するために、フィールドを無効な状態に設定しています。 このプロパティは、 `boolean`フィールドまたは`string`になります。 `string`の場合は、無効な状態が表示されるとともに、下にテキストが表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>ストリング</h5>
      </td>

      <td>
        テキスト ラベルを関連付けて、スイッチによって制御される設定の簡単なタイトルまたは説明をユーザーに提供します。 ラベルはスイッチの左側に配置されます。
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>ブール値</h5>
      </td>

      <td>
        アクションが進行中かどうかを示すには、特に完了までに 1 秒以上かかる場合は、読み込み状態を表示する必要があります。 スイッチはロード中は対話型ではありません。ロード状態に関連付けられたアクションが失敗した場合、チェックされたプロパティは以前の値に戻される必要があります。
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        ユーザーがスイッチを切り替えるたびにコールバックが起動されます。 現在のチェック状態は、コールバックで`event.target.checked`を介して評価できます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          Switch.SPACING_TYPE.EXTRA_LARGE、Switch.SPACING_TYPE.LARGE、Switch.SPACING_TYPE.MEDIUM、Switch.SPACING_TYPE.NONE、Switch.SPACING_TYPE.OMIT、Switch.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル用のインライン スタイル。 位置決めと間隔調整の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。 **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>
  </tbody>
</table>