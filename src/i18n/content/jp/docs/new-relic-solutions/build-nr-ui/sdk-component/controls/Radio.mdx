---
title: 無線
metaDescription: Learn how to work the Radio component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

ユーザーが提供されたオプションのうち1つだけを選択できる場合

ラジオ ボタンの状態は、 `checked`プロパティを通じて制御できます。 `checked`プロパティが設定されていない場合、ラジオ ボタンは独自の状態を管理します。

`onClick`または`onChange`プロパティを通じてラジオ ボタン イベントをリッスンできます。

### 使用方法

```js
import { Radio } from 'nr1'
```

### 例

#### ベーシック

```js
<div className="nr1-Docs-prettify">
  <Radio onChange={(event) => alert('Foo')} label="Foo" />
  <Radio checked label="Bar" />
  <Radio checked disabled label="Baz" />
</div>
```

#### 情報あり

```js
<Radio onChange={(event) => alert('Foo')} info="Info value" label="Foo" />
```

#### 説明付き

```js
<Radio
  onChange={(event) => alert('Foo')}
  description="Description value"
  label="Foo"
/>
```

#### 無効なメッセージ

```js
<Radio
  onChange={(event) => alert('Foo')}
  invalid="Invalid message value"
  label="Foo"
/>
```

#### 制御対象コンポーネント

```js
class MyNerdlet extends React.PureComponent {
  constructor(props) {
    super(props);


    this.state = {
      isChecked: false,
    };


    this.onClick = this.onClick.bind(this);
  }


  onClick(event) {
    this.setState((state) => {
      return { isChecked: !state.isChecked };
    });
  }


  render() {
    return (
      <Radio
        checked={this.state.isChecked}
        onClick={this.onClick}
        label="Radio button"
      />
    );
  }
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `checked` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、ラジオ ボタンがチェックされます。定義されている場合、入力が[制御されたコンポーネント](https://facebook.github.io/react/docs/forms.html)に変換されます。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        フォーム フィールドに入力する方法を説明するメッセージ。
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>ブール値</h5>
      </td>

      <td>
        `true`の場合、インタラクションのラジオ ボタンは使用できません。
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>ストリング</h5>
      </td>

      <td>
        追加情報は、ラベルの横にある情報ツールチップに表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>ブール値|文字列</h5>
      </td>

      <td>
        true の場合、この特定のフィールドに対してユーザーの注意が必要であることを通知するために、フィールドを無効な状態に設定しています。 このプロパティは、 `boolean`フィールドまたは`string`になります。 `string`の場合は、無効な状態が表示されるとともに、下にテキストが表示されます。
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>ストリング</h5>
      </td>

      <td>
        ラベルとして表示するテキスト。
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        ラジオ ボタンの選択状態が変化するたびにコールバックが起動されます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback."},{"name":"value","type":"any","description":"The value of the radio button."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>機能</h5>
      </td>

      <td>
        ラジオ ボタンがクリックされるたびにコールバックが起動されます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          Radio.SPACING_TYPE.EXTRA_LARGE、Radio.SPACING_TYPE.LARGE、Radio.SPACING_TYPE.MEDIUM、Radio.SPACING_TYPE.NONE、Radio.SPACING_TYPE.OMIT、Radio.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。 **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントの値。 選択されたラジオ ボタンを識別するために`<RadioGroup/>`によって使用されます。
      </td>
    </tr>
  </tbody>
</table>