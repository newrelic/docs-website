---
title: 形状
metaDescription: Learn how to work the Form component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

`<Form>` 関連するユーザー入力フィールドのセットを構造化された方法で表示するコンテナーです。 次のフォーム コンポーネントを 1 つ以上含めることができます。

* `<TextField>`
* `<MultilineTextField>`
* `<Checkbox>`
* `<Radio>`
* `<RadioGroup>`
* `<Switch>`
* `<Select>`
* `<Dropdown>`

コンポーネントは、 `layoutType`プロパティを通じて指定できるさまざまなタイプのレイアウトをサポートします。

### 使用方法

```js
import { Form } from 'nr1'
```

### 例

#### デフォルトのレイアウト

```js
<Form>
  <TextField placeholder="AAA Production" label="Name" />
  <Select label="Select account">
    <SelectItem>Option 1</SelectItem>
    <SelectItem>Option 2</SelectItem>
  </Select>
  <TextField
    defaultValue="help-production-db"
    label="Help channel"
    invalid="Please enter a valid slack channel"
  />
  <MultilineTextField
    label="Description"
    info="Info tooltip"
    placeholder="Placeholder text"
  />
  <Button
    type={Button.TYPE.PRIMARY}
    sizeType={Button.SIZE_TYPE.SMALL}
    spacingType={[
      HeadingText.SPACING_TYPE.EXTRA_LARGE,
      HeadingText.SPACING_TYPE.NONE,
    ]}
  >
    Create dashboard
  </Button>
</Form>
```

#### 分割レイアウト

```js
<Form layoutType={Form.LAYOUT_TYPE.SPLIT}>
  <TextField placeholder="AAA Production" label="Name" />
  <Select label="Select account">
    <SelectItem>Option 1</SelectItem>
    <SelectItem>Option 2</SelectItem>
  </Select>
  <TextField
    defaultValue="help-production-db"
    label="Help channel"
    invalid="Please enter a valid slack channel"
  />
  <MultilineTextField
    label="Description"
    info="Info tooltip"
    placeholder="Placeholder text"
  />
  <Button
    type={Button.TYPE.PRIMARY}
    sizeType={Button.SIZE_TYPE.SMALL}
    spacingType={[
      HeadingText.SPACING_TYPE.EXTRA_LARGE,
      HeadingText.SPACING_TYPE.NONE,
    ]}
  >
    Create dashboard
  </Button>
</Form>
```

#### サイズタイプによる分割レイアウト

```js
<Form
  layoutType={Form.LAYOUT_TYPE.SPLIT}
  splitSizeType={Form.SPLIT_SIZE_TYPE.LARGE}
>
  <TextField placeholder="AAA Production" label="Name" />
  <Select label="Select account">
    <SelectItem>Option 1</SelectItem>
    <SelectItem>Option 2</SelectItem>
  </Select>
  <TextField
    defaultValue="help-production-db"
    label="Help channel"
    invalid="Please enter a valid slack channel"
  />
  <MultilineTextField
    label="Description"
    info="Info tooltip"
    placeholder="Placeholder text"
  />
  <Button
    type={Button.TYPE.PRIMARY}
    sizeType={Button.SIZE_TYPE.SMALL}
    spacingType={[
      HeadingText.SPACING_TYPE.EXTRA_LARGE,
      HeadingText.SPACING_TYPE.NONE,
    ]}
  >
    Create dashboard
  </Button>
</Form>
```

#### 必須フィールドを含むフォーム

```js
<Form>
  <TextField required placeholder="AAA Production" label="Name" />
  <TextField defaultValue="help-production-db" label="Help channel" />
  <MultilineTextField
    label="Description"
    info="Info tooltip"
    placeholder="Placeholder text"
  />
  <Button
    type={Button.TYPE.PRIMARY}
    sizeType={Button.SIZE_TYPE.SMALL}
    spacingType={[
      HeadingText.SPACING_TYPE.EXTRA_LARGE,
      HeadingText.SPACING_TYPE.NONE,
    ]}
  >
    Create dashboard
  </Button>
</Form>
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>ノード</h5>
      </td>

      <td>
        表示するフォーム フィールド。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `layoutType` <h5>enum</h5>
      </td>

      <td>
        フォームのレイアウト:

        * Form.LAYOUT_TYPE.NORMAL: フォームフィールドは垂直に揃えられます

        * Form.LAYOUT_TYPE.SPLIT: ラジオボタンとチェックボックス コンポーネントを除き、フォーム フィールド ラベルは左側に配置され、入力は右側に配置されます。 `SPLIT`レイアウトは、 `splitSizeType`プロパティを通じて指定できるさまざまなサイズをサポートしています。

          <OptionReference>
            フォーム.LAYOUT_TYPE.NORMALフォーム.LAYOUT_TYPE.SPLIT
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          Form.SPACING_TYPE.EXTRA_LARGE、Form.SPACING_TYPE.LARGE、Form.SPACING_TYPE.MEDIUM、Form.SPACING_TYPE.NONE、Form.SPACING_TYPE.OMIT、Form.SPACING_TYPE.SMALL、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `splitSizeType` <h5>enum</h5>
      </td>

      <td>
        分割レイアウトを使用する場合の列のサイズを指定します。 <OptionReference>フォーム.SPLIT_SIZE_TYPE.LARGE、フォーム.SPLIT_SIZE_TYPE.MEDIUM、フォーム.SPLIT_SIZE_TYPE.SMALL、</OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。 **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>
  </tbody>
</table>