---
title: セグメント化コントロール
metaDescription: Learn how to work the SegmentedControl component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

一連のボタンを表示するコンポーネント。一度にアクティブにできるのはそのうちの 1 つだけです。 「制御モード」（ `value`プロパティを設定）または非制御モードで使用できます。

### 使用方法

```js
import { SegmentedControl } from 'nr1'
```

### 例

#### ベーシック

```js
<SegmentedControl onChange={(evt, value) => console.log(evt, value)}>
  <SegmentedControlItem value="one" label="One" />
  <SegmentedControlItem value="two" label="Two" />
  <SegmentedControlItem disabled value="three" label="Three" />
  <SegmentedControlItem value="four" label="Four" />
</SegmentedControl>
```

#### いくつかのヒント

```js
<SegmentedControl onChange={(evt, value) => console.log(evt, value)}>
  <SegmentedControlItem value="one" label="One" />
  <SegmentedControlItem value="two" label="Two" />
  <SegmentedControlItem
    hint="The third item"
    disabled
    value="three"
    label="Three"
  />
  <SegmentedControlItem hint="The fourth item" value="four" label="Four" />
</SegmentedControl>
```

#### 制御された

```js
class TestComponent extends React.Component {
  constructor(props) {
    super(props);


    this.state = {
      value: 'one',
    };
  }


  render() {
    return (
      <SegmentedControl
        value={this.state.value}
        onChange={(evt, value) => this.setState({ value })}
      >
        <SegmentedControlItem value="one" label="One" />
        <SegmentedControlItem value="two" label="Two" />
        <SegmentedControlItem disabled value="three" label="Three" />
        <SegmentedControlItem value="four" label="Four" />
      </SegmentedControl>
    );
  }
}
```

#### アイコン付き

```js
class TestComponent extends React.Component {
  constructor(props) {
    super(props);


    this.state = {
      value: 'one',
    };
  }


  render() {
    return (
      <SegmentedControl
        value={this.state.value}
        onChange={(evt, value) => this.setState({ value })}
      >
        <SegmentedControlItem
          label="One"
          value="one"
          iconType={
            SegmentedControlItem.ICON_TYPE.HARDWARE_AND_SOFTWARE__SOFTWARE__CODE
          }
        />
        <SegmentedControlItem
          label="Two"
          value="two"
          iconType={
            SegmentedControlItem.ICON_TYPE.INTERFACE__OPERATIONS__FILTER
          }
        />
        <SegmentedControlItem
          label="Three"
          value="three"
          iconType={SegmentedControlItem.ICON_TYPE.INTERFACE__SIGN__NUMBER}
        />
      </SegmentedControl>
    );
  }
}
```

#### ヒント付きのアイコンのみ

```js
class TestComponent extends React.Component {
  constructor(props) {
    super(props);


    this.state = {
      value: 'one',
    };
  }


  render() {
    return (
      <SegmentedControl
        type={SegmentedControl.TYPE.ICONS_ONLY}
        value={this.state.value}
        onChange={(evt, value) => this.setState({ value })}
      >
        <SegmentedControlItem
          label="One"
          value="one"
          iconType={
            SegmentedControlItem.ICON_TYPE.HARDWARE_AND_SOFTWARE__SOFTWARE__CODE
          }
        />
        <SegmentedControlItem
          label="Two"
          value="two"
          iconType={
            SegmentedControlItem.ICON_TYPE.INTERFACE__OPERATIONS__FILTER
          }
          hint="The second item."
        />
        <SegmentedControlItem
          label="Three"
          value="three"
          iconType={SegmentedControlItem.ICON_TYPE.INTERFACE__SIGN__NUMBER}
          hint="The third item."
        />
      </SegmentedControl>
    );
  }
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>ストリング</h5>
      </td>

      <td>
        このコントロールにわかりやすいラベルを付けます。例: 「テーマセレクター」。
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>必須</h5><h5>ノード</h5>
      </td>

      <td>
        コントロール内のクリック可能なセグメントを記述する最大 5 個の`<SegmentedControlItem />`の配列。 要素は`label`のみ、または`label`と`icon`を持つことができますが、子全体で均一である必要があります。 すべての要素には、そのボタンを無効にするためのオプションの`disabled`プロパティを設定できます。
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>機能</h5>
      </td>

      <td>
        `<SegmentedControlItem />`がクリックされるたびに、その要素の`value`プロパティを使用して呼び出されます。 現在選択されている要素が再度クリックされた場合にも発生します。 状態を制御する場合は、このコールバックを使用して`value`プロパティを更新できます。

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":""},{"name":"value","type":"any","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>列挙型\[]</h5>
      </td>

      <td>
        間隔プロパティ。 間隔は、0 から 4 個の値のタプルとして定義され、 `margin`や`padding`などの CSS プロパティと同じ規則に従います。 値を省略するには、 `SPACING_TYPE.OMIT`を使用します。

        <OptionReference array>
          SegmentedControl.SPACING_TYPE.EXTRA_LARGE、SegmentedControl.SPACING_TYPE.LARGE、SegmentedControl.SPACING_TYPE.MEDIUM、SegmentedControl.SPACING_TYPE.NONE、SegmentedControl.SPACING_TYPE.OMIT、SegmentedControl.SPACING_TYPE.SMALL、
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストおよび E2E テストでコンポーネントを ターゲット するために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、`my-nerdpack.some-element` のようになります。**注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        アイコンのみを表示するには、これを`SegmentedControl.TYPE.ICONS_ONLY`に設定します。子にアイコンが見つからない場合は無視されます。

        <OptionReference>
          SegmentedControl.TYPE.ICONS_ONLY、SegmentedControl.TYPE.NORMAL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>どんな</h5>
      </td>

      <td>
        このプロパティを更新して、コントロールで現在選択されているオプションを設定します。
      </td>
    </tr>
  </tbody>
</table>