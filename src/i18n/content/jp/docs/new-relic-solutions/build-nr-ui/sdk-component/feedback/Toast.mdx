---
title: トースト
metaDescription: Learn how to work the Toast component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

トースト通知により、ユーザーはタスクが正常に完了したこと、またはタスクを完了しようとして重大な失敗があったことを即座に確認できます。

テキストの横にクリック可能なボタンとして表示されるカスタム アクションを渡すことで、トーストをカスタマイズできます。

### 使用方法

```js
import { Toast } from 'nr1'
```

### 例

#### 基本的なトースト通知

```js
function render() {
  const onClick = () =>
    Toast.showToast({
      title: 'Feedback received',
      description: 'We will contact you soon.',
      actions: [
        {
          label: 'Go back',
          onClick: () => console.log('Go back'),
        },
      ],
      type: Toast.TYPE.NORMAL,
    });


  return <Button onClick={onClick}>Show toast</Button>;
}
```

#### 追加情報リンク付き

```js
function render() {
  const onClick = () =>
    Toast.showToast({
      title: 'TV Mode activated',
      description: 'Press ESC to exit',
      additionalInfoLink: {
        label: 'Learn more about TV Mode',
        to: 'https://discuss.newrelic.com/t/tv-mode-in-new-relic-one/92963',
      },
      type: Toast.TYPE.NORMAL,
    });


  return <Button onClick={onClick}>Show toast</Button>;
}
```

#### 重要なトースト通知

```js
function render() {
  const onClick = () =>
    Toast.showToast({
      title: 'Service unavailable',
      description: 'Your data could not be retrieved',
      actions: [
        {
          label: 'Retry',
          onClick: () => console.log('Retry'),
        },
      ],
      type: Toast.TYPE.CRITICAL,
    });


  return <Button onClick={onClick}>Update</Button>;
}
```

### 小道具

<table>
  <tbody>
    <tr>
      <td>
        `actions` <h5>形\[]</h5>
      </td>

      <td>
        ユーザーに選択してもらう必要がある特定のアクションがある場合は、カスタム アクション コンポーネントを使用します。 この通知はタスクの成功または失敗をユーザーに示すためのものであり、ユーザーを別のタスクに誘導するために使用するものではないため、複数のアクションの使用を制限することをお勧めします。

        <h3>形</h3>

        `label` <h5>必須</h5><h5>文字列</h5>

        `onClick` <h5>必須</h5><h5>機能</h5>
      </td>
    </tr>

    <tr>
      <td>
        `additionalInfoLink` <h5>形</h5>
      </td>

      <td>
        タイトルと説明に表示される情報は、ドキュメントへのリンクで補足/拡張できます。 このリンクは、アクションとしてではなく、コンテキスト情報を提供するためにのみ使用する必要があります。

        <h3>形</h3>

        `label` <h5>必須</h5><h5>文字列</h5>

        <SideBySide>
          <Side>
            `onClick` <h5>機能</h5>
          </Side>

          <Side>
            ユーザーがボタンをクリックするたびにコールバックが起動されます。

            <FunctionDefinition
              returnValue={[]}
              arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
            />
          </Side>
        </SideBySide>

        <SideBySide>
          <Side>
            `to` <h5>形状|文字列</h5>
          </Side>

          <Side>
            リンク先の場所オブジェクトまたは URL 文字列。 <h4>形状</h4>`pathname`<h5>必須</h5><h5>文字列</h5>

            `search` <h5>ストリング</h5>

            `hash` <h5>ストリング</h5>
          </Side>
        </SideBySide>
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>ストリング</h5>
      </td>

      <td>
        コンポーネントにクラス名を追加します。配置と間隔の目的にのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>ストリング</h5>
      </td>

      <td>
        以前に実行したアクションの成功または失敗のコンテキストを示すタイトルとメッセージを提供することをお勧めします。 メッセージ領域を使用して、ユーザーがシステムで作成したオブジェクトにアクセスするためのリンクを提供することもできます。
      </td>
    </tr>

    <tr>
      <td>
        `onHideEnd` <h5>機能</h5>
      </td>

      <td>
        トーストが非表示になった後に実行されるコールバック。
      </td>
    </tr>

    <tr>
      <td>
        `onHideStart` <h5>機能</h5>
      </td>

      <td>
        非表示アニメーションの開始時に実行されるコールバック。
      </td>
    </tr>

    <tr>
      <td>
        `sticky` <h5>ブール値</h5>
      </td>

      <td>
        ユーザーによる明示的な終了や表示を必要としないタスクの場合は、タイムアウト トースト通知を使用して、メッセージが短時間で画面から自動的に消えるようにすることをお勧めします。
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>物体</h5>
      </td>

      <td>
        カスタム スタイル設定用のインライン スタイル。配置と間隔設定の目的でのみ使用してください。
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>ストリング</h5>
      </td>

      <td>
        `data-test-id`属性を追加します。 ユニット テストと E2E テストでコンポーネントをターゲットにするために使用します。テスト ID を有効にするには、nerdpack ID をプレフィックスとして付け、その後にドットを付けます。たとえば、 `my-nerdpack.some-element` 。

        **注:** `data-test-id` 属性はDOMから削除されているため表示されない場合があります。デバッグするには、URL に `e2e-test` クエリ引数を渡します。
      </td>
    </tr>

    <tr>
      <td>
        `title` <h5>必須</h5><h5>文字列</h5>
      </td>

      <td>
        タイトルのみのオプションでは、ユーザーが完了したタスクに関するコンテキストが提供されないため、使用はお勧めしません。
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enum</h5>
      </td>

      <td>
        * 正常 - システムがユーザーのタスクを正常に完了した場合

        * 重大 - システムがユーザーのタスクを完了できなかった場合

          <OptionReference>
            トースト.TYPE.CRITICAL、トースト.TYPE.NORMAL、
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>

### メソッド

### `Toast.showToast`

<FunctionDefinition
  arguments={[{"description":"Object containing the Toast options. Any `Toast` prop is valid as an option.","name":"props","type":"Object"}]}
  returnValue={{"description":null,"type":"undefined"}}
/>