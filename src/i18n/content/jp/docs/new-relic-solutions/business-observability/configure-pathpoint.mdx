---
title: JSON 構成ファイルを使用してパスポイントを構成する
tags:
  - Business observability
  - Pathpoint
  - Pathpoint configurations
metaDescription: How to configure a New Relic Pathpoint using JSON configuration file.
translationType: machine
---

import jsonfile from 'images/pathpoint_diagram_jsonfile.png'

import uploadJson from 'images/pathpoint_screenshot_upload-json.png'

import openFile from 'images/pathpoint_screenshot_open-file.png'

import downloadFile from 'images/pathpoint_screenshot_download-file.png'

import threeLines from 'images/icon-three-lines.png'

New Relic Pathpoint は、ビジネス固有のニーズに合わせてカスタマイズできる柔軟な構成システムを提供します。構成プロセスには、目的のセットアップを指定する JSON ファイルの作成が含まれます。構成ファイルがロードされたら、タッチポイントとそのクエリを手動でさらに微調整できます。

このレベルのカスタマイズにより、組織固有の要件に合わせて調整されたオブザーバビリティ ソリューションを作成できます。

このページでは、次のことを学びます。

* JSON 構成ファイルとは
* 新しい JSON 構成ファイルをアップロードする方法
* 現在アクティブな JSON 構成ファイルをダウンロードする方法

## JSON 構成ファイルとは何ですか? [#json-config]

JSON (JavaScript Object Notation) はデータ構造であり、その基本機能は情報の交換を可能にすることです。この構造により、Pathpoint の実装を容易にする各要素とコンポーネントを特定できます。これには、その属性、クエリ、およびデータ出力の機能を理解することが含まれます。

<img
  title="JSON file"
  alt="JSON file"
  src={jsonfile}
/>

上の画像は、各段階をいくつかのステップに関連付けることができる方法を示しています。これらのステップは、単一のタッチポイントまたはいくつかのタッチポイントにリンクできます。

新しいステージ、ステップ、およびタッチポイントをプログラムするには、新しい JSON 構成ファイルをアップロードします。

## 新しい JSON 構成ファイルをアップロードする [#upload-json-config]

新しい JSON 構成ファイルをロードするには:

<Steps>
  <Step>
    New Relic アカウントから、 **Apps** > **Pathpoint**<img src={threeLines} class="inline" style={{height: "30px", width: "30px"}} alt="Three lines" title="Three lines">に移動し、</img>

    。
  </Step>

  <Step>
    表示されたウィンドウで、 **JSON configuration**\[JSON 構成] をクリックします。
  </Step>

  <Step>
    これにより、ダイアログ ボックス ウィンドウが開きます。 **Description**\[説明]で、新しい JSON 構成ファイルについて説明します。 次に、メモを入力し、**From file**\[ファイルから] をクリックして JSON 構成ファイルを参照します。

    <img
      title="Upload new JSON config file"
      alt="Upload new JSON config file"
      src={uploadJson}
    />
  </Step>

  <Step>
    パスポイントの更新に使用する JSON 構成ファイルを見つけて、 **Open**\[開く]をクリックします。

    <img
      title="open new JSON config file"
      alt="open new JSON config file"
      src={openFile}
    />
  </Step>

  最後に、パスポイントが表示され、選択した特定の JSON ファイルが更新されます。
</Steps>

## 現在アクティブな JSON 構成ファイルをダウンロードします [#download-current-config]

Pathpoint は、現在の JSON 構成ファイルをコンピューターに直接ダウンロードする柔軟性を提供します。この機能は、ファイルを変更する必要がある場合に特に便利です。

現在アクティブな JSON 構成ファイルをダウンロードするには:

<Steps>
  <Step>
    New Relic アカウントから、 **Apps** > **Pathpoint**に移動します。<img src={threeLines} class="inline" style={{height: "30px", width: "30px"}} alt="Three lines" title="Three lines">クリックしてください</img>

    左上隅にあります。
  </Step>

  <Step>
    **JSON configuration**\[JSON 構成]をクリックします。
  </Step>

  <Step>
    次に、下矢印をクリックします。

    <img
      title="download active JSON config file"
      alt="download active JSON config file"
      src={downloadFile}
    />
  </Step>

  アクティブな JSON 構成ファイルがダウンロードされました。
</Steps>

## 詳細については、GitHub リポジトリをご覧ください [#details]

Pathpoint に関する技術的な詳細が必要ですか? [GitHub リポジトリを](https://github.com/newrelic/nr1-pathpoint)参照してください。