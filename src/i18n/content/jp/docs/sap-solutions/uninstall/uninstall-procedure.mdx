---
title: New Relic ABAPアドオンをアンインストールする
metaDescription: Step-by-step instructions to uninstall the New Relic ABAP add-on (NEWR component) from your SAP central monitoring system.
freshnessValidatedDate: never
translationType: machine
---

このガイドでは、SAPセントラルシステムから<DNT>**New Relic ABAP component (NEWR)**</DNT>をアンインストールする方法について説明します。監視を廃止する際、または新しいバージョンにアップグレードする前に、これを行う必要がある場合があります。

<Callout variant="caution">
  <DNT>**New Relic ABAP component**</DNT>をアンインストールすると、すべての監視データの収集が停止し、SAPシステムからコンポーネントが削除されます。New Relicは履歴データを保存しますが、アンインストール後は新しいデータを収集しません。このアクションは元に戻せません。
</Callout>

## 前提条件

* 〜へのアドミニストレーターアクセス <DNT>**SAP system**</DNT>
* アクセス先 <DNT>**client 000**</DNT>
* 任意のカスタム設定のバックアップ（将来の再インストールに必要な場合）

## New Relic監視アーティファクトを削除します。

<DNT>**ABAP component**</DNT> をアンインストールする前に、中央監視システムの監視アーティファクトをクリーンアップしてください：

1. `/NEWR/*`プログラムを実行するスケジュールされた収集ジョブを停止し、スケジュールを解除します。トランザクション`SM37`を使用して、これらのジョブを検索し、キャンセルします。
2. 名前が`/NEWR/*`で始まるプリフィックスの付いたロールをすべて削除します。
3. トランザクション`SM59`で、New Relicのデータ収集のためだけに作成された、モニター対象システムへの<DNT>**RFC connections**</DNT>をすべて削除します。これらの接続は、指定されたNew Relicデータ収集ログインアカウント、またはNew Relicデータプロバイダーのレジストリリストを参照することで特定できます。

## ABAPコンポーネントのアンインストール

セントラルシステムからABAPコンポーネントをアンインストールするには、次の手順に従ってください：

1. <DNT>**client 000**</DNT>にログインします。
2. トランザクションコード`SAINT`を入力します。
3. <DNT>**Uninstallable Components**</DNT>タブをクリックします。

<img title="ABAP component on central system" alt="The SAINT transaction showing the NEWR component in the Uninstallable Components list" src="/images/ABAP-component-on-cms.webp" width="700" />

4. <DNT>**NEWR**</DNT>コンポーネントを選択し、バージョン番号がインストールされているバージョンと一致することを確認します。
5. <DNT>**Start**</DNT>をクリックします。
6. 以降の各画面で、緑色のチェックマークをクリックして確認します。
7. プロセスが完了したら、<DNT>**Finish**</DNT>をクリックします。
8. <DNT>**NEWR**</DNT>コンポーネントが<DNT>**Installed Components**</DNT>タブに表示されなくなったことを確認します。

## モニター対象システムからNew Relicを削除する

<DNT>**NEWR**</DNT> ABAPコンポーネントはセントラルシステムにのみインストールされるため、モニター対象システムにはアンインストールするコンポーネントがありません。モニター対象システムからNew Relicをアンインストールするには、データ収集のために作成されたアクセスを削除します：

1. トランザクション`SU01`で、データ収集に使用される指定のNew Relic <DNT>**ABAP user account**</DNT>を削除します。
2. 関連するNew Relic認可ロール（名前の先頭に`/NEWR/*`が付くもの）を削除します。

## アンインストールの確認

アンインストールプロセス完了後：

* <DNT>**NEWR**</DNT>コンポーネントが<DNT>**Installed Components**</DNT>タブに表示されなくなります。
* トランザクション`SM37`のデータ収集ジョブは実行されなくなります。
* New Relicは、このシステムからの監視データの受信を停止します。

**注**: New Relicの過去の監視データには引き続きアクセスできます。SAPシステムからデータ収集コンポーネントのみが削除されます。