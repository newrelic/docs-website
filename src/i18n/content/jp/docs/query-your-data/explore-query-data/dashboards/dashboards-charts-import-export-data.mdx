---
title: ダッシュボードとグラフのインポート、エクスポート、追加
tags:
  - Query your data
  - Explore and query data
  - Dashboards
  - Charts
metaDescription: Import and export data from dashboards and charts.
freshnessValidatedDate: '2024-02-07T00:00:00.000Z'
translationType: machine
---

[ダッシュボードを](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/)使用すると、システムの健全性を視覚化して追跡し、問題に対処し、さらなるステップを計画することができます。 さまざまなインポートおよびエクスポート オプションを使用して、すべての情報をチーム、顧客、その他の関係者と共有する方法を学びます。

## ダッシュボードを JSON としてインポートする [#import-json]

コードレベルですぐに使用できるダッシュボードを変更する場合は、ダッシュボードを JSON としてインポートできます。 たとえば、2 つの既存のダッシュボードを結合してカスタムダッシュボードを作成する場合、ベースのダッシュボードの JSON を変更して、それをNew Relicにインポートし直すことができます。

ダッシュボードを JSON としてインポートするには:

1. <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Dashboards**</DNT>に移動します。

2. <DNT>**...**</DNT>アイコンをクリックして既存のダッシュボードのJSONを開き、<DNT>**Download dashboard**</DNT>を選択します。

3. フォーマットオプションから<DNT>**JSON**</DNT>を選び、<DNT>**Copy to clipboard**</DNT>を選択します。

4. <DNT>**Dashboards**</DNT>ページに戻ります。

5. 右上隅にある<DNT>**Import dashboard**</DNT>をクリックします。

6. JSON コードを貼り付けます。

7. ダッシュボードのアカウントと権限設定を選択します。 一度設定したアカウントを変更することはできませんが、 [権限はいつでも変更](/docs/query-your-data/explore-query-data/dashboards/manage-your-dashboard/#dash-settings)できます。

8. <DNT>**Save**</DNT>をクリックします。

## ダッシュボードのエクスポート [#export-dashboards-pdf]

ダッシュボードをJSON、PDF、またはPNGファイルとしてエクスポートするには：

1. ダッシュボードの右上隅にある<DNT>**...**</DNT>アイコンをクリックし、<DNT>**Download dashboard**</DNT>を選択します。

2. 出力形式として<DNT>**JSON**</DNT>、<DNT>**PDF**</DNT>、または<DNT>**PNG**</DNT>を選択します。

3. （オプション）<DNT>**PDF**</DNT>または<DNT>**PNG**</DNT>の場合、エクスポートの幅と高さをピクセル単位で設定します。どちらもデフォルトは1920×1080です。

4. <DNT>**Download**</DNT>をクリックします。

UI経由でダッシュボードをPDFまたはPNGとしてエクスポートする場合、New Relicは[カスタムビジュアライゼーション](/docs/query-your-data/explore-query-data/dashboards/add-custom-visualizations-your-dashboards/)をサポートしていません。

テンプレート変数の上書きやエンティティフィルターなどの追加オプションを使用してPDFまたはPNGとしてエクスポートするには、[NerdGraph API](#export-api)を使用します。

## NerdGraph API経由でダッシュボードとチャートをエクスポートする [#export-api]

NerdGraph APIを使用して、ダッシュボードや個々のチャートウィジェットをスナップショット画像としてプログラムでエクスポートします。APIは、カスタムディメンション、テンプレート変数のオーバーライド、エンティティフィルターなど、UIでは利用できないオプションをサポートしています。

### ダッシュボードをPDFまたはPNGとしてエクスポート [#api-dashboard-snapshot]

`dashboardCreateSnapshotUrl`ミューテーションは、ダッシュボードページ全体のスナップショットURLを生成します。

<Callout variant="tip">
  `guid`パラメーターには、ダッシュボードのGUIDではなく、**ページ**のGUIDが必要です。ダッシュボードには複数のページを含めることができます。ページのGUIDを見つけるには、`entitySearch`クエリを使用するか、ダッシュボードページを開いて<DNT>**...**</DNT>メニューから<DNT>**Manage JSON**</DNT>を選択します。

  ```graphql
  {
    actor {
      entitySearch(query: "parentId ='YOUR_DASHBOARD_GUID' AND tags.isDashboardPage = 'true'") {
        results {
          entities {
            guid
            name
          }
        }
      }
    }
  }
  ```
</Callout>

**Mutation schema：**

```graphql
mutation {
  dashboardCreateSnapshotUrl(
    guid: EntityGuid!
    params: SnapshotUrlInput
  ): String
}

input SnapshotUrlInput {
  timeWindow: SnapshotUrlTimeWindowInput
  display: SnapshotUrlDisplayInput
  variables: [SnapshotUrlVariableInput!]
  filter: String
  format: SnapshotUrlFormat
}

input SnapshotUrlTimeWindowInput {
  beginTime: EpochMilliseconds
  endTime: EpochMilliseconds
  duration: Milliseconds
}

input SnapshotUrlDisplayInput {
  width: Int
  height: Int
}

input SnapshotUrlVariableInput {
  name: String!
  values: [String!]!
}

enum SnapshotUrlFormat {
  PDF
  PNG
}
```

**パラメーターリファレンス：**

<table>
  <thead>
    <tr>
      <th>
        パラメータ
      </th>

      <th>
        タイプ
      </th>

      <th>
        デフォルト
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `guid`
      </td>

      <td>
        `EntityGuid!`
      </td>

      <td>
        —
      </td>

      <td>
        必須。ダッシュボードページのエンティティGUID。
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.beginTime`
      </td>

      <td>
        `EpochMilliseconds`
      </td>

      <td>
        ダッシュボードの現在のタイムウィンドウ
      </td>

      <td>
        データウィンドウの開始（エポックミリ秒）。
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.endTime`
      </td>

      <td>
        `EpochMilliseconds`
      </td>

      <td>
        ダッシュボードの現在のタイムウィンドウ
      </td>

      <td>
        データウィンドウの終了（エポックミリ秒）。
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.duration`
      </td>

      <td>
        `Milliseconds`
      </td>

      <td>
        —
      </td>

      <td>
        `beginTime`

        /

        `endTime`

        の代替となる相対的なタイムウィンドウ。
      </td>
    </tr>

    <tr>
      <td>
        `params.display.width`
      </td>

      <td>
        `Int`
      </td>

      <td>
        1920
      </td>

      <td>
        スナップショットのピクセル単位の幅。
      </td>
    </tr>

    <tr>
      <td>
        `params.display.height`
      </td>

      <td>
        `Int`
      </td>

      <td>
        1080
      </td>

      <td>
        スナップショットの高さ（ピクセル単位）。
      </td>
    </tr>

    <tr>
      <td>
        `params.variables`
      </td>

      <td>
        `[SnapshotUrlVariableInput!]`
      </td>

      <td>
        ダッシュボードの現在の変数値
      </td>

      <td>
        ダッシュボードテンプレート変数の特定の値を固定します。
      </td>
    </tr>

    <tr>
      <td>
        `params.filter`
      </td>

      <td>
        `String`
      </td>

      <td>
        フィルターなし
      </td>

      <td>
        スナップショット時に適用されるNRQLスタイルのエンティティフィルター。
      </td>
    </tr>

    <tr>
      <td>
        `params.format`
      </td>

      <td>
        `SnapshotUrlFormat`
      </td>

      <td>
        `PDF`
      </td>

      <td>
        出力フォーマット：

        `PDF`

        または

        `PNG`

        。
      </td>
    </tr>
  </tbody>
</table>

**例：**

<CollapserGroup>
  <Collapser id="basic-export" title="基本エクスポート（PDF、デフォルト設定）">
    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
      )
    }
    ```
  </Collapser>

  <Collapser id="png-custom-dimensions" title="カスタムサイズでPNGとしてエクスポート">
    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
        params: {
          format: PNG
          display: {
            width: 1920
            height: 1080
          }
        }
      )
    }
    ```
  </Collapser>

  <Collapser id="variables-filter" title="変数の上書きとフィルターを使用したエクスポート">
    これを使用して、特定の変数値とフィルターをスナップショットに固定します。これは、スケジュールされたレポートの自動化に役立ちます。

    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
        params: {
          variables: [
            { name: "environment", values: ["production"] }
            { name: "region", values: ["us-east-1", "eu-west-1"] }
          ]
          filter: "env = 'production' AND service = 'checkout'"
        }
      )
    }
    ```
  </Collapser>
</CollapserGroup>

<Callout variant="important">
  スナップショットURLは認証なしでパブリックにアクセス可能であり、3か月後に期限切れになります。組織のデータポリシーに従って慎重に共有してください。
</Callout>

### チャート画像を取得する [#api-widget-snapshot]

`dashboardWidgetCreateSnapshotUrl`ミューテーションは、単一のチャートウィジェットのPNGスナップショットURLを生成します。ウィジェットがダッシュボード上に存在する必要はありません — インラインで定義できるため、プログラムでチャート画像を生成するのに役立ちます。

**Mutation schema：**

```graphql
mutation {
  dashboardWidgetCreateSnapshotUrl(
    widget: DeclarativeUiWidget!
  ): WidgetSnapshotResult
}

type WidgetSnapshotResult {
  url: String!
}
```

`widget`パラメーターは`DeclarativeUiWidget`を受け付けます — 3つのネストされたレイヤーを持つJSONオブジェクト：

```
declarative/widget envelope
└── widget node        ← title and widget-level props
    └── visualization  ← visualization type and configuration
```

```json
{
  "type": "declarative/widget",
  "version": 1,
  "content": {
    "type": "widget",
    "props": {
      "title": "My Chart Title"
    },
    "content": {
      "type": "visualization",
      "id": "viz.line",
      "props": {
        // visualization-specific config (nrqlQueries, colors, thresholds, etc.)
      }
    }
  }
}
```

JSONとしてエクスポートしたダッシュボードからウィジェット定義を構築する場合は、次のフィールドマッピングを使用します：

<table>
  <thead>
    <tr>
      <th>
        ダッシュボードJSONフィールド
      </th>

      <th>
        DeclarativeUiWidgetフィールド
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `title`
      </td>

      <td>
        `content.props.title`
      </td>
    </tr>

    <tr>
      <td>
        `visualization.id`
      </td>

      <td>
        `content.content.id`
      </td>
    </tr>

    <tr>
      <td>
        `rawConfiguration.*`
      </td>

      <td>
        `content.content.props.*`

         （同じフィールド名）
      </td>
    </tr>

    <tr>
      <td>
        `layout`

        、 

        `id`

         、 

        `linkedEntityGuids`
      </td>

      <td>
        含まれていません — スタンドアロンのスナップショットには必要ありません
      </td>
    </tr>
  </tbody>
</table>

**例：**

<CollapserGroup>
  <Collapser id="line-chart-snapshot" title="折れ線グラフのエクスポート">
    ```graphql
    mutation {
      dashboardWidgetCreateSnapshotUrl(
        widget: {
          version: 1
          type: "declarative/widget"
          content: {
            type: "widget"
            props: { title: "Throughput by environment" }
            content: {
              type: "visualization"
              id: "viz.line"
              props: {
                nrqlQueries: [
                  {
                    accountIds: [YOUR_ACCOUNT_ID]
                    query: "SELECT count(*) FROM Transaction TIMESERIES"
                  }
                ]
                legend: { enabled: true }
              }
            }
          }
        }
      ) {
        url
      }
    }
    ```
  </Collapser>

  <Collapser id="billboard-snapshot" title="閾値付きビルボードチャートをエクスポートする">
    ```graphql
    mutation {
      dashboardWidgetCreateSnapshotUrl(
        widget: {
          version: 1
          type: "declarative/widget"
          content: {
            type: "widget"
            props: { title: "Error rate" }
            content: {
              type: "visualization"
              id: "viz.billboard"
              props: {
                nrqlQueries: [
                  {
                    accountIds: [YOUR_ACCOUNT_ID]
                    query: "SELECT percentage(count(*), WHERE error IS true) AS 'Error rate' FROM Transaction SINCE 1 hour ago"
                  }
                ]
                thresholds: [
                  { alertSeverity: "WARNING", value: 1 }
                  { alertSeverity: "CRITICAL", value: 5 }
                ]
              }
            }
          }
        }
      ) {
        url
      }
    }
    ```
  </Collapser>
</CollapserGroup>

<Callout variant="tip">
  NerdGraphのレガシー`actor.account.nrql.staticChartUrl`フィールドは引き続き利用可能ですが、閾値、カラーフォーマット、新しいチャートタイプなどの最新の視覚化機能はサポートしていません。今後は`dashboardWidgetCreateSnapshotUrl`が推奨されるアプローチです。
</Callout>

<Callout variant="important">
  `dashboardWidgetCreateSnapshotUrl` PNG画像のスナップショットURLを生成します。これらは認証なしでパブリックにアクセス可能であり、3か月後に有効期限が切れます。
</Callout>

## チャートをCSVとしてエクスポート [#export-charts-csv]

ダッシュボードのテーブル チャートを CSV ファイルとしてエクスポートするには、チャートの右上隅で<DNT>**...**</DNT>をクリックし、 <DNT>**Export as CSV**</DNT>を選択します。

<img title="Export chart as CSV" alt="Screenshot of how to export a table chart as a CSV file" src="/images/dashboards_screenshot-crop_export-csv.webp" />

## チャートのクエリと探索 [#explore-chart-data]

グラフでできることの詳細については、 [「クエリとグラフのオプション」](/docs/new-relic-solutions/new-relic-one/ui-data/basic-ui-features/#data-analysis)を参照してください。