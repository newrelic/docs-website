---
title: ノートブックを使用してデータドリブン文書を作成する
tags:
  - Query your data
  - Explore and query data
  - Notebooks
metaDescription: 'Use New Relic notebooks to create structured, data-driven documents that combine queries, visualizations, and descriptive text into shareable assets for investigations, post-mortems, and analytical insights.'
freshnessValidatedDate: never
translationType: machine
---

複雑な問題を調査したり、チームとインサイトを共有したりするとき、データから完全なストーリーをつなぎ合わせながら、さまざまなツール間を飛び回ったり、書き込みをコピーしたり、スクリーンショットを撮ったり、コンテキストを維持するのに苦労していることに気づくことがよくあります。 従来のダッシュボードは監視には最適ですが、調査を文書化したり、詳細な事後分析を作成したり、段階的な分析ワークフローを構築したりする必要がある場合には不十分です。

ノートブックは、データを使って完全なストーリーを伝えるための動的かつ体系的な方法を提供することで、この問題を解決します。これらは構造化されたデータドリブン ドキュメントであり、書き込み、視覚化、わかりやすいテキストを 1 つの共有可能なアセットに組み合わせることができるため、分析中に複数のツールを切り替えたり、重要なコンテキストを失ったりする必要がなくなります。

## 使用事例

ノートブックを使用してワークフローを改善し、生データを実用的なストーリーに変換します。

* **データの執筆、分析、および視覚化**: すべての観察データ (ログ、メトリクス、イベント) を執筆し、システムのパフォーマンスをより深く理解するために視覚化を作成します。
* **事後分析と頻繁イベントの文書を作成する**: チームと協力して調査を文書化し、頻繁イベントの事後分析と根本原因分析のための明確で再利用可能な記録を作成します。
* **再利用可能なワークフローとランブックを構築する**: 複雑な分析のためのステップバイステップのガイドを作成して共有します。
* **コンテキストと物語を追加する**: Markdown を使用すると、クエリやグラフに詳細なタイトル、メモ、説明を簡単に追加して、断片的なデータ ポイントをまとまりのあるストーリーに変換できます。

## ノートブックを使い始める

ノートブックの基本を理解したので、独自のノートブックの作成と管理を開始する準備が整いました。

### ノートブックにアクセスする

ノートブックにアクセスするには:

1. **[one.newrelic.com](https://one.newrelic.com)**に移動します。
2. ページの下部にあるドロワーから<DNT>**Query your data**</DNT>コンソールを開きます。
3. 組織によって作成されたすべてのノートブックのリストを表示するには、クエリビルダーヘッダーの<DNT>**Notebooks**</DNT>をクリックします。

<img title="Navigate to notebooks from query your data" alt="Screenshot showing a notebook in New Relic with NRQL queries and an area chart visualizing checkout process error rate" src="/images/notebooks-navigation.webp" />

<figcaption>
  次に移動してノートブックに移動します <DNT>**Query your data &gt; Notebooks**</DNT>
</figcaption>

<CollapserGroup>
  <Collapser id="create-notebook" title="ノートブックを作成する">
    新しいノートブックを開始するには、次の 2 つの簡単な方法があります。

    ### ノートブックのインデックスから始める

    1. インデックスの右上隅にある<DNT>**+ Create a notebook**</DNT>をクリックします。
    2. ノートブックに意味のある名前を付けます (名前は検索可能なので、サービス名やアプリケーション名などの関連するキーワードを使用します)。
    3. <DNT>**Create**</DNT>をクリックします。
    4. クエリ コンソール ドロワーに、最初のクエリを入力します。

    ### クエリコンソールから開始する

    1. クエリ コンソール (プラットフォームの下部にある引き出し) を開きます。
    2. 必要なクエリと Markdown ブロックを作成します。
    3. <DNT>**Save**</DNT>をクリックします。
    4. ノートブックに名前を付けて、<DNT>**Save**</DNT>をクリックします。
  </Collapser>

  <Collapser id="manage-notebooks" title="ノートブックを管理する">
    ### ノートブックの検索と並べ替え

    組織内の特定のノートブックを検索するには:

    1. **名前で検索**: ノートブック インデックスの上部にある検索バーを使用して、名前またはキーワードでノートブックを検索します。

    2. **ノートブックの並べ替え**: 並べ替えドロップダウンをクリックして、ノートブックを次の基準で整理します。

       * 名前（AZまたはZA）
       * 最終更新日（最新または古い順）
       * 作成日（新しい順または古い順）
       * お気に入り（スター付きのノートブックが最初に表示されます）

    3. **お気に入りでフィルタリング**: 星型フィルタをクリックすると、お気に入りのノートブックだけが表示されます。

    ### ノートブックを複製する

    1. 複製したいノートブックを開きます。
    2. コンソールドロワーのタブ名の横にある矢印をクリックし、<DNT>**duplicate**</DNT>をクリックします。
    3. コンテキスト メニューから<DNT>**Duplicate**</DNT>をクリックします。
    4. ノートブックのコピーが新しいタブで開き、編集できるようになります。

    ### ノートブックをお気に入りとしてマークする

    最も重要なノートブックをすばやく見つけるには、それらをお気に入りとしてマークすることができます。

    1. インデックス ページでノートブックの名前の横にある星のアイコンをクリックします。
    2. アイコンが黄色に変わり、ノートブックはインデックス リストの上部にグループ化されます。
    3. お気に入りから削除するには、星アイコンをもう一度クリックします。
  </Collapser>

  <Collapser id="delete-notebook" title="ノートブックを削除する">
    <Callout variant="caution">
      ノートブックの削除は永久的であり、元に戻すことはできません。削除されると、ノートブックとそのすべてのコンテンツはシステムから完全に削除されます。削除する前に、保存したいノートブックを必ず複製してください。
    </Callout>

    1. ノートブックのインデックス リストで、削除するノートブックの 3 つのドットのアクション メニューをクリックします。
    2. <DNT>**Delete**</DNT>オプションをクリックします。
    3. もう一度<DNT>**Delete**</DNT>をクリックして削除を確認します。
  </Collapser>
</CollapserGroup>

## ノートブックブロック

ブロックはノートブックの基本的な構築単位です。これらは、分析と説明を構成するために追加する個別のセクションです。

### クエリブロック

クエリブロックを使用して[<DNT>NRQL</DNT>クエリ](/docs/nrql/nrql-syntax-clauses-functions/)を記述し、結果を視覚化します。標準のクエリビルダーで利用可能なすべての単一クエリの[チャートタイプ](https://docs.newrelic.com/docs/query-your-data/explore-query-data/use-charts/chart-types/)とカスタマイズは、ノートブックでサポートされています。

### テキストブロック

Markdown を使用して、リッチ テキスト、タイトル、コンテキスト、説明、わかりやすいテキストを追加して、調査のストーリーを伝えます。

### ブロックアクション

各[ブロックを](/docs/query-your-data/explore-query-data/notebooks/introduction-notebooks/)操作してドキュメントを整理できます。

* <DNT>**Duplicate**</DNT>: ブロックのコピーを作成する
* <DNT>**Delete**</DNT>: ノートブックからブロックを削除します
* <DNT>**Move Up/Down**</DNT>: ノートブック内のブロックを並べ替えます
* <DNT>**Move to Top/Bottom**</DNT> : ブロックを先頭または末尾にすばやく移動します

## ノートブックの権限

ノートブックの作成を開始する前に、権限の仕組みと、誰がノートブックにアクセスできるかを理解することが大切です。

### 組織協会

すべてのノートブックは、それが作成された組織に属するリソースです。この関連付けは永続的であり、変更することはできません。ノートブックは異なる組織間で共有できません。組織が削除されると、ノートブックも削除されます。ユーザーが別の組織に移動すると、以前の組織のノートブックにアクセスできなくなります。

### 現在の権限モデル

New Relic Notebooks uses custom roles with CRUD capabilities to control access. There are two levels of access: organization-level (set by an admin) and notebook-level (set per notebook by its creator).

#### Organization-level permissions

These are granted via custom roles in the New Relic admin UI.

<table>
  <thead>
    <tr>
      <th>
        ロール
      </th>

      <th>
        機能
      </th>

      <th>
        What it allows
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        **Full access**
      </td>

      <td>
        `notebooks.create.notebook`

        <br />

        `notebooks.read.notebook`

        <br />

        `notebooks.update.notebook`

        <br />

        `notebooks.delete.notebook`
      </td>

      <td>
        Create, read, edit, and delete any notebook in the org
      </td>
    </tr>

    <tr>
      <td>
        **Creator**
      </td>

      <td>
        `notebooks.create.notebook`
      </td>

      <td>
        Create new notebooks (own notebooks only)
      </td>
    </tr>
  </tbody>
</table>

#### Notebook-level roles

These are assigned on a specific notebook, typically by the notebook&apos;s creator.

<table>
  <thead>
    <tr>
      <th>
        ロール
      </th>

      <th>
        許可
      </th>

      <th>
        What it allows
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        **オーナー**
      </td>

      <td>
        `notebook_owner`
      </td>

      <td>
        Read, edit, delete, and manage permissions — automatically assigned to the creator
      </td>
    </tr>

    <tr>
      <td>
        **Editor**
      </td>

      <td>
        `notebook_editor`
      </td>

      <td>
        Edit the notebook
      </td>
    </tr>

    <tr>
      <td>
        **Reader**
      </td>

      <td>
        `notebook_reader`
      </td>

      <td>
        View the notebook (read-only)
      </td>
    </tr>
  </tbody>
</table>

#### How it works together

1. An admin gives a user the **Creator** org-level role — this lets them create notebooks.
2. When a user creates a notebook, they automatically become its **Owner**.
3. The owner can then share the notebook with others by granting them **Editor** or **Reader** roles on that specific notebook.
4. Users who need access to all notebooks org-wide (for example, admins) get the **Full access** role instead.

## 次は何ですか？

ノートブックの基本を理解したら、次のことができるようになります。

* [ノートブックのブロック](/docs/query-your-data/explore-query-data/notebooks/add-queries-to-notebooks/)について詳しく学ぶ
* [ノートブックの変数](/docs/query-your-data/explore-query-data/notebooks/notebook-variables)を使用して、動的でパラメータ化されたランブックを構築します。
* [ノートブック向けBlob Storage API](/docs/query-your-data/explore-query-data/notebooks/blob-storage-api-for-notebooks)を使用して、プログラムでノートブックを管理します。

関連する機能もご覧ください:

* 永続的なデータ視覚化を構築するための[Dashboards](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards)
* 個別のクエリを作成するための[クエリビルダー](/docs/query-your-data/explore-query-data/query-builder/introduction-query-builder)
* 視覚化オプションを理解するための[チャート](/docs/query-your-data/explore-query-data/use-charts/use-your-charts)