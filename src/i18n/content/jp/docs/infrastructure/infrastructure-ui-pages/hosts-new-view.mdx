---
title: インフラストラクチャ ホスト ページ (新しいビュー)
tags:
  - Infrastructure
  - Infrastructure monitoring UI
  - Infrastructure UI
  - Hosts page
metaDescription: Use the Hosts page to view the most important metrics from across your server/host environment.
translationType: machine
---

import infrastructureHostsNewSummaryAnimated from 'images/infrastructure_screenshot-crop_host-page-tiles.gif'

import infrastructureExportCSV from 'images/infrastructure_icon_export-csv.webp'

import infrastructureHostNewSystem from 'images/infrastructure_screenshot-full_host-new-system.webp'

import infrastructureHostNewNetwork from 'images/infrastructure_screenshot-full_host-new-network.webp'

import infrastructureHostNewProcesses from 'images/infrastructure_screenshot-full_host-new-processes.webp'

import infrastructureHostNewStorage from 'images/infrastructure_screenshot-full_host-new-storage.webp'

import infrastructureHostNewContainers from 'images/infrastructure_screenshot-full_host-new-containers.webp'

import infrastructureFiltertoHost from 'images/infrastructure_screenshot-crop_filter-to-host.webp'

import infrastructureHostMetadata from 'images/infrastructure_screenshot-scrop_hosts-show-more.webp'

import infrastructureActivityStream from 'images/infrastructure_screenshot-crop_activity-stream.webp'

import infrastructureHostApplication from 'images/infrastructure_screenshot-crop_applications-tile-list.webp'

import infrastructureEventsPage from 'images/infrastructure_screenshot-crop_infra-events-page.webp'

<img
  title="Hosts (New) Summary page"
  alt="Hosts (New) Summary page"
  src={infrastructureHostsNewSummaryAnimated}
/>

<figcaption>
  **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New)**
</figcaption>

**Hosts** \[ホスト] ページとそのタイルを使用すると、リソースの使用状況、ネットワークとプロセスのパフォーマンス、コンテナに関するデータとコンテナ間の関係など、重要なパフォーマンス メトリックをより深く理解できます。属性でホストをフィルタリングし、そのパフォーマンスをインベントリ変更イベントと比較して、根本原因を特定することができます。問題を防ぐためにアラート通知を設定することもできます。

## 新しいインフラストラクチャUIにアクセスする [#access-new-ui]

ホスト情報を表示するには、 **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts**に移動し、 **Overview** \[概要] タブをクリックします。

新しい UI には、ホストで何が起こっているかを高レベルで確認できるだけでなく、特定のホストの詳細を確認してそれらのホストでフィルタリングするためにすばやくドリルダウンするのに役立つサマリー タイルが含まれています。

## フィルターと保存済みビュー [#filter]

ページの上部にあるフィルターを使用して、提案されたフィルターを使用するか、ホストの名前または添付されたタグを入力して、ホストを見つけます。

インフラストラクチャ用に保存されたビューがある場合は、 **Saved views**\[保存されたビュー] ボタンからアクセスできます。

## 「ホスト」タブ [#tabs]

**\[ホスト]** ページで使用できるタブは次のとおりです。

<CollapserGroup>
  <Collapser
    id="summary"
    title="概要"
  >
    **Summary**\[概要] タブには、ホストのパフォーマンスの概要が表示されます。

    ページの上部にある 4 つのタイルには、ホストとそのアプリケーション、イベント、アラートの概要が表示されます。これらのタイルを使用して、ホストに関する詳細を表示したり、概要ビュー全体を特定のホストでフィルターしたりできます。

    ### ホスト

    ホスト タイルには、アカウント内のホストの合計数が表示されます。クリックすると、すべてのホストのリストが表示されます。そのリストから、特定のホストを検索または参照します。特定のホスト フィルターを展開してそのホストにビューを表示するか、より多くのデータを表示します。

    <img
      title="Host metadata"
      alt="Host metadata"
      src={infrastructureHostMetadata}
      style={{ height: '100%', width: '100%'}}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New)** の順に選択し、 **Hosts** \[ホスト] タイルをクリックしてホストを選択します。
    </figcaption>

    **Show more** \[さらに表示] ボタンをクリックすると、そのホストの詳細なメタデータが表示されます。

    **Filter to host** \[ホストにフィルター] ボタンをクリックして、 **Hosts** \[ホスト] ページをそのホストにフィルターします。ビュー全体がフィルターされ、そのホストのアプリケーション、イベント、アラート、およびページ上の他のすべてのグラフがフィルターされます。

    ### アプリケーション

    <img
      title="Applications tile plus list"
      alt="applications tile plus list"
      src={infrastructureHostApplication}
      style={{ height: '100%', width: '100%'}}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New)** に移動し、 **Applications** \[アプリケーション] タイルをクリックしてアプリケーションを選択します。
    </figcaption>

    アプリケーション タイルには、ホストに直接インストールされている APM アプリケーションの総数が表示されます。この数値は、適用したホスト フィルターに基づいて変化します。

    フィルター ボタンをクリックすると、そのアプリケーションに関連するホスト、イベント、アラートだけが表示され、そのアプリケーションに対してフィルターされたページ内の他のすべてのグラフが表示されます。

    **View in APM** \[APM で表示] をクリックすると、そのアプリケーションの APM 概要ページが表示されます。

    ### イベント

    イベント タイルには、ホストに関連するイベントの合計数が表示されます。

    <img
      title="Infrastructure events page"
      alt="Infrastructure events page"
      src={infrastructureEventsPage}
      style={{ height: '100%', width: '100%'}}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New)**、 **Events** \[イベント] タイルをクリックしてイベント ページに移動します。
    </figcaption>

    **Events**\[イベント] タイルをクリックして、**[one.newrelic.com](https://one.newrelic.com/all-capabilities) > インフラストラクチャ > イベント** の **Events**\[イベント] ページに移動します。

    ### アラート

    アラート タイルには、重大または警告状態にあるホストの数が表示されます。タイルをクリックして詳細を表示し、特定のインシデントをクリックして他の関連問題、根本原因の分析、影響を受けるその他のエンティティを表示します。

    <img
      title="Activity stream"
      alt="Activity stream"
      src={infrastructureActivityStream}
      style={{ height: '100%', width: '100%'}}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New)** に移動し、 **Alerting** \[アラート] タイルをクリックして、アラート アクティビティを確認します。
    </figcaption>
  </Collapser>

  <Collapser
    id="system"
    title="システム"
  >
    **System**\[システム] タブには、システムの概要が表示されます。

    **System** \[システム] テーブルでは、メトリック名でホストをフィルタリングできます。 とともに<img src={infrastructureExportCSV} class="inline" style={{height: "30px", width: "30px"}} alt="Export CSV" title="Export CSV"/>

    ボタンをクリックすると、このテーブルを CSV ファイルとしてエクスポートできます。

    <img
      title="Hosts System page"
      alt="Hosts System page"
      src={infrastructureHostNewSystem}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New) > System**
    </figcaption>
  </Collapser>

  <Collapser
    id="network"
    title="通信網"
  >
    **Network**\[ネットワーク] タブには、ネットワーク インターフェイスに関する帯域幅とエラー データが表示されます。

    **Network** \[ネットワーク] テーブルでは、メトリック名でホストをフィルタリングできます。 とともに<img src={infrastructureExportCSV} class="inline" style={{height: "30px", width: "30px"}} alt="Export CSV" title="Export CSV"/>

    ボタンをクリックすると、このテーブルを CSV ファイルとしてエクスポートできます。

    <img
      title="Hosts Network page"
      alt="Hosts Network page"
      src={infrastructureHostNewNetwork}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New) > Network**
    </figcaption>
  </Collapser>

  <Collapser
    id="processes"
    title="プロセス"
  >
    **Processes**\[プロセス] タブには、個々のプロセスまたはプロセスのグループの CPU パーセンテージ、I/O バイト、およびメモリ使用量に関するデータが表示されます。

    **Processes** \[プロセス] テーブルでは、メトリック名でホストをフィルターできます。 とともに<img src={infrastructureExportCSV} class="inline" style={{height: "30px", width: "30px"}} alt="Export CSV" title="Export CSV"/>

    ボタンをクリックすると、このテーブルを CSV ファイルとしてエクスポートできます。

    <img
      title="Hosts Processes page"
      alt="Hosts Processes page"
      src={infrastructureHostNewProcesses}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New) > Process**
    </figcaption>
  </Collapser>

  <Collapser
    id="storage"
    title="ストレージ"
  >
    **Storage**\[ストレージ] タブには、デバイスの全体的な使用率、ディスク使用率、I/O 操作など、リソースの容量と効率が表示されます。

    **Storage** \[ストレージ] テーブルでは、メトリック名でホストをフィルター処理できます。 と<img src={infrastructureExportCSV} class="inline" style={{height: "30px", width: "30px"}} alt="Export CSV" title="Export CSV"/>

    ボタンをクリックすると、このテーブルを CSV ファイルとしてエクスポートできます。

    <img
      title="Hosts Storage page"
      alt="Hosts Storage page"
      src={infrastructureHostNewStorage}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New) > Storage**
    </figcaption>
  </Collapser>

  <Collapser
    id="containers"
    title="コンテナ"
  >
    **Containers**\[コンテナー] タブには、個々のコンテナーまたはコンテナーのグループの CPU パーセンテージ、I/O バイト、およびメモリ使用量が表示されます。

    **Containers** \[コンテナー] テーブルでは、メトリック名でホストをフィルターできます。 とともに<img src={infrastructureExportCSV} class="inline" style={{height: "30px", width: "30px"}} alt="Export CSV" title="Export CSV"/>

    ボタンをクリックすると、このテーブルを CSV ファイルとしてエクスポートできます。

    <img
      title="Hosts Containers page"
      alt="Hosts Containers page"
      src={infrastructureHostNewContainers}
    />

    <figcaption>
      **[one.newrelic.com](https://one.newrelic.com/all-capabilities) > Infrastructure > Hosts (New) > Containers**
    </figcaption>
  </Collapser>
</CollapserGroup>

## イベントと属性

インフラストラクチャのイベントと属性について詳しく知りたい場合は、 [それらの説明を確認してください](/docs/infrastructure/manage-your-data/data-instrumentation/default-infrastructure-monitoring-data/#infrastructure-events)。