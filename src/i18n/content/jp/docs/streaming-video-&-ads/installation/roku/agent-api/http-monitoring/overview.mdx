---
title: HTTP監視
metaDescription: 'Methods to enable, disable, and send HTTP request and response events.'
freshnessValidatedDate: never
translationType: machine
---

## HTTP監視メソッド

<table>
  <thead>
    <tr>
      <th width="30%">
        方法
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [HTTPイベントを有効にする](/docs/streaming-video-&-ads/installation/roku/agent-api/http-monitoring/nrenablehttpevents)
      </td>

      <td>
        HTTP\_CONNECT/HTTP\_COMPLETE イベントを有効にします。
      </td>
    </tr>

    <tr>
      <td>
        [HTTPイベントを無効にする](/docs/streaming-video-&-ads/installation/roku/agent-api/http-monitoring/nrdisablehttpevents)
      </td>

      <td>
        HTTP\_CONNECT/HTTP\_COMPLETE イベントを無効にします。
      </td>
    </tr>

    <tr>
      <td>
        [HTTPリクエストの送信](/docs/streaming-video-&-ads/installation/roku/agent-api/http-monitoring/nrsendhttprequest)
      </td>

      <td>
        ConnectedDeviceSystem タイプの HTTP\_REQUEST イベントを送信します。
      </td>
    </tr>

    <tr>
      <td>
        [送信Httpレスポンス](/docs/streaming-video-&-ads/installation/roku/agent-api/http-monitoring/nrsendhttpresponse)
      </td>

      <td>
        ConnectedDeviceSystem タイプの HTTP\_RESPONSE イベントを送信します。
      </td>
    </tr>
  </tbody>
</table>

## 例

### HTTPイベントを有効にする

```js
  	nrEnableHttpEvents(nr)
```

### HTTPイベントを無効にする

```js
  	nrDisableHttpEvents(nr)
```

### HTTPリクエストの送信

```js
  	urlReq = CreateObject("roUrlTransfer")
	  urlReq.SetUrl(_url)
	  ...
	  nrSendHttpRequest(m.nr, urlReq)
```

### 送信Httpレスポンス

```js
  	msg = wait(5000, m.port)
	  if type(msg) = "roUrlEvent" then
		        nrSendHttpResponse(m.nr, _url, msg)
	  end if
```