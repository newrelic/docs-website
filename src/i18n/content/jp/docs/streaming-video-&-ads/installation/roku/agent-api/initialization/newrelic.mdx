---
title: ニューレリック
type: apiDoc
shortDescription: New Relicエージェント オブジェクトを構築します。
tags: null
metaDescription: Build a New Relic Agent object.
freshnessValidatedDate: never
translationType: machine
---

## 構文

```js
NewRelic(account as String, apikey as String, region = "US" as String, activeLogs = false as Boolean) as Object
```

## 説明

New Relicエージェント オブジェクトを構築します。

## 引数

<table>
  <thead>
    <tr>
      <th width="25%">
        引数
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        アカウント
      </td>

      <td>
        New Relic アカウント番号。
      </td>
    </tr>

    <tr>
      <td>
        アピキー
      </td>

      <td>
        APIキー。
      </td>
    </tr>

    <tr>
      <td>
        リージョン
      </td>

      <td>
        (オプション) New Relic API リージョン (EU または US)。 デフォルトは米国です。
      </td>
    </tr>

    <tr>
      <td>
        アクティブログ
      </td>

      <td>
        (オプション) ログを有効にするかどうか。 デフォルトは False。
      </td>
    </tr>
  </tbody>
</table>

## 戻る

New Relicエージェント オブジェクト。

## 例

```js
	sub Main(aa as Object)
		screen = CreateObject("roSGScreen")
		m.port = CreateObject("roMessagePort")
		screen.setMessagePort(m.port)
		scene = screen.CreateScene("VideoScene")
		screen.show()
	
		m.nr = NewRelic("ACCOUNT ID", "API KEY")
```