---
title: newrelic_capture_params (PHP エージェント API)
type: apiDoc
shortDescription: URLパラメータの取得を有効または無効にします。
tags:
  - Agents
  - PHP agent
  - PHP agent API
metaDescription: New Relic PHP agent API call to enable attribute capture and override config settings.
translationType: machine
---

## 構文

```
newrelic_capture_params([bool $enable_flag])
```

URLパラメータの取得を有効または無効にします。

## 要件

すべてのAgentバージョンに対応しています。

## 説明

URLパラメータ（URL中の `?` 以降のすべて）のキャプチャを有効または無効にします。これは、 `newrelic.capture_params` 設定ファイルの設定よりも優先されます。

<Callout variant="important">
  機密情報をURLで直接渡す場合は、これを無効にしてください。
</Callout>

## パラメータ

<table>
  <thead>
    <tr>
      <th width="25%">
        パラメーター
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$enable_flag`

        _boolean_
      </td>

      <td>
        オプションです。デフォルトは `true` 。

        `true` または省略された場合、URLパラメータのキャプチャを有効にします。

        `false` の場合、URLパラメータのキャプチャを無効にします。
      </td>
    </tr>
  </tbody>
</table>

## 例

### URLパラメータの取得を可能にする [#enable-params]

```
function example() {
    if (extension_loaded('newrelic')) { // Ensure PHP agent is available
	    newrelic_capture_params();
   	}
    <var>...</var>
}
```

### URLパラメータのキャプチャを無効にする [#disable-params]

```
function example() {
    if (extension_loaded('newrelic')) { // Ensure PHP agent is available
	    newrelic_capture_params(false);
    }
    <var>...</var>
}
```