---
title: shutdown_agent (PythonエージェントAPI)
type: apiDoc
shortDescription: エージェントを強制的にシャットダウンします。
tags:
  - Agents
  - Python agent
  - Python agent API
metaDescription: 'Python API: This call shuts down the Python agent, with an optional timeout value.'
translationType: machine
---

## 構文

```
newrelic.agent.shutdown_agent(timeout=None)
```

エージェントを強制的にシャットダウンします。

## 説明

エージェントを強制的にシャットダウンします。エージェントは、データを [コレクター](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#collector) にアップロードする最後の試みを行います。

## パラメータ

<table>
  <thead>
    <tr>
      <th width="25%">
        パラメーター
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `タイムアウト`

        _int_ or _float_
      </td>

      <td>
        任意ですが、強く推奨します。エージェントがデータのアップロードを終了するまで呼び出し側が待機する最大時間です。エージェントが終了する前にタイムアウト期間が終了した場合、エージェントはバックグラウンドでデータを報告し続けますが、呼び出し側はプロセス全体を強制的に終了させ、アップロードを停止させることができます。

        値が設定されていない場合、または `None` に設定されている場合、エージェントはデフォルトのグローバルタイムアウト値を使用します。このタイムアウト値は設定で上書きすることができますが、デフォルトは2.5秒です。
      </td>
    </tr>
  </tbody>
</table>

## 戻り値

なし。

## 例

### 3秒間のタイムアウトで [#timeout-example]

```
newrelic.agent.shutdown_agent(timeout=3)
```