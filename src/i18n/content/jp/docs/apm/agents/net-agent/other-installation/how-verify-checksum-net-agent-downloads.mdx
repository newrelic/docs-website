---
title: .NETエージェントのダウンロードのチェックサムを確認する方法
tags:
  - Agents
  - NET agent
  - Other installation
translationType: machine
---

New Relic の .NET エージェントインストーラーのチェックサムを確認する方法についてはこちらをご覧ください。現在のリリースのハッシュが必要な場合は、 [ダウンロードサイト](http://download.newrelic.com/dot_net_agent/latest_release/SHA256/) からSHA256ハッシュをダウンロードしてください。

## Windows

Windowsでは、CertUtilまたはGet-FileHashを使ってファイルハッシュを計算することをお勧めします。

### CertUtil

このユーティリティは、Certificate Services の一部として提供されており、ファイルのハッシュを計算するために使用することができます。このユーティリティは、コマンドプロンプトまたは Powershell から実行できます。

ハッシュを計算するには、次のように実行します。

`CertUtil -hashfile YOUR_FILENAME SHA256`

成功すると、このような回答が得られます。

```
SHA256 hash of <var>YOUR_FILENAME</var>:
5092fd52e40132a41ac06c320296cd9d63196d6de0fa6ce9b74fc4de1b3f9502
CertUtil: -hashfile command completed successfully.
```

### Powershellです。Get-FileHash [#getfilehash]

Get-FileHashコマンドレットは、Powershellバージョン5.1+に含まれており、ファイルのハッシュを計算するために使用することができます。

ハッシュを計算するには、次のように実行します。

`Get-FileHash YOUR_FILENAME アルゴリズム SHA256 | Format-List`

成功すると、このような回答が得られます。

```
Algorithm : SHA256
Hash      : 5092FD52E40132A41AC06C320296CD9D63196D6DE0FA6CE9B74FC4DE1B3F9502
Path      : C:\Path\To\<var>YOUR_FILENAME</var>
```

## Linux

Linuxでは

```
sha256sum
```

を使用して、ファイルハッシュを計算します。

### sha256sum [#she256sum]

ハッシュを計算するには、次のように実行します。

`sha256sum YOUR_FILENAME`

成功すると、このような回答が得られます。

```
5092fd52e40132a41ac06c320296cd9d63196d6de0fa6ce9b74fc4de1b3f9502 <var>YOUR_FILENAME</var>
```