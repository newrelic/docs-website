---
title: データベースクエリの命名
tags:
  - Agents
  - NET agent
  - Custom instrumentation
metaDescription: 'For New Relic .NET agent, how to name queries in your application.'
translationType: machine
---

import apmScreenshotShowingWhereYouWouldSeeaCustomQueryName from 'images/apm_screenshot-crop_screenshot-showing-where-you-would-see-a-custom-query-name.webp'

New Relic .NETエージェントは、アプリケーションのSQLクエリを計測およびグループ化します。通常、.NETエージェントは、クエリのプロパティに基づいてクエリに名前を自動的に割り当てます。または、クエリにカスタム名を割り当てることもできます。

## クエリに名前を付ける方法 [#name-query]

.NETエージェントでサポートされているすべてのSQLバージョンには、カスタム名を付けることができます。

クエリに名前を付けるには、 `/* NewRelicQueryName: myQueryName */`のような形式でクエリテキストにコメントを追加します。これをアプリのコードに追加する例を次に示します。

```cs
var connection = new MySqlConnection(mySqlConnectionString);
var command = new MySqlCommand("/* NewRelicQueryName: My query name */ SELECT date FROM dates", connection);
```

## 名前付きクエリを検索 [#find-named-query]

データベースとクエリデータは、[**データベース**UIページ](/docs/apm/apm-ui-pages/monitoring/databases-page-view-operations-throughput-response-time)にあります。名前付きクエリは、名前が角かっこで囲まれて表示されます。その例を次に示します。

<img
  title="named_query.png"
  alt="named_query.png"
  src={apmScreenshotShowingWhereYouWouldSeeaCustomQueryName}
/>

## トラブルシューティング

New Relicにクエリデータが表示されませんか？<a href="/docs/apm/agents/net-agent/troubleshooting/no-data-appears-net">トラブルシューティングのドキュメント</a>をご覧ください。