---
title: removeAllAttributes（iOS SDK API）
type: apiDoc
shortDescription: セッションからすべての属性を削除します。
tags:
  - Mobile monitoring
  - New Relic Mobile iOS
  - iOS SDK API
metaDescription: New Relic API for iOS mobile app monitoring API to remove all attributes from the session.
translationType: machine
---

## 構文

```
removeAllAttributes();
NewRelic.removeAllAttributes()
```

セッションからすべての属性を削除します。

## 要件

すべてのAgentバージョンに対応しています。

## 説明

セッションからすべての属性を削除します。

このAPIの使用方法については、カスタム属性やイベントをInsights forに送信する際のドキュメントを参照してください。

* [Objective-C](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/work-ios-sdk-api#objc-custom-att-events)
* [Swift](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/work-ios-sdk-api#swift-custom-att-events)

## 戻り値

成功した場合は `true` を、失敗した場合は `false` を返します。

## 例

### Objective-C [#obj-c]

**方法です。**

```
+ (BOOL) removeAllAttributes();
```

**例:**

```
BOOL attributesRemoved = NewRelic.removeAllAttributes();
```

### Swift

**方法です。**

```
NewRelic.removeAllAttributes() -> Bool
```

**例:**

```
let attributesRemoved = NewRelic.removeAllAttributes()
```