---
title: removeAttribute（iOS SDK API）
type: apiDoc
shortDescription: 指定されたセッション属性を削除します。
tags:
  - Mobile monitoring
  - New Relic Mobile iOS
  - iOS SDK API
metaDescription: New Relic API for iOS mobile app monitoring API to remove an attribute from the session.
translationType: machine
---

## 構文

```
removeAttribute:(NSString*)name;
NewRelic.removeAttribute()
```

指定されたセッション属性を削除します。

## 要件

すべてのAgentバージョンに対応しています。

## 説明

このメソッドは、名前の文字列で指定された属性をセッションから削除します。

このAPIの使用方法については、カスタム属性やイベントをInsights forに送信する際のドキュメントを参照してください。

* [Objective-C](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/work-ios-sdk-api#objc-custom-att-events)
* [Swift](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/work-ios-sdk-api#swift-custom-att-events)

## パラメータ

<table>
  <thead>
    <tr>
      <th width="25%">
        パラメーター
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$name`

        _文字列_
      </td>

      <td>
        必須項目です。削除するセッション属性の名前です。
      </td>
    </tr>
  </tbody>
</table>

## 戻り値

成功した場合は `true` を、失敗した場合は `false` を返します。

## 例

### Objective-C [#obj-c]

**方法です。**

```
+ (BOOL) removeAttribute:(NSString*)name;
```

**例:**

`NSString*` で指定されたセッション属性を削除します。例えば、 `rate`.

```
BOOL attributeRemoved = [NewRelic removedAttribute@"rate"];
```

### Swift

**方法です。**

```
NewRelic.removeAttribute(name: String!) -> Bool
```

**例:**

`rate` という名前のセッション属性を削除しています。

```
let attributeRemoved = NewRelic.removedAttribute("rate")
```