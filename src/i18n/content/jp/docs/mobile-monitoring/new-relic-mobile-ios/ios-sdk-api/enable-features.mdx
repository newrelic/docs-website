---
title: enableFeatures (iOS SDK API)
type: apiDoc
shortDescription: 機能を有効にするには、このビット単位のフラグを設定します。
tags:
  - Mobile monitoring
  - New Relic Mobile iOS
  - iOS SDK API
metaDescription: New Relic for iOS mobile app monitoring API to enable features.
translationType: machine
---

## 構文

```
- (void) enableFeatures:(NRMAFeatureFlags)featureFlags;
```

機能を有効にするには、このビット単位のフラグを設定します。

## 要件

すべてのAgentバージョンに対応しています。

## 説明

New Relic iOS エージェントのほとんどの機能は、デフォルトで有効になっています。デフォルトでは有効になっていない機能を有効にする場合は、 `enableFeatures` API を使用できます。デフォルトで有効になっている機能の詳細については、 [iOS エージェントの構成と機能フラグを](/docs/mobile-monitoring/new-relic-mobile-ios/api-guides/ios-agent-configuration-feature-flags)参照してください。

<Callout variant="important">
  設定を有効にするには、 `startWithApplicationToken:`を呼び出す前にこの関数を呼び出す必要があります。
</Callout>

## パラメーター

<table>
  <thead>
    <tr>
      <th width="25%">
        パラメータ
      </th>

      <th>
        説明
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$featureFlags`

        NRMAFeatureFlags
      </td>

      <td>
        必要。機能を有効にするビット単位のフラグ。
      </td>
    </tr>
  </tbody>
</table>

## 例

### Objective-C [#obj-c]

**方法です。**

```
+[NewRelic enableFeatures:(NRMAFeatureFlags)flags];
```

**例：**

Objective-C の例:

```
[NewRelic enableFeatures:NRFeatureFlag_SwiftInteractionTracing | NRFeatureFlag_GestureInstrumentation];
```

迅速な例:

```
 NewRelic.enableFeatures([ NRMAFeatureFlags.NRFeatureFlag_SwiftInteractionTracing, NRMAFeatureFlags.NRFeatureFlag_GestureInstrumentation ])
```