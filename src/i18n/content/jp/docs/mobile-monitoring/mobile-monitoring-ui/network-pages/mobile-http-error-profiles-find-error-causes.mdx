---
title: モバイルHTTPエラーのプロファイル。エラーの原因究明
tags:
  - Mobile monitoring
  - Mobile monitoring UI
  - Network pages
metaDescription: 'An explanation of HTTP error profile feature, which gives you insight about your HTTP errors.'
freshnessValidatedDate: never
translationType: machine
---

import mobileHttpErrorProfiles from 'images/mobile_screenshot-full_http-error-profiles.webp'

import mobileDeviceExample from 'images/mobile_screenshot-full_device-example.webp'

[HTTPエラーのUIページ](/docs/mobile-monitoring/mobile-monitoring-ui/network-pages/http-errors-network-failure-analysis), **HTTPエラーのプロファイル** では、HTTPエラーの潜在的な原因について知ることができます。

## HTTPエラープロファイルの表示 [#view]

閲覧するには<InlinePopover type="mobile"/>エラープロファイル:

1. **[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities) > Mobile >** (アプリを選択) **> More views > HTTP errors**に移動します。
2. HTTP エラー プロファイルの詳細を確認したい場合は、HTTP エラー UI ページの下部に移動します。次に、表の上にある**HTTP error profiles** \[\[HTTP エラー プロファイル]]タブを選択し、HTTP エラー プロファイルを選択して展開します。

## HTTPエラープロファイルとは何ですか？ [#about]

**HTTP error profile** \[HTTP エラー プロファイルは]、成功した HTTP リクエスト ( `MobileRequest`イベント) と比較した場合に、HTTP エラー値 ( `MobileRequestError`イベント) の間に見られる、潜在的に意味のある相関関係です。これらの相関関係は、HTTP エラーの背後にある原因と要因を調査するのに役立ちます。

以下のスクリーンショットは、HTTPエラープロファイルの例です。

<img
  title="Mobile error profile"
  alt="Mobile error profile"
  src={mobileHttpErrorProfiles}
/>

<figcaption>
  **[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities) > Mobile >** (アプリを選択) **> More views > HTTP errors >** ( **HTTP エラー プロファイル**まで下にスクロールします)
</figcaption>

また、 [HTTPエラープロファイルの生成に使用されたデータをフィルタリングすることで、潜在的な原因を絞り込むことができます。](#filter)

[](#filter)

[特定のHTTPエラープロファイルの分析については、 ](#filter)[例のHTTPエラープロファイルの説明](#example-profile) を参照してください。

## フィルタリングされたHTTPエラープロファイルデータ [#filter]

HTTP エラー プロファイルとその比較ページ ビューの生成に使用されるデータをさらにフィルタリングするには、次の[HTTP エラー ページ](/docs/mobile-monitoring/mobile-monitoring-ui/network-pages/http-errors-network-failure-analysis#group-sort-filters)機能を使用します。

* **Time picker**: データを生成する際に使用する時間範囲を選択します。
* **フィルター**: フィルターを使用して、選択した属性値を持つHTTPエラープロファイルとページビューのみを表示します。複数の属性値を選択できます。

## HTTPエラープロファイルの説明例 [#example-profile]

独自の HTTP エラー プロファイル データをよりよく理解するのに役立つように、これは HTTP エラー プロファイルのデータの例の説明です。この HTTP エラー プロファイルの例には、 `deviceType = iPhone 12` の [フィルタ](#filter) が適用されているため、iPhone 12 デバイスからの HTTP エラー データのみが表示されています。

<img
  title="Mobile error profile example"
  alt="Mobile error profile example"
  src={mobileDeviceExample}
/>

<figcaption>
  **[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities) > Mobile >** (アプリを選択) **> More views > HTTP errors** > (iOS 12 デバイスでフィルター): このサンプル プロファイルには、iPhone からの HTTP エラー データのみが表示されるように [フィルター](#filter) が適用されています。 12台のデバイス。
</figcaption>

コラムの説明です。

* 最初の列は「 **Errors** 」列です。成功したHTTPリクエストと比較して、最も偏差の大きい属性値を示しています。重要度の低い属性値は、 **Other** のカテゴリに集約されています。この例では、Chrome ユーザーの HTTP エラーの 100% が OS バージョン 6.0、5.1、7.0、および 7.1.1 で占められています。

* **Errors** \[エラー]列には、最初の列の属性値を持つ`MobileRequestError`イベントの割合が表示されます。この列の合計は 100% となり、選択した[時間範囲とフィルター セット](#filter)からのすべての HTTP エラーを表します。この例では、HTTP エラーの大部分が Galaxy OS バージョン 7.1.1 から発生しています。これをさらに調査したい場合は、OS バージョン 7.1.1 のみを表示するようにフィルタリングできます。データ。

* **Requests** \[リクエスト]列には、 `MobileRequest`イベント (成功した HTTP リクエスト) のうち、最初の列の属性値が含まれる割合が表示されます。この例では、OS バージョン 6 のユーザーによって行われたリクエストは 14% 未満ですが、HTTP エラーの 24% はそのバージョンのユーザーによって生成されています。

* **Difference** 列は、 **Errors** 列と **Requests** 列の間のパーセンテージの違いを示しています。この例では、

  <Icon name="nr-caret-up"/>

  11%は、HTTPエラーイベントの方がリクエストよりも属性が11ポイント高いことを示しています。