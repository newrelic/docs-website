---
title: シャットダウン (Android SDK API)
type: apiDoc
shortDescription: 実行時にエージェントをシャットダウンする
tags:
  - Mobile monitoring
  - New Relic Mobile Android
  - Android SDK API
metaDescription: 'New Relic API for Android mobile app monitoring: shut down agent.'
translationType: machine
---

## 構文

```
NewRelic.shutdown()
```

実行時に、現在のアプリケーション ライフサイクル内でエージェントをシャットダウンします。

## 要件

エージェント バージョン 6.10.0 以降。

## 説明

この API は、実行時に現在のアプリケーション ライフサイクル内でエージェントをシャットダウンする機能を提供します。エージェントがシャットダウンすると、現在のアプリケーション ライフサイクル内で再起動することはできません。

## 例

### Android エージェントをシャットダウンする

エージェントをシャットダウンする例を次に示します。

```
NewRelic.shutdown()
```