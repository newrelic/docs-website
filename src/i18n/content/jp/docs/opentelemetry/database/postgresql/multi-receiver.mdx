---
title: NRDOT Collectorを使用したマルチレシーバー設定
tags:
  - OpenTelemetry
  - NRDOT
  - PostgreSQL
  - Self-hosted
metaDescription: Set up a multi-receiver configuration with NRDOT Collector
freshnessValidatedDate: never
translationType: machine
---

<Callout title="プレビュー">
  この機能はまだ開発中ですが、ぜひお試しください。

  この機能は現在、弊社の[プレリリース ポリシー](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/)に従ってプレビューの一部として提供されています。
</Callout>

1つのコレクターから複数のPostgreSQLインスタンスをモニターするようにNRDOT Collectorを設定できます。これは、異なるホストまたはポートで実行されている複数のPostgreSQLデータベースがあり、単一のNRDOT Collectorインスタンスを使用してそれらすべてからメトリクスを収集したい場合に便利です。

<Steps>
  <Step>
    ## 前提条件 [#prerequisites]

    * NRDOTはPostgreSQLインスタンスにインストールおよび設定されています：
      * [セルフホスト型PostgreSQL](/docs/opentelemetry/database/postgresql/hosted)
  </Step>

  <Step>
    ## マルチレシーバー設定の追加 [#multi]

    以下の例は、マルチレシーバー設定を使用して2つのPostgreSQLインスタンスをモニターするようにNRDOT Collectorを設定する方法を示しています。追加のPostgreSQLインスタンス用に、必要に応じてレシーバーを追加できます。

    ```yaml
    receivers:
      nrpostgresql/db1: &nrpostgresql-common
        endpoint: "<YOUR_DB1_HOST>:<YOUR_DB1_PORT>"
        username: "<YOUR_DB_USERNAME>"
        password: "<YOUR_DB_PASSWORD>"
        databases:
          - <YOUR_DB1_DATABASE_NAME>
        collection_interval: 10s
      nrpostgresql/db2:
        <<: *nrpostgresql-common
        endpoint: "<YOUR_DB2_HOST>:<YOUR_DB2_PORT>"
        databases:
          - <YOUR_DB2_DATABASE_NAME>

    service:
      pipelines:
        metrics:
          receivers: [nrpostgresql/db1, nrpostgresql/db2]
          exporters: [otlp/newrelic]
    ```
  </Step>

  <Step>
    ## NRDOT Collectorを再起動します [#restart]

    再起動は必須であり、任意ではありません — コレクターは起動時に一度だけ設定を読み込み、ホットリロードしません。プロセスを再起動するまで、設定の変更は反映されません。

    ```bash
    sudo systemctl restart nrdot-collector
    ```
  </Step>
</Steps>

## 関連ドキュメント [#related]

<DocTiles>
  <DocTile title="互換性と前提条件" path="/docs/opentelemetry/database/postgresql/compatibility">
    サポートされているPostgreSQLのバージョン、必要な権限、およびネットワーク要件について確認してください。
  </DocTile>

  <DocTile title="メトリクスリファレンス" path="/docs/opentelemetry/database/postgresql/metrics-reference">
    NRDOT Collectorによって収集される利用可能なメトリクスについて説明します。
  </DocTile>

  <DocTile title="トラブルシューティングガイド" path="/docs/opentelemetry/database/postgresql/troubleshooting">
    PostgreSQL監視に関する一般的な問題のトラブルシューティング方法について説明します。
  </DocTile>
</DocTiles>