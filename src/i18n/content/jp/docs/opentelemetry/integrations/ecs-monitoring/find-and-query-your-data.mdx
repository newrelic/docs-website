---
title: ECS監視データを検索してクエリを実行する
tags:
  - Integrations
  - OpenTelemetry
  - Amazon ECS
  - AWS
  - Query data
  - Infrastructure monitoring
metaDescription: 'Learn how to find, explore, and query your ECS monitoring data in New Relic after setting up OpenTelemetry collection.'
freshnessValidatedDate: never
translationType: machine
---

Amazon ECS環境のOpenTelemetry監視をセットアップした後、New Relicのインフラストラクチャおよびクエリインターフェース全体でデータを検索してクエリできます。このガイドは、ECSメトリクスを見つけ、監視とトラブルシューティングのための意味を持つクエリを作成するのに役立ちます。

## ECSデータの検索場所 [#find-data]

ECSの監視データは、メトリクスの種類やECSの起動タイプに応じて、New Relic内の複数の場所に表示されます。

### インフラストラクチャのモニタリング [#infrastructure-view]

ECSインフラストラクチャを表示するには、<DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; All Capabilities &gt; Infrastructure**</DNT>に移動します：

#### EC2上のECSの場合

1. ホストビュー：ホストでフィルターして、ECSコンテナを実行しているEC2インスタンスを表示します
2. コンテナビュー: ECSタスク内の個々のコンテナのパフォーマンスを確認する

#### ECS Fargateの場合

1. コンテナビュー：Fargateコンテナメトリクスの主要な場所

<Callout variant="tip">
  フィルターオプションを使用して特定のクラスタ、サービス、またはコンテナ名に絞り込み、必要なデータをすばやく見つけます。
</Callout>

### 指標とイベント [#metrics-events]

**<DNT>[one.newrelic.com](https://one.newrelic.com)</DNT> &gt; Query your data**に移動して、以下を行います：

* カスタムチャートとダッシュボードの作成
* 特定のメトリクスの閾値に基づいてアラートを設定する

## クエリの例 [#query-examples]

### コンテナのCPU使用率をモニターする

```sql
SELECT average(container.cpu.utilization)
FROM Metric
WHERE ClusterName = 'your-cluster-name'
AND ServiceName = 'your-service-name'
TIMESERIES AUTO
FACET container.name
```

### メモリ使用量の傾向を追跡する

```sql
SELECT max(container.memory.usage.total) / 1024 / 1024 as 'Memory (MB)'
FROM Metric
WHERE ClusterName = 'your-cluster-name'
TIMESERIES AUTO
FACET ServiceName
```

### ホストレベルのメトリクスをモニターする（EC2のみ）

```sql
SELECT average(1 - system.cpu.utilization) * 100 AS 'CPU used %',
       average(system.memory.utilization) * 100 AS 'Memory used %'
FROM Metric
WHERE aws.ecs.launchtype = 'ec2'
AND aws.ecs.cluster.arn = 'your-cluster-arn'
TIMESERIES AUTO
```

## データギャップのトラブルシューティング [#troubleshooting]

予期しているデータが表示されない場合：

1. **コレクターのステータスを確認**: OpenTelemetryコレクターのコンテナログを確認します。
2. **ネットワーク接続の確認**：New RelicへのアウトバウンドHTTPSアクセスを確保する
3. **ライセンスキーの確認**: ライセンスキーがSSM パラメーターストアで正しく設定されていることを確認します
4. **タスク定義の確認**: コレクターコンテナが適切なリソース割り当てで実行されていることを確認します。

## 次のステップ [#next-steps]

* ECS固有の視覚化を備えた[高度なダッシュボードを構築](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/)
* プロアクティブな監視のための[インテリジェントなアラートの設定](/docs/alerts/create-alert/create-alert-condition/alert-conditions/)
* コンテナログをNew Relicに転送する場合は、[ログの相関関係を調べる](/docs/logs/ui-data/use-logs-ui/)