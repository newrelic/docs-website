---
title: Instalar el agente eBPF de New Relic para hosts Linux
metaDescription: Learn how to install and configure the New Relic eBPF agent for your Linux host with Debian package installation.
tags:
  - New Relic integrations with eBPF
  - New Relic eBPF agent
  - eBPF integration
  - eAPM
  - eBPF APM
  - Linux host
  - New Relic eBPF agent for Linux
freshnessValidatedDate: never
translationType: machine
---

Puede instalar el agente eBPF de New Relic en su host Linux para monitorear el estado de todo su sistema. El agente eBPF proporciona una visibilidad profunda del rendimiento de la aplicación sin requerir cambios de código ni desplegar un agente específico del idioma.

## Instalar el agente eBPF de New Relic [#install-linux]

<Steps>
  <Step>
    ### Antes de que empieces [#requirements]

    Debe cumplir con los requisitos previos descritos en la documentación [de requisitos y compatibilidad de eBPF](/docs/ebpf/requirements#linux) para hosts Linux.
  </Step>

  <Step>
    ### Instalar el agente eBPF [#install]

    Para instalar el agente eBPF:

    1. Log sesión en su cuenta New Relic .

    2. Vaya a <DNT>**[left navigation pane &gt; + Integration &amp; Agents &gt; eBPF Agent](https://onenr.io/0oR8XWW36RG)**</DNT>.

    3. En la pantalla <DNT>Select an account</DNT>, seleccione la cuenta en la que desea instalar el agente eBPF y haga clic en <DNT>**Continue**</DNT>.

    4. En la página <DNT>Select an installation method</DNT>, seleccione <DNT>**On a host**</DNT> y haga clic en <DNT>**Continue**</DNT>.

    5. En la pantalla <DNT>Enter your user key</DNT>, seleccione una de las siguientes opciones y luego haga clic en <DNT>**Continue**</DNT>:

       * <DNT>**Use an existing key**</DNT>: Si ya tiene una clave de usuario, proporcione la clave de usuario. Para obtener más información, consulta [Claves de usuario](/docs/apis/intro-apis/new-relic-api-keys/#user-key).
       * <DNT>**Create a new key**</DNT>: Si no tienes una clave de usuario, haz clic en <DNT>**Create a new key**</DNT> para crear una.

    6. En la pantalla <DNT>Configure the host integration</DNT>, ingrese el nombre de despliegue para el host, luego haga clic en <DNT>**Continue**</DNT>.

    7. En la pantalla <DNT>Install the agent</DNT>, ejecute el comando proporcionado en su terminal para instalar el agente eBPF a través del paquete de Debian.

       1. Copie y pegue el comando mostrado para instalar el agente eBPF en su host Debian.

       2. Para verificar la instalación, ejecute el siguiente comando:

          ```bash
          sudo systemctl status newrelic-ebpf-agent
          ```

       Tras una instalación exitosa, el estado de estos servicios se mostrará como activo (en ejecución).

       <Callout variant="tip">
         Puede personalizar la configuración del agente eBPF editando el archivo <DNT>`newrelic-ebpf-agent.yaml`</DNT> disponible en <DNT>`/etc/newrelic-ebpf-agent/newrelic-ebpf-agent.yaml`</DNT>. Para obtener más información sobre el parámetro de configuración, consulte [parámetro de configuración](#config-params).
       </Callout>
  </Step>

  <Step>
    ### Acceda a los datos eBPF en New Relic [#access-data]

    Una vez instalado el agente eBPF, comienza a recopilar datos automáticamente de su host Linux. Puede acceder a estos datos en UI OpenTelemetry de New Relic. Para obtener más información sobre UI OpenTelemetry New Relic, consulte [UI OpenTelemetry APM ](/docs/opentelemetry/get-started/apm-monitoring/opentelemetry-apm-ui).

    **Para ver los datos eBPF en New Relic:**

    1. Vaya a <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; APM &amp; Services**</DNT>.
    2. En el banner de búsqueda, establezca el criterio de búsqueda como <DNT>`instrumentation.name = nr_ebpf`</DNT>:
       <img style={{ align: 'left' }} title="eBPF filters" alt="eBPF filter for eBPF data in New Relic OpenTelemetry UI" src="/images/ebpf_filters.webp" />
  </Step>
</Steps>

## Actualizar el agente eBPF [#upgrade]

Para actualizar el agente eBPF en un host Linux, vuelva a ejecutar el comando de instalación desde la UI New Relic. Este proceso instalará la nueva versión del agente y eliminará automáticamente la anterior.

<Callout variant="important">
  No se admite la actualización a través de un administrador de paquetes. Debes emplear el comando de instalación desde la UI New Relic para una actualización exitosa.
</Callout>

Para obtener el último comando de instalación:

1. Vaya a <DNT>**[one.newrelic.com](https://one.newrelic.com) &gt; + Integration &amp; Agents &gt; eBPF Agent**</DNT>.

2. Sigue el flujo de instalación para obtener el comando actualizado.

3. Ejecute el comando en su host para actualizar el agente.

4. Reinicie el servicio del agente ebpf usando el siguiente comando:

   ```bash
   sudo systemctl restart newrelic-ebpf-agent
   ```

## Parámetro de configuración [#config-params]

El archivo `newrelic-ebpf-agent.yaml` contiene el siguiente parámetro de configuración:

<Callout variant="tip">
  El agente eBPF genera automáticamente nombres de entidades de forma diferente según el entorno:

  * En los hosts o Docker, estos nombres son una combinación del nombre del proceso, su directorio o ID de contenedor y el puerto de escucha. Por ejemplo, `ruby:/home/ubuntu/app:[5678]` o `java:f4aead533895:[8080]`.

  * En Kubernetes, estos nombres se derivan del nombre del servicio, por ejemplo, `mysql-database-service`.

  Asignar un nombre personalizado a la aplicación:

  * Puede asignar un nombre personalizado a su aplicación configurando la variable de entorno `NEW_RELIC_APP_NAME` tanto para Kubernetes como para las aplicaciones en el host.
</Callout>

<CollapserGroup>
  <Collapser id="general-configuration" title="Configuración general">
    Estos parámetros controlan la identidad principal y el destino de los datos del agente eBPF.

    <table>
      <thead>
        <tr>
          <th>
            Nombre del parámetro
          </th>

          <th>
            Descripción
          </th>

          <th>
            Tipo de datos
          </th>

          <th>
            Valor de ejemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `licenseKey`
          </td>

          <td>
            Especifica su clave de licencia de New Relic, que es necesaria para enviar datos.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"22387565c...FFFFNRAL"`
          </td>
        </tr>

        <tr>
          <td>
            `deploymentName`
          </td>

          <td>
            Especifica un nombre único para su implementación para ayudar a identificar sus datos en New Relic.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"my-deployment"`
          </td>
        </tr>

        <tr>
          <td>
            `region`
          </td>

          <td>
            If using a customSecretLicenseKey, you must supply your region 

            `"US"`

            /

            `"EU"`

            . Otherwise, leave this value as an empty string.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `customOtlpEndpoint`
          </td>

          <td>
            Custom OTLP endpoint URL. When set, this takes precedence over the region-based static endpoints.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `customOtlpEndpointTlsEnabled`
          </td>

          <td>
            Whether TLS is enabled on the custom OTLP endpoint. Only applicable when 

            `customOtlpEndpoint`

             is set.
          </td>

          <td>
            Booleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `customOtlpEndpointTlsCertPath`
          </td>

          <td>
            Path to custom CA certificates for OTLP endpoint TLS validation. If empty, system default CA certificates are used.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `logLevel`
          </td>

          <td>
            Establece el nivel de log del agente. Las opciones son 

            `OFF`

            , 

            `FATAL`

            , 

            `ERROR`

            , 

            `WARNING`

            , 

            `INFO`

            , 

            `DEBUG`

             en orden creciente de verbosidad.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"INFO"`
          </td>
        </tr>

        <tr>
          <td>
            `logFilePath`
          </td>

          <td>
            Especifica la ruta del archivo para los logs del agente. Si está vacío o la ruta no es válida, los logs se envían a la salida estándar (stdout).
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"/var/log/newrelic-ebpf-agent"`
          </td>
        </tr>

        <tr>
          <td>
            `reportApmData`
          </td>

          <td>
            Controls APM data reporting. Accepted values: 

            `"true"`

             (always send), 

            `"false"`

             (never send), 

            `"auto"`

             (send only when neither APM nor OTel agent is attached).
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"auto"`
          </td>
        </tr>

        <tr>
          <td>
            `reportNetworkMetrics`
          </td>

          <td>
            Controls network metrics reporting. When enabled, the agent collects and reports network metrics including TCP statistics. Accepted values: 

            `"true"`

             (always send), 

            `"false"`

             (never send), 

            `"auto"`

            .
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"auto"`
          </td>
        </tr>

        <tr>
          <td>
            `includePortInServerEntityIdentification`
          </td>

          <td>
            La configuración le permite incluir o excluir puertos de los nombres de entidades descubiertos automáticamente. Normalmente, los nombres aparecen como 

            `processName:cwd/container_name:[port]`

            , por ejemplo, 

            `python:frosty_merkle:[80]`

            . Para excluir los detalles del puerto, establezca esta opción en 

            `false`

             para nombres como 

            `python:frosty_merkle`

            .
          </td>

          <td>
            Booleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `entityLabels`
          </td>

          <td>
            Custom labels to be added to all entities reported by the eBPF agent. These labels appear as 

            `tags.*`

             attributes in New Relic (e.g., 

            `tags.environment`

            , 

            `tags.datacenter`

            ).
          </td>

          <td>
            Mapa
          </td>

          <td>
            `{datacenter: "us-east-1", environment: "production"}`
          </td>
        </tr>

        <tr>
          <td>
            `downloadedPackagedHeadersPath`
          </td>

          <td>
            Establece la ruta absoluta del directorio completo donde se descargan manualmente los encabezados de Linux necesarios y se colocan para que los emplee el agente eBPF. Esto es útil en entornos restringidos donde el agente no puede descargar los encabezados de Linux necesarios. El agente identifica los encabezados requeridos según la versión del kernel. Usar sólo luego de la recomendación de soporte de NR.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `distroKernelHeadersPath`
          </td>

          <td>
            Establece la ruta absoluta del directorio completo donde están presentes los encabezados de Linux para que los emplee el agente eBPF. Esto es útil cuando no se pudieron instalar los encabezados de Linux necesarios o no se pudo determinar la ruta. Usar sólo luego de la recomendación de soporte de NR.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="all-data-filters" title="Todos los filtros de datos">
    Configure filters to drop all types of data (network metrics and APM data) based on provided configuration.

    <table>
      <thead>
        <tr>
          <th>
            Nombre del parámetro
          </th>

          <th>
            Descripción
          </th>

          <th>
            Tipo de datos
          </th>

          <th>
            Valor de ejemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `allDataFilters.dropServiceNameRegex`
          </td>

          <td>
            Regular expression pattern to match entity names for dropping ALL data (network metrics + APM data). For example: 

            `"java-app|otel-collector|\\bservice1\\b"`

            . Use either dropServiceNameRegex or keepServiceNameRegex to filter service names, not both. If both are provided, keepServiceNameRegex takes precedence.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `allDataFilters.keepServiceNameRegex`
          </td>

          <td>
            Regular expression pattern to match entity names to always keep ALL data for. This bypasses all drop filters. For example: 

            `"critical-service|important-app|\\bmust-monitor\\b"`

            . Use either dropServiceNameRegex or keepServiceNameRegex to filter service names, not both. If both are provided, keepServiceNameRegex takes precedence.
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>

        <tr>
          <td>
            `allDataFilters.dropApmAgentEnabledEntity`
          </td>

          <td>
            Descartar todos los datos de aplicaciones/entidades que tengan agentes APM de NewRelic/OpenTelemetry en ejecución.
          </td>

          <td>
            Booleano
          </td>

          <td>
            `false`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="apm-data-filters" title="Filtros de datos APM">
    Configure filters to drop eBPF APM data based on provided configuration.

    <table>
      <thead>
        <tr>
          <th>
            Nombre del parámetro
          </th>

          <th>
            Descripción
          </th>

          <th>
            Tipo de datos
          </th>

          <th>
            Valor de ejemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `apmDataFilters.dropEntityName`
          </td>

          <td>
            List of entity names for dropping eBPF APM data. For example: 

            `["kube-dns", "otel-collector", "service1"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>

        <tr>
          <td>
            `apmDataFilters.keepEntityName`
          </td>

          <td>
            List of entity names to always keep eBPF APM data. By default all entities are kept/enabled. This bypasses 

            `apmDataFilters.dropEntityName`

             filter. For example: 

            `["critical-service", "important-app"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>

        <tr>
          <td>
            `apmDataFilters.jvmMetricsReporting`
          </td>

          <td>
            Habilite el reporte de métricas de la JVM. Cuando está habilitado, el agente recopila y reporta métricas de JVM para aplicaciones Java.
          </td>

          <td>
            Booleano
          </td>

          <td>
            `true`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="network-metrics-data-filters" title="Filtros de datos de métricas de red">
    Configure filters to drop/keep network metrics data based on provided configuration.

    <table>
      <thead>
        <tr>
          <th>
            Nombre del parámetro
          </th>

          <th>
            Descripción
          </th>

          <th>
            Tipo de datos
          </th>

          <th>
            Valor de ejemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `networkMetricsDataFilter.dropEntityName`
          </td>

          <td>
            List of entity names for dropping network metrics data. For example: 

            `["kube-dns", "otel-collector", "service1"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>

        <tr>
          <td>
            `networkMetricsDataFilter.keepEntityName`
          </td>

          <td>
            List of entity names to always keep network metrics data. By default all entities are kept/enabled. This bypasses 

            `networkMetricsDataFilter.dropEntityName`

             filter. For example: 

            `["critical-service", "important-app"]`

            .
          </td>

          <td>
            Lista
          </td>

          <td>
            `[]`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>

  <Collapser id="protocol-tracing" title="Configuración de seguimiento de protocolo">
    Esta sección le permite habilitar el monitoreo para protocolos de red específicos y configurar cómo se recopilan los datos de traza (spans). Puede habilitar o deshabilitar el monitoreo para protocolos como HTTP, MySQL y otros, y establecer parámetros para la recopilación de intervalos en función de la latencia o la tasa de errores. Se admiten los siguientes protocolos:

    * HTTP
    * Thrift
    * MySQL
    * PostgreSQL
    * MongoDB
    * Apache Cassandra
    * Redis
    * DinamoDB
    * MSSQL
    * kafka
    * AMQP
    * DNS

    <table>
      <thead>
        <tr>
          <th>
            Nombre del parámetro
          </th>

          <th>
            Descripción
          </th>

          <th>
            Tipo de datos
          </th>

          <th>
            Valor de ejemplo
          </th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>
            `protocols.global.max_unlinked_spans`
          </td>

          <td>
            Controls maximum unlinked spans reported per protocol. Set to 0 to disable limit. Unlinked spans are spans that the eBPF agent captures but cannot associate with its parent.
          </td>

          <td>
            Entero
          </td>

          <td>
            `100`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.global.unlinked_spans_error_quota_percentage`
          </td>

          <td>
            Reserves a configurable percentage of the unlinked span quota for error spans, ensuring error visibility even when normal spans arrive first.
          </td>

          <td>
            Entero
          </td>

          <td>
            `30`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.enabled`
          </td>

          <td>
            If 

            `true`

            , enables monitoring for the specified protocol (for example, 

            `http`

            , 

            `mysql`

            , and any others).
          </td>

          <td>
            Booleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.spans.enabled`
          </td>

          <td>
            Si 

            `true`

            , exporta los intervalos de traza para el protocolo habilitado.
          </td>

          <td>
            Booleano
          </td>

          <td>
            `true`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.spans.samplingLatency`
          </td>

          <td>
            Define el umbral de ejemplificación basada en latencia para exportar intervalos. Opciones válidas: 

            `p1`

            , 

            `p10`

            , 

            `p50`

            , 

            `p90`

            , 

            `p99`

            .
          </td>

          <td>
            Cadena
          </td>

          <td>
            `"p50"`
          </td>
        </tr>

        <tr>
          <td>
            `protocols.<protocol-name>.spans.samplingErrorRate`
          </td>

          <td>
            Solo para HTTP y Thrift. Exporta spans de cualquier ruta donde la tasa de errores supere el porcentaje especificado (1-100).
          </td>

          <td>
            Cadena
          </td>

          <td>
            `""`
          </td>
        </tr>
      </tbody>
    </table>
  </Collapser>
</CollapserGroup>

## Desinstalar el agente eBPF [#uninstall]

Para desinstalar el agente eBPF de un host Linux, emplee las herramientas de administración de paquetes de su sistema:

```bash
sudo apt-get purge newrelic-ebpf-agent
```

<Callout variant="tip">
  No es necesario detener el servicio antes de ejecutar el comando de desinstalación.
</Callout>

<DocTiles>
  <DocTile title="Instalación de eBPF Kubernetes" path="/docs/ebpf/k8s-installation/">
    Aprenda a configurar el agente eBPF de New Relic para su clúster de Kubernetes.
  </DocTile>

  <DocTile title="resolución de problemas eBPF" path="/docs/ebpf/troubleshooting/no-ui-data/">
    Aprenda a solucionar problemas con el agente eBPF de New Relic.
  </DocTile>

  <DocTile title="eBPF mejores prácticas" path="/docs/ebpf/best-practices/">
    Conozca las mejores prácticas para emplear el agente eBPF de New Relic.
  </DocTile>
</DocTiles>