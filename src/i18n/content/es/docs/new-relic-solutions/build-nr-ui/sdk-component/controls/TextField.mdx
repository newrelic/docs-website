---
title: Campo de texto
metaDescription: Learn how to work the TextField component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Los formularios permiten al usuario proporcionar información para completar una tarea.

### Uso

```js
import { TextField } from 'nr1'
```

### Ejemplos

#### Básico

```js
<TextField label="Username" placeholder="e.g. John Doe" />
```

#### Con etiqueta en línea

```js
<TextField label="Username" labelInline placeholder="e.g. John Doe" />
```

#### Buscar

```js
<TextField type={TextField.TYPE.SEARCH} placeholder="e.g. Placeholder" />
```

#### Contraseña

```js
<TextField type={TextField.TYPE.PASSWORD} placeholder="e.g. Placeholder" />
```

#### Preformateado

```js
<TextField type={TextField.TYPE.PREFORMATTED} placeholder="npm run build" />
```

#### Cargando

```js
<TextField loading placeholder="e.g Placeholder" />
```

#### Desactivado

```js
<TextField defaultValue="Some value" disabled placeholder="e.g Placeholder" />
```

#### Solo lectura

```js
<TextField defaultValue="Some value" readOnly placeholder="e.g Placeholder" />
```

#### Con descripción

```js
<TextField description="Description value" placeholder="e.g Placeholder" />
```

#### Con mensaje inválido

```js
<TextField
  invalid="Not enough text, needs more"
  placeholder="e.g Placeholder"
/>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>cadena</h5>
      </td>

      <td>
        Proporcione una etiqueta para que los lectores de pantalla describan esta entrada. Esto solo es necesario si la etiqueta del lector de pantalla debe ser diferente a la propiedad `label` o si no proporciona ninguna propiedad `label` .
      </td>
    </tr>

    <tr>
      <td>
        `ariaLabelledBy` <h5>cadena</h5>
      </td>

      <td>
        Proporcione el `id` único de un elemento cuyo contenido describa esta entrada para los lectores de pantalla. Esto solo es necesario si la etiqueta del lector de pantalla debe ser diferente a la propiedad `label` o si no proporciona ninguna propiedad `label` . Emplee esto o `ariaLabel`, no ambos.
      </td>
    </tr>

    <tr>
      <td>
        `autoFocus` <h5>booleano</h5>
      </td>

      <td>
        Si es `true`, el campo de texto se enfocará automáticamente en el montaje.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>cadena</h5>
      </td>

      <td>
        Valor predeterminado del campo de texto.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>cadena</h5>
      </td>

      <td>
        Mensaje con instrucciones sobre cómo completar el campo del formulario.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>booleano</h5>
      </td>

      <td>
        Establece si el campo puede ser editable o no.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>cadena</h5>
      </td>

      <td>
        Se puede mostrar información adicional en una información sobre herramientas junto a la etiqueta.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>boolean|string</h5>
      </td>

      <td>
        Cuando es verdadero, establece el campo en un estado no válido para notificar al usuario que se necesita atención sobre este campo en individuo. Esta propiedad puede ser un campo `boolean` o un `string`. Cuando es un `string`, además de mostrar el estado no válido, el texto se mostrará a continuación.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>cadena</h5>
      </td>

      <td>
        Texto para mostrar como etiqueta.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline` <h5>booleano</h5>
      </td>

      <td>
        Muestre la etiqueta en línea en el control del formulario. Úsela solo cuando el componente no esté dentro de un `Form`. En ese caso, establezca `layoutType` en `Form.LAYOUT_TYPE.SPLIT` en el componente `Form` .
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>booleano</h5>
      </td>

      <td>
        Si carga una lista de datos u otra cosa en un campo de formulario, indique al usuario que algo se está cargando mediante el formulario de estado de carga. Se emplea para informar que el campo puede cargar valores dinámicamente. Cuando se establece en `true`, muestra una rueda de carga; y cuando se establece en `false`, reservación algo de espacio en la UI para que potencialmente se pueda mostrar el control giratorio. No establezca la propiedad (ni la establezca en `undefined`) si no se espera que el campo se cargue en un punto determinado. a tiempo.
      </td>
    </tr>

    <tr>
      <td>
        `multiline` <h5>booleano</h5> <h5>obsoleto</h5>
      </td>

      <td>
        <Callout
          variant="caution"
          title="Vencimiento el 1 de enero de 2022"
        >
          El accesorio "multilínea" está obsoleto; emplee el componente MultilineTextField en su lugar para fines multilínea.
        </Callout>

        Si la entrada acepta una o más líneas. Esto también afecta el comportamiento de la tecla \[Intro], que creará una nueva línea, en lugar de una opción no operativa en un campo de una sola línea.
      </td>
    </tr>

    <tr>
      <td>
        `onBlur` <h5>función</h5>
      </td>

      <td>
        Devolución de llamada que se activa cuando el campo de texto pierde el foco.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>función</h5>
      </td>

      <td>
        Devolución de llamada que se activa cuando cambia el valor del campo de texto.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":"Event source of the callback. You can pull out the new value by accessing `event.target.value`."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onFocus` <h5>función</h5>
      </td>

      <td>
        Devolución de llamada que se activa cuando el campo de texto recibe el foco.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.FocusEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyDown` <h5>función</h5>
      </td>

      <td>
        Devolución de llamada que se activa cuando se presiona una tecla.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `onKeyUp` <h5>función</h5>
      </td>

      <td>
        Devolución de llamada que se activa cuando se suelta una tecla.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.KeyboardEvent","description":"Event source of the callback."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `placeholder` <h5>cadena</h5>
      </td>

      <td>
        Sugerencia que se muestra en el campo de texto cuando no tiene valor.
      </td>
    </tr>

    <tr>
      <td>
        `readOnly` <h5>booleano</h5>
      </td>

      <td>
        Si es `true`, hace que el campo de texto sea de solo lectura.
      </td>
    </tr>

    <tr>
      <td>
        `required` <h5>booleano</h5>
      </td>

      <td>
        Si `true`, indica el campo del formulario según sea necesario.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          TextField.SPACING_TYPE.EXTRA_LARGE, TextField.SPACING_TYPE.LARGE, TextField.SPACING_TYPE.MEDIUM, TextField.SPACING_TYPE.NONE, TextField.SPACING_TYPE.OMIT, TextField.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spellCheck` <h5>booleano</h5>
      </td>

      <td>
        Si es `true`, permite que el browser revise si hay errores ortográficos.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`. **Nota:** Es posible que no vea el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pase un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeración</h5>
      </td>

      <td>
        * Texto: cuando un usuario ingresa una línea de texto en un campo

        * Email: cuando necesita que un usuario ingrese una dirección de email

        * Buscar: cuando un usuario necesita buscar

        * Preformateado: cuando un usuario necesita ingresar contenido de código

        * Contraseña: cuando un usuario ingresa una contraseña

        * URL: cuando un usuario ingresa una URL

          <OptionReference>
            TextField.TIPO.EMAIL, TextField.TIPO.CONTRASEÑA, TextField.TIPO.PREFORMATEADO, TextField.TIPO.BÚSQUEDA, TextField.TIPO.TEXTO, TextField.TIPO.URL,
          </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>cadena</h5>
      </td>

      <td>
        Valor del campo de texto. Esto es obligatorio para un componente controlado.
      </td>
    </tr>
  </tbody>
</table>