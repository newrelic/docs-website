---
title: Grupo de mosaicos
metaDescription: Learn how to work the TileGroup component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

### Uso

```js
import { TileGroup } from 'nr1'
```

### Ejemplos

#### Grupo de mosaicos básico

```js
<TileGroup>
  <Tile onClick={console.log}>Tile 1</Tile>
  <Tile onClick={console.log}>Tile 2</Tile>
  <Tile onClick={console.log}>Tile 3</Tile>
</TileGroup>
```

#### Grupo de mosaicos con tipo de espacio pequeño.

```js
<TileGroup gapType={TileGroup.GAP_TYPE.SMALL}>
  <Tile onClick={console.log}>Tile 1</Tile>
  <Tile onClick={console.log}>Tile 2</Tile>
  <Tile onClick={console.log}>Tile 3</Tile>
</TileGroup>
```

#### Grupo de mosaicos con 3 columnas.

```js
<TileGroup tileWidth="4fr">
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### Grupo de mosaicos con ancho de mosaico personalizado

```js
<TileGroup tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### Azulejos seleccionables

```js
<TileGroup selectionType={TileGroup.SELECTION_TYPE.SINGLE} tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### Mosaicos seleccionables con el mosaico predeterminado marcado

```js
<TileGroup
  defaultValue="tile-0"
  selectionType={TileGroup.SELECTION_TYPE.SINGLE}
  tileWidth={120}
>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile value={`tile-${index}`} onClick={console.log}>
      Tile {index + 1}
    </Tile>
  ))}
</TileGroup>
```

#### Azulejos seleccionables controlados

```js
class MyComponent extends React.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      checkedTile: 'tile-0',
    };
    this._onChange = this._onChange.bind(this);
  }
  _onChange(evt, tileValue) {
    this.setState({ checkedTile: tileValue });
  }
  render() {
    const { checkedTile } = this.state;
    return (
      <TileGroup
        value={checkedTile}
        onChange={this._onChange}
        tileWidth={120}
        selectionType={TileGroup.SELECTION_TYPE.SINGLE}
      >
        {Array.from({ length: 12 }).map((_, index) => (
          <Tile
            sizeType={Tile.SIZE_TYPE.SMALL}
            value={`tile-${index}`}
            key={index}
            onClick={console.log}
          >
            Tile {index + 1}
          </Tile>
        ))}
      </TileGroup>
    );
  }
}
```

#### Mosaicos de selección múltiple

```js
<TileGroup selectionType={TileGroup.SELECTION_TYPE.MULTIPLE} tileWidth={120}>
  {Array.from({ length: 12 }).map((_, index) => (
    <Tile onClick={console.log}>Tile {index + 1}</Tile>
  ))}
</TileGroup>
```

#### Mosaicos de selección múltiple controlados

```js
class MyComponent extends React.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      checkedTiles: [],
    };
    this._onChange = this._onChange.bind(this);
  }
  _onChange(evt, tileValue, checked) {
    this.setState((state) => {
      return {
        checkedTiles: checked
          ? [...state.checkedTiles, tileValue]
          : state.checkedTiles.filter((value) => value !== tileValue),
      };
    });
  }
  render() {
    const { checkedTiles } = this.state;
    return (
      <TileGroup
        value={checkedTiles}
        onChange={this._onChange}
        tileWidth={120}
        selectionType={TileGroup.SELECTION_TYPE.MULTIPLE}
      >
        {Array.from({ length: 12 }).map((_, index) => (
          <Tile
            sizeType={Tile.SIZE_TYPE.SMALL}
            value={`tile-${index}`}
            key={index}
            onClick={console.log}
          >
            Tile {index + 1}
          </Tile>
        ))}
      </TileGroup>
    );
  }
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>Nodo</h5> <h5>REQUERIDO</h5>
      </td>

      <td>
        Azulejos para mostrar.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `defaultValue` <h5>cadena|número|cadena\[]|número\[]</h5>
      </td>

      <td>
        El mosaico inicial que se debe verificar.
      </td>
    </tr>

    <tr>
      <td>
        `gapType` <h5>enumeración</h5>
      </td>

      <td>
        Tamaño del espacio entre baldosas.<OptionReference>TileGroup.GAP_TYPE.MEDIUMTileGroup.GAP_TYPE.SMALL</OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>función</h5>
      </td>

      <td>
        Función que se llama cuando un mosaico está marcado o desmarcado.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":"Event source of the callback."},{"name":"tileValue","type":"any","description":"The value of the tile."},{"name":"checked","type":"boolean","description":"The new checked state."}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selectionType` <h5>enumeración</h5>
      </td>

      <td>
        <OptionReference>
          TileGroup.SELECTION_TYPE.MULTIPLE, TileGroup.SELECTION_TYPE.NONE, TileGroup.SELECTION_TYPE.SINGLE,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          TileGroup.SPACING_TYPE.EXTRA_LARGE, TileGroup.SPACING_TYPE.LARGE, TileGroup.SPACING_TYPE.MEDIUM, TileGroup.SPACING_TYPE.NONE, TileGroup.SPACING_TYPE.OMIT, TileGroup.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.

        **Nota:** Es posible que no veas el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pasa un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `tileWidth` <h5>cadena|número</h5>
      </td>

      <td>
        Representa el ancho de las columnas. Las columnas siempre tienen el mismo ancho. Puede ser de uno de los siguientes tipos:

        * `'YYpx'`: donde `YY` es un número, representa el ancho mínimo de las columnas en pixeles. Cuando los mosaicos no caben en el ancho disponible, se ajustarán en una nueva fila.
        * `'YYfr'`: donde `YY` es un número, representa una fracción del ancho disponible en una cuadrícula de 12 fracciones; Por ejemplo, un grupo de mosaicos con un ancho de mosaico de `'6fr'` siempre representará dos columnas (`12fr/6fr = 2`) donde cada mosaico ocupará el 50% del ancho disponible.
      </td>
    </tr>

    <tr>
      <td>
        `value` <h5>cadena|número|cadena\[]|número\[]</h5>
      </td>

      <td>
        Se comprobarán los mosaicos con el valor coincidente. Puede ser un valor único o una matriz de valores cuando el tipo de selección es `multiple`. Si se define, convierte el componente en un [componente controlado](https://facebook.github.io/react/docs/forms.html).
      </td>
    </tr>
  </tbody>
</table>