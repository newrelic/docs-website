---
title: Enlace
metaDescription: Learn how to work the Link component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Proporciona navegación declarativa y accesible por la aplicación y enlaces externos.

### Uso

```js
import { Link } from 'nr1'
```

### Ejemplos

#### Básico

```js
<Link to="https://www.google.com">Click me</Link>
```

#### Con objeto de ubicación

```js
function render() {
  const launcher = { id: 'nr1-core.explorer' };
  const location = navigation.getOpenLauncherLocation(launcher);


  return <Link to={location}>Click me</Link>;
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>cadena</h5>
      </td>

      <td>
        Úselo para describir mejor el contexto de la acción del componente para el usuario en los lectores de pantalla.

        ```js
        <Link to="/dashboards" ariaLabel="Go to your dashboard">Go</Link> 
        ```
      </td>
    </tr>

    <tr>
      <td>
        `children` <h5>Nodo</h5> <h5>REQUERIDO</h5>
      </td>

      <td>
        Contenido del enlace.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>función</h5>
      </td>

      <td>
        La devolución de llamada se activa cada vez que el usuario hace clic en el enlace.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `role` <h5>cadena</h5>
      </td>

      <td>
        El atributo ARIA se emplea para describir la función de `Link` para la accesibilidad, como `role="menuitem"`. De forma predeterminada, la función de `Link` es "enlace", por lo que una función explícita se emplea dentro de otros elementos accesibles, como elementos `role="menu"` y otros. [Consulte las mejores prácticas de ARIA](https://www.w3.org/TR/wai-aria-practices/) para ver a qué se pueden aplicar los `role` `Link` y en qué contextos.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Enlace.SPACING_TYPE.EXTRA_LARGE, Enlace.SPACING_TYPE.LARGE, Enlace.SPACING_TYPE.MEDIUM, Enlace.SPACING_TYPE.NONE, Enlace.SPACING_TYPE.OMIT, Enlace.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.

        **Nota:** Es posible que no veas el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pasa un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>shape|string</h5>
      </td>

      <td>
        Objeto de ubicación o cadena de URL a la que vincular. <h3>forma</h3>

        `pathname` <h5>cadena</h5><h5>REQUERIDA</h5>

        `search` <h5>cadena</h5>

        `hash` <h5>cadena</h5>
      </td>
    </tr>
  </tbody>
</table>