---
title: ngql
metaDescription: Learn how to use the ngql API
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Una etiqueta literal de plantilla de JavaScript que analiza cadenas de consulta GraphQL en documentos GraphQL que se pueden usar para realizar consultas NerdGraph.

### Uso

```js
import { ngql } from 'nr1'
```

### Ejemplos

#### Ejemplo 1

```js
import { ngql } from 'nr1';


const query = ngql`
  {
    actor {
      user {
        id
        email
        name
      }
    }
  }
`;


function render() {
  return (
    <NerdGraphQuery query={query}>
      {({ data }) => JSON.stringify(data, null, 2)}
    </NerdGraphQuery>
  );
}
```