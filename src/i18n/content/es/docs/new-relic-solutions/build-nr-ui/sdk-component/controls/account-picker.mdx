---
title: AccountPicker
tags:
  - SDK components
  - New Relic apps
  - UI components
  - chart components
  - query and storage components
  - Platform APIs
  - AccountPicker
metaDescription: Learn how to work the AccountPicker component
freshnessValidatedDate: '2024-05-13T00:00:00.000Z'
translationType: machine
---

## Uso [#usage]

```js
import { AccountPicker } from 'nr1'
```

## Ejemplos [#examples]

### Básico [#basic]

```jsx
class Example extends React.Component {
  constructor() {
    super(...arguments);

    this.state = { accountId: null };

    this.onChangeAccount = this.onChangeAccount.bind(this);
  }

  onChangeAccount(_, value) {
    alert(`Selected account: ${value}`);

    this.setState({ accountId: value });
  }

  render() {
    return (
      <AccountPicker
        value={this.state.accountId}
        onChange={this.onChangeAccount}
      />
    );
  }
}
```

### Con etiqueta en línea [#inline-label]

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);

    this.state = { accountId: null };

    this.onChangeAccount = this.onChangeAccount.bind(this);
  }

  onChangeAccount(_, value) {
    alert(`Selected account: ${value}`);

    this.setState({ accountId: value });
  }

  render() {
    return (
      <AccountPicker
        label="Account"
        labelInline
        value={this.state.accountId}
        onChange={this.onChangeAccount}
      />
    );
  }
}
```

## Props [#props]

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel`
      </td>

      <td>
        cadena
      </td>

      <td>
        Proporcione una etiqueta descriptiva para este control, por ejemplo **Accounts**.
      </td>
    </tr>

    <tr>
      <td>
        `className`
      </td>

      <td>
        cadena
      </td>

      <td>
        Agrega nombres de clases al componente. Úselo sólo para fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `crossAccount`
      </td>

      <td>
        booleano
      </td>

      <td>
        Por defecto, su valor es `false`. Muestra una opción en la parte superior llamada **All accounts**. Cuando se selecciona, el valor proporcionado por la devolución de llamada `onChange` es `AccountPicker.CROSS_ACCOUNT`. Esto es útil cuando tiene una funcionalidad que puede operar en varias cuentas.
      </td>
    </tr>

    <tr>
      <td>
        `disabled`
      </td>

      <td>
        booleano
      </td>

      <td>
        Si es `true`, el menú desplegable no está disponible para la interacción.
      </td>
    </tr>

    <tr>
      <td>
        `label`
      </td>

      <td>
        cadena
      </td>

      <td>
        Texto para mostrar como etiqueta.
      </td>
    </tr>

    <tr>
      <td>
        `labelInline`
      </td>

      <td>
        booleano
      </td>

      <td>
        Muestre la etiqueta en línea en el control del formulario. Úselo solo cuando el componente no esté dentro de un `Form`. En ese caso, establezca `layoutType` en `Form.LAYOUT_TYPE.SPLIT` en el componente `Form` .
      </td>
    </tr>

    <tr>
      <td>
        `onChange`
      </td>

      <td>
        función
      </td>

      <td>
        La devolución de llamada se activa cada vez que el usuario hace clic en una cuenta de la lista.

        ```
        function (
            event: React.MouseEvent, 
            value: number 
            Id of the account selected.

        )
        ```
      </td>
    </tr>

    <tr>
      <td>
        `spacingType`
      </td>

      <td>
        enum\[]
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        ```js
        <Array of
            <One of
                AccountPicker.SPACING_TYPE.EXTRA_LARGE,
                AccountPicker.SPACING_TYPE.LARGE,
                AccountPicker.SPACING_TYPE.MEDIUM,
                AccountPicker.SPACING_TYPE.NONE,
                AccountPicker.SPACING_TYPE.OMIT,
                AccountPicker.SPACING_TYPE.SMALL,
            >
        >
        ```
      </td>
    </tr>

    <tr>
      <td>
        `style`
      </td>

      <td>
        objeto
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe emplear únicamente con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId`
      </td>

      <td>
        cadena
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E.

        Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.

        **Nota**: Es posible que no vea el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pase un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `value`
      </td>

      <td>
        number|enum
      </td>

      <td>
        ID de la cuenta seleccionada.
      </td>
    </tr>
  </tbody>
</table>