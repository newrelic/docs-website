---
title: Elemento desplegable
metaDescription: Learn how to work the DropdownItem component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Elemento secundario del componente `<Dropdown>` .

Admite evento de clic a través del accesorio `onClick` y la posibilidad de redirigir a otras páginas a través del accesorio `to` .

Admite subelementos a través de la propiedad `items` y pasando una función de representación a la propiedad `children` .

### Uso

```js
import { DropdownItem } from 'nr1'
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` Nodo <h5>REQUERIDO</h5> <h5>| (nodo|función)\[]</h5>
      </td>

      <td>
        Contenido que se mostrará dentro del elemento. También acepta un método de renderizado como último hijo al renderizar listas anidadas. Requiere pasar una matriz a la propiedad `items` .

        ```js
        import { Dropdown, DropdownItem } from 'nr1';

        const nestedItems = [
          { id: '3.1', text: 'Item 3.1' },
          { id: '3.2', text: 'Item 3.2' },
          { id: '3.3', text: 'Item 3.3' },
          { id: '3.4', text: 'Item 3.4' },
        ];

        const nestedDropdown = (
          <Dropdown title="Nested Dropdown">
            <DropdownItem>Item 1</DropdownItem>
            <DropdownItem>Item 2</DropdownItem>
            <DropdownItem items={nestedItems}>
              Item 3
              {({ item }) => <DropdownItem key={item.id}>{item.text}</DropdownItem>}
            </DropdownItem>
            <DropdownItem>Item 4</DropdownItem>
          </Dropdown>
        );
        ```
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>booleano</h5>
      </td>

      <td>
        Si es `true`, el elemento no está disponible para la interacción y su devolución de llamada onClick no se activará.
      </td>
    </tr>

    <tr>
      <td>
        `items` <h5>matriz</h5>
      </td>

      <td>
        Elementos para representar una sublista del elemento actual. Este accesorio también requiere pasar una función como último `children` luego del contenido del elemento para funcionar.
      </td>
    </tr>

    <tr>
      <td>
        `onClick` <h5>función</h5>
      </td>

      <td>
        La devolución de llamada se activa cada vez que el usuario hace clic en el elemento.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `selected` <h5>booleano</h5>
      </td>

      <td>
        Muestra la opción seleccionada.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, anteponga su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.**Nota:** Es posible que no vea el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pase un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `to` <h5>shape|string</h5>
      </td>

      <td>
        Objeto de ubicación o cadena de URL a la que vincular.

        <h3>forma</h3>

        `pathname` <h5>cadena</h5><h5>REQUERIDA</h5>

        `search` <h5>cadena</h5>

        `hash` <h5>cadena</h5>
      </td>
    </tr>
  </tbody>
</table>