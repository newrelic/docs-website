---
title: Texto en bloque
metaDescription: Learn how to work the BlockText component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Los estilos de tipografía se emplean en toda la UI. Si se usan de manera constante, lo ayudarán a mantener una jerarquía clara para el usuario.

### Uso

```js
import { BlockText } from 'nr1'
```

### Ejemplos

#### Básico

```js
<>
  <BlockText>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
</>
```

#### Tipos

```js
<>
  <BlockText type={BlockText.TYPE.NORMAL}>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
  <BlockText type={BlockText.TYPE.PARAGRAPH}>
    Typography is at the core of <strong>great</strong> user interface design.
  </BlockText>
</>
```

#### Cambiando semántica

```js
<BlockText type={BlockText.TYPE.PARAGRAPH} tagType={BlockText.TYPE.DIV}>
  This is a div element styled as a paragraph
</BlockText>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` <h5>nodo</h5>
      </td>

      <td>
        Texto a mostrar.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          BlockText.SPACING_TYPE.EXTRA_LARGE, BlockText.SPACING_TYPE.LARGE, BlockText.SPACING_TYPE.MEDIUM, BlockText.SPACING_TYPE.NONE, BlockText.SPACING_TYPE.OMIT, BlockText.SPACING_TYPE.SMALL,
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `tagType` <h5>enumeración</h5>
      </td>

      <td>
        Etiqueta en la que envolver el texto. Útil para cambiar el significado semántico del texto. Esto no aplica ningún estilo.

        <OptionReference>
          BlockText.etiqueta.DIV, BlockText.etiqueta.P
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.

        **Nota:** Es posible que no veas el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pasa un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeración</h5>
      </td>

      <td>
        Tipo de texto que desea mostrar. Esto aplica estilos según el tipo.

        <OptionReference>
          BlockText.TIPO.NORMAL, BlockText.TIPO.PARAGRAPH
        </OptionReference>
      </td>
    </tr>
  </tbody>
</table>