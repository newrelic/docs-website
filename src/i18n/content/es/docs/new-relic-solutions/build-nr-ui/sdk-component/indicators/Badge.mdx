---
title: Insignia
metaDescription: Learn how to work the Badge component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Las insignias indican el estado de un objeto.

### Uso

```js
import { Badge } from 'nr1'
```

### Ejemplos

#### Básico

```js
<div className="nr1-Docs-prettify">
  <Badge>Normal</Badge>
</div>
```

#### Tipos

```js
<div className="nr1-Docs-prettify">
  <Badge>Normal</Badge>
  <Badge type={Badge.TYPE.INFO}>New</Badge>
  <Badge type={Badge.TYPE.SUCCESS}>Success</Badge>
  <Badge type={Badge.TYPE.WARNING}>Warning</Badge>
  <Badge type={Badge.TYPE.CRITICAL}>Critical</Badge>
</div>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `children` Cadena <h5>REQUERIDA</h5><h5>|número</h5>
      </td>

      <td>
        Cadena para renderizar dentro de la insignia. Se aplicarán puntos suspensivos si el contenido tiene más de 24 caracteres.
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Insignia.SPACING_TYPE.EXTRA_LARGE, Insignia.SPACING_TYPE.LARGE, Insignia.SPACING_TYPE.MEDIUM, Insignia.SPACING_TYPE.NONE, Insignia.SPACING_TYPE.OMIT, Insignia.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`. **Nota:** Es posible que no vea el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pase un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeración</h5>
      </td>

      <td>
        El tipo puede ser:

        * Normal: indica estado general o en progreso, p. 'Próximamente' o 'Menor'.

        * Información: indica un estado nuevo, creado o de ayuda, por ejemplo. 'Nuevo', 'Beta' o 'Información'.

        * Éxito: indica un estado exitoso.

        * Advertencia: indica elementos que requieren un estado de aviso.

        * Crítico: indica elementos problemáticos.

          <OptionReference>
            Insignia.TIPO.CRÍTICO, Insignia.TIPO.INFOBadge.TIPO.NORMAL, Insignia.TIPO.ÉXITO, Insignia.TIPO.ADVERTENCIA,
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>