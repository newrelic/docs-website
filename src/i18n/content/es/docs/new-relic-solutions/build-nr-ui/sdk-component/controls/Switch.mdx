---
title: Cambiar
metaDescription: Learn how to work the Switch component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

Los interruptores se emplean como alternancia en los filtros de contenido o para proporcionar una forma para que un usuario seleccione una preferencia binaria.

### Uso

```js
import { Switch } from 'nr1'
```

### Ejemplos

#### Básico

```js
<div className="nr1-Docs-prettify">
  <Switch onChange={(e) => alert(`Toggle to: ${e.target.checked}`)} />
  <Switch
    defaultChecked
    onChange={(e) => alert(`Toggle to: ${e.target.checked}`)}
  />
</div>
```

#### Desactivado

```js
<div className="nr1-Docs-prettify">
  <Switch disabled />
  <Switch defaultChecked disabled />
</div>
```

#### Estado de carga

```js
<div className="nr1-Docs-prettify">
  <Switch loading />
  <Switch defaultChecked loading />
</div>
```

#### Con etiqueta

```js
<div className="nr1-Docs-prettify">
  <Switch label="This is a label" />
  <Switch disabled label="This is a label" />
</div>
```

#### Con etiqueta e información.

```js
<Switch label="This is a label" info="Info value" />
```

#### Con descripción

```js
<Switch label="This is a label" description="Description value" />
```

#### Con mensaje inválido

```js
<Switch label="This is a label" invalid="Invalid message value" />
```

#### Componente controlado configurado para cargar mientras se realiza una solicitud

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(resolve, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

#### Componente controlado configurado para cargar mientras se realiza una solicitud que vuelve a estar sin marcar si la solicitud falla

```js
class Example extends React.Component {
  constructor() {
    super(...arguments);


    this.state = {
      checked: false,
      loading: false,
    };


    this._onChange = this._onChange.bind(this);
  }


  _requestMock() {
    return new Promise((resolve, reject) => {
      setTimeout(reject, 1000);
    });
  }


  _onChange() {
    let initialChecked;


    this.setState(({ checked }) => {
      initialChecked = checked;


      return {
        checked: !checked,
        loading: true,
      };
    });


    this._requestMock()
      .then(() => {
        // do some stuff
      })
      .catch(() => {
        this.setState({
          checked: initialChecked,
        });
      })
      .finally(() => {
        this.setState({
          loading: false,
        });
      });
  }


  render() {
    const { checked, loading } = this.state;


    return (
      <Switch
        checked={checked}
        label="This is a label"
        loading={loading}
        onChange={this._onChange}
      />
    );
  }
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `ariaLabel` <h5>cadena</h5>
      </td>

      <td>
        Proporcione una etiqueta descriptiva para este control, por ejemplo "Cambiar vista".
      </td>
    </tr>

    <tr>
      <td>
        `checked` <h5>booleano</h5>
      </td>

      <td>
        Determina el estado marcado del interruptor, donde `true` corresponde al interruptor que parece estar "encendido" y un valor `false` al interruptor que parece estar "apagado". Cuando se define `checked` , el componente Switch se comportará como un [componente controlado](https://reactjs.org/docs/forms.html#controlled-components).
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clases al componente. Debe emplear únicamente con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `defaultChecked` <h5>booleano</h5>
      </td>

      <td>
        Establece el valor marcado predeterminado para el interruptor cuando funciona como un [componente no controlado](https://reactjs.org/docs/uncontrolled-components.html). es decir. Cuando la propiedad `checked` aún no esté estipulada.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>cadena</h5>
      </td>

      <td>
        Mensaje con instrucciones sobre cómo completar el campo del formulario.
      </td>
    </tr>

    <tr>
      <td>
        `disabled` <h5>booleano</h5>
      </td>

      <td>
        Determina si el usuario puede interactuar con el conmutador o si debe mostrar en un estado deshabilitado.
      </td>
    </tr>

    <tr>
      <td>
        `info` <h5>cadena</h5>
      </td>

      <td>
        Se puede mostrar información adicional en una información sobre herramientas junto a la etiqueta.
      </td>
    </tr>

    <tr>
      <td>
        `invalid` <h5>boolean|string</h5>
      </td>

      <td>
        Cuando es verdadero, establece el campo en un estado no válido para notificar al usuario que se necesita atención sobre este campo en individuo. Esta propiedad puede ser un campo `boolean` o un `string`. Cuando es un `string`, además de mostrar el estado no válido, el texto se mostrará a continuación.
      </td>
    </tr>

    <tr>
      <td>
        `label` <h5>cadena</h5>
      </td>

      <td>
        Asocie una etiqueta de texto para proporcionar al usuario un breve título o explicación de la configuración controlada por el interruptor. La etiqueta se ubicará en el lado izquierdo del interruptor.
      </td>
    </tr>

    <tr>
      <td>
        `loading` <h5>booleano</h5>
      </td>

      <td>
        Para indicar si una acción está en progreso, especialmente en el caso de que tarde más de 1 segundo en completar, debe mostrar el estado de carga. El interruptor no es interactivo durante la carga. Si la acción asociada al estado de carga falla, la propiedad marcada debe restablecer a su valor anterior.
      </td>
    </tr>

    <tr>
      <td>
        `onChange` <h5>función</h5>
      </td>

      <td>
        La devolución de llamada se activa cada vez que el usuario activa el interruptor. El estado marcado actual se puede evaluar en la devolución de llamada a través de `event.target.checked`.

        <FunctionDefinition
          returnValue={[]}
          arguments={[{"name":"event","type":"React.ChangeEvent","description":""}]}
        />
      </td>
    </tr>

    <tr>
      <td>
        `spacingType` <h5>enum\[]</h5>
      </td>

      <td>
        Propiedad de espaciado. El espaciado se define como una tupla de cero a cuatro valores, que siguen las mismas convenciones que las propiedades CSS como `margin` o `padding`. Para omitir un valor, use `SPACING_TYPE.OMIT`.

        <OptionReference array>
          Switch.SPACING_TYPE.EXTRA_LARGE, Switch.SPACING_TYPE.LARGE, Switch.SPACING_TYPE.MEDIUM, Switch.SPACING_TYPE.NONE, Switch.SPACING_TYPE.OMIT, Switch.SPACING_TYPE.SMALL
        </OptionReference>
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe emplear únicamente con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`. **Nota:** Es posible que no vea el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pase un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>
  </tbody>
</table>