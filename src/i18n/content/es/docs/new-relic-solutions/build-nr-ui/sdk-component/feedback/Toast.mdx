---
title: Tostada
metaDescription: Learn how to work the Toast component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

La notificación de brindis brinda al usuario una confirmación inmediata de la finalización exitosa de una tarea o de un error crítico al intentar completar una tarea.

Puede personalizar el brindis pasando acciones personalizadas que se mostrarán como botones en los que se puede hacer clic junto al texto.

### Uso

```js
import { Toast } from 'nr1'
```

### Ejemplos

#### Notificación básica del brindis

```js
function render() {
  const onClick = () =>
    Toast.showToast({
      title: 'Feedback received',
      description: 'We will contact you soon.',
      actions: [
        {
          label: 'Go back',
          onClick: () => console.log('Go back'),
        },
      ],
      type: Toast.TYPE.NORMAL,
    });


  return <Button onClick={onClick}>Show toast</Button>;
}
```

#### Con enlace de información adicional

```js
function render() {
  const onClick = () =>
    Toast.showToast({
      title: 'TV Mode activated',
      description: 'Press ESC to exit',
      additionalInfoLink: {
        label: 'Learn more about TV Mode',
        to: 'https://discuss.newrelic.com/t/tv-mode-in-new-relic-one/92963',
      },
      type: Toast.TYPE.NORMAL,
    });


  return <Button onClick={onClick}>Show toast</Button>;
}
```

#### Notificación de brindis crítico

```js
function render() {
  const onClick = () =>
    Toast.showToast({
      title: 'Service unavailable',
      description: 'Your data could not be retrieved',
      actions: [
        {
          label: 'Retry',
          onClick: () => console.log('Retry'),
        },
      ],
      type: Toast.TYPE.CRITICAL,
    });


  return <Button onClick={onClick}>Update</Button>;
}
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `actions` <h5>forma\[]</h5>
      </td>

      <td>
        Si tiene una acción específica que necesita que el usuario elija, emplee el componente de acción personalizada. Recomendamos limitar el uso de múltiples acciones, ya que esta notificación está destinada a indicarle al usuario el éxito o el fracaso de una tarea y no debe usar para guiar al usuario a otra tarea.

        <h3>forma</h3>

        `label` <h5>cadena</h5><h5>REQUERIDA</h5>

        `onClick` <h5>Función</h5><h5>REQUERIDA</h5>
      </td>
    </tr>

    <tr>
      <td>
        `additionalInfoLink` <h5>forma</h5>
      </td>

      <td>
        La información mostrada en el título y descripción se puede complementar/ampliar con un enlace a la documentación. Este enlace solo debe usar para proporcionar información contextual y no como una Acción.

        <h3>forma</h3>

        `label` <h5>cadena</h5><h5>REQUERIDA</h5>

        <SideBySide>
          <Side>
            `onClick` <h5>función</h5>
          </Side>

          <Side>
            La devolución de llamada se activa cada vez que el usuario hace clic en el botón.

            <FunctionDefinition
              returnValue={[]}
              arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
            />
          </Side>
        </SideBySide>

        <SideBySide>
          <Side>
            `to` <h5>shape|string</h5>
          </Side>

          <Side>
            Objeto de ubicación o cadena de URL a la que vincular. <h4>forma</h4> `pathname` <h5>cadena</h5><h5>REQUERIDA</h5>

            `search` <h5>cadena</h5>

            `hash` <h5>cadena</h5>
          </Side>
        </SideBySide>
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>cadena</h5>
      </td>

      <td>
        Recomendamos proporcionar un título y un mensaje para dar contexto sobre el éxito o el fracaso de la acción anterior realizada. Incluso puede emplear el área de mensajes para proporcionarle al usuario un enlace para acceder al objeto que acaba de crear en el sistema.
      </td>
    </tr>

    <tr>
      <td>
        `onHideEnd` <h5>función</h5>
      </td>

      <td>
        La devolución de llamada se ejecuta después de que se oculta el brindis.
      </td>
    </tr>

    <tr>
      <td>
        `onHideStart` <h5>función</h5>
      </td>

      <td>
        La devolución de llamada se ejecuta cuando comienza la animación de ocultar.
      </td>
    </tr>

    <tr>
      <td>
        `sticky` <h5>booleano</h5>
      </td>

      <td>
        Para tareas que no requieren el descarte explícito o la visualización por parte de un usuario, recomendamos emplear una notificación de tiempo de espera para que el mensaje desaparezca automáticamente de la pantalla luego de un corto periodo de tiempo.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.

        **Nota:** Es posible que no veas el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pasa un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `title` <h5>cadena</h5><h5>REQUERIDA</h5>
      </td>

      <td>
        No recomendamos usar la opción de solo título, ya que NO proporciona un contexto para el usuario sobre la tarea que acaba de completar.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeración</h5>
      </td>

      <td>
        * Normal: cuando el sistema completa con éxito la tarea del usuario.

        * Crítico: cuando el sistema no logra completar la tarea del usuario

          <OptionReference>
            Tostada.TIPO.CRÍTICO, Tostada.TIPO.NORMAL,
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>

### Métodos

### `Toast.showToast`

<FunctionDefinition
  arguments={[{"description":"Object containing the Toast options. Any `Toast` prop is valid as an option.","name":"props","type":"Object"}]}
  returnValue={{"description":null,"type":"undefined"}}
/>