---
title: SecciónMensaje
metaDescription: Learn how to work the SectionMessage component
freshnessValidatedDate: '2024-06-03T00:00:00.000Z'
translationType: machine
---

## SecciónMensaje

Emplee un Mensaje de Sección para informar del estado de una sección o un grupo de elementos.

El usuario no puede descartar los mensajes de la sección. Elimínelos una vez resuelta la situación o el usuario tomó la acción adecuada.

### Uso

```js
import { SectionMessage } from 'nr1'
```

### Ejemplos

#### Tipos

```js
<Stack
  directionType={Stack.DIRECTION_TYPE.VERTICAL}
  horizontalType={Stack.HORIZONTAL_TYPE.FILL}
>
  <StackItem>
    <SectionMessage
      description="Your Metric query has been transformed into a compatible query with the previous Infrastructure metric format."
      actions={[{ label: 'See our docs', to: 'https://support.newrelic.com/' }]}
    />
  </StackItem>
  <StackItem>
    <SectionMessage
      type={SectionMessage.TYPE.SUCCESS}
      title="File has been uploaded"
      description="We are unable to save any progress at this time."
      actions={[
        { label: 'Primary action', onClick: console.log },
        { label: 'Secondary action', onClick: console.log },
      ]}
    />
  </StackItem>
  <StackItem>
    <SectionMessage
      type={SectionMessage.TYPE.WARNING}
      title="Cannot connect to the database"
      description="We are unable to save any progress at this time."
      actions={[
        { label: 'Primary action', onClick: console.log },
        { label: 'Secondary action', onClick: console.log },
      ]}
    />
  </StackItem>
  <StackItem>
    <SectionMessage
      type={SectionMessage.TYPE.CRITICAL}
      title="Error on the request"
      description="We tried to transform your query into a query that is compatible with both new Metric and previous Infrastructure metric formats, but the transformation process failed."
      actions={[
        { label: 'Contact support', to: 'https://support.newrelic.com/' },
      ]}
    />
  </StackItem>
</Stack>
```

### Props

<table>
  <tbody>
    <tr>
      <td>
        `actions` <h5>forma\[]</h5>
      </td>

      <td>
        Puede usar hasta dos acciones para ayudar al usuario a navegar a otra vista, activar un evento de fondo o cambiar el comportamiento del nerdlet/sección.

        <h3>forma</h3>

        `ariaLabel` <h5>cadena</h5>

        `label` <h5>cadena</h5><h5>REQUERIDA</h5>

        <SideBySide>
          <Side>
            `onClick` <h5>función</h5>
          </Side>

          <Side>
            La devolución de llamada se activa cada vez que el usuario hace clic en el botón.

            <FunctionDefinition
              returnValue={[]}
              arguments={[{"name":"event","type":"React.MouseEvent","description":""}]}
            />
          </Side>
        </SideBySide>

        <SideBySide>
          <Side>
            `to` <h5>shape|string</h5>
          </Side>

          <Side>
            Objeto de ubicación o cadena de URL a la que vincular.

            <h3>forma</h3>

            `pathname` <h5>cadena</h5><h5>REQUERIDA</h5>

            `search` <h5>cadena</h5>

            `hash` <h5>cadena</h5>
          </Side>
        </SideBySide>
      </td>
    </tr>

    <tr>
      <td>
        `className` <h5>cadena</h5>
      </td>

      <td>
        Agrega nombres de clase al componente. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `description` <h5>cadena</h5>
      </td>

      <td>
        Úselo para proporcionar más detalles y explicar, si corresponde, los pasos que debe seguir el usuario.

        Nota: Los accesorios `title` y `description` son opcionales, pero se debe proporcionar al menos uno de ellos.
      </td>
    </tr>

    <tr>
      <td>
        `style` <h5>objeto</h5>
      </td>

      <td>
        Estilo en línea para un estilo personalizado. Debe usar solo con fines de posicionamiento y espaciado.
      </td>
    </tr>

    <tr>
      <td>
        `testId` <h5>cadena</h5>
      </td>

      <td>
        Agrega un atributo `data-test-id` . Úselo para apuntar al componente en pruebas unitarias y E2E. Para que una identificación de prueba sea válida, antepóngale su identificación de nerdpack, seguido de un punto. Por ejemplo, `my-nerdpack.some-element`.

        **Nota:** Es posible que no veas el atributo `data-test-id` ya que se eliminan del DOM. Para depurarlos, pasa un parámetro de consulta `e2e-test` a la URL.
      </td>
    </tr>

    <tr>
      <td>
        `title` <h5>cadena</h5>
      </td>

      <td>
        Úselo para proporcionar el mensaje principal. Nota: Los accesorios `title` y `description` son opcionales, pero se debe proporcionar al menos uno de ellos.
      </td>
    </tr>

    <tr>
      <td>
        `type` <h5>enumeración</h5>
      </td>

      <td>
        El tipo puede ser:

        * Información (predeterminado): úselo para indicar que algo es nuevo, que se acaba de crear un elemento o para brindar ayuda.

        * Éxito: úselo para comunicar cambios o condiciones positivas.

        * Advertencia: empléela para comunicar un posible problema, una situación no deseada, un consejo de precaución o un aviso anticipado de algún tipo.

        * Crítico: úselo para comunicar un problema.

          <OptionReference>
            SecciónMensaje.TIPO.CRÍTICO, SecciónMensaje.TIPO.INFO, SecciónMensaje.TIPO.ÉXITO, SecciónMensaje.TIPO.ADVERTENCIA,
          </OptionReference>
      </td>
    </tr>
  </tbody>
</table>