---
title: Obtener definiciones de flujo de trabajo
tags:
  - workflow automation
  - workflow
  - Get Workflow Definitions
  - workflow automation API
metaDescription: Used to get and return a list of workflow definitions under the user account’s scope.
freshnessValidatedDate: never
translationType: machine
---

Esta API obtiene y devuelve una lista de todos los esquemas de definición de flujo de trabajo dentro del alcance dado.

<Callout variant="important">
  La definición del flujo de trabajo YAML no está disponible con esta consulta. Un YAML de definición de flujo de trabajo solo se devuelve usando una consulta `GetWorkflowDefinition` `/{actor/account(:id)/workflowAutomation/workflow(:name, :version)}`.
</Callout>

## parámetro de entrada

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        Parámetro
      </th>

      <th>
        Tipo
      </th>

      <th>
        Requerido
      </th>

      <th>
        Descripción
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `account.id`
      </td>

      <td>
        Número
      </td>

      <td>
        Sí
      </td>

      <td>
        Su ID de cuenta.
      </td>
    </tr>

    <tr>
      <td>
        `workflows.cursor`
      </td>

      <td>
        Cadena
      </td>

      <td>
        No
      </td>

      <td>
        Cursor para la paginación.
      </td>
    </tr>
  </tbody>
</table>

## Solicitud de muestra

```graphql
{
  actor {
    account(id: 11933347) {
      workflowAutomation {
        workflows(cursor: "xyz123") {
          results {
            definition {
              name
              scope {
                id
                type
              }
              version
            }
          }
        }
      }
    }
  }
}
```

## Ejemplo de respuesta

La consulta devuelve:

* `workflows.results`: Una matriz de contornos de definición de flujo de trabajo, cada uno de los cuales contiene:

  * `definition.name`: El nombre de la definición del flujo de trabajo
  * `definition.scope.id`: El ID de la cuenta
  * `definition.scope.type`: El tipo de alcance
  * `definition.version`: El número de versión de la definición del flujo de trabajo

## Temas relacionados [#related-topics]

<DocTiles>
  <DocTile title="Obtener la definición del flujo de trabajo" path="/docs/workflow-automation/workflow-automation-apis/get-workflow-definition">
    Obtener el YAML completo para un flujo de trabajo específico
  </DocTile>

  <DocTile title="Crear definición de flujo de trabajo" path="/docs/workflow-automation/workflow-automation-apis/create-workflow-definition">
    Agregar un nuevo flujo de trabajo a su cuenta
  </DocTile>

  <DocTile title="Eliminar definición de flujo de trabajo" path="/docs/workflow-automation/workflow-automation-apis/delete-workflow-definition">
    Eliminar flujos de trabajo de su cuenta
  </DocTile>
</DocTiles>