---
title: Iniciar ejecución del flujo de trabajo
tags:
  - workflow automation
  - workflow
  - StartWorkflowRun
  - workflow automation API
metaDescription: Start a workflow run for a specified workflow definition.
freshnessValidatedDate: never
translationType: machine
---

Esta API inicia la ejecución de un flujo de trabajo para una definición de flujo de trabajo específica.

<Callout variant="important">
  El número de ejecuciones simultáneas del flujo de trabajo y el número de pasos ejecutados en una ejecución determinada se definen en [Límites del sistema de flujo de trabajo](/docs/workflow-automation/limitations-and-faq/workflow-limits).
</Callout>

## parámetro de entrada

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        Parámetro
      </th>

      <th>
        Tipo
      </th>

      <th>
        Requerido
      </th>

      <th>
        Descripción
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `scope.id`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Sí
      </td>

      <td>
        Su ID de cuenta.
      </td>
    </tr>

    <tr>
      <td>
        `scope.type`
      </td>

      <td>
        Enumeración
      </td>

      <td>
        Sí
      </td>

      <td>
        El tipo de alcance (CUENTA).
      </td>
    </tr>

    <tr>
      <td>
        `definition.name`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Sí
      </td>

      <td>
        El nombre de la definición del flujo de trabajo a ejecutar.
      </td>
    </tr>

    <tr>
      <td>
        `workflowInputs`
      </td>

      <td>
        Matriz
      </td>

      <td>
        No
      </td>

      <td>
        Pares clave-valor opcionales para pasar como entradas al flujo de trabajo.
      </td>
    </tr>

    <tr>
      <td>
        `options.logLevel`
      </td>

      <td>
        Enumeración
      </td>

      <td>
        No
      </td>

      <td>
        Controla el logging durante la ejecución. Valores: 

        `NONE`

         (predeterminado), 

        `INFO`

        , 

        `DEBUG`

        .
      </td>
    </tr>
  </tbody>
</table>

### opción logLevel

La opción `logLevel` controla la cantidad de información de logging generada durante la ejecución del flujo de trabajo.

**Valores disponibles:**

* `NONE` (predeterminado): no se generarán salidas de log.
* `INFO`:Emite información de nivel de paso.
* `DEBUG`:Agrega atributo a los logs de inicio del paso con todas las entradas del paso de acción.

## Solicitud de muestra

Emplee el [generador de consultas](https://one.newrelic.com/nerdgraph-graphiql?state=b8f3b3a9-8d3e-c03e-5da4-34b98a57ff8f) para crear y editar un flujo de trabajo. Consulte [el explorador de la API de NerdGraph](/docs/apis/nerdgraph/get-started/nerdgraph-explorer).

```graphql
mutation {
  workflowAutomationStartWorkflowRun(
    definition: {name: "json-parsing"}
    workflowInputs: [
      {key: "checkOperational", value: "false"}
      {key: "checkNonOperational", value: "true"}]
    scope: {id: "11933347"}
    idempotencyKey: "45e65924-5316-479c-a977-f6adfa17e07a"
    options: {logLevel: INFO}
  ) {
    runId
  }
}
```

## Ejemplo de respuesta

La mutación devuelve:

* `runId`: El identificador único para la ejecución del flujo de trabajo. Use este ID con las API `WorkflowRunInfo`, `SignalWorkflowRun` o `StopWorkflowRun`.