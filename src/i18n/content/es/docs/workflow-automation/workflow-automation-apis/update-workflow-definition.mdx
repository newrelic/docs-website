---
title: Actualizar definición de flujo de trabajo
tags:
  - workflow automation
  - workflow
  - Update Workflow Definition
  - workflow automation API
metaDescription: Used to update an existing workflow definition.
freshnessValidatedDate: never
translationType: machine
---

Esta API actualiza una definición de flujo de trabajo existente. Devuelve un error si la definición del flujo de trabajo aún no existe.

## parámetro de entrada

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        Parámetro
      </th>

      <th>
        Tipo
      </th>

      <th>
        Requerido
      </th>

      <th>
        Descripción
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `definition.yaml`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Sí
      </td>

      <td>
        El YAML de definición de flujo de trabajo codificado en base64.
      </td>
    </tr>

    <tr>
      <td>
        `scope.id`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Sí
      </td>

      <td>
        Su ID de cuenta.
      </td>
    </tr>

    <tr>
      <td>
        `scope.type`
      </td>

      <td>
        Enumeración
      </td>

      <td>
        Sí
      </td>

      <td>
        El tipo de alcance (CUENTA).
      </td>
    </tr>
  </tbody>
</table>

## Solicitud de muestra

```graphql
  mutation {
    workflowAutomationUpdateWorkflowDefinition(
      definition: {yaml: "<workflow definition YAML>"}
      scope: {id: "11608789", type: ACCOUNT}
    ) {
      definition {
        name
        version
        yaml
      }
    }
  }
```

## Ejemplo de respuesta

La mutación devuelve:

* `definition.name`: El nombre de la definición del flujo de trabajo actualizado
* `definition.version`: El nuevo número de versión de la definición del flujo de trabajo
* `definition.yaml`: La definición del flujo de trabajo YAML codificada en base64

## Temas relacionados [#related-topics]

<DocTiles>
  <DocTile title="Validar la definición del flujo de trabajo" path="/docs/workflow-automation/workflow-automation-apis/validate-workflow-definition">
    Prueba la sintaxis YAML de tu flujo de trabajo antes de actualizar
  </DocTile>

  <DocTile title="Obtener la definición del flujo de trabajo" path="/docs/workflow-automation/workflow-automation-apis/get-workflow-definition">
    Vea la definición actual del flujo de trabajo antes de actualizar
  </DocTile>

  <DocTile title="Iniciar ejecución de flujo de trabajo" path="/docs/workflow-automation/workflow-automation-apis/start-workflow-run">
    Ejecute su flujo de trabajo actualizado
  </DocTile>

  <DocTile title="Esquema de definición" path="/docs/workflow-automation/workflow-automation-apis/definition-schema">
    Referencia para la estructura YAML del flujo de trabajo
  </DocTile>
</DocTiles>