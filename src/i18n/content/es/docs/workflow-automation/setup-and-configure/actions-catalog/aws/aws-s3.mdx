---
title: Acciones de AWS S3
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS S3 actions
metaDescription: A reference for AWS S3 actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

Esta página proporciona una referencia de las acciones de AWS S3 disponibles en el catálogo de acciones de automatización de flujo de trabajo. Utilice estas acciones para listar, recuperar, cargar y eliminar objetos en buckets de Amazon S3.

## Requisitos previos

Antes de emplear las acciones AWS en la automatización del flujo de trabajo, cerciorar de tener lo siguiente:

* Una cuenta de AWS con las licencias adecuadas.
* Credenciales de AWS configuradas (credenciales de usuario de IAM, ARN de rol de IAM o credenciales de sesión).
* Las licencias IAM necesarios para los servicios específicos de AWS que planea emplear.

Consulte [Configurar credenciales de AWS](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) para obtener información sobre cómo crear usuarios y roles de IAM, y configurar credenciales de AWS estáticas y de sesión para la integración con las acciones de AWS de automatización de flujos de trabajo.

## Listar objetos en un bucket

El identificador de la acción es `aws.s3.listObjectsV2`.

Devuelve algunos o todos (hasta 1000) los objetos de un bucket con sus metadatos. Es la versión recomendada de `listObjects`.

La siguiente tabla describe los campos de entrada para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Aporte**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descripción**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN del rol de IAM que se debe asumir para las llamadas API de AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de clave de acceso de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Clave de acceso secreta de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de sesión temporal para la autenticación STS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Región de AWS del bucket objetivo.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del bucket de S3 del cual listar objetos.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `prefix`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Limita la respuesta a las claves que comienzan con este prefijo.
      </td>

      <td>
        `path/to/folder/`
      </td>
    </tr>

    <tr>
      <td>
        `maxKeys`
      </td>

      <td>
        Entero
      </td>

      <td>
        Opcional. Número máximo de claves a devolver (hasta 1000).
      </td>

      <td>
        `100`
      </td>
    </tr>

    <tr>
      <td>
        `continuationToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de una respuesta anterior para recuperar la siguiente página de resultados.
      </td>

      <td>
        `some-token`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Mapa
      </td>

      <td>
        Opcional. Parámetros adicionales de boto3 para pasar a la llamada API.
      </td>

      <td>
        `{"Delimiter": "/"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        Lista
      </td>

      <td>
        Opcional. Selectores JQ para extraer campos específicos de la salida de la acción.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Los campos opcionales adicionales de boto3 (`EncodingType`, `FetchOwner`, `StartAfter`, `RequestPayer`, `ExpectedBucketOwner`, `OptionalObjectAttributes`, `Delimiter`) están disponibles a través de la entrada `parameters`. Consulte la [referencia de boto3 de list\_objects\_v2](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html) para obtener más detalles.

La siguiente tabla describe los campos de salida para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Producción**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        Consulte la 

        [referencia de list_objects_v2 de boto3](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booleano
      </td>

      <td>
        `true`

         o 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Cadena
      </td>

      <td>
        `"Parameter validation failed: Invalid bucket name"`
      </td>
    </tr>
  </tbody>
</table>

**Flujo de trabajo de ejemplo**

```yaml
name: aws-s3-list-objects-v2
description: 'List Objects in an AWS S3 Bucket'
steps:
  - name: aws_s3_listObjectsV2_1
    type: action
    action: aws.s3.listObjectsV2
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      prefix: "path/to/folder/"
      maxKeys: 100
    next: end
```

## Eliminar un objeto de un bucket

El identificador de la acción es `aws.s3.deleteObject`.

Elimina permanentemente un único objeto de un bucket. Para los buckets versionados, esta operación inserta un marcador de eliminación, que oculta el objeto sin eliminarlo permanentemente a menos que se especifique un `VersionId`.

La siguiente tabla describe los campos de entrada para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Aporte**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descripción**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN del rol de IAM que se debe asumir para las llamadas API de AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de clave de acceso de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Clave de acceso secreta de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de sesión temporal para la autenticación STS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Región de AWS del bucket objetivo.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del bucket de S3 que contiene el objeto.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Clave completa (ruta) del objeto a eliminar.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Mapa
      </td>

      <td>
        Opcional. Parámetros adicionales de boto3 para pasar a la llamada API.
      </td>

      <td>
        `{"VersionId": "testVersion", "MFA": "Test"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        Lista
      </td>

      <td>
        Opcional. Selectores JQ para extraer campos específicos de la salida de la acción.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Los campos opcionales adicionales de boto3 (`RequestPayer`, `BypassGovernanceRetention`, `ExpectedBucketOwner`, `IfMatch`, `VersionId`, `MFA`) están disponibles a través de la entrada `parameters`. Consulte la [referencia de boto3 de delete\_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html) para obtener más detalles.

La siguiente tabla describe los campos de salida para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Producción**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        Consulte la 

        [referencia de boto3 de delete_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booleano
      </td>

      <td>
        `true`

         o 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Cadena
      </td>

      <td>
        `"Parameter validation failed: Invalid bucket name"`
      </td>
    </tr>
  </tbody>
</table>

**Flujo de trabajo de ejemplo**

```yaml
name: aws-s3-delete-object
description: 'Delete an AWS S3 Object'
steps:
  - name: aws_s3_deleteObject_1
    type: action
    action: aws.s3.deleteObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-west-2"
      bucket: "my-bucket"
      key: "path/to/object.txt"
    next: end
```

## Agregar un objeto a un bucket

El identificador de la acción es `aws.s3.putObject`.

Agrega un objeto a un bucket. Amazon S3 es un sistema distribuido —si recibe múltiples requests de escritura para el mismo objeto simultáneamente, sobrescribe todos excepto el último objeto escrito.

La siguiente tabla describe los campos de entrada para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Aporte**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descripción**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN del rol de IAM que se debe asumir para las llamadas API de AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de clave de acceso de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Clave de acceso secreta de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de sesión temporal para la autenticación STS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Región de AWS del bucket objetivo.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del bucket de S3 al que se cargará el objeto.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Clave completa (ruta) para el objeto en el bucket.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `body`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Los datos del objeto a cargar.
      </td>

      <td>
        `file content`
      </td>
    </tr>

    <tr>
      <td>
        `contentType`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Tipo MIME estándar del objeto.
      </td>

      <td>
        `plain/text`
      </td>
    </tr>

    <tr>
      <td>
        `tagging`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Conjunto de etiquetas codificadas en URL para aplicar al objeto.
      </td>

      <td>
        `key1=value1`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Mapa
      </td>

      <td>
        Opcional. Parámetros adicionales de boto3 para pasar a la llamada API.
      </td>

      <td>
        `{"ServerSideEncryption": "AES256", "Metadata": {"metadata1": "value1"}}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        Lista
      </td>

      <td>
        Opcional. Selectores JQ para extraer campos específicos de la salida de la acción.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Los campos opcionales adicionales de boto3 están disponibles a través de la entrada `parameters`. Consulte la [referencia de boto3 de put\_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html) para ver la lista completa.

La siguiente tabla describe los campos de salida para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Producción**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        Consulte la 

        [referencia de boto3 de put_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booleano
      </td>

      <td>
        `true`

         o 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Cadena
      </td>

      <td>
        `"An error occurred (NoSuchBucket) when calling the PutObject operation: The specified bucket does not exist"`
      </td>
    </tr>
  </tbody>
</table>

**Flujo de trabajo de ejemplo**

```yaml
name: s3-put-object
description: 'Put an AWS S3 Object'
steps:
  - name: aws_s3_putObject_1
    type: action
    action: aws.s3.putObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
      body: "Hello world"
      contentType: "plain/text"
      tagging: "key1=value1"
    next: end
```

## Recuperar un objeto de un bucket

El identificador de la acción es `aws.s3.getObject`.

Recupera un objeto de Amazon S3. Especifique el nombre completo de la clave para el objeto en la solicitud.

<Callout variant="important">
  Esta acción falla si el objeto es mayor a 100 KB.
</Callout>

La siguiente tabla describe los campos de entrada para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Aporte**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descripción**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN del rol de IAM que se debe asumir para las llamadas API de AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de clave de acceso de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Clave de acceso secreta de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de sesión temporal para la autenticación STS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Región de AWS del bucket objetivo.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del bucket de S3 que contiene el objeto.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Clave completa (ruta) del objeto a recuperar.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `versionId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de versión del objeto a recuperar. Si se omite, se devuelve la versión actual.
      </td>

      <td>
        `some-version-id`
      </td>
    </tr>

    <tr>
      <td>
        `range`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Encabezado de rango HTTP para recuperar un rango de bytes específico del objeto.
      </td>

      <td>
        `bytes=0-99`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Mapa
      </td>

      <td>
        Opcional. Parámetros adicionales de boto3 para pasar a la llamada API.
      </td>

      <td>
        `{"ChecksumMode": "ENABLED"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        Lista
      </td>

      <td>
        Opcional. Selectores JQ para extraer campos específicos de la salida de la acción.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Los campos opcionales adicionales de boto3 están disponibles a través de la entrada `parameters`. Consulte la [referencia de boto3 de get\_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html) para ver la lista completa.

La siguiente tabla describe los campos de salida para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Producción**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        Consulte la 

        [referencia de boto3 de get_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booleano
      </td>

      <td>
        `true`

         o 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Cadena
      </td>

      <td>
        `"An error occurred (InvalidArgument) when calling the GetObject operation: Invalid version id specified"`
      </td>
    </tr>
  </tbody>
</table>

**Flujo de trabajo de ejemplo**

```yaml
name: s3-get-object
description: 'Get an AWS S3 Object'
steps:
  - name: aws_s3_getObject_1
    type: action
    action: aws.s3.getObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
    next: end
```