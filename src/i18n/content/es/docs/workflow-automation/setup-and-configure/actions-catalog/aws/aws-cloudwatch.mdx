---
title: Acciones de AWS CloudWatch
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS CloudWatch actions
metaDescription: A reference for AWS CloudWatch actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

Esta página proporciona una referencia de las acciones de AWS CloudWatch disponibles en el catálogo de acciones de automatización del flujo de trabajo. Utilice estas acciones para recuperar y cargar datos de log en AWS CloudWatch.

## Requisitos previos

Antes de emplear las acciones AWS en la automatización del flujo de trabajo, cerciorar de tener lo siguiente:

* Una cuenta de AWS con las licencias adecuadas.
* Credenciales de AWS configuradas (credenciales de usuario de IAM, ARN de rol de IAM o credenciales de sesión).
* Las licencias IAM necesarios para los servicios específicos de AWS que planea emplear.

Consulte [Configurar credenciales de AWS](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) para obtener información sobre cómo crear usuarios y roles de IAM, y configurar credenciales de AWS estáticas y de sesión para la integración con las acciones de AWS de automatización de flujos de trabajo.

## Obtener eventos de log

El identificador de la acción es `aws.cloudwatch.getLogEvents`.

Recupera un lote de logs de eventos de un flujo de logs específico en AWS CloudWatch Logs.

La siguiente tabla describe los campos de entrada para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Aporte**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descripción**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN del rol de IAM que se debe asumir para las llamadas API de AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de clave de acceso de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Clave de acceso secreta de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de sesión temporal para la autenticación STS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Región de AWS del grupo de logs objetivo.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `logStreamName`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del flujo de registros del cual recuperar eventos.
      </td>

      <td>
        `2023/10/27/[$LATEST]abcdef123456`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupName`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Nombre del grupo de logs que contiene el flujo de logs.
      </td>

      <td>
        `/aws/lambda/my-function`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupIdentifier`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN o nombre del grupo de logs. Úselo en lugar de 

        `logGroupName`

         al hacer referencia a un grupo de logs por ARN.
      </td>

      <td>
        `arn:partition:service:region:account-id:resource`
      </td>
    </tr>

    <tr>
      <td>
        `startTime`
      </td>

      <td>
        En t
      </td>

      <td>
        Opcional. Inicio del rango de tiempo, expresado como un timestamp de Unix en milisegundos.
      </td>

      <td>
        `1759296000000`
      </td>
    </tr>

    <tr>
      <td>
        `endTime`
      </td>

      <td>
        En t
      </td>

      <td>
        Opcional. Fin del rango de tiempo, expresado como un timestamp de Unix en milisegundos.
      </td>

      <td>
        `1759296000000`
      </td>
    </tr>

    <tr>
      <td>
        `limit`
      </td>

      <td>
        En t
      </td>

      <td>
        Opcional. Número máximo de logs de evento para devolver.
      </td>

      <td>
        `50`
      </td>
    </tr>

    <tr>
      <td>
        `startFromHead`
      </td>

      <td>
        Booleano
      </td>

      <td>
        Opcional. Si es 

        `true`

        , devuelve primero los logs de evento más antiguos. Por defecto es 

        `false`

        .
      </td>

      <td>
        `true`
      </td>
    </tr>

    <tr>
      <td>
        `unmask`
      </td>

      <td>
        Booleano
      </td>

      <td>
        Opcional. Si es 

        `true`

        , devuelve datos de log de evento sin enmascarar. Por defecto es 

        `false`

        .
      </td>

      <td>
        `false`
      </td>
    </tr>

    <tr>
      <td>
        `nextToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de paginación de una respuesta anterior para recuperar la siguiente página de resultados.
      </td>

      <td>
        `f/39218833627378687642013305455131706539523449361490509828/s`
      </td>
    </tr>
  </tbody>
</table>

La siguiente tabla describe los campos de salida para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Producción**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        `{"events": [{"timestamp": 123, "message": "string", "ingestionTime": 123}], "nextForwardToken": "string", "nextBackwardToken": "string"}`
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booleano
      </td>

      <td>
        `true`

         o 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Cadena
      </td>

      <td>
        `"An error occurred (ExpiredTokenException) when calling the GetLogEvents operation: The security token included in the request is expired"`
      </td>
    </tr>
  </tbody>
</table>

El siguiente flujo de trabajo de ejemplo muestra cómo usar esta acción.

```yaml
name: get-lambda-logs
description: 'Retrieve log events from an AWS Lambda function'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: /aws/lambda/my-function
  logStreamName:
    type: String
    defaultValue: 2023/10/27/[$LATEST]abcdef123456
steps:
  - name: get_events_step
    type: action
    action: aws.cloudwatch.getLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      limit: 100
    next: end
```

## Cargar eventos de log

El identificador de la acción es `aws.cloudwatch.putLogEvents`.

Carga un lote de logs de eventos en la secuencia de logs especificada en AWS CloudWatch Logs.

La siguiente tabla describe los campos de entrada para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Aporte**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Descripción**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ARN del rol de IAM que se debe asumir para las llamadas API de AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. ID de clave de acceso de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Clave de acceso secreta de AWS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Opcional. Token de sesión temporal para la autenticación STS. Páselo como un secreto.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Región de AWS del grupo de logs objetivo.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupName`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del grupo de logs en el que cargar eventos.
      </td>

      <td>
        `/aws/lambda/hello-you`
      </td>
    </tr>

    <tr>
      <td>
        `logStreamName`
      </td>

      <td>
        Cadena
      </td>

      <td>
        Obligatorio. Nombre del flujo de logs al que se cargarán los eventos.
      </td>

      <td>
        `2025/09/24/[$LATEST]09f7ca9e9ab044f389419ce60305f594`
      </td>
    </tr>

    <tr>
      <td>
        `logEvents`
      </td>

      <td>
        Lista
      </td>

      <td>
        Obligatorio. Lista de logs de evento para cargar. Cada evento debe incluir un 

        `timestamp`

         (milisegundos de Unix) y una cadena 

        `message`

        .
      </td>

      <td>
        `[{"timestamp": 1698384000000, "message": "Workflow task started."}]`
      </td>
    </tr>

    <tr>
      <td>
        `entity`
      </td>

      <td>
        Dict
      </td>

      <td>
        Opcional. Metadatos de la entidad para asociar con los logs de eventos para la correlación de entidades de New Relic.
      </td>

      <td>
        `{"keyAttributes": {"ResourceType": "AWS::ElasticLoadBalancingV2::LoadBalancer", "Identifier": "app/my-web-lb/12345", "Environment": "Production"}, "attributes": {"MonitoringTier": "Gold"}}`
      </td>
    </tr>
  </tbody>
</table>

La siguiente tabla describe los campos de salida para esta acción.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Producción**
      </th>

      <th>
        **Tipo**
      </th>

      <th>
        **Ejemplo**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objeto
      </td>

      <td>
        `{"nextSequenceToken": "string", "rejectedLogEventsInfo": {"tooNewLogEventStartIndex": 123, "tooOldLogEventEndIndex": 123, "expiredLogEventEndIndex": 123}}`
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booleano
      </td>

      <td>
        `true`

         o 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Cadena
      </td>

      <td>
        `"An error occurred (ExpiredTokenException) when calling the PutLogEvents operation: The security token included in the request is expired"`
      </td>
    </tr>
  </tbody>
</table>

El siguiente flujo de trabajo de ejemplo muestra cómo usar esta acción.

```yaml
name: put-custom-logs
description: 'Upload a batch of custom log events to CloudWatch'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: my-custom-app-logs
  logStreamName:
    type: String
    defaultValue: instance-01-thread-01
steps:
  - name: upload_logs
    type: action
    action: aws.cloudwatch.putLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      logEvents:
        - timestamp: 1698384000000
          message: "Workflow started successfully"
        - timestamp: 1698384005000
          message: "Step 1 completed"
    next: end
```