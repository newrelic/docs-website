---
subject: Streaming Video & Ads for Browser
title: Agente de medios para Video JS v4.2.0
releaseDate: '2026-06-19'
version: 4.2.0
metaDescription: Release notes for Media agent for Video JS version 4.2.0
category: Video JS
translationType: machine
---

## Aspectos destacados

* Inclusión explícita para **AWS MediaTailor**, con compatibilidad total para **dominios de CDN personalizados**.
* Nueva API `config.ad.type` para elegir entre el seguimiento de anuncios **CSAI** y **SSAI** (`DAI`, `MT`) — no más suposiciones basadas en URL para SSAI.
* El logging ahora se enruta a través de `nrvideo.Log` de extremo a extremo, con el modo de detección activa registrado en el inicio.

## Característica

### Compatibilidad con CDN personalizado de MediaTailor

Se reemplazó la detección automática basada en URL con la inclusión explícita.

* Los clientes ahora pasan `mediatailor: true` (o `mediatailor: { trackingUrl, adSegmentPrefix }`) para habilitar el rastreador — admite tanto los nombres de host predeterminados de AWS como los dominios de CDN personalizados.
* Se agregó la constante `MT_DEFAULT_AD_SEGMENT_PATH` (`/tm/`) para la ruta de segmento de anuncios de CDN recomendada por AWS; los segmentos de anuncios reescritos a un dominio de CDN personalizado bajo `/tm/` se detectan automáticamente.
* `isMediaTailorSegment()` ahora verifica el nombre de host de segmentos predeterminado de AWS, la ruta `/tm/` y un `adSegmentPrefix` opcional proporcionado por el cliente.
* Se enlazó `adSegmentPrefix` a través del análisis de manifiestos HLS (VHS) y DASH.
* Se agregó la inicialización de sesión explícita a través de `mediatailor: { trackingUrl }` para los flujos de `POST /v1/session/`.

### Configuración de seguimiento de anuncios

Se introdujo `config.ad.type` para controlar la selección del rastreador de anuncios.

* Se expuso la constante `AD_TRACKING` con **CSAI** (valor plano que cubre IMA / Brightcove IMA / Freewheel / genérico) y los subtipos de **SSAI** (`DAI`, `MT`).
* SSAI requiere un subtipo explícito — cada plataforma necesita su propio SDK y no se puede detectar automáticamente.
* `SSAI.MT` implica `mediatailor: true`.
* Cuando `config.ad.type` no está configurado, vuelve a la detección automática de CSAI con una advertencia (compatible con versiones anteriores para la v4.1.2 usuarios).
* Se ubicaron conjuntamente `segmentPrefix` y `trackingUrl` bajo `config.ad`.
* Se agregó `DaiAdsTracker` a las exportaciones estáticas.

### Mejoras de logging

* Se expuso `VideojsTracker.Log` como estático para que los llamadores UMD puedan establecer el nivel de logs.
* Registra el modo de detección de segmento de anuncios activo en el inicio del rastreador.
* Registra la ruta de detección de segmento de anuncios coincidente en la primera pausa publicitaria (una vez por sesión).
* Registra qué framework de CSAI se detectó automáticamente (BrightcoveIma / IMA / Freewheel / genérico).
* Se reemplazó `console.log/warn/error` por `nrvideo.Log` en todos los archivos de MediaTailor.

## Corrección de errores

* **Buffer End:** se corrigió el manejo del final del búfer en el rastreador.
* **Registro de plug-in:** se corrigió que `register-plugin.js` descartara silenciosamente el objeto de opciones y no lo reenviara al constructor de `TrackerJS`.

## Documentación

* **README y documentación de SSAI:** actualizados para la compatibilidad con CDN personalizada, se aclaró cuándo se necesitan las anulaciones de `trackingUrl` y `adSegmentPrefix`, se limpiaron las referencias a `sessionId`.
* **Resolución de problemas de SSAI:** se corrigieron las referencias de `adSegmentPrefix` a `config.ad.segmentPrefix`.

## Actualización desde la 4.1.x

* **No hay cambios importantes para los usuarios de CSAI.** Si `config.ad.type` no está configurado, el rastreador aún detecta automáticamente los frameworks de CSAI (con una advertencia única).

* **Los usuarios de MediaTailor deben habilitarlo explícitamente.** Reemplace cualquier dependencia de patrón de URL con uno de:

  * `mediatailor: true` (nombres de host predeterminados de AWS + ruta `/tm/`)
  * `mediatailor: { trackingUrl, adSegmentPrefix }` (CDN personalizado / inicialización de sesión explícita)
  * `config.ad.type: AD_TRACKING.SSAI.MT`

* Si anteriormente pasó `adSegmentPrefix` en el nivel superior, muévalo a `config.ad.segmentPrefix`.