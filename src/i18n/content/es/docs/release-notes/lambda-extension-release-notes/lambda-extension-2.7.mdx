---
subject: Lambda-Extension
releaseDate: '2026-08-19'
version: 2.7.0
features:
  - Added LIM support
  - Added 'entity.name' for logs
enhancements:
  - Updated NEW_RELIC_LABELS configuration to support Transaction and Span attribute propagation.
bugs:
  - Fixed telemetry dispatch timing to send agent payloads immediately upon arrival instead of waiting to pair with platform.report events.
security: []
translationType: machine
---

## Notas

* Se agregó soporte para instancias administradas de AWS Lambda (LMI).
* `Updated NEW_RELIC_LABELS` configuración para admitir la propagación del atributo de transacción y Span.
* Se agregó soporte para `NEW_RELIC_DATA_COLLECTION_TIMEOUT` y `NEW_RELIC_HTTP_TIMEOUT` para el modo sin servidor
* Se agregó `entity.name` para los logs

## Fijado

* Se corrigió el tiempo de envío de telemetría para enviar la carga del agente inmediatamente después de su llegada en lugar de esperar a emparejarse con `platform.report`
* Se deshabilitaron los valores predeterminados legacy de smithy rustls