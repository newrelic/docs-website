---
subject: Lambda-Extension
releaseDate: '2026-01-09'
version: 2.4.3
translationType: machine
---

## Notas

* Se implementó el almacenamiento en búfer de logs previo a la invocación con validación de metadatos y soporte para ARN de respaldo

* Se eliminó el log de depuración para el almacenamiento en búfer en el búfer `pre-invoke`

* Se agregó código de reserva para `Aws-arn`

## Fijado

* Eliminar la exposición de `License key` de los logs de CloudWatch