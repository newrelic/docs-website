---
subject: Lambda-Extension
releaseDate: '2025-06-05'
version: 2.3.20
translationType: machine
---

## Notas

* Se agregó lógica para verificar el apagado de `logserver` y evitar que el controlador envíe mensajes al canal de log

* Eliminar el flujo de trabajo de Circle CI

## Fijado

* Agregue `WaitGroup` para resolver `log server panic` resultante de que varios controladores de servidores de log envíen logs a un canal cerrado