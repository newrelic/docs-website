---
subject: Lambda-Extension
releaseDate: '2025-04-08'
version: 2.3.18
translationType: machine
---

## Notas

* Agregado ejemplo para New Relic monitoreo de IA, junto con instrumentación de función Lambda con respuesta streaming

* Se actualizaron `SAM` ejemplos para incluir las últimas capas y paquetes

* Ejemplo de instrumentación Lambda actualizado `Ruby`

* Se actualizó el archivo Readme para incluir la variable de entorno `NEW_RELIC_COLLECT_TRACE_ID` que se puede usar para agregar `trace-id` a los logs de Lambda

## Problemas solucionados

* Se resolvió el problema en el que la extensión capturaba de manera incorrecta las duraciones de tiempo de espera para los errores de tiempo de espera de la función Lambda.

* Tiempo de suspensión actualizado para resolver el problema intermitente con la prueba unitaria `TestClientSendRetry`