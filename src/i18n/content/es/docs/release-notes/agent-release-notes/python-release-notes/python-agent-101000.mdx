---
subject: Python agent
releaseDate: '2025-04-24'
version: 10.10.0
downloadLink: 'https://pypi.python.org/pypi/newrelic'
features:
  - Add async support for elasticsearch
bugs:
  - Add parsing of graphql config setting
  - Fix crash in Kombu when running with Sentry
translationType: machine
---

## Notas

Esta versión del agente Python agrega soporte para [elasticsearch](https://pypi.org/project/elasticsearch/) asíncrono, corrige un problema de análisis de configuración de graphql, corrige un error en la instrumentación [de Kombu](https://pypi.org/project/kombu/) y vuelve a habilitar la instrumentación de Kombu.

Instale el agente usando `easy_install/pip/distribute` a través del [Índice de paquetes de Python](https://pypi.python.org/pypi/newrelic) o descárguelo directamente del [sitio de descarga de New Relic](https://download.newrelic.com/python_agent/release).

## Nueva característica

* Agregar soporte asíncrono para elasticsearch

  * Agrega soporte para [elasticsearch](https://pypi.org/project/elasticsearch/) asíncrono. ¡Gracias [fagnerzulin](https://github.com/fagnerzulin) por la contribución!

## Corrección de errores

* Agregar análisis de la configuración de GraphQL

  * Anteriormente, cuando se establecía la configuración `instrumentation.graphql.capture_introspection_queries` en el archivo de configuración, esta configuración no tenía efecto. Esto fue arreglado.

* Se solucionó el fallo en Kombu al ejecutar con Sentry

  * Corrige un fallo en la instrumentación [de Kombu](https://pypi.org/project/kombu/) donde se generó un `TypeError` desde el módulo `inspect` . Este error se debió a un conflicto en el envoltorio entre New Relic y Sentry. También se volvió a habilitar la instrumentación Kombu.

## Declaración de apoyo

Recomendamos actualizar a la última versión del agente tan pronto como esté disponible. Si no puede actualizar a la última versión, actualice su agente a una versión que no tenga más de 90 días de antigüedad. Obtenga [más información](/docs/new-relic-solutions/new-relic-one/install-configure/update-new-relic-agent/) sobre cómo mantener al agente actualizado.

Consulte la New Relic Python [política](/docs/apm/agents/python-agent/getting-started/python-agent-eol-policy/) de fin de vida del agente para obtener información sobre los lanzamientos del agente y las fechas de soporte.