---
subject: Ruby agent
releaseDate: '2026-06-25'
version: 10.6.0
downloadLink: 'https://rubygems.org/gems/newrelic_rpm/versions/10.6.0'
features:
  - SpanLink events are now supported for the Hybrid Agent
  - SpanEvent events are now supported for the Hybrid Agent
  - Set Span Kind on all Hybrid Agent spans
  - 'Add support for OpenTelemetry::Tracer#start_root_span'
bugs:
  - 'Fix `instrumentation.rails_event_logger: false` not disabling the instrumentation'
  - Normalize boolean-like values to `disabled` for instrumentation config keys
  - Per-library logging supportability metrics now reflect each library's instrumentation state
security: []
translationType: machine
---

<Callout variant="important">
  Recomendamos actualizar a la última versión del agente tan pronto como esté disponible. Si no puede actualizar a la última versión, actualice su agente a una versión que no tenga más de 90 días de antigüedad. Obtenga más información sobre [cómo mantener al agente actualizado](/docs/new-relic-solutions/new-relic-one/install-configure/update-new-relic-agent/).

  Consulte la [política de fin de vida](/docs/apm/agents/ruby-agent/getting-started/ruby-agent-eol-policy/) del agente Ruby New Relic para obtener información sobre los lanzamientos del agente y las fechas de soporte.
</Callout>

## v10.6.0

* **Característica: los eventos SpanLink ahora son compatibles con el agente Hybrid**

  Los spans creados por una API de OpenTelemetry ahora pueden tener [Span Links](https://opentelemetry.io/docs/concepts/signals/traces/#span-links) asociados a ellos. Los enlaces se pueden agregar al inicio de un span, pasándolos al argumento `links` o llamando a la API `OpenTelemetry::Trace::Span#add_link`. [PR#3586](https://github.com/newrelic/newrelic-ruby-agent/pull/3586)

* **Característica: los eventos SpanEvent ahora son compatibles con el agente Hybrid**

  Los spans creados por una API de OpenTelemetry ahora pueden tener [eventos SpanEvent](https://opentelemetry.io/docs/concepts/signals/traces/#span-events) asociados a través de la API `OpenTelemetry::Trace::Span#add_event`. Los eventos SpanEvent capturan anotaciones con timestamp en un span y se envían a New Relic junto con el span principal. [PR#3587](https://github.com/newrelic/newrelic-ruby-agent/pull/3587)

* **Característica: establecer el tipo de span en todos los spans del agente Hybrid**

  Anteriormente, solo los spans de OpenTelemetry que se traducían en segmentos de solicitudes externas o segmentos de almacenamiento de datos agregaban el tipo de span como atributo. Ahora, el agente agrega el tipo de span a todos los spans de OpenTelemetry donde el valor está disponible. [PR#3589](https://github.com/newrelic/newrelic-ruby-agent/pull/3589)

* **Característica: agregar soporte para OpenTelemetry::Tracer#start\_root\_span**

  La API `OpenTelemetry::Tracer#start_root_span` ahora se puede usar para forzar el inicio de una transacción para un span determinado, siempre que tenga un tipo de span `:server` o `:consumer`. Para cualquier otro tipo de span, no realizará ninguna operación. Este método se usa más comúnmente en la instrumentación de trabajos en segundo plano. [PR#3588](https://github.com/newrelic/newrelic-ruby-agent/pull/3588)

* **Corrección de error: solucionar que `instrumentation.rails_event_logger: false` no deshabilite la instrumentación**

  Anteriormente, establecer `instrumentation.rails_event_logger` en `false` no deshabilitaba la instrumentación de `Rails.event` como se esperaba; se seguía instalando durante el arranque de Rails. Esto ya fue solucionado. [PR#3564](https://github.com/newrelic/newrelic-ruby-agent/pull/3564)

* **Corrección de error: normalizar los valores de tipo booleano a `disabled` para las claves de configuración de instrumentación**

  Anteriormente, solo `disabled` desactivaba una clave de configuración de `instrumentation.*`. Ahora, los valores de tipo booleano como `false`, `no` o `off` también se resuelven como `disabled` y evitan que se instale la instrumentación. [PR#3579](https://github.com/newrelic/newrelic-ruby-agent/pull/3579)

* **Corrección de error: las métricas de compatibilidad de logging por biblioteca ahora reflejan el estado de instrumentación de cada biblioteca**

  Anteriormente, las métricas de `Supportability/Logging/Ruby/{library}/{enabled|disabled}` informaban el valor de la configuración global de `application_logging.enabled` para cada biblioteca, en lugar del estado real de cada biblioteca. Como resultado, la métrica informaba `enabled` incluso cuando se había deshabilitado la instrumentación de logging para una biblioteca específica o no se estaba usando la gema de esa biblioteca en absoluto. Ahora, la métrica de cada biblioteca refleja si su propia instrumentación de logging está habilitada. [PR#3571](https://github.com/newrelic/newrelic-ruby-agent/pull/3571)