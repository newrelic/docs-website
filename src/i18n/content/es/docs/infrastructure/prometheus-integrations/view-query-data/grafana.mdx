---
title: Traducir el dashboard de Grafana al dashboard de New Relic
metaDescription: You can use New Relic dashboard translation API to convert your existing Grafana dashboards into New Relic native dashboards.
tags:
  - Integrations
  - Prometheus integrations
  - View and query data
freshnessValidatedDate: never
translationType: machine
---

Convierta sus dashboard de Grafana existentes en dashboard nativos de New Relic utilizando nuestra API REST de traducción. Ya sea que busque consolidar sus herramientas de observabilidad, migrar toda su configuración de monitoreo a New Relic, o simplemente probar los dashboard de New Relic junto con su configuración de Grafana existente, esta API facilita el inicio.

Esta API utiliza la lógica de traducción de PromQL de New Relic para traducir tus consultas. Para ver una lista de las características y funciones de PromQL compatibles, consulta la [guía de soporte de características de PromQL](/docs/infrastructure/prometheus-integrations/view-query-data/supported-promql-features).

## Requisitos previos [#prerequisites]

Antes de usar esta API, necesitará:

* Una [clave de API de usuario de New Relic](/docs/apis/intro-apis/new-relic-api-keys/#overview-keys)
* Tu [ID de cuenta](/docs/accounts/accounts-billing/account-structure/account-id)de New Relic

## Realice una solicitud a los extremos de API [#api-endpoints]

Para traducir un dashboard de Grafana a un dashboard de New Relic, envíe una solicitud `POST` al extremo adecuado para su región con el dashboard que desea traducir en el cuerpo de la solicitud.

* A NOSOTROS: `https://prometheus-api.newrelic.com/api/v1/translate/dashboard/grafana`
* UE: `https://prometheus-api.eu.newrelic.com/api/v1/translate/dashboard/grafana`
* JP: `https://prometheus-api.jp.newrelic.com/api/v1/translate/dashboard/grafana`

## Referencia API [#api-reference]

### Autenticación [#authentication]

Todas las llamadas API requieren una [clave de API de usuario](/docs/apis/intro-apis/new-relic-api-keys/#overview-keys). Incluye esta clave en el encabezado `api-key` de tu solicitud.

### Cuerpo de la solicitud [#request-body]

El cuerpo de la solicitud debe ser un objeto JSON con el siguiente formato:

```json
{
  "accountId": int [optional],
  "dashboard": object [required]
}
```

Donde dashboard es el objeto JSON del dashboard de Grafana que desea traducir. El campo accountId es opcional — si se omite, el dashboard traducido consultará datos de la cuenta del usuario que realiza la solicitud.

### Formato de respuesta [#response-format]

La API devuelve un objeto JSON que contiene el dashboard de New Relic traducido:

```json
{
  "dashboard": {
    "name": "Translated Dashboard",
    "permissions": "PUBLIC_READ_WRITE",
    "pages": [...]
    }
}
```

## Ejemplos [#examples]

<CollapserGroup>
  <Collapser id="dashboard-translation-basic" title="Ejemplo: llamada API básica para traducir un dashboard de Grafana">
    ```bash
    export NR_USER_KEY="YOUR_NEW_RELIC_USER_API_KEY"
    export JSON_FILE="my_grafana_dashboard.json"
    export TRANSLATOR_ENDPOINT="<TRANSLATOR_ENDPOINT_FOR_YOUR_REGION>" # (see API endpoints section above)

    curl -s ${TRANSLATOR_ENDPOINT} \
    -H "Content-Type: application/json" \
    -H "api-key:${NR_USER_KEY}" \
    -d "$(jq -c -n --slurpfile dashboard "${JSON_FILE}" '{dashboard: $dashboard[0]}')" \
    | jq '.dashboard'
    ```
  </Collapser>

  <Collapser id="dashboard-translation-account-override" title="Ejemplo: traducir dashboard con sobrescritura del ID de la cuenta">
    Si desea sobrescribir la cuenta desde la cual su dashboard consultará datos, puede incluir `accountId: int` en la carga de la solicitud. Esto hará que todas las consultas en el dashboard consulten datos de la cuenta especificada.

    ```bash
    export NR_USER_KEY="YOUR_NEW_RELIC_USER_API_KEY"
    export ACCOUNT_ID="YOUR_NEW_RELIC_STAGING_ACCOUNT_ID"
    export JSON_FILE="my_grafana_dashboard.json"
    export TRANSLATOR_ENDPOINT="<TRANSLATOR_ENDPOINT_FOR_YOUR_REGION>" # (see API endpoints section above)

    curl -s ${TRANSLATOR_ENDPOINT} \
    -H "Content-Type: application/json" \
    -H "api-key:${NR_USER_KEY}" \
    -d "$(jq -c -n --arg accountId "$ACCOUNT_ID" --slurpfile dashboard "$JSON_FILE" '{accountId: $accountId, dashboard: $dashboard[0]}')" \
    | jq '.dashboard'
    ```
  </Collapser>

  <Collapser id="dashboard-translation-and-upload" title="Ejemplo: subir el dashboard traducido a New Relic usando la API de NerdGraph">
    <Callout variant="tip">
      Para obtener más información sobre cómo usar NerdGraph y encontrar tu extremo regional, consulta [Introducción a New Relic NerdGraph](/docs/apis/nerdgraph/get-started/introduction-new-relic-nerdgraph).
    </Callout>

    ```bash
    export NR_USER_KEY="YOUR_NEW_RELIC_USER_API_KEY"
    export ACCOUNT_ID="YOUR_NEW_RELIC_STAGING_ACCOUNT_ID"
    export JSON_FILE="my_grafana_dashboard.json"
    export TRANSLATOR_ENDPOINT="<TRANSLATOR_ENDPOINT_FOR_YOUR_REGION>" # (see API endpoints section above)
    export NERDGRAPH_ENDPOINT="<NERDGRAPH_ENDPOINT_FOR_YOUR_REGION>"

    NR_DASHBOARD=$(curl -s ${TRANSLATOR_ENDPOINT} \
    -H "Content-Type: application/json" \
    -H "api-key:${NR_USER_KEY}" \
    -d "$(jq -c -n --arg accountId "$ACCOUNT_ID" --slurpfile dashboard "$JSON_FILE" '{accountId: $accountId, dashboard: $dashboard[0]}')" \
    | jq '.dashboard')


    curl -X POST ${NERDGRAPH_ENDPOINT} \
    -H "Content-Type: application/json" \
    -H "API-Key: ${NR_USER_KEY}" \
    -d '{
    "query": "mutation($accountId: Int!, $dashboard: DashboardInput!) { dashboardCreate(accountId: $accountId, dashboard: $dashboard) { entityResult { guid name } errors { description } } }",
    "variables": {
    "accountId": '${ACCOUNT_ID}',
    "dashboard": '${NR_DASHBOARD}'
    }
    }'
    ```
  </Collapser>
</CollapserGroup>

## Resolución de errores de traducción [#troubleshooting]

Cuando el traductor encuentra problemas que impiden la traducción completa de un widget o consulta, crea &quot;Action Items&quot; — widget especiales de Markdown que explican qué salió mal y sugieren correcciones.

<img title="Translator action item example" alt="Action Item Example" src="/images/grafana_dashboard_translator_action_items.webp" />

## Tipos de widget de Grafana y New Relic

La siguiente es una lista de los tipos de widgets de Grafana que admitimos y sus correspondientes tipos de widgets de New Relic.

<table>
  <thead>
    <tr>
      <th>
        Tipo de widget de Grafana
      </th>

      <th>
        Tipo de widget de New Relic
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [singlestat](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/)

         

        *(obsoleto)*
      </td>

      <td>
        [CARTELERA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-billboard)
      </td>
    </tr>

    <tr>
      <td>
        [estadística](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/stat/)
      </td>

      <td>
        [CARTELERA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-billboard)
      </td>
    </tr>

    <tr>
      <td>
        [bargauge](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/bar-gauge/)
      </td>

      <td>
        [CARTELERA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-billboard)
      </td>
    </tr>

    <tr>
      <td>
        [medidor](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/gauge/)
      </td>

      <td>
        [VIÑETA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-bullet)
      </td>
    </tr>

    <tr>
      <td>
        [tabla](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/table/)
      </td>

      <td>
        [TABLA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-table)
      </td>
    </tr>

    <tr>
      <td>
        [gráfico (gráfico de líneas)](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/time-series/)
      </td>

      <td>
        [Línea](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-line)
      </td>
    </tr>

    <tr>
      <td>
        [gráfico (gráfico de área)](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/time-series/)
      </td>

      <td>
        [ÁREA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-area)
      </td>
    </tr>

    <tr>
      <td>
        [texto](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/text/)
      </td>

      <td>
        [Markdown](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-markdown)
      </td>
    </tr>

    <tr>
      <td>
        [piechart](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/pie-chart/)
      </td>

      <td>
        [PIE](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-pie)
      </td>
    </tr>

    <tr>
      <td>
        [mapa de calor](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/heatmap/)
      </td>

      <td>
        [MAPA DE CALOR](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-heat-map)
      </td>
    </tr>

    <tr>
      <td>
        [gráfico de barras](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/bar-chart/)
      </td>

      <td>
        [BARRA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-bar)
      </td>
    </tr>

    <tr>
      <td>
        [gráfico de barras](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/bar-chart/)
      </td>

      <td>
        [BARRA](/docs/query-your-data/explore-query-data/use-charts/chart-types/#widget-bar)
      </td>
    </tr>
  </tbody>
</table>