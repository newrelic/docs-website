---
title: UI de hosts de infraestructura
tags:
  - Infrastructure
  - Infrastructure monitoring UI
  - Infrastructure UI
  - Hosts page
metaDescription: 'For New Relic infrastructure monitoring, use the Hosts UI page to view the most important metrics from across your server/host environment.'
freshnessValidatedDate: never
translationType: machine
---

En nuestra UI de monitoreo de infraestructura, la página UI <DNT>**Hosts**</DNT> es el lugar principal para comprender y solucionar problemas con su infraestructura.

<Callout variant="tip">
  Estos documentos describen la nueva UI <DNT>**Hosts**</DNT> agregada en abril de 2023. Puede cambiar entre la vista nueva y la vista <DNT>**Classic**</DNT> .
</Callout>

<img title="Infrastructure UI - Main hosts UI" alt="Infrastructure UI - Main hosts UI" src="/images/infrastructure_screenshot-crop_infrastructure-main-hosts-ui.webp" />

<figcaption>
  <DNT>
    **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure**
  </DNT>
</figcaption>

## Un caso de uso de ejemplo [#use-case]

La página <DNT>**Hosts**</DNT> es el lugar al que acudir para investigar problemas en su infraestructura. A continuación se muestra un ejemplo de un flujo de trabajo común que utilizan nuestros clientes para diagnosticar un problema:

1. En la [página de resumen de APM](/docs/apm/agents/manage-apm-agents/agent-data/triage-run-diagnostics), observa un problema con su servicio Kafka. El rendimiento ha disminuido y hay muchos errores.
2. Según los datos de infraestructura que ve en APM, cree que es un problema del host, por lo que hace clic en la página de infraestructura <DNT>**Hosts**</DNT> .
3. La página <DNT>**Hosts**</DNT> filtra automáticamente su servicio Kafka. Verá que la mitad de los hosts que ejecutan ese servicio han maximizado su porcentaje de CPU.
4. Si sospecha que un cambio en la configuración del host podría ser el origen de su problema, vaya a la pestaña <DNT>**Inventory**</DNT> y verifique las configuraciones de su sistema por host.

## Característica UI para explorar y analizar sus hosts [#ui-features]

Hay algunas características importantes UI disponibles en la UI <DNT>**Hosts**</DNT>. Comprender cómo usarlos le ayudará a aprovechar al máximo esta UI.

<img title="Infrastructure UI - Hosts UI options" alt="Infrastructure UI - Hosts UI options" src="/images/infrastructure_screenshot-crop_hosts-ui-options.webp" />

* <DNT>**A.**</DNT> La barra de filtro: filtre los hosts que desea ver. Para obtener sugerencias, consulte [Barra de filtros](/docs/new-relic-solutions/new-relic-one/core-concepts/search-filter-entities).
* <DNT>**B.**</DNT> Vistas guardadas: cuando crea una vista que le gusta (que puede incluir filtros y selecciones de tablas y gráficos personalizados), haga clic en <DNT>**Save**</DNT>.
* <DNT>**C.**</DNT> Opciones de gráfico: puede cambiar a diferentes métricas para cada gráfico y puede hacer clic en `...` para ver [más opciones de gráfico](/docs/new-relic-solutions/new-relic-one/introduction-new-relic-platform/#data-analysis), incluida la obtención de un enlace para compartir y la creación de una alerta para esa métrica.
* <DNT>**D.**</DNT> Agrupar por: utilice esto para agrupar los resultados del gráfico por atributo específico. Por ejemplo, puede mostrar las regiones de AWS con el mayor uso de CPU agrupadas por `awsRegion`.
* <DNT>**E.**</DNT> Filtro de atributos: esto le permite filtrar sus hosts a aquellos con un valor [de atributo](/docs/new-relic-solutions/get-started/glossary/#attribute) específico. Por ejemplo,
* <DNT>**F.**</DNT> Tabla de hosts: de forma predeterminada, se seleccionan 25 hosts para su visualización. Utilice las columnas de la tabla y el filtro de atributos para explorar los hosts y seleccione diferentes hosts para mostrarlos. Puede exportar la tabla como CSV.
* <DNT>**G.**</DNT> Detalles del host: para un host específico en la tabla de host, haga clic en el <Icon name="fe-more-horizontal" /> Icono para ver más opciones para aprender sobre él, incluidas opciones para ver el registro, ver un [mapa de entidades](/docs/new-relic-solutions/new-relic-one/ui-data/automaps) y más.

## Pestañas en la página UI del host [#host-views]

Aquí hay más detalles sobre las pestañas disponibles en la página <DNT>**Hosts**</DNT> UI:

<CollapserGroup>
  <Collapser id="summary" title="Pestaña Resumen">
    En la página <DNT>**Hosts**</DNT> , la pestaña <DNT>**Summary**</DNT> le ofrece una descripción general del rendimiento de sus hosts.

    <img title="Hosts Summary page" alt="Hosts Summary page" src="/images/infrastructure_screenshot-crop_host-page-tiles.gif" />

    <figcaption>
      <DNT>
        **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure**
      </DNT>
    </figcaption>

    Los mosaicos en la parte superior de esta página le brindan una descripción general de sus hosts, aplicación, evento y alerta. Utilice estos mosaicos para ver más detalles sobre un host o filtrar toda la página <DNT>**Summary**</DNT> a un host específico.

    El selector de tiempo en la página <DNT>**Summary**</DNT> solo ajusta los mosaicos y gráficos métricos, pero **no** ajusta la tabla de host. Las otras pestañas de la UI <DNT>**Hosts**</DNT> tienen tablas que reflejan el intervalo de tiempo establecido por el selector de tiempo.
  </Collapser>

  <Collapser id="system" title="UI del sistema">
    La pestaña <DNT>**System**</DNT> le ofrece una descripción general de su sistema. Es algo similar a la pestaña <DNT>**Summary**</DNT> pero le permite buscar detalles más detallados sobre algunos aspectos de su sistema.

    <img title="Hosts System page" alt="Hosts System page" src="/images/infrastructure_screenshot-full_host-new-system.webp" />

    <figcaption>
      <DNT>
        **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure &gt; Hosts &gt; System**
      </DNT>
    </figcaption>

    Estos son los gráficos predeterminados que se muestran en la página <DNT>**System**</DNT> :

    * <DNT>**CPU %**</DNT>: Esta es una métrica derivada de varias otras métricas en el evento `SystemSample` . Específicamente, el atributo [`cpuPercent`](/docs/infrastructure/new-relic-infrastructure/data-instrumentation/default-infrastructure-attributes-events#systemsample-cpu-percent) es una agregación de `cpuUserPercent`, `cpuSystemPercent`, `cpuIoWaitPercent` y `cpuStealPercent`.
    * <DNT>**Load average five minute**</DNT>: representa un promedio del número de tareas en espera de realizarse en un sistema durante los últimos 5 minutos.
    * <DNT>**Memory free %**</DNT>: compara la cantidad de bytes de memoria libres con la cantidad de bytes de memoria utilizados.

    Para obtener detalles sobre los datos utilizados para generar estos gráficos, consulte [SystemSample](attribute-dictionary/?event=SystemSample).
  </Collapser>

  <Collapser id="network" title="UI de red">
    La página <DNT>**Network**</DNT> proporciona visibilidad en tiempo real del estado y el rendimiento de hosts individuales, servidores web u otros grupos de recursos en su red. Los gráficos predeterminados muestran el ancho de banda métrica por paquete, el ancho de banda por bytes y los errores por segundo.

    <img title="Hosts Network page" alt="Hosts Network page" src="/images/infrastructure_screenshot-full_host-new-network.webp" />

    <figcaption>
      <DNT>
        **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure &gt; Hosts &gt; Network**
      </DNT>
    </figcaption>

    Los detalles sobre interfaces individuales pueden ayudarle a:

    * Examine los niveles de saturación en su red o en hosts seleccionados.
    * Compare los equilibrios de carga entre recursos específicos.
    * Identifique diferencias inesperadas en patrones de capacidad entre hosts similares.
    * Evalúe los cinco principales errores de red detectados.

    Estos datos de red en tiempo real pueden ayudarlo a determinar si debe resolver errores reconfigurando su red, reequilibrando las cargas o tomando otras acciones de mantenimiento preventivo antes de tener que tomar una decisión de compra.

    Para obtener detalles sobre los datos utilizados para generar estos gráficos, consulte [NetworkSample](/attribute-dictionary/?event=NetworkSample).
  </Collapser>

  <Collapser id="processes" title="UI de procesos">
    <Callout variant="important">
      De forma predeterminada, el agente de infraestructura no envía [datos sobre los procesos del sistema operativo](/attribute-dictionary/?event=ProcessSample) a menos que emplee la instalación guiada. Para habilitar el envío del conjunto de datos de proceso [`enable_process_metrics`](/docs/infrastructure/install-infrastructure-agent/configuration/infrastructure-agent-configuration-settings/) a `true`. Para definir con precisión qué procesos desea monitor, configure [`include_matching_metrics`](/docs/infrastructure/install-configure-manage-infrastructure/configuration/infrastructure-configuration-settings#include-matching-metrics) o [`exclude_matching_metrics`](/docs/infrastructure/install-configure-manage-infrastructure/configuration/infrastructure-configuration-settings#exclude-matching-metrics).
    </Callout>

    Utilice la pestaña <DNT>**Processes**</DNT> para obtener información sobre los procesos que se ejecutan en sus hosts y para configurar alertas sobre la métrica del proceso. La pestaña <DNT>**Processes**</DNT> muestra datos como el porcentaje de CPU, bytes de E/S y uso de memoria para procesos individuales o grupos de procesos.

    <img title="Hosts Processes page" alt="Hosts Processes page" src="/images/infrastructure_screenshot-full_host-new-processes.webp" />

    <figcaption>
      <DNT>
        **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure &gt; Hosts &gt; Processes**
      </DNT>
    </figcaption>

    En la pestaña <DNT>**Processes**</DNT> , el porcentaje de CPU se limita a procesos individuales, en lugar de hosts. Debido a esto, la métrica del porcentaje de CPU no tiene en cuenta los recursos de todo el sistema. En cambio, muestra cuánto de un solo núcleo de CPU está tomando cada proceso.

    Para obtener detalles sobre los datos utilizados para generar estos gráficos, consulte [ProcessSample](/attribute-dictionary/?event=ProcessSample).

    A continuación se muestra un ejemplo de cómo identificar un problema y configurar una alerta:

    1. Observa en la página <DNT>**Hosts**</DNT> que un clúster tiene un porcentaje de CPU mucho más alto de lo normal.
    2. Vaya a la página <DNT>**Processes**</DNT> y filtre hasta los procesos de ese clúster.
    3. Observa que varias instancias del mismo proceso tienen un porcentaje de CPU excesivo.
    4. Después de solucionar el problema, decide crear una condición de alerta que se activa cuando el porcentaje de CPU para ese tipo de proceso excede un cierto umbral.

    <Callout variant="tip">
      No puede iniciar, detener ni reiniciar procesos desde la UI. En su lugar, siga los procedimientos estándar para [iniciar, detener, verificar o reiniciar](/docs/infrastructure/new-relic-infrastructure/configuration/start-stop-restart-check-infrastructure-agent-status) el agente de infraestructura manualmente.
    </Callout>
  </Collapser>

  <Collapser id="storage" title="UI de almacenamiento">
    <img title="Hosts Storage page" alt="Hosts Storage page" src="/images/infrastructure_screenshot-full_host-new-storage.webp" />

    <figcaption>
      <DNT>
        **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure &gt; Hosts &gt; Storage**
      </DNT>
    </figcaption>

    La pestaña <DNT>**Storage**</DNT> le permite monitor la capacidad y eficiencia de sus recursos, incluida la utilización general de sus dispositivos, el uso del disco o las operaciones de E/S. Esto puede ayudarte a:

    * Examinar patrones inesperados; por ejemplo, un grupo de máquinas reflejadas que no procesan tareas de E/S de manera uniforme.
    * Monitor los niveles de uso antes de que se agote por completo el espacio en disco.
    * Establezca condición de alerta para notificarle sobre problemas con uno o más hosts; por ejemplo, tiempos de procesamiento prolongados para operaciones de lectura o escritura, disponibilidad o utilización del disco en función de porcentaje o bytes, etc.
    * Tome decisiones informadas sobre dónde redistribuir hosts con solicitudes de procesamiento superiores o inferiores a las normales.
    * Utilice datos para ayudar a planificar adiciones o cambios a su presupuesto de infraestructura antes de que surja una emergencia.

    Para obtener detalles sobre los datos utilizados para generar estos gráficos, consulte [StorageSample](/attribute-dictionary/?event=StorageSample).
  </Collapser>

  <Collapser id="containers" title="Contenedor">
    Utilice la pestaña <DNT>**Containers**</DNT> para obtener información sobre el contenedor que se ejecuta en sus hosts y para configurar una alerta en el contenedor métrico. Esta pestaña muestra datos como el porcentaje de CPU, bytes de E/S y uso de memoria para contenedores individuales o grupos de contenedores. Utilice esta información para:

    * Identifique los contenedores que están experimentando problemas de rendimiento.
    * Cree <InlinePopover type="alerts" />para condiciones como el porcentaje de CPU y el uso de memoria.

    <img title="Hosts Containers page" alt="Hosts Containers page" src="/images/infrastructure_screenshot-full_host-new-containers.webp" />

    <figcaption>
      <DNT>
        **[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Infrastructure &gt; Hosts (New) &gt; Containers**
      </DNT>
    </figcaption>
  </Collapser>
</CollapserGroup>

## Datos de infraestructura [#data]

Para obtener más información sobre los datos de infraestructura, consulte [datos de infraestructura](/docs/infrastructure/manage-your-data/data-instrumentation/default-infrastructure-monitoring-data/#infrastructure-events).