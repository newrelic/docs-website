---
title: Eliminar todos los atributos
type: apiDoc
shortDescription: Elimina todos los atributos.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to remove all attributes.
freshnessValidatedDate: '2023-07-21T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unidad
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxis [#syntax]

      ### Java [#java]

      ```java
      NewRelic.removeAllAttributes ()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.removeAllAttributes()
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Valores de retorno [#return-values]

      Devuelve `true` si se registró correctamente y `false` si no es así.

      ## Ejemplo [#example]

      ### Java [#java]

      ```java
      boolean attributesRemoved = NewRelic.removeAllAttributes();
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      val attributesRemoved = NewRelic.removeAllAttributes()
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxis [#syntax]

      ### C objetivo [#objc]

      ```objectivec
      + (BOOL) removeAllAttributes();
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.removeAllAttributes() -> Bool
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Valores de retorno [#return-values]

      Devuelve `true` si se registró correctamente y `false` si no es así.

      ## Ejemplos [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      BOOL attributesRemoved = NewRelic.removeAllAttributes();
      ```

      ### Swift [#swift]

      ```swift
      let attributesRemoved = NewRelic.removeAllAttributes()
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Sintaxis [#syntax]

      ```typescript
      removeAllAttributes(options?: {} | undefined) => void
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Ejemplo [#example]

      ```typescript
      NewRelicCapacitorPlugin.removeAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Sintaxis [#syntax]

      ```csharp
      RemoveAllAttributes() : bool;
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Ejemplo [#example]

      ```csharp
      CrossNewRelic.Current.RemoveAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Sintaxis [#syntax]

      ```typescript
      removeAllAttributes(): void;
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Ejemplo [#example]

      ```js
      NewRelic.removeAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxis [#syntax]

      ```js
      removeAllAttributes(): void;
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Ejemplo [#example]

      ```js
      NewRelic.removeAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="unity">
      ## Sintaxis [#syntax]

      ```csharp
      RemoveAllAttributes() : bool;
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Ejemplo [#example]

      ```csharp
      NewRelicAgent.RemoveAllAttributes();
      ```
    </TabsPageItem>

    <TabsPageItem id="unreal">
      ## Sintaxis [#syntax]

      ```cpp
      removeAllAttributes() : void;
      ```

      ## Descripción [#description]

      Este método elimina todos los atributos de una sesión. Si desea eliminar un único atributo de sesión, consulte [Eliminar un atributo](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/remove-all-attributes).

      ## Ejemplo [#example]

      ```cpp
      #include "NewRelicBPLibrary.h"

      UNewRelicBPLibrary::removeAllAttributes();
      ```

      <img title="Unreal Engine Plugin Remove Attributes" alt="Screenshot of the Unreal Engine Plugin Remove Attributes" src="/images/newrelic_unreal_sdk_remove_attribute.webp" />
    </TabsPageItem>
  </TabsPages>
</Tabs>