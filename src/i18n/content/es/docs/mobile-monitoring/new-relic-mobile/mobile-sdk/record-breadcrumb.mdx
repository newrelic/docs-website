---
title: Registro de ruta de navegación
type: apiDoc
shortDescription: Registra un evento MobileBreadcrumb utilizado para el análisis de fallos.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to record a MobileBreadcrumb event for crash analysis.
freshnessValidatedDate: '2023-07-18T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="flutter">
      Flutter
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unidad
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxis [#syntax]

      ### Java [#java]

      ```java
            NewRelic.recordBreadcrumb(
                        string $Name,
                        map<string, object> $eventAttributes
                    )
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.recordBreadcrumb(
                  breadcrumbName: String?,
                  attributes: MutableMap<String?, Any?>?
              )
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además de cualquier atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `Name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `eventAttributes`
            </td>

            <td>
              `dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplos [#examples]

      A continuación se muestra un ejemplo de una llamada de evento de ruta de navegación simple:

      ### Java [#java]

      ```java
      Map attributes = new HashMap();
      attributes.put("attributeName1", "value1");
      attributes.put("attributeName1", 2);
      boolean eventRecorded = NewRelic.recordBreadcrumb("Name", attributes);
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      val map = mutableMapOf<String,Any>("attributeName1" to "value1", "attributeName1" to 2)
      val eventRecorded = NewRelic.recordBreadcrumb("Name", map)
      ```

      A continuación se muestra un ejemplo de un evento de ruta de navegación con valores del mundo real:

      ### Java [#java]

      ```java
      Map attributes = new HashMap();
      attributes.put("button", "sign-in");
      attributes.put("location", "LaunchFragment");

      NewRelic.recordBreadcrumb("user tapped sign in button", attributes);
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      val attributes = mutableMapOf<String,Any>("button" to "sign-in", "location" to "LaunchFragment")
      NewRelic.recordBreadcrumb("user tapped sign in button", attributes)
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxis [#syntax]

      ### C objetivo [#objc]

      ```objectivec
      recordBreadcrumb:(NSString* __nonnull)name attributes:(NSDictionary* __nullable)attributes;
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.recordBreadcrumb(string $Name, map $eventAttributes)
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; créelos para la actividad de la aplicación que crea que le ayudará a solucionar fallos.

      Además de cualquier atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia de usar [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      Usar esta llamada tiene el mismo resultado que usar la [llamada`recordCustomEvent` ](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/record-custom-events/)con `MobileBreadcrumb` como tipo de evento. Para conocer el contexto sobre cómo utilizar esta API, lea sobre el envío de atributos personalizados y eventos:

      * [Objective-C](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/mobile-sdk-api-guide#objc-custom-att-events)
      * [Swift](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/mobile-sdk-api-guide#swift-custom-att-events)

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `$Name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `$eventAttributes`
            </td>

            <td>
              `dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplos [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      [NewRelic recordBreadcrumb:@"user 11-3435 tapped sign-in button on LaunchViewController"
      attributes:@{ @"button" : @"sign-in", @"action" : @"tapped", @"userId" : @"11-3435", @"location" : @"LaunchViewController"}];
      ```

      ### Swift [#swift]

      ```swift
      let eventRecorded = NewRelic.recordBreadcrumb("user 11-3435 tapped sign-in from LaunchViewController",
                  attributes:["button" : "sign-in", "userId" : "11-3435", "action" : "tapped", "location" : "LaunchViewController"])
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Sintaxis [#syntax]

      ```typescript
      recordBreadcrumb(options: { name: string; eventAttributes: object; }) => void
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además de cualquier atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `eventAttributes`
            </td>

            <td>
              `object`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplo [#example]

      ```typescript
      NewRelicCapacitorPlugin.recordBreadcrumb({ name: "shoe", eventAttributes: {"shoeColor": "blue","shoesize": 9,"shoeLaces": true} });
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Sintaxis [#syntax]

      ```typescript
      recordBreadcrumb(eventName: string, attributes?: {[key: string]: boolean | number | string}): void;
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además de cualquier atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `eventName`
            </td>

            <td>
              Cadena
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `Attributes`
            </td>

            <td>
              `object`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplo [#example]

      ```js
      NewRelic.recordBreadcrumb("shoe", {"shoeColor": "blue","shoesize": 9,"shoeLaces": true});
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Sintaxis [#syntax]

      ```csharp
      RecordBreadcrumb(string name, Dictionary<string, object> attributes): bool;
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además de cualquier atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `Attributes`
            </td>

            <td>
              `Dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplo [#example]

      ```csharp
      CrossNewRelic.Current.RecordBreadcrumb("MAUIExampleBreadcrumb", new Dictionary<string, object>()
          {
              {"BreadNumValue", 12.3 },
              {"BreadStrValue", "MAUIBread" },
              {"BreadBoolValue", true }
          }
      );
      ```
    </TabsPageItem>

    <TabsPageItem id="flutter">
      ## Sintaxis [#syntax]

      ```dart
      recordBreadcrumb(String name,{Map<String, dynamic>? eventAttributes}): void;
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además del atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `eventAttributes`
            </td>

            <td>
              `Dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplo [#example]

      ```dart
      NewrelicMobile.instance.recordBreadcrumb("Button Got Pressed on Screen 3");
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxis [#syntax]

      ```js
      recordBreadcrumb(name: string, attributes?: {[key: string]: any}): void;
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además del atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `Attributes`
            </td>

            <td>
              `Dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplo [#example]

      ```js
      NewRelic.recordBreadcrumb("shoe", {"shoeColor": "blue","shoesize": 9,"shoeLaces": true});
      ```
    </TabsPageItem>

    <TabsPageItem id="unity">
      ## Sintaxis [#syntax]

      ```csharp
      RecordBreadcrumb(string name, Dictionary<string, object> attributes): bool;
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además del atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `name`
            </td>

            <td>
              `string`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `Attributes`
            </td>

            <td>
              `Dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Valores de retorno [#return-values]

      Devuelve `true` si el evento se registra correctamente, o `false` en caso contrario.

      ## Ejemplo [#example]

      ```csharp
      Dictionary<string, object> dic = new Dictionary<string, object>();
      dic.Add("Unity Attribute", "Data1");

      NewRelicAgent.RecordBreadCrumb("Unity BreadCrumb Example", dic);
      ```
    </TabsPageItem>

    <TabsPageItem id="unreal">
      ## Sintaxis [#syntax]

      ```cpp
      recordBreadCrumb(FString breadcrumbName);

      recordBreadCrumbWithAttributes(FString breadcrumbName, TMap <FString, FString> customData):void
      ```

      ## Descripción [#description]

      Esta llamada crea y registra un evento `MobileBreadcrumb` , que se puede consultar con NRQL y en el [crash trazar el evento](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/mobile-crash-event-trail). Las rutas de navegación móviles son útiles para el análisis de fallos; Deben crearse para la actividad de la aplicación que pueda ser útil para la resolución de problemas.

      Además del atributo personalizado que elijas, el evento también tendrá asociado [un atributo de sesión](/docs/insights/explore-data/attributes/mobile-default-attributes-insights#mobile-list). A diferencia del uso de [`setAttribute`](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/create-attribute/), agregar atributos a un evento de ruta de navegación los agrega solo a ese evento; No son atributos de sesión.

      ## Parámetros [#parameters]

      <table>
        <thead>
          <tr>
            <th width="30%">
              Parámetro
            </th>

            <th width={200}>
              Tipo
            </th>

            <th>
              Descripción
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `breadcrumbName`
            </td>

            <td>
              `FString`
            </td>

            <td>
              Requerido. El nombre que desea darle al evento de ruta de navegación.
            </td>
          </tr>

          <tr>
            <td>
              `Attributes`
            </td>

            <td>
              `Dictionary`
            </td>

            <td>
              Opcional. Un mapa que incluye una lista de atributos del evento de ruta de navegación. Cree un atributo para cualquier descriptor de evento que crea que será útil.
            </td>
          </tr>
        </tbody>
      </table>

      ## Ejemplo [#example]

      ```cpp
      #include "NewRelicBPLibrary.h"

      UNewRelicBPLibrary::.recordBreadCrumb("Unreal Breadcrumb Without Attribute");

      TMap<FString, FString> breadCrumbMap;
      breadCrumbMap.Add("place", TEXT("Robots"));
      breadCrumbMap.Add("user", TEXT("Nisarg"));

      UNewRelicBPLibrary::recordBreadCrumbWithAttributes("Unreal Breadcrumb",breadCrumbMap);
      ```

      <img title="Unreal Engine Plugin Record BreadCrumb" alt="Screenshot of the Unreal Engine Plugin Record breadcrumb" src="/images/newrelic_unreal_sdk_record_breadcrumb.webp" />
    </TabsPageItem>
  </TabsPages>
</Tabs>