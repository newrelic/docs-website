---
title: Cerrar el agente
type: apiDoc
shortDescription: Apague el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to shut down the agent within the current application lifecycle during runtime.
freshnessValidatedDate: '2023-07-21T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Unreal Engine
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Sintaxis [#syntax]

      ### Java [#java]

      ```java
      NewRelic.shutdown()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.shutdown()
      ```

      ## Descripción [#description]

      Esta API proporciona la capacidad de apagar el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución. Una vez que el agente se ha cerrado, no se puede reiniciar dentro del ciclo de vida actual de la aplicación.

      ## Ejemplo [#example]

      ### Java [#java]

      ```java
      NewRelic.shutdown()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.shutdown()
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Sintaxis [#syntax]

      ### C objetivo [#objc]

      ```objectivec
      + (void) shutdown();
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.shutdown() -> Void
      ```

      ## Descripción [#description]

      Esta API proporciona la capacidad de apagar el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución. Una vez que el agente se ha cerrado, no se puede reiniciar dentro del ciclo de vida actual de la aplicación.

      ## Ejemplos [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      [NewRelic shutdown];
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.shutdown()
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Sintaxis [#syntax]

      ```typescript
      shutdown(options?: {} | undefined) => void
      ```

      ## Descripción [#description]

      Apague el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución.

      ## Ejemplo [#example]

      ```typescript
      NewRelicCapacitorPlugin.shutdown();
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Sintaxis [#syntax]

      ```typescript
      shutdown() : void;
      ```

      ## Descripción [#description]

      Apague el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución.

      ## Ejemplo [#example]

      ```js
      NewRelic.shutdown();
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Sintaxis [#syntax]

      ```csharp
      Shutdown() : void
      ```

      ## Descripción [#description]

      Apague el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución.

      ## Ejemplo [#example]

      ```csharp
      CrossNewRelic.Current.Shutdown());
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Sintaxis [#syntax]

      ```js
      shutdown() : void;
      ```

      ## Descripción [#description]

      Apague el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución.

      ## Ejemplo [#example]

      ```js
      NewRelic.shutdown();
      ```
    </TabsPageItem>

    <TabsPageItem id="unreal">
      ## Sintaxis [#syntax]

      ```cpp
      shutdown(): void;
      ```

      ## Descripción [#description]

      Apague el agente dentro del ciclo de vida actual de la aplicación durante el tiempo de ejecución.

      ## Ejemplo [#example]

      ```cpp
      UNewRelicBPLibrary::shutdown();;
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>