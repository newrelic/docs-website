---
title: No se puede aumentar el búfer o no hay memoria
type: troubleshooting
tags:
  - Integrations
  - Kubernetes integration
  - Troubleshooting
  - OpenShift
metaDescription: Some troubleshooting tips if you have memory or buffer errors on the fluent bit pods
freshnessValidatedDate: '2024-12-09T00:00:00.000Z'
translationType: machine
---

## Problema

Estás recibiendo errores como `cannot increase buffer: current=32000 requested=64768 max=32000` o `pod out of memory`.

## Solución

Agregue el atributo `k8sBufferSize` para aumentar el tamaño del búfer de Fluent Bit.

```yaml
newrelic-logging:
  enabled: true
  containerSecurityContext:
    privileged: true
  fluentBit:
    k8sBufferSize: "64k"
    persistence:
      mode: "none"
```
