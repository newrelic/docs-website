---
title: Métodos del ciclo de vida
metaDescription: Methods to send lifecycle events and process system log messages.
freshnessValidatedDate: never
translationType: machine
---

## Métodos del ciclo de vida

<table>
  <thead>
    <tr>
      <th width="30%">
        Método
      </th>

      <th>
        Descripción
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [nrAppIniciado](/docs/streaming-video-&-ads/installation/roku/agent-api/lifecycle/nrappstarted)
      </td>

      <td>
        Envía un evento APP\_STARTED del tipo ConnectedDeviceSystem.
      </td>
    </tr>

    <tr>
      <td>
        [nrEscena cargada](/docs/streaming-video-&-ads/installation/roku/agent-api/lifecycle/nrsceneloaded)
      </td>

      <td>
        Envía un evento SCENE\_LOADED del tipo ConnectedDeviceSystem.
      </td>
    </tr>

    <tr>
      <td>
        [nrProcesoMensaje](/docs/streaming-video-&-ads/installation/roku/agent-api/lifecycle/nrprocessmessage)
      </td>

      <td>
        Comprueba si hay un mensaje de log del sistema, lo procesa y envía el evento apropiado.
      </td>
    </tr>
  </tbody>
</table>

## Ejemplos

### Enviar evento APP\_STARTED

```brightscript
sub Main(aa as Object)
    ...
    nrAppStarted(m.nr, aa)
```

### Enviar evento SCENE\_LOADED

```brightscript
nrSceneLoaded(m.nr, "MyVideoScene")
```

### Procesar mensajes de log del sistema

```brightscript
while (true)
    msg = wait(0, m.port)
    if nrProcessMessage(m.nr, msg) = false
        if type(msg) = "roPosterScreenEvent"
            if msg.isScreenClosed()
                exit while
            end if
        end if
    end if
end while
```