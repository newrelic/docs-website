---
title: nrEnviarRespuestaHttp
type: apiDoc
shortDescription: Envía un evento HTTP_RESPONSE del tipo ConnectedDeviceSystem.
tags: null
metaDescription: Send an HTTP_RESPONSE event of type ConnectedDeviceSystem.
freshnessValidatedDate: never
translationType: machine
---

## Sintaxis

```js
  nrSendHttpResponse(nr as Object, _url as String, msg as Object) as Void
```

## Descripción

Envía un evento HTTP\_RESPONSE del tipo ConnectedDeviceSystem.

## Argumentos

<table>
  <thead>
    <tr>
      <th width="25%">
        Argumentos
      </th>

      <th>
        Descripción
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `nr`
      </td>

      <td>
        Objeto agente New Relic .
      </td>
    </tr>

    <tr>
      <td>
        `_url`
      </td>

      <td>
        URL de solicitud.
      </td>
    </tr>

    <tr>
      <td>
        `msg`
      </td>

      <td>
        Un mensaje de tipo roUrlEvent.
      </td>
    </tr>
  </tbody>
</table>

## Devolver

Nada.

## Ejemplo

```js
  	msg = wait(5000, m.port)
	  if type(msg) = "roUrlEvent" then
		        nrSendHttpResponse(m.nr, _url, msg)
	  end if
```