---
title: Filtrar evento AjaxRequest
metaDescription: 'For New Relic browser monitoring: use the app settings page to filter which network calls are recorded as AjaxRequest events.'
freshnessValidatedDate: never
translationType: machine
---

En [la versión del agente 1211](/docs/release-notes/new-relic-browser-release-notes/browser-agent-release-notes/) o superior, todas las solicitudes de red realizadas por una página se registran como `AjaxRequest` evento. Puede utilizar las opciones de configuración de la lista de denegación en la página <DNT>**Application settings**</DNT> para filtrar qué eventos de registro de solicitudes.

## Usando la lista de denegación

Las solicitudes se pueden bloquear de tres formas:

* Para bloquear la grabación de todos los eventos `AjaxRequest` , agregue un asterisco `*` como comodín.
* Para bloquear la grabación del evento `AjaxRequest` en un dominio, ingrese solo el nombre del dominio. Ejemplo: `example.com`
* Para bloquear la grabación del evento `AjaxRequest` en un dominio y ruta específicos, ingrese el dominio y la ruta. Ejemplo: `example.com/path`

La lista de denegación ignora el protocolo, el puerto, la búsqueda y el hash de una URL.

## Accede a la lista de denegados

Para actualizar la lista de URL denegadas que su aplicación filtrará para la creación de eventos, vaya a la página de configuración de la aplicación:

1. Vaya a

   <DNT>
     **[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities) > Browser > (select an app) > Settings > Application settings**
   </DNT>

   .

2. En

   <DNT>
     **Ajax Request Deny List**
   </DNT>

   , agregue los filtros que desea aplicar a su aplicación.

3. Seleccione

   <DNT>
     **Save application settings**
   </DNT>

   para actualizar la configuración del agente.

4. [Vuelva a implementar el agente del navegador](/docs/browser/new-relic-browser/installation/upgrade-browser-agent) (ya sea reiniciando el agente

   <InlinePopover type="apm"/>

   asociado o actualizando la instalación de copiar y pegar del navegador).

## Validación

Para validar si los filtros que ha agregado funcionan como se esperaba, ejecute una consulta NRQL para `AjaxRequest` evento que coincida con su filtro.

```sql
FROM AjaxRequest SELECT * WHERE requestUrl LIKE `%example.com%`
```
