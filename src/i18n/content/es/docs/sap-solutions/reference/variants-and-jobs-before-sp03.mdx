---
title: Programar trabajos antes de V3.0 SP03
metaDescription: Instructions for scheduling collection manager jobs before SP03.
freshnessValidatedDate: never
translationType: machine
---

## Crear variantes para /NEWR/ABAP\_COLLECTION\_MANAGERN

1. Use transaction `SE38` to set up `/NEWR/ABAP_COLLECTION_MANAGERN`.
2. On the <DNT>**ABAP: Variants - Initial Screen**</DNT>, enter `/NEWR/ABAP_COLLECTION_MANAGERN` in the <DNT>**Program**</DNT> field.
3. Seleccione <DNT>**Variant**</DNT> y haga clic en <DNT>**Create**</DNT>.
4. Siga los pasos a continuación para actualizar los valores:

<Tabs>
  <TabsBar>
    <TabsBarItem id="trace-variant">
      variante traza
    </TabsBarItem>

    <TabsBarItem id="idoc-variant">
      Variante de IDOC
    </TabsBarItem>

    <TabsBarItem id="system-variant">
      Variante del SISTEMA
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="trace-variant">
      1. Enter the variant name, e.g., <DNT>**TRACE\_ONLY**</DNT>, and click <DNT>**Create**</DNT>.

      2. Ensure the <DNT>**New Relic Account ID**</DNT> is auto-populated.

      3. Establezca <DNT>**Records per transmission**</DNT> en <DNT>**10000**</DNT>.

      4. Click <DNT>**Multiple Selection (Active)**</DNT> to add data provider names.

      5. Add the following in <DNT>**Select Single Values**</DNT>:

         * `NR.SAP.TRANSACTION`
         * `NR.SAP.TRANSACTION.COMPLETE`
         * `NR.SAP.SYSTEM.PROCESS`

      6. Seleccione <DNT>**Sequential Processing**</DNT> como <DNT>**Execution Mode**</DNT>.

      7. If <DNT>**DIA WP**</DNT> ≥ 30, set to <DNT>**6**</DNT>; otherwise, set to <DNT>**3**</DNT>.

      8. Click <DNT>**Attributes**</DNT>, add a description, and click <DNT>**Save**</DNT>.
    </TabsPageItem>

    <TabsPageItem id="idoc-variant">
      1. Enter the variant name, e.g., <DNT>**IDOC\_ONLY**</DNT>, and click <DNT>**Create**</DNT>.

      2. Ensure the <DNT>**New Relic Account ID**</DNT> is auto-populated.

      3. Establezca <DNT>**Records per transmission**</DNT> en <DNT>**10000**</DNT>.

      4. Click <DNT>**Multiple Selection (Active)**</DNT> to add data provider names.

      5. Add the following in <DNT>**Select Single Values**</DNT>:

         * `NR_SAP_IDOC`
         * `NR_SAP_IDOC_COMPLETE`

      6. Seleccione <DNT>**Sequential Processing**</DNT> como <DNT>**Execution Mode**</DNT>.

      7. Click <DNT>**Attributes**</DNT>, add a description, and click <DNT>**Save**</DNT>.
    </TabsPageItem>

    <TabsPageItem id="system-variant">
      1. Enter the variant name, e.g., <DNT>**System**</DNT>, and click <DNT>**Create**</DNT>.
      2. Ensure the <DNT>**New Relic Account ID**</DNT> is auto-populated.
      3. Establezca <DNT>**Records per transmission**</DNT> en <DNT>**10000**</DNT>.
      4. Click <DNT>**Multiple Selection (Active)**</DNT> to add data provider names.
      5. Add variants from <DNT>**TRACE**</DNT> and <DNT>**IDOC**</DNT> providers in <DNT>**Exclude Single Values**</DNT>.
      6. Seleccione <DNT>**Parallel Processing**</DNT> como <DNT>**Execution Mode**</DNT>.
      7. If <DNT>**DIA WP**</DNT> ≥ 30, set to <DNT>**6**</DNT>; otherwise, set to <DNT>**3**</DNT>.
      8. Click <DNT>**Attributes**</DNT>, add a description, and click <DNT>**Save**</DNT>.
    </TabsPageItem>
  </TabsPages>
</Tabs>

<img title="Variants" alt="A screenshot displaying the Variants" src="/images/parallel-processing.webp" width="" />

## Crear variantes para RSBTONEJOB2

1. Use transaction `SE38` to set up `RSBTONEJOB2`.

<img title="Message Processing" alt="A screenshot displaying the Message Processing" src="/images/cua-message-processing.webp" width="600" />

2. Crea variantes con los siguientes valores:

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Nombre de la variante**
      </th>

      <th>
        **Nombre del trabajo**
      </th>

      <th>
        **Programa**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        **traza**
      </td>

      <td>
        `/NEWR/ONE_COLLECTION`
      </td>

      <td>
        `/NEWR/ABAP_COLLECTION_MANAGERN`
      </td>
    </tr>

    <tr>
      <td>
        **IDOC**
      </td>

      <td>
        `NR_COLLECTION_IDOC`
      </td>

      <td>
        `/NEWR/ABAP_COLLECTION_MANAGERN`
      </td>
    </tr>

    <tr>
      <td>
        **SISTEMA**
      </td>

      <td>
        `NR_COLLECTION_SYSTEM`
      </td>

      <td>
        `/NEWR/ABAP_COLLECTION_MANAGERN`
      </td>
    </tr>
  </tbody>
</table>

## Programar trabajos de recolección

1. Use transaction `SM36` to schedule three jobs.
2. Enter the job name for each job and set the <DNT>**Job Class**</DNT> to <DNT>**C**</DNT>.
3. Click <DNT>**Step**</DNT>, enter program name `RSBTONEJOB2`, and specify the variant for each job.
4. Click <DNT>**Start Condition**</DNT>, set the start date and time, check <DNT>**Periodic Job**</DNT>, click <DNT>**Period Values**</DNT>, then <DNT>**Other Period**</DNT>, and enter <DNT>**5**</DNT> for minutes.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Nombre del trabajo**
      </th>

      <th>
        **Programa**
      </th>

      <th>
        **Variante**
      </th>

      <th>
        **Frecuencia de funcionamiento**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `NR_COLLECTION_TRACE`
      </td>

      <td>
        `RSBTONEJOB2`
      </td>

      <td>
        **traza**
      </td>

      <td>
        Cada 5 minutos
      </td>
    </tr>

    <tr>
      <td>
        `NR_COLLECTION_IDOC`
      </td>

      <td>
        `RSBTONEJOB2`
      </td>

      <td>
        **IDOC**
      </td>

      <td>
        Cada 5 minutos
      </td>
    </tr>

    <tr>
      <td>
        `NR_COLLECTION_SYSTEM`
      </td>

      <td>
        `RSBTONEJOB2`
      </td>

      <td>
        **SISTEMA**
      </td>

      <td>
        Cada 5 minutos
      </td>
    </tr>
  </tbody>
</table>