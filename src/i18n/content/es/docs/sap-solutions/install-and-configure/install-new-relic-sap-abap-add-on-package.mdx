---
title: Instalar el paquete complementario SAP ABAP de New Relic
metaDescription: This section applies to both initial installation and upgrade installation.
freshnessValidatedDate: never
translationType: machine
---

El componente ABAP de <DNT>**NRM4SAP**</DNT> está empaquetado como un complemento de SAP y se puede instalar mediante la transacción `SAINT` en <DNT>**client 000**</DNT>.

## Archivo de instalación del complemento

* <DNT>**Initial installation**</DNT>: descargue el archivo de complemento `K-xxxAOIINNEWR.SAR` a una carpeta en su PC local
* <DNT>**Upgrade from previous version**</DNT>: use el archivo llamado `K-xxxAOUINNEWR.SAP`

## Instalar el archivo complementario de SAP

1. Iniciar sesión en <DNT>**client 000**</DNT>
2. Escriba `SAINT` en el campo de comando <DNT>**SAP GUI**</DNT>
3. Vaya a <DNT>**Installation Package**</DNT> &gt; <DNT>**Load Packages**</DNT>
4. Elija el método adecuado según dónde se encuentre su archivo de instalación:

<DNT>**Which method should you use?**</DNT>

* <DNT>**SAR archive from frontend**</DNT> (Más común): el archivo de instalación está en su computadora local
* <DNT>**SAR archive from application server**</DNT>: el archivo SAR se ha precargado en `/usr/sap/trans/download/`
* <DNT>**EPS files from application server**</DNT>: primero, necesita extraer el archivo SAR en archivos PAT en el servidor

<Tabs>
  <TabsBar>
    <TabsBarItem id="sar-archive-from-frontend">Archivo SAR desde el frontend</TabsBarItem> <TabsBarItem id="sar-archive-from-application-server">Archivo SAR desde el servidor de aplicaciones</TabsBarItem> <TabsBarItem id="eps-files-from-application-server">Archivos EPS desde el servidor de aplicaciones</TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="sar-archive-from-frontend">
      1. Navegar y abrir `K-xxxAOIINNEWR.SAR`
      2. En la ventana emergente <DNT>**Content of the compressed file**</DNT>, haga clic <DNT>**Decompress**</DNT>

      <img title="Content of the compressed file" alt="A screenshot displaying the Content of the compressed file" src="/images/content-of-compressed-file-1.webp" width="80%" />

      3. Verifique que el mensaje <DNT>**File K-xxxAOIINNEWR.SAR successfully transferred, decompressed, and deleted**</DNT> aparezca
    </TabsPageItem>

    <TabsPageItem id="sar-archive-from-application-server">
      1. Asegúrese de que haya un <DNT>**download directory**</DNT> debajo de <DNT>**trans directory**</DNT> en el servidor de la aplicación (`/usr/sap/trans/download`)
      2. Copie el <DNT>**add-on file**</DNT> a este directorio
    </TabsPageItem>

    <TabsPageItem id="eps-files-from-application-server">
      1. Extraiga el <DNT>**SAR file**</DNT> en <DNT>**PAT files**</DNT>. Ejecute los siguientes comandos:

      ```bash
      cd /usr/sap/trans
      cp /tmp/K-xxxAOIINNEWR.SAR /usr/sap/trans
      SAPCAR -xvf K-xxxAOIINNEWR.SAR
      ```

      2. En la ventana emergente <DNT>**Do you want to upload OCS Packages from the EPS inbox?**</DNT>, seleccione <DNT>**Yes**</DNT>
      3. Asegúrese de que el <DNT>**OCS file**</DNT> se cargue sin errores
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Complete la instalación

1. Haga clic en <DNT>**Start**</DNT>, seleccione <DNT>**Installable Add-on packages for your system**</DNT> y haga clic <DNT>**Continue**</DNT>

2. Cuando se le pregunte <DNT>**Do you want to add Modification Adjustment Transports to the queue?**</DNT>:

   * Haga clic en <DNT>**No**</DNT> para la instalación estándar (por defecto)
   * Haga clic en <DNT>**Yes**</DNT> solo si su paquete incluye transportes de ajuste con instrucciones específicas

   <img title="Do you want to add Modification Adjustment Transports to the queue?" alt="A screenshot displaying question about Modification Adjustment Transports" src="/images/modify-adjustement-transports.webp" width="80%" />

3. Haga clic en el botón de marca de verificación verde para continuar.

   <Callout variant="important">
     Si aparece la página <DNT>**Errors during Disassembling OCS Packages**</DNT>, haga clic en <DNT>**Ignore**</DNT>. Este error es esperado para ciertos tipos de paquetes y no afectará la instalación.
   </Callout>

4. Una vez instalado el paquete, haga clic <DNT>**Finish**</DNT>

5. Aparece la ventana emergente <DNT>**Sending the runtime analysis**</DNT>. Seleccione uno de los siguientes:

   * <DNT>**Send**</DNT>: abre un formulario de comentarios con el <DNT>**runtime analysis data**</DNT> en una ventana del navegador local
   * <DNT>**Display**</DNT>: muestra primero el <DNT>**runtime analysis**</DNT>, luego abre el formulario de comentarios
   * <DNT>**Do not send**</DNT>: cierra el cuadro de diálogo sin enviar <DNT>**runtime analysis data**</DNT>

6. Verifique la instalación en la pestaña <DNT>**Installed Components**</DNT>. El <DNT>**New Relic add-on**</DNT> debería aparecer en la lista con un estado que indique una instalación exitosa

7. Cerrar sesión en <DNT>**Client 000**</DNT>