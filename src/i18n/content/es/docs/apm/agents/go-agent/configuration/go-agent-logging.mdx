---
title: Logging del agente Go
tags:
  - Agents
  - Go agent
  - Configuration
metaDescription: You can set up several logging levels for your New Relic Go agent; this is useful for troubleshooting your New Relic Golang integration.
freshnessValidatedDate: never
translationType: machine
---

Logging [New Relic para Go](/docs/agents/go-agent/get-started/new-relic-go) utiliza el [paquete de log de Go](https://golang.org/pkg/log/) estándar y un paquete New Relic `Logger` . El logging es útil para [la resolución de problemas](/docs/agents/go-agent/troubleshooting/no-data-appears-go) de integración de New Relic; por ejemplo, con problemas [de instalación](/docs/agents/go-agent/get-started/install-new-relic-go) o [configuración](/docs/agents/go-agent/instrumentation/go-agent-configuration) .

## Escribir archivo de registro [#write-logfiles]

Para utilizar los métodos del agente Go para escribir archivos de registro y auditoría, consulte [log.go en el repositorio de GitHub del agente](https://github.com/newrelic/go-agent/blob/20541a9393ae651949eb75b82666d4a7c2a10dec/v3/newrelic/log.go).

## Integraciones de Logrus [#logrus]

New Relic ofrece dos integraciones de Logrus para diferentes casos de uso:

### Logging del agente (para mensajes de depuración del agente Go) [#agent-logging]

Utilice esta integración para enviar los mensajes de log internos del agente Go a Logrus. Esto es útil para solucionar problemas del propio agente.

**Integración:** [`nrlogrus`](https://github.com/newrelic/go-agent/tree/master/v3/integrations/nrlogrus)

Este es un ejemplo del uso de la integración de New Relic Logrus para el logging del agente:

1. Importa los paquetes requeridos:

   ```go
   import (
       "os"

       "github.com/newrelic/go-agent/v3/integrations/nrlogrus"
       "github.com/newrelic/go-agent/v3/newrelic"
       "github.com/sirupsen/logrus"
   )
   ```

2. Configura el agente Go para usar Logrus para el logging del agente:

   ```go
   app, err := newrelic.NewApplication(
       newrelic.ConfigAppName("Your Application Name"),
       newrelic.ConfigLicense(os.Getenv("NEW_RELIC_LICENSE_KEY")),
       func(config *newrelic.Config) {
           logrus.SetLevel(logrus.DebugLevel)
           config.Logger = nrlogrus.StandardLogger()
       },
   )
   ```

### Logs de aplicación (logs en contexto) [#application-logging]

Utilice esta integración para enviar los mensajes de log de Logrus de su aplicación a New Relic con correlación automática de trazas. Esto habilita [logs en contexto](/docs/logs/logs-context/configure-logs-context-go/), lo que le permite ver los mensajes de log relacionados con sus errores y trazas directamente en la interfaz de usuario de su aplicación.

**Integración:** [`logcontext-v2/nrlogrus`](https://github.com/newrelic/go-agent/tree/master/v3/integrations/logcontext-v2/nrlogrus)

Este es un ejemplo de uso de la integración de Logrus de New Relic para el logging de aplicaciones:

1. Importa los paquetes requeridos:

   ```go
   import (
       "context"
       "os"

       "github.com/newrelic/go-agent/v3/integrations/logcontext-v2/nrlogrus"
       "github.com/newrelic/go-agent/v3/newrelic"
       "github.com/sirupsen/logrus"
   )
   ```

2. Configure Logrus para usar el formateador de New Relic:

   ```go
   app, err := newrelic.NewApplication(
       newrelic.ConfigAppName("Your Application Name"),
       newrelic.ConfigLicense(os.Getenv("NEW_RELIC_LICENSE_KEY")),
       newrelic.ConfigAppLogForwardingEnabled(true),
   )

   logger := logrus.New()
   logger.SetFormatter(nrlogrus.NewFormatter(app, &logrus.TextFormatter{}))
   ```

3. Utilice el logger en su aplicación con contexto de transacción:

   ```go
   txn := app.StartTransaction("myTransaction")
   defer txn.End()

   ctx := newrelic.NewContext(context.Background(), txn)
   logger.WithContext(ctx).Info("This log will be correlated with the transaction")
   ```

Para más ejemplos, consulte los [ejemplos de logcontext-v2/nrlogrus](https://github.com/newrelic/go-agent/tree/master/v3/integrations/logcontext-v2/nrlogrus/example).

## Otras integraciones de logging admitidas [#other-integrations]

Además de Logrus, New Relic ofrece integraciones para varias otras bibliotecas de logging populares de Go:

### Integraciones de logging del agente [#other-agent-logging]

Estas integraciones envían los mensajes de depuración internos del agente Go a su framework de logging:

* **[nrzap](https://github.com/newrelic/go-agent/tree/master/v3/integrations/nrzap)** - Integración con el logger Zap de Uber
* **[nrslog](https://github.com/newrelic/go-agent/tree/master/v3/integrations/nrslog)** - Integración con el paquete `log/slog` de la biblioteca estándar de Go
* **[nrzerolog](https://github.com/newrelic/go-agent/tree/master/v3/integrations/nrzerolog)** - Integración con Zerolog
* **[nrlogxi](https://github.com/newrelic/go-agent/tree/master/v3/integrations/nrlogxi)** - Integración con Logxi

### Integraciones de logs en contexto [#other-logs-context]

Estas integraciones habilitan logs en contexto para los logs de su aplicación:

* **[logcontext-v2/zerologWriter](https://github.com/newrelic/go-agent/tree/master/v3/integrations/logcontext-v2/zerologWriter)** - Envíe mensajes de log de Zerolog a New Relic con correlación de trazas
* **[logcontext-v2/logWriter](https://github.com/newrelic/go-agent/tree/master/v3/integrations/logcontext-v2/logWriter)** - Envíe mensajes del paquete `log` de la biblioteca estándar a New Relic con correlación de trazas

Cada integración sigue un patrón similar a los ejemplos de Logrus anteriores. Consulte los respectivos repositorios de GitHub para obtener instrucciones de uso detalladas y ejemplos.

## Ver el log de sus datos de infraestructura y APM [#logs-context]

Con [logs en contexto](/docs/logs/logs-context/configure-logs-context-go/), puede ver mensajes de log relacionados con sus errores y trazas directamente en la interfaz de usuario de su aplicación. También puede ver logs en el contexto de sus [datos de infraestructura](/docs/logs/forward-logs/forward-your-logs-using-infrastructure-agent/), como clústeres de Kubernetes. No es necesario cambiar a otra página de la interfaz de usuario.

Para habilitar logs en contexto, utilice una de las [integraciones de logs en contexto](#other-logs-context) listadas anteriormente. Para Logrus específicamente, consulte la integración [`logcontext-v2/nrlogrus`](#application-logging).