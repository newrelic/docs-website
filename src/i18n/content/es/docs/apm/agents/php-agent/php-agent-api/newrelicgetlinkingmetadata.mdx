---
title: newrelic_get_linking_metadata
type: apiDoc
shortDescription: Devuelve una colección de metadatos necesarios para vincular datos a una traza o una entidad.
tags:
  - Agents
  - PHP agent
  - PHP agent API
freshnessValidatedDate: never
translationType: machine
---

## Sintaxis

```php
newrelic_get_linking_metadata()
```

Devuelve una colección de metadatos necesarios para vincular datos a una traza o una entidad.

## Requisitos

Requiere [la versión 9.3 o superior](/docs/release-notes/agent-release-notes/php-release-notes) del agente PHP.

Debe llamarse dentro de una [transacción](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#transaction).

## Descripción

Esta llamada devuelve un mapa opaco de pares de valores principales que se pueden usar para correlacionar esta aplicación con otros datos en el backend de New Relic.

## Ejemplos

### Crear una carga útil de enlace

Obtiene los datos de enlace y los codifica como JSON para exportarlos a herramientas:

```php
if (extension_loaded('newrelic')) {
    $linking_md = newrelic_get_linking_metadata();
    $payload = json_encode($linking_md);
}
```
