---
title: Activar o desactivar atributo
tags:
  - Agents
  - PHP agent
  - Attributes
metaDescription: 'How to include and exclude specific attributes for your New Relic PHP agent, including properties and rules.'
freshnessValidatedDate: never
translationType: machine
---

Conozca las propiedades para habilitar o deshabilitar el atributo y las reglas que utiliza New Relic al determinar qué atributo incluir o excluir para un [destino](/docs/features/agent-attributes#destinations). Esto también incluye un resumen de las propiedades del agente PHP que han quedado obsoletas con el lanzamiento del atributo del agente New Relic.

## Propiedades

Utilice las siguientes propiedades de destino para abrir o cerrar el destino de cualquier colección de atributos:

<CollapserGroup>
  <Collapser
    id="cfg-attributes-enabled"
    title={<InlineCode>newrelic.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo
          </th>

          <td>
            Booleano
          </td>
        </tr>

        <tr>
          <th>
            Por defecto
          </th>

          <td>
            Verdadero
          </td>
        </tr>
      </tbody>
    </table>

    Activa o desactiva todos los atributos en todos los destinos.
  </Collapser>

  <Collapser
    id="cfg-browser-attributes-enabled"
    title={<InlineCode>newrelic.browser_monitoring.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Booleano
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            FALSO
          </td>
        </tr>
      </tbody>
    </table>

    Activa o desactiva todos los atributos de <InlinePopover type="browser"/>. Estos son los datos que se adjuntan al evento de vista de página. Si `newrelic.attributes.enabled` es `false`, no se enviará ningún atributo al monitoreo del navegador independientemente de cómo esté configurada esta propiedad.
  </Collapser>

  <Collapser
    id="cfg-error-attributes-enabled"
    title={<InlineCode>newrelic.error_collector.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Booleano
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Verdadero
          </td>
        </tr>
      </tbody>
    </table>

    Activa o desactiva todos los atributos por errores de traza. Si `newrelic.attributes.enabled` es `false`, no se enviará ningún atributo a los errores de traza independientemente de cómo se establezca esta propiedad.
  </Collapser>

  <Collapser
    id="cfg-events-attributes-enabled"
    title={<InlineCode>newrelic.transaction_events.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Booleano
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Verdadero
          </td>
        </tr>
      </tbody>
    </table>

    Activa o desactiva todos los atributos del evento de transacción. Si `newrelic.attributes.enabled` es `false`, no se enviará ningún atributo al evento de transacción independientemente de cómo esté configurada esta propiedad.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-enabled"
    title={<InlineCode>newrelic.transaction_tracer.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo
          </th>

          <td>
            Booleano
          </td>
        </tr>

        <tr>
          <th>
            Por defecto
          </th>

          <td>
            Verdadero
          </td>
        </tr>
      </tbody>
    </table>

    Activa o desactiva todos los atributos de la traza de la transacción. Si `newrelic.attributes.enabled` es `false`, no se enviará ningún atributo a la traza de la transacción independientemente de cómo esté configurada esta propiedad.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-enabled"
    title={<InlineCode>newrelic.span_events.attributes.enabled</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo
          </th>

          <td>
            Booleano
          </td>
        </tr>

        <tr>
          <th>
            Por defecto
          </th>

          <td>
            Verdadero
          </td>
        </tr>
      </tbody>
    </table>

    Activa o desactiva todos los atributos para `Span` datos. Si `newrelic.attributes.enabled` es `false`, no se enviará ningún atributo a los intervalos, independientemente de cómo esté configurada esta propiedad.
  </Collapser>
</CollapserGroup>

Utilice las siguientes propiedades `.include` o `.exclude` específicas de atributo/destino para agregar o eliminar atributos específicos en destinos específicos:

<CollapserGroup>
  <Collapser
    id="cfg-attributes-include"
    title={<InlineCode>newrelic.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Si los atributos están habilitados, todas las claves de atributos en esta lista se enviarán a New Relic.
  </Collapser>

  <Collapser
    id="cfg-attributes-exclude"
    title={<InlineCode>newrelic.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Todas las claves de atributos en esta lista **no** se enviarán a New Relic.
  </Collapser>

  <Collapser
    id="cfg-bm-attributes-include"
    title={<InlineCode>newrelic.browser_monitoring.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Si el atributo está habilitado para `browser_monitoring`, todas las claves de atributo en esta lista se enviarán en los datos de vista de página.
  </Collapser>

  <Collapser
    id="cfg-bm-attributes-exclude"
    title={<InlineCode>newrelic.browser_monitoring.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Todas las claves de atributos que se encuentran en esta lista **no** se enviarán a New Relic en las visitas a la página.
  </Collapser>

  <Collapser
    id="cfg-ec-attributes-include"
    title={<InlineCode>newrelic.error_collector.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Si los atributos están habilitados para errores de traza, todas las claves de atributos en esta lista se enviarán a New Relic en errores de traza.
  </Collapser>

  <Collapser
    id="cfg-ec-attributes-exclude"
    title={<InlineCode>newrelic.error_collector.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Todas las claves de atributos en esta lista **no** se enviarán a New Relic en caso de errores de traza.
  </Collapser>

  <Collapser
    id="cfg-te-attributes-include"
    title={<InlineCode>newrelic.transaction_events.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Si los atributos están habilitados para el evento de transacción, todas las claves de atributos en esta lista se enviarán a New Relic en el evento de transacción.
  </Collapser>

  <Collapser
    id="cfg-te-attributes-exclude"
    title={<InlineCode>newrelic.transaction_events.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Todas las claves de atributos en esta lista **no** se enviarán a New Relic en el evento de transacción.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-include"
    title={<InlineCode>newrelic.transaction_tracer.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Si los atributos están habilitados para la traza de la transacción, todas las claves de atributos en esta lista se enviarán a New Relic en la traza de la transacción.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-exclude"
    title={<InlineCode>newrelic.transaction_tracer.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Todas las claves de atributos en esta lista **no** se enviarán a New Relic en la traza de la transacción.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-include"
    title={<InlineCode>newrelic.span_events.attributes.include</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Si los atributos están habilitados para el evento span, todas las claves de atributos en esta lista se enviarán a New Relic en el evento span.
  </Collapser>

  <Collapser
    id="cfg-tt-attributes-exclude"
    title={<InlineCode>newrelic.span_events.attributes.exclude</InlineCode>}
  >
    <table>
      <tbody>
        <tr>
          <th>
            Tipo:
          </th>

          <td>
            Lista de cadenas delimitadas por comas
          </td>
        </tr>

        <tr>
          <th>
            Por defecto:
          </th>

          <td>
            Vacío
          </td>
        </tr>
      </tbody>
    </table>

    Todas las claves de atributos en esta lista **no** se enviarán a New Relic en el evento span.
  </Collapser>
</CollapserGroup>

## Reglas de atributos [#attruls]

New Relic sigue estas reglas al determinar qué atributo incluir o excluir para un destino.

<CollapserGroup>
  <Collapser
    id="rule-root-precedence"
    title="El nivel raíz tiene prioridad para habilitado."
  >
    El campo `newrelic.attributes.enabled` prevalece sobre todas las demás configuraciones. Cuando `false`, no se informará ningún atributo a New Relic.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.enabled = false
    newrelic.attributes.include = foo,bar
    newrelic.transaction_tracer.attributes.enabled = true
    ```

    Salida de ejemplo:

    ```
    Keys passed in: password, foo, bar, bat
    Keys included for all destinations:
    Keys excluded for all destinations: password, foo, bar, bat
    ```
  </Collapser>

  <Collapser
    id="rule-destination-precedence"
    title="El destino habilitado tiene prioridad sobre incluir y excluir."
  >
    `newrelic.{destination}.attributes.enabled` Las banderas tienen prioridad sobre las claves de inclusión y exclusión.

    Configuración de ejemplo:

    ```ini
    newrelic.transaction_tracer.attributes.enabled = false
    newrelic.attributes.include = one,two
    newrelic.transaction_tracer.attributes.include = three,four
    ```

    Salida de ejemplo:

    ```
    Keys passed in: one, two, three, four
    Keys included for transaction traces:
    Keys excluded for transaction traces: one, two, three, four
    ```
  </Collapser>

  <Collapser
    id="rule-included-destination"
    title="El atributo se incluye si el destino está habilitado."
  >
    Si un destino está habilitado, todos los atributos del usuario se envían a ese destino de forma predeterminada.

    Todos los atributos de usuario están predeterminados en `true`. Sin embargo, de forma predeterminada, el atributo de solicitud y el parámetro de mensaje están deshabilitados para todos los destinos.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.exclude = myAttKey
    ```

    Salida de ejemplo:

    ```
    Keys passed in: foo, bar, myAttKey
    Keys included: foo, bar
    Keys excluded: myAttKey
    ```
  </Collapser>

  <Collapser
    id="rule-exclude-wins"
    title="Excluir siempre reemplaza a incluir."
  >
    Si la misma clave aparece en las listas de inclusión y exclusión, entonces se excluirá el atributo con la clave especificada.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.include = foo,myCustomAtt
    newrelic.attributes.exclude = password,myCustomAtt
    ```

    Salida de ejemplo:

    ```
    Keys passed in: foo, myCustomAtt, password
    Keys included: foo
    Keys excluded: password, myCustomAtt
    ```
  </Collapser>

  <Collapser
    id="rule-keys-case-sensitive"
    title="Las claves distinguen entre mayúsculas y minúsculas."
  >
    Las claves distinguen entre mayúsculas y minúsculas.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.exclude = password,PaSsWoRd
    ```

    Salida de ejemplo:

    ```
    Keys passed in: password, Password, PASSWORD, PaSsWoRd, PassWORD
    Keys included: Password, PASSWORD, PassWORD
    Keys excluded: password, PaSsWoRd
    ```
  </Collapser>

  <Collapser
    id="rule-star-wildcard"
    title={<>Utilice una estrella (<InlineCode>\*</InlineCode>) como comodín.</>}
  >
    Puede utilizar un asterisco o una estrella (`*`) al final de una clave como comodín. Esto coincidirá con un conjunto de atributos con el mismo prefijo.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.include = custom*
    newrelic.attributes.exclude = request.parameters.*
    ```

    Salida de ejemplo:

    ```
    Keys passed in: custom, custom.key1, custom.key2, request.parameters., request.parameters.foo, request.parameters.bar
    Keys included: custom, custom.key1, custom.key2
    Keys excluded: request.parameters., request.parameters.foo, request.parameters.bar
    ```
  </Collapser>

  <Collapser
    id="rule-specific-wins"
    title="La configuración más específica de una clave tiene prioridad."
  >
    Si varios atributos de inclusión o exclusión afectan a la misma clave, la configuración más específica tendrá prioridad.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.enabled = true
    newrelic.attributes.include = request.parameters.foo
    newrelic.attributes.exclude = request.parameters.*
    ```

    Salida de ejemplo:

    ```
    Keys passed in: request.parameters., request.parameters.foo, request.parameters.bar
    Keys included: request.parameters.foo
    Keys excluded: request.parameters., request.parameters.bar
    ```
  </Collapser>

  <Collapser
    id="rule-destination-include-exclude"
    title="Incluir o excluir afecta al destino específico."
  >
    Si el atributo incluir o excluir se especifica en un destino, solo afecta a ese destino.

    Configuración de ejemplo:

    ```ini
    newrelic.attributes.include = foo
    newrelic.transaction_events.attributes.exclude = foo
    ```

    Salida de ejemplo:

    ```
    Keys passed in: foo
    Keys included for transaction events: 
    Keys included for other destinations: foo
    Keys excluded for transaction events: foo
    ```
  </Collapser>
</CollapserGroup>

## Propiedades obsoletas [#deprecated]

Las siguientes propiedades han quedado obsoletas. Cambie a la nueva configuración de atributos para estas propiedades cuando actualice su agente PHP.

<table>
  <thead>
    <tr>
      <th width={200}>
        <DNT>
          **Deprecated property**
        </DNT>
      </th>

      <th>
        <DNT>
          **New property**
        </DNT>
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `newrelic.capture_params`
      </td>

      <td>
        `newrelic.attributes.include = request.parameters.*`

        De forma predeterminada, los parámetros de solicitud no se envían a New Relic. Agregue `request.parameters.*` a la lista `newrelic.attributes.include` para activar todos los parámetros de solicitud. La propiedad `newrelic.capture_params` ha quedado obsoleta.
      </td>
    </tr>

    <tr>
      <td>
        `newrelic.ignored_params`
      </td>

      <td>
        `newrelic.attributes.exclude = request.parameters.{name}`

        Agregue cada clave de parámetro de solicitud a la lista `newrelic.attributes.exclude` , delimitada por comas. Asegúrese de anteponer la clave con `request.parameters`. La propiedad `newrelic.ignored_params` ha quedado obsoleta.
      </td>
    </tr>

    <tr>
      <td>
        `newrelic.{destination}.capture_attributes`
      </td>

      <td>
        `newrelic.{destination}.attributes.enabled`

        El antiguo indicador `newrelic.{destination}.capture_attributes` desactivaría la recopilación de atributos del usuario. Esto ha quedado obsoleto. Utilice `newrelic.{destination}.attributes.enabled` en su lugar.
      </td>
    </tr>
  </tbody>
</table>
