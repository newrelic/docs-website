---
title: newrelic_get_browser_timing_header (API del agente PHP)
type: apiDoc
shortDescription: Devuelve un fragmento de monitoreo del navegador para inyectarlo en el encabezado de la salida HTML.
tags:
  - Agents
  - PHP agent
  - PHP agent API
metaDescription: New Relic PHP agent API call to instrument a webpage with New Relic. Pair with newrelic_get_browser_timing_footer.
freshnessValidatedDate: never
translationType: machine
---

## Sintaxis

```php
newrelic_get_browser_timing_header([bool $include_tags])
```

Devuelve un fragmento <InlinePopover type="browser"/>para insertar en el encabezado de la salida HTML.

## Descripción

Devuelve una cadena de JavaScript para insertar en el `<head>` de su salida HTML. Utilice esta llamada con [`newrelic_get_browser_timing_footer`](/docs/agents/php-agent/php-agent-api/newrelic_get_browser_timing_footer) para agregar manualmente monitoreo de navegador a una página web. Si es posible, New Relic recomienda habilitar el monitoreo del navegador a través de la UI de New Relic o copiar y pegar el fragmento de JavaScript. Para obtener instrucciones sobre el uso de estas opciones, consulte [monitoreo de navegador y PHP](/docs/agents/php-agent/features/page-load-timing-php).

<Callout variant="tip">
  Compare [`newrelic_disable_autorum`](/docs/agents/php-agent/php-agent-api/newrelic_disable_autorum), que <DNT>**disables**</DNT> es el script browser en una página.
</Callout>

## Parámetros

<table>
  <thead>
    <tr>
      <th width="25%">
        Parámetro
      </th>

      <th>
        Descripción
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$include_tags`

        _bool_
      </td>

      <td>
        Opcional. El valor predeterminado es `true`.

        Si `true` o se omite, la cadena de JavaScript se incluye en un elemento `<script>` para facilitar su inclusión en el HTML de la página.
      </td>
    </tr>
  </tbody>
</table>

## Valores de retorno

Devuelve la cadena de encabezado JavaScript del agente del navegador.

## Ejemplos

### Instrumento una página web [#webpage]

```php
function example(){
    if (extension_loaded('newrelic')) { // Ensure PHP agent is available
        $newrelic_header = newrelic_get_browser_timing_header();
    }
    
    // Output to page
    if (extension_loaded('newrelic')) { // Ensure PHP agent is available
        $newrelic_footer = newrelic_get_browser_timing_footer();
    }
}
```
