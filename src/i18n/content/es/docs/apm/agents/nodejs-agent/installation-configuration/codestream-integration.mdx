---
title: Métrica a nivel de código
tags:
  - Agents
  - Nodejs agent
  - Configuration
metaDescription: Code-level metrics for Node.js can be displayed in your IDE using the New Relic CodeStream extension.
freshnessValidatedDate: never
translationType: machine
---

Las métricas a nivel de código permiten a los desarrolladores que utilizan la [extensión New Relic CodeStream](/docs/codestream/observability/code-level-metrics) ver los datos de APM mostrados contextualmente en su IDE, junto con los métodos individuales en el código. Esto permite a los desarrolladores ser más proactivos a la hora de abordar los problemas de rendimiento mientras escriben y revisan el código.

## Empezar [#get-started]

Primero, [instale](/docs/codestream/start-here/install-codestream) la extensión New Relic CodeStream en el IDE compatible de su elección e log sesión.

<Callout variant="important">
  Las métricas a nivel de código están disponibles para la versión 9.7.5 o superior del agente Node.js y están habilitadas de forma predeterminada para la versión 10.5.0 y superior. Para cambiar esta configuración, consulte nuestra [documentación](/docs/apm/agents/nodejs-agent/installation-configuration/nodejs-agent-configuration#code-level-metrics).
</Callout>

## Agente Atributo [#attributes]

El agente de Node.js informa y adjunta el siguiente atributo a la mayoría de los intervalos producidos por [el marco web](/docs/apm/agents/nodejs-agent/getting-started/compatibility-requirements-nodejs-agent#support) en su aplicación.

<Callout variant="important">
  En algunos casos, el agente empaquetará directamente el código framework base que se invoca dentro de su aplicación y no se mostrará en su IDE.
</Callout>

<table>
  <thead>
    <tr>
      <th style={{ width: "300px" }}>
        Nombre
      </th>

      <th>
        Descripción
      </th>

      <th>
        Ejemplo
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `code.function`
      </td>

      <td>
        El nombre de la función instrumentada. Será `(anonymous)` para funciones anónimas y de flecha.
      </td>

      <td>
        routeHandler
      </td>
    </tr>

    <tr>
      <td>
        `code.filepath`
      </td>

      <td>
        La ruta absoluta al archivo de código fuente donde se define `code.function` .
      </td>

      <td>
        /usr/local/app/sample-node-app/index.js
      </td>
    </tr>

    <tr>
      <td>
        `code.lineno`
      </td>

      <td>
        El número de línea donde `code.function` está definido en `code.filepath`.
      </td>

      <td>
        639
      </td>
    </tr>

    <tr>
      <td>
        `code.lineno`
      </td>

      <td>
        El número de columna donde `code.function` está definido en `code.filepath`.
      </td>

      <td>
        47
      </td>
    </tr>
  </tbody>
</table>
