---
title: Configuration supplémentaire
metaDescription: Additional configuration instructions for SAP systems
freshnessValidatedDate: never
translationType: machine
---

Effectuez ces tâches de configuration pour chaque système monitoré.

## Rôle d&apos;autorisation de téléchargement [#upload-authorization-role]

1. Utilisez <DNT>**SAP transaction**</DNT> `PFCG`.

2. Accédez à <DNT>**Role**</DNT> &gt; <DNT>**Upload**</DNT> et téléchargez le fichier <DNT>**\_NEWR\_DATA\_PROVIDER.SAP**</DNT> à partir du package d’installation.

3. Confirmez que le rôle `/NEWR/DATA_PROVIDER` se charge.

   <img title="Authorization Role" alt="A screenshot displaying the authorization role upload" src="/images/authorisation-role.webp" width="600" />

4. Sélectionnez l&apos;<img src="/images/change-icon.webp" class="inline" style={{height: "22px", width: "22px"}} alt="change icon in SAP" title="Change icon indicator in SAP" />

   icône après le chargement du rôle.

5. Dans l&apos;onglet <DNT>**Authorizations**</DNT>, sélectionnez<img src="/images/change-icon.webp" class="inline" style={{height: "20px", width: "20px"}} alt="change icon in SAP" title="Change icon indicator in SAP" />

   <DNT>**Change Authorization Data**</DNT>, puis activez-le à l&apos;aide de<img src="/images/activate-option.webp" class="inline" style={{height: "25px", width: "30px"}} alt="activate icon in SAP" title="Activate Icon in SAP">icône.</img>

   <img title="Authorizations Tab" alt="A screenshot displaying the authorization tab and Change Authorization Data in SAP upload" src="/images/sap-authorization-1.webp" width="500" />

6. Si un nom de profil est suggéré, sélectionnez l&apos;icône de coche verte.

7. Vérifiez que <DNT>**Profile(s) were updated**</DNT> apparaît dans la barre d&apos;état.

<Callout variant="important">
  Si vous avez besoin de <DNT>**SLT module monitoring**</DNT> sur le système, chargez également le rôle `/NEWR/SLT_COLLECTOR` à partir du fichier `_NEWR_SLT_COLLECTOR.SAP` dans le package d’installation.
</Callout>

## Créer un nouvel identifiant utilisateur RFC Relic [#create-rfc-user-id]

Après avoir activé le <DNT>**authorization profile**</DNT>, utilisez la transaction `SU01` pour créer un nouveau <DNT>**RFC user ID**</DNT> (par exemple, `NR4SAPDP`). Définissez le <DNT>**user type**</DNT> sur <DNT>**Communication**</DNT> sur les systèmes monitorés (fournisseur de données) et sur <DNT>**System**</DNT> sur le système de monitoring central (CMS). Ajoutez ensuite le rôle `/NEWR/DATA_PROVIDER`.

<img title="Create New RFC User" alt="A screenshot displaying the creation of a new RFC user in SAP" src="/images/rfc-userid.webp" width="600" />

## Destination RFC standard [#standard-rfc-destination]

Utilisez la transaction `SM59` pour créer un <DNT>**RFC destination**</DNT> pour chaque système monitoré.

1. Sélectionnez <DNT>**ABAP Connections**</DNT>, puis sélectionnez l&apos;icône <DNT>**Create**</DNT> pour configurer un nouveau <DNT>**RFC destination**</DNT>.

2. Saisissez un nom de <DNT>**RFC Destination**</DNT> descriptif. Nous recommandons : `NR_<SYSID>CLNT<CLNT#>`

3. Réglez <DNT>**Connection Type**</DNT> sur `3 - ABAP Connection`.

4. Dans l&apos;onglet <DNT>**Technical Settings**</DNT>, saisissez :

   * <DNT>**Target Host**</DNT> (domaine entièrement qualifié ou IP) et <DNT>**Instance No.**</DNT>
   * <DNT>**Gateway Host**</DNT> et <DNT>**Service**</DNT> si nécessaire.

   <img title="Create RFC destinations for monitored systems" alt="A screenshot displaying the how to create RFC destinations for monitored systems" src="/images/rfc-destination.webp" width="600" />

   <Callout variant="tip">
     Utilisez <DNT>**load balancing**</DNT> chaque fois que possible. Cela permet à la collecte de données de continuer même lorsqu&apos;une seule connexion est interrompue. Pour des étapes détaillées, consultez [Setting Up an RFC Destination with Load Balancing](https://help.sap.com/doc/saphelp_nw75/7.5.5/en-US/cd/d9a38c1709414b8bc6a4380c398063/content.htm?no_cache=true) dans la documentation SAP.
   </Callout>

5. Dans l&apos;onglet <DNT>**Logon &amp; Security**</DNT>, entrez le <DNT>**RFC user**</DNT> (`NR4SAPDP`) et le mot de passe.

6. Sélectionnez <DNT>**Save**</DNT>.

7. Allez à <DNT>**Utility &gt; Test &gt; Connection Test**</DNT> et <DNT>**Authorization Test**</DNT>.

   * <DNT>**Expected result**</DNT>: les deux tests affichent un état de réussite.

## Définir la destination HTTP sur le cloud New Relic [#set-http-destination-to-new-relic-cloud]

Utilisez la transaction `SM59` pour configurer <DNT>**HTTPS connections**</DNT> sur le <DNT>**New Relic API endpoints**</DNT>.

<Callout variant="important">
  Créez des <DNT>**RFC destinations**</DNT> individuels pour <DNT>**Events**</DNT>, <DNT>**Logs**</DNT>, <DNT>**Metrics**</DNT> et <DNT>**Traces**</DNT> séparément.
</Callout>

1. Saisissez un <DNT>**Destination Name**</DNT> (par exemple, `NR_API_EVENT`).

2. Définissez <DNT>**Connection Type**</DNT> sur `G` (Connexions HTTP vers un serveur externe).

3. Saisissez <DNT>**Target Host**</DNT>, <DNT>**Service No.**</DNT> comme <DNT>**443**</DNT> (voir [le port standard](/docs/sap-solutions/reference/network-accessibility) pour <DNT>**HTTPS**</DNT>), et <DNT>**Path Prefix**</DNT> (voir [la liste de contrôle de préparation à l’installation](/docs/sap-solutions/reference/pre-installation-checklist/#installation-readiness-checklist) pour plus de détails).

4. Si vous avez besoin d&apos;un proxy, configurez-le dans l&apos;onglet <DNT>**Technical Settings**</DNT>.

5. Dans l&apos;onglet <DNT>**Security Options**</DNT>, sélectionnez l&apos;option <DNT>**Active**</DNT> pour <DNT>**SSL**</DNT>.

   <img title="HTTP destination to New Relic" alt="A screenshot displaying the connection setup for HTTP destination to New Relic" src="/images/http-destination-to-new-relic.webp" width="600" />

6. Si le certificat SSL n’est pas encore configuré, téléchargez les <DNT>**root and intermediate certificates**</DNT> depuis le point de terminaison New Relic dans votre navigateur, et importez-les dans <DNT>**SAP**</DNT> à l’aide de la transaction `STRUST`.

7. Sélectionnez <DNT>**Connection Test**</DNT> pour vérifier la connexion.

   * <DNT>**Expected result**</DNT>: une connexion réussie vous prompt à entrer les données de connexion.
   * <DNT>**If test times out**</DNT>: un pare-feu bloque probablement la connexion. Consultez votre équipe réseau pour vous assurer que l’accès est accordé au <DNT>**New Relic domains**</DNT>.

8. Au prompt de connexion, sélectionnez <DNT>**Cancel**</DNT> pour continuer. Un écran de code de réponse <DNT>**HTTP 4xx**</DNT> confirme que la connexion fonctionne.

9. Répétez cette procédure pour tous les <DNT>**New Relic API endpoints**</DNT>.

## Destination RFC Cloud (BTP) [#cloud-btp-rfc-destination]

Sélectionnez l’onglet pour votre service :

<Tabs>
  <TabsBar>
    <TabsBarItem id="btp-process-integration-runtime">
      <DNT>**BTP process integration runtime**</DNT>
    </TabsBarItem>

    <TabsBarItem id="cloud-alm-api">
      <DNT>**CloudALM API**</DNT>
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="btp-process-integration-runtime">
      1. Ouvrez le <DNT>**Service Key**</DNT> téléchargé pour identifier et enregistrer le `clientId`, le `clientsecret` et le `tokenurl`.
      2. Accédez à l&apos;URL spécifiée dans votre clé de service à l&apos;aide d&apos;un navigateur Web.
      3. À partir de cette page, obtenez les certificats <DNT>**SAP on demand**</DNT>, <DNT>**Digicert global**</DNT>, et <DNT>**Digicert root**</DNT>.

      <img title="BTP process integration runtime" alt="A screenshot displaying the connection setup for BTP process integration runtime" src="/images/btp-process-integration.webp" width="600" />

      4. Accédez au code de transaction `STRUST` et sélectionnez <DNT>**SSL Client (Standard)**</DNT> en mode édition.
      5. Téléchargez individuellement les trois certificats, sélectionnez <DNT>**Add to Certificate List**</DNT> pour chacun, puis enregistrez vos modifications.
      6. Accédez au Tcode `SMICM`, puis sélectionnez <DNT>**Administration &gt; ICM &gt; Exit Soft &gt; Global**</DNT>. L’exécution de cette action déclenche un redémarrage de tous les <DNT>**ICM processes**</DNT> dans le système.

      <img title="Administration ICM" alt="A screenshot displaying the Administration ICM" src="/images/administration-icm.webp" width="600" />

      7. Accédez au système SAP et naviguez jusqu’au code de transaction `SM59`, puis établissez un nouveau <DNT>**RFC connection**</DNT> dans la catégorie <DNT>**HTTP Connections to External Server**</DNT>.
      8. Configurez la destination en divisant l&apos;URL du jeton en ses composants respectifs d&apos;hôte et de préfixe de chemin.
      9. Définissez le numéro de service sur `443` et modifiez le préfixe de chemin en ajoutant `?grant_type=client_credentials`.

      <img title="Service No." alt="A screenshot displaying the Service No." src="/images/service-no.webp" width="600" />

      10. Dans l’onglet <DNT>**Login &amp; Security**</DNT>, sélectionnez <DNT>**Basic Authentication**</DNT> et entrez `clientId` et `clientsecret` comme nom d’utilisateur et mot de passe.
      11. Définissez le statut de <DNT>**Secure Protocol**</DNT> sur <DNT>**Active**</DNT> et enregistrez la configuration.
      12. Sélectionnez <DNT>**Connection Test**</DNT>.
    </TabsPageItem>

    <TabsPageItem id="cloud-alm-api">
      1. Notez les `clientId`, `clientsecret` et `url` du <DNT>**Service Key**</DNT> téléchargé.
      2. Accédez au <DNT>**url**</DNT> dans un navigateur Web et téléchargez les certificats <DNT>**Digicert root**</DNT>, <DNT>**global**</DNT> et <DNT>**SAP on demand**</DNT>.

      <img title="CloudALM API" alt="A screenshot displaying the connection setup for CloudALM API" src="/images/cloudalm-api.webp" width="600" />

      3. Accédez au code de transaction `STRUST` et sélectionnez <DNT>**SSL Client (Standard)**</DNT> en mode édition.
      4. Téléchargez individuellement chacun des trois certificats, puis sélectionnez <DNT>**Add to Certificate List**</DNT> et enregistrez vos modifications.
      5. Accédez au Tcode `SMICM`, puis sélectionnez <DNT>**Administration &gt; ICM &gt; Exit Soft &gt; Global**</DNT>. L’exécution de cette action déclenche un redémarrage de tous les <DNT>**ICM processes**</DNT> du système.

      <img title="Administration ICM" alt="A screenshot displaying the Administration ICM" src="/images/administration-icm.webp" width="600" />

      6. Accédez au code de transaction `SM59` après vous être connecté au système SAP, puis établissez un nouveau <DNT>**RFC connection**</DNT> dans la catégorie <DNT>**HTTP Connections to External Server**</DNT>.
      7. Configurez le <DNT>**Target Host**</DNT> avec l’URL appropriée et définissez le <DNT>**Service No.**</DNT> sur `443`. Définissez le <DNT>**Path Prefix**</DNT> sur `/oauth/token?grant_type=client_credentials`.

      <img title="SM59 Configuration" alt="A screenshot displaying the SM59 configuration" src="/images/sm59-configuration.webp" width="600" />

      8. Accédez à l&apos;onglet <DNT>**Login &amp; Security**</DNT> et choisissez <DNT>**Basic Authentication**</DNT> dans la section <DNT>**Login with user**</DNT>. Fournissez le `clientId` comme nom d&apos;utilisateur et le `clientsecret` comme mot de passe.
      9. Localisez le statut <DNT>**Secure Protocol**</DNT> ci-dessous et définissez-le sur <DNT>**Active**</DNT>.
      10. Appliquez les modifications de configuration en enregistrant, puis sélectionnez <DNT>**Connection Test**</DNT>.
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Créer une connexion à la base de données [#create-database-connection]

Utilisez la transaction `DBACOCKPIT` pour définir le <DNT>**HANA Database connections**</DNT> distant. Dans le volet de gauche, double-cliquez sur <DNT>**Database Connections**</DNT>, puis sélectionnez <DNT>**Add**</DNT> pour créer une nouvelle connexion.

<img title="Database Connection" alt="A screenshot displaying the Database Connection" src="/images/database-connection.webp" width="600" />

Sous <DNT>**Database Details**</DNT>, effectuez les étapes suivantes :

1. Saisissez le <DNT>**Connection Name**</DNT> (nous recommandons d’utiliser le <DNT>**SAP system ID**</DNT>).

2. Sélectionnez <DNT>**HANA Database**</DNT> comme <DNT>**Database System**</DNT> désigné.

3. Fournissez un identifiant d’utilisateur de base de données (`NRAGENT` est l’identifiant recommandé), puis entrez et confirmez votre mot de passe.

4. Saisissez le <DNT>**Database Host**</DNT> et le <DNT>**SQL Port**</DNT>.

   <img title="SAP Connection Name" alt="A screenshot displaying the SAP Connection Name" src="/images/connection-name.webp" width="600" />

5. Après avoir enregistré vos entrées, sélectionnez <DNT>**Test**</DNT> pour vérifier la connexion à la base de données.

   * <DNT>**Expected result**</DNT>: le test de connexion indique un statut de réussite.