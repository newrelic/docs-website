---
title: Monitorer NGINX sur Kubernetes avec OpenTelemetry
metaDescription: Send your NGINX metrics from Kubernetes to New Relic using the OpenTelemetry Collector.
freshnessValidatedDate: never
translationType: machine
---

Monitorez vos serveurs NGINX fonctionnant dans des clusters Kubernetes en utilisant le collecteur NRDOT (recommandé) ou le collecteur OpenTelemetry contrib pour envoyer des métriques et des données télémétriques à New Relic.

Cette intégration spécifique à Kubernetes découvre automatiquement les pods NGINX dans votre cluster et collecte des métriques sans configuration manuelle pour chaque instance. Elle exploite le [nginxreceiver](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/receiver/nginxreceiver) et le [receivercreator](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/receiver/receivercreator) OpenTelemetry pour monitorer dynamiquement les métriques de performance NGINX, les statistiques de connexion et l&apos;état de santé du serveur dans votre environnement conteneurisé.

## Configurer le monitoring NGINX [#setup]

Choisissez votre collecteur préféré et suivez les étapes :

<Tabs>
  <TabsBar>
    <TabsBarItem id="nrdot-k8s-prereqs">
      Collecteur NRDOT
    </TabsBarItem>

    <TabsBarItem id="otelcol-k8s-prereqs">
      OpenTelemetry Collector Contrib
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="nrdot-k8s-prereqs">
      ## Avant de commencer [#prerequisites-nrdot]

      Assurez-vous d&apos;avoir :

      * [Clé de licence](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)New Relic valide
      * Activez le module [HTTP stub status](https://nginx.org/en/docs/http/ngx_http_stub_status_module.html) sur le pod NGINX qui doit être monitoré
      * Ajoutez les étiquettes `app` et `role` à chaque pod NGINX qui doit être monitoré
      * Terminez l&apos;installation de base du [manifeste Kubernetes OpenTelemetry](/docs/kubernetes-pixie/k8s-otel/install/#manifest-install)

      ## Configurer le collecteur NRDOT [#install-nrdot-collector]

      Installer le collecteur NRDOT à l&apos;aide de manifestes Kubernetes. La prise en charge des charts Helm sera bientôt disponible.

      <CollapserGroup>
        <Collapser id="nrdot-k8s-manifest-install" title="Installation via manifeste">
          Après avoir terminé l&apos;installation du manifeste Kubernetes OpenTelemetry de base, configurez le monitoring de NGINX en suivant ces étapes :

          1. Mettre à jour l&apos;image du collecteur pour utiliser le collecteur NRDOT.

             Dans les fichiers [`deployment.yaml`](https://github.com/newrelic/helm-charts/blob/master/charts/nr-k8s-otel-collector/examples/k8s/rendered/deployment.yaml) et [`daemonset.yaml`](https://github.com/newrelic/helm-charts/blob/master/charts/nr-k8s-otel-collector/examples/k8s/rendered/daemonset.yaml) de votre répertoire local `rendered`, mettez à jour l&apos;image vers :

             ```yaml
             image: newrelic/nrdot-collector:latest
             ```

          2. Mettez à jour le [`deployment-configmap.yaml`](https://github.com/newrelic/helm-charts/blob/master/charts/nr-k8s-otel-collector/examples/k8s/rendered/deployment-configmap.yaml) pour le monitoring de NGINX :

             Choisissez l&apos;une des options de configuration suivantes en fonction de vos exigences de monitoring :

             <Collapser id="option1-nginx-only" title="Monitoring NGINX uniquement">
               <Callout variant="important">
                 Cette option monitore uniquement NGINX et supprime la collecte d&apos;autres métriques Kubernetes. Vous supprimerez les collecteurs supplémentaires plus tard pour éviter l&apos;ingestion de métriques indésirables.
               </Callout>

               Remplacez le contenu sous [`deployment-config.yaml: |`](https://github.com/newrelic/helm-charts/blob/4177f3cd73b73a82884206afc83963b504d51111/charts/nr-k8s-otel-collector/examples/k8s/rendered/deployment-configmap.yaml#L15) par la configuration spécifique à NGINX ci-dessous :

               ```yaml
               extensions:
                 health_check:
                   endpoint: 0.0.0.0:13133
                 k8s_observer:
                   auth_type: serviceAccount
                   observe_pods: true
                   observe_nodes: true

               receivers:
                 receiver_creator/nginx:
                   watch_observers: [k8s_observer]
                   receivers:
                     nginx:
                       rule: type == "pod" && labels["app"] == "nginx" && labels["role"] == "reverse-proxy"  # Update with your labels
                       config:
                         endpoint: 'http://`endpoint`:<YOUR_STUB_STATUS_PORT>/<YOUR_STUB_STATUS_PATH>'  # Update port and path as needed
                         metrics:
                           nginx.requests:
                             enabled: true
                           nginx.connections_accepted:
                             enabled: true
                           nginx.connections_handled:
                             enabled: true
                           nginx.connections_current:
                             enabled: true
                         collection_interval: 30s
                       resource_attributes:
                         nginx.server.endpoint: 'http://`endpoint`:<YOUR_STUB_STATUS_PORT>/<YOUR_STUB_STATUS_PATH>'  # Update port and path as needed
                         nginx.port: '<YOUR_STUB_STATUS_PORT>'  # Update to match your configuration

               processors:
                 batch:
                   send_batch_max_size: 1000
                   timeout: 30s
                   send_batch_size: 800

                 memory_limiter:
                   check_interval: 1s
                   limit_percentage: 80
                   spike_limit_percentage: 25

                 resource/cluster:
                   attributes:
                     - key: k8s.cluster.name
                       value: "<CLUSTER_NAME>"  # Replace with your cluster name
                       action: insert

                 transform/nginx:
                   metric_statements:
                     - context: resource
                       statements:
                         - set(attributes["nginx.display.name"], Concat([
                             "server",
                             "k8s",
                             attributes["k8s.cluster.name"],
                             attributes["k8s.namespace.name"],
                             "pod",
                             attributes["k8s.pod.name"],
                             "nginx",
                             attributes["nginx.port"]
                           ], ":"))
                         - set(attributes["nginx.deployment.name"], attributes["k8s.pod.name"])
                 
                 transform/metadata_nullify:
                   metric_statements:
                     - context: metric
                       statements:
                         - set(description, "")
                         - set(unit, "")

               exporters:
                 otlp_http/newrelic:
                   endpoint: "<YOUR_NEWRELIC_OTLP_ENDPOINT>"
                   headers:
                     api-key: ${env:NR_LICENSE_KEY}

               service:
                 extensions: [health_check, k8s_observer]
                 pipelines:
                   metrics/nginx:
                     receivers: [receiver_creator/nginx]
                     processors: [batch, resource/cluster, transform/nginx, transform/metadata_nullify, memory_limiter]
                     exporters: [otlp_http/newrelic]
               ```

               #### Paramètres de configuration

               Le tableau suivant décrit les principaux paramètres de configuration :

               <table>
                 <thead>
                   <tr>
                     <th>
                       paramètres
                     </th>

                     <th>
                       Description
                     </th>
                   </tr>
                 </thead>

                 <tbody>
                   <tr>
                     <td>
                       `<YOUR_STUB_STATUS_PORT>`
                     </td>

                     <td>
                       Remplacez par votre port de statut stub NGINX (par exemple 80, 8080)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `<YOUR_STUB_STATUS_PATH>`
                     </td>

                     <td>
                       Remplacez par le chemin de votre statut stub NGINX (par exemple basic_status)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `<CLUSTER_NAME>`
                     </td>

                     <td>
                       Remplacez par le nom de votre cluster Kubernetes pour l&apos;identification dans New Relic
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `<YOUR_NEWRELIC_OTLP_ENDPOINT>`
                     </td>

                     <td>
                       Mettez à jour avec le point de terminaison OTLP de votre région. Consultez la 

                       [documentation sur les points de terminaison OTLP](/docs/opentelemetry/best-practices/opentelemetry-otlp/#configure-endpoint-port-protocol)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `app`

                        et 

                       `role`

                        labels
                     </td>

                     <td>
                       Labels de pod utilisés pour identifier les pods NGINX (mettez à jour la règle pour correspondre à vos labels)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `collection_interval`
                     </td>

                     <td>
                       Intervalle en secondes pour collecter les métriques. La valeur par défaut est définie sur 

                       `30s`
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `send_batch_max_size`
                     </td>

                     <td>
                       Nombre maximal de métriques à regrouper en lot avant l&apos;envoi. La valeur par défaut est définie sur 

                       `1000`
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `timeout`
                     </td>

                     <td>
                       Délai d&apos;attente en secondes avant d&apos;envoyer les métriques groupées. La valeur par défaut est définie sur 

                       `30s`
                     </td>
                   </tr>
                 </tbody>
               </table>
             </Collapser>

             <Collapser id="option2-k8s-nginx" title="Monitoring K8s + NGINX">
               Ajoutez les sections suivantes à votre `deployment-configmap.yaml` existant :

               Extensions à ajouter :

               ```yaml
               extensions:
                 health_check:
                   endpoint: 0.0.0.0:13133
                 k8s_observer:
                   auth_type: serviceAccount
                   observe_pods: true
                   observe_nodes: true
               ```

               Récepteurs à ajouter :

               ```yaml
               receivers:
                 receiver_creator/nginx:
                   watch_observers: [k8s_observer]
                   receivers:
                     nginx:
                       rule: type == "pod" && labels["app"] == "nginx" && labels["role"] == "reverse-proxy"  # Update with your labels
                       config:
                         endpoint: 'http://`endpoint`:<YOUR_STUB_STATUS_PORT>/<YOUR_STUB_STATUS_PATH>'  # Update port and path as needed
                         metrics:
                           nginx.requests:
                             enabled: true
                           nginx.connections_accepted:
                             enabled: true
                           nginx.connections_handled:
                             enabled: true
                           nginx.connections_current:
                             enabled: true
                         collection_interval: 30s
                       resource_attributes:
                         nginx.server.endpoint: 'http://`endpoint`:<YOUR_STUB_STATUS_PORT>/<YOUR_STUB_STATUS_PATH>'  # Update port and path as needed
                         nginx.port: '<YOUR_STUB_STATUS_PORT>'  # Update to match your configuration
               ```

               Processeurs à ajouter :

               ```yaml
               processors:
                 resource/cluster:
                   attributes:
                     - key: k8s.cluster.name
                       value: "<CLUSTER_NAME>"  # Replace with your cluster name
                       action: insert

                 transform/nginx:
                   metric_statements:
                     - context: resource
                       statements:
                         - set(attributes["nginx.display.name"], Concat([
                             "server",
                             "k8s",
                             attributes["k8s.cluster.name"],
                             attributes["k8s.namespace.name"],
                             "pod",
                             attributes["k8s.pod.name"],
                             "nginx",
                             attributes["nginx.port"]
                           ], ":"))
                         - set(attributes["nginx.deployment.name"], attributes["k8s.pod.name"])
                 
                 transform/metadata_nullify:
                   metric_statements:
                     - context: metric
                       statements:
                         - set(description, "")
                         - set(unit, "")
               ```

               Pipelines de service à ajouter :

               ```yaml
               service:
                 extensions: [health_check, k8s_observer]  # Add to existing extensions
                 pipelines:
                   metrics/nginx:
                     receivers: [receiver_creator/nginx]
                     processors: [batch, resource/cluster, transform/nginx, transform/metadata_nullify, memory_limiter]
                     exporters: [otlphttp/newrelic]
               ```

               #### Paramètres de configuration

               Le tableau suivant décrit les principaux paramètres de configuration :

               <table>
                 <thead>
                   <tr>
                     <th>
                       paramètres
                     </th>

                     <th>
                       Description
                     </th>
                   </tr>
                 </thead>

                 <tbody>
                   <tr>
                     <td>
                       `<YOUR_STUB_STATUS_PORT>`
                     </td>

                     <td>
                       Remplacez par votre port de statut stub NGINX (par exemple 80, 8080)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `<YOUR_STUB_STATUS_PATH>`
                     </td>

                     <td>
                       Remplacez par le chemin de votre statut stub NGINX (par exemple basic_status)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `<CLUSTER_NAME>`
                     </td>

                     <td>
                       Remplacez par le nom de votre cluster Kubernetes pour l&apos;identification dans New Relic
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `app`

                        et 

                       `role`

                        labels
                     </td>

                     <td>
                       Labels de pod utilisés pour identifier les pods NGINX (mettez à jour la règle pour correspondre à vos labels)
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `collection_interval`
                     </td>

                     <td>
                       Intervalle en secondes pour collecter les métriques. La valeur par défaut est définie sur 

                       `30s`
                     </td>
                   </tr>

                   <tr>
                     <td>
                       `memory_limiter`
                     </td>

                     <td>
                       Processeur utilisé dans la configuration Kubernetes existante pour limiter l&apos;utilisation de la mémoire
                     </td>
                   </tr>
                 </tbody>
               </table>
             </Collapser>

          3. Appliquez les manifestes mis à jour et redémarrez le déploiement.

             1. Pour le monitoring NGINX uniquement, exécutez ces commandes :

             ```bash
             kubectl apply -n newrelic -R -f rendered
             kubectl delete daemonset nr-k8s-otel-collector-daemonset -n newrelic
             kubectl delete deployment nr-k8s-otel-collector-kube-state-metrics -n newrelic
             kubectl rollout restart deployment nr-k8s-otel-collector-deployment -n newrelic
             ```

             2. Pour le monitoring de K8s + NGINX, exécutez ces commandes :

             ```bash
             kubectl apply -n newrelic -R -f rendered
             kubectl rollout restart deployment nr-k8s-otel-collector-deployment -n newrelic
             ```
        </Collapser>

        <Collapser id="nrdot-k8s-helm-install" title="Installation Helm">
          La prise en charge des charts Helm pour le collecteur NRDOT avec le monitoring NGINX sera bientôt disponible.
        </Collapser>
      </CollapserGroup>
    </TabsPageItem>

    <TabsPageItem id="otelcol-k8s-prereqs">
      ## Avant de commencer [#prerequisites-otel]

      Assurez-vous d&apos;avoir :

      * [Clé de licence](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)New Relic valide
      * Activez le module [HTTP stub status](https://nginx.org/en/docs/http/ngx_http_stub_status_module.html) sur le pod NGINX qui doit être monitoré
      * Ajoutez les étiquettes `app` et `role` à chaque pod NGINX qui doit être monitoré
      * [Helm](https://helm.sh/docs/intro/install/) installé

      ## Configurer le OpenTelemetry Collector [#install-otelcol-collector]

      Déployez le collecteur OpenTelemetry sur votre cluster Kubernetes à l&apos;aide de Helm. Le collecteur découvrira et extraira automatiquement les métriques de vos pods NGINX.

      <CollapserGroup>
        <Collapser id="create-values-yaml-otelcol" title="Étape 1 : créer une configuration values.yaml personnalisée">
          Téléchargez ou créez un fichier `values.yaml` personnalisé basé sur le [values.yaml de l&apos;OpenTelemetry Collector](https://github.com/open-telemetry/opentelemetry-helm-charts/blob/main/charts/opentelemetry-collector/values.yaml).

          Mettez à jour les sections suivantes dans votre fichier `values.yaml` :

          1. Définir le mode sur déploiement :

             ```yaml
             mode: deployment
             ```

          2. Remplacez le référentiel d&apos;images :

             ```yaml
             image:
               repository: otel/opentelemetry-collector-contrib
             ```

          3. Configurer le rôle de cluster :

             ```yaml
             clusterRole:
               create: true
               rules:
                 - apiGroups: [""]
                   resources: ["pods", "nodes", "nodes/stats", "nodes/proxy"]
                   verbs: ["get", "list", "watch"]
                 - apiGroups: ["apps"]
                   resources: ["replicasets"]
                   verbs: ["get", "list", "watch"]
             ```

          4. Configurer les limites de ressources :

             ```yaml
             resources:
               limits:
                 cpu: 250m
                 memory: 512Mi
             ```

          5. Remplacez toute la section de configuration par la configuration de monitoring NGINX :

             ```yaml
                config:
                  extensions:
                    health_check:
                      endpoint: 0.0.0.0:13133
                    k8s_observer:
                      auth_type: serviceAccount
                      observe_pods: true
                      observe_nodes: true

                  receivers:
                    receiver_creator/nginx:
                      watch_observers: [k8s_observer]
                      receivers:
                        nginx:
                          rule: type == "pod" && labels["app"] == "nginx" && labels["role"] == "reverse-proxy" # Update with your labels
                          config:
                            endpoint: 'http://`endpoint`:<YOUR_STUB_STATUS_PORT>/<YOUR_STUB_STATUS_PATH>'  # Update port and path as needed
                            metrics:
                              nginx.requests:
                                enabled: true
                              nginx.connections_accepted:
                                enabled: true
                              nginx.connections_handled:
                                enabled: true
                              nginx.connections_current:
                                enabled: true
                            collection_interval: 30s
                          resource_attributes:
                            nginx.server.endpoint: 'http://`endpoint`:<YOUR_STUB_STATUS_PORT>/<YOUR_STUB_STATUS_PATH>'  # Update port and path as needed
                            nginx.port: '<YOUR_STUB_STATUS_PORT>'  # Update to match your configuration

                  processors:
                    batch:
                      send_batch_size: 1024
                      timeout: 30s

                    resource/cluster:
                      attributes:
                        - key: k8s.cluster.name
                          value: "<CLUSTER_NAME>"  # Replace with your cluster name
                          action: insert

                    transform/nginx:
                      metric_statements:
                        - context: resource
                          statements:
                            - set(attributes["nginx.display.name"], Concat([
                                "server",
                                "k8s",
                                attributes["k8s.cluster.name"],
                                attributes["k8s.namespace.name"],
                                "pod",
                                attributes["k8s.pod.name"],
                                "nginx",
                                attributes["nginx.port"]
                                ], ":"))
                            - set(attributes["nginx.deployment.name"], attributes["k8s.pod.name"])

                    transform/metadata_nullify:
                      metric_statements:
                        - context: metric
                          statements:
                            - set(description, "")
                            - set(unit, "")

                  exporters:
                    otlp_http/newrelic:
                      endpoint: "<YOUR_NEWRELIC_OTLP_ENDPOINT>"  # Update for your region
                      headers:
                        api-key: "<YOUR_NEW_RELIC_LICENSE_KEY>"  # Replace with your New Relic license key

                  service:
                    extensions: [health_check, k8s_observer]
                    pipelines:
                      metrics/nginx:
                        receivers: [receiver_creator/nginx]
                        processors: [batch, resource/cluster, transform/nginx, transform/metadata_nullify]
                        exporters: [otlp_http/newrelic]
             ```

          #### Paramètres de configuration

          Le tableau suivant décrit les principaux paramètres de configuration :

          <table>
            <thead>
              <tr>
                <th>
                  paramètres
                </th>

                <th>
                  Description
                </th>
              </tr>
            </thead>

            <tbody>
              <tr>
                <td>
                  `<YOUR_STUB_STATUS_PORT>`
                </td>

                <td>
                  Remplacez par votre port de statut stub NGINX (par exemple 80, 8080)
                </td>
              </tr>

              <tr>
                <td>
                  `<YOUR_STUB_STATUS_PATH>`
                </td>

                <td>
                  Remplacez par le chemin de votre statut stub NGINX (par exemple basic_status)
                </td>
              </tr>

              <tr>
                <td>
                  `<CLUSTER_NAME>`
                </td>

                <td>
                  Remplacez par le nom de votre cluster Kubernetes pour l&apos;identification dans New Relic
                </td>
              </tr>

              <tr>
                <td>
                  `<YOUR_NEWRELIC_OTLP_ENDPOINT>`
                </td>

                <td>
                  Mettez à jour avec le point de terminaison OTLP de votre région. Consultez la 

                  [documentation sur les points de terminaison OTLP](/docs/opentelemetry/best-practices/opentelemetry-otlp/#configure-endpoint-port-protocol)
                </td>
              </tr>

              <tr>
                <td>
                  `<YOUR_NEW_RELIC_LICENSE_KEY>`
                </td>

                <td>
                  Remplacez par votre clé de licence New Relic
                </td>
              </tr>

              <tr>
                <td>
                  `app`

                   et 

                  `role`

                   labels
                </td>

                <td>
                  Labels de pod utilisés pour identifier les pods NGINX (mettez à jour la règle pour correspondre à vos labels)
                </td>
              </tr>

              <tr>
                <td>
                  `basic_status`
                </td>

                <td>
                  Chemin du point de terminaison de statut stub NGINX (mettez à jour si vous utilisez un chemin différent)
                </td>
              </tr>

              <tr>
                <td>
                  `collection_interval`
                </td>

                <td>
                  Intervalle en secondes pour collecter les métriques. La valeur par défaut est définie sur 

                  `30s`
                </td>
              </tr>

              <tr>
                <td>
                  `send_batch_size`
                </td>

                <td>
                  Nombre de métriques à regrouper par lot avant l&apos;envoi. La valeur par défaut est définie sur 

                  `1024`
                </td>
              </tr>

              <tr>
                <td>
                  `timeout`
                </td>

                <td>
                  Délai d&apos;attente en secondes avant d&apos;envoyer les métriques groupées. La valeur par défaut est définie sur 

                  `30s`
                </td>
              </tr>
            </tbody>
          </table>
        </Collapser>

        <Collapser id="install-helm-otelcol" title="Étape 2 : installer avec Helm">
          Suivez le [guide d&apos;installation du chart Helm OpenTelemetry Collector](https://github.com/open-telemetry/opentelemetry-helm-charts/blob/main/charts/opentelemetry-collector/README.md#opentelemetry-collector-helm-chart) pour installer le collecteur à l&apos;aide de votre fichier `values.yaml` personnalisé.

          Exemples de commandes :

          ```bash
          helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
          helm repo update
          helm upgrade my-opentelemetry-collector open-telemetry/opentelemetry-collector -f your-custom-values.yaml -n newrelic --create-namespace --install
          ```
        </Collapser>

        <Collapser id="verify-deployment-otelcol" title="Étape 3 : vérifier le déploiement et la collecte de données">
          1. Vérifiez que les pods sont en cours d&apos;exécution :

             ```bash
             kubectl get pods -n newrelic --watch
             ```

             Vous devriez voir les pods du Collector OpenTelemetry dans un état `Running` dans l&apos;espace de nommage `newrelic`.

          2. Exécutez une requête NRQL dans New Relic pour vérifier la collecte des données. Remplacez le nom du cluster par le nom réel de votre cluster :

             ```sql
             FROM Metric
             SELECT *
             WHERE metricName LIKE 'nginx.%'
               AND instrumentation.provider = 'opentelemetry'
               AND k8s.cluster.name = 'your-cluster-name'
             SINCE 10 minutes ago
             ```
        </Collapser>
      </CollapserGroup>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Affichez vos données dans New Relic [#find-data]

Une fois votre configuration terminée et que les données circulent, vous pouvez accéder à vos métriques NGINX dans les dashboards New Relic et créer des alertes personnalisées.

Pour des instructions complètes sur l&apos;accès aux dashboards, l&apos;interrogation des données avec NRQL et la création d&apos;alertes, consultez [Trouver et interroger vos données NGINX](/docs/opentelemetry/integrations/nginx/find-and-query-your-data).

## Référence des métriques et des attributs [#metrics]

Cette intégration collecte les mêmes métriques NGINX de base que le déploiement sur l&apos;hôte, avec des attributs de ressource spécifiques à Kubernetes supplémentaires pour l&apos;identification du cluster, de l&apos;espace de noms et du pod.

**Pour une référence complète des métriques et attributs :** Consultez la [référence des métriques et attributs NGINX OpenTelemetry](/docs/opentelemetry/integrations/nginx/nginx-otel-metrics-reference/) pour des descriptions détaillées de toutes les métriques, types et attributs de ressources pour les déploiements Kubernetes.

## Prochaines étapes [#next-steps]

**Explorer le monitoring connexe :**

* [Monitorer NGINX Plus avec OpenTelemetry](/docs/opentelemetry/integrations/nginx-plus/nginx-plus-otel/) - Pour les déploiements commerciaux de NGINX Plus
* [Monitorer NGINX auto-hébergé avec OpenTelemetry](/docs/opentelemetry/integrations/nginx/nginx-otel-host/) - Pour les déploiements de serveurs traditionnels

**Ressources spécifiques à Kubernetes :**

* [OpenTelemetry Collector sur Kubernetes](https://opentelemetry.io/docs/kubernetes/) - Configurations avancées du collecteur