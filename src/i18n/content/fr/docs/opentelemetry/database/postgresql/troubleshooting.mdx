---
title: Dépannage du monitoring PostgreSQL NRDOT
tags:
  - OpenTelemetry
  - NRDOT
  - PostgreSQL
  - Troubleshooting
metaDescription: Troubleshoot common issues with PostgreSQL monitoring using NRDOT Collector
freshnessValidatedDate: never
translationType: machine
---

<Callout title="Aperçu">
  Nous travaillons toujours sur cette fonctionnalité, mais nous aimerions que vous l&apos;essayiez !

  Cette fonctionnalité est actuellement fournie dans le cadre d&apos;un aperçu conformément à nos [politiques de pré-sortie](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

Résoudre les problèmes courants que vous pouvez rencontrer lors du monitoring de votre PostgreSQL avec le NRDOT Collector.

## Tester la connectivité

### Testez la connectivité réseau vers PostgreSQL

```bash
psql --host=<YOUR_DB_HOST> --port=<YOUR_DB_PORT> --username=<YOUR_DB_USERNAME> --dbname=<YOUR_DATABASE_NAME> --command 'SELECT 1;'
```

Une connexion et un résultat réussis confirment que l&apos;utilisateur de monitoring, l&apos;hôte et le port sont accessibles avant le dépannage du collecteur lui-même.

### Tester la connectivité du point de terminaison OTLP

Vérifiez que le Collector NRDOT peut atteindre le point de terminaison OTLP de New Relic :

```bash
curl -v <YOUR_NEWRELIC_OTLP_ENDPOINT>
```

## Symptômes courants

| Symptôme                                                                                                        | Cause probable                                                                                                                                                         |
| --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Aucun événement `db.server.query_sample` / `db.server.top_query`                                                | Le bloc `events` n&apos;est pas activé dans la configuration du Récepteur (les deux sont désactivés par défaut)                                                        |
| `db.server.top_query` événements totalement manquants                                                           | `pg_stat_statements` Extension non créée dans cette base de données                                                                                                    |
| `db.server.query_sample` détail de session bloquante manquant                                                   | L’utilisateur de monitoring manque de `pg_monitor`                                                                                                                     |
| Requêtes longues tronquées dans `db.server.query_sample`                                                        | `track_activity_query_size` par défaut (1 024 octets) — augmentez-le, voir [Compatibilité et prérequis](/docs/opentelemetry/database/postgresql/compatibility#params)  |
| `postgresql.function.calls` toujours vide, ou widget &quot;Appels de fonction/min&quot; du dashboard vide       | `track_functions` non défini sur `pl` ou `all` dans `postgresql.conf`                                                                                                  |
| Plusieurs widgets de dashboard vides malgré le flux de métriques                                                | Ces métriques sont désactivées par défaut — consultez la [Référence des métriques](/docs/opentelemetry/database/postgresql/metrics-reference#dashboard)                |
| `db.query.comment_tags.nr_service_guid` toujours vide                                                           | `allowed_comment_keys: [nr_service_guid]` non défini sur `query_sample_collection` / `top_query_collection` — vide par défaut                                          |
| Métriques de table/index/fonction manquantes pour certaines tables                                              | L’utilisateur de monitoring ne dispose pas de `SELECT` sur ces tables/schémas — `pg_monitor` accorde la visibilité des statistiques, pas au niveau de l’objet `SELECT` |
| `postgresql.wal.lag` / `.wal.age` manquant                                                                      | Nécessite l’activation de WAL avec au moins un réplica connecté — omis silencieusement sur les instances autonomes/sources uniquement, non mis à zéro                  |
| L’entité PostgreSQL n’apparaît pas, ou la relation app→PostgreSQL ne se résout pas dans l’Explorateur d’entités | `server.address`/`server.port` n’est pas présent sur la ressource ; le Récepteur seul n’émet pas ces éléments sous le modèle de ressource par défaut (legacy)          |

## Documentation associée [#related]

<DocTiles>
  <DocTile title="Instrumentation dans les environnements auto-hébergés" path="/docs/opentelemetry/database/postgresql/hosted">
    Découvrez comment configurer le monitoring PostgreSQL dans des environnements auto-hébergés avec New Relic.
  </DocTile>

  <DocTile title="Instrumentation dans les environnements RDS" path="/docs/opentelemetry/database/postgresql/rds">
    Découvrez comment configurer le monitoring de PostgreSQL dans les environnements RDS avec New Relic.
  </DocTile>

  <DocTile title="Référence des métriques" path="/docs/opentelemetry/database/postgresql/metrics-reference">
    Découvrez les métriques disponibles collectées par le collecteur NRDOT.
  </DocTile>
</DocTiles>