---
title: Configurer les paramètres monitoring des applications mobiles
type: apiDoc
shortDescription: API pour activer ou désactiver monitoring des paramètres des applications mobiles.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: New Relic mobile monitoring API to enable or disable mobile monitoring settings.
freshnessValidatedDate: '2024-09-06T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="flutter">
      Flutter
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unity">
      Unité
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Moteur Unreal
    </TabsBarItem>

    <TabsBarItem id="xamarin">
      Xamarin
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      Utilisez les méthodes ci-dessous pour modifier les paramètres monitoring par défaut des applications mobiles. Tous les paramètres, y compris l'appel pour invoquer l'agent, sont appelés dans la méthode `onCreate` de la classe `MainActivity` .

      Pour modifier les paramètres, vous avez deux options (si le paramètre le permet) :

      * Modifiez le paramètre sur sa propre ligne pour chaque condition spécifique. Par exemple:

      ```java
      NewRelic.disableFeature(FeatureFlag.DefaultInteractions);
      NewRelic.enableFeature(FeatureFlag.CrashReporting);
      NewRelic.withApplicationToken(NEW_RELIC_TOKEN).start(this.getApplication());
      ```

      OU

      * Modifiez le paramètre dans le cadre de l'appel de démarrage de l'agent à l'aide de la méthode `.with` . Par exemple:

      ```java
      NewRelic.withApplicationToken(NEW_RELIC_TOKEN)
              .withDefaultInteractions(false)
              .withCrashReportingEnabled(true)
              .start(this.getApplication());
      ```

      ## Paramètres du point de terminaison des données [#android-data-endpoint-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver les rapports d'incident, qui apparaissent sur la [page<DNT>**Crash analysis**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

              Spécifie le composant d’autorité URI du point de terminaison de téléchargement des données de récolte. Ce point de terminaison est également utilisé pour les téléchargements d’exceptions gérées.

              Pour remplacer le point de terminaison de données que l'agent utilisera lors de la génération de rapports sur les collectes de données, ajoutez `andCollectorAddress:` à votre appel de méthode `NewRelic.withApplicationToken(“<TOKEN>”)` .

              La valeur par défaut est `mobile-collector.newrelic.com`.
            </td>

            <td>
              ```java
              NewRelic.withApplicationToken(“<TOKEN>”)
                      .usingCollectorAddress("harvest-upload.domain.com")
                      .usingCrashCollectorAddress("crash-upload.domain.com")
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le composant d’autorité de l’URI de téléchargement des données de crash.

              Pour remplacer le point de terminaison de données que l'agent utilisera lors du signalement des pannes, ajoutez `andCrashCollectorAddress:` à votre appel de méthode `NewRelic.withApplicationToken(“<TOKEN>”)` .

              La valeur par défaut est `mobile-crash.newrelic.com`.
            </td>

            <td>
              ```java
              NewRelic.withApplicationToken(“<TOKEN>”)
                      .usingCollectorAddress("harvest-upload.domain.com")
                      .usingCrashCollectorAddress("crash-upload.domain.com")
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Paramètres d'analyse [#android-analytics-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver la collecte de données d'événement. Ces événements [peuvent être interrogés avec NRQL](/docs/insights/explore-data/attributes/mobile-default-attributes-insights) et utilisés dans la [page<DNT>**Crash analysis**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            withAnalyticsEvents(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Activer ou désactiver le reporting des données d'événements. l'événement <DNT>**collection**</DNT> aura quand même lieu, mais l&amp;apos;événement ne sera pas envoyé à notre collecteur. Vous pouvez décider d&amp;apos;utiliser ceci à la place de `withAnalyticsEvents` si vous souhaitez désactiver la collecte tout en pouvant voir ce que l&amp;apos;agent collecte.

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.AnalyticsEvents(true)
            ```
          </td>
        </tr>
      </table>

      ## Paramètres de l'heure de lancement de l'application [#android-applaunchtime]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver le reporting du temps de lancement de l'application (temps froid et chaud) aux [métriques](/docs/data-apis/understand-data/new-relic-data-types/#metrics):

            * Le temps de démarrage à froid fait référence au démarrage d'une application à partir de zéro. Cela signifie que le processus de l'application n'a pas encore été créé par le système.

            * Le temps de démarrage à chaud fait référence au moment où le processus de votre application est déjà en cours d'exécution en arrière-plan.

              La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.AppStartMetrics(false)
            ```
          </td>
        </tr>
      </table>

      ## Paramètres de l'application [#android-application-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Définissez la version de l'application sous forme de chaîne. La valeur par défaut est le numéro de version défini dans `AndroidManifest.xml`.
          </td>

          <td>
            ```java
            withApplicationVersion("MY APP VERSION")
            ```
          </td>
        </tr>

        <tr>
          <td>
            Définissez la version de la build sous forme de chaîne. La valeur par défaut est l'ID de build défini dans `AndroidManifest.xml`.
          </td>

          <td>
            ```java
            withApplicationBuild("MY BUILD VERSION")
            ```
          </td>
        </tr>
      </table>

      ## Rapport de fond [#android-background-reporting]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```java
              NewRelic.enableFeature(FeatureFlag.BackgroundReporting)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Paramètres de rapport d'erreurs et de plantages [#crash-error-reporting-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Active ou désactive [le rapport de crash différé](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/android-agent-crash-reporting/#deferred-crash-reporting). La valeur par défaut est `false`.
          </td>

          <td>
            ```java
            withCrashReportingEnabled(true)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Activer ou désactiver l'enregistrement des événements d'exceptions traitées, qui apparaissent sur la [page Exceptions traitées](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/handled-exceptions-analyze-trends-prevent-crashes). La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.HandledExceptions(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Activez ou désactivez l'enregistrement des plantages d'exécution natifs, des exceptions et des conditions d'application non réactive (ANR), qui apparaissent sur la [page Analyse des plantages](/docs/mobile-monitoring/mobile-monitoring-ui/crashes). La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.NativeReporting(false)
            ```
          </td>
        </tr>
      </table>

      ## Tracing distribué [#dt]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver l'ajout d'en-têtes de tracing distribués aux requests réseau.

            La valeur par défaut est `true`.

            Consultez [la section Fonctionnement du tracing distribué New Relic](/docs/distributed-tracing/concepts/how-new-relic-distributed-tracing-works#browser-spans) pour plus d’informations sur le tracing distribué dans les applications mobiles.
          </td>

          <td>
            ```java
            FeatureFlag.DistributedTracing(false)
            ```
          </td>
        </tr>
      </table>

      ## persistance de l'événement [#android-event-persistence]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver la persistance des événements.
            </td>

            <td>
              la persistance des événements est désactivée par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```java
              NewRelic.enableFeature(FeatureFlag.EventPersistence)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Paramètres FedRamp [#fedrampenabled]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver les données de rapport à l'aide de différents points de terminaison pour les clients du gouvernement américain.

            La valeur par défaut est `true` (à partir de la version 6.11.0 de l&amp;apos;agent).
          </td>

          <td>
            ```java
            FeatureFlag.FedRampEnabled(false)
            ```
          </td>
        </tr>
      </table>

      ## Paramètres d'interaction [#interaction-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée.

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.InteractionTracing(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Activer ou désactiver l'interaction par défaut. instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela activera ou désactivera l'interaction par défaut uniquement pendant que l'interaction personnalisée reste activée.

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.DefaultInteractions(false)
            ```
          </td>
        </tr>
      </table>

      ## Paramètres de logging [#logging]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver le logging de l'agent.

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            withLoggingEnabled(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Spécifie le niveau de log. Les options incluent :

            * `ERROR` (le moins verbeux)

            * `WARN`

            * `INFO`

            * `VERBOSE`

            * `DEBUG`

            * `AUDIT` (le plus verbeux)

              La valeur par défaut est `INFO`.
          </td>

          <td>
            ```java
            withLogLevel(AgentLog.ERROR)
            ```
          </td>
        </tr>
      </table>

      ## Paramètres réseau [#networking]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tr>
          <td>
            Activer ou désactiver le signalement requests HTTP réussies au type d'événement [MobileRequest](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequest-examples) .

            La valeur par défaut est `true` (à partir de la version 5.15.2 de l&amp;apos;agent).
          </td>

          <td>
            ```java
            FeatureFlag.NetworkRequests(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Activez ou désactivez la création de rapports sur les erreurs de réseau et de requête HTTP pour le type d'événement [MobileRequestError](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequesterror-examples) . Disponible pour l&amp;apos;agent Android version 5.11.0 ou supérieure.

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.NetworkErrorRequests(false)
            ```
          </td>
        </tr>

        <tr>
          <td>
            Activer ou désactiver la capture des corps de réponse HTTP pour la trace d'erreur HTTP et l'événement [MobileRequestError](/docs/insights/nrql-new-relic-query-language/nrql-query-examples/insights-query-examples-new-relic-mobile#mobilerequesterror-examples) .

            La valeur par défaut est `true`.
          </td>

          <td>
            ```java
            FeatureFlag.HttpResponseBodyCaptureEnabled(false)
            ```
          </td>
        </tr>
      </table>

      ## Stockage hors ligne [#android-offline-storage]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible.

              Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage).
            </td>

            <td>
              Le stockage hors ligne est désactivé par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```java
              NewRelic.enableFeature(FeatureFlag.OfflineStorage)
              ```
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="ios">
      L'agent iOS de New Relic fournit des paramètres de configuration pour modifier le comportement par défaut de l'agent. Si vous apportez des modifications aux paramètres par défaut, assurez-vous d’ajouter les indicateurs de fonctionnalité juste après avoir appelé le jeton d’application. Par exemple:

      * Objective-C:

      ```objectivec
      + [NewRelic startWithApplicationToken:]
      ```

      * Swift:

      ```swift
      NewRelic.start(withApplicationToken:)
      ```

      ## Activer ou désactiver les indicateurs de fonctionnalité [#ios-feature-flags]

      Pour configurer les indicateurs de fonctionnalité, utilisez la méthode suivante définie dans `NewRelic.h:`

      <table>
        <thead>
          <tr>
            <th width={150}>
              Langue iOS
            </th>

            <th>
              Procédure
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td id="enable-obj-c">
              Objective-C
            </td>

            <td>
              En Objective-C, ces fonctionnalités sont masquées par des bits, vous pouvez donc utiliser un `|` pour activer ou désactiver plusieurs fonctionnalités en même temps.

              Activer un indicateur de fonctionnalité :

              ```objectivec
              +[NewRelic enableFeatures:(NRMAFeatureFlags)flags];
              ```

              Désactiver un indicateur de fonctionnalité :

              ```objectivec
              +[NewRelic disableFeatures:(NRMAFeatureFlags)flags];
              ```

              Voici un exemple où :

              * Le tracing des interactions et instrumentation de mise en réseau expérimentale sont activés.
              * `NSURLSession` l&amp;apos;instrumentation et les rapports de crash sont désactivés.

              ```objectivec
              [NewRelic enableFeatures:NRFeatureFlag_SwiftInteractionTracing | NRFeatureFlag_FedRampEnabled];
              [NewRelic disableFeatures:NRFeatureFlag_NSURLSessionInstrumentation | NRFeatureFlag_CrashReporting];
              [NewRelic startWithApplicationToken:...];
              ```
            </td>
          </tr>

          <tr>
            <td>
              Swift
            </td>

            <td>
              Activer un indicateur de fonctionnalité :

              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.[NRMAFeatureFlag])
              ```

              Désactiver un indicateur de fonctionnalité :

              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.[NRMAFeatureFlag])
              ```

              Exemple pour désactiver le tracing interaction par défaut :

              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DefaultInteractions)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Paramètres du point de terminaison des données [#ios-data-endpoint-settings]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver les rapports d'incident, qui apparaissent sur la [page<DNT>**Crash analysis**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

              Spécifie le composant d’autorité URI du point de terminaison de téléchargement des données de récolte. Ce point de terminaison est également utilisé pour les téléchargements d’exceptions gérées.

              Pour remplacer le point de terminaison de données que l'agent utilisera lors de la génération de rapports sur les collectes de données, ajoutez `andCollectorAddress:` à votre appel de méthode `NewRelic.start(withApplicationToken:)` .

              La valeur par défaut est `mobile-collector.newrelic.com`.
            </td>

            <td>
              ```swift
              NewRelic.start(withApplicationToken:YOUR_APP_TOKEN
                      andCollectorAddress:"harvest-upload.domain.com"
                      andCrashCollectorAddress:"crash-upload.domain.com");
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le composant d’autorité de l’URI de téléchargement des données de crash.

              Pour remplacer le point de terminaison de données que l'agent utilisera lors du signalement des pannes, ajoutez `andCrashCollectorAddress:` à votre appel de méthode `NewRelic.start(withApplicationToken:)` .

              La valeur par défaut est `mobile-crash.newrelic.com`.
            </td>

            <td>
              ```swift
              NewRelic.start(withApplicationToken:YOUR_APP_TOKEN
                    andCollectorAddress:"harvest-upload.domain.com"
                    andCrashCollectorAddress:"crash-upload.domain.com");
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Indicateur de fonctionnalité de rapport d'erreur et de panne [#ios-crashes]

      Si vous modifiez l'un des paramètres de rapport d'erreur et de plantage ci-dessous, assurez-vous d'appeler l'indicateur de fonctionnalité avant l'appel de démarrage de l'agent iOS.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver les rapports d'incident, qui apparaissent sur la [page**Analyse des incidents** ](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/crash-analysis-group-filter-your-crashes).

              Spécifie le composant d’autorité URI du point de terminaison de téléchargement des données de récolte. Ce point de terminaison est également utilisé pour les téléchargements d’exceptions gérées.

              Pour remplacer le point de terminaison de données que l'agent utilisera lors de la génération de rapports sur les collectes de données, ajoutez `.usingCollectorAddress()` à votre appel de méthode `NewRelic.withApplicationToken()` .

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_CrashReporting)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activez ou désactivez globalement l'enregistrement des événements d'exceptions gérées, qui apparaissent sur la [page Exceptions gérées](/docs/mobile-monitoring/mobile-monitoring-ui/crashes/handled-exceptions-analyze-trends-prevent-crashes).

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_HandledExceptionEvents)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Tracing distribué [#ios-dt]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver l'ajout d'en-têtes de tracing distribués aux requests réseau. Consultez [la section Fonctionnement du tracing distribué New Relic](/docs/understand-dependencies/distributed-tracing/get-started/how-new-relic-distributed-tracing-works/#no-sampling) pour plus d’informations sur le tracing distribué dans les applications mobiles.

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DistributedTracing)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## drapeaux de fonctionnalité d'interaction [#ios-interactions]

      Si vous modifiez l’un des paramètres de rapport d’erreur et de plantage ci-dessous, assurez-vous d’appeler l’indicateur de fonctionnalité juste avant l’appel de démarrage de l’agent New Relic iOS.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver le tracing interaction uniquement pour les classes et méthodes instrumentées par défaut. Les interactions personnalisées seront signalées. La trace apparaîtra sur la [page<DNT>**Interactions**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page).

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_DefaultInteractions)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver globalement la trace d'interaction, qui apparaît sur la [page<DNT>**Interactions**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page).

              Le tracing des interactions sera désactivé après le démarrage de l'agent. l'interaction pour toute activité ou méthode exécutée avant le démarrage de l'agent peut toujours apparaître sur la page <DNT>**Interactions**</DNT> .

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_InteractionTracing)
              ```
            </td>
          </tr>

          <tr>
            <td>
              <Callout variant="caution">
                Avant d'activer cette fonctionnalité, consultez Activer [le Swift suivi d'interaction](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/enable-swift-interaction-traces). Si les mesures appropriées ne sont pas prises, l’activation de cette fonctionnalité peut provoquer une instabilité.
              </Callout>

              Activer ou désactiver le tracing interaction pour le code Swift . La trace apparaît sur la [page<DNT>**Interactions**</DNT> ](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page). Cette fonctionnalité sera toujours désactivée si [`NRFeatureFlag_InteractionTracing`](#interactionTracing) est désactivé.

              La valeur par défaut est `false`.
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_SwiftInteractionTracing)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'instrumentation automatique WKWebView.

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_WebViewInstrumentation)
              ```
            </td>
          </tr>

          <tr>
            <td>
              <Callout variant="caution">
                Cette fonctionnalité est obsolète, l'activation de cet appel d'indicateur de fonctionnalité peut diminuer la stabilité des applications. Évitez de l'utiliser sauf indication contraire de New Relic.
              </Callout>

              Activer ou désactiver l'instrumentation gestuelle automatique.

              La valeur par défaut est `false`.
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_GestureInstrumentation)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Indicateurs de fonctionnalités réseau [#ios-networking]

      Si vous modifiez l’un des paramètres ci-dessous, assurez-vous d’appeler l’indicateur de fonctionnalité avant l’appel de démarrage de l’agent New Relic iOS.

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              <Callout variant="caution">
                Cette fonctionnalité est obsolète, l'activation de cet appel d'indicateur de fonctionnalité peut diminuer la stabilité des applications. Évitez de l'utiliser sauf indication contraire de New Relic.
              </Callout>

              Activer ou désactiver l'instrumentation réseau expérimentale. Cela force toutes requests réseau `NSURLConnection` via le `NRMAURProtocol`.

              La valeur par défaut est `false`.
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_ExperimentalNetworkingInstrumentation)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l’instrumentation réseau pour `NSURLSession`. Ces traces d&amp;apos;URL sont envoyées à la page [des requêtes HTTP](/docs/mobile-monitoring/mobile-monitoring-ui/network-pages/http-requests-page) .

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_NSURLSessionInstrumentation)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'instrumentation réseau. Cela envoie les données de réponse HTTP en tant qu'événement [MobileRequest](/docs/insights/insights-data-sources/default-data/mobile-default-event-attributes-insights#mobilerequest-attributes) .

              La valeur par défaut est `true` (à partir de la version 6.0.0 de l&amp;apos;agent).
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_NetworkRequestEvents)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver la génération d'événements d'erreur de requête HTTP d'appels réseau instrumentés qui entraînent une erreur ou un échec. Ces événements sont signalés comme événement [MobileRequestError](/docs/insights/insights-data-sources/default-data/mobile-default-event-attributes-insights#mobilerequesterror-attributes) .

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_RequestErrorEvents)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver la capture des corps de réponse HTTP pour la trace d'erreur HTTP et l'événement `MobileRequestError` .

              La valeur par défaut est `true`.
            </td>

            <td>
              ```swift
              NewRelic.disableFeatures(NRMAFeatureFlags.NRFeatureFlag_HttpResponseBodyCapture)
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'instrumentation pour les sessions d'URL d'attente asynchrones.

              La valeur par défaut est `false` (à partir de la version 7.4.5 de l&amp;apos;agent).
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_SwiftAsyncURLSessionSupport)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Paramètres de l'heure de lancement de l'application [#ios-applaunchtime]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver la génération de rapports sur l'heure de démarrage de l'application (heure froide et chaude) en tant que métrique :

              * Le temps froid fait référence au temps entre le démarrage de l'application et le premier tirage.

              * Le temps chaud fait référence au temps nécessaire à une application pour reprendre son exécution en arrière-plan jusqu'au premier tirage.

                La valeur par défaut est `true` (à partir de la version 7.4.0 de l&amp;apos;agent).
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_AppStartMetrics)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Paramètres de terminaison de point FedRAMP [#ios-fedramp]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver à l'aide du point de terminaison FedRAMP.

              La valeur par défaut est `false` (à partir de la version 7.4.5 de l&amp;apos;agent).
            </td>

            <td>
              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_FedRampEnabled)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Stockage hors ligne [#ios-offline-storage]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#ios).
            </td>

            <td>
              Le stockage hors ligne est désactivé par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```swift
              NewRelic.enableFeatures(NRMAFeatureFlags.NRFeatureFlag_OfflineStorage)
              ```
            </td>
          </tr>
        </tbody>
      </table>

      ## Rapport de fond [#ios-background-reporting]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```swift
              NewRelic.enableFeatures([NRMAFeatureFlags.NRFeatureFlag_BackgroundReporting])
              ```

              Prérequis dans votre projet d'application iOS pour utiliser l'instrumentation en arrière-plan :

              * Ajoutez la clé `Permitted background task scheduler identifiers` avec un éventail incluant un élément contenant l&amp;apos;ID de votre bundle d&amp;apos;applications.

              * Activez **la récupération en arrière-plan** et **le traitement en arrière-plan** dans la section **Modes d'arrière-plan** des fonctionnalités du projet Xcode.

              Remarque : iOS garantit uniquement qu'iOS effectuera la récolte en arrière-plan une fois par période de vingt-quatre heures.
            </td>
          </tr>
        </tbody>
      </table>

      ## Collecte automatique log [#ios-log-collection]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver la collecte de `stdout` et `stderr` pour le logging à distance.
            </td>

            <td>
              La collecte des logs est désactivée par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```swift
              NewRelic.disableFeatures([NRMAFeatureFlags.NRFeatureFlag_AutoCollectLogs])
              ```

              Pour plus d’informations, consultez [le logging de l’agent iOS](/docs/mobile-monitoring/new-relic-mobile-ios/configuration/ios-remote-logging).
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      Le SDK de l'agent Capacitor vous permet de configurer les paramètres par défaut pour modifier le comportement de l'agent.

      Si vous apportez des modifications aux paramètres par défaut, assurez-vous d'ajouter les indicateurs de fonctionnalité juste après avoir appelé `appToken`.

      Voici un exemple de configuration :

      ```typescript
      import { NewRelicCapacitorPlugin, NREnums, AgentConfiguration } from '@newrelic/newrelic-capacitor-plugin';
      import { Capacitor } from '@capacitor/core';

      var appToken;

      if(Capacitor.getPlatform() === 'ios') {
        appToken = 'IOS_APP_TOKEN';
      } else {
        appToken = 'ANDROID_APP_TOKEN';
      }

      let agentConfig : AgentConfiguration = {
        analyticsEventEnabled: false,
        crashReportingEnabled: false,
        interactionTracingEnabled: false,
        networkRequestEnabled: false,
        networkErrorRequestEnabled: false,
        httpResponseBodyCaptureEnabled: false,
        loggingEnabled: false,
        logLevel: NREnums.LogLevel.INFO,
        webViewInstrumentation: false,
        collectorAddress: "",
        crashCollectorAddress: "",
        sendConsoleEvents: false,
        fedRampEnabled: false,
        offlineStorageEnabled: false
      }

      NewRelicCapacitorPlugin.start({appKey:appToken, agentConfiguration:agentConfig})
      ```

      ## Configuration disponible [#capacitor-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android uniquement) Activez ou désactivez la collecte de données d'événement.
            </td>

            <td>
              ```typescript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'incident.
            </td>

            <td>
              ```typescript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée.
            </td>

            <td>
              ```typescript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement requests HTTP réussies au type d'événement `MobileRequest`.
            </td>

            <td>
              ```typescript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement des erreurs de réseau et de requête HTTP pour le type d'événement `MobileRequestError` .
            </td>

            <td>
              ```typescript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver la capture des corps de réponse HTTP pour la trace d'erreur HTTP et l'événement `MobileRequestError` .
            </td>

            <td>
              ```typescript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le logging de l'agent.
            </td>

            <td>
              ```typescript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le niveau de log. Omettez ce champ pour le niveau de log par défaut.

              Les options incluent : `ERROR` (le moins verbeux), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (le plus verbeux).
            </td>

            <td>
              ```typescript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS uniquement) Activer/désactiver l'instrumentation automatique de `WebViews`.
            </td>

            <td>
              ```typescript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur spécifique pour l'envoi de données. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```typescript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur de plantages spécifique pour l'envoi des plantages. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```typescript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'envoi du log de la console JavaScript à New Relic.
            </td>

            <td>
              ```typescript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les données de rapport à l'aide de différents points de terminaison pour les clients du gouvernement américain.
            </td>

            <td>
              ```typescript
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#capacitor).
            </td>

            <td>
              Le stockage hors ligne est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```typescript
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```typescript
              backgroundReportingEnabled: true
              ```

              Pour utiliser l'instrumentation d'arrière-plan, vous aurez besoin des éléments suivants dans votre projet d'application iOS :

              * Dans le fichier `Info.plist` de votre application, ajoutez la clé « Identifiant du planificateur de tâches d&amp;apos;arrière-plan autorisé ». Cette clé doit être un éventail contenant une seule valeur de chaîne, qui est l&amp;apos;identifiant de bundle de votre application.

              * Dans la section **Background Modes** des fonctionnalités du projet Xcode, activez **Background Fetch** et **Background Processing**.

              Remarque : iOS garantit uniquement qu'iOS effectuera la récolte en arrière-plan une fois par période de vingt-quatre heures.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="cordova">
      Le SDK d'agent Cordova vous permet de configurer les paramètres par défaut pour modifier le comportement de l'agent.

      Si vous apportez des modifications aux paramètres par défaut, assurez-vous d'ajouter les indicateurs de fonctionnalité juste après avoir appelé `appToken`. 

      Voici un exemple de configuration :

      ```shell
      # Disable Crash Reporting
      cordova plugin add https://github.com/newrelic/newrelic-cordova-plugin.git
        --variable IOS_APP_TOKEN="YOUR_IOS_APP_TOKEN"
        --variable ANDROID_APP_TOKEN="YOUR_ANDROID_APP_TOKEN"
        --variable CRASH_REPORTING_ENABLED="false"
        --variable OFFLINE_STORAGE_ENABLED="false"
      ```

      ## Configuration disponible [#cordova-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              Activer ou désactiver les rapports d'incident.
            </td>

            <td>
              ```shell
              CRASH_REPORTING_ENABLED = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée.
            </td>

            <td>
              ```shell
              INTERACTION_TRACING_ENABLED = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement requests HTTP réussies au type d'événement `MobileRequest`.
            </td>

            <td>
              ```typescript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement des erreurs de réseau et de requête HTTP pour le type d'événement `MobileRequestError` .
            </td>

            <td>
              ```typescript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver la capture des corps de réponse HTTP pour la trace d'erreur HTTP et l'événement `MobileRequestError` .
            </td>

            <td>
              ```typescript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le logging de l'agent.
            </td>

            <td>
              ```shell
              LOGGING_ENABLED = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le niveau de log. Omettez ce champ pour le niveau de log par défaut.

              Les options incluent : `ERROR` (le moins verbeux), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (le plus verbeux).
            </td>

            <td>
              ```shell
              LOG_LEVEL = "INFO"
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS uniquement) Activer/désactiver l'instrumentation automatique de `WebViews`.
            </td>

            <td>
              ```shell
              WEB_VIEW_INSTRUMENTATION = "true"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur spécifique pour l'envoi de données. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```shell
              COLLECTOR_ADDRESS = "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur de plantages spécifique pour l'envoi des plantages. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```shell
              CRASH_COLLECTOR_ADDRESS = "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'envoi du log de la console JavaScript à New Relic.
            </td>

            <td>
              ```typescript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les données de rapport à l'aide de différents points de terminaison pour les clients du gouvernement américain.
            </td>

            <td>
              ```shell
              FEDRAMP_ENABLED = "false"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#cordova).
            </td>

            <td>
              Le stockage hors ligne est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```shell
              OFFLINE_STORAGE_ENABLED = "false"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```shell
              BACKGROUND_REPORTING_ENABLED = "true"
              ```

              Pour utiliser l'instrumentation d'arrière-plan, vous aurez besoin des éléments suivants dans votre projet d'application iOS :

              * Dans le fichier `Info.plist` de votre application, ajoutez la clé « Identifiant du planificateur de tâches d&amp;apos;arrière-plan autorisé ». Cette clé doit être un éventail contenant une seule valeur de chaîne, qui est l&amp;apos;identifiant de bundle de votre application.

              * Dans la section **Background Modes** des fonctionnalités du projet Xcode, activez **Background Fetch** et **Background Processing**.

              Remarque : iOS garantit uniquement qu'iOS effectuera la récolte en arrière-plan une fois par période de vingt-quatre heures.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="maui">
      Le SDK .NET MAUI de l'agent vous permet de configurer les paramètres par défaut pour modifier le comportement de l'agent.

      Si vous apportez des modifications aux paramètres par défaut, assurez-vous d'ajouter les indicateurs de fonctionnalité juste après avoir appelé `appToken`.

      Voici un exemple de configuration :

      ```csharp
      using NewRelic.MAUI.Plugin;
      ...

      public static MauiApp CreateMauiApp()
      {
        var builder = MauiApp.CreateBuilder();
        builder
          .UseMauiApp<App>()
          .ConfigureFonts(fonts =>
          {
            fonts.AddFont("OpenSans-Regular.ttf", "OpenSansRegular");
            fonts.AddFont("OpenSans-Semibold.ttf", "OpenSansSemibold");
          });

            builder.ConfigureLifecycleEvents(AppLifecycle => {
            #if ANDROID
                AppLifecycle.AddAndroid(android => android
                    .OnCreate((activity, savedInstanceState) => StartNewRelic()));
            #endif
            #if IOS

                  AppLifecycle.AddiOS(iOS => iOS.WillFinishLaunching((_,__) => {
                    StartNewRelic();
                    return false;
                }));
            #endif
            });
        return builder.Build();
      }

      private static void StartNewRelic()
      {
        CrossNewRelic.Current.HandleUncaughtException();

        // Set optional agent configuration. Options are: 
        // crashReportingEnabled, loggingEnabled, logLevel, collectorAddress, 
        // crashCollectorAddress,analyticsEventEnabled, networkErrorRequestEnabled,
        // networkRequestEnabled, interactionTracingEnabled, webViewInstrumentation,
        // fedRampEnabled, offlineStorageEnabled, newEventSystemEnabled, backgroundReportingEnabled
        // AgentStartConfiguration agentConfig = new AgentStartConfiguration(crashReportingEnabled:false);

        if (DeviceInfo.Current.Platform == DevicePlatform.Android)
        {
          CrossNewRelic.Current.Start("APP_TOKEN_HERE");
          // Start with optional agent configuration
          // CrossNewRelic.Current.Start("APP_TOKEN_HERE", agentConfig);
        } 
        else if (DeviceInfo.Current.Platform == DevicePlatform.iOS)
        {
          CrossNewRelic.Current.Start("APP_TOKEN_HERE");
          // Start with optional agent configuration
          // CrossNewRelic.Current.Start("APP_TOKEN_HERE", agentConfig);
        }
      }
      ```

      ## Configuration disponible [#maui-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android uniquement) Activez ou désactivez la collecte de données d'événement.
            </td>

            <td>
              ```csharp
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'incident.
            </td>

            <td>
              ```csharp
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le logging de l'agent.
            </td>

            <td>
              ```csharp
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement requests HTTP réussies au type d'événement `MobileRequest`.
            </td>

            <td>
              ```csharp
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement des erreurs de réseau et de requête HTTP pour le type d'événement `MobileRequestError` .
            </td>

            <td>
              ```csharp
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée. par défaut faux
            </td>

            <td>
              ```csharp
              interactionTracingEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer/désactiver l'instrumentation automatique des WebViews.
            </td>

            <td>
              ```csharp
              webViewInstrumentation: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les données de rapport à l'aide de différents points de terminaison pour les clients du gouvernement américain.
            </td>

            <td>
              ```csharp
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le niveau de log. Omettez ce champ pour le niveau de log par défaut.

              Les options incluent : `ERROR` (le moins verbeux), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (le plus verbeux).
            </td>

            <td>
              ```csharp
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur spécifique pour l'envoi de données. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```csharp
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur de plantages spécifique pour l'envoi des plantages. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```csharp
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#maui).
            </td>

            <td>
              Le stockage hors ligne est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```csharp
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```csharp
              backgroundReportingEnabled: true
              ```

              Pour utiliser l'instrumentation d'arrière-plan, vous aurez besoin des éléments suivants dans votre projet d'application iOS :

              * Dans le fichier `Info.plist` de votre application, ajoutez la clé « Identifiant du planificateur de tâches d&amp;apos;arrière-plan autorisé ». Cette clé doit être un éventail contenant une seule valeur de chaîne, qui est l&amp;apos;identifiant de bundle de votre application.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="flutter">
      Le SDK Flutter de l'agent vous permet de configurer les paramètres par défaut pour modifier le comportement de l'agent.

      Si vous apportez des modifications aux paramètres par défaut, assurez-vous d'ajouter les indicateurs de fonctionnalité juste après avoir appelé `appToken`.

      Voici un exemple de configuration :

      ```dart
      if (Platform.isAndroid) {
        appToken = AppConfig.androidToken;
      } else if (Platform.isIOS) {
        appToken = AppConfig.iOSToken;
      }

      Config config = Config(
          accessToken: appToken,
          analyticsEventEnabled: false,
          networkErrorRequestEnabled: false,
          networkRequestEnabled: false,
          crashReportingEnabled: false,
          interactionTracingEnabled: false,
          httpResponseBodyCaptureEnabled: false,
          loggingEnabled: false,
          webViewInstrumentation: false,
          printStatementAsEventsEnabled: false,
          httpInstrumentationEnabled: false,
          offlineStorageEnabled: true);

      // NewrelicMobile.instance.start(config, () {
      //   runApp(MyApp());
      // });

      runZonedGuarded(() async {
        WidgetsFlutterBinding.ensureInitialized();
        FlutterError.onError = NewrelicMobile.onError;
        await NewrelicMobile.instance.startAgent(config);
        runApp(MyApp());
      }, (Object error, StackTrace stackTrace) {
        NewrelicMobile.instance.recordError(error, stackTrace);
      });
      ```

      ## Configuration disponible [#flutter-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android uniquement) Activez ou désactivez la collecte de données d'événement.
            </td>

            <td>
              ```dart
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'incident.
            </td>

            <td>
              ```dart
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée.
            </td>

            <td>
              ```dart
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement requests HTTP réussies au type d'événement `MobileRequest`.
            </td>

            <td>
              ```dart
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement des erreurs de réseau et de requête HTTP pour le type d'événement `MobileRequestError` .
            </td>

            <td>
              ```dart
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver la capture des corps de réponse HTTP pour la trace d'erreur HTTP et l'événement `MobileRequestError` .
            </td>

            <td>
              ```dart
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le logging de l'agent.
            </td>

            <td>
              ```dart
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le niveau de log. Omettez ce champ pour le niveau de log par défaut.

              Les options incluent : `ERROR` (le moins verbeux), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (le plus verbeux).
            </td>

            <td>
              ```dart
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS uniquement) Activer/désactiver l'instrumentation automatique de `WebViews`.
            </td>

            <td>
              ```dart
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur spécifique pour l'envoi de données. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```dart
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur de plantages spécifique pour l'envoi des plantages. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```dart
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'envoi du log de la console JavaScript à New Relic.
            </td>

            <td>
              ```dart
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#flutter).
            </td>

            <td>
              Le stockage hors ligne est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```dart
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'ajout d'en-têtes de tracing distribués aux requests réseau.

              La valeur par défaut est `true`.

              Pour plus d'informations sur le tracing distribué dans les applications mobiles, consultez [les rapports trace des navigateurs et des appareils mobiles.](/docs/distributed-tracing/concepts/how-new-relic-distributed-tracing-works#browser-spans)
            </td>

            <td>
              le tracing distribué est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```dart
              distributedTracingEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```typescript
              backgroundReportingEnabled: true
              ```

              Pour utiliser l'instrumentation d'arrière-plan, vous aurez besoin des éléments suivants dans votre projet d'application iOS :

              * Dans le fichier `Info.plist` de votre application, ajoutez la clé « Identifiant du planificateur de tâches d&amp;apos;arrière-plan autorisé ». Cette clé doit être un éventail contenant une seule valeur de chaîne, qui est l&amp;apos;identifiant de bundle de votre application.

              * Dans la section **Background Modes** des fonctionnalités du projet Xcode, activez **Background Fetch** et **Background Processing**.

              Remarque : iOS garantit uniquement qu'iOS effectuera la récolte en arrière-plan une fois par période de vingt-quatre heures.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="react">
      Le SDK d'agent React Native vous permet de configurer les paramètres par défaut pour modifier le comportement de l'agent.

      Si vous apportez des modifications aux paramètres par défaut, assurez-vous d'ajouter les indicateurs de fonctionnalité juste après avoir appelé `appToken`.

      Voici un exemple de configuration :

      ```javascript
      import NewRelic from 'newrelic-react-native-agent';
      import * as appVersion from './package.json';
      import {Platform} from 'react-native';

      let appToken;

      if (Platform.OS === 'ios') {
        appToken = 'IOS_APP_TOKEN';
      } else {
        appToken = 'ANDROID_APP_TOKEN';
      }


      const agentConfiguration = {
        Config config = Config(
          analyticsEventEnabled: false,
          crashReportingEnabled: false,
          interactionTracingEnabled: false,
          networkRequestEnabled: false,
          networkErrorRequestEnabled: false,
          httpResponseBodyCaptureEnabled: false,
          loggingEnabled: false,
          logLevel: NREnums.LogLevel.INFO,
          webViewInstrumentation: false,
          collectorAddress: "",
          crashCollectorAddress: "",
          sendConsoleEvents: false,
          fedRampEnabled: false,
          offlineStorageEnabled:false
        )
      };

      NewRelic.startAgent(appToken,agentConfiguration);
      NewRelic.setJSAppVersion(appVersion.version);
      AppRegistry.registerComponent(appName, () => App);
      ```

      ## Configuration disponible [#react-native-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android uniquement) Activez ou désactivez la collecte de données d'événement.
            </td>

            <td>
              ```javascript
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'incident.
            </td>

            <td>
              ```javascript
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée.
            </td>

            <td>
              ```javascript
              interactionTracingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement requests HTTP réussies au type d'événement `MobileRequest`.
            </td>

            <td>
              ```javascript
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement des erreurs de réseau et de requête HTTP pour le type d'événement `MobileRequestError` .
            </td>

            <td>
              ```javascript
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver la capture des corps de réponse HTTP pour la trace d'erreur HTTP et l'événement `MobileRequestError` .
            </td>

            <td>
              ```javascript
              httpResponseBodyCaptureEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le logging de l'agent.
            </td>

            <td>
              ```javascript
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le niveau de log. Omettez ce champ pour le niveau de log par défaut.

              Les options incluent : `ERROR` (le moins verbeux), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (le plus verbeux).
            </td>

            <td>
              ```javascript
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              (iOS uniquement) Activer/désactiver l'instrumentation automatique de `WebViews`.
            </td>

            <td>
              ```javascript
              webViewInstrumentation: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur spécifique pour l'envoi de données. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```javascript
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur de plantages spécifique pour l'envoi des plantages. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```javascript
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver l'envoi du log de la console JavaScript à New Relic.
            </td>

            <td>
              ```javascript
              sendConsoleEvents: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les données de rapport à l'aide de différents points de terminaison pour les clients du gouvernement américain.
            </td>

            <td>
              ```javascript
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#react).
            </td>

            <td>
              Le stockage hors ligne est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```typescript
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```typescript
              backgroundReportingEnabled: true
              ```

              Pour utiliser l'instrumentation d'arrière-plan, vous aurez besoin des éléments suivants dans votre projet d'application iOS :

              * Dans le fichier `Info.plist` de votre application, ajoutez la clé « Identifiant du planificateur de tâches d&amp;apos;arrière-plan autorisé ». Cette clé doit être un éventail contenant une seule valeur de chaîne, qui est l&amp;apos;identifiant de bundle de votre application.

              * Dans la section **Background Modes** des fonctionnalités du projet Xcode, activez **Background Fetch** et **Background Processing**.

              Remarque : iOS garantit uniquement qu'iOS effectuera la récolte en arrière-plan une fois par période de vingt-quatre heures.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="unity">
      New Relic propose des paramètres par défaut pour monitoring des applications mobiles dans Unity. Vous pouvez facilement ajuster ces paramètres dans l'éditeur Unity pour les adapter à vos besoins spécifiques.

      Pour configurer ces paramètres :

      1. lancez l'éditeur Unity et ouvrez votre projet Unity.

      2. Dans la barre de menus, sélectionnez **Tools &gt; New Relic &gt; Getting Started &gt; New Relic Configuration**.

      3. La fenêtre de **Inspector** de gauche affiche une liste de paramètres par défaut. Cochez simplement la case à côté d&amp;apos;un paramètre pour l&amp;apos;activer ou décochez la case pour le désactiver.

         <img title="Unity editor" alt="Screenshot of the Unity editor to configure settings" src="/images/mobile_screenshot-crop_Unity-editor-UI.webp" />

      4. Cliquez sur **Ajouter un composant**.
    </TabsPageItem>

    <TabsPageItem id="unreal">
      New Relic propose des paramètres par défaut pour monitoring des applications mobiles dans Unreal Engine. Vous pouvez facilement ajuster ces paramètres dans l'éditeur Unreal pour répondre à vos besoins spécifiques.

      Pour configurer ces paramètres :

      1. lancez l'Unreal Editor et ouvrez votre projet Unreal.

      2. Dans la barre de menu, sélectionnez \*\*Plugins &gt; New Relic &gt; \*\*.

      <img title="Unreal editor" alt="Screenshot of the Unreal editor to configure settings" src="/images/newrelic_unreal_sdk_configuration.webp" />
    </TabsPageItem>

    <TabsPageItem id="xamarin">
      Le SDK d'agent Xamarin vous permet de configurer les paramètres par défaut pour modifier le comportement de l'agent.

      Si vous apportez des modifications aux paramètres par défaut, assurez-vous d'ajouter les indicateurs de fonctionnalité juste après avoir appelé `appToken`.

      Voici un exemple de configuration :

      ```csharp
      public App ()
      {
          InitializeComponent();

          MainPage = new MainPage();
          Application.Current.PageAppearing += OnPageAppearing;
          Application.Current.PageDisappearing += PageDisappearing;

          CrossNewRelicClient.Current.HandleUncaughtException();
          CrossNewRelicClient.Current.TrackShellNavigatedEvents();

          // Set optional agent configuration. Options are: 
          // crashReportingEnabled, loggingEnabled, logLevel, collectorAddress, 
          // crashCollectorAddress, analyticsEventEnabled, networkErrorRequestEnabled,
          // networkRequestEnabled, interactionTracingEnabled,webViewInstrumentation,
          // fedRampEnabled, offlineStorageEnabled
          AgentStartConfiguration agentConfig = new AgentStartConfiguration(crashReportingEnabled: false, offlineStorageEnabled: false);

          if (Device.RuntimePlatform == Device.Android)
          {
              //CrossNewRelicClient.Current.Start("APP_TOKEN_HERE");
              // Start with optional agent configuration
              CrossNewRelicClient.Current.Start("APP_TOKEN_HERE", agentConfig);
          }
          else if (Device.RuntimePlatform == Device.iOS)
          {
              //CrossNewRelicClient.Current.Start("APP_TOKEN_HERE");
              // Start with optional agent configuration
              CrossNewRelicClient.Current.Start("APP_TOKEN_HERE", agentConfig);
          }
      }
      ```

      ## Configuration disponible [#xamarain-configurations]

      <table>
        <thead>
          <tr>
            <th style={{ width: "800px" }}>
              Description
            </th>

            <th>
              Exemple
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              (Android uniquement) Activez ou désactivez la collecte de données d'événement.
            </td>

            <td>
              ```csharp
              analyticsEventEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'incident.
            </td>

            <td>
              ```csharp
              crashReportingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le logging de l'agent.
            </td>

            <td>
              ```csharp
              loggingEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement requests HTTP réussies au type d'événement `MobileRequest`.
            </td>

            <td>
              ```csharp
              networkRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le signalement des erreurs de réseau et de requête HTTP pour le type d'événement `MobileRequestError` .
            </td>

            <td>
              ```csharp
              networkErrorRequestEnabled: true
              ```
            </td>
          </tr>

          <tr>
            <td>
              Spécifie le niveau de log. Omettez ce champ pour le niveau de log par défaut.

              Les options incluent : `ERROR` (le moins verbeux), `WARNING`, `INFO`, `VERBOSE`, `AUDIT` (le plus verbeux).
            </td>

            <td>
              ```csharp
              logLevel: NREnums.LogLevel.INFO
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur spécifique pour l'envoi de données. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```csharp
              collectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Définissez une adresse de collecteur de plantages spécifique pour l'envoi des plantages. Omettez ce champ pour l'adresse par défaut.
            </td>

            <td>
              ```csharp
              crashCollectorAddress: "crash-upload-delegate.domain.com"
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le tracing interaction . instrumentation de traces est toujours effectuée, mais aucune trace n'est récoltée. Cela désactivera l'interaction par défaut et personnalisée. par défaut faux
            </td>

            <td>
              ```csharp
              interactionTracingEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer/désactiver l'instrumentation automatique des WebViews.
            </td>

            <td>
              ```csharp
              webViewInstrumentation: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les données de rapport à l'aide de différents points de terminaison pour les clients du gouvernement américain.
            </td>

            <td>
              ```csharp
              fedRampEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver le stockage de données hors ligne lorsqu'aucune connexion Internet n'est disponible. Pour configurer la quantité de stockage hors ligne, voir [Définir la taille maximale de stockage hors ligne](/docs/mobile-monitoring/new-relic-mobile/mobile-sdk/set-max-offline-storage/#xamarin).
            </td>

            <td>
              Le stockage hors ligne est activé par défaut. Pour le désactiver, ajoutez l'indicateur de fonctionnalité suivant :

              ```csharp
              offlineStorageEnabled: false
              ```
            </td>
          </tr>

          <tr>
            <td>
              Activer ou désactiver les rapports d'arrière-plan lorsque l'application passe à l'état d'arrière-plan.
            </td>

            <td>
              Les rapports en arrière-plan sont désactivés par défaut. Pour l'activer, ajoutez l'indicateur de fonctionnalité suivant :

              ```csharp
              backgroundReportingEnabled: true
              ```

              Pour utiliser l'instrumentation d'arrière-plan, vous aurez besoin des éléments suivants dans votre projet d'application iOS :

              * Ajoutez la clé `Permitted background task scheduler identifiers` avec un éventail incluant un élément contenant l&amp;apos;ID de votre bundle d&amp;apos;applications.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>
  </TabsPages>
</Tabs>