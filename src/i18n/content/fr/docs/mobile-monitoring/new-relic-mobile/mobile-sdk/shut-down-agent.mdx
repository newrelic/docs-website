---
title: Arrêter l'agent
type: apiDoc
shortDescription: Arrêtez l'agent dans le cycle de vie actuel de l'application pendant l'exécution.
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API used to shut down the agent within the current application lifecycle during runtime.
freshnessValidatedDate: '2023-07-21T00:00:00.000Z'
translationType: machine
---

<Tabs>
  <TabsBar>
    <TabsBarItem id="android">
      Android
    </TabsBarItem>

    <TabsBarItem id="ios">
      iOS
    </TabsBarItem>

    <TabsBarItem id="capacitor">
      Capacitor
    </TabsBarItem>

    <TabsBarItem id="cordova">
      Cordova
    </TabsBarItem>

    <TabsBarItem id="maui">
      .NET MAUI
    </TabsBarItem>

    <TabsBarItem id="react">
      React Native
    </TabsBarItem>

    <TabsBarItem id="unreal">
      Moteur Unreal
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="android">
      ## Syntaxe [#syntax]

      ### Java [#java]

      ```java
      NewRelic.shutdown()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.shutdown()
      ```

      ## Description [#description]

      Cette API offre la possibilité d&apos;arrêter l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution. Une fois l’agent arrêté, il ne peut pas être redémarré dans le cycle de vie actuel de l’application.

      ## Exemple [#example]

      ### Java [#java]

      ```java
      NewRelic.shutdown()
      ```

      ### Kotlin [#kotlin]

      ```kotlin
      NewRelic.shutdown()
      ```
    </TabsPageItem>

    <TabsPageItem id="ios">
      ## Syntaxe [#syntax]

      ### Objectif-C [#objc]

      ```objectivec
      + (void) shutdown();
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.shutdown() -> Void
      ```

      ## Description [#description]

      Cette API offre la possibilité d&apos;arrêter l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution. Une fois l’agent arrêté, il ne peut pas être redémarré dans le cycle de vie actuel de l’application.

      ## Exemples [#examples]

      ### Objective-C [#obj-c]

      ```objectivec
      [NewRelic shutdown];
      ```

      ### Swift [#swift]

      ```swift
      NewRelic.shutdown()
      ```
    </TabsPageItem>

    <TabsPageItem id="capacitor">
      ## Syntaxe [#syntax]

      ```typescript
      shutdown(options?: {} | undefined) => void
      ```

      ## Description [#description]

      Arrêtez l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution.

      ## Exemple [#example]

      ```typescript
      NewRelicCapacitorPlugin.shutdown();
      ```
    </TabsPageItem>

    <TabsPageItem id="cordova">
      ## Syntaxe [#syntax]

      ```typescript
      shutdown() : void;
      ```

      ## Description [#description]

      Arrêtez l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution.

      ## Exemple [#example]

      ```js
      NewRelic.shutdown();
      ```
    </TabsPageItem>

    <TabsPageItem id="maui">
      ## Syntaxe [#syntax]

      ```csharp
      Shutdown() : void
      ```

      ## Description [#description]

      Arrêtez l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution.

      ## Exemple [#example]

      ```csharp
      CrossNewRelic.Current.Shutdown());
      ```
    </TabsPageItem>

    <TabsPageItem id="react">
      ## Syntaxe [#syntax]

      ```js
      shutdown() : void;
      ```

      ## Description [#description]

      Arrêtez l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution.

      ## Exemple [#example]

      ```js
      NewRelic.shutdown();
      ```
    </TabsPageItem>

    <TabsPageItem id="unreal">
      ## Syntaxe [#syntax]

      ```cpp
      shutdown(): void;
      ```

      ## Description [#description]

      Arrêtez l&apos;agent dans le cycle de vie actuel de l&apos;application pendant l&apos;exécution.

      ## Exemple [#example]

      ```cpp
      UNewRelicBPLibrary::shutdown();;
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>