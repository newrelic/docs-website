---
title: Statut 200 avec API Explorer
type: troubleshooting
tags:
  - APIs
  - REST API v2
  - Troubleshooting
metaDescription: 'If you received an HTTP 200 status code, but the message body is empty, make sure you have specified an Accept header in your GET request.'
freshnessValidatedDate: never
translationType: machine
---

## Problème

Vous avez reçu un code d’état `HTTP 200` , mais le corps du message est vide.

## Solution

Pour corriger cela, ajoutez un en-tête `Accept` contenant `application/json` ou `application/xml` à votre requête `GET` . Par exemple:

```sh
curl -X GET 'https://api.newrelic.com/v2/applications/YOUR_APP_ID/metrics/data.json'
     -H 'X-Api-Key:YOUR_API_KEY' -i
     -H 'Accept: application/json'
```

## Cause

Cela peut se produire lors de l&apos;utilisation d&apos;un en-tête `GET` par défaut qui ne spécifie pas le type de contenu accepté. Pour plus d&apos;informations sur les en-têtes HTTP, consultez ce didacticiel sur [la négociation de contenu à l&apos;aide des en-têtes HTTP](https://restfulapi.net/content-negotiation/).