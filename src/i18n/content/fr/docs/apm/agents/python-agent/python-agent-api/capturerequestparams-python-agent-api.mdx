---
title: capture_request_params (Python API d'agent)
type: apiDoc
shortDescription: Permet la capture du paramètre de chaîne de requête d'une transaction.
tags:
  - Agents
  - Python agent
  - Python agent API
metaDescription: 'Python API: This call enables collection of query string parameters.'
freshnessValidatedDate: never
translationType: machine
---

## Syntaxe

```py
newrelic.agent.capture_request_params(flag=True)
```

Permet la capture du paramètre de chaîne de requête d&apos;une transaction.

## Description

Cet appel permet la capture du paramètre de chaîne de requête d&apos;un site Web de transaction en tant [qu&apos;attribut](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#attribute). Cette collection est bloquée par défaut pour des raisons de sécurité. Si vous souhaitez plutôt activer la collecte de paramètres de chaîne de requête pour l&apos;ensemble de votre application, utilisez le paramètre de configuration [`record_sql`](/docs/agents/python-agent/installation-configuration/python-agent-configuration#txn-tracer-sql) .

[Le mode haute sécurité](/docs/accounts-partnerships/accounts/security/high-security) remplace cet appel s&apos;il est actif, auquel cas cet appel n&apos;a aucun effet. Cet appel remplace les autres paramètres pertinents dans la [configuration](/docs/agents/python-agent/installation-configuration/python-agent-configuration) de l&apos;agent.

<Callout variant="tip">
  Vous pouvez également activer la capture de chaîne de requête dans le dictionnaire d&apos;environnement WSGI. Pour ce faire, marquez la clé `newrelic.capture_request_params` pour la demande spécifique dans le dictionnaire d’environnement WSGI transmis par le serveur WSGI à l’application WSGI cible.
</Callout>

## Paramètres

<table>
  <thead>
    <tr>
      <th width="25%">
        paramètres
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `flag`

        *booléen*
      </td>

      <td>
        Facultatif. La valeur par défaut est `True`. Utilisez `False` pour désactiver la collecte.
      </td>
    </tr>
  </tbody>
</table>

## Valeurs de retour

Aucun.

## Exemples

### Activer la capture de la chaîne de requête [#capture-example]

Placez cet appel dans la section de code correspondant à une transaction pour laquelle vous souhaitez collecter le paramètre de chaîne de requête :

```py
newrelic.agent.capture_request_params()
```