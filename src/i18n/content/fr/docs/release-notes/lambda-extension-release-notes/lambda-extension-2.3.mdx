---
subject: Lambda-Extension
releaseDate: '2025-05-12'
version: 2.3.19
translationType: machine
---

## Remarques

* Les mises à jour vont vers la version pour résoudre les vulnérabilités de `go/stdlib`

* Exemple de mises à jour pour le monitoring de l&apos;IA en streaming de réponse

* Met à jour le code de test local `newrelic_lambda_wrapper`

* Mises à jour `ubuntu` runner

* Mises à jour de la version d&apos;extension pour éviter l&apos;erreur `/lib64/libc.so.6`: version `GLIBC_X.YZ` introuvable.

* Augmenter axios de `1.7.7` à `1.8.2` dans /examples/sam/containerized-lambda/nodejs-sam-example/hello-world

* Bump golang.org/x/net de `0.33.0` à `0.38.0`