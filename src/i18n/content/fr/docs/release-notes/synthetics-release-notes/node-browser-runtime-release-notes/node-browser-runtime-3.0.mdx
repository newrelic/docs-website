---
subject: Node browser runtime
releaseDate: '2026-09-15'
version: 3.0.228
features: []
enhancements:
  - Added `AbortController` and `AbortSignal` as available host globals in the Scripted Browser monitor runtime sandbox.
bugs:
  - 'Removed the `--jitless` Chrome JS flag to re-enable JIT compilation, improving script execution performance for Scripted Browser monitors.'
security: []
translationType: machine
---

### Améliorations

Ajout de `AbortController` et `AbortSignal` en tant que variables globales d&apos;hôte disponibles dans la sandbox d&apos;exécution du moniteur de navigateur scripté.

### Débogage

Suppression de l’indicateur JS Chrome `--jitless` pour réactiver la compilation JIT, améliorant les performances d’exécution de script pour monitorer Scripted Browser.