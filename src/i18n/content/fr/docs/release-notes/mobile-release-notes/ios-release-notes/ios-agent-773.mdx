---
subject: iOS agent
releaseDate: '2026-07-02'
version: 7.7.3
downloadLink: 'https://download.newrelic.com/ios_agent/NewRelic_XCFramework_Agent_7.7.3.zip'
translationType: machine
---

### Nouvelle fonctionnalité

Fonctionnalité de stockage hors ligne pour le Session Replay mobile.

### Corrigé dans cette sortie

Remplacement du LCG basé sur l&apos;horodatage dans IGuidGenerator par arc4random\_buf.

Ajout de la détection de la région UE à l&apos;envoi de sourcemaps RN.

Blocage de la récursion pour le swizzle d&apos;interaction par défaut de style MAUI.

Renommage de appVersionId en appVersion dans l&apos;envoi de sourcemaps RN.

Correction d’une boucle infinie dans EventDeserializer qui gelait l’agent au lancement.

Modification du mode de masquage par défaut de Session Replay pour ne pas autoriser le démasquage à l&apos;aide d&apos;API programmatiques et correction des API de remplacement d&apos;UIKit.

Correction d&apos;un plantage qui pouvait se produire dans NRMAWKWebViewNavigationDelegate webViewWebContentProcessDidTerminate.

Garantie que les données de Session Replay sont téléchargées au prochain lancement si le téléchargement a échoué lors de la fermeture de l&apos;application.

Suppression des rapports de plantage définitivement rejetés (400/403).

Ajout de l&apos;éviction TTL pour le stockage hors ligne (#786).

Correction du plantage d’initialisation de NRMAHarvestController (#793).