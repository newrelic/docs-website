---
title: nrEnvoyer une réponse HTTP
type: apiDoc
shortDescription: Envoyer un événement HTTP_RESPONSE de type ConnectedDeviceSystem.
tags: null
metaDescription: Send an HTTP_RESPONSE event of type ConnectedDeviceSystem.
freshnessValidatedDate: never
translationType: machine
---

## Syntaxe

```js
  nrSendHttpResponse(nr as Object, _url as String, msg as Object) as Void
```

## Description

Envoyer un événement HTTP\_RESPONSE de type ConnectedDeviceSystem.

## Arguments

<table>
  <thead>
    <tr>
      <th width="25%">
        Arguments
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `nr`
      </td>

      <td>
        Nouvel objet Agent Relic.
      </td>
    </tr>

    <tr>
      <td>
        `_url`
      </td>

      <td>
        URL de la demande.
      </td>
    </tr>

    <tr>
      <td>
        `msg`
      </td>

      <td>
        Un message de type roUrlEvent.
      </td>
    </tr>
  </tbody>
</table>

## Retour

Rien.

## Exemple

```js
  	msg = wait(5000, m.port)
	  if type(msg) = "roUrlEvent" then
		        nrSendHttpResponse(m.nr, _url, msg)
	  end if
```