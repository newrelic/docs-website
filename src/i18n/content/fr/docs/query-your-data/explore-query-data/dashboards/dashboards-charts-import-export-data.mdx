---
title: 'Importer, exporter et ajouter des dashboards et des graphiques'
tags:
  - Query your data
  - Explore and query data
  - Dashboards
  - Charts
metaDescription: Import and export data from dashboards and charts.
freshnessValidatedDate: '2024-02-07T00:00:00.000Z'
translationType: machine
---

Vous pouvez utiliser [le dashboard](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) pour visualiser et suivre l&apos;état de votre système, résoudre les problèmes et planifier les étapes ultérieures. Apprenez à partager toutes ces informations avec votre équipe, vos clients ou d’autres parties prenantes grâce à diverses options d’importation et d’exportation.

## Importer un dashboard au format JSON [#import-json]

Vous pouvez importer un dashboard au format JSON lorsque vous souhaitez modifier un dashboard prêt à l&apos;emploi au niveau du code. Par exemple, si vous souhaitez créer un dashboard personnalisé en fusionnant deux dashboards existants, vous pouvez modifier le JSON d&apos;un dashboard de base, puis le réimporter dans New Relic.

Pour importer un dashboard au format JSON :

1. Allez à <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Dashboards**</DNT>.

2. Ouvrez le JSON d’un dashboard existant en cliquant sur l’icône <DNT>**...**</DNT>, puis sélectionnez <DNT>**Copy JSON**</DNT>.

3. Retournez à la page <DNT>**Dashboards**</DNT> .

4. Dans le coin supérieur droit, cliquez sur <DNT>**Import dashboard**</DNT>.

5. Collez votre code JSON.

6. Choisissez les paramètres de compte et d&apos;autorisation pour le dashboard. Vous ne pouvez pas modifier le compte une fois que vous l&apos;avez configuré, mais vous pouvez [modifier les autorisations](/docs/query-your-data/explore-query-data/dashboards/manage-your-dashboard/#dash-settings) à tout moment.

7. Cliquez sur <DNT>**Save**</DNT>.

## Exporter un dashboard au format PDF [#export-dashboards-pdf]

Pour exporter votre dashboard sous forme de fichier PDF, dans le coin supérieur droit du dashboard, cliquez sur l&apos;icône <DNT>**...**</DNT> et sélectionnez <DNT>**Export dashboard as PDF**</DNT>.

<img title="Export dashboard as PDF" alt="Screenshot of how to export a dashboard as a PDF file" src="/images/dashboards_screenshot-full_export-pdf.webp" />

<figcaption>
  Accédez à <DNT>**[one.newrelic.com &gt; All capabilities](https://one.newrelic.com/all-capabilities) &gt; Dashboards**</DNT> et ouvrez un dashboard existant.
</figcaption>

Tenez compte des éléments suivants lors de l’exportation d’un dashboard au format PDF via l’interface utilisateur :

* [Les visualisations personnalisées](/docs/query-your-data/explore-query-data/dashboards/add-custom-visualizations-your-dashboards/) ne sont pas prises en charge.

Pour exporter au format PDF ou PNG avec des options supplémentaires telles que des dimensions personnalisées et des remplacements de variables de modèle, utilisez l’ [API NerdGraph](#export-api).

## Exporter des dashboards et des graphiques via l&apos;API NerdGraph [#export-api]

Utilisez l&apos;API NerdGraph pour exporter par programmation des dashboards et des widgets de graphiques individuels sous forme d&apos;images instantanées. L&apos;API prend en charge des options non disponibles dans l&apos;interface utilisateur, notamment les dimensions personnalisées, les surcharges de variables de modèle et les filtres d&apos;entité.

### Exporter un dashboard au format PDF ou PNG [#api-dashboard-snapshot]

La mutation `dashboardCreateSnapshotUrl` génère une URL d’instantané pour une page de dashboard complète.

<Callout variant="tip">
  Le paramètre `guid` nécessite le GUID de la **page**, et non le GUID du dashboard. Un dashboard peut avoir plusieurs pages. Pour trouver les GUID de page, utilisez la requête `entitySearch` ou ouvrez la page du dashboard et sélectionnez <DNT>**Manage JSON**</DNT> dans le menu <DNT>**...**</DNT>.

  ```graphql
  {
    actor {
      entitySearch(query: "parentId ='YOUR_DASHBOARD_GUID' AND tags.isDashboardPage = 'true'") {
        results {
          entities {
            guid
            name
          }
        }
      }
    }
  }
  ```
</Callout>

**Schéma de mutation :**

```graphql
mutation {
  dashboardCreateSnapshotUrl(
    guid: EntityGuid!
    params: SnapshotUrlInput
  ): String
}

input SnapshotUrlInput {
  timeWindow: SnapshotUrlTimeWindowInput
  display: SnapshotUrlDisplayInput
  variables: [SnapshotUrlVariableInput!]
  filter: String
  format: SnapshotUrlFormat
}

input SnapshotUrlTimeWindowInput {
  beginTime: EpochMilliseconds
  endTime: EpochMilliseconds
  duration: Milliseconds
}

input SnapshotUrlDisplayInput {
  width: Int
  height: Int
}

input SnapshotUrlVariableInput {
  name: String!
  values: [String!]!
}

enum SnapshotUrlFormat {
  PDF
  PNG
}
```

**Référence des paramètres :**

<table>
  <thead>
    <tr>
      <th>
        paramètres
      </th>

      <th>
        Type
      </th>

      <th>
        Défaut
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `guid`
      </td>

      <td>
        `EntityGuid!`
      </td>

      <td>
        —
      </td>

      <td>
        Obligatoire. Le GUID de l’entité de la page du dashboard.
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.beginTime`
      </td>

      <td>
        `EpochMilliseconds`
      </td>

      <td>
        Fenêtre temporelle actuelle du dashboard
      </td>

      <td>
        Début de la fenêtre de données (epoch ms).
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.endTime`
      </td>

      <td>
        `EpochMilliseconds`
      </td>

      <td>
        Fenêtre temporelle actuelle du dashboard
      </td>

      <td>
        Fin de la fenêtre de données (epoch ms).
      </td>
    </tr>

    <tr>
      <td>
        `params.timeWindow.duration`
      </td>

      <td>
        `Milliseconds`
      </td>

      <td>
        —
      </td>

      <td>
        Fenêtre de temps relative, alternative à 

        `beginTime`

        /

        `endTime`

        .
      </td>
    </tr>

    <tr>
      <td>
        `params.display.width`
      </td>

      <td>
        `Int`
      </td>

      <td>
        1920
      </td>

      <td>
        Largeur en pixels pour le snapshot.
      </td>
    </tr>

    <tr>
      <td>
        `params.display.height`
      </td>

      <td>
        `Int`
      </td>

      <td>
        1080
      </td>

      <td>
        Hauteur en pixels pour l’instantané.
      </td>
    </tr>

    <tr>
      <td>
        `params.variables`
      </td>

      <td>
        `[SnapshotUrlVariableInput!]`
      </td>

      <td>
        Valeurs actuelles des variables du dashboard
      </td>

      <td>
        Épinglez des valeurs spécifiques pour les variables de modèle de dashboard.
      </td>
    </tr>

    <tr>
      <td>
        `params.filter`
      </td>

      <td>
        `String`
      </td>

      <td>
        Aucun filtre
      </td>

      <td>
        Filtre d&apos;entité de type NRQL appliqué au moment du snapshot.
      </td>
    </tr>

    <tr>
      <td>
        `params.format`
      </td>

      <td>
        `SnapshotUrlFormat`
      </td>

      <td>
        `PDF`
      </td>

      <td>
        Format de sortie : 

        `PDF`

         ou 

        `PNG`

        .
      </td>
    </tr>
  </tbody>
</table>

**Exemples :**

<CollapserGroup>
  <Collapser id="basic-export" title="Exportation de base (PDF, paramètres par défaut)">
    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
      )
    }
    ```
  </Collapser>

  <Collapser id="png-custom-dimensions" title="Exporter en PNG avec des dimensions personnalisées">
    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
        params: {
          format: PNG
          display: {
            width: 1920
            height: 1080
          }
        }
      )
    }
    ```
  </Collapser>

  <Collapser id="variables-filter" title="Exporter avec des remplacements de variables et un filtre">
    Utilisez ceci pour épingler des valeurs de variables et des filtres spécifiques dans l’instantané, ce qui est utile pour l’automatisation des rapports planifiés.

    ```graphql
    mutation {
      dashboardCreateSnapshotUrl(
        guid: "YOUR_DASHBOARD_PAGE_GUID"
        params: {
          variables: [
            { name: "environment", values: ["production"] }
            { name: "region", values: ["us-east-1", "eu-west-1"] }
          ]
          filter: "env = 'production' AND service = 'checkout'"
        }
      )
    }
    ```
  </Collapser>
</CollapserGroup>

<Callout variant="important">
  Les URL de snapshot sont accessibles publiquement sans authentification et expirent après 3 mois. Partagez-les avec précaution conformément aux politiques de données de votre organisation.
</Callout>

### Obtenir une image de graphique [#api-widget-snapshot]

La mutation `dashboardWidgetCreateSnapshotUrl` génère une URL d’instantané PNG pour un seul widget de graphique. Le widget n’a pas besoin d’exister sur un dashboard — vous pouvez le définir en ligne, ce qui le rend utile pour générer des images de graphiques par programmation.

**Schéma de mutation :**

```graphql
mutation {
  dashboardWidgetCreateSnapshotUrl(
    widget: DeclarativeUiWidget!
  ): WidgetSnapshotResult
}

type WidgetSnapshotResult {
  url: String!
}
```

Le paramètre `widget` accepte un `DeclarativeUiWidget` — un objet JSON avec trois couches imbriquées :

```
declarative/widget envelope
└── widget node        ← title and widget-level props
    └── visualization  ← visualization type and configuration
```

```json
{
  "type": "declarative/widget",
  "version": 1,
  "content": {
    "type": "widget",
    "props": {
      "title": "My Chart Title"
    },
    "content": {
      "type": "visualization",
      "id": "viz.line",
      "props": {
        // visualization-specific config (nrqlQueries, colors, thresholds, etc.)
      }
    }
  }
}
```

Si vous créez la définition du widget à partir d’un dashboard que vous avez exporté au format JSON, utilisez cette modélisation de champ :

<table>
  <thead>
    <tr>
      <th>
        Champ JSON du dashboard
      </th>

      <th>
        Champ DeclarativeUiWidget
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `title`
      </td>

      <td>
        `content.props.title`
      </td>
    </tr>

    <tr>
      <td>
        `visualization.id`
      </td>

      <td>
        `content.content.id`
      </td>
    </tr>

    <tr>
      <td>
        `rawConfiguration.*`
      </td>

      <td>
        `content.content.props.*`

         (mêmes noms de champs)
      </td>
    </tr>

    <tr>
      <td>
        `layout`

        , 

        `id`

        , 

        `linkedEntityGuids`
      </td>

      <td>
        Non inclus — non nécessaire pour les instantanés autonomes
      </td>
    </tr>
  </tbody>
</table>

**Exemples :**

<CollapserGroup>
  <Collapser id="line-chart-snapshot" title="Exporter un graphique linéaire">
    ```graphql
    mutation {
      dashboardWidgetCreateSnapshotUrl(
        widget: {
          version: 1
          type: "declarative/widget"
          content: {
            type: "widget"
            props: { title: "Throughput by environment" }
            content: {
              type: "visualization"
              id: "viz.line"
              props: {
                nrqlQueries: [
                  {
                    accountIds: [YOUR_ACCOUNT_ID]
                    query: "SELECT count(*) FROM Transaction TIMESERIES"
                  }
                ]
                legend: { enabled: true }
              }
            }
          }
        }
      ) {
        url
      }
    }
    ```
  </Collapser>

  <Collapser id="billboard-snapshot" title="Exporter un graphique billboard avec des seuils">
    ```graphql
    mutation {
      dashboardWidgetCreateSnapshotUrl(
        widget: {
          version: 1
          type: "declarative/widget"
          content: {
            type: "widget"
            props: { title: "Error rate" }
            content: {
              type: "visualization"
              id: "viz.billboard"
              props: {
                nrqlQueries: [
                  {
                    accountIds: [YOUR_ACCOUNT_ID]
                    query: "SELECT percentage(count(*), WHERE error IS true) AS 'Error rate' FROM Transaction SINCE 1 hour ago"
                  }
                ]
                thresholds: [
                  { alertSeverity: "WARNING", value: 1 }
                  { alertSeverity: "CRITICAL", value: 5 }
                ]
              }
            }
          }
        }
      ) {
        url
      }
    }
    ```
  </Collapser>
</CollapserGroup>

<Callout variant="tip">
  Le champ `actor.account.nrql.staticChartUrl` legacy dans NerdGraph reste disponible mais ne prend pas en charge les dernières fonctionnalités de visualisation telles que les seuils, le formatage des couleurs et les types de graphiques plus récents. `dashboardWidgetCreateSnapshotUrl` est l’approche recommandée à l’avenir.
</Callout>

<Callout variant="important">
  Les URL de snapshot générées par `dashboardWidgetCreateSnapshotUrl` sont des images PNG, accessibles publiquement sans authentification, et expirent après 3 mois.
</Callout>

## Exporter un graphique au format CSV [#export-charts-csv]

Pour exporter un graphique de tableau de dashboard sous forme de fichier CSV, dans le coin supérieur droit du graphique, cliquez sur <DNT>**...**</DNT> et sélectionnez <DNT>**Export as CSV**</DNT>.

<img title="Export chart as CSV" alt="Screenshot of how to export a table chart as a CSV file" src="/images/dashboards_screenshot-crop_export-csv.webp" />

Tenez compte des éléments suivants lors de l’exportation d’un graphique au format CSV :

* Les données sont toujours exportées en heure UTC
* L&apos;exportation ne prend pas en charge les fonctions d&apos;agrégation telles que `average()`
* L&apos;exportation ne prend pas en charge la clause `COMPARE_WITH`
* Dans certains scénarios, l&apos;UI peut effectuer un formatage supplémentaire des données qui ne sont pas répliquées dans le fichier CSV exporté, ce qui peut entraîner de petites divergences (comme le rendu de -1.1E-17 sous la forme 0 dans l&apos;UI mais pas dans le fichier CSV)
* Vous ne pouvez pas exporter les résultats d&apos;une requête inter-comptes.

## requête et exploration des graphiques [#explore-chart-data]

Pour plus de détails sur ce que vous pouvez faire avec les graphiques, voir [Options d&apos;interrogation et de création de graphiques](/docs/new-relic-solutions/new-relic-one/ui-data/basic-ui-features/#data-analysis).