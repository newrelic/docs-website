---
title: Supprimer la définition du workflow
tags:
  - workflow automation
  - workflow
  - Delete Workflow Definition
  - workflow automation API
metaDescription: Used to delete all the workflow definition versions with the specified name.
freshnessValidatedDate: never
translationType: machine
---

Cette API supprime toutes les versions de définition workflow portant le nom spécifié.

<Callout variant="important">
  * Pour supprimer une définition, il ne doit exister aucune planification utilisant cette définition. Si la définition est actuellement utilisée par une planification, sa suppression échouera et renverra une erreur en conséquence.
  * Tous les flux workflow en cours s&apos;exécuteront jusqu&apos;à leur terme même si la définition est supprimée.
</Callout>

## Paramètres d&apos;entrée

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        paramètres
      </th>

      <th>
        Type
      </th>

      <th>
        Requis
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `definition.name`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Oui
      </td>

      <td>
        Le nom de la définition de workflow à supprimer. Toutes les versions portant ce nom seront supprimées.
      </td>
    </tr>

    <tr>
      <td>
        `scope.id`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Oui
      </td>

      <td>
        Votre ID de compte.
      </td>
    </tr>

    <tr>
      <td>
        `scope.type`
      </td>

      <td>
        Énumération
      </td>

      <td>
        Oui
      </td>

      <td>
        Le type de portée (ACCOUNT).
      </td>
    </tr>
  </tbody>
</table>

## Demande d&apos;échantillon

```graphql
mutation {
  workflowAutomationDeleteWorkflowDefinition(
    definition: {name: "my-workflow"}
    scope: {id: "12345678", type: ACCOUNT}
  ) {
    definition {
      name
    }
  }
}
```