---
title: WorkflowRunInfo
tags:
  - workflow automation
  - workflow
  - Workflow Run Info
  - workflow automation API
metaDescription: Used to retrieve information about a specific workflow run.
freshnessValidatedDate: never
translationType: machine
---

Cette API récupère des informations sur une exécution de workflow spécifique, y compris son statut, l&apos;URL du log et tout état de workflow enregistré pendant l&apos;exécution.

## Paramètres d&apos;entrée

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        paramètres
      </th>

      <th>
        Type
      </th>

      <th>
        Requis
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `account.id`
      </td>

      <td>
        Nombre
      </td>

      <td>
        Oui
      </td>

      <td>
        Votre ID de compte.
      </td>
    </tr>

    <tr>
      <td>
        `runId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Oui
      </td>

      <td>
        L&apos;identifiant unique de l&apos;exécution du workflow. Obtenu à partir de l&apos;API 

        `StartWorkflowRun`

        .
      </td>
    </tr>

    <tr>
      <td>
        `state.keys`
      </td>

      <td>
        String[]
      </td>

      <td>
        Non
      </td>

      <td>
        Un éventail de noms de clés d&apos;état à récupérer. Renvoie uniquement les paires valeur clé correspondantes enregistrées par 

        `state`

         étapes dans le workflow. Les clés introuvables dans l&apos;état du workflow sont omises de la réponse.
      </td>
    </tr>
  </tbody>
</table>

## Demande d&apos;échantillon

Utilisez le [générateur de requêtes](https://one.newrelic.com/nerdgraph-graphiql?state=03119d23-7b51-7b7a-1c99-d2cd026300e5) pour créer et éditer un workflow. Voir [l&apos;explorateur d&apos;API NerdGraph](/docs/apis/nerdgraph/get-started/nerdgraph-explorer).

```graphql
{
  actor {
    account(id: 11933347) {
      workflowAutomation {
        workflowRunInfo(runId: "8911c7e4-5a1f-4316-90c8-67adde72dd1c") {
          state(keys: ["foo", "count"]) {
            key
            value
          }
          logUrl
          status
        }
      }
    }
  }
}
```

## Exemple de réponse

La requête retourne :

* `logUrl`: URL pour afficher les logs d&apos;exécution du workflow.
* `status`: L’état actuel de l’exécution du workflow. Valeurs possibles : `STARTED`, `COMPLETED`, `FAILED`, `TIMED_OUT`, `STOPPED`.
* `state`: un éventail d&apos;objets `{ key, value }` pour chaque clé demandée trouvée dans l&apos;état du workflow. Les clés qui ne sont pas présentes dans l&apos;état du workflow sont omises.

```graphql
{
  "data": {
    "actor": {
      "account": {
        "workflowAutomation": {
          "workflowRunInfo": {
            "logUrl": "https://one.newrelic.com/...",
            "status": "COMPLETED",
            "state": [
              { "key": "foo", "value": "bar" },
              { "key": "count", "value": "2" }
            ]
          }
        }
      }
    }
  }
}
```

## Sujets connexes [#related-topics]

<DocTiles>
  <DocTile title="Démarrer l'exécution du workflow" path="/docs/workflow-automation/workflow-automation-apis/start-workflow-run">
    Exécutez un workflow pour obtenir un runId
  </DocTile>

  <DocTile title="Arrêter l'exécution du workflow" path="/docs/workflow-automation/workflow-automation-apis/stop-workflow-run">
    Annuler une instance de workflow en cours
  </DocTile>

  <DocTile title="Signaler l'exécution du workflow" path="/docs/workflow-automation/workflow-automation-apis/signal-workflow-run">
    Envoyer une entrée aux workflows en attente de signaux
  </DocTile>

  <DocTile title="Dépannage" path="/docs/workflow-automation/troubleshooting">
    Déboguer les erreurs courantes d&apos;exécution de workflow
  </DocTile>
</DocTiles>