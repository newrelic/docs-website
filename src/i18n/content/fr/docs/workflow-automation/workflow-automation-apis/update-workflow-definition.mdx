---
title: Mise à jour de la définition du workflow
tags:
  - workflow automation
  - workflow
  - Update Workflow Definition
  - workflow automation API
metaDescription: Used to update an existing workflow definition.
freshnessValidatedDate: never
translationType: machine
---

Cette API met à jour une définition de workflow existante. Elle renvoie une erreur si la définition du workflow n&apos;existe pas déjà.

## Paramètres d&apos;entrée

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        paramètres
      </th>

      <th>
        Type
      </th>

      <th>
        Requis
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `definition.yaml`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Oui
      </td>

      <td>
        La définition de workflow YAML encodée en base64.
      </td>
    </tr>

    <tr>
      <td>
        `scope.id`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Oui
      </td>

      <td>
        Votre ID de compte.
      </td>
    </tr>

    <tr>
      <td>
        `scope.type`
      </td>

      <td>
        Énumération
      </td>

      <td>
        Oui
      </td>

      <td>
        Le type de portée (ACCOUNT).
      </td>
    </tr>
  </tbody>
</table>

## Demande d&apos;échantillon

```graphql
  mutation {
    workflowAutomationUpdateWorkflowDefinition(
      definition: {yaml: "<workflow definition YAML>"}
      scope: {id: "11608789", type: ACCOUNT}
    ) {
      definition {
        name
        version
        yaml
      }
    }
  }
```

## Exemple de réponse

La mutation retourne :

* `definition.name`: Le nom de la définition de workflow mise à jour
* `definition.version`: Le nouveau numéro de version de la définition du workflow
* `definition.yaml`: La définition de workflow YAML encodée en base64

## Sujets connexes [#related-topics]

<DocTiles>
  <DocTile title="Valider la définition du workflow" path="/docs/workflow-automation/workflow-automation-apis/validate-workflow-definition">
    Testez la syntaxe YAML de votre workflow avant de mettre à jour
  </DocTile>

  <DocTile title="Récupérer la définition du workflow" path="/docs/workflow-automation/workflow-automation-apis/get-workflow-definition">
    Afficher la définition actuelle du workflow avant la mise à jour
  </DocTile>

  <DocTile title="Démarrer l'exécution du workflow" path="/docs/workflow-automation/workflow-automation-apis/start-workflow-run">
    Exécutez votre workflow mis à jour
  </DocTile>

  <DocTile title="Schéma de définition" path="/docs/workflow-automation/workflow-automation-apis/definition-schema">
    Référence de la structure YAML du workflow
  </DocTile>
</DocTiles>