---
title: Actions AWS S3
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS S3 actions
metaDescription: A reference for AWS S3 actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

Cette page fournit une référence pour les actions AWS S3 disponibles dans le catalogue des actions d&apos;automatisation workflow. Utilisez ces actions pour lister, récupérer, charger, et supprimer des objets dans les buckets Amazon S3.

## Prérequis

Avant d&apos;utiliser AWS Actions dans l&apos;automatisation workflow, assurez-vous de disposer des éléments suivants :

* Un compte AWS disposant des autorisations appropriées.
* Informations d&apos;identification AWS configurées (informations d&apos;identification de l&apos;utilisateur IAM, ARN du rôle IAM ou informations d&apos;identification de session).
* Les autorisations IAM nécessaires pour les services AWS spécifiques que vous prévoyez d&apos;utiliser.

Consultez la section [Configurer les identifiants AWS](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) pour savoir comment créer des utilisateurs et des rôles IAM, et configurer des identifiants AWS statiques et de session pour l&apos;intégration aux actions AWS de Workflow Automation.

## Lister les objets dans un bucket

L’identifiant de l’action est `aws.s3.listObjectsV2`.

Renvoie une partie ou la totalité (jusqu’à 1000) des objets d’un bucket avec leurs métadonnées. Il s’agit de la version recommandée de `listObjects`.

Le tableau suivant décrit les champs d’entrée pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saisir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Description**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN du rôle IAM à assumer pour les appels d’API AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de clé d’accès AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Clé d’accès secrète AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de session temporaire pour l’authentification STS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Région AWS du bucket cible.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Nom du bucket S3 à partir duquel lister les objets.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `prefix`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Limite la réponse aux clés qui commencent par ce préfixe.
      </td>

      <td>
        `path/to/folder/`
      </td>
    </tr>

    <tr>
      <td>
        `maxKeys`
      </td>

      <td>
        Entier
      </td>

      <td>
        Facultatif. Nombre maximum de clés à retourner (jusqu&apos;à 1000).
      </td>

      <td>
        `100`
      </td>
    </tr>

    <tr>
      <td>
        `continuationToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton d’une réponse précédente pour récupérer la page de résultats suivante.
      </td>

      <td>
        `some-token`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Carte
      </td>

      <td>
        Facultatif. Paramètres boto3 supplémentaires à transmettre à l&apos;appel d&apos;API.
      </td>

      <td>
        `{"Delimiter": "/"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        List
      </td>

      <td>
        Facultatif. Sélecteurs JQ pour extraire des champs spécifiques de la sortie de l’action.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Des champs boto3 facultatifs supplémentaires (`EncodingType`, `FetchOwner`, `StartAfter`, `RequestPayer`, `ExpectedBucketOwner`, `OptionalObjectAttributes`, `Delimiter`) sont disponibles via l’entrée `parameters`. Consultez la [référence list\_objects\_v2 de boto3](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html) pour plus de détails.

Le tableau suivant décrit les champs de sortie pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Sortir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objet
      </td>

      <td>
        Voir la 

        [référence boto3 list_objects_v2](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booléen
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        `"Parameter validation failed: Invalid bucket name"`
      </td>
    </tr>
  </tbody>
</table>

**Exemple de workflow**

```yaml
name: aws-s3-list-objects-v2
description: 'List Objects in an AWS S3 Bucket'
steps:
  - name: aws_s3_listObjectsV2_1
    type: action
    action: aws.s3.listObjectsV2
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      prefix: "path/to/folder/"
      maxKeys: 100
    next: end
```

## Supprimer un objet d&apos;un bucket

L’identifiant de l’action est `aws.s3.deleteObject`.

Supprime définitivement un seul objet d&apos;un bucket. Pour les buckets versionnés, cette opération insère un marqueur de suppression, qui masque l&apos;objet sans le supprimer définitivement à moins qu&apos;un `VersionId` ne soit spécifié.

Le tableau suivant décrit les champs d’entrée pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saisir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Description**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN du rôle IAM à assumer pour les appels d’API AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de clé d’accès AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Clé d’accès secrète AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de session temporaire pour l’authentification STS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Région AWS du bucket cible.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Nom du bucket S3 contenant l&apos;objet.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Clé complète (chemin) de l&apos;objet à supprimer.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Carte
      </td>

      <td>
        Facultatif. Paramètres boto3 supplémentaires à transmettre à l&apos;appel d&apos;API.
      </td>

      <td>
        `{"VersionId": "testVersion", "MFA": "Test"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        List
      </td>

      <td>
        Facultatif. Sélecteurs JQ pour extraire des champs spécifiques de la sortie de l’action.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Des champs boto3 facultatifs supplémentaires (`RequestPayer`, `BypassGovernanceRetention`, `ExpectedBucketOwner`, `IfMatch`, `VersionId`, `MFA`) sont disponibles via l’entrée `parameters`. Consultez la [référence boto3 delete\_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html) pour plus de détails.

Le tableau suivant décrit les champs de sortie pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Sortir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objet
      </td>

      <td>
        Consultez la 

        [référence boto3 delete_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booléen
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        `"Parameter validation failed: Invalid bucket name"`
      </td>
    </tr>
  </tbody>
</table>

**Exemple de workflow**

```yaml
name: aws-s3-delete-object
description: 'Delete an AWS S3 Object'
steps:
  - name: aws_s3_deleteObject_1
    type: action
    action: aws.s3.deleteObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-west-2"
      bucket: "my-bucket"
      key: "path/to/object.txt"
    next: end
```

## Ajouter un objet à un bucket

L’identifiant de l’action est `aws.s3.putObject`.

Ajoute un objet à un bucket. Amazon S3 est un système distribué : s&apos;il reçoit plusieurs requests d&apos;écriture pour le même objet simultanément, il écrase tout sauf le dernier objet écrit.

Le tableau suivant décrit les champs d’entrée pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saisir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Description**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN du rôle IAM à assumer pour les appels d’API AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de clé d’accès AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Clé d’accès secrète AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de session temporaire pour l’authentification STS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Région AWS du bucket cible.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Obligatoire. Nom du bucket S3 dans lequel charger l&apos;objet.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Clé complète (chemin) pour l&apos;objet dans le bucket.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `body`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Obligatoire. Les données de l&apos;objet à charger.
      </td>

      <td>
        `file content`
      </td>
    </tr>

    <tr>
      <td>
        `contentType`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Type MIME standard de l’objet.
      </td>

      <td>
        `plain/text`
      </td>
    </tr>

    <tr>
      <td>
        `tagging`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Ensemble de tags encodé en URL à appliquer à l’objet.
      </td>

      <td>
        `key1=value1`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Carte
      </td>

      <td>
        Facultatif. Paramètres boto3 supplémentaires à transmettre à l&apos;appel d&apos;API.
      </td>

      <td>
        `{"ServerSideEncryption": "AES256", "Metadata": {"metadata1": "value1"}}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        List
      </td>

      <td>
        Facultatif. Sélecteurs JQ pour extraire des champs spécifiques de la sortie de l’action.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Des champs boto3 facultatifs supplémentaires sont disponibles via l’entrée `parameters`. Consultez la [référence boto3 put\_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html) pour la liste complète.

Le tableau suivant décrit les champs de sortie pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Sortir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objet
      </td>

      <td>
        Consultez la 

        [référence boto3 put_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booléen
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        `"An error occurred (NoSuchBucket) when calling the PutObject operation: The specified bucket does not exist"`
      </td>
    </tr>
  </tbody>
</table>

**Exemple de workflow**

```yaml
name: s3-put-object
description: 'Put an AWS S3 Object'
steps:
  - name: aws_s3_putObject_1
    type: action
    action: aws.s3.putObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
      body: "Hello world"
      contentType: "plain/text"
      tagging: "key1=value1"
    next: end
```

## Récupérer un objet depuis un bucket

L’identifiant de l’action est `aws.s3.getObject`.

Récupère un objet depuis Amazon S3. Spécifiez le nom de clé complet pour l’objet dans la requête.

<Callout variant="important">
  Cette action échoue si l’objet est supérieur à 100 Ko.
</Callout>

Le tableau suivant décrit les champs d’entrée pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saisir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Description**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN du rôle IAM à assumer pour les appels d’API AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de clé d’accès AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Clé d’accès secrète AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de session temporaire pour l’authentification STS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Région AWS du bucket cible.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `bucket`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Nom du bucket S3 contenant l&apos;objet.
      </td>

      <td>
        `examplebucket`
      </td>
    </tr>

    <tr>
      <td>
        `key`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Clé complète (chemin) de l’objet à récupérer.
      </td>

      <td>
        `path/to/object.txt`
      </td>
    </tr>

    <tr>
      <td>
        `versionId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de version de l&apos;objet à récupérer. En cas d&apos;omission, la version actuelle est retournée.
      </td>

      <td>
        `some-version-id`
      </td>
    </tr>

    <tr>
      <td>
        `range`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. En-tête HTTP Range pour récupérer une plage d’octets spécifique de l’objet.
      </td>

      <td>
        `bytes=0-99`
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        Carte
      </td>

      <td>
        Facultatif. Paramètres boto3 supplémentaires à transmettre à l&apos;appel d&apos;API.
      </td>

      <td>
        `{"ChecksumMode": "ENABLED"}`
      </td>
    </tr>

    <tr>
      <td>
        `selectors`
      </td>

      <td>
        List
      </td>

      <td>
        Facultatif. Sélecteurs JQ pour extraire des champs spécifiques de la sortie de l’action.
      </td>

      <td>
        `[{"name": "response", "expression": ".response"}]`
      </td>
    </tr>
  </tbody>
</table>

Des champs boto3 facultatifs supplémentaires sont disponibles via l’entrée `parameters`. Consultez la [référence boto3 get\_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html) pour obtenir la liste complète.

Le tableau suivant décrit les champs de sortie pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Sortir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objet
      </td>

      <td>
        Consultez la 

        [référence boto3 get_object](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html)

        .
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booléen
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        `"An error occurred (InvalidArgument) when calling the GetObject operation: Invalid version id specified"`
      </td>
    </tr>
  </tbody>
</table>

**Exemple de workflow**

```yaml
name: s3-get-object
description: 'Get an AWS S3 Object'
steps:
  - name: aws_s3_getObject_1
    type: action
    action: aws.s3.getObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
    next: end
```