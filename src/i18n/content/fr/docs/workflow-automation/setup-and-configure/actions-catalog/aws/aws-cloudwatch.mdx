---
title: Actions AWS CloudWatch
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS CloudWatch actions
metaDescription: A reference for AWS CloudWatch actions available in the workflow automation actions catalog.
freshnessValidatedDate: '2026-02-24T00:00:00.000Z'
translationType: machine
---

Cette page fournit une référence pour les actions AWS CloudWatch disponibles dans le catalogue des actions d&apos;automatisation des workflows. Utilisez ces actions pour récupérer et charger des données de log dans AWS CloudWatch.

## Prérequis

Avant d&apos;utiliser AWS Actions dans l&apos;automatisation workflow, assurez-vous de disposer des éléments suivants :

* Un compte AWS disposant des autorisations appropriées.
* Informations d&apos;identification AWS configurées (informations d&apos;identification de l&apos;utilisateur IAM, ARN du rôle IAM ou informations d&apos;identification de session).
* Les autorisations IAM nécessaires pour les services AWS spécifiques que vous prévoyez d&apos;utiliser.

Consultez la section [Configurer les identifiants AWS](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) pour savoir comment créer des utilisateurs et des rôles IAM, et configurer des identifiants AWS statiques et de session pour l&apos;intégration aux actions AWS de Workflow Automation.

## Get événement de log

L’identifiant de l’action est `aws.cloudwatch.getLogEvents`.

Récupère un lot d&apos;événements de log à partir d&apos;un flux de log spécifié dans AWS CloudWatch Logs.

Le tableau suivant décrit les champs d’entrée pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saisir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Description**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN du rôle IAM à assumer pour les appels d’API AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de clé d’accès AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Clé d’accès secrète AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de session temporaire pour l’authentification STS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Région AWS du groupe de logs cible.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `logStreamName`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Nom du flux de logs à partir duquel récupérer les événements.
      </td>

      <td>
        `2023/10/27/[$LATEST]abcdef123456`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupName`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Nom du groupe de log contenant le flux de log.
      </td>

      <td>
        `/aws/lambda/my-function`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupIdentifier`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN ou nom du groupe de logs. Utilisez à la place de 

        `logGroupName`

         lorsque vous référencez un groupe de logs par ARN.
      </td>

      <td>
        `arn:partition:service:region:account-id:resource`
      </td>
    </tr>

    <tr>
      <td>
        `startTime`
      </td>

      <td>
        Int
      </td>

      <td>
        Facultatif. Début de la plage de temps, exprimé sous forme d&apos;horodatage Unix en millisecondes.
      </td>

      <td>
        `1759296000000`
      </td>
    </tr>

    <tr>
      <td>
        `endTime`
      </td>

      <td>
        Int
      </td>

      <td>
        Facultatif. Fin de la plage de temps, exprimée sous forme d&apos;horodatage Unix en millisecondes.
      </td>

      <td>
        `1759296000000`
      </td>
    </tr>

    <tr>
      <td>
        `limit`
      </td>

      <td>
        Int
      </td>

      <td>
        Facultatif. Nombre maximal d&apos;événements de log à renvoyer.
      </td>

      <td>
        `50`
      </td>
    </tr>

    <tr>
      <td>
        `startFromHead`
      </td>

      <td>
        Booléen
      </td>

      <td>
        Facultatif. Si 

        `true`

        , renvoie les événements de log les plus anciens en premier. La valeur par défaut est 

        `false`

        .
      </td>

      <td>
        `true`
      </td>
    </tr>

    <tr>
      <td>
        `unmask`
      </td>

      <td>
        Booléen
      </td>

      <td>
        Facultatif. Si 

        `true`

        , renvoie les données d&apos;événement de log non masquées. La valeur par défaut est 

        `false`

        .
      </td>

      <td>
        `false`
      </td>
    </tr>

    <tr>
      <td>
        `nextToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de pagination d’une réponse précédente pour récupérer la page de résultats suivante.
      </td>

      <td>
        `f/39218833627378687642013305455131706539523449361490509828/s`
      </td>
    </tr>
  </tbody>
</table>

Le tableau suivant décrit les champs de sortie pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Sortir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objet
      </td>

      <td>
        `{"events": [{"timestamp": 123, "message": "string", "ingestionTime": 123}], "nextForwardToken": "string", "nextBackwardToken": "string"}`
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booléen
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        `"An error occurred (ExpiredTokenException) when calling the GetLogEvents operation: The security token included in the request is expired"`
      </td>
    </tr>
  </tbody>
</table>

L&apos;exemple de workflow suivant montre comment utiliser cette action.

```yaml
name: get-lambda-logs
description: 'Retrieve log events from an AWS Lambda function'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: /aws/lambda/my-function
  logStreamName:
    type: String
    defaultValue: 2023/10/27/[$LATEST]abcdef123456
steps:
  - name: get_events_step
    type: action
    action: aws.cloudwatch.getLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      limit: 100
    next: end
```

## Téléverser des événements de log

L’identifiant de l’action est `aws.cloudwatch.putLogEvents`.

Télécharge un lot d&apos;événements de log vers le flux de log spécifié dans AWS CloudWatch Logs.

Le tableau suivant décrit les champs d’entrée pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Saisir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Description**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `awsRoleArn`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ARN du rôle IAM à assumer pour les appels d’API AWS.
      </td>

      <td>
        `arn:aws:iam::123456789012:role/my-workflow-role`
      </td>
    </tr>

    <tr>
      <td>
        `awsAccessKeyId`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. ID de clé d’accès AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsAccessKeyId }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSecretAccessKey`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Clé d’accès secrète AWS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSecretAccessKey }}`
      </td>
    </tr>

    <tr>
      <td>
        `awsSessionToken`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Facultatif. Jeton de session temporaire pour l’authentification STS. Transmettez en tant que secret.
      </td>

      <td>
        `${{ :secrets:awsSessionToken }}`
      </td>
    </tr>

    <tr>
      <td>
        `region`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Région AWS du groupe de logs cible.
      </td>

      <td>
        `us-east-2`
      </td>
    </tr>

    <tr>
      <td>
        `logGroupName`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Obligatoire. Nom du groupe de logs dans lequel charger les événements.
      </td>

      <td>
        `/aws/lambda/hello-you`
      </td>
    </tr>

    <tr>
      <td>
        `logStreamName`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        Requis. Nom du flux de log vers lequel télécharger les événements.
      </td>

      <td>
        `2025/09/24/[$LATEST]09f7ca9e9ab044f389419ce60305f594`
      </td>
    </tr>

    <tr>
      <td>
        `logEvents`
      </td>

      <td>
        List
      </td>

      <td>
        Requis. Liste des événements de log à envoyer. Chaque événement doit inclure un 

        `timestamp`

         (millisecondes Unix) et une chaîne 

        `message`

        .
      </td>

      <td>
        `[{"timestamp": 1698384000000, "message": "Workflow task started."}]`
      </td>
    </tr>

    <tr>
      <td>
        `entity`
      </td>

      <td>
        Dict
      </td>

      <td>
        Facultatif. Métadonnées d&apos;entité à associer aux événements de log pour la corrélation d&apos;entité New Relic.
      </td>

      <td>
        `{"keyAttributes": {"ResourceType": "AWS::ElasticLoadBalancingV2::LoadBalancer", "Identifier": "app/my-web-lb/12345", "Environment": "Production"}, "attributes": {"MonitoringTier": "Gold"}}`
      </td>
    </tr>
  </tbody>
</table>

Le tableau suivant décrit les champs de sortie pour cette action.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>
        **Sortir**
      </th>

      <th>
        **Type**
      </th>

      <th>
        **Exemple**
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `response`
      </td>

      <td>
        Objet
      </td>

      <td>
        `{"nextSequenceToken": "string", "rejectedLogEventsInfo": {"tooNewLogEventStartIndex": 123, "tooOldLogEventEndIndex": 123, "expiredLogEventEndIndex": 123}}`
      </td>
    </tr>

    <tr>
      <td>
        `success`
      </td>

      <td>
        Booléen
      </td>

      <td>
        `true`

         ou 

        `false`
      </td>
    </tr>

    <tr>
      <td>
        `errorMessage`
      </td>

      <td>
        Chaîne
      </td>

      <td>
        `"An error occurred (ExpiredTokenException) when calling the PutLogEvents operation: The security token included in the request is expired"`
      </td>
    </tr>
  </tbody>
</table>

L&apos;exemple de workflow suivant montre comment utiliser cette action.

```yaml
name: put-custom-logs
description: 'Upload a batch of custom log events to CloudWatch'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: my-custom-app-logs
  logStreamName:
    type: String
    defaultValue: instance-01-thread-01
steps:
  - name: upload_logs
    type: action
    action: aws.cloudwatch.putLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      logEvents:
        - timestamp: 1698384000000
          message: "Workflow started successfully"
        - timestamp: 1698384005000
          message: "Step 1 completed"
    next: end
```