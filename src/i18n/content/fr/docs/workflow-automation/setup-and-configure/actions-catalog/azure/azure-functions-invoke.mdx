---
title: Invoquer une Azure Function
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - Azure Functions actions
metaDescription: Documentation for the azure.functions.invoke action in the New Relic workflow automation actions catalog
freshnessValidatedDate: '2026-08-24T00:00:00.000Z'
translationType: machine
---

L’action Azure Functions Invoke vous permet d’appeler n’importe quelle Azure Function déclenchée par HTTP directement depuis vos workflows. Elle fournit une interface prise en charge et validée pour appeler des Azure Functions (avec des entrées typées, une validation d’URL, et une authentification basée sur des secrets) plutôt que de construire des appels HTTP bruts.

## Invoquer une Azure Function [#azure-functions-invoke]

Cette action envoie une requête HTTP à un point de terminaison Azure Function et renvoie la réponse. L’authentification utilise le mécanisme de clé de fonction d’Azure ; le workflow la transmet en tant qu’en-tête `x-functions-key`.

<Tabs>
  <TabsBar>
    <TabsBarItem id="azurefunctionsinvokeinputs">
      Entrées
    </TabsBarItem>

    <TabsBarItem id="azurefunctionsinvokeoutputs">
      Sorties
    </TabsBarItem>

    <TabsBarItem id="azurefunctionsinvokeexample">
      Exemple
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="azurefunctionsinvokeinputs">
      Les champs suivants configurent la requête Azure Function.

      <table>
        <thead>
          <tr>
            <th>
              Champ de saisie
            </th>

            <th>
              optionnalité
            </th>

            <th>
              Type
            </th>

            <th>
              Description
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `function`
            </td>

            <td>
              Requis
            </td>

            <td>
              Chaîne
            </td>

            <td>
              URL HTTPS complète du point de terminaison de déclencheur HTTP Azure Function. Par exemple, 

              `"https://myapp.azurewebsites.net/api/MyFunction"`

              .
            </td>
          </tr>

          <tr>
            <td>
              `key`
            </td>

            <td>
              Facultatif
            </td>

            <td>
              Chaîne
            </td>

            <td>
              Une clé API au niveau de la fonction ou de l’hôte pour l’authentification. Le workflow l’envoie en tant qu’en-tête 

              `x-functions-key`

              . Transmettez en tant que secret.
            </td>
          </tr>

          <tr>
            <td>
              `method`
            </td>

            <td>
              Requis
            </td>

            <td>
              Chaîne
            </td>

            <td>
              Méthode HTTP à utiliser. L’une des valeurs 

              `"GET"`

              , 

              `"POST"`

              , 

              `"PUT"`

              , 

              `"DELETE"`

              , 

              `"PATCH"`

              . La valeur par défaut est 

              `"POST"`

              .
            </td>
          </tr>

          <tr>
            <td>
              `body`
            </td>

            <td>
              Facultatif
            </td>

            <td>
              Chaîne
            </td>

            <td>
              Corps de la requête sous forme de chaîne JSON. Ne s’applique que lorsque 

              `method`

               n’est pas 

              `GET`

               ou 

              `HEAD`

              . Accepte les références secrètes.
            </td>
          </tr>

          <tr>
            <td>
              `headers`
            </td>

            <td>
              Facultatif
            </td>

            <td>
              Carte
            </td>

            <td>
              En-têtes HTTP supplémentaires au format JSON. Par exemple, 

              `{"X-Custom-Header": "value", "Content-Type": "application/json"}`

              . Accepte les références secrètes.
            </td>
          </tr>

          <tr>
            <td>
              `queryParameters`
            </td>

            <td>
              Facultatif
            </td>

            <td>
              Carte
            </td>

            <td>
              Paramètres de requête à ajouter à l’URL de la fonction. Ne doit pas commencer par 

              `?`

               ou 

              `&`

              . Par exemple, 

              `param1=value1&param2=value2`

              .
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="azurefunctionsinvokeoutputs">
      L&apos;action renvoie les champs suivants.

      <table>
        <thead>
          <tr>
            <th>
              Champ de sortie
            </th>

            <th>
              Type
            </th>

            <th>
              Description
            </th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              `statusCode`
            </td>

            <td>
              Entier
            </td>

            <td>
              Code d’état HTTP renvoyé par l’Azure Function. Par exemple, 

              `200`

              , 

              `404`

               ou 

              `500`

              .
            </td>
          </tr>

          <tr>
            <td>
              `response`
            </td>

            <td>
              Chaîne
            </td>

            <td>
              Corps de la réponse de l’Azure Function. Peut être au format JSON, texte brut, HTML, ou tout autre format.
            </td>
          </tr>

          <tr>
            <td>
              `success`
            </td>

            <td>
              Booléen
            </td>

            <td>
              Indique si l&apos;appel de la fonction a réussi. 

              `true`

               pour les réponses 2xx (200–299), 

              `false`

               sinon.
            </td>
          </tr>
        </tbody>
      </table>
    </TabsPageItem>

    <TabsPageItem id="azurefunctionsinvokeexample">
      ### Exemple : déclencher une fonction de remédiation

      ```yaml
      name: azure-function-invoke-remediation

      steps:
        - name: invokeRemediationFunction
          type: action
          action: azure.functions.invoke
          version: 1
          inputs:
            function: "https://remediation.azurewebsites.net/api/fix-incident"
            key: ${{ :secrets:azure:functionKey }}
            method: "POST"
            body: '{"incidentId":"${{ .workflowInputs.incidentId }}"}'
      ```

      ### Exemple : requête GET avec des paramètres de requête

      ```yaml
      name: azure-function-invoke-get

      steps:
        - name: getFromFunction
          type: action
          action: azure.functions.invoke
          version: 1
          inputs:
            function: "https://myapp.azurewebsites.net/api/GetStatus"
            key: ${{ :secrets:azure:functionKey }}
            method: "GET"
            queryParameters: "env=prod&region=eastus"
      ```

      ### Exemple : invoquez une fonction et enregistrez le résultat

      ```yaml
      name: azure-function-invoke-and-log

      steps:
        - name: invokeFunction
          type: action
          action: azure.functions.invoke
          version: 1
          inputs:
            function: "https://myapp.azurewebsites.net/api/RunCheck"
            key: ${{ :secrets:azure:functionKey }}
            method: "POST"
            body: '{"target":"${{ .workflowInputs.target }}"}'
            headers:
              Content-Type: "application/json"

        - name: logFunctionResult
          type: action
          action: newrelic.ingest.sendLogs
          version: 1
          inputs:
            logs:
              - message: "Azure Function invocation complete"
                attributes:
                  success: ${{ .steps.invokeFunction.outputs.success }}
                  statusCode: ${{ .steps.invokeFunction.outputs.statusCode }}
                  response: ${{ .steps.invokeFunction.outputs.response }}
      ```
    </TabsPageItem>
  </TabsPages>
</Tabs>