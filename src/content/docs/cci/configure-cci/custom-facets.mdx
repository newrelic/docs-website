---
title: Custom facets
tags:
  - cloud cost intelligence
  - custom facets
  - virtual dimensions
  - cost grouping
  - configure CCI
metaDescription: "Create custom facets in Cloud Cost Intelligence to define virtual dimensions that group multi-cloud costs based on your own rules."
freshnessValidatedDate: 2026-08-06
---

Custom facets let you create virtual dimensions that group cloud costs based on rules you define. After you create a facet, it's available as a new dimension across CCI features — without needing to reapply filters each time.

Custom facets work the same way as the built-in <DNT>**Custom Dimensions**</DNT> already visible in <DNT>**Cost Overview**</DNT> (for example, account name, cell type, environment). The difference is that custom facets are dimensions you define yourself by combining tags and conditions from your cloud accounts.

## How custom facets work

A custom facet is a dimension made up of one or more named values. For each value, you define a set of conditions. The system evaluates each cost row against those conditions and assigns matching rows to the corresponding value. Rows that match no conditions receive the default value you specify. The facet then appears as a new dimension throughout CCI.

For example, you could create a facet named `platform-team` with conditions that match specific AWS tags, account names, and regions. The system assigns every cost row matching those conditions to `platform-team`, which you can then use to build budgets, set alert conditions, or analyze costs in unit economics.

## Use cases

### Multi-cloud cost grouping

If the same workload runs across AWS, Azure, and GCP, you can combine all three into a single facet group. Define conditions that match the relevant accounts, services, or tags from each provider. The resulting facet lets you view and act on the combined cost without managing separate filters for each cloud.

### Cost per transaction across clouds

To calculate cost per transaction for a service that spans multiple cloud providers:

1. Create a facet that groups the relevant AWS and Azure resources for that service.
2. In <DNT>**Unit economics**</DNT>, select the facet as the cost dimension.
3. Divide by your transaction metric to get cost per transaction.

This gives you a unified view of the cost regardless of which cloud the traffic is processed on.

### Dimensional budgets for teams or projects

Create a facet that matches the tags or accounts belonging to a specific team or project. Then build a budget directly on that facet so the team can track their cloud spend without needing a separate cloud account per team.

## Limits and exclusions

| Item | Detail |
|------|--------|
| Maximum facets per user | 12 |
| Resources excluded | The system doesn't evaluate deleted or inactive resources against facet rules, and they don't appear in facet results |

## Create a custom facet

You can create facets through the UI or by uploading a YAML file.

### Create using the UI

1. Go to <DNT>[**one.newrelic.com**](https://one.newrelic.com)</DNT> > <DNT>**All Capabilities**</DNT> > <DNT>**Cloud Cost Intelligence**</DNT> > <DNT>**Custom Facets**</DNT>.
2. Click <DNT>**Create facet**</DNT>.
3. Enter a <DNT>**Name**</DNT>, <DNT>**Description**</DNT>, and <DNT>**Default value**</DNT> for the facet.
4. Click <DNT>**+ Add facet value**</DNT>.
5. Add one or more conditions. Each condition specifies:
   - **Dimension** (for example, account, cloud provider, AI model)
   - **Operator** (for example, `in`, `not in`)
   - **Values** (one or more)
6. Save the facet.

<img title="Cloud cost intelligence add facet" alt="A form for adding a custom facet with condition fields for dimension, operator, and values" src="/images/add-facet.webp"/>

The facet appears as a new dimension in <DNT>**Cost Overview**</DNT> and other CCI features.

### Import using YAML

To define multiple facets at once or migrate rules from another system, upload a YAML configuration file.

1. In <DNT>**Custom Facets**</DNT>, click <DNT>**Upload YAML**</DNT>.
2. Select your configuration file.

The system validates the file and applies the rules. To see the required file structure, click <DNT>**Download sample YAML**</DNT> and use the sample as a template.

### Backfill historical data

By default, a new facet applies to cost data from the date it was created. To apply the same rules to historical data, use backfill.

1. After creating or uploading your facet rules, select the <DNT>**Backfill**</DNT> option.
2. Specify the start date for the historical period you want to include.

The system reprocesses the matching cost rows for that period and populates the facet dimension retroactively.

## Use facets across CCI

After you create a facet, it's available as a dimension in:

- <DNT>**Cost Overview**</DNT>: Filter and group costs by the facet dimension
- <DNT>**Budgets**</DNT>: Create a budget scoped to a specific facet value
- <DNT>**Alert conditions**</DNT>: Set alerts triggered by spend on a facet group
- <DNT>**Unit economics**</DNT>: Use a facet as the denominator or numerator dimension for cost-per-unit calculations
- <DNT>**Recommendations**</DNT>: Get optimization recommendations scoped to the facet group
