---
title: 'Mobile Logs'
tags:
  - Mobile monitoring
  - Mobile monitoring UI
  - Logs
metaDescription: Use New Relic's Mobile Logs Monitoring UI page to view Mobile Logs
freshnessValidatedDate: 2024-06-27
---

In mobile apps, logs can be sent to New Relic.

## APIs to Capture Logs [#logs-apis]

<Callout variant="important">
### Understanding Mobile Logging delays [#mobile-logging-delays]

It will take up to 15 minutes for logs to appear within New Relics Logs Tab. 
It will take up to 15 minutes for changes to Mobile Logs toggle, sampling rate, and log level to be reflected within your mobile application.
</Callout>


Our mobile agents report logs to New Relic. This can be used to monitor the logs being emitted by your application.

iOS agent: To use the logging APIs add the following APIs:

```
NewRelic.logError("Encountered error=error=\(error.localizedDescription).")

NewRelic.logWarning("Warning text.")

NewRelic.logInfo("Info text.")

NewRelic.logVerbose("NewRelic.start was called.")

NewRelic.logDebug("Debug text.")
```

## View Logs in New Relic [#view-logs-in-new-relic]

By default, our mobile agent does not collect logs.

To view the Logs page:

1. Go to <DoNotTranslate>**[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities)</DoNotTranslate>.
2. Click <DoNotTranslate>**Mobile**</DoNotTranslate>.
3. Select a Mobile app.
4. In the left pane, click <DoNotTranslate>**Logs**</DoNotTranslate>.

## Enable mobile logs [#enable-mobile-logs]

If you need to enable mobile logs:

1. In New Relic, navigate to your mobile app.
2. In the left pane under <DoNotTranslate>**Settings**</DoNotTranslate>, click <DoNotTranslate>**Application**</DoNotTranslate>.
3. Toggle **Mobile Logs** on.
4. Click **Save**.

## Configure log level and sampling rate for mobile logs [#configure-mobile-logs]

If you need to configure your mobile logs sampling rate or log level:

1. In New Relic, navigate to your mobile app.
2. In the left pane under <DoNotTranslate>**Settings**</DoNotTranslate>, click <DoNotTranslate>**Application**</DoNotTranslate>.
3. To change the sample rate select a new value in the field under "Sample rate of total sessions".
4. To change the log level select your preferred log level in the drop down under "Logs verbosity".