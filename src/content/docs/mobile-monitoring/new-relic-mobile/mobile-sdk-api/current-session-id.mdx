---
title: Current session ID 
type: apiDoc
shortDescription: 'Returns ID for current session.'
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Mobile app monitoring API to return the ID for current session.
redirects:
- /docs/mobile-monitoring/new-relic-mobile-ios/ios-sdk-api/currentsessionid-ios-sdk-api
- /docs/mobile-monitoring/new-relic-mobile-android/android-sdk-api/currentsessionid-android-agent-api
- /docs/mobile-monitoring/new-relic-mobile-android/android-sdk-api/current-session-id
- /docs/mobile-monitoring/new-relic-mobile-ios/ios-sdk-api/current-session-id
- /docs/mobile-monitoring/new-relic-mobile-android/android-sdk-api/currentsessionid-android-sdk-api
---

<Tabs>
	<TabsBar>
        <TabsBarItem id="android">
            Android
        </TabsBarItem>
        <TabsBarItem id="ios">
            iOS
        </TabsBarItem>
        <TabsBarItem id="capacitor">
            Capacitor
        </TabsBarItem>
        <TabsBarItem id="cordova">
            Cordova
        </TabsBarItem>
        <TabsBarItem id="flutter">
            Flutter
        </TabsBarItem>
        <TabsBarItem id="react">
            React Native
        </TabsBarItem>
        <TabsBarItem id="xamarin">
            Xamarin
        </TabsBarItem>
        <TabsBarItem id="maui">
            .NET MAUI
        </TabsBarItem>
    </TabsBar>

    <TabsPages>
        <TabsPageItem id="android">
## Syntax

```java
NewRelic.currentSessionId()
```

## Description

Returns ID for current session.

This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier. For example, you might want to use the same identifier for marketing analytics, or user analytics.

For more context on how to use this API, see [Send custom attributes and events](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/work-android-sdk-api#attributes-events-insights).


## Requirements

Compatible with all agent versions.

## Return values

Returns ID string for the current session.

## Example

```java
String sessionId = NewRelic.currentSessionId();
```

        </TabsPageItem>
        <TabsPageItem id="ios">

## Syntax

```
crashNow;
crashNow:(NSString* __nullable)message;
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier. For example, you might want to use the same identifier for marketing analytics or user analytics. This session ID will change every time the app comes to the foreground. 

For context on how to use this API, see the documentation about sending custom attributes and events:

* [Objective-C](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/work-ios-sdk-api#objc-custom-att-events)
* [Swift](/docs/mobile-monitoring/new-relic-mobile-ios/install-configure/work-ios-sdk-api#swift-custom-att-events)

## Requirements

Compatible with all agent versions.

## Return values

Returns ID string for the current session.

## Examples

### Objective-C [#obj-c]

Method:

```objectivec
+ (NSString*) currentSessionId;
```

Example:

```objectivec
NSString* sessionId = [NewRelic currentSessionId()];
```

### Swift

Method:

```swift
NewRelic.currentSessionId() -> String
```

Example:

```swift
let sessionId = NewRelic.currentSessionId()
```

        </TabsPageItem>
        <TabsPageItem id="capacitor">

## Syntax

```typescript
currentSessionId(options?: {} | undefined) => Promise<{ sessionId: string; }>
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier.

## Requirements

Capacitor version _ or higher

## Return values

`Promise<{ sessionId: string; }>`

## Example

```typescript
        let { sessionId } = await NewRelicCapacitorPlugin.currentSessionId();
```

        </TabsPageItem>
        <TabsPageItem id="cordova">

## Syntax

```typescript
currentSessionId(): Promise<sessionId>;
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier.

## Requirements

Cordova version _ or higher 

## Example

  ```js
    let sessionId = await NewRelic.currentSessionId();
  ```

        </TabsPageItem>
 
  <TabsPageItem id="flutter">

## Syntax

```dart
currentSessionId(): Promise;
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier.

## Requirements

Flutter version _ or higher 

## Example

  ```dart
    var sessionId = await NewrelicMobile.instance.currentSessionId();
  ```

    </TabsPageItem>

        <TabsPageItem id="react">

## Syntax

```js
currentSessionId(): Promise;
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier.

## Requirements

React Native _ or higher

## Example

 ```js
    let sessionId = await NewRelic.currentSessionId();
  ```

        </TabsPageItem>
        <TabsPageItem id="xamarin">

## Syntax

```csharp
CurrentSessionId() : string;
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier.

## Requirements

Xamarin agent version _ or igher

## Example

``` csharp
string sessionId = CrossNewRelic.Current.CurrentSessionId();
```

        </TabsPageItem>
        <TabsPageItem id="maui">

## Syntax

```csharp
    CurrentSessionId() : string;
```

## Description

Returns the current session ID. This method is useful for consolidating monitoring of app data (not just New Relic data) based on a single session definition and identifier.

## Requirements

.NET MAUI agent version ? or higher

## Example

``` C#
string sessionId = CrossNewRelic.Current.CurrentSessionId();
```
        </TabsPageItem>
    </TabsPages>
</Tabs>
