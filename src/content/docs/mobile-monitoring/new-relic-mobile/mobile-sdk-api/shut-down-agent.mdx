---
title: Shut down the agent
type: apiDoc
shortDescription: 'Shut down the agent within the current application lifecycle during runtime.'
tags:
  - Mobile monitoring
  - Mobile SDK API
  - Custom instrumentation
metaDescription: Shut down the agent within the current application lifecycle during runtime.
redirects:
- /docs/mobile-monitoring/new-relic-mobile-android/android-sdk-api/shut-down
- /docs/mobile-monitoring/new-relic-mobile-ios/ios-sdk-api/shut-down
---

<Tabs>
	<TabsBar>
        <TabsBarItem id="android">
            Android
        </TabsBarItem>
        <TabsBarItem id="ios">
            iOS
        </TabsBarItem>
        <TabsBarItem id="capacitor">
            Capacitor
        </TabsBarItem>
        <TabsBarItem id="cordova">
            Cordova
        </TabsBarItem>
        <TabsBarItem id="flutter">
            Flutter
        </TabsBarItem>
        <TabsBarItem id="react">
            React Native
        </TabsBarItem>
        <TabsBarItem id="xamarin">
            Xamarin
        </TabsBarItem>
        <TabsBarItem id="maui">
            .NET MAUI
        </TabsBarItem>
    </TabsBar>

    <TabsPages>
        <TabsPageItem id="android">
## Syntax

```java
NewRelic.shutdown()
```

## Description

This API provides the ability to shut down the agent within the current application lifecycle during runtime. Once the agent has shut down, it cannot be restarted within the current application lifecycle.

## Example


```java
NewRelic.shutdown()
```

        </TabsPageItem>
        <TabsPageItem id="ios">

## Syntax

```objectivec
+ (void)shutdown;
```

## Description

This API provides the ability to shut down the agent within the current application lifecycle during runtime. Once the agent has shut down, it cannot be restarted within the current application lifecycle.

## Examples

### Objective-C [#obj-c]

```objectivec
[NewRelic shutdown];
```

### Swift

```swift
NewRelic.shutdown()
```

        </TabsPageItem>
        <TabsPageItem id="capacitor">

## Syntax

```typescript
shutdown(options?: {} | undefined) => void
```

## Description

Shut down the agent within the current application lifecycle during runtime.

## Example

```typescript
    NewRelicCapacitorPlugin.shutdown();
```

        </TabsPageItem>
        <TabsPageItem id="cordova">

## Syntax

```typescript
shutdown() : void;
```

## Description

Shut down the agent within the current application lifecycle during runtime.

## Example

  ```js
    NewRelic.shutdown();
  ```

        </TabsPageItem>

        <TabsPageItem id="react">

## Syntax

```js
shutdown() : void;
```

## Description

Shut down the agent within the current application lifecycle during runtime.

## Example

 ```js
    NewRelic.shutdown();
  ```

        </TabsPageItem>
        <TabsPageItem id="xamarin">

## Syntax

```csharp
Shutdown(): void;
```

## Description

Shut down the agent within the current application lifecycle during runtime.

## Example

``` csharp
    CrossNewRelicClient.Current.Shutdown();
```

        </TabsPageItem>
        <TabsPageItem id="maui">

## Syntax

```csharp
Shutdown() : void
```

## Description

Shut down the agent within the current application lifecycle during runtime.

## Example

``` C#
    CrossNewRelic.Current.Shutdown());
```
        </TabsPageItem>
    </TabsPages>
</Tabs>
