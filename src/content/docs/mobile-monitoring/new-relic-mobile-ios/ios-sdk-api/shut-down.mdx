---
title: "shutdown (iOS SDK API)"
type: apiDoc
shortDescription: Shut down the agent during runtime
tags:
  - Mobile monitoring
  - New Relic Mobile iOS
  - iOS SDK API
metaDescription: 'New Relic API for iOS mobile app monitoring: shut down agent.'
---

## Syntax

```
+ (void)shutdown;
```

Shut down the agent within the current application lifecycle during runtime.

## Requirements

Agent version 7.5.0 or higher.

## Description

This API provides the ability to shut down the agent within the current application lifecycle during runtime. Once the agent has shut down, it cannot be restarted within the current application lifecycle.

## Examples

### Objective-C [#obj-c]

**Method:**

```
+ (void) shutdown();
```

**Example:**

```
[NewRelic shutdown];
```

### Swift [#swift]

**Method:**

```
NewRelic.shutdown() -> Void
```

**Example:**

```
NewRelic.shutdown()
```
