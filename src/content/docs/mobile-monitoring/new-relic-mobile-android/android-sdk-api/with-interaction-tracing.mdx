---
title: withInteractionTracing (Android SDK API)
contentType: apiDoc
template: basicDoc
shortDescription: Globally disable/enable interaction tracing.
topics:
  - Mobile monitoring
  - New Relic Mobile Android
  - Android SDK API
---

## Syntax

```
NewRelic.withInteractionTracing(boolean $tracingFlag)
```

Globally disable/enable interaction tracing.

## Requirements

Agent version 5.4.0 or higher.

## Description

Use `withInteractionTracing()` to globally enable or disable interaction traces, which appear on the [Mobile **Interactions** page](/docs/mobile-monitoring/mobile-monitoring-ui/mobile-app-pages/interactions-page). Ensure you call this before the New Relic agent start call.

<Callout variant="important">
  Interaction tracing will be disabled after the agent has started. Interactions for any activity or methods executed prior to starting the agent may still appear in the [Interactions page](/docs/mobile-monitoring-ui/interactions-dashboard).
</Callout>

For general info on using this API, see the [Android SDK API usage guide](/docs/mobile-monitoring/new-relic-mobile-android/install-configure/work-android-sdk-api).

## Parameters

<Table>
  <thead>
    <tr>
      <th width="25%">
        Parameter
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `tracingFlag`

        _boolean_
      </td>

      <td>
        Required. Use `false` to disable interaction tracing. Use `true` to enable tracing. Tracing is on by default.
      </td>
    </tr>
  </tbody>
</Table>

## Examples

### Disable interactions

To turn off all interaction traces collected by the Android agent:

```
NewRelic.withInteractionTracing(false)
```
