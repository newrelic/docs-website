---
title: Obfuscate browser data
tags:
  - Browser monitoring
  - Obfuscation
metaDescription: "You can obfuscate sensitive browser data either through the browser agent configuration or through ingest obfuscation rules in application settings."
freshnessValidatedDate: 2023-10-24
---

While New Relic recommends avoiding sensitive information in the public structure of your application, we understand that's not always possible. You have two ways to obfuscate sensitive data captured by browser monitoring:

* **[Obfuscate data in the browser agent](#agent-obfuscation)**: Configure obfuscation rules directly in the agent using copy/paste or NPM installation methods.
* **[Obfuscate data in application settings](#ingest-obfuscation)**: Create ingest obfuscation rules in the UI to mask sensitive data across any browser event before it's harvested.

## Obfuscate data in the browser agent [#agent-obfuscation]

<Callout variant="important">
  Browser agent obfuscation is only available for copy/paste or NPM browser installation methods. There are no UI or NerdGraph configuration options for this method.
</Callout>

You can configure the browser agent to selectively obfuscate data in every payload it sends. This is useful if your application uses sensitive data in places the agent captures, such as navigation paths, error messages, and more.

### How it works [#how-it-works]

As of browser agent [version 1216](/docs/release-notes/new-relic-browser-release-notes/browser-agent-release-notes) and higher, obfuscation rules can be applied to outgoing harvest payloads.

To set up these rules, configure the following browser agent property:

* The property `init.obfuscate` contains an array of selectors and replacements which will be used to modify each harvest before sending.
  * Since obfuscation currently requires you to use the copy/paste or NPM installation methods, you'll need to manually edit your JavaScript configuration section and set the `obfuscate` array to contain your obfuscation conditions.

### Recommendations [#recommendations]

When configuring this property, we recommend the following:

* Use intentional regex patterns to obfuscate only what needs obfuscation.
  * Needlessly obfuscating can have side-effects such as less granularity when grouping data and less ability to digest what the agent captured.
* Replace your sensitive data with neutral and generic terms that also indicate what data has been redacted.
  * Example: `/account-id/g` --> `ACCOUNT_ID`

### Copy/paste installation [#copy-paste]

If you're using the copy/paste installation method, add the following configuration to your browser JavaScript configurations before the agent loader:

```js
window.NREUM.init = {
    ...<other init properties>...,
    obfuscate: [
      {
        regex: <RegExp | string>
        replacement: <string>
      },
      ...<other obfuscation rules>...
    ]
}
```

### NPM installation [#npm]

If you're using the NPM browser installation method, add the following configuration when initializing the browser agent:

```js
new BrowserAgent({ 
  init: {
    ...<other init properties>...,
    obfuscate: [
      {
        regex: <RegExp | string>
        replacement: <string>
      },
      ...<other obfuscation rules>...
    ]
  }
})
```

### Examples [#examples]

```js
window.NREUM.init = {
    ...<other init properties>...,
    obfuscate: [
      {
        regex: /user-id/g,
        replacement: 'USER_ID'
      },
    ]
}
```

```js
new BrowserAgent({ 
  init: {
    ...<other init properties>...,
    obfuscate: [
      {
        regex: /account-id/g,
        replacement: 'ACCOUNT_ID'
      },
    ]
  }
})
```

## Obfuscate data in application settings [#ingest-obfuscation]

You can create ingest obfuscation rules directly from the UI to mask sensitive data before it's harvested. These rules apply to any browser event, including `AjaxRequest`, `PageView`, `PageAction`, `JavaScriptError`, `PageViewTiming`, `UserAction`, and others.

To add an obfuscation rule:

1. Go to <DNT>**[one.newrelic.com > All capabilities](https://one.newrelic.com/all-capabilities) > Browser > (select an app) > Settings > Application settings**</DNT>.
2. Under <DNT>**Ingest obfuscation**</DNT>, click <DNT>**Add a rule**</DNT>.
3. In the <DNT>**Original value**</DNT> field, enter a regex pattern to match the sensitive data you want to mask.
4. In the <DNT>**Replace with**</DNT> field, enter a neutral or replacement term.
5. Click <DNT>**Add rule**</DNT> to save.

The obfuscation rule is applied to matching data across all browser events before ingestion.
