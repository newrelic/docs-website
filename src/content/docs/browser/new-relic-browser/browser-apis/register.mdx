---
title: register
type: apiDoc
shortDescription: Registers a micro frontend or component with the browser agent and returns a scoped API interface.
tags:
  - Browser
  - Browser monitoring
  - Browser agent and SPA API
  - Micro frontends
metaDescription: Browser API call to register a micro frontend or component for independent monitoring within a container application.
freshnessValidatedDate: never
---

## Syntax [#syntax]

```js
newrelic.register(object $options)
```

Registers a micro frontend or component with the browser agent and returns a scoped API interface for independent monitoring.

## Requirements [#requirements]

* Browser Pro or Pro+SPA agent
* The main browser agent must be running on the container page

## Description [#description]

This API call registers a micro frontend or component with the container browser agent, enabling independent monitoring and data collection. The registration process:

1. **Connects the micro frontend** to the New Relic platform for entity synthesis and sampling
2. **Creates relationships** between the container application and the micro frontend entity
3. **Returns a scoped API interface** with methods pre-configured for the registered micro frontend
4. **Enables data isolation** so each micro frontend's telemetry is tracked separately
5. **Tracks basic lifecycle timings** so that usage from import to deregistration can be quantified

After registration, all data captured through the returned API interface will be attributed to the micro frontend entity rather than the container application, unless explicitly configured to duplicate across applications.

<Callout variant="important">
The micro frontend browser entity must be created before calling `register`. Entities can be created through the New Relic UI or programmatically via NerdGraph.
</Callout>

## Parameters [#parameters]

<table>
  <thead>
    <tr>
      <th width="25%">
        Parameter
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `$config`

        _object_
      </td>

      <td>
        Required. Configuration object for the micro frontend registration. Contains the following properties:

        * `id` _(string or number)_: Unique identifier for the micro frontend. Must match the `applicationId` of the browser entity.
        * `name` _(string)_: Display name for the micro frontend. Should match the application name configured for the browser entity.
        * `tags` _(string[])_: A list of tags to assign to the micro frontend entity.
      </td>
    </tr>
  </tbody>
</table>

## Return value [#return-value]

The registration returns an object with the following scoped methods:

- [`addPageAction()`](/docs/browser/new-relic-browser/browser-apis/addpageaction) - Record custom page actions specific to your micro frontend
- [`addRelease()`](/docs/browser/new-relic-browser/browser-apis/addrelease) - Track deployments and releases for your micro frontend
- [`deregister()`](/docs/browser/new-relic-browser/browser-apis/deregister) - Unregister the micro frontend when it unmounts or is no longer needed. Timings describing the lifecycle of your micro frontend will be automatically tabulated based on the usage of this API.
- [`interaction()`](/docs/browser/new-relic-browser/browser-apis/interaction) - Access the SPA interaction API for custom timing
- [`log()`](/docs/browser/new-relic-browser/browser-apis/log) - Capture log messages scoped to your micro frontend
- [`measure()`](/docs/browser/new-relic-browser/browser-apis/measure) - Create custom performance measurements
- [`noticeError()`](/docs/browser/new-relic-browser/browser-apis/noticeerror) - Manually capture errors with micro frontend attribution
- [`recordCustomEvent()`](/docs/browser/new-relic-browser/browser-apis/recordcustomevent) - Record custom events for analytics
- [`register()`](/docs/browser/new-relic-browser/browser-apis/register) - Register an entity for scoped data observability
- [`setApplicationVersion()`](/docs/browser/new-relic-browser/browser-apis/setapplicationversion) - Track the specific version of your micro frontend
- [`setCustomAttribute()`](/docs/browser/new-relic-browser/browser-apis/setcustomattribute) - Add custom attributes that apply to all data from this micro frontend
- [`setUserId()`](/docs/browser/new-relic-browser/browser-apis/setuserid) - Associate a user ID with this micro frontend's data

Each of these methods works identically to the standard browser agent APIs, but automatically scopes all collected data to your micro frontend entity.

## Examples [#examples]
### Basic registration [#basic-example]

See our [micro frontends monitoring guide](/docs/browser/new-relic-browser/browser-apis/micro-frontends-monitoring) for complete setup instructions and examples.

### Performance timing [#performance-timing]

For examples of using the returned API to track micro frontend performance, see [micro frontend performance timing](/docs/browser/new-relic-browser/browser-apis/micro-frontend-performance-timing).

### NPM package usage [#npm-usage]

If you're using the browser agent via NPM, see [micro frontend NPM usage](/docs/browser/new-relic-browser/browser-apis/micro-frontend-npm-usage) for integration examples.

## Troubleshooting [#troubleshooting]

For troubleshooting help, see [Micro frontend monitoring limitations](/docs/browser/new-relic-browser/troubleshooting/micro-frontend-monitoring-limitations).