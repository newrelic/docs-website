---
# ─── Existing NR frontmatter — PRESERVE these on every page ───
title: Introduction to <Integration Name>
metaDescription: <One-sentence page description for SEO, ~140 chars>
tags:
  - <topic tag>
  - <product tag>
freshnessValidatedDate: YYYY-MM-DD    # Set to YYYY-MM-DD when you last update the content
redirects:                        # Include only when migrating from an existing URL
  - /old/path/if/any

# ─── New IA fields — required ───
contentType: onboarding-intro
audience: [human, ai]
surface: [concept]
integration: <integration-slug>          # e.g. kubernetes, apm-nodejs, infrastructure-agent
flow:
  # `prev` is null on the Intro page (this is the entry point).
  prev: null
  # `next` — most common form: single string pointing to the next page in the linear flow.
  next: /install/<integration-slug>/<combo-slug>/install-configure
  #
  # Multi-value form (uncomment if this page is reached from / leads to multiple pages):
  #
  # next:
  #   - /install/<integration-slug>/<combo-a-slug>/install-configure
  #   - /install/<integration-slug>/<combo-b-slug>/install-configure
  #
  # Labeled form (recommended when multi-value — future auto-rendered nav will need the labels):
  #
  # next:
  #   - path: /install/kubernetes/linux-helm/install-configure
  #     label: "Helm install"
  #   - path: /install/kubernetes/linux-manifest/install-configure
  #     label: "Manifest install"

# ─── New IA fields — optional in V1 (uncomment when applicable) ───
# roundtripTTFV: 15m                     # Total onboarding time target (Intro → Install-with-verify)
---

{/*
  WRITER: This is the Intro page. User intent: "Is this for me?"
  Orientation only — NOT enablement.
  If you're writing install steps or a compatibility table here, STOP — those live on the Install page.
  See IA-authoring-guidelines-v1.md Section 3 for the full page-structure spec.
*/}

{/* WRITER: The Intro page opens with a lede paragraph directly after the title — no H2 heading.
    One paragraph, 3–5 sentences. Plain language. What does this integration DO for the user?
    What will they be able to see or answer that they can't today?
    Focus on outcomes, not features. No marketing copy. */}

<Describe what the integration does for the user, in one paragraph. Concrete outcomes over feature lists.>

## Capabilities [#capabilities]

{/* WRITER: Bullet list of what this integration or feature provides.

    For telemetry-producing integrations (APM, infra, DBM, browser, mobile, logs, OpenTelemetry):
    list the telemetry types — Metrics, Events, Logs, Traces, Entities — with a short qualifier per bullet.

    For non-telemetry capabilities (Change Tracking, Alerts, SRE Agent, NR Control, Lens, etc.):
    list what the feature does — what you can track, define, query, govern, or automate.

    Include only the capabilities that actually apply. Don't invent generic entries.
    If your integration doesn't fit either shape, flag it in review — we'll refine this section
    together for that specific capability. */}

- **<Capability 1>** — <one-line description>
- **<Capability 2>** — <one-line description>
- **<Capability 3>** — <one-line description>

## Choose your install path [#choose]

{/* OPTIONAL — include only if the integration has multiple install paths (Helm vs. Manifest, etc.).
    Delete this whole section if there's only one supported path. */}

{/* WRITER: List every supported install path for this integration. Mark the recommended one
    explicitly with "(recommended)". For each path, one line on when it's the right pick
    — environment fit, upgrade model, tooling assumptions.
    If this Intro is for a specific combo, that combo should still appear in the list; the
    reader can then confirm they're on the right page or click over to a better-fit alternative. */}

- **<Path 1>** — <one line on when this path is the right pick>
- **<Path 2>** — <one line on when this path is the right pick>
- **<Path 3>** — <one line on when this path is the right pick>

## Guided installation [#ui-install]

{/* OPTIONAL — include only if a UI install path exists.
    Delete this section if there's no UI alternative. */}

Guided installation is available at <DNT>**one.newrelic.com → Integrations & Agents → <Search for Integration Name>**</DNT>. The UI provides a guided end-to-end workflow.

{/* AVOID: "Click the gear icon, then select..." — ANY step-by-step walkthrough.
   The UI documents itself; docs don't duplicate. If in-app assistance is weak, that's a
   product problem — flag it, don't bandaid in docs. */}


## Installation at a glance [#at-a-glance]

{/* WRITER: Preview of the high-level tasks the reader will do on the Install page.
    Typically 4–6 items. Keep each task to a short phrase — this is a glimpse, not the
    actual instructions. The sequence below is a common shape; adapt to what applies
    for this integration. Drop items that don't apply; add integration-specific tasks
    if needed (e.g. "Set up an ingest endpoint" or "Create a service account"). */}

<Steps>

<Step>

1. Check compatibility and gather prerequisites
</Step>

<Step>
2. Set up user permissions and account access
</Step>

<Step>
3. Run the installation
</Step>

<Step>
4. Apply the minimum configuration
</Step>

<Step>
5. Verify data is flowing to New Relic
</Step>

</Steps>

## Next: install and configure [#next]

[**Install and configure →**](/install/<integration-slug>/<combo-slug>/install-configure)