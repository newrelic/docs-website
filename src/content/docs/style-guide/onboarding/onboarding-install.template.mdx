---
# ─── Existing NR frontmatter — PRESERVE these on every page ───
title: Install and configure <Integration Name> — <combo description, e.g. Linux + Helm>
metaDescription: <One-sentence page description for SEO, ~140 chars>
tags:
  - <topic tag>
  - <product tag>
freshnessValidatedDate: never    # Set to YYYY-MM-DD when you verify against the current product
redirects:                        # Include only when migrating from an existing URL
  - /old/path/if/any

# ─── New IA fields — required ───
contentType: onboarding-install
audience: [human, ai]
surface: [cli]
integration: <integration-slug>
flow:
  # Common single-value case:
  prev: /install/<integration-slug>/<combo-slug>/intro
  # `next` is null — Install is the last onboarding page. Verification lives on this page (see Verify section).
  # Deeper usage/reference content lives outside onboarding and is linked from Next steps.
  next: null
  #
  # Multi-value form for `prev` — uncomment when this Install page is reached from multiple entry points:
  #
  # prev:
  #   - /install/<integration-slug>/<combo-slug>/intro
  #   - /install/<integration-slug>/                       # reached from the integration-root selector
  #
  # Labeled form (recommended when multi-value):
  #
  # prev:
  #   - path: /install/<integration-slug>/<combo-slug>/intro
  #     label: "From the introduction"
  #   - path: /install/<integration-slug>/
  #     label: "From the integration selector"

# ─── New IA fields — optional in V1 ───
# stepTTFV: 5m                           # Time budget for THIS page's steps (install + verify)
# verifiedAgainst: <product@version>
# owners: [<team-slug>]
---

{/*
  WRITER: This is the Install & Configure page. User intent: "Do it — and confirm it worked."
  DO NOT re-explain what the integration does (that's the Intro page).
  Verification of successful install lives HERE, in the Verify section — there is no separate View Data page.
  One opinionated install path. Failure-mode notes go inline at the step they apply to.
*/}

{/* WRITER: A short lede — one to two sentences — between the page title and the first section.
    Confirm the page's scope and the outcome the reader will reach by the end (install + minimum
    configuration + verified data flow). Include the TTFV if it applies.
    DO NOT restate what the integration is or does — that belongs on the Intro page.
    DO NOT begin the install steps here. */}

<One or two sentences. Confirm the page's purpose and the outcome. Example: "This page installs the New Relic Kubernetes integration on Linux via Helm, applies the minimum configuration, and verifies data is flowing to New Relic. Expect ~15 minutes.">

{/* OPTIONAL — include only if a UI install path exists. Delete this callout if there's no UI alternative.

    Reader context: this page is for readers doing the CLI install. The callout serves the
    search-first reader who landed here without seeing the Intro's Guided-installation section —
    give them the redirect before they dig into CLI prerequisites and steps.

    Callout vs. section: this is intentionally a callout (aside), not an H2 section (topic),
    since the content is a pointer, not a page-level subject. The Intro uses a section for the
    same content because it's orientation material there; here it's an alternative-path aside. */}

<Callout variant="tip" title="Guided installation">
  To install through the New Relic UI instead, go to <DNT>**[one.newrelic.com](https://one.newrelic.com) → Integrations & Agents → <Search for Integration Name>**</DNT>. This page covers the CLI install path only.
</Callout>

{/* AVOID: "Click the gear icon, then select..." — ANY step-by-step walkthrough inside the callout.
   The UI documents itself; docs don't duplicate. */}

## Compatibility and requirements [#requirements]

{/* WRITER: Hand-author the block below in this exact three-heading structure — Supported,
    Not supported, You need. Consistency comes from every writer using the same shape.
    See guide Section 8 for rules; if this block exceeds ~25–30 lines, fork to a standalone
    Compatibility topic and keep an "at a glance" summary here with all deal-breaker constraints.
    See guide Section 9 for when to include "Not supported" entries vs. when they're redundant. */}

### Supported
- <Platform / version range, e.g. Kubernetes ≥ 1.24>
- <OS variants, e.g. Linux nodes (x86_64, arm64)>
- <Cloud variants if applicable>

### Not supported
- <Explicit item, with alternative if one exists, e.g. "Windows nodes — use the [Windows manifest install](...) instead">
- <Item — include only when omission would create a foreseeable false assumption; see guide Section 9>

### You need
- A New Relic <InlinePopover type="licenseKey"/>
- <Stack version requirement, e.g. Helm 3.x>
- <Hard gates: RBAC, namespace, security context, network policy>

{/* OPTIONAL ADDITIONAL H2 — insert here if the integration has substantive prerequisites
    (multi-step permissions setup, service account creation, namespace provisioning, ingest
    endpoint configuration, etc.) that don't fit inside the "You need" bullets above.

    Use a verb-first heading:
      ## Set up permissions [#permissions]
      ## Create a service account [#service-account]
      ## Provision a namespace [#namespace]
      ## Set up an ingest endpoint [#ingest]

    Trigger: the reader has multi-step work to DO, not just state to HAVE. "Have kubectl
    installed" stays in `You need`; "run these five commands to create and bind the service
    account" gets its own H2.

    IMPORTANT: When you add an H2 here, add a matching item to the Intro's `Installation at
    a glance` — the correspondence rule requires 1:1 alignment between actionable Install H2s
    and preview items. See guide Section 3 → "Substantive prerequisites get their own H2". */}

## Install [#install]

{/* WRITER: One opinionated path. Numbered steps. Each step gets a stable anchor [#install-step-N]
    so AI/RAG consumers can cite a specific step.
    Inline failure-mode notes appear in italics IMMEDIATELY after the step they apply to.
    DO NOT collect failure modes at the bottom of the page. */}

1. <Step description> [#install-step-1]

   ```shell
   <command>
   ```

   *<If you see [common error], [one-line fix or cross-reference].>*

2. <Step description> [#install-step-2]

   ```yaml
   <config or command>
   ```

3. <Step description> [#install-step-3]

{/* AVOID: multiple parallel install paths in this section. Combinations that need
    different steps get forked into separate flows — see guide Section 4. */}

## Configure [#configure]

{/* OPTIONAL SECTION — include only if the integration needs explicit configuration beyond
    what the install steps do. Delete the heading if not needed.
    Minimum config only. Advanced configuration lives on a separate reference page. */}

<Minimum configuration needed for first telemetry. Anything advanced links out.>

## Verify your install [#verify]

{/* WRITER: This section marks the closure of onboarding. Two purposes:
    1) Local check — the install command didn't error (process running, pods scheduled, config valid).
    2) Data-flowing check — telemetry is actually reaching New Relic within the expected time window.
    Give the reader CONCRETE signals from whichever surfaces apply — CLI output, NR1 entities, NRQL.
    "Data will appear shortly" is NOT acceptable. Match the roundtripTTFV declared on the Intro page. */}

Within ~<N> minutes of completing the install, you should see:

- <Concrete signal 1 — e.g., specific pod running: `newrelic-nrk8s-ksm`>
- <Concrete signal 2 — e.g., cluster entity appearing at **one.newrelic.com → Infrastructure → <Integration Name>**>
- <Concrete signal 3 — e.g., `FROM K8sClusterSample SELECT count(*) WHERE clusterName = 'YOUR_CLUSTER'` returns ≥ 1 row>

If you don't see data within ~<N> minutes, see [<Integration Name> troubleshooting](/docs/<integration-slug>/troubleshooting/not-seeing-data).

## Related articles [#related-articles]

{/* WRITER: This is the transition point from sequential onboarding to a-temporal content.
    Everything before this section was ordered (Compatibility → Install → Verify); everything
    linked here is affinity, not sequence — deeper topics, advanced config, related integrations,
    usage/analysis references, dashboards, alerts. The reader picks what they need, in any order.
    
    Do NOT frame these as "next steps" — they're not sequential steps in an onboarding flow.
    The Install page IS the end of the sequential flow.
    
    Use <DocTiles> for the tile-card layout; a plain bullet list is also acceptable. */}

<DocTiles>
  <DocTile title="<Deeper topic 1>" path="/docs/<integration-slug>/...">
    <Short description of what the topic covers>
  </DocTile>
  <DocTile title="<Deeper topic 2>" path="/docs/<integration-slug>/...">
    <Short description>
  </DocTile>
  <DocTile title="<Deeper topic 3>" path="/docs/<integration-slug>/...">
    <Short description>
  </DocTile>
</DocTiles>