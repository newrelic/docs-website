---
title: Install VegaTracker for Shaka Player
metaDescription: 'Install and configure the New Relic VegaTracker for Shaka Player in your Amazon Fire TV (Vega OS) application.'
redirects:
freshnessValidatedDate: never
---

Install the New Relic `VegaTracker` to monitor Shaka Player video playback in your Amazon Fire TV application.

## Before you begin [#before-begin]

Complete the [common installation steps](/docs/streaming-video-&-ads/installation/amazon-fire-tv/get-started-with-amazon-fire-tv#install) first to create your application and retrieve your mobile application token.

## Install the tracker [#install]

<Steps>

<Step>
### Install the Shaka package

```shell
npm install @newrelic/video-shaka --save
```
</Step>

<Step>
### Install device info (optional but recommended)

The device info library provides real device attributes — model, OS version, and UUID — in your video events. Without it, events use placeholder values for `deviceModel` and `deviceUuid`.

```shell
npm install @amazon-devices/react-native-device-info --save
```
</Step>

<Step>
### Initialize VegaTracker

Initialize `VegaTracker` inside `onSurfaceViewCreated`. By the time this callback fires, the player's async attach has completed.

```javascript
import { VegaTracker } from '@newrelic/video-shaka/vega';
import {
  getDeviceId,
  getSystemVersion,
  getModel,
  getBrand,
  getBuildIdSync,
  getBuildNumber,
} from '@amazon-devices/react-native-device-info';

const tracker = useRef(null);

const onSurfaceViewCreated = (surfaceHandle) => {
  shakaPlayer.setSurfaceHandle(surfaceHandle);
  shakaPlayer.play();

  const deviceInfo = {
    uuid:               getDeviceId(),
    osVersion:          getSystemVersion(),
    deviceModel:        getModel(),
    deviceManufacturer: getBrand(),
    osBuild:            getBuildIdSync(),
    appBuild:           getBuildNumber(),
    architecture:       'aarch64',
  };

  tracker.current = new VegaTracker(shakaPlayer, {
    info: {
      accountId:        'YOUR_ACCOUNT_ID',
      applicationToken: 'YOUR_APPLICATION_TOKEN',
      endpoint:         'US',
      deviceInfo,
    },
  });
};

const onEnded = () => {
  tracker.current?.dispose();
  tracker.current = null;
};
```
</Step>

</Steps>

## Configuration reference [#configuration]

### `info` options [#info-options]

<table>
  <thead>
    <tr>
      <th style={{width: "220px"}}>
        Option
      </th>
      <th style={{width: "100px"}}>
        Required
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        `info.accountId`
      </td>
      <td>
        Yes
      </td>
      <td>
        Your New Relic [account ID](/docs/accounts/accounts-billing/account-setup/account-id).
      </td>
    </tr>
    <tr>
      <td>
        `info.applicationToken`
      </td>
      <td>
        Yes
      </td>
      <td>
        The mobile application token generated when you created your application in New Relic.
      </td>
    </tr>
    <tr>
      <td>
        `info.endpoint`
      </td>
      <td>
        Yes
      </td>
      <td>
        The New Relic data center region: `"US"`, `"EU"`, `"JP"`, or `"GOV"`. Defaults to `"US"`.
      </td>
    </tr>
    <tr>
      <td>
        `info.deviceInfo`
      </td>
      <td>
        No
      </td>
      <td>
        Object containing device attributes. See [deviceInfo options](#deviceinfo-options).
      </td>
    </tr>
  </tbody>
</table>

### `info.deviceInfo` options [#deviceinfo-options]

<table>
  <thead>
    <tr>
      <th style={{width: "220px"}}>
        Option
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        `uuid`
      </td>
      <td>
        Unique device identifier.
      </td>
    </tr>
    <tr>
      <td>
        `osVersion`
      </td>
      <td>
        Operating system version string.
      </td>
    </tr>
    <tr>
      <td>
        `deviceModel`
      </td>
      <td>
        Device model name.
      </td>
    </tr>
    <tr>
      <td>
        `deviceManufacturer`
      </td>
      <td>
        Device manufacturer name.
      </td>
    </tr>
    <tr>
      <td>
        `osBuild`
      </td>
      <td>
        OS build identifier.
      </td>
    </tr>
    <tr>
      <td>
        `appBuild`
      </td>
      <td>
        Application build number.
      </td>
    </tr>
    <tr>
      <td>
        `architecture`
      </td>
      <td>
        Device CPU architecture (for example, `"aarch64"`).
      </td>
    </tr>
  </tbody>
</table>

### `config` options [#config-options]

<table>
  <thead>
    <tr>
      <th style={{width: "220px"}}>
        Option
      </th>
      <th style={{width: "100px"}}>
        Required
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        `config.qoeIntervalFactor`
      </td>
      <td>
        No
      </td>
      <td>
        Controls how frequently Quality of Experience metrics are sampled during playback.
      </td>
    </tr>
    <tr>
      <td>
        `config.obfuscate`
      </td>
      <td>
        No
      </td>
      <td>
        When enabled, obfuscates sensitive values in video events before they are sent to New Relic.
      </td>
    </tr>
  </tbody>
</table>

## What's next? [#whats-next]

- [View your data](/docs/streaming-video-&-ads/view-data-in-newrelic/streaming-video-&-ads-features): Explore monitoring features and dashboards.
- [Compatibility requirements](/docs/streaming-video-&-ads/installation/compatibility-requirements): Check supported platforms and versions.
- [Update the agent](/docs/streaming-video-&-ads/update-the-media-agent): Keep your agent up to date.
