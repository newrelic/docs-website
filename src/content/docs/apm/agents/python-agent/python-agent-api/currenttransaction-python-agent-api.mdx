---
title: current_transaction (Python agent API)
type: apiDoc
shortDescription: Returns an object corresponding to the current transaction.
tags:
  - Agents
  - Python agent
  - Python agent API
metaDescription: 'New Relic Python API: Retrieves an object corresponding to the current transaction, which is used by some other calls.'
redirects:
  - /docs/agents/python-agent/python-agent-api/currenttransaction-python-agent-api
  - /docs/agents/python-agent/python-agent-api/current_transaction
---

## Syntax

```py
newrelic.agent.current_transaction(active_only=True)
```

Returns an object corresponding to the current transaction.

## Description

Use `current_transaction` to retrieve the object representing the current [transaction](/docs/accounts-partnerships/education/getting-started-new-relic/glossary#transaction). It will return `None` if there is no active transaction.

## Parameters

<table>
  <thead>
    <tr>
      <th width="25%">
        Parameter
      </th>

      <th>
        Description
      </th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `active_only`

        _boolean_
      </td>

      <td>
        Optional. Default is `True`, meaning it will only return active transactions. If set to `False`, the call is capable of returning a transaction that has ended or stopped (for example, if `end_of_transaction` was used).
      </td>
    </tr>
  </tbody>
</table>

## Return values

Returns a transaction object corresponding to current transaction. Returns `None` if there is no active transaction.

## Examples

### Get the current transaction [#function-trace-example]

```py
import newrelic.agent

@newrelic.agent.background_task()
def main():
    transaction = newrelic.agent.current_transaction()
```
