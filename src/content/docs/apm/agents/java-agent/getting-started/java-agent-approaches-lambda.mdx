---
title: Java serverless AWS Lambda performance monitoring
tags:
  - Agents
  - Java agent
  - Getting started
metaDescription: 'This guide helps users decide which approach to take for Java serverless AWS Lambda performance monitoring.'
freshnessValidatedDate: never
---

Unified visibility into the most detailed behaviors of your Java Lambda functions is essential so you can understand what's happening in your serverless applications.

You can use the slim layer Java agent variation to gain visibility into your Java Lambda functions. This layer enables lite mode and the AWS Lambda core instrumentation.

## New Relic Java agent [#java-agent]

The New Relic Java agent supports instrumenting AWS Lambda functions through a dedicated **serverless mode**. When running inside a Lambda function, the agent automatically detects the environment and switches into this mode, which is optimized for the short-lived, event-driven nature of Lambda invocations.

<Callout variant="important">
  Java agent serverless mode requires **Java 17 or later**. Java 11 and older runtimes are not supported due to excessive cold start overhead.
</Callout>

### How it works

In serverless mode, the agent replaces its standard periodic harvest and HTTP transport with a per-invocation model:

1. When a Lambda function is invoked, the agent's bytecode weaver intercepts the handler before customer code executes.
2. A transaction is created automatically for the invocation, capturing context like the Lambda ARN, request ID, and whether this is a cold start.
3. If the event payload is recognized (for example, an API Gateway or SQS event), the agent extracts event-source metadata, classifies the transaction as a web or non-web transaction, and adds relevant attributes.
4. When the transaction finishes, the agent synchronously harvests all buffered telemetry and writes it as a compressed, Base64-encoded payload to `/tmp/newrelic-telemetry` and as plain JSON to stdout.
5. The [New Relic Lambda Extension](https://github.com/newrelic/newrelic-lambda-extension/tree/main) (bundled in the layer) reads the telemetry file and forwards the data to New Relic after each invocation.

### Lambda layers [#layers]

Two Lambda layer variants are published for the Java agent:

* **Full agent layer**: all instrumentation modules are enabled. Provides maximum out-of-the-box visibility, but incurs higher cold start overhead due to the full set of auto-instrumentation.
* **Slim layer (recommended)**: lite mode is enabled by default, which disables all auto-instrumentation *except* the `aws-lambda-java-core` modules. This means Java Lambda handlers that implement `RequestHandler` or `RequestStreamHandler` are automatically instrumented and begin emitting telemetry out of the box, while other instrumentation (AWS SDK calls, HTTP clients, etc.) is disabled unless you explicitly opt in. This is the recommended starting point for most customers.

For installation instructions, see [Layered instrumentation](/docs/serverless-function-monitoring/aws-lambda-monitoring/instrument-lambda-function/instrument-your-own/).

When using either layer, set your Lambda function's handler to one of the following wrapper values so the agent can intercept the invocation:

* `RequestHandler` implementation: `com.newrelic.java.HandlerWrapper::handleRequest`
* `RequestStreamHandler` implementation: `com.newrelic.java.HandlerWrapper::handleStreamsRequest`

### Automatic detection

The agent automatically enables serverless mode when the `AWS_LAMBDA_FUNCTION_NAME` environment variable is present in the execution environment, no explicit configuration is required when using the Lambda layer.

To manually enable or disable serverless mode, set:

```ini
NEW_RELIC_SERVERLESS_MODE_ENABLED=true
```

Or in `newrelic.yml`:

```yaml
common: &default_settings
  serverless_mode:
    enabled: true
```

<Callout variant="important">
  Logs-in-context will be captured by the Lambda extension or CloudWatch, not by in-agent log forwarding.
</Callout>

### Event source detection and transaction classification [#event-sources]

When APM Lambda mode is enabled, the agent inspects the incoming event payload to detect the AWS service that triggered the invocation. Based on the detected event source, the agent:

* Renames the transaction using the format `{WebTransaction or OtherTransaction}/Function/{eventSourceType} {functionName}`.
* Adds event-specific agent attributes (for example, HTTP method and URI for API Gateway events).
* Classifies the transaction as a **web transaction** for API Gateway Proxy, API Gateway V2, and Application Load Balancer events, or as an **other transaction** for all other event sources (SQS, S3, DynamoDB, etc.).

When APM Lambda mode is disabled, all transactions use the default naming format `{WebTransaction or OtherTransaction}/Function/{functionName}`.

### Cold starts [#cold-starts]

The agent tracks cold starts using a static flag that is set on the first invocation of a given execution environment. On cold start invocations, the `aws.lambda.coldStart=true` agent attribute is added to the transaction. This attribute is omitted entirely on warm invocations, it is not set to `false`.

<Callout variant="important">
  **Cold start overhead**: Adding the Java agent to a Lambda function increases cold start time by approximately 4,000–6,200 ms compared to an uninstrumented function. This is a one-time cost per execution environment, warm invocations are not affected. The slim layer mitigates this by limiting the number of instrumentation modules loaded at startup.
</Callout>

### Telemetry collected [#telemetry]

In serverless mode, the Java agent collects and buffers the following telemetry types during invocation:

* Transaction traces
* Error events and traced errors (with stack traces)
* Span events (for distributed tracing)
* Transaction (analytic) events
* Custom events
* Log events
* Performance metrics

All collected data is written as a single `NR_LAMBDA_MONITORING` payload and forwarded to New Relic after the invocation completes.

### Limitations [#limitations]

* **Java 17+ only.** Java 11 and earlier are not supported.
* Thread profiling is not available in serverless mode.
* JFR (Java Flight Recorder) monitoring is disabled in serverless mode.
* The JAR collector (module scanning) is disabled in serverless mode.
