---
title: Install the Node.js agent
tags:
  - Agents
  - Nodejs agent
  - Installation and configuration
translate:
  - jp
  - kr
metaDescription: Install New Relic's Node.js agent to start monitoring your Node.js application.
redirects:
  - /docs/agents/nodejs-agent/installation-configuration/install-nodejs-agent
  - /docs/nodejs/installing-and-maintaining-nodejs
  - /docs/agents/nodejs-agent/installation-and-configuration/installing-and-maintaining-nodejs
  - /docs/agents/nodejs-agent/installation-configuration/installing-maintaining-nodejs
  - /docs/agents/nodejs-agent/installation-configuration/install-maintain-nodejs
freshnessValidatedDate: never
---

The Node.js agent is publicly available on the [npm repository](https://npmjs.org/package/newrelic) and on [GitHub](https://github.com/newrelic/node-newrelic).

<Callout variant="tip">
  Using Docker? See [Install the Node.js agent in Docker](/docs/apm/agents/nodejs-agent/installation-configuration/install-nodejs-agent-docker).
</Callout>

## Prerequisites [#prerequisites]

Check these requirements before you start.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>Requirement</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**New Relic account**</td>
      <td>You need a [New Relic account](https://newrelic.com/signup) and your <InlinePopover type="licenseKey"/>. Have it ready before you begin.</td>
    </tr>
    <tr>
      <td>**Node.js version**</td>
      <td>See [Node.js agent compatibility and requirements](/docs/apm/agents/nodejs-agent/getting-started/compatibility-requirements-nodejs-agent) for supported Node.js versions.</td>
    </tr>
  </tbody>
</table>

<Steps>
  <Step>
    ## Install the agent [#install]

    Install the agent package in your application directory:

    ```bash
    npm install newrelic
    ```

    Then copy the default configuration file from the package into the root of your application:

    ```bash
    cp node_modules/newrelic/newrelic.js .
    ```
  </Step>

  <Step>
    ## Configure the agent [#configure]

    Open `newrelic.js` in the root of your application. The only two settings you **must** change before going live:

    1. Set `license_key` to your <InlinePopover type="licenseKey"/>.
    2. Set `app_name` to a meaningful name for your application.

    ```js
    exports.config = {
      app_name: ['Your Application Name'],
      license_key: 'YOUR_LICENSE_KEY',
      // ...
    }
    ```

    You can also set these via environment variables instead of editing the file:

    ```bash
    NEW_RELIC_APP_NAME="Your Application Name"
    NEW_RELIC_LICENSE_KEY="YOUR_LICENSE_KEY"
    ```
  </Step>

  <Step>
    ## Start the agent [#start]

    Load the agent before any other `require` statements by adding `-r newrelic` to your startup command:

    ```bash
    node -r newrelic ./your-app.js
    ```

    For Docker, update your `CMD` in the Dockerfile:

    ```dockerfile
    CMD ["node", "-r", "newrelic", "server.js"]
    ```

    <Callout variant="important">
      If you cannot use the `-r` flag, add `require('newrelic')` as the **first line** of your app's main module. Do not use this approach with Babel or other transpilers — it will cause instrumentation issues.
    </Callout>
  </Step>

  <Step>
    ## Verify data is flowing [#verify]

    1. Generate some traffic to your application.

    2. Wait 2–3 minutes, then open [one.newrelic.com > APM](https://one.newrelic.com/apm) and look for your app by name.

    <Callout variant="tip">
      App not appearing? See [Troubleshooting your Node.js installation](/docs/apm/agents/nodejs-agent/troubleshooting/troubleshooting-your-nodejs-installation).
    </Callout>
  </Step>
</Steps>

## Related articles [#related-docs]

* [Node.js agent compatibility and requirements](/docs/apm/agents/nodejs-agent/getting-started/compatibility-requirements-nodejs-agent) — Supported Node.js versions and frameworks.
* [Node.js agent configuration](/docs/apm/agents/nodejs-agent/installation-configuration/nodejs-agent-configuration) — All available configuration settings.
* [Install in Docker](/docs/apm/agents/nodejs-agent/installation-configuration/install-nodejs-agent-docker) — Container-specific installation steps.
* [Update the Node.js agent](/docs/apm/agents/nodejs-agent/installation-configuration/update-nodejs-agent) — How to upgrade to a newer version.
