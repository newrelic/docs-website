---
title: 'PHP agent installation: Tar file (x86_64, aarch64)'
tags:
  - Agents
  - PHP agent
  - Installation
metaDescription: 'To install or update your New Relic PHP agent from compressed tar archives, start here.'
redirects:
  - /docs/agents/php-agent/installation/php-agent-installation-tar-file
  - /docs/php/php-agent-installation-tar-files
  - /docs/php/php-agent-installation-tar-file
freshnessValidatedDate: 2024-07-31
---

You can install New Relic's PHP agent from compressed tar files on any supported operating system on any supported processor architecture. However, for Redhat or CentOS on x86_64 it is more common to use the [RPM package](/docs/agents/php-agent/installation/php-agent-installation-aws-linux-redhat-centos/). For Ubuntu or Debian on x86_64 it is more common to use the [Debian package](/docs/agents/php-agent/installation/php-agent-installation-ubuntu-debian).

<DocTiles numbered>
  <DocTile
    title="Install the agent"
    label={{text: 'You are here', color: '#FCD672'}}
    path="/docs/apm/agents/php-agent/installation/php-agent-installation-tar-file#download"
  />

  <DocTile
    title="Configure your app"
    path="/docs/apm/agents/php-agent/installation/php-agent-installation-tar-file#configure"
  />

  <DocTile
    title="Verify data is flowing"
    path="/docs/apm/agents/php-agent/installation/php-agent-installation-tar-file#verify"
  />

  <DocTile
    title="Complete your setup"
    path="/docs/apm/agents/php-agent/installation/php-agent-installation-tar-file#complete-setup"
  />
</DocTiles>

## Download the tar distribution [#download]

Unlike other installation options, tar archives do not require any special repository setup. All you need to do is download the archive and follow these instructions.

Download the appropriate tar distribution file from <DNT>**[download.newrelic.com/php_agent/release/](https://download.newrelic.com/php_agent/release/)**</DNT>:

* For Linux with GNU libc (most Linux distributions), download `newrelic-php5-X.X.X.X-linux.tar.gz`.
* For Linux with musl libc (Alpine Linux), download `newrelic-php5-X.X.X.X-linux-musl.tar.gz`.

<Callout variant="tip">
  The package name for the PHP agent is `newrelic-php5`. Although the package name references PHP 5, this package works for all [supported PHP versions](/docs/agents/php-agent/getting-started/php-agent-compatibility-requirements#php-release).
</Callout>

## Install or update [#install]

With tar file installation, the steps for first time installation and for updating are the same. Replace X.X.X.X with the correct New Relic version. To install or update the agent:

1. Download the appropriate tar file from the [New Relic website](https://download.newrelic.com/php_agent/release/), and save it to a local disk in a convenient location. Don't use the `/tmp` folder.
2. Decompress and extract the archive:

   ```bash
   gzip -dc newrelic-php5-X.X.X.X-OS.tar.gz | tar xf -
   ```
3. Change to the newly created directory:

   ```bash
   cd newrelic-php5-X.X.X.X-OS
   ```
4. Run the installation script:

   ```bash
   ./newrelic-install
   ```
5. Change the default [application name](/docs/site/naming-your-application) to a meaningful name.
6. Restart your web server (Apache, Nginx, PHP-FPM, etc.).
7. Wait a few minutes for your application to send data, then [check your app's performance in the UI](/docs/apm/applications-menu/monitoring/apm-overview-page).

<InstallFeedback/>

## Name and configure your app [#configure]

**Required: Name your application**

[Give your app a meaningful name](/docs/apm/agents/php-agent/configuration/name-your-php-application). This is how New Relic groups your metrics — if you skip this, all apps report under the default name and become impossible to tell apart.

Set it directly in `newrelic.ini`:

```ini
newrelic.appname = "Your App Name"
```

Or use this command to set it automatically (replace `YOUR_APP_NAME`):

```bash
sudo find /etc/php.d /etc/php /etc/php.ini -name 'newrelic.ini' -type f 2>/dev/null | \
  xargs sudo sed -i 's/newrelic.appname = "PHP Application"/newrelic.appname = "YOUR_APP_NAME"/g'
```

**Before production: Review key settings**

Check your [transaction tracing settings](/docs/apm/agents/php-agent/configuration/php-agent-configuration#inivar-tt-settings) and [error collection](/docs/apm/agents/php-agent/configuration/php-agent-configuration#inivar-error-collector) before going live. See the full [PHP agent configuration reference](/docs/apm/agents/php-agent/configuration/php-agent-configuration) for all options.

## Verify data is flowing [#verify]

1. Restart your web server (Apache, Nginx, PHP-FPM, etc.).
2. Send at least two requests to your application — the PHP daemon requires two transactions on first start before it begins reporting data.
3. Run the following command to manually trigger the agent and confirm it initializes:

   ```bash
   php -r "phpinfo(INFO_GENERAL);"
   ```

4. Wait a few minutes, then [check your app's performance in New Relic](/docs/apm/applications-menu/monitoring/apm-overview-page).

<img
  title="Restart your web server — PHP guided install"
  alt="The New Relic guided install wizard showing restart commands for PHP-FPM, Apache, and Nginx with copy-to-clipboard buttons."
  src="/images/apm_screenshot-full_php-guided-install-restart-webserver.png"
/>

<figcaption>
  The guided install provides restart commands for each web server type. Use whichever matches your setup.
</figcaption>

<Callout variant="tip">
  No data after a few minutes? See [No data appears (PHP)](/docs/agents/php-agent/troubleshooting/no-data-appears-php) for the most common causes.
</Callout>

## Complete your setup [#complete-setup]

A basic install gives you transaction traces, error rates, and throughput. To get the full observability picture, enable these after your initial install:

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>Feature</th>
      <th>What it gives you</th>
      <th>Doc</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Distributed tracing</td>
      <td>See requests flowing across services and microservices end-to-end</td>
      <td>[Enable distributed tracing](/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent)</td>
    </tr>
    <tr>
      <td>Browser monitoring</td>
      <td>Correlate back-end performance with real-user front-end experience</td>
      <td>[Browser monitoring with PHP](/docs/apm/agents/php-agent/features/browser-monitoring-php-agent)</td>
    </tr>
    <tr>
      <td>Logs in context</td>
      <td>See log messages alongside your errors and traces without switching tools</td>
      <td>[PHP logs in context](/docs/logs/logs-context/configure-logs-context-php/)</td>
    </tr>
    <tr>
      <td>Code-level metrics</td>
      <td>View APM data inline in your IDE via the CodeStream extension</td>
      <td>[Code-level metrics](/docs/apm/agents/php-agent/features/php-code-level-metrics)</td>
    </tr>
    <tr>
      <td>Custom instrumentation</td>
      <td>Capture transactions not covered by automatic instrumentation</td>
      <td>[PHP custom instrumentation](/docs/apm/agents/php-agent/features/php-custom-instrumentation)</td>
    </tr>
    <tr>
      <td>Framework-specific setup</td>
      <td>Extra steps for Laravel, WordPress, Drupal, Magento, and others</td>
      <td>[Frameworks and libraries](/docs/apm/agents/php-agent/frameworks-libraries)</td>
    </tr>
  </tbody>
</table>

## What's next [#whats-next]

<DocTiles>
  <DocTile
    title="View your data in APM"
    path="/docs/apm/applications-menu/monitoring/apm-overview-page"
  >
    Confirm data is flowing and explore transactions, errors, and throughput on your APM summary page.
  </DocTile>

  <DocTile
    title="Enable distributed tracing"
    path="/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent"
  >
    See requests flowing end-to-end across services. Enabled by default since agent 9.21 — verify your setup here.
  </DocTile>

  <DocTile
    title="Explore frameworks and libraries"
    path="/docs/apm/agents/php-agent/frameworks-libraries"
  >
    Extra setup steps for Laravel, WordPress, Drupal, Magento, Guzzle, and other supported frameworks.
  </DocTile>

  <DocTile
    title="No data appearing?"
    path="/docs/agents/php-agent/troubleshooting/no-data-appears-php"
  >
    If nothing shows up after a few minutes, start here for the most common causes and fixes.
  </DocTile>
</DocTiles>
