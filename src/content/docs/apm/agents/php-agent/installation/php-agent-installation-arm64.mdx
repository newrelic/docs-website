---
title: "Install on ARM64 (aarch64)"
tags:
  - Agents
  - PHP agent
  - Installation
  - Arm64
metaDescription: "How to install New Relic's PHP agent on ARM64 (aarch64) architecture using the tar archive method."
redirects:
  - /docs/php/php-agent-installation-arm64
freshnessValidatedDate: 2024-07-31
---

Our PHP agent auto-instruments your code so you can start monitoring applications. This page covers installation on aarch64 (ARM64) architecture.

<Callout variant="important">
  **ARM64 uses the tar archive method — not apt or yum.** The New Relic apt and yum repositories only carry x86_64 builds. If you're on Ubuntu, Debian, Amazon Linux, RHEL, or CentOS running ARM64, the package manager method will not work. Use the tar archive instructions on this page instead.
</Callout>

## Prerequisites [#prerequisites]

Check these requirements before you start. If your environment doesn't match, you'll hit a blocker mid-install.

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>Requirement</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**New Relic account**</td>
      <td>You need a [New Relic account](https://newrelic.com/signup) and your <InlinePopover type="licenseKey"/>. The install script prompts for it — have it ready before you begin.</td>
    </tr>
    <tr>
      <td>**CPU architecture**</td>
      <td>aarch64 (ARM64) only. On x86_64? Use the [Ubuntu/Debian](/docs/apm/agents/php-agent/installation/php-agent-installation-ubuntu-debian) or [Amazon Linux/RHEL/CentOS](/docs/apm/agents/php-agent/installation/php-agent-installation-aws-linux-redhat-centos) page instead.</td>
    </tr>
    <tr>
      <td>**PHP version**</td>
      <td>PHP 8.0 or higher. ARM64 binaries are not available for PHP 7.x. See [PHP agent compatibility and requirements](/docs/agents/php-agent/getting-started/php-agent-compatibility-requirements).</td>
    </tr>
    <tr>
      <td>**OS**</td>
      <td>Any Linux distribution on aarch64. Common: Amazon Linux 2, Ubuntu 20.04+, Debian 11+, RHEL 8+.</td>
    </tr>
    <tr>
      <td>**Web server**</td>
      <td>Apache (mod_php), Nginx + PHP-FPM, or a standalone PHP-FPM pool. CLI scripts are also instrumented automatically.</td>
    </tr>
    <tr>
      <td>**Permissions**</td>
      <td>`sudo` or root access required — needed to run the `newrelic-install` script.</td>
    </tr>
  </tbody>
</table>

<Steps>
  <Step>
    ## Install the agent [#install]

    ARM64 installation uses the tar archive method. Download the archive matching your libc type, replacing `X.X.X.X` with the [latest version](/docs/release-notes/agent-release-notes/php-release-notes):

    ```
    newrelic-php5-X.X.X.X-linux-aarch64.tar.gz       # glibc (most distros)
    newrelic-php5-X.X.X.X-linux-musl-aarch64.tar.gz  # musl libc (Alpine Linux)
    ```

    Then follow the [tar file installation instructions](/docs/apm/agents/php-agent/installation/php-agent-installation-tar-file#install) to extract the archive, run `newrelic-install`, and restart your web server.

  </Step>

  <Step>
    ## Configure your app [#configure]

    The install script creates `newrelic.ini` with your license key pre-filled. The only setting you **must** change before going live is the app name.

    Open `newrelic.ini` and replace the default:

    ```ini
    newrelic.appname = "Your App Name"
    ```

    Every app that shares the same name reports its data together in New Relic. Use a name that clearly identifies this service (for example, `"Payments API - Production"`).

    Can't find `newrelic.ini`? Run:

    ```bash
    php --ini | grep newrelic
    ```

    For the full settings reference, see [PHP agent configuration](/docs/apm/agents/php-agent/configuration/php-agent-configuration).
  </Step>

  <Step>
    ## Verify data is flowing [#verify]

    1. Send at least **two requests** to your application — the PHP daemon requires two transactions on first start before it begins reporting.

    2. Wait 2–3 minutes, then open [one.newrelic.com > APM](https://one.newrelic.com/apm) and look for your app by name.

    <Callout variant="tip">
      App not appearing after a few minutes? Run `php -r "phpinfo();" | grep -i newrelic` to confirm the extension is loaded. For other common causes, see [No data appears (PHP)](/docs/agents/php-agent/troubleshooting/no-data-appears-php).
    </Callout>
  </Step>
</Steps>

## Related articles [#related-docs]

* [View your data in APM](/docs/apm/applications-menu/monitoring/apm-overview-page) — Confirm data is flowing and explore transactions, errors, and throughput on your APM summary page.
* [Enable distributed tracing](/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent) — See requests flowing end-to-end across services. Enabled by default since agent 9.21 — verify your setup here.
* [No data appearing?](/docs/agents/php-agent/troubleshooting/no-data-appears-php) — If nothing shows up after a few minutes, start here for the most common causes and fixes.
