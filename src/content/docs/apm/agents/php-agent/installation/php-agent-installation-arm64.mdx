---
title: "PHP agent installation: aarch64 (ARM64)"
tags:
  - Agents
  - PHP agent
  - Installation
  - Arm64
metaDescription: "How to install New Relic's PHP agent on ARM64 (aarch64) architecture using the tar archive method."
redirects:
  - /docs/php/php-agent-installation-arm64
freshnessValidatedDate: 2024-07-31
---

Our PHP agent auto-instruments your code so you can start monitoring applications. This page covers installation on aarch64 (ARM64) architecture.

<Callout variant="important">
  **ARM64 uses the tar archive method — not apt or yum.** The New Relic apt and yum repositories only carry x86_64 builds. If you're on Ubuntu, Debian, Amazon Linux, RHEL, or CentOS running ARM64, the package manager method will not work. Use the tar archive instructions on this page instead.
</Callout>

## Before you begin [#prerequisites]

<table>
  <thead>
    <tr>
      <th style={{ width: "200px" }}>Requirement</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**New Relic account**</td>
      <td>You need a [New Relic account](https://newrelic.com/signup) and your <InlinePopover type="licenseKey"/>.</td>
    </tr>
    <tr>
      <td>**CPU architecture**</td>
      <td>aarch64 (ARM64) only. On x86_64? Use the [Ubuntu/Debian](/docs/apm/agents/php-agent/installation/php-agent-installation-ubuntu-debian) or [Amazon Linux/RHEL/CentOS](/docs/apm/agents/php-agent/installation/php-agent-installation-aws-linux-redhat-centos) page instead.</td>
    </tr>
    <tr>
      <td>**PHP version**</td>
      <td>PHP 8.0 or higher. ARM64 binaries are not available for PHP 7.x. See [PHP agent compatibility and requirements](/docs/agents/php-agent/getting-started/php-agent-compatibility-requirements).</td>
    </tr>
    <tr>
      <td>**OS**</td>
      <td>Any Linux distribution on aarch64. Common: Amazon Linux 2, Ubuntu 20.04+, Debian 11+, RHEL 8+.</td>
    </tr>
    <tr>
      <td>**Web server**</td>
      <td>Apache (mod_php), Nginx + PHP-FPM, or a standalone PHP-FPM pool.</td>
    </tr>
    <tr>
      <td>**Permissions**</td>
      <td>`sudo` or root access required.</td>
    </tr>
  </tbody>
</table>

## Install [#install]

ARM64 installation uses the tar archive method. Follow the [tar file installation instructions](/docs/apm/agents/php-agent/installation/php-agent-installation-tar-file), selecting the `aarch64` archive when prompted.

When downloading, look for a filename containing `aarch64` or `musl-aarch64`:

```
newrelic-php5-X.X.X.X-linux-aarch64.tar.gz       # glibc (most distros)
newrelic-php5-X.X.X.X-linux-musl-aarch64.tar.gz  # musl libc (Alpine Linux)
```

The rest of the install process — running `newrelic-install`, setting your app name, and restarting your web server — is identical to the tar file method.

<InstallFeedback/>

## What's next [#whats-next]

<DocTiles>
  <DocTile
    title="View your data in APM"
    path="/docs/apm/applications-menu/monitoring/apm-overview-page"
  >
    Confirm data is flowing and explore transactions, errors, and throughput on your APM summary page.
  </DocTile>

  <DocTile
    title="Enable distributed tracing"
    path="/docs/apm/agents/php-agent/configuration/distributed-tracing-php-agent"
  >
    See requests flowing end-to-end across services. Enabled by default since agent 9.21 — verify your setup here.
  </DocTile>

  <DocTile
    title="No data appearing?"
    path="/docs/agents/php-agent/troubleshooting/no-data-appears-php"
  >
    If nothing shows up after a few minutes, start here for the most common causes and fixes.
  </DocTile>
</DocTiles>
