---
subject: Streaming Video & Ads for Mobile
title: Media agent for iOS v5.0.0
releaseDate: '2026-09-23'
version: 5.0.0
metaDescription: Release notes for Media agent for iOS version 5.0.0
category: iOS
features:
  - Added `NRTHEOplayerTracker`, a new tracker for THEOplayer (Dolby OptiView Player), with content lifecycle events, QoE/rendition tracking, and error mapping
bugs:
  - Fixed `NRVAVideo.addPlayer()` silently defaulting to an AVPlayer tracker when it couldn't identify the player type; it now returns without creating a tracker unless `playerType` is set explicitly
enhancements: []
security: []
---

### ⚠ BREAKING CHANGES

* `NRVAVideo.addPlayer()`: Now returns without creating a tracker when it can't identify the player type and no explicit `playerType` was set, instead of silently defaulting to an AVPlayer tracker. If you pass a custom or wrapped player object, set `NRVAVideoPlayerConfiguration.playerType` explicitly.

### Features

* `NRTHEOplayerTracker`: Added a new tracker for THEOplayer (Dolby OptiView Player) — content lifecycle events, QoE/rendition tracking, and error mapping. iOS only, iOS 13.0+. Available via CocoaPods, XCFrameworks, and Swift Package Manager.
