---
subject: Flutter agent
releaseDate: '2026-08-18'
version: 1.2.11
downloadLink: 'https://pub.dev/packages/newrelic_mobile/versions/1.2.11'
features: []
enhancements: ["Updated the native Android agent to version 7.8.1.", "Updated the native iOS agent to version 7.7.6."]
bugs: ["Fixed the iOS bridge hardcoding `andParams: nil`, which silently dropped custom `httpParams` and tracked-header values from `MobileRequest`/`MobileRequestError` events"]
security: []
---

## Bug Fixes

* Fixed the iOS bridge hardcoding `andParams: nil`, which silently dropped custom `httpParams` and tracked-header values from `MobileRequest`/`MobileRequestError` events. Android already forwarded params correctly.

## Enhancements

* Updated the native Android agent to version 7.8.1.
* Updated the native iOS agent to version 7.7.6.
