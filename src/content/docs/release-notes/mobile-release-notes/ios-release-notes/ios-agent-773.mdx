---
subject: iOS agent
releaseDate: '2026-07-02'
version: 7.7.3
downloadLink: 'https://download.newrelic.com/ios_agent/NewRelic_XCFramework_Agent_7.7.3.zip'
---

### New Features

Offline storage functionality for Mobile Session Replay.

### Fixed in this release

Replace timestamp-seeded LCG in IGuidGenerator with arc4random_buf.

Add EU/JP region detection to RN Sourcemap upload.

Block recursion for MAUI style default interaction swizzle.

Rename appVersionId to appVersion in RN source map upload.

Fixed infinite loop in EventDeserializer that froze the agent on launch.

Change default session replay masking mode to not allow unmasking using programmatic apis and fix UIKit override APIs.

Fixed crash that could occur in NRMAWKWebViewNavigationDelegate webViewWebContentProcessDidTerminate.

Session replay data should be uploaded on next launch if it failed to upload when the app closed

Discard permanently-rejected (400/403) crash reports. 

Add TTL eviction for offline storage (#786). 

Fixed NRMAHarvestController initialize crash (#793) 
