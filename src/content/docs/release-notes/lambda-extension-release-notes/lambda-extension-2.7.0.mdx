---
subject: "Lambda-Extension"
releaseDate: '2026-08-19'
version: 2.7.0
---

## Notes

* Added full support for Lambda Managed Instances (LMI)
* `NEW_RELIC_LABELS` now supports Transaction and Span attributes
* Added `NEW_RELIC_DATA_COLLECTION_TIMEOUT` and `NEW_RELIC_HTTP_TIMEOUT` support for serverless mode
* Added `entity.name` for logs


## Fixed

* Send agent payload immediately on arrival instead of pairing with `platform.report`
* Disabled legacy smithy rustls defaults
