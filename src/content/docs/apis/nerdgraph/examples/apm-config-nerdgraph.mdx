---
title: "NerdGraph tutorial: APM agent configuration examples"
tags:
  - APIs
  - NerdGraph
  - Examples
metaDescription: Examples of using New Relic NerdGraph API to adjust APM 
redirects:
  - /docs/apis/nerdgraph/examples/export-dashboards-pdfpng-api-tutorial
---

This doc is a place for examples of configuring APM agents using our NerdGraph API. 

## Configure server-side configuration [#server-side-config]

See [server-side config requirements](/docs/apm/agents/manage-apm-agents/configuration/server-side-agent-configuration#requirements).

Here's an example query returning the status of the server side configuration setting for a given entity. 

```
query ExampleReadQuery {
  actor {
    entity(guid:"ZjY1ODgxfEFQTXxBUFBYSUNBVElPTnz0ODEwMTY3NzZ") {
      ...on ApmApplicationEntity {
        apmSettings {
          apmConfig {
            useServerSideConfig
          }
        }
      }
    }
  }
}
```

Here's an example of disabling [server-side configuration](/docs/apm/agents/manage-apm-agents/configuration/server-side-agent-configuration): 

```
mutation ExampleUpdateQuery($settings: [AgentApplicationSettingsUpdateInput!]!) {
  agentApplicationSettingsUpdate(settings: $settings) {
    apmSettings {
      apmConfig {
        useServerSideConfig
      }
    }
    errors {
      description
    }
  }
}
```

And here's an example of what might be used for the `settings` query variables. Note that this uses an array, which may be helpful if you have a lot of entities you want to update. 

```
{
  "settings": [{
    "guid": "ZjY1ODgxfEFQTXxBUFBYSUNBVElPTnz0ODEwMTY3NzZ",
    "apmConfig": {
      "useServerSideConfig": false
    }
  }]
}
```

For how to find an entity's GUID, see [Find entity data](/docs/new-relic-one/use-new-relic-one/core-concepts/what-entity-new-relic/#find).