---
title: "Microsoft SQL Server monitoring with NRDOT"
tags:
  - OpenTelemetry
  - OpenTelemetry Collector
  - nrdot-collector-MSSQL
  - nrdot-collector-Database
  - NRDOT
  - SQL Server
metaDescription: New Relic's Microsoft SQL Server monitoring integration using NRDOT collector
freshnessValidatedDate: never
---

import AgentConfig from '../../../../components/AgentConfig';

<Callout title="Preview">

  We're still working on this feature, but we'd love for you to try it out!

  This feature is currently provided as part of a preview pursuant to our [pre-release policies](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).

</Callout>

New Relic now empowers your team to monitor your Microsoft SQL Server database performance using New Relic Distribution of OpenTelemetry (NRDOT) with New Relic's database monitoring capabilities. This integration provides comprehensive insights into database metrics, query performance, and system health using the NRDOT collector.

This NRDOT-based approach complements our existing On-Host Integration (OHI) by leveraging OpenTelemetry standards for database monitoring, making it easier to integrate with your existing observability stack.

## Prerequisites [#prerequisites]

Before you begin, ensure you have the following:

* Valid New Relic [license key](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)
* Supported database versions: SQL Server 2017 or later
* SQL Server database setup:
  * Administrative access to your SQL Server instance (`sysadmin` role or equivalent)
  * Microsoft SQL Server that supports on-premises or managed instances
  * Network connectivity between the NRDOT collector and your SQL Server
  * SQL Server Management Studio (SSMS) or `sqlcmd` utility
  * Server hostname or IP address
  * Port number (usually 1433) or custom port
  * Windows domain or SQL Server authentication or gMSA
  * SSL preference

* System requirements:
  * Windows or Linux machine to run the NRDOT collector
  * Network connectivity to New Relic's OTLP endpoint

* This integration is available as a part of New Relic public preview program. Check with your Organization Manager to opt in from the [Previews & Trials](https://one.newrelic.com/admin-portal/promotion-management/home) page.

{/* ## How OTEL database monitoring differs from OHI

New Relic offers two approaches for SQL Server monitoring:

<Tabs>
<TabsBar>
<TabsBarItem id="otel-approach">NRDOT</TabsBarItem>
<TabsBarItem id="ohi-approach">On-Host Integration (OHI)</TabsBarItem>
</TabsBar>

<TabsPages>
<TabsPageItem id="otel-approach">

**NRDOT approach:**
* Uses New Relic Distribution of OpenTelemetry (NRDOT)
* Runs as a separate collector process
* Vendor-neutral and portable across platforms
* Integrates with any OTEL-compatible observability platform
* Ideal for cloud-native and containerized environments

</TabsPageItem>
<TabsPageItem id="ohi-approach">

**On-Host Integration approach:**
* Uses New Relic's proprietary infrastructure agent
* Tightly integrated with New Relic's infrastructure monitoring
* Includes additional New Relic-specific features
* Requires New Relic infrastructure agent installation
* Optimal for New Relic-centric monitoring strategies

</TabsPageItem>
</TabsPages>
</Tabs>

*/}


## Set-up the NRDOT Collector for Microsoft SQL Server monitoring [#install-collector]

You can use the NRDOT Collector to monitor your Microsoft SQL Server databases. The NRDOT collector is a pre-configured distribution that includes New Relic-specific components.

You can configure NRDOT Collector using the following methods:

* [Guided install](/docs/opentelemetry/database/otel-mssql/#guided): The guided install methods provides step-by-step instructions on the New Relic platform to help you install and configure the NRDOT Collector for SQL Server monitoring. 
* [Self-hosted](/docs/opentelemetry/database/otel-mssql/#manual): The self-hosted method allows you to set up the NRDOT Collector on your own by following the instructions in this documentation. This method is ideal for users who prefer a hands-on approach or have specific configuration requirements.
* [RDS](/docs/opentelemetry/database/otel-mssql/#rds): The RDS method provides instructions for setting up the NRDOT Collector for Amazon RDS SQL Server instances.


<Tabs>
<TabsBar>
<TabsBarItem id="guided">Guided install</TabsBarItem>
<TabsBarItem id="manual">Self-hosted</TabsBarItem>
<TabsBarItem id="rds">RDS</TabsBarItem>
</TabsBar>

<TabsPages>
<TabsPageItem id="guided">


To install the NRDOT Collector:

1. Log in to your [New Relic account](https://one.newrelic.com/).
2. Go to <DNT>**left navigation pane > + Integration & Agents > MSSQL (OpenTelemetry)**</DNT>.
3. On the <DNT>Select an account</DNT> screen, select the account you want to install the MSSQL integration on, and click <DNT>**Continue**</DNT>.
4. On the <DNT>Enter your credential</DNT> screen, select one of the following options, then click **Continue**:

    * **Use an existing key**: If you already have a license key, provide the license key. For more information, refer to [User keys](/docs/apis/intro-apis/new-relic-api-keys/#user-key).
    * **Create a new key**: If you don't have a license key, click **Create a new key** to create one.

5. On the <DNT>Select platform</DNT> screen, select the platform to install the MSSQL integration, and click <DNT>**Continue**</DNT>.
6. Follow the on-screen instructions to set up the integration for the selected platform.
7. After installation, on the <DNT>Test your integration</DNT> screen, click **Test connection** to verify that the NRDOT Collector is able to connect to your SQL Server instance and send data to New Relic. If the test is successful, you should see a confirmation message.


### Set up APM-database correlation [#optional-service-id]

To correlate your application performance with database operations, you can set up database service identification. This feature allows you to see exactly which applications are generating specific database workloads. For more information, refer to [set up database service identification](/docs/opentelemetry/database/database-service-identification/) to get APM-database correlation in New Relic.

<Callout variant="important">

  To view database performance data in APM, both entities must be in the same New Relic account. If the entities are in different New Relic accounts, you must have access to both accounts to view the data.

</Callout>


### Find and use your data [#find-use-data]

Once your data is being collected, you can access comprehensive SQL Server database monitoring through New Relic's UI. 

To find your SQL Server database entity in New Relic:

1. Go to <DNT>**[https://one.newrelic.com](https://one.newrelic.com) > All Capabilities > Databases**</DNT>.
2. Set the search criteria as <DNT>`instrumentation.provider = opentelemetry`</DNT>.
3. Select your SQL Server database from the list of entities.





</TabsPageItem>

<TabsPageItem id="manual">

Select your environment to see installation steps specific to your setup:

<Tabs>
<TabsBar>
<TabsBarItem id="windows-env">Windows</TabsBarItem>
<TabsBarItem id="linux-env">Linux</TabsBarItem>
</TabsBar>

<TabsPages>
<TabsPageItem id="windows-env">

Select your authentication method:

<Tabs>
<TabsBar>
<TabsBarItem id="windows-sql-auth">SQL Authentication</TabsBarItem>
<TabsBarItem id="windows-domain-auth">Windows Domain Authentication</TabsBarItem>
<TabsBarItem id="windows-gmsa-auth">gMSA</TabsBarItem>
</TabsBar>

<TabsPages>
<TabsPageItem id="windows-sql-auth">

<Steps>

<Step>

### Setup the NRDOT Collector [#setup-collector-windows-sql]

Install the NRDOT Collector on your Windows system.

<Callout variant="important">

It is recommended to install the NRDOT Collector on the same host as your SQL Server instance to view infra-level metrics for query performance monitoring.

</Callout>

1. Download and install the NRDOT Collector using PowerShell:

  ```powershell
  $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i nrdot-collector.msi /qn /norestart" -Wait
  ```

</Step>

<Step>

### Configure database user [#configure-user-windows-sql]

Run the following script as a `root user/sysadmin` to create the `newrelic` monitoring user and grant global read permissions.

To create a `newrelic` monitoring user, run the following script using SQL Server Management Studio (SSMS):

```sql
USE [master];
GO
CREATE LOGIN [newrelic] WITH PASSWORD = '<PASSWORD>';
GO
-- Instance-level permissions
GRANT VIEW SERVER STATE TO [newrelic];
GRANT VIEW ANY DEFINITION TO [newrelic];
GRANT VIEW ANY DATABASE TO [newrelic];
GO
-- Grant read access privileges to all user databases
DECLARE @name SYSNAME;
DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
SELECT [name]
FROM [master].[sys].[databases]
WHERE [name] NOT IN ('master', 'msdb', 'tempdb', 'model', 'rdsadmin', 'distribution')
AND [state] = 0; -- Only online databases
OPEN db_cursor;
FETCH NEXT FROM db_cursor INTO @name;
WHILE @@FETCH_STATUS = 0
BEGIN
  BEGIN TRY
    PRINT 'Processing database: ' + @name;
    EXEC('USE [' + @name + '];
    IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''newrelic'')
    BEGIN
      CREATE USER [newrelic] FOR LOGIN [newrelic];
    END;
    GRANT VIEW DATABASE STATE TO [newrelic];
    GRANT VIEW DEFINITION TO [newrelic];
    ALTER ROLE db_datareader ADD MEMBER [newrelic];');
    PRINT 'Success: ' + @name;
  END TRY
  BEGIN CATCH
    PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
  END CATCH
  FETCH NEXT FROM db_cursor INTO @name;
END
CLOSE db_cursor;
DEALLOCATE db_cursor;
GO
```

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-windows-sql]

Select your configuration type based on your monitoring needs:

<CollapserGroup>
  <Collapser
    id="full-config-sql"
    title="NRDOT Collector with Host Metrics + Database Monitoring"
  >

This configuration includes both infrastructure host metrics and database monitoring. **Enter your values below and see the configuration update in real-time:**

Replace your `config.yaml` file at `C:\Program Files\nrdot-collector\config.yaml`:

<Callout variant="important">

  If you're an existing NRDOT Collector user, ensure to merge this configuration with your existing `config.yaml` file. This includes adding the new receivers, processors, and exporters for MSSQL Database monitoring while retaining your current configurations.

</Callout> 

<AgentConfig
  inputOptions={[
    {
      name: 'username',
      label: 'SQL Server Username',
      codeLine: 62,
      defaultValue: 'newrelic',
      toolTip: 'Enter your SQL Server username for authentication',
    },
    {
      name: 'password',
      label: 'SQL Server Password',
      codeLine: 63,
      defaultValue: 'your_password',
      toolTip: 'Enter your SQL Server password for authentication',
    },
    {
      name: 'server',
      label: 'Server Hostname/IP',
      codeLine: 64,
      defaultValue: 'sqlserver.example.com',
      toolTip: 'Enter your SQL Server hostname or IP address',
    },
    {
      name: 'port',
      label: 'Server Port',
      codeLine: 65,
      defaultValue: '1433',
      toolTip: 'Enter your SQL Server port number (default: 1433)',
    },
    {
      name: 'endpoint',
      label: 'New Relic OTLP Endpoint',
      codeLine: 224,
      defaultValue: 'https://otlp.nr-data.net',
      toolTip: 'Enter your New Relic OTLP endpoint (e.g., https://otlp.nr-data.net or https://otlp.eu01.nr-data.net)',
    },
    {
      name: 'licenseKey',
      label: 'New Relic License Key',
      codeLine: 226,
      defaultValue: 'YOUR_LICENSE_KEY',
      toolTip: 'Enter your New Relic license key',
    },
  ]}
  config={`# NRDOT Collector Configuration for New Relic SQL Server Integration

extensions:
  health_check:

receivers:
  otlp:
    protocols:
      grpc:
      http:

  host_metrics:
    collection_interval: 60s
    scrapers:
      cpu:
        metrics:
          system.cpu.time:
            enabled: false
          system.cpu.utilization:
            enabled: true
      load:
      memory:
        metrics:
          system.memory.utilization:
            enabled: true
      paging:
        metrics:
          system.paging.utilization:
            enabled: false
          system.paging.faults:
            enabled: false
      filesystem:
        metrics:
          system.filesystem.utilization:
            enabled: true
      disk:
        metrics:
          system.disk.merged:
            enabled: false
          system.disk.pending_operations:
            enabled: false
          system.disk.weighted_io_time:
            enabled: false
      network:
        metrics:
          system.network.connections:
            enabled: false

  file_log:
    include:
      - /var/log/alternatives.log
      - /var/log/cloud-init.log
      - /var/log/auth.log
      - /var/log/dpkg.log
      - /var/log/syslog
      - /var/log/messages
      - /var/log/secure
      - /var/log/yum.log

  nrsqlserver:
    collection_interval: 15s
    username: <input1>
    password: <input2>
    server: <input3>
    port: <input4>

    # Only 2 specific metrics kept enabled
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true

    # Query performance monitoring (QPM) settings
    events:
      db.server.query_sample:
        enabled: true  
      db.server.top_query:
        enabled: true  

    top_query_collection:
      lookback_time: 15s              
      max_query_sample_count: 1000    
      top_query_count: 20             
      collection_interval: 15s        

    query_sample_collection:
      max_rows_per_query: 20          

    collect_full_query_text: true
    allowed_comment_keys:
      - nr_service_guid

processors:
  metrics_transform:
    transforms:
      - include: system.cpu.utilization
        action: update
        operations:
          - action: aggregate_labels
            label_set: [ state ]
            aggregation_type: mean
      - include: system.paging.operations
        action: update
        operations:
          - action: aggregate_labels
            label_set: [ direction ]
            aggregation_type: sum

  filter/exclude_cpu_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "interrupt"'
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "nice"'
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "softirq"'

  filter/exclude_memory_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "slab_unreclaimable"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "inactive"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "cached"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "buffered"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "slab_reclaimable"'

  filter/exclude_memory_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.memory.usage" and attributes["state"] == "slab_unreclaimable"'
        - 'metric.name == "system.memory.usage" and attributes["state"] == "inactive"'

  filter/exclude_filesystem_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.utilization" and attributes["type"] == "squashfs"'

  filter/exclude_filesystem_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.usage" and attributes["type"] == "squashfs"'
        - 'metric.name == "system.filesystem.usage" and attributes["state"] == "reserved"'

  filter/exclude_filesystem_inodes_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.inodes.usage" and attributes["type"] == "squashfs"'
        - 'metric.name == "system.filesystem.inodes.usage" and attributes["state"] == "reserved"'

  filter/exclude_system_disk:
    metrics:
      datapoint:
        - 'metric.name == "system.disk.operations" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.merged" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.io" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.io_time" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.operation_time" and IsMatch(attributes["device"], "^loop.*") == true'

  filter/exclude_system_paging:
    metrics:
      datapoint:
        - 'metric.name == "system.paging.usage" and attributes["state"] == "cached"'
        - 'metric.name == "system.paging.operations" and attributes["type"] == "cached"'

  filter/exclude_network:
    metrics:
      datapoint:
        - 'IsMatch(metric.name, "^system.network.*") == true and attributes["device"] == "lo"'

  attributes/exclude_system_paging:
    include:
      match_type: strict
      metric_names:
        - system.paging.operations
    actions:
      - key: type
        action: delete

  cumulativetodelta:

  transform/host:
    metric_statements:
      - context: metric
        statements:
          - set(metric.description, "")
          - set(metric.unit, "")

  transform:
    trace_statements:
      - context: span
        statements:
          - truncate_all(span.attributes, 4095)
          - truncate_all(resource.attributes, 4095)
    log_statements:
      - context: log
        statements:
          - truncate_all(log.attributes, 4095)
          - truncate_all(resource.attributes, 4095)

  memory_limiter:
    check_interval: 1s
    limit_mib: \${env:NEW_RELIC_MEMORY_LIMIT_MIB:-100}

  batch:
    timeout: 10s
    send_batch_size: 1024

  resource_detection:
    detectors: ["system"]
    system:
      hostname_sources: ["os"]
      resource_attributes:
        host.id:
          enabled: true

  resource_detection/cloud:
    detectors: ["gcp", "ec2", "azure"]
    timeout: 2s
    override: true

  resource_detection/env:
    detectors: ["env"]
    timeout: 2s
    override: true

exporters:
  otlphttp:
    endpoint: <input5>
    headers:
      api-key: <input6>
    tls:
      insecure: false
    compression: gzip

  debug:
    verbosity: detailed
    sampling_initial: 5
    sampling_thereafter: 200

service:
  telemetry:
    metrics:
      level: none

  extensions: [health_check]

  pipelines:
    metrics/host:
      receivers: [host_metrics, nrsqlserver]
      processors:
        - memory_limiter
        - metrics_transform
        - filter/exclude_cpu_utilization
        - filter/exclude_memory_utilization
        - filter/exclude_memory_usage
        - filter/exclude_filesystem_utilization
        - filter/exclude_filesystem_usage
        - filter/exclude_filesystem_inodes_usage
        - filter/exclude_system_disk
        - filter/exclude_network
        - attributes/exclude_system_paging
        - transform/host
        - resource_detection
        - resource_detection/cloud
        - resource_detection/env
        - cumulativetodelta
        - batch
      exporters: [otlphttp, debug]

    logs/host:
      receivers: [file_log, nrsqlserver]
      processors:
        - memory_limiter
        - transform
        - resource_detection
        - resource_detection/cloud
        - resource_detection/env
        - batch
      exporters: [otlphttp, debug]

    traces:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]

    metrics:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]`}
  fileName="config.yaml"
  tipMdx={{ body: null }}
  onChange={() => {}}
/>

#### Configuration parameters

The following table describes the key configuration parameters for the `nrsqlserver` receiver:

<table>
<thead>
  <tr>
    <th>Parameter</th>
    <th>Description</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>`<input1>` - Username</td>
    <td>Enter your database username for authentication</td>
  </tr>
  <tr>
    <td>`<input2>` - Password</td>
    <td>Enter your database password for authentication</td>
  </tr>
  <tr>
    <td>`<input3>` - Server</td>
    <td>Enter your SQL Server hostname or IP address</td>
  </tr>
  <tr>
    <td>`<input4>` - Port</td>
    <td>Enter your SQL Server port number. The default value is 1433.</td>
  </tr>
  <tr>
    <td>`<input5>` - OTLP Endpoint</td>
    <td>
    Enter the New Relic OTLP endpoint (e.g., https://otlp.nr-data.net). For more information, refer to [New Relic OTLP endpoints documentation](/docs/opentelemetry/best-practices/opentelemetry-otlp/).
    </td>
  </tr>
  <tr>
    <td>`<input6>` - License Key</td>
    <td>Enter your New Relic license key.</td>
  </tr>
  <tr>
    <td>`collection_interval`</td>
    <td>Interval in seconds to collect database metrics. Default: 15s</td>
  </tr>
  <tr>
    <td>`top_query_count`</td>
    <td>Number of top queries to track. Default: 20</td>
  </tr>
  <tr>
    <td>`max_rows_per_query`</td>
    <td>Maximum rows per query sample. Default: 20</td>
  </tr>
</tbody>
</table>

<Callout variant="tip">

**Interactive Configuration:** Use the form above to enter your specific values, and the configuration will update in real-time. Copy the generated YAML to your config file.

</Callout>

  </Collapser>

  <Collapser
    id="db-only-config-sql"
    title="Database Monitoring Only (Recommended)"
  >

This configuration focuses only on database monitoring without host infrastructure metrics. **Enter your values below and see the configuration update in real-time:**

<AgentConfig
  inputOptions={[
    {
      name: 'username',
      label: 'SQL Server Username',
      codeLine: 8,
      defaultValue: 'newrelic',
      toolTip: 'Enter your SQL Server username',
    },
    {
      name: 'password',
      label: 'SQL Server Password',
      codeLine: 9,
      defaultValue: 'your_password',
      toolTip: 'Enter your SQL Server password',
    },
    {
      name: 'server',
      label: 'Server Hostname/IP',
      codeLine: 10,
      defaultValue: 'sqlserver.example.com',
      toolTip: 'Enter your SQL Server hostname or IP address',
    },
    {
      name: 'port',
      label: 'Server Port',
      codeLine: 11,
      defaultValue: '1433',
      toolTip: 'Enter your SQL Server port number (default: 1433)',
    },
    {
      name: 'endpoint',
      label: 'New Relic OTLP Endpoint',
      codeLine: 52,
      defaultValue: 'https://otlp.nr-data.net',
      toolTip: 'Enter your New Relic OTLP endpoint (e.g., https://otlp.nr-data.net or https://otlp.eu01.nr-data.net)',
    },
    {
      name: 'licenseKey',
      label: 'New Relic License Key',
      codeLine: 54,
      defaultValue: 'YOUR_LICENSE_KEY',
      toolTip: 'Enter your New Relic license key',
    },
  ]}
  config={`
receivers:
  nrsqlserver:
    collection_interval: 15s
    # Connection details
    username: <input1>
    password: <input2>
    server: <input3>
    port: <input4>

    # Enable query performance monitoring
    events:
      db.server.query_sample:
        enabled: true
      db.server.top_query:
        enabled: true

    # Top query collection
    top_query_collection:
      lookback_time: 15s
      max_query_sample_count: 1000
      top_query_count: 20
      collection_interval: 15s

    # Query sample collection
    query_sample_collection:
      max_rows_per_query: 20

    # APM correlation
    collect_full_query_text: true
    allowed_comment_keys:
      - nr_service_guid

    # Core metrics (uptime, CPU count only)
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

exporters:
  otlphttp:
    endpoint: <input5>
    headers:
      api-key: <input6>
    compression: gzip

  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: none

  pipelines:
    metrics:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]`}
  fileName="config.yaml"
  tipMdx={{ body: null }}
  onChange={() => {}}
/>

<Callout variant="tip">

Use the form above to enter your specific values, and the configuration will update in real-time. Copy this configuration to `C:\Program Files\nrdot-collector\config.yaml` for database-only monitoring

</Callout>

  </Collapser>
</CollapserGroup>

  </Step>

  <Step>

### Validate NRDOT Collector Configuration [#validate-windows-sql]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```powershell
& "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\config.yaml"
```

</Step>

  <Step>

### Restart NRDOT collector [#restart-windows-sql]

After updating your configuration, restart the NRDOT Collector service:

```shell
net stop nrdot-collector
net start nrdot-collector
```

<Callout variant="tip">

Always restart the NRDOT collector service after making configuration changes to ensure the new settings take effect.

</Callout>

</Step>

</Steps>

</TabsPageItem>

<TabsPageItem id="windows-domain-auth">

<Steps>

<Step>

### Setup the NRDOT Collector [#setup-collector-windows-domain]

Install the NRDOT Collector on your Windows system.

1. Download and install the NRDOT Collector using PowerShell:

  ```powershell
  $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i nrdot-collector.msi /qn /norestart" -Wait
  ```

</Step>

<Step>

### Enable your Microsoft SQL Server [#configure-user-windows-domain]

1. Create a new login and grant CONNECT and VIEW permissions:

   ```sql
   USE master;
   CREATE LOGIN [DomainName\DomainUser] FROM windows;
   GRANT CONNECT SQL TO [DomainName\DomainUser];
   GRANT VIEW SERVER STATE TO [DomainName\DomainUser];
   GRANT VIEW ANY DEFINITION TO [DomainName\DomainUser];
   GO
   ```

2. Use the following statements to grant READ access privileges to the user:

   ```sql
   DECLARE @name SYSNAME
   DECLARE db_cursor CURSOR
   READ_ONLY FORWARD_ONLY
   FOR
   SELECT NAME
   FROM master.sys.databases
   WHERE NAME NOT IN ('master','msdb','tempdb','model','rdsadmin','distribution')
   OPEN db_cursor
   FETCH NEXT FROM db_cursor INTO @name WHILE @@FETCH_STATUS = 0
   BEGIN
       EXECUTE('USE "' + @name + '"; CREATE USER [DomainName\DomainUser] FOR LOGIN [DomainName\DomainUser];' );
       FETCH next FROM db_cursor INTO @name
   END
   CLOSE db_cursor
   DEALLOCATE db_cursor
   GO
   ```

<Callout variant="tip">

Run these SQL scripts using SQL Server Management Studio (SSMS) or `sqlcmd` utility. Make sure to replace `[DomainName\DomainUser]` with your actual Windows domain and username (e.g., `[CONTOSO\SQLMonitor]`).

</Callout>

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-windows-domain]

<Callout variant="important">

Windows Domain Authentication uses integrated security (Windows authentication), which uses the same configuration approach as gMSA authentication. The configurations support **both Windows Domain Authentication and gMSA authentication methods**.

</Callout>

**Refer to the [gMSA Configuration Templates](#gmsa-config-templates) section below** (after all installation tabs) for the complete NRDOT Collector configuration. Choose either:
- **Full Configuration** (Host Metrics + Database Monitoring)
- **Database Monitoring Only** - Recommended

Both configurations use Windows integrated security and work for:
- ✅ **Windows Domain Authentication** (domain user accounts)
- ✅ **gMSA** (Group Managed Service Accounts)

In the `datasource` connection string for on-premises SQL Server, use your server hostname:
```yaml
datasource: "server=<YOUR-HOSTNAME>;integrated security=true;encrypt=true;TrustServerCertificate=true;"
```

Example: `server=SQLServer01` or `server=192.168.1.100`

<Callout variant="tip">

**For Windows Domain Authentication specifically:** Ensure the NRDOT Collector service is configured to run as a domain user account that has been granted SQL Server permissions (see Step 2 above).

</Callout>

</Step>

<Step>

### Validate NRDOT Collector Configuration [#validate-windows-domain]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```powershell
& "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\config.yaml"
```

</Step>

<Step>

### Restart NRDOT collector [#restart-windows-domain]

After updating your configuration, restart the NRDOT Collector service:

```shell
net stop nrdot-collector
net start nrdot-collector
```

<Callout variant="tip">

Always restart the NRDOT collector service after making configuration changes to ensure the new settings take effect.

</Callout>

</Step>

</Steps>

</TabsPageItem>

<TabsPageItem id="windows-gmsa-auth">

<Steps>

<Step>

### Setup the NRDOT Collector [#setup-collector-gmsa]

Install the NRDOT Collector on your Windows system.

1. Download and install the NRDOT Collector using PowerShell:

  ```powershell
  $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i nrdot-collector.msi /qn /norestart" -Wait
  ```

</Step>

<Step>

### Enable your Microsoft SQL Server:**

Open SSMS, log in via Windows Authentication, open a New Query, and execute the following database administration script:

```sql
USE master;
GO

-- Provision an engine login identity for the newly created gMSA
CREATE LOGIN [dev\SQL_gMSA$] FROM WINDOWS;
GO

-- Elevate the gMSA to a full System Administrator role
ALTER SERVER ROLE [sysadmin] ADD MEMBER [dev\SQL_gMSA$];
GO

-- Grant visibility permissions to look at diagnostic DMVs and system metrics
GRANT VIEW SERVER STATE TO [dev\SQL_gMSA$];
GRANT VIEW ANY DEFINITION TO [dev\SQL_gMSA$];
GO
```

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-gmsa]

<Callout variant="important">

  gMSA requires a **different configuration** than SQL/Domain authentication. The NRDOT collector authenticates using the Windows service account (gMSA) without requiring username/password fields in the config.

</Callout>

**Refer to the [gMSA Configuration Templates](#gmsa-config-templates) section below** (after all installation tabs) for the complete configuration. Choose either:
- **Full Configuration** (Host Metrics + Database Monitoring)
- **Database Monitoring Only** - Recommended

In the datasource connection string, use your on-premises server hostname:
```
datasource: "server=<YOUR-HOSTNAME>;integrated security=true;encrypt=true;TrustServerCertificate=true;"
```

Example: `server=SQLServer01` or `server=192.168.1.100`

</Step>

<Step>

### Validate NRDOT Collector Configuration [#validate-gmsa]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```powershell
& "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\config.yaml"
```

</Step>

<Step>

### Restart NRDOT collector [#restart-gmsa]

After updating your configuration, restart the NRDOT Collector service:

```shell
net stop nrdot-collector
net start nrdot-collector
```

<Callout variant="tip">

Always restart the NRDOT collector service after making configuration changes to ensure the new settings take effect.

</Callout>

</Step>

</Steps>

</TabsPageItem>

</TabsPages>
</Tabs>

</TabsPageItem>

<TabsPageItem id="linux-env">

<Steps>

<Step>

### Setup the NRDOT Collector [#setup-collector-linux]

Install the NRDOT Collector on your Linux system.

<Callout variant="important">

It is recommended to install the NRDOT Collector on the same host as your SQL Server instance to view infra-level metrics for query performance monitoring.

</Callout>

1. **Debian / Ubuntu**

**For AMD64 (x86_64) Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_amd64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
  ```

**For ARM64 Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_arm64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
  ```

2. **RHEL / CentOS**

**For AMD64 (x86_64) Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_x86_64.rpm" --output nrdot-collector.rpm && sudo rpm -ivh nrdot-collector.rpm
  ```

**For ARM64 Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_arm64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
  ```

</Step>

<Step>

### Configure database user [#configure-user-linux]

Run the following script as a `root user/sysadmin` to create the `newrelic` monitoring user and grant global read permissions.

1. To create a `newrelic` monitoring user in Linux, create a file named `nr-grant-permission.sql` on your Linux instance with the following content:

  ```sql
  USE [master];
  GO
  CREATE LOGIN [newrelic] WITH PASSWORD = '<PASSWORD>';
  GO
  -- Instance-level permissions
  GRANT VIEW SERVER STATE TO [newrelic];
  GRANT VIEW ANY DEFINITION TO [newrelic];
  GRANT VIEW ANY DATABASE TO [newrelic];
  GO
  -- Grant read access privileges to all user databases
  DECLARE @name SYSNAME;
  DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
  SELECT [name]
  FROM [master].[sys].[databases]
  WHERE [name] NOT IN ('master', 'msdb', 'tempdb', 'model', 'rdsadmin', 'distribution')
  AND [state] = 0; -- Only online databases
  OPEN db_cursor;
  FETCH NEXT FROM db_cursor INTO @name;
  WHILE @@FETCH_STATUS = 0
  BEGIN
    BEGIN TRY
      PRINT 'Processing database: ' + @name;
      EXEC('USE [' + @name + '];
      IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''newrelic'')
      BEGIN
        CREATE USER [newrelic] FOR LOGIN [newrelic];
      END;
      GRANT VIEW DATABASE STATE TO [newrelic];
      GRANT VIEW DEFINITION TO [newrelic];
      ALTER ROLE db_datareader ADD MEMBER [newrelic];');
      PRINT 'Success: ' + @name;
    END TRY
    BEGIN CATCH
      PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
    END CATCH
    FETCH NEXT FROM db_cursor INTO @name;
  END
  CLOSE db_cursor;
  DEALLOCATE db_cursor;
  GO
  ```

2. To execute the script, run the following command. Replace `<Your_SA_Password>` with your SQL Server administrator password:

  ```bash
  sqlcmd -S localhost -U sa -P '<Your_SA_Password>' -C -i nr-grant-permission.sql
  ```

3. *(Optional)* To verify that the user was created successfully and has the correct permissions, run the following command. Replace `<Your_SA_Password>` with your SQL Server administrator password:

  ```bash
  sqlcmd -S localhost -U sa -P '<Your_SA_Password>' -C -Q "SELECT sp.name AS [User], p.permission_name AS [Permission_Granted] FROM sys.server_permissions p JOIN sys.server_principals sp ON p.grantee_principal_id = sp.principal_id WHERE sp.name = 'newrelic';"
  ```

  **Expected Output:**

  ```
  User Permission_Granted
  ---------------------------------------------------------------------------------------------
  <your_username> CONNECT SQL
  <your_username> VIEW ANY DEFINITION
  <your_username> VIEW ANY DATABASE
  <your_username> VIEW SERVER STATE
  ```

  This output confirms that your user has been created successfully with the required server-level permissions.

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-linux]

Select your configuration type based on your monitoring needs:

<CollapserGroup>
  <Collapser
    id="full-config-linux"
    title="NRDOT Collector with Host Metrics + Database Monitoring"
  >

This configuration includes both infrastructure host metrics and database monitoring.

Replace your `config.yaml` file at `/etc/nrdot-collector/config.yaml`:

<Callout variant="important">

If you're an existing NRDOT Collector user, ensure to merge the configuration with your existing `config.yaml` file. This includes adding the new receivers, processors, and exporters for MSSQL Database monitoring while retaining your current configurations.

</Callout>

```yaml
# NRDOT Collector Configuration for New Relic SQL Server Integration

extensions:
  health_check:

receivers:
  otlp:
    protocols:
      grpc:
      http:

  host_metrics:
    collection_interval: 60s
    scrapers:
      cpu:
        metrics:
          system.cpu.time:
            enabled: false
          system.cpu.utilization:
            enabled: true
      load:
      memory:
        metrics:
          system.memory.utilization:
            enabled: true
      paging:
        metrics:
          system.paging.utilization:
            enabled: false
          system.paging.faults:
            enabled: false
      filesystem:
        metrics:
          system.filesystem.utilization:
            enabled: true
      disk:
        metrics:
          system.disk.merged:
            enabled: false
          system.disk.pending_operations:
            enabled: false
          system.disk.weighted_io_time:
            enabled: false
      network:
        metrics:
          system.network.connections:
            enabled: false

  file_log:
    include:
      - /var/log/alternatives.log
      - /var/log/cloud-init.log
      - /var/log/auth.log
      - /var/log/dpkg.log
      - /var/log/syslog
      - /var/log/messages
      - /var/log/secure
      - /var/log/yum.log

  nrsqlserver:
    collection_interval: 15s
    username: <YOUR_USERNAME>
    password: <YOUR_PASSWORD>
    server: <YOUR_SERVER>
    port: 1433

    # Only 2 specific metrics kept enabled
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true

    # Query performance monitoring (QPM) settings
    events:
      db.server.query_sample:
        enabled: true  
      db.server.top_query:
        enabled: true  

    top_query_collection:
      lookback_time: 15s              
      max_query_sample_count: 1000    
      top_query_count: 20             
      collection_interval: 15s        

    query_sample_collection:
      max_rows_per_query: 20          

    collect_full_query_text: true
    allowed_comment_keys:
      - nr_service_guid

processors:
  metrics_transform:
    transforms:
      - include: system.cpu.utilization
        action: update
        operations:
          - action: aggregate_labels
            label_set: [ state ]
            aggregation_type: mean
      - include: system.paging.operations
        action: update
        operations:
          - action: aggregate_labels
            label_set: [ direction ]
            aggregation_type: sum

  filter/exclude_cpu_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "interrupt"'
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "nice"'
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "softirq"'

  filter/exclude_memory_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "slab_unreclaimable"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "inactive"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "cached"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "buffered"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "slab_reclaimable"'

  filter/exclude_memory_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.memory.usage" and attributes["state"] == "slab_unreclaimable"'
        - 'metric.name == "system.memory.usage" and attributes["state"] == "inactive"'

  filter/exclude_filesystem_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.utilization" and attributes["type"] == "squashfs"'

  filter/exclude_filesystem_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.usage" and attributes["type"] == "squashfs"'
        - 'metric.name == "system.filesystem.usage" and attributes["state"] == "reserved"'

  filter/exclude_filesystem_inodes_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.inodes.usage" and attributes["type"] == "squashfs"'
        - 'metric.name == "system.filesystem.inodes.usage" and attributes["state"] == "reserved"'

  filter/exclude_system_disk:
    metrics:
      datapoint:
        - 'metric.name == "system.disk.operations" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.merged" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.io" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.io_time" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.operation_time" and IsMatch(attributes["device"], "^loop.*") == true'

  filter/exclude_system_paging:
    metrics:
      datapoint:
        - 'metric.name == "system.paging.usage" and attributes["state"] == "cached"'
        - 'metric.name == "system.paging.operations" and attributes["type"] == "cached"'

  filter/exclude_network:
    metrics:
      datapoint:
        - 'IsMatch(metric.name, "^system.network.*") == true and attributes["device"] == "lo"'

  attributes/exclude_system_paging:
    include:
      match_type: strict
      metric_names:
        - system.paging.operations
    actions:
      - key: type
        action: delete

  cumulativetodelta:

  transform/host:
    metric_statements:
      - context: metric
        statements:
          - set(metric.description, "")
          - set(metric.unit, "")

  transform:
    trace_statements:
      - context: span
        statements:
          - truncate_all(span.attributes, 4095)
          - truncate_all(resource.attributes, 4095)
    log_statements:
      - context: log
        statements:
          - truncate_all(log.attributes, 4095)
          - truncate_all(resource.attributes, 4095)

  memory_limiter:
    check_interval: 1s
    limit_mib: \${env:NEW_RELIC_MEMORY_LIMIT_MIB:-100}

  batch:
    timeout: 10s
    send_batch_size: 1024

  resource_detection:
    detectors: ["system"]
    system:
      hostname_sources: ["os"]
      resource_attributes:
        host.id:
          enabled: true

  resource_detection/cloud:
    detectors: ["gcp", "ec2", "azure"]
    timeout: 2s
    override: true

  resource_detection/env:
    detectors: ["env"]
    timeout: 2s
    override: true

exporters:
  otlphttp:
    endpoint: <YOUR_OTLP_ENDPOINT>
    headers:
      api-key: <YOUR_LICENSE_KEY>
    tls:
      insecure: false
    compression: gzip

  debug:
    verbosity: detailed
    sampling_initial: 5
    sampling_thereafter: 200

service:
  telemetry:
    metrics:
      level: none

  extensions: [health_check]

  pipelines:
    metrics/host:
      receivers: [host_metrics, nrsqlserver]
      processors:
        - memory_limiter
        - metrics_transform
        - filter/exclude_cpu_utilization
        - filter/exclude_memory_utilization
        - filter/exclude_memory_usage
        - filter/exclude_filesystem_utilization
        - filter/exclude_filesystem_usage
        - filter/exclude_filesystem_inodes_usage
        - filter/exclude_system_disk
        - filter/exclude_network
        - attributes/exclude_system_paging
        - transform/host
        - resource_detection
        - resource_detection/cloud
        - resource_detection/env
        - cumulativetodelta
        - batch
      exporters: [otlphttp, debug]

    logs/host:
      receivers: [file_log, nrsqlserver]
      processors:
        - memory_limiter
        - transform
        - resource_detection
        - resource_detection/cloud
        - resource_detection/env
        - batch
      exporters: [otlphttp, debug]

    traces:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]

    metrics:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]
```

  </Collapser>

  <Collapser
    id="db-only-config-linux"
    title="Database Monitoring Only (Recommended)"
  >

This configuration focuses only on database monitoring without host infrastructure metrics.

Replace your `config.yaml` file at `/etc/nrdot-collector/config.yaml`:

```yaml
receivers:
  nrsqlserver:
    collection_interval: 15s
    # Connection details
    username: <YOUR_USERNAME>
    password: <YOUR_PASSWORD>
    server: <YOUR_SERVER>
    port: 1433

    # Enable query performance monitoring
    events:
      db.server.query_sample:
        enabled: true
      db.server.top_query:
        enabled: true

    # Top query collection
    top_query_collection:
      lookback_time: 15s
      max_query_sample_count: 1000
      top_query_count: 20
      collection_interval: 15s

    # Query sample collection
    query_sample_collection:
      max_rows_per_query: 20

    # APM correlation
    collect_full_query_text: true
    allowed_comment_keys:
      - nr_service_guid

    # Core metrics (uptime, CPU count only)
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

exporters:
  otlphttp:
    endpoint: <YOUR_OTLP_ENDPOINT>
    headers:
      api-key: <YOUR_LICENSE_KEY>
    compression: gzip

  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: none

  pipelines:
    metrics:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]
```

<Callout variant="tip">

This configuration provides database monitoring only for a smaller resource footprint. Replace the placeholder values with your actual SQL Server connection details and New Relic credentials.

</Callout>

  </Collapser>
</CollapserGroup>

#### Configuration parameters

The following table describes the key configuration parameters for the `nrsqlserver` receiver:

<table>
<thead>
  <tr>
    <th>Parameter</th>
    <th>Description</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>`<YOUR_USERNAME>`</td>
    <td>Enter your database username for authentication</td>
  </tr>
  <tr>
    <td>`<YOUR_PASSWORD>`</td>
    <td>Enter your database password for authentication</td>
  </tr>
  <tr>
    <td>`<YOUR_SERVER>`</td>
    <td>Enter your SQL Server hostname or IP address</td>
  </tr>
  <tr>
    <td>`port`</td>
    <td>Enter your SQL Server port number. The default value is 1433.</td>
  </tr>
  <tr>
    <td>`<YOUR_OTLP_ENDPOINT>`</td>
    <td>
    Enter the New Relic OTLP endpoint (e.g., https://otlp.nr-data.net). For more information, refer to [New Relic OTLP endpoints documentation](/docs/opentelemetry/best-practices/opentelemetry-otlp/).
    </td>
  </tr>
  <tr>
    <td>`<YOUR_LICENSE_KEY>`</td>
    <td>Enter your New Relic license key.</td>
  </tr>
  <tr>
    <td>`collection_interval`</td>
    <td>Interval in seconds to collect database metrics. Default: 15s</td>
  </tr>
  <tr>
    <td>`top_query_count`</td>
    <td>Number of top queries to track. Default: 20</td>
  </tr>
  <tr>
    <td>`max_rows_per_query`</td>
    <td>Maximum rows per query sample. Default: 20</td>
  </tr>
</tbody>
</table>

</Step>

<Step>

### Restart NRDOT collector [#restart-linux]

After updating your configuration, restart the NRDOT Collector service:

```bash
sudo systemctl restart nrdot-collector
```

<Callout variant="tip">

Always restart the NRDOT collector service after making configuration changes to ensure the new settings take effect.

</Callout>

</Step>

</Steps>

</TabsPageItem>

</TabsPages>
</Tabs>

</TabsPageItem>

<TabsPageItem id="rds">

Select your environment to see RDS installation steps specific to your setup:

<Tabs>
<TabsBar>
<TabsBarItem id="rds-windows-env">Windows</TabsBarItem>
<TabsBarItem id="rds-linux-env">Linux</TabsBarItem>
</TabsBar>

<TabsPages>
<TabsPageItem id="rds-windows-env">

Select your authentication method:

<Tabs>
<TabsBar>
<TabsBarItem id="rds-windows-sql-auth">SQL Authentication</TabsBarItem>
<TabsBarItem id="rds-windows-domain-auth">Windows Domain Authentication</TabsBarItem>
<TabsBarItem id="rds-windows-gmsa-auth">gMSA</TabsBarItem>
</TabsBar>

<TabsPages>
<TabsPageItem id="rds-windows-sql-auth">

<Steps>

<Step>

### Setup the NRDOT Collector [#setup-collector-rds-windows-sql]

Install the NRDOT Collector on an EC2 Windows instance or a local Windows machine that can connect to your RDS SQL Server instance.

<Callout variant="important">

For RDS monitoring, install the NRDOT Collector on an EC2 instance in the same VPC as your RDS instance for optimal network connectivity and lower latency.

</Callout>

1. Download and install the NRDOT Collector using PowerShell:

  ```powershell
  $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i nrdot-collector.msi /qn /norestart" -Wait
  ```

3. Ensure your security group allows inbound connections from the NRDOT Collector host to your RDS instance on port 1433 (or your custom port).

</Step>

<Step>

### Configure database user [#configure-user-rds-windows-sql]

Connect to your RDS SQL Server instance using SQL Server Management Studio (SSMS) or sqlcmd with your admin credentials.

Run the following script to create the `newrelic` monitoring user with appropriate permissions:

```sql
USE [master];
GO
CREATE LOGIN [newrelic] WITH PASSWORD = '<PASSWORD>';
GO
-- Instance-level permissions
GRANT VIEW SERVER STATE TO [newrelic];
GRANT VIEW ANY DEFINITION TO [newrelic];
GRANT VIEW ANY DATABASE TO [newrelic];
GO
-- Grant read access privileges to all user databases
DECLARE @name SYSNAME;
DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
SELECT [name]
FROM [master].[sys].[databases]
WHERE [name] NOT IN ('master', 'msdb', 'tempdb', 'model', 'rdsadmin', 'distribution')
AND [state] = 0; -- Only online databases
OPEN db_cursor;
FETCH NEXT FROM db_cursor INTO @name;
WHILE @@FETCH_STATUS = 0
BEGIN
  BEGIN TRY
    PRINT 'Processing database: ' + @name;
    EXEC('USE [' + @name + '];
    IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''newrelic'')
    BEGIN
      CREATE USER [newrelic] FOR LOGIN [newrelic];
    END;
    GRANT VIEW DATABASE STATE TO [newrelic];
    GRANT VIEW DEFINITION TO [newrelic];
    ALTER ROLE db_datareader ADD MEMBER [newrelic];');
    PRINT 'Success: ' + @name;
  END TRY
  BEGIN CATCH
    PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
  END CATCH
  FETCH NEXT FROM db_cursor INTO @name;
END
CLOSE db_cursor;
DEALLOCATE db_cursor;
GO
```

<Callout variant="tip">

**For RDS:** Use your RDS master username (or a user with `rds_superuser` role) to execute these commands. The script automatically excludes the `rdsadmin` database which is reserved by AWS.

</Callout>

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-rds-windows-sql]

Update your NRDOT Collector configuration file at `C:\Program Files\nrdot-collector\config.yaml`.

<Callout variant="tip">

**RDS Endpoint Format:** Your RDS endpoint will be in the format: `your-db-instance.xxxxxxxxxxxx.region.rds.amazonaws.com` (e.g., `mydb-instance.c9akciq32.us-east-1.rds.amazonaws.com`). Find this in your AWS RDS Console under "Connectivity & security".

</Callout>

**Enter your values below and see the configuration update in real-time:**

<AgentConfig
  inputOptions={[
    {
      name: 'username',
      label: 'SQL Server Username',
      codeLine: 8,
      defaultValue: 'newrelic',
      toolTip: 'Enter your SQL Server username',
    },
    {
      name: 'password',
      label: 'SQL Server Password',
      codeLine: 9,
      defaultValue: 'your_password',
      toolTip: 'Enter your SQL Server password',
    },
    {
      name: 'server',
      label: 'RDS Endpoint',
      codeLine: 10,
      defaultValue: 'mydb-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com',
      toolTip: 'Enter your RDS SQL Server endpoint',
    },
    {
      name: 'port',
      label: 'Server Port',
      codeLine: 11,
      defaultValue: '1433',
      toolTip: 'Enter your SQL Server port number (default: 1433)',
    },
    {
      name: 'endpoint',
      label: 'New Relic OTLP Endpoint',
      codeLine: 52,
      defaultValue: 'https://otlp.nr-data.net',
      toolTip: 'Enter your New Relic OTLP endpoint (e.g., https://otlp.nr-data.net or https://otlp.eu01.nr-data.net)',
    },
    {
      name: 'licenseKey',
      label: 'New Relic License Key',
      codeLine: 54,
      defaultValue: 'YOUR_LICENSE_KEY',
      toolTip: 'Enter your New Relic license key',
    },
  ]}
  config={`
receivers:
  nrsqlserver:
    collection_interval: 15s
    # Connection details
    username: <input1>
    password: <input2>
    server: <input3>
    port: <input4>

    # Enable query performance monitoring
    events:
      db.server.query_sample:
        enabled: true
      db.server.top_query:
        enabled: true

    # Top query collection
    top_query_collection:
      lookback_time: 15s
      max_query_sample_count: 1000
      top_query_count: 20
      collection_interval: 15s

    # Query sample collection
    query_sample_collection:
      max_rows_per_query: 20

    # APM correlation
    collect_full_query_text: true
    allowed_comment_keys:
      - nr_service_guid

    # Core metrics (uptime, CPU count only)
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

exporters:
  otlphttp:
    endpoint: <input5>
    headers:
      api-key: <input6>
    compression: gzip

  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: none

  pipelines:
    metrics:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]`}
  fileName="config.yaml"
  tipMdx={{ body: null }}
  onChange={() => {}}
/>

</Step>

<Step>

### Validate NRDOT Collector Configuration [#validate-rds-windows-sql]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```powershell
& "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\config.yaml"
```

</Step>

<Step>

### Restart NRDOT collector [#restart-rds-windows-sql]

After updating your configuration, restart the NRDOT Collector service on your EC2 or local Windows machine:

```powershell
net stop nrdot-collector
net start nrdot-collector
```

</Step>

</Steps>

</TabsPageItem>

<TabsPageItem id="rds-windows-domain-auth">

<Steps>

<Step>

### Enable your Microsoft SQL Server [#enable-sql-server-rds-domain]

Connect to your RDS SQL Server instance using SQL Server Management Studio (SSMS) or sqlcmd with your admin credentials.

Run the following script to create a Windows domain login and grant necessary permissions:

```sql
USE master;
CREATE LOGIN [DomainName\DomainUser] FROM WINDOWS;
GRANT CONNECT SQL TO [DomainName\DomainUser];
GRANT VIEW SERVER STATE TO [DomainName\DomainUser];
GRANT VIEW ANY DEFINITION TO [DomainName\DomainUser];
GO
```

<Callout variant="important">

Replace `DomainName\DomainUser` with your actual domain and username (e.g., `CORP\SQLMonitor`).

</Callout>

</Step>

<Step>

### Grant READ access privileges [#grant-permissions-rds-domain]

Use the following statements to grant READ access privileges to the domain user across all databases:

```sql
DECLARE @name SYSNAME
DECLARE db_cursor CURSOR
READ_ONLY FORWARD_ONLY
FOR
SELECT NAME
FROM master.sys.databases
WHERE NAME NOT IN ('master','msdb','tempdb','model','rdsadmin','distribution')
OPEN db_cursor
FETCH NEXT FROM db_cursor INTO @name 
WHILE @@FETCH_STATUS = 0
BEGIN
    EXECUTE('USE "' + @name + '"; CREATE USER [DomainName\DomainUser] FOR LOGIN [DomainName\DomainUser];' );
    FETCH NEXT FROM db_cursor INTO @name
END
CLOSE db_cursor
DEALLOCATE db_cursor
GO
```

<Callout variant="tip">

**For RDS:** The script automatically excludes the `rdsadmin` database which is reserved by AWS. Ensure you replace `DomainName\DomainUser` with your actual domain credentials.

</Callout>

</Step>

<Step>

### Setup the NRDOT Collector [#setup-collector-rds-domain]

Install the NRDOT Collector on a domain-joined EC2 Windows instance or a local Windows machine that is part of the same domain and can connect to your RDS SQL Server instance.

1. Download and install the NRDOT Collector using PowerShell:

  ```powershell
  $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i nrdot-collector.msi /qn /norestart" -Wait
  ```

3. **Change Service Logon to Domain Account (Using PowerShell):**

   Configure the NRDOT Collector service to run as your domain user using PowerShell:

   ```powershell
   # Configure service to use domain account
   # The space after obj= and password= is required syntax for sc.exe
   sc.exe config "nrdot-collector" obj= "OTEL\Admin" password= "<password>"
   
   # Restart the service
   Restart-Service nrdot-collector
   ```

   Verify the service identity:

   ```powershell
   Get-WmiObject Win32_Service -Filter "Name='nrdot-collector'" | Select Name, State, StartName
   # Expected: StartName = OTEL\Admin
   ```

   <Callout variant="important">

   Replace `OTEL\Admin` with your actual domain and username (e.g., `CORP\SQLMonitor`), and `<password>` with the domain user's password.

   </Callout>
4. Ensure your security group allows inbound connections from the NRDOT Collector host to your RDS instance on port 1433 (or your custom port).

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-rds-domain]

Update your NRDOT Collector configuration file at `C:\Program Files\nrdot-collector\config.yaml`.

<Callout variant="tip">

**RDS Endpoint Format:** Your RDS endpoint will be in the format: `your-db-instance.xxxxxxxxxxxx.region.rds.amazonaws.com`. Find this in your AWS RDS Console under "Connectivity & security".

</Callout>

<Callout variant="important">

Windows Domain Authentication uses the same configuration as gMSA (integrated security). Simply update the `datasource` server value to your RDS endpoint. The NRDOT service will authenticate using the domain account you configured in Step 3.

</Callout>

**Scroll down to the [gMSA Configuration Templates section](#gmsa-config-templates)** and use the **Database Monitoring Only** configuration.

In the `datasource` line, replace the server value with your RDS endpoint:

```yaml
datasource: "server=<YOUR-RDS-ENDPOINT>;integrated security=true;encrypt=true;TrustServerCertificate=true;"
```

Example:
```yaml
datasource: "server=mydb-instance.c9akciq32.us-east-1.rds.amazonaws.com;integrated security=true;encrypt=true;TrustServerCertificate=true;"
```

</Step>

<Step>

### Validate NRDOT Collector Configuration [#validate-rds-domain]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```powershell
& "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\config.yaml"
```

</Step>

<Step>

### Restart NRDOT collector [#restart-rds-domain]

After updating your configuration, restart the NRDOT Collector service on your domain-joined EC2 Windows instance:

```powershell
net stop nrdot-collector
net start nrdot-collector
```

<Callout variant="tip">

Verify the service is running under the domain account by checking Windows Services (services.msc). The "Log On As" column should show your domain account (e.g., `CORP\SQLMonitor`).

</Callout>

</Step>

</Steps>

</TabsPageItem>

<TabsPageItem id="rds-windows-gmsa-auth">

<Steps>

<Step>

### Enable your Microsoft SQL Server [#enable-sql-server-rds-gmsa]

Connect to your RDS SQL Server instance and run the following SQL commands to create a login for the gMSA account and grant necessary permissions:

```sql
USE master;
CREATE LOGIN [DomainName\gMSA] FROM WINDOWS;
GRANT CONNECT SQL TO [DomainName\gMSA];
GRANT VIEW SERVER STATE TO [DomainName\gMSA];
GRANT VIEW ANY DEFINITION TO [DomainName\gMSA];
```

<Callout variant="important">

Replace `DomainName\gMSA` with your actual domain name and gMSA account name (e.g., `CORP\SQL-gMSA$`). Note that gMSA accounts typically end with a `$` symbol.

</Callout>

</Step>

<Step>

### Setup the NRDOT Collector [#setup-collector-rds-gmsa]

Install the NRDOT Collector on an EC2 Windows instance that is:

<Callout variant="important">

For RDS with gMSA, the NRDOT Collector **must** be installed on a domain-joined Windows EC2 instance that has permissions to use the gMSA account. Ensure your security group allows connections to RDS on port 1433.

</Callout>

1. Download and install the NRDOT Collector using PowerShell:

  ```powershell
  $NRDOT_VERSION = (Invoke-RestMethod -Uri "https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest").tag_name; $WebClient = New-Object System.Net.WebClient; $WebClient.Headers.Add("User-Agent", "Mozilla/5.0"); $WebClient.DownloadFile("https://github.com/newrelic/nrdot-collector-releases/releases/download/$NRDOT_VERSION/nrdot-collector_${NRDOT_VERSION}_windows_x64.msi", "nrdot-collector.msi"); Start-Process msiexec.exe -ArgumentList "/i nrdot-collector.msi /qn /norestart" -Wait
  ```

</Step>

<Step>

### Configure NRDOT Collector Service to Run as gMSA [#configure-service-rds-gmsa]

After installing the NRDOT Collector, configure the service to run as your gMSA account using PowerShell:

```powershell
# Stop the NRDOT Collector service
Stop-Service nrdot-collector

# Configure service to use gMSA account
# Use cmd /c to avoid PowerShell treating $ as a variable
cmd /c 'sc config nrdot-collector obj= "OTEL\sqlotel$" password= ""'

# Verify the configuration
Get-WmiObject Win32_Service -Filter "Name='nrdot-collector'" | Select Name, StartName
```

<Callout variant="important">

Replace `OTEL\sqlotel$` with your actual domain and gMSA account name (e.g., `CORP\SQL-gMSA$`). Note the empty password - gMSA accounts don't require passwords.

</Callout>

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-rds-gmsa]

Update your NRDOT Collector configuration file at `C:\Program Files\nrdot-collector\config.yaml`.

<Callout variant="tip">

**RDS Endpoint Format:** Your RDS endpoint will be in the format: `your-db-instance.xxxxxxxxxxxx.region.rds.amazonaws.com`. Find this in your AWS RDS Console under "Connectivity & security".

</Callout>

<Callout variant="important">

The configuration uses Windows integrated security (gMSA). Simply update the `datasource` server value to your RDS endpoint.

</Callout>

**Refer to the [gMSA Configuration Templates](#gmsa-config-templates) section below** (after all installation tabs) for the complete configuration. Use the **Database Monitoring Only** configuration.

In the datasource connection string, use your full RDS endpoint:
```
datasource: "server=<YOUR-RDS-ENDPOINT>;integrated security=true;encrypt=true;TrustServerCertificate=true;"
```

Example: `server=mydb-instance.c9akciq32.us-east-1.rds.amazonaws.com`

</Step>

<Step>

### Validate NRDOT Collector Configuration [#validate-rds-gmsa]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```powershell
& "C:\Program Files\nrdot-collector\nrdot-collector.exe" validate --config="C:\Program Files\nrdot-collector\config.yaml"
```

</Step>

<Step>

### Restart NRDOT collector [#restart-rds-gmsa]

After updating your configuration, restart the NRDOT Collector service on your domain-joined EC2 Windows instance:

```powershell
net stop nrdot-collector
net start nrdot-collector
```

<Callout variant="tip">

Verify the service is running under the gMSA account by checking Windows Services (services.msc). The "Log On As" column should show your gMSA account (e.g., `CORP\SQL-gMSA$`).

</Callout>

</Step>

</Steps>

</TabsPageItem>

</TabsPages>
</Tabs>

</TabsPageItem>

<TabsPageItem id="rds-linux-env">

<Steps>

<Step>

### Setup the NRDOT Collector [#setup-collector-rds-linux]

Install the NRDOT Collector on your Linux EC2 instance or local Linux machine that can connect to your RDS SQL Server instance.

<Callout variant="important">

Install the NRDOT Collector on an EC2 instance or local machine that has network connectivity to your RDS SQL Server instance. Ensure your security group allows inbound connections on port 1433 (or your custom port).

</Callout>

1. **Debian / Ubuntu**

**For AMD64 (x86_64) Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_amd64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
  ```

**For ARM64 Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_arm64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
  ```

2. **RHEL / CentOS**

**For AMD64 (x86_64) Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_x86_64.rpm" --output nrdot-collector.rpm && sudo rpm -ivh nrdot-collector.rpm
  ```

**For ARM64 Architecture:**

  ```bash
  NRDOT_VERSION=$(curl -s https://api.github.com/repos/newrelic/nrdot-collector-releases/releases/latest | grep '"tag_name":' | awk -F'"' '{print $4}') && curl -L "https://github.com/newrelic/nrdot-collector-releases/releases/download/${NRDOT_VERSION}/nrdot-collector_${NRDOT_VERSION}_linux_arm64.deb" --output nrdot-collector.deb && sudo dpkg -i nrdot-collector.deb
  ```

</Step>

<Step>

### Configure database user [#configure-user-rds-linux]

Connect to your RDS SQL Server instance and create the `newrelic` monitoring user with appropriate permissions.

1. Create a file named `nr-grant-permission-rds.sql` on your Linux instance with the following content:

  ```sql
  USE [master];
  GO
  CREATE LOGIN [newrelic] WITH PASSWORD = '<PASSWORD>';
  GO
  -- Instance-level permissions
  GRANT VIEW SERVER STATE TO [newrelic];
  GRANT VIEW ANY DEFINITION TO [newrelic];
  GRANT VIEW ANY DATABASE TO [newrelic];
  GO
  -- Grant read access privileges to all user databases
  DECLARE @name SYSNAME;
  DECLARE db_cursor CURSOR READ_ONLY FORWARD_ONLY FOR
  SELECT [name]
  FROM [master].[sys].[databases]
  WHERE [name] NOT IN ('master', 'msdb', 'tempdb', 'model', 'rdsadmin', 'distribution')
  AND [state] = 0; -- Only online databases
  OPEN db_cursor;
  FETCH NEXT FROM db_cursor INTO @name;
  WHILE @@FETCH_STATUS = 0
  BEGIN
    BEGIN TRY
      PRINT 'Processing database: ' + @name;
      EXEC('USE [' + @name + '];
      IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = ''newrelic'')
      BEGIN
        CREATE USER [newrelic] FOR LOGIN [newrelic];
      END;
      GRANT VIEW DATABASE STATE TO [newrelic];
      GRANT VIEW DEFINITION TO [newrelic];
      ALTER ROLE db_datareader ADD MEMBER [newrelic];');
      PRINT 'Success: ' + @name;
    END TRY
    BEGIN CATCH
      PRINT 'Error on ' + @name + ': ' + ERROR_MESSAGE();
    END CATCH
    FETCH NEXT FROM db_cursor INTO @name;
  END
  CLOSE db_cursor;
  DEALLOCATE db_cursor;
  GO
  ```

2. Execute the script using your RDS master user credentials. Replace `<YOUR-RDS-ENDPOINT>` with your actual RDS endpoint and `<Your_Master_Password>` with your master password:

  ```bash
  sqlcmd -S <YOUR-RDS-ENDPOINT> -U <YOUR_MASTER_USER> -P '<Your_Master_Password>' -C -i nr-grant-permission-rds.sql
  ```

3. *(Optional)* Verify the user was created successfully:

  ```bash
  sqlcmd -S <YOUR-RDS-ENDPOINT> -U <YOUR_MASTER_USER> -P '<Your_Master_Password>' -C -Q "SELECT sp.name AS [User], p.permission_name AS [Permission_Granted] FROM sys.server_permissions p JOIN sys.server_principals sp ON p.grantee_principal_id = sp.principal_id WHERE sp.name = 'newrelic';"
  ```

<Callout variant="tip">

**For RDS:** Use your RDS master username and password to execute these commands. The script automatically excludes the `rdsadmin` database which is reserved by AWS.

</Callout>

</Step>

<Step>

### Configure NRDOT Collector [#configure-collector-rds-linux]

Replace your `config.yaml` file at `/etc/nrdot-collector/config.yaml`:

<Callout variant="important">

If you're an existing NRDOT Collector user, ensure to merge the configuration with your existing `config.yaml` file. This includes adding the new receivers, processors, and exporters for MSSQL Database monitoring while retaining your current configurations.

</Callout>

```yaml
receivers:
  nrsqlserver:
    collection_interval: 15s
    # Connection details - Use your RDS endpoint
    username: <YOUR_USERNAME>
    password: <YOUR_NEWRELIC_PASSWORD>
    server: <YOUR-RDS-ENDPOINT>
    port: 1433

    # Enable query performance monitoring
    events:
      db.server.query_sample:
        enabled: true
      db.server.top_query:
        enabled: true

    # Top query collection
    top_query_collection:
      lookback_time: 15s
      max_query_sample_count: 1000
      top_query_count: 20
      collection_interval: 15s

    # Query sample collection
    query_sample_collection:
      max_rows_per_query: 20

    # APM correlation
    collect_full_query_text: true
    allowed_comment_keys:
      - nr_service_guid

    # Core metrics (uptime, CPU count only)
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

exporters:
  otlphttp:
    endpoint: <YOUR_OTLP_ENDPOINT>
    headers:
      api-key: <YOUR_LICENSE_KEY>
    compression: gzip

  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: none

  pipelines:
    metrics:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]
```

<Callout variant="tip">

**Replace the placeholder values:**
- `<YOUR_USERNAME>`: Enter your database username for authentication
- `<YOUR-RDS-ENDPOINT>`: Your RDS SQL Server endpoint (e.g., `mydb-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com`)
- `<YOUR_NEWRELIC_PASSWORD>`: The password you set for the newrelic user
- `<YOUR_OTLP_ENDPOINT>`: Your New Relic OTLP endpoint (e.g., `https://otlp.nr-data.net`)
- `<YOUR_LICENSE_KEY>`: Your New Relic license key

</Callout>

</Step>

<Step>

### Validate NRDOT Collector Configuration [#validate-rds-linux]

Validate the NRDOT Collector configuration to ensure it's correctly formatted and will work properly.

```bash
sudo /usr/bin/nrdot-collector validate --config=/etc/nrdot-collector/config.yaml
```

</Step>

<Step>

### Restart NRDOT collector [#restart-rds-linux]

After updating your configuration, restart the NRDOT Collector service:

```bash
sudo systemctl restart nrdot-collector
```

<Callout variant="tip">

Always restart the NRDOT collector service after making configuration changes to ensure the new settings take effect.

</Callout>

</Step>

</Steps>

</TabsPageItem>

</TabsPages>
</Tabs>

</TabsPageItem>

</TabsPages>

</Tabs>

## gMSA Configuration Templates [#gmsa-config-templates]

Use these configuration templates for **both on-premises SQL Server and RDS SQL Server** when using Windows integrated security authentication.

<Callout variant="important">

**These configurations support the following authentication methods:**
- ✅ **Windows Domain Authentication** (Self-hosted and RDS)
- ✅ **gMSA (Group Managed Service Account)** (Self-hosted and RDS)

Both authentication methods use Windows integrated security (`integrated security=true`) in the connection string.

</Callout>

<Callout variant="tip">

**For On-Premises SQL Server:** Use `server=<hostname>` in the datasource (e.g., `server=SQLServer01`)

**For Amazon RDS SQL Server:** Use `server=<RDS-endpoint>` in the datasource (e.g., `server=mydb-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com`)

</Callout>

<CollapserGroup>
  <Collapser
    id="gmsa-template-full"
    title="Full Configuration (Host Metrics + Database Monitoring)"
  >

This configuration includes both infrastructure host metrics and database monitoring using gMSA authentication.

Replace your `config.yaml` file at `C:\Program Files\nrdot-collector\config.yaml`:

```yaml
# NRDOT Collector Configuration for SQL Server with gMSA Authentication
# Works for both On-Premises and RDS SQL Server

extensions:
  health_check:

receivers:
  otlp:
    protocols:
      grpc:
      http:

  host_metrics:
    collection_interval: 60s
    scrapers:
      cpu:
        metrics:
          system.cpu.time:
            enabled: false
          system.cpu.utilization:
            enabled: true
      load:
      memory:
        metrics:
          system.memory.utilization:
            enabled: true
      paging:
        metrics:
          system.paging.utilization:
            enabled: false
          system.paging.faults:
            enabled: false
      filesystem:
        metrics:
          system.filesystem.utilization:
            enabled: true
      disk:
        metrics:
          system.disk.merged:
            enabled: false
          system.disk.pending_operations:
            enabled: false
          system.disk.weighted_io_time:
            enabled: false
      network:
        metrics:
          system.network.connections:
            enabled: false

  file_log:
    include:
      - /var/log/alternatives.log
      - /var/log/cloud-init.log
      - /var/log/auth.log
      - /var/log/dpkg.log
      - /var/log/syslog
      - /var/log/messages
      - /var/log/secure
      - /var/log/yum.log

  # SQL Server Receiver with gMSA authentication
  nrsqlserver:
    collection_interval: 10s
    datasource: "server=<HOSTNAME-OR-RDS-ENDPOINT>;integrated security=true;encrypt=true;TrustServerCertificate=true;"
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true
      sqlserver.database.io:
        enabled: true
      sqlserver.memory.usage:
        enabled: true

processors:
  metrics_transform:
    transforms:
      - include: system.cpu.utilization
        action: update
        operations:
          - action: aggregate_labels
            label_set: [ state ]
            aggregation_type: mean
      - include: system.paging.operations
        action: update
        operations:
          - action: aggregate_labels
            label_set: [ direction ]
            aggregation_type: sum

  filter/exclude_cpu_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "interrupt"'
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "nice"'
        - 'metric.name == "system.cpu.utilization" and attributes["state"] == "softirq"'

  filter/exclude_memory_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "slab_unreclaimable"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "inactive"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "cached"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "buffered"'
        - 'metric.name == "system.memory.utilization" and attributes["state"] == "slab_reclaimable"'

  filter/exclude_memory_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.memory.usage" and attributes["state"] == "slab_unreclaimable"'
        - 'metric.name == "system.memory.usage" and attributes["state"] == "inactive"'

  filter/exclude_filesystem_utilization:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.utilization" and attributes["type"] == "squashfs"'

  filter/exclude_filesystem_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.usage" and attributes["type"] == "squashfs"'
        - 'metric.name == "system.filesystem.usage" and attributes["state"] == "reserved"'

  filter/exclude_filesystem_inodes_usage:
    metrics:
      datapoint:
        - 'metric.name == "system.filesystem.inodes.usage" and attributes["type"] == "squashfs"'
        - 'metric.name == "system.filesystem.inodes.usage" and attributes["state"] == "reserved"'

  filter/exclude_system_disk:
    metrics:
      datapoint:
        - 'metric.name == "system.disk.operations" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.merged" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.io" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.io_time" and IsMatch(attributes["device"], "^loop.*") == true'
        - 'metric.name == "system.disk.operation_time" and IsMatch(attributes["device"], "^loop.*") == true'

  filter/exclude_system_paging:
    metrics:
      datapoint:
        - 'metric.name == "system.paging.usage" and attributes["state"] == "cached"'
        - 'metric.name == "system.paging.operations" and attributes["type"] == "cached"'

  filter/exclude_network:
    metrics:
      datapoint:
        - 'IsMatch(metric.name, "^system.network.*") == true and attributes["device"] == "lo"'

  attributes/exclude_system_paging:
    include:
      match_type: strict
      metric_names:
        - system.paging.operations
    actions:
      - key: type
        action: delete

  cumulativetodelta:

  transform/host:
    metric_statements:
      - context: metric
        statements:
          - set(metric.description, "")
          - set(metric.unit, "")

  transform:
    trace_statements:
      - context: span
        statements:
          - truncate_all(span.attributes, 4095)
          - truncate_all(resource.attributes, 4095)
    log_statements:
      - context: log
        statements:
          - truncate_all(log.attributes, 4095)
          - truncate_all(resource.attributes, 4095)

  memory_limiter:
    check_interval: 1s
    limit_mib: \${env:NEW_RELIC_MEMORY_LIMIT_MIB:-100}

  batch:
    timeout: 10s
    send_batch_size: 1024

  resource_detection:
    detectors: ["system"]
    system:
      hostname_sources: ["os"]
      resource_attributes:
        host.id:
          enabled: true

  resource_detection/cloud:
    detectors: ["gcp", "ec2", "azure"]
    timeout: 2s
    override: true

  resource_detection/env:
    detectors: ["env"]
    timeout: 2s
    override: true

exporters:
  otlphttp:
    endpoint: ${env:OTEL_EXPORTER_OTLP_ENDPOINT:-https://otlp.nr-data.net}
    headers:
      api-key: ${env:NEW_RELIC_LICENSE_KEY}
    compression: gzip

  debug:
    verbosity: detailed

service:
  extensions: [health_check]

  pipelines:
    metrics/host:
      receivers: [host_metrics, nrsqlserver]
      processors:
        - memory_limiter
        - metrics_transform
        - filter/exclude_cpu_utilization
        - filter/exclude_memory_utilization
        - filter/exclude_memory_usage
        - filter/exclude_filesystem_utilization
        - filter/exclude_filesystem_usage
        - filter/exclude_filesystem_inodes_usage
        - filter/exclude_system_disk
        - filter/exclude_network
        - attributes/exclude_system_paging
        - transform/host
        - resource_detection
        - resource_detection/cloud
        - resource_detection/env
        - cumulativetodelta
        - batch
      exporters: [otlphttp, debug]

    logs/host:
      receivers: [file_log, nrsqlserver]
      processors:
        - memory_limiter
        - transform
        - resource_detection
        - resource_detection/cloud
        - resource_detection/env
        - batch
      exporters: [otlphttp, debug]

    traces:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]

    metrics:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [otlp]
      processors: [memory_limiter, transform, resource_detection, resource_detection/cloud, resource_detection/env, batch]
      exporters: [otlphttp, debug]
```

<Callout variant="important">

**Replace `<HOSTNAME-OR-RDS-ENDPOINT>`** in the `datasource` line:
- For on-premises: Use your server hostname (e.g., `SQLServer01`)
- For RDS: Use your full RDS endpoint (e.g., `mydb-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com`)

</Callout>

  </Collapser>

  <Collapser
    id="gmsa-template-db-only"
    title="Database Monitoring Only"
  >

This configuration focuses only on database monitoring using gMSA authentication, without host infrastructure metrics.

Replace your `config.yaml` file at `C:\Program Files\nrdot-collector\config.yaml`:

```yaml
# NRDOT Collector Configuration for SQL Server with gMSA Authentication
# Database Monitoring Only - Works for both On-Premises and RDS

receivers:
  nrsqlserver:
    collection_interval: 10s
    datasource: "server=<HOSTNAME-OR-RDS-ENDPOINT>;integrated security=true;encrypt=true;TrustServerCertificate=true;"
    metrics:
      sqlserver.computer.uptime:
        enabled: true
      sqlserver.cpu.count:
        enabled: true
      sqlserver.database.io:
        enabled: true
      sqlserver.memory.usage:
        enabled: true

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

exporters:
  otlphttp:
    endpoint: <YOUR_NEWRELIC_OTLP_ENDPOINT>
    headers:
      api-key: <YOUR_NEWRELIC_LICENSE_KEY>
    compression: gzip
  debug:
    verbosity: detailed

service:
  pipelines:
    metrics:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]

    logs:
      receivers: [nrsqlserver]
      processors: [batch]
      exporters: [otlphttp, debug]
```

<Callout variant="important">

**Replace the following values:**
- `<HOSTNAME-OR-RDS-ENDPOINT>`: Your server hostname (on-premises) or RDS endpoint
- `<YOUR_NEWRELIC_OTLP_ENDPOINT>`: Your New Relic OTLP endpoint (e.g., `https://otlp.nr-data.net`)
- `<YOUR_NEWRELIC_LICENSE_KEY>`: Your New Relic license key

</Callout>

  </Collapser>
</CollapserGroup>

### (Optional) Set up APM-database correlation [#optional-service-id]

To correlate your application performance with database operations, you can set up database service identification. This feature allows you to see exactly which applications are generating specific database workloads. For more information, refer to [set up database service identification](/docs/opentelemetry/database/database-service-identification/) to get APM-database correlation in New Relic.

<Callout variant="important">

To view database performance data in APM, both entities must be in the same New Relic account. If the entities are in different New Relic accounts, you must have access to both accounts to view the data.

</Callout>

### (Optional) Configure secret management [#rds-secret-management]

The secret management feature allows you to securely manage sensitive information, such as database credentials. This helps to enhance the security of your monitoring setup by avoiding hardcoding sensitive data in configuration files. For more information, refer to [secret management](/docs/opentelemetry/database/database-service-identification/#secret-management).

### Find and use your data [#find-use-data]

Once your data is being collected, you can access comprehensive SQL Server database monitoring through New Relic's UI. 

To find your SQL Server database entity in New Relic:

1. Go to <DNT>**[https://one.newrelic.com](https://one.newrelic.com) > All Capabilities > Databases**</DNT>.
2. Set the search criteria as <DNT>`instrumentation.provider = opentelemetry`</DNT>.
3. Select your SQL Server database from the list of entities.

## NRDOT collector supported commands [#commands]

To manage the NRDOT Collector service, view logs, or check the status of the service, use the following available commands based on your operating system.

<CollapserGroup>

  <Collapser 
    id="windows-service-management" 
    title="Windows service management"
  >

    * To restart NRDOT Collector:
      
      ```shell
      net stop nrdot-collector
      net start nrdot-collector
      ```

    * To check NRDOT Collector status:
      
      ```shell
      sc.exe query nrdot-collector
      ```

    * To view NRDOT Collector logs:

      1. Type `Event Viewer` in the search bar and select it.
      2. On the left-hand sidebar, expand the folder named Windows Logs.
      3. Click on Application to load the application-specific events into the center pane.
      4. Look for events with source type = `nrdot-collector`

    * To delete NRDOT Collector service, uninstall via `Add/Remove Programs`.

  </Collapser>

  <Collapser 
    id="linux-service-management" 
    title="Linux service management"
  >

    * To validate installation status:

      * For Debian/Ubuntu-based distributions: 

        ```bash
        dpkg -l | grep nrdot-collector
        ```
      
      * For RHEL/CentOS-based distributions:

        ```bash
        rpm -q nrdot-collector || ls /etc/nrdot-collector
        ```

    * To restart NRDOT Collector:

      ```bash
      sudo systemctl restart nrdot-collector
      ```

    * To check NRDOT Collector status:
      
        ```bash
        sudo systemctl status nrdot-collector
        ```

    * To view NRDOT Collector logs:

        ```bash
        sudo journalctl -u nrdot-collector -f
        ```

    * To delete NRDOT Collector service:

      * For Debian/Ubuntu-based distributions: 

        ```bash
        sudo apt-get purge -y nrdot-collector
        ```
      
      * For RHEL/CentOS-based distributions:

        ```bash
        sudo dnf remove -y nrdot-collector&& sudo rm -rf /etc/nrdot-collector
        ```
      

  </Collapser>

</CollapserGroup>


## Troubleshooting [#troubleshooting]

<CollapserGroup>

  <Collapser
    id="network-connectivity"
    title="Network connectivity issues"
  >

If you encounter any network connectivity issues with the NRDOT Collector, run the following commands to test connectivity to your SQL Server instance on port 1433:

* For Windows, run:

  ```shell
  Test-NetConnection -ComputerName <YOUR_IP_ADDRESS> -Port <YOUR_PORT_NUMBER>
  ```

* For Linux, run:

  ```bash
  nc -zv <YOUR_IP_ADDRESS> <YOUR_PORT_NUMBER>
  ```

</Collapser>

  <Collapser
    id="ssl"
    title="SSL connection issues"
  >

  To connect to Microsoft SQL Server using SSL without validation of the certificate, ensure that the `enable_ssl` parameter is set to `true` and the `trust_server_certificate` parameter is set to `true` in your NRDOT Collector configuration.

  ```yaml
  enable_ssl: true
  trust_server_certificate: true
  ```

  </Collapser>

  <Collapser
    id="validate-nrdot-configuration"
    title="Validate NRDOT Collector Configuration"
  >

After installing and configuring the NRDOT Collector, validate that it's working correctly:

**For Windows:**

1. **Check NRDOT Collector Service Status:**
   ```powershell
   Get-Service -Name "nrdot-collector" | Select-Object Name, Status, StartType
   ```

2. **Validate Configuration File:**
   ```powershell
   & "C:\Program Files\nrdot-collector\nrdot-collector.exe" --config="C:\Program Files\nrdot-collector\config.yaml" --dry-run
   ```

**For Linux:**

1. **Check NRDOT Collector Service Status:**
   ```bash
   sudo systemctl status nrdot-collector
   ```

2. **Validate Configuration File:**
   ```bash
   sudo /usr/local/bin/nrdot-collector --config=/etc/nrdot-collector/config.yaml --dry-run
   ```

If the validation is successful, you should see output indicating that the configuration is valid and the collector can connect to both your SQL Server instance and New Relic.

  </Collapser>

</CollapserGroup>

## Available metrics [#otel-metrics]

The following 7 metrics are collected automatically for New Relic UI functionality. These are **enabled by default** and work on any platform via a direct SQL Server connection.

<CollapserGroup>

  <Collapser
    id="default-metrics"
    title="Default metrics (non-PDH)"
  >

7 metrics that are **enabled by default**

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.batch.request.rate`</td>
      <td>Number of batch requests received by SQL Server</td>
      <td>`{requests}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.batch.sql_compilation.rate`</td>
      <td>Number of SQL compilations needed</td>
      <td>`{compilations}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.batch.sql_recompilation.rate`</td>
      <td>Number of SQL recompilations needed</td>
      <td>`{compilations}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.lock.wait.rate`</td>
      <td>Number of lock requests resulting in a wait</td>
      <td>`{requests}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.page.buffer_cache.hit_ratio`</td>
      <td>Pages found in the buffer pool without having to read from disk</td>
      <td>`%`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.page.life_expectancy`</td>
      <td>Time a page will stay in the buffer pool</td>
      <td>`s`</td>
      <td>gauge (int)</td>
      <td>`performance_counter.object_name`</td>
    </tr>
    <tr>
      <td>`sqlserver.user.connection.count`</td>
      <td>Number of users connected to the SQL Server</td>
      <td>`{connections}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

</CollapserGroup>

## Additional metrics [#additional-metrics]

The following 89 metrics are **disabled by default** but can be enabled for deeper insights into SQL Server performance and health. These metrics are organized by functionality and most require a direct SQL Server connection.

<CollapserGroup>

  <Collapser
    id="batch-compilation-metrics"
    title="Batch / Compilation metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.attention.rate`</td>
      <td>Number of SQL attentions (client cancellation interrupts) received per second</td>
      <td>`{attentions}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.batch.compilation.utilization`</td>
      <td>Number of SQL compilations per batch request</td>
      <td>`1`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.batch.page_split.utilization`</td>
      <td>Number of page splits per batch request</td>
      <td>`1`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.recompilation.ratio`</td>
      <td>Ratio of SQL recompilations to compilations, expressed as a percentage</td>
      <td>`%`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.parameterization.rate`</td>
      <td>Rate of auto-parameterization activity, broken down by result</td>
      <td>`{params}/s`</td>
      <td>gauge (double)</td>
      <td>`sqlserver.parameterization.result` (auto_attempted, safe, unsafe, failed, forced)</td>
    </tr>
    <tr>
      <td>`sqlserver.plan.execution.rate`</td>
      <td>Rate of plan executions, classified by plan guide result</td>
      <td>`{executions}/s`</td>
      <td>gauge (double)</td>
      <td>`sqlserver.plan.guidance.result` (guided, misguided)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="database-general-metrics"
    title="Database — General metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.database.backup_or_restore.rate`</td>
      <td>Total number of backups/restores</td>
      <td>`{backups_or_restores}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.database.count`</td>
      <td>The number of databases</td>
      <td>`{databases}`</td>
      <td>gauge (int)</td>
      <td>`database.status` (online, restoring, recovering, pending_recovery, suspect, offline)</td>
    </tr>
    <tr>
      <td>`sqlserver.database.execution.errors`</td>
      <td>Number of execution errors</td>
      <td>`{errors}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.database.file.size`</td>
      <td>Size of database files</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`file_type`, `db.namespace`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.full_scan.rate`</td>
      <td>The number of unrestricted full table or index scans</td>
      <td>`{scans}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.database.transactions.active`</td>
      <td>Number of active transactions in the database</td>
      <td>`{transactions}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`sqlserver.deadlock.rate`</td>
      <td>Total number of deadlocks</td>
      <td>`{deadlocks}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="database-io-latency-metrics"
    title="Database — I/O & Latency metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.database.io`</td>
      <td>The number of bytes of I/O on this file</td>
      <td>`By`</td>
      <td>sum (cumulative, int)</td>
      <td>`physical_filename`, `logical_filename`, `file_type`, `direction`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.latency`</td>
      <td>Total time that the users waited for I/O issued on this file</td>
      <td>`s`</td>
      <td>sum (cumulative, double)</td>
      <td>`physical_filename`, `logical_filename`, `file_type`, `direction`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.operations`</td>
      <td>The number of operations issued on the file</td>
      <td>`{operations}`</td>
      <td>sum (cumulative, int)</td>
      <td>`physical_filename`, `logical_filename`, `file_type`, `direction`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.page_file.size`</td>
      <td>Reserved space allocated to the database, broken down by usage state</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `page_file.state` (used, free, total)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="database-security-principals-metrics"
    title="Database — Security & Principals metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.database.principals.count`</td>
      <td>Number of database security principals broken down by type</td>
      <td>`{principals}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `principal.type` (sql_user, windows_user, role, application_role, certificate_mapped_user, asymmetric_key_mapped_user)</td>
    </tr>
    <tr>
      <td>`sqlserver.database.principals.old`</td>
      <td>Number of database principals created more than one year ago</td>
      <td>`{principals}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.principals.orphaned_users`</td>
      <td>Number of SQL users in the database that have no matching server login</td>
      <td>`{principals}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.principals.recently_created`</td>
      <td>Number of database principals created in the last 30 days</td>
      <td>`{principals}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.role.members.count`</td>
      <td>Number of database role members broken down by member kind</td>
      <td>`{members}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `member.kind` (app_role, cross_role, high_privilege, unique)</td>
    </tr>
    <tr>
      <td>`sqlserver.database.role.memberships.count`</td>
      <td>Number of database role memberships broken down by kind</td>
      <td>`{memberships}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `membership.kind` (active, custom, nested, users)</td>
    </tr>
    <tr>
      <td>`sqlserver.database.role.permission.risk_level`</td>
      <td>Risk level assigned to each database role (1=low, 4=high)</td>
      <td>`1`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `role`</td>
    </tr>
    <tr>
      <td>`sqlserver.database.role.roles.count`</td>
      <td>Number of database roles broken down by usage state</td>
      <td>`{roles}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `role.state` (empty, with_members)</td>
    </tr>
    <tr>
      <td>`sqlserver.database.security.role_membership.count`</td>
      <td>Number of members in a database role</td>
      <td>`{members}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `role`</td>
    </tr>
    <tr>
      <td>`sqlserver.server.security.principal.count`</td>
      <td>Number of security principals (logins, users) at the server level</td>
      <td>`{principals}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.server.security.role_membership.count`</td>
      <td>Number of members in a server role</td>
      <td>`{members}`</td>
      <td>gauge (int)</td>
      <td>`role`</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="failover-cluster-metrics"
    title="Failover Cluster / Always-On Availability Groups metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.failover_cluster.ag.cluster_type`</td>
      <td>Cluster type of the Always-On Availability Group</td>
      <td>`1`</td>
      <td>gauge (int)</td>
      <td>`ag.name`, `ag.cluster_type` (wsfc, external, none, unknown)</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.ag.failure_condition_level`</td>
      <td>Failure condition level configured for the Availability Group (1-5)</td>
      <td>`1`</td>
      <td>gauge (int)</td>
      <td>`ag.name`</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.ag.health_check_timeout`</td>
      <td>Health-check timeout configured for the Availability Group</td>
      <td>`ms`</td>
      <td>gauge (int)</td>
      <td>`ag.name`</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.ag.required_sync_secondaries`</td>
      <td>Number of synchronized secondary replicas required to commit on the Availability Group</td>
      <td>`{secondaries}`</td>
      <td>gauge (int)</td>
      <td>`ag.name`</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.replica.database.queue_size`</td>
      <td>Size of the log-send or redo queue for an AG database replica</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`ag.name`, `replica.server_name`, `db.namespace`, `replica.queue_kind` (log_send, redo)</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.replica.database.redo.rate`</td>
      <td>Redo rate for an AG database replica</td>
      <td>`By/s`</td>
      <td>gauge (double)</td>
      <td>`ag.name`, `replica.server_name`, `db.namespace`</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.replica.flow_control_time`</td>
      <td>Cumulative time spent in AG flow control, in milliseconds per second observed</td>
      <td>`ms`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.replica.role`</td>
      <td>Role of the availability replica</td>
      <td>`1`</td>
      <td>gauge (int)</td>
      <td>`ag.name`, `replica.server_name`, `replica.role` (primary, secondary, resolving, unknown)</td>
    </tr>
    <tr>
      <td>`sqlserver.failover_cluster.replica.synchronization_health`</td>
      <td>Synchronization health of the availability replica</td>
      <td>`1`</td>
      <td>gauge (int)</td>
      <td>`ag.name`, `replica.server_name`, `replica.sync_health` (healthy, partially_healthy, not_healthy, unknown)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="index-search-metrics"
    title="Index & Search metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.index.search.rate`</td>
      <td>Total number of index searches</td>
      <td>`{searches}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="latch-metrics"
    title="Latch metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.latch.superlatch.count`</td>
      <td>Number of superlatches currently active</td>
      <td>`{superlatch}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.latch.superlatch.transition.rate`</td>
      <td>Rate of superlatch promotions or demotions</td>
      <td>`{transition}/s`</td>
      <td>gauge (double)</td>
      <td>`transition.direction` (promotion, demotion)</td>
    </tr>
    <tr>
      <td>`sqlserver.latch.wait.rate`</td>
      <td>Number of latch waits per second</td>
      <td>`{wait}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.latch.wait_time.avg`</td>
      <td>Average time spent waiting for latches (lighter-weight synchronization)</td>
      <td>`s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.latch.wait_time.total`</td>
      <td>Total latch wait time</td>
      <td>`s`</td>
      <td>sum (cumulative, double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="locks-detailed-metrics"
    title="Locks (Detailed) metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.lock.by_mode.count`</td>
      <td>Number of currently active locks held in the database, grouped by lock mode</td>
      <td>`{locks}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `lock.mode` (shared, exclusive, update, intent, schema, bulk_update, shared_intent_exclusive)</td>
    </tr>
    <tr>
      <td>`sqlserver.lock.by_resource.count`</td>
      <td>Number of currently active locks held in the database, grouped by resource type</td>
      <td>`{locks}`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `lock.resource` (key, page, row, table, extent, file, hobt, metadata, application, allocation_unit, database_level)</td>
    </tr>
    <tr>
      <td>`sqlserver.lock.timeout.rate`</td>
      <td>Total number of lock timeouts</td>
      <td>`{timeouts}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.lock.wait.count`</td>
      <td>Cumulative count of lock waits that occurred</td>
      <td>`{wait}`</td>
      <td>sum (cumulative, int)</td>
      <td>`workload_group.name` (default, internal)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="login-logout-metrics"
    title="Login / Logout metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.login.rate`</td>
      <td>Total number of logins</td>
      <td>`{logins}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.logout.rate`</td>
      <td>Total number of logouts</td>
      <td>`{logouts}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.kill_connection.error.rate`</td>
      <td>Number of kill-connection errors per second</td>
      <td>`{errors}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="memory-metrics"
    title="Memory metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.memory.area`</td>
      <td>Amount of memory used by the SQL Server memory pool</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`memory.pool` (target, total, sql_cache, optimizer, connection, granted_workspace, max_workspace)</td>
    </tr>
    <tr>
      <td>`sqlserver.memory.cache.object.count`</td>
      <td>Number of cache objects in the SQL Server cache</td>
      <td>`{object}`</td>
      <td>gauge (int)</td>
      <td>`cache.state` (in_use, total)</td>
    </tr>
    <tr>
      <td>`sqlserver.memory.grants.pending.count`</td>
      <td>Total number of memory grants pending</td>
      <td>`{grants}`</td>
      <td>sum (cumulative, int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.memory.page.count`</td>
      <td>Number of pages in the SQL Server buffer pool</td>
      <td>`{page}`</td>
      <td>gauge (int)</td>
      <td>`page.pool` (cache, total, target, database, stolen, reserved, free)</td>
    </tr>
    <tr>
      <td>`sqlserver.memory.target`</td>
      <td>Maximum amount of memory SQL Server is willing to use (target server memory)</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.memory.usage`</td>
      <td>Total memory in use</td>
      <td>`KB`</td>
      <td>sum (cumulative, double)</td>
      <td>`workload_group.name` (default, internal)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="os-level-metrics"
    title="OS-Level metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.computer.uptime`</td>
      <td>Computer uptime</td>
      <td>`{seconds}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.cpu.count`</td>
      <td>Number of CPUs</td>
      <td>`{CPUs}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.os.disk.size`</td>
      <td>Total disk space across volumes hosting SQL Server database files</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.os.memory.usage`</td>
      <td>Amount of system physical memory observed by SQL Server</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`memory.state` (available, total)</td>
    </tr>
    <tr>
      <td>`sqlserver.os.memory.utilization`</td>
      <td>Fraction of system physical memory in use by the SQL Server process</td>
      <td>`1`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.os.scheduler.runnable_tasks.count`</td>
      <td>Total number of runnable tasks across online schedulers</td>
      <td>`{tasks}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.os.wait.duration`</td>
      <td>Total wait time for this wait type</td>
      <td>`s`</td>
      <td>sum (cumulative, double)</td>
      <td>`wait.category`, `wait.type`</td>
    </tr>
    <tr>
      <td>`sqlserver.os.wait.tasks.count`</td>
      <td>Cumulative number of tasks that have waited on this wait type since SQL Server startup</td>
      <td>`{tasks}`</td>
      <td>sum (cumulative, int)</td>
      <td>`wait.category`, `wait.type`</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="page-buffer-detailed-metrics"
    title="Page Buffer (Detailed) metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.page.buffer_cache.free_list.stalls.rate`</td>
      <td>Number of free list stalls</td>
      <td>`{stalls}/s`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.page.lookup.rate`</td>
      <td>Total number of page lookups</td>
      <td>`{lookups}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="processes-sessions-metrics"
    title="Processes / Sessions metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.process.count`</td>
      <td>Number of SQL Server processes (user sessions), broken down by status</td>
      <td>`{processes}`</td>
      <td>gauge (int)</td>
      <td>`process.status` (background, dormant, preconnect, runnable, running, sleeping, suspended)</td>
    </tr>
    <tr>
      <td>`sqlserver.processes.blocked`</td>
      <td>The number of processes that are currently blocked</td>
      <td>`{processes}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="replica-metrics"
    title="Replica metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.replica.data.rate`</td>
      <td>Throughput rate of replica data</td>
      <td>`By/s`</td>
      <td>gauge (double)</td>
      <td>`replica.direction` (transmit, receive)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="resource-pool-throttling-metrics"
    title="Resource Pool / Throttling metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.resource_pool.disk.operations`</td>
      <td>The rate of operations issued</td>
      <td>`{operations}/s`</td>
      <td>gauge (double)</td>
      <td>`direction` (read, write)</td>
    </tr>
    <tr>
      <td>`sqlserver.resource_pool.disk.throttled.read.rate`</td>
      <td>The number of read operations that were throttled in the last second</td>
      <td>`{reads}/s`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.resource_pool.disk.throttled.write.rate`</td>
      <td>The number of write operations that were throttled in the last second</td>
      <td>`{writes}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="tables-metrics"
    title="Tables metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.table.count`</td>
      <td>The number of tables</td>
      <td>`{tables}`</td>
      <td>sum (cumulative, int)</td>
      <td>`table.state` (active, inactive), `table.status` (temporary, permanent)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="tempdb-metrics"
    title="TempDB metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.database.tempdb.space`</td>
      <td>Total free space in temporary DB</td>
      <td>`KB`</td>
      <td>sum (cumulative, int)</td>
      <td>`tempdb.state` (free, used)</td>
    </tr>
    <tr>
      <td>`sqlserver.database.tempdb.version_store.size`</td>
      <td>TempDB version store size</td>
      <td>`KB`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.tempdb.allocation.wait_time.total`</td>
      <td>Cumulative wait time on tempdb allocation pages, broken down by page type</td>
      <td>`s`</td>
      <td>sum (cumulative, double)</td>
      <td>`allocation.page_type` (gam, sgam, pfs, other)</td>
    </tr>
    <tr>
      <td>`sqlserver.tempdb.contention.waiters.count`</td>
      <td>Number of tempdb pagelatch wait types with at least one active waiter</td>
      <td>`{waiters}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.tempdb.data_files.count`</td>
      <td>Number of tempdb data files configured on the instance</td>
      <td>`{files}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.tempdb.file.size`</td>
      <td>Size of each tempdb data or log file</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`file_type`, `tempdb.file.id`</td>
    </tr>
    <tr>
      <td>`sqlserver.tempdb.space.usage`</td>
      <td>Space used by tempdb, broken down by allocation category</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`tempdb.space_kind` (user_objects, internal_objects, version_store, free)</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="thread-pool-metrics"
    title="Thread Pool metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.thread_pool.tasks.count`</td>
      <td>Number of SQL Server tasks broken down by state</td>
      <td>`{tasks}`</td>
      <td>gauge (int)</td>
      <td>`task.state` (current, queued, waiting_for_threadpool)</td>
    </tr>
    <tr>
      <td>`sqlserver.thread_pool.workers.count`</td>
      <td>Number of SQL Server worker threads broken down by state</td>
      <td>`{workers}`</td>
      <td>gauge (int)</td>
      <td>`worker.state` (running, suspended_or_sleeping)</td>
    </tr>
    <tr>
      <td>`sqlserver.thread_pool.workers.max`</td>
      <td>Maximum number of SQL Server worker threads configured on the instance</td>
      <td>`{workers}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.thread_pool.workers.utilization`</td>
      <td>Fraction of configured SQL Server worker threads currently running</td>
      <td>`1`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="transactions-detailed-metrics"
    title="Transactions (Detailed) metrics"
  >

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`sqlserver.transaction.delay`</td>
      <td>Time consumed in transaction delays</td>
      <td>`ms`</td>
      <td>sum (cumulative, double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.transaction.longest_running_time`</td>
      <td>Age in seconds of the longest currently-open transaction on the server</td>
      <td>`s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.transaction.mirror_write.rate`</td>
      <td>Total number of mirror write transactions</td>
      <td>`{transactions}/s`</td>
      <td>gauge (double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.transaction.version_cleanup.rate`</td>
      <td>Cumulative bytes cleaned from the tempdb version store</td>
      <td>`By`</td>
      <td>sum (cumulative, double)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`sqlserver.transaction.version_generation.rate`</td>
      <td>Cumulative bytes of row versions written to the tempdb version store</td>
      <td>`By`</td>
      <td>sum (cumulative, double)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

  </Collapser>


</CollapserGroup>

## What's next [#whats-next]

After setting up SQL Server database monitoring with NRDOT:

* [Create custom dashboards](/docs/query-your-data/explore-query-data/dashboards/introduction-dashboards/) to visualize your database metrics
* [Set up alerts](/docs/alerts/create-alert/create-alert-condition/alert-conditions/) for critical database performance thresholds
* [Explore your data](/docs/query-your-data/explore-query-data/browse-data/introduction-data-explorer/) using New Relic's query capabilities
