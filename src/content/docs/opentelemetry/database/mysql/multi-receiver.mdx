---
title: "Multi-receiver configuration with NRDOT"
tags:
  - OpenTelemetry
  - NRDOT
  - MySQL
  - MariaDB
  - Self-hosted
  - AWS RDS
metaDescription: Set up a multi-receiver configuration with NRDOT Collector
freshnessValidatedDate: never
---

<Callout title="Preview">
  We're still working on this feature, but we'd love for you to try it out!

  This feature is currently provided as part of a preview pursuant to our [pre-release policies](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

You can configure the NRDOT Collector to monitor multiple MySQL instances from one collector. This is useful if you have multiple MySQL databases running on different hosts or ports and want to collect metrics from all of them using a single NRDOT Collector instance.

<Steps>
<Step>
## Prerequisites [#prerequisites]
- NRDOT is installed and configured for your MySQL instances:
    - [Self-hosted MySQL](/docs/opentelemetry/database/mysql/hosted)
    - [RDS MySQL monitoring](/docs/opentelemetry/database/mysql/rds)


</Step>

<Step>

## Add multi-receiver configuration [#multi]

The following example shows how to configure the NRDOT Collector to monitor two MySQL instances using a multi-receiver configuration. You can add more receivers as needed for additional MySQL instances.

```yaml
receivers:
  nrmysql/instance1: &nrmysql-common
    endpoint: "<INSTANCE_1_HOST>:<PORT>"
    username: "<YOUR_DB_USERNAME>"
    password: "<YOUR_DB_PASSWORD>"
    collection_interval: 10s
  nrmysql/instance2:
    <<: *nrmysql-common
    endpoint: "<INSTANCE_2_HOST>:<PORT>"

service:
  pipelines:
    metrics:
      receivers: [nrmysql/instance1, nrmysql/instance2]
      exporters: [otlp/newrelic]
```

</Step>

<Step>

## Restart NRDOT Collector [#restart]

Restarting is required, not optional — the collector loads its configuration once at startup and doesn't hot-reload. Any configuration change has no effect until you restart the process.

```bash
sudo systemctl restart nrdot-collector
```


</Step>

</Steps>

## Related documentation [#related]

<DocTiles>
  <DocTile title="Compatibility and prerequisites" path="/docs/opentelemetry/database/mysql/compatibility">Learn about supported MySQL versions, required grants, and network requirements.</DocTile>
  <DocTile title="Metrics reference" path="/docs/opentelemetry/database/mysql/metrics-reference">Learn about the available metrics collected by the NRDOT Collector.</DocTile>
  <DocTile title="Troubleshooting guide" path="/docs/opentelemetry/database/mysql/troubleshooting">Learn how to troubleshoot common issues with MySQL monitoring.</DocTile>
</DocTiles>