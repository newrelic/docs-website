---
title: "PostgreSQL NRDOT metrics reference"
tags:
  - OpenTelemetry
  - NRDOT
  - PostgreSQL
  - Metrics
metaDescription: Complete reference of PostgreSQL metrics collected by NRDOT Collector
freshnessValidatedDate: never
---

<Callout title="Preview">
  We're still working on this feature, but we'd love for you to try it out!

  This feature is currently provided as part of a preview pursuant to our [pre-release policies](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

Monitor your PostgreSQL database performance with comprehensive metrics collected by the NRDOT Collector. By default, New Relic collects the metrics referenced in the [Default metrics](#default) section. You can enhance your monitoring by enabling your desired metrics listed in the [Additional metrics](#additional) section.

<CollapserGroup>

  <Collapser
    id="default"
    title="Default metrics"
  >

The following 22 metrics are collected automatically for New Relic UI functionality.

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Type</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`postgresql.backends`</td>
      <td>The number of backends (connections)</td>
      <td>`1`</td>
      <td>sum (cumulative, int)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.bgwriter.buffers.allocated`</td>
      <td>Number of buffers allocated</td>
      <td>`{buffers}`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.bgwriter.buffers.writes`</td>
      <td>Number of buffers written</td>
      <td>`{buffers}`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`bg_buffer_source` (backend, backend_fsync, checkpoints, bgwriter)</td>
    </tr>
    <tr>
      <td>`postgresql.bgwriter.checkpoint.count`</td>
      <td>The number of checkpoints performed</td>
      <td>`{checkpoints}`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`bg_checkpoint_type` (requested, scheduled)</td>
    </tr>
    <tr>
      <td>`postgresql.bgwriter.duration`</td>
      <td>Total time spent writing and syncing files to disk by checkpoints</td>
      <td>`ms`</td>
      <td>sum (cumulative, double, monotonic)</td>
      <td>`bg_duration_type` (sync, write)</td>
    </tr>
    <tr>
      <td>`postgresql.bgwriter.maxwritten`</td>
      <td>Times the background writer stopped a cleaning scan for writing too many buffers</td>
      <td>`1`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.blocks_read`</td>
      <td>The number of blocks read</td>
      <td>`1`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`source`, `db.namespace`, `db.collection.name`</td>
    </tr>
    <tr>
      <td>`postgresql.commits`</td>
      <td>The number of commits</td>
      <td>`1`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.connection.max`</td>
      <td>Configured maximum number of client connections allowed</td>
      <td>`{connections}`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.database.count`</td>
      <td>Number of user databases</td>
      <td>`{databases}`</td>
      <td>sum (cumulative, int, non-monotonic)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.db_size`</td>
      <td>The database disk usage</td>
      <td>`By`</td>
      <td>sum (cumulative, int, non-monotonic)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.index.scans`</td>
      <td>The number of index scans on a table</td>
      <td>`{scans}`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`db.namespace`, `db.collection.name`, `postgresql.index.name`</td>
    </tr>
    <tr>
      <td>`postgresql.index.size`</td>
      <td>The size of the index on disk</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`db.namespace`, `db.collection.name`, `postgresql.index.name`</td>
    </tr>
    <tr>
      <td>`postgresql.operations`</td>
      <td>The number of db row operations</td>
      <td>`1`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`operation` (ins, upd, del, hot_upd), `db.namespace`, `db.collection.name`</td>
    </tr>
    <tr>
      <td>`postgresql.replication.data_delay`</td>
      <td>The amount of data delayed in replication</td>
      <td>`By`</td>
      <td>gauge (int)</td>
      <td>`replication_client`</td>
    </tr>
    <tr>
      <td>`postgresql.rollbacks`</td>
      <td>The number of rollbacks</td>
      <td>`1`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.rows`</td>
      <td>The number of rows in the database</td>
      <td>`1`</td>
      <td>sum (cumulative, int, non-monotonic)</td>
      <td>`state` (dead, live), `db.namespace`, `db.collection.name`</td>
    </tr>
    <tr>
      <td>`postgresql.table.count`</td>
      <td>Number of user tables in a database</td>
      <td>`{table}`</td>
      <td>sum (cumulative, int, non-monotonic)</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.table.size`</td>
      <td>Disk space used by a table</td>
      <td>`By`</td>
      <td>sum (cumulative, int, non-monotonic)</td>
      <td>`db.namespace`, `db.collection.name`</td>
    </tr>
    <tr>
      <td>`postgresql.table.vacuum.count`</td>
      <td>Number of times a table has manually been vacuumed</td>
      <td>`{vacuum}`</td>
      <td>sum (cumulative, int, monotonic)</td>
      <td>`db.namespace`, `db.collection.name`</td>
    </tr>
    <tr>
      <td>`postgresql.wal.age`</td>
      <td>Age of the oldest WAL file. Requires WAL enabled with at least one replica</td>
      <td>`s`</td>
      <td>gauge (int)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.wal.lag`</td>
      <td>Time between flushing recent WAL locally and receiving notification the standby completed an operation</td>
      <td>`s`</td>
      <td>gauge (int)</td>
      <td>`wal_operation_lag` (flush, replay, write), `replication_client`</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="additional"
    title="Additional metrics"
  >

The following 20 metrics are **disabled by default**. Enable them in your collector configuration as needed.

### Dashboard metrics

The out-of-the-box `INFRA-POSTGRESQLINSTANCE` dashboard uses 7 additional metrics that must be explicitly enabled:

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Dashboard widget</th>
      <th>Extra requirements</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`postgresql.database.locks`</td>
      <td>The number of database locks</td>
      <td>`{lock}`</td>
      <td>Database Locks by Mode/Type</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.deadlocks`</td>
      <td>The number of deadlocks</td>
      <td>`{deadlock}`</td>
      <td>Deadlocks</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.function.calls`</td>
      <td>Calls made to a function</td>
      <td>`{call}`</td>
      <td>Function Calls/min</td>
      <td>Requires `track_functions=pl|all` in `postgresql.conf`</td>
    </tr>
    <tr>
      <td>`postgresql.query.conflicts`</td>
      <td>Queries canceled due to recovery conflicts (standby-only; zero on primaries)</td>
      <td>`{query}`</td>
      <td>Query Conflicts</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.sequential_scans`</td>
      <td>The number of sequential scans</td>
      <td>`{sequential_scan}`</td>
      <td>Sequential Scans/min</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.temp.io`</td>
      <td>Total data written to temporary files by queries</td>
      <td>`By`</td>
      <td>Temp Files & Temp I/O (MB/min)</td>
      <td>—</td>
    </tr>
    <tr>
      <td>`postgresql.temp_files`</td>
      <td>The number of temp files</td>
      <td>`{temp_file}`</td>
      <td>Temp Files & Temp I/O (MB/min)</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

### Database activity metrics

Additional database activity metrics not used by the dashboard:

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`postgresql.blks_hit`</td>
      <td>Disk blocks found already in the buffer cache</td>
      <td>`{blks_hit}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.blks_read`</td>
      <td>Disk blocks read in this database</td>
      <td>`{blks_read}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.tup_fetched`</td>
      <td>Rows fetched by queries in the database</td>
      <td>`{tup_fetched}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.tup_returned`</td>
      <td>Rows returned by queries in the database</td>
      <td>`{tup_returned}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.tup_inserted`</td>
      <td>Rows inserted by queries in the database</td>
      <td>`{tup_inserted}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.tup_updated`</td>
      <td>Rows updated by queries in the database</td>
      <td>`{tup_updated}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.tup_deleted`</td>
      <td>Rows deleted by queries in the database</td>
      <td>`{tup_deleted}`</td>
      <td>`db.namespace`</td>
    </tr>
  </tbody>
</table>

### Advanced metrics

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Requirements</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`postgresql.wal.delay`</td>
      <td>More precise replacement for `wal.lag`, behind the `postgresqlreceiver.preciselagmetrics` feature gate</td>
      <td>`s`</td>
      <td>Feature gate enabled</td>
    </tr>
  </tbody>
</table>

### Vector metrics (pgvector extension)

Vector similarity search metrics require PostgreSQL 13+, `pg_stat_statements` extension, and `pgvector` extension:

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Description</th>
      <th>Unit</th>
      <th>Attributes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`postgresql.vector.search.calls`</td>
      <td>Vector similarity search executions, grouped by distance function</td>
      <td>`{search}`</td>
      <td>`postgresql.distance.function.name`, `db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.vector.search.duration`</td>
      <td>Cumulative execution time of vector searches</td>
      <td>`s`</td>
      <td>`postgresql.distance.function.name`, `db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.vector.search.rows_returned`</td>
      <td>Cumulative rows returned by vector searches</td>
      <td>`{rows}`</td>
      <td>`postgresql.distance.function.name`, `db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.vector.insert.rows`</td>
      <td>Vectors inserted into pgvector tables</td>
      <td>`{vectors}`</td>
      <td>`db.namespace`</td>
    </tr>
    <tr>
      <td>`postgresql.vector.insert.duration`</td>
      <td>Cumulative execution time of pgvector inserts</td>
      <td>`s`</td>
      <td>`db.namespace`</td>
    </tr>
  </tbody>
</table>

  </Collapser>

  <Collapser
    id="query-events"
    title="Query events"
  >

## `db.server.query_sample`

Real-time visibility into currently-running database statements — a point-in-time snapshot taken every scrape, not a complete execution log. Disabled by default. Requires the monitoring user to hold `pg_monitor`.

<table>
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`db.system.name`</td>
      <td>Always `postgresql`</td>
    </tr>
    <tr>
      <td>`db.namespace`</td>
      <td>The database namespace</td>
    </tr>
    <tr>
      <td>`db.query.text`</td>
      <td>The SQL statement text</td>
    </tr>
    <tr>
      <td>`db.query.comment_tags` / `.nr_service_guid`</td>
      <td>APM-correlation comment tags</td>
    </tr>
    <tr>
      <td>`db.query.text.normalized.hash`</td>
      <td>MD5 of normalized SQL for cross-engine correlation</td>
    </tr>
    <tr>
      <td>`user.name`</td>
      <td>The user logged into this backend</td>
    </tr>
    <tr>
      <td>`postgresql.state`</td>
      <td>Current overall state of the backend</td>
    </tr>
    <tr>
      <td>`postgresql.pid`</td>
      <td>Process ID of this backend</td>
    </tr>
    <tr>
      <td>`postgresql.backend_start`</td>
      <td>UTC (RFC3339) timestamp the backend/connection started</td>
    </tr>
    <tr>
      <td>`postgresql.session_duration`</td>
      <td>Whole seconds since backend start</td>
    </tr>
    <tr>
      <td>`postgresql.application_name`</td>
      <td>Name of the connected application</td>
    </tr>
    <tr>
      <td>`network.peer.address` / `network.peer.port`</td>
      <td>Client IP address / TCP port</td>
    </tr>
    <tr>
      <td>`postgresql.client_hostname`</td>
      <td>Reverse-DNS hostname of the client</td>
    </tr>
    <tr>
      <td>`postgresql.query_start`</td>
      <td>When the currently active (or last) query started</td>
    </tr>
    <tr>
      <td>`postgresql.wait_event` / `.wait_event_type`</td>
      <td>Wait event name / category, if the backend is currently waiting</td>
    </tr>
    <tr>
      <td>`postgresql.query_id`</td>
      <td>Identifier of this backend's most recent query</td>
    </tr>
    <tr>
      <td>`postgresql.total_exec_time`</td>
      <td>Query duration in delta milliseconds</td>
    </tr>
    <tr>
      <td>`postgresql.blocking.pids`</td>
      <td>Array of PIDs blocking this session. Empty when not blocked</td>
    </tr>
    <tr>
      <td>`postgresql.blocking.start_time`</td>
      <td>UTC timestamp when the current lock wait began</td>
    </tr>
    <tr>
      <td>`postgresql.blocking.wait_duration`</td>
      <td>Whole seconds waited for a lock</td>
    </tr>
    <tr>
      <td>`postgresql.blocking.lock.mode` / `.lock.type` / `.lock.relation`</td>
      <td>Lock mode / resource type / relation name being waited on</td>
    </tr>
    <tr>
      <td>`postgresql.blocking.transaction.start_time`</td>
      <td>UTC timestamp when the current transaction started</td>
    </tr>
  </tbody>
</table>

## `db.server.top_query`

Aggregated per-normalized-query execution stats from `pg_stat_statements`, reported as **incremental (delta) values** since the last collection cycle. Disabled by default. Requires the `pg_stat_statements` extension in every scanned database.

<table>
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`db.system.name`</td>
      <td>Always `postgresql`</td>
    </tr>
    <tr>
      <td>`db.namespace`</td>
      <td>The database namespace</td>
    </tr>
    <tr>
      <td>`db.query.text`</td>
      <td>The SQL statement text (normalized, `$1`/`$2` placeholders)</td>
    </tr>
    <tr>
      <td>`db.query.comment_tags` / `.nr_service_guid`</td>
      <td>APM correlation tags extracted from SQL comments</td>
    </tr>
    <tr>
      <td>`db.query.text.normalized.hash`</td>
      <td>MD5 of normalized SQL for cross-engine correlation</td>
    </tr>
    <tr>
      <td>`postgresql.calls` / `.rows`</td>
      <td>Times executed / rows retrieved-affected, delta values</td>
    </tr>
    <tr>
      <td>`postgresql.shared_blks_dirtied` / `.hit` / `.read` / `.written`</td>
      <td>Shared block activity, delta values</td>
    </tr>
    <tr>
      <td>`postgresql.temp_blks_read` / `.written`</td>
      <td>Temp block activity, delta values</td>
    </tr>
    <tr>
      <td>`postgresql.queryid`</td>
      <td>Hash identifying identical normalized queries</td>
    </tr>
    <tr>
      <td>`postgresql.rolname`</td>
      <td>Role that executed the query</td>
    </tr>
    <tr>
      <td>`postgresql.total_exec_time` / `.total_plan_time`</td>
      <td>Total execution / planning time, delta milliseconds</td>
    </tr>
    <tr>
      <td>`postgresql.query_plan`</td>
      <td>The EXPLAIN plan for the statement, if obtainable</td>
    </tr>
  </tbody>
</table>

### Execution plan collection (EXPLAIN) mechanics

* **Rate limiting:** `top_query_collection.max_explain_each_interval` (default 1000) caps EXPLAINs per scrape
* **Caching:** `query_plan_cache_size` / `query_plan_cache_ttl` (default 1000 entries / 1h) cache plans once obtained
* **PostgreSQL 12+ requirement:** Parameterized-query EXPLAIN needs `plan_cache_mode = force_generic_plan`
* **Privilege boundary:** By default, EXPLAIN runs as the monitoring user. Use `explain_function_name` for write statements without granting write access
* If a plan can't be obtained, `postgresql.query_plan` is empty for that row

  </Collapser>

</CollapserGroup>

## Configuration examples

### Enable dashboard metrics

```yaml
metrics:
  postgresql.database.locks:
    enabled: true
  postgresql.deadlocks:
    enabled: true
  postgresql.function.calls:
    enabled: true
  postgresql.query.conflicts:
    enabled: true
  postgresql.sequential_scans:
    enabled: true
  postgresql.temp.io:
    enabled: true
  postgresql.temp_files:
    enabled: true
```

### Enable query events

```yaml
events:
  db.server.query_sample:
    enabled: true
  db.server.top_query:
    enabled: true
```

### Enable vector metrics

```yaml
metrics:
  postgresql.vector.search.calls:
    enabled: true
  postgresql.vector.search.duration:
    enabled: true
  postgresql.vector.search.rows_returned:
    enabled: true
  postgresql.vector.insert.rows:
    enabled: true
  postgresql.vector.insert.duration:
    enabled: true
```

## Related documentation [#related]

<DocTiles>
  <DocTile title="Instrumentation in self-hosted environments" path="/docs/opentelemetry/database/postgresql/hosted">Learn how to set up PostgreSQL monitoring in self-hosted environments with New Relic.</DocTile>
  {/* <DocTile title="Instrumentation in RDS environments" path="/docs/opentelemetry/database/postgresql/rds">Learn how to set up PostgreSQL monitoring in RDS environments with New Relic.</DocTile> */}
  <DocTile title="Troubleshooting guide" path="/docs/opentelemetry/database/postgresql/troubleshooting">Learn how to troubleshoot common issues with PostgreSQL monitoring.</DocTile>
</DocTiles>