---
title: "Compatibility and prerequisites"
tags:
  - OpenTelemetry
  - NRDOT
  - PostgreSQL
  - Prerequisites
metaDescription: Environment compatibility and prerequisites for PostgreSQL monitoring with NRDOT Collector
freshnessValidatedDate: never
---

<Callout title="Preview">
  We're still working on this feature, but we'd love for you to try it out!

  This feature is currently provided as part of a preview pursuant to our [pre-release policies](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

Before you begin monitoring your PostgreSQL with NRDOT, ensure your environment meets these requirements.

## Prerequisites

Before you begin, ensure you have the following:

* Valid New Relic [license key](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)
* Administrative access to your PostgreSQL instance
* Network connectivity between the NRDOT Collector and your database
* PostgreSQL client tools (`psql` CLI)
* This integration is available as a part of New Relic public preview program. Check with your Organization Manager to opt in from the [Previews & Trials](https://one.newrelic.com/admin-portal/promotion-management/home) page.

## Supported database versions

The receiver detects the database version on first connection and adjusts its behavior accordingly — unsupported/older versions still work, but with reduced functionality.

<table>
  <thead>
    <tr>
      <th>PostgreSQL version</th>
      <th>Query plans on `db.server.top_query`</th>
      <th>Function call metrics</th>
      <th>Vector metrics (pgvector)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>14.x</td>
      <td>Yes (PostgreSQL 12+)</td>
      <td>Yes</td>
      <td>Yes (with pgvector extension)</td>
    </tr>
    <tr>
      <td>15.x</td>
      <td>Yes</td>
      <td>Yes</td>
      <td>Yes (with pgvector extension)</td>
    </tr>
    <tr>
      <td>16.x</td>
      <td>Yes</td>
      <td>Yes</td>
      <td>Yes (with pgvector extension)</td>
    </tr>
    <tr>
      <td>17.x</td>
      <td>Yes</td>
      <td>Yes</td>
      <td>Yes (with pgvector extension)</td>
    </tr>
    <tr>
      <td>18.x</td>
      <td>Yes</td>
      <td>Yes</td>
      <td>Yes (with pgvector extension)</td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
Version detection is non-fatal: if it fails, the receiver falls back to basic functionality rather than erroring.
</Callout>

## Required grants

### Cluster-wide grants (one-time setup)

The monitoring user needs these cluster-wide privileges:

```sql
CREATE USER otelu WITH LOGIN PASSWORD '<USER_PASSWORD>';
GRANT SELECT ON pg_stat_database TO otelu;
GRANT pg_monitor TO otelu;
```

The `pg_monitor` role (PostgreSQL 10+) grants access to `pg_read_all_stats`, `pg_read_all_settings`, and full `pg_stat_activity`/`pg_locks` visibility, required for `db.server.query_sample`.

### Per-database grants

Some grants must be repeated **in every database** you want to monitor, as PostgreSQL privileges and extensions are database-scoped:

```sql
GRANT USAGE ON SCHEMA public TO otelu;
GRANT SELECT ON ALL TABLES IN SCHEMA public TO otelu;
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;
```

For additional schemas beyond `public`:
```sql
GRANT USAGE ON SCHEMA <schema_name> TO otelu;
GRANT SELECT ON ALL TABLES IN SCHEMA <schema_name> TO otelu;
```

### Optional grants for enhanced monitoring

For pgvector metrics (requires PostgreSQL 13+ and pgvector extension):
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

## Recommended server parameters

Configure these PostgreSQL server parameters in `postgresql.conf` for optimal monitoring:

<table>
  <thead>
    <tr>
      <th>Parameter</th>
      <th>Recommended value</th>
      <th>Why</th>
      <th>Restart required</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`shared_preload_libraries`</td>
      <td>`pg_stat_statements` (**required**)</td>
      <td>Enables `pg_stat_statements` extension for top queries and vector metrics</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>`track_activity_query_size`</td>
      <td>`4096` (optional; default `1024`)</td>
      <td>Improves query sample text fidelity — PostgreSQL truncates `pg_stat_activity.query` at this limit</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>`track_functions`</td>
      <td>`pl` or `all` (optional; default `none`)</td>
      <td>Required for `postgresql.function.calls` metric and dashboard function widgets</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>`pg_stat_statements.max`</td>
      <td>`10000` (optional)</td>
      <td>Number of normalized queries tracked — increase for high-cardinality workloads</td>
      <td>No</td>
    </tr>
    <tr>
      <td>`pg_stat_statements.track`</td>
      <td>`all` (optional)</td>
      <td>Also tracks statements inside functions/procedures</td>
      <td>No</td>
    </tr>
  </tbody>
</table>

## System requirements

* System to run the NRDOT Collector (Linux, Windows, or macOS)
* Network connectivity to New Relic's OTLP endpoint
* At least 512MB RAM and 1 CPU core for the collector

## Next steps

Once you've verified your environment meets these prerequisites:

1. Choose your installation method:
  - [Self-hosted setup](/docs/opentelemetry/database/postgresql/hosted)
  - [AWS RDS setup](/docs/opentelemetry/database/postgresql/rds)

2. Review the [available metrics](/docs/opentelemetry/database/postgresql/metrics-reference) that will be collected
3. Check our [troubleshooting guide](/docs/opentelemetry/database/postgresql/troubleshooting) for common issues