---
title: Advanced configuration for Elasticsearch monitoring
metaDescription: Advanced, optional configuration options for Elasticsearch monitoring with NRDOT and the OpenTelemetry Collector Contrib.
tags:
  - OpenTelemetry
  - NRDOT
  - OTel Collector Contrib
  - Elasticsearch
  - Advanced configuration
freshnessValidatedDate: never
---

You can configure the following options to get deeper insights into your self-hosted Elasticsearch monitoring setup:

- [Secured Elasticsearch with authentication and SSL](#secured)
- [Collect host metrics](#host-metrics)
- [Collect Elasticsearch logs](#logs)
- [Add custom metadata tags](#custom-tags)

Each option modifies the same `receivers`/`processors`/`service` blocks you set up in [Install and configure NRDOT on Elasticsearch self-hosted](/docs/opentelemetry/integrations/elasticsearch/install-configure/self-hosted/nrdot) or [Install and configure the OTel Collector Contrib for Elasticsearch monitoring](/docs/opentelemetry/integrations/elasticsearch/install-configure/self-hosted/otel-collector-contrib). Pick the tab for the collector you're using below, add the snippet to your existing config file — `/etc/nrdot-collector/config.yaml` for NRDOT, or `/etc/otelcol-contrib/config.yaml` for OTel Collector Contrib — then restart that collector to apply it.

## Secured Elasticsearch with authentication and SSL [#secured]

For secured Elasticsearch with authentication and SSL, modify the receiver configuration:

<CollapserGroup>

<Collapser id="secured-nrdot" title="NRDOT">

```yaml
receivers:
  elasticsearch:
    endpoint: "https://<YOUR_ELASTICSEARCH_HOST>:9200"
    username: "<YOUR_ELASTICSEARCH_USERNAME>"
    password: "<YOUR_ELASTICSEARCH_PASSWORD>"
    tls:
      ca_file: "/etc/elasticsearch/certs/http_ca.crt"
      insecure_skip_verify: false
    collection_interval: 15s
```

</Collapser>

<Collapser id="secured-otel" title="OTel Collector Contrib">

```yaml
receivers:
  elasticsearch:
    endpoint: "https://<YOUR_ELASTICSEARCH_HOST>:9200"
    username: "<YOUR_ELASTICSEARCH_USERNAME>"
    password: "<YOUR_ELASTICSEARCH_PASSWORD>"
    tls:
      ca_file: "/etc/elasticsearch/certs/http_ca.crt"
      insecure_skip_verify: false
    collection_interval: 15s
```

</Collapser>

</CollapserGroup>

## Collect host metrics [#host-metrics]

To collect host metrics, add the `hostmetrics` receiver:

<CollapserGroup>

<Collapser id="host-metrics-nrdot" title="NRDOT">

```yaml
receivers:
  hostmetrics:
    collection_interval: 60s
    scrapers:
      cpu:
        metrics:
          system.cpu.utilization: { enabled: true }
          system.cpu.time: { enabled: true }
      load:
        metrics:
          system.cpu.load_average.1m: { enabled: true }
          system.cpu.load_average.5m: { enabled: true }
          system.cpu.load_average.15m: { enabled: true }
      memory:
        metrics:
          system.memory.usage: { enabled: true }
          system.memory.utilization: { enabled: true }
      disk:
        metrics:
          system.disk.io: { enabled: true }
          system.disk.operations: { enabled: true }
      filesystem:
        metrics:
          system.filesystem.usage: { enabled: true }
          system.filesystem.utilization: { enabled: true }
      network:
        metrics:
          system.network.io: { enabled: true }
          system.network.packets: { enabled: true }
      process:
        metrics:
          process.cpu.utilization:
            enabled: true
```

And add to the service pipelines:

```yaml
service:
  pipelines:
    metrics/host:
      receivers: [hostmetrics]
      processors: [memory_limiter, resourcedetection, batch]
      exporters: [otlphttp]
```

</Collapser>

<Collapser id="host-metrics-otel" title="OTel Collector Contrib">

```yaml
receivers:
  hostmetrics:
    collection_interval: 60s
    scrapers:
      cpu:
        metrics:
          system.cpu.utilization: { enabled: true }
          system.cpu.time: { enabled: true }
      load:
        metrics:
          system.cpu.load_average.1m: { enabled: true }
          system.cpu.load_average.5m: { enabled: true }
          system.cpu.load_average.15m: { enabled: true }
      memory:
        metrics:
          system.memory.usage: { enabled: true }
          system.memory.utilization: { enabled: true }
      disk:
        metrics:
          system.disk.io: { enabled: true }
          system.disk.operations: { enabled: true }
      filesystem:
        metrics:
          system.filesystem.usage: { enabled: true }
          system.filesystem.utilization: { enabled: true }
      network:
        metrics:
          system.network.io: { enabled: true }
          system.network.packets: { enabled: true }
      process:
        metrics:
          process.cpu.utilization:
            enabled: true
```

And add to the service pipelines:

```yaml
service:
  pipelines:
    metrics/host:
      receivers: [hostmetrics]
      processors: [memory_limiter, resourcedetection, batch]
      exporters: [otlphttp]
```

</Collapser>

</CollapserGroup>

## Collect Elasticsearch logs [#logs]

To collect Elasticsearch logs, add the `filelog` receiver.

<CollapserGroup>

<Collapser id="logs-nrdot" title="NRDOT">

Ensure the account running the collector service (`nrdot-collector`) has read access to your Elasticsearch log files.

**If running Elasticsearch on Linux (host):**

```yaml
receivers:
  filelog:
    include:
      - /var/log/elasticsearch/elasticsearch.log
      - /var/log/elasticsearch/*.log
```

**If running Elasticsearch in Docker:**

```yaml
receivers:
  filelog:
    include:
      - /var/lib/docker/containers/*/*.log
    operators:
      - type: move
        from: attributes.log
        to: body
```

And add to the service pipelines:

```yaml
service:
  pipelines:
    logs:
      receivers: [filelog]
      processors: [resource/cluster_name_override]
      exporters: [otlphttp]
```

</Collapser>

<Collapser id="logs-otel" title="OTel Collector Contrib">

Ensure the account running the collector service (`otelcol-contrib`) has read access to your Elasticsearch log files.

**If running Elasticsearch on Linux (host):**

```yaml
receivers:
  filelog:
    include:
      - /var/log/elasticsearch/elasticsearch.log
      - /var/log/elasticsearch/*.log
```

**If running Elasticsearch in Docker:**

```yaml
receivers:
  filelog:
    include:
      - /var/lib/docker/containers/*/*.log
    operators:
      - type: move
        from: attributes.log
        to: body
```

And add to the service pipelines:

```yaml
service:
  pipelines:
    logs:
      receivers: [filelog]
      processors: [resource/cluster_name_override]
      exporters: [otlphttp]
```

</Collapser>

</CollapserGroup>

## Add custom metadata tags [#custom-tags]

To add custom metadata tags to your metrics, use the `resource/static_override` processor:

<CollapserGroup>

<Collapser id="custom-tags-nrdot" title="NRDOT">

```yaml
processors:
  resource/static_override:
    attributes:
      - key: env
        value: "production"
        action: upsert
service:
  pipelines:
    metrics/elasticsearch:
      receivers: [elasticsearch]
      processors: [memory_limiter, resourcedetection, resource/cluster_name_override, resource/static_override, attributes/cardinality_reduction, cumulativetodelta, transform/metadata_nullify, batch]
      exporters: [otlphttp]
```

</Collapser>

<Collapser id="custom-tags-otel" title="OTel Collector Contrib">

```yaml
processors:
  resource/static_override:
    attributes:
      - key: env
        value: "production"
        action: upsert
service:
  pipelines:
    metrics/elasticsearch:
      receivers: [elasticsearch]
      processors: [memory_limiter, resourcedetection, resource/cluster_name_override, resource/static_override, attributes/cardinality_reduction, cumulativetodelta, transform/metadata_nullify, batch]
      exporters: [otlphttp]
```

</Collapser>

</CollapserGroup>

## Related articles [#related-articles]

- [Install and configure NRDOT on Elasticsearch self-hosted](/docs/opentelemetry/integrations/elasticsearch/install-configure/self-hosted/nrdot)
- [Install and configure the OTel Collector Contrib for Elasticsearch monitoring](/docs/opentelemetry/integrations/elasticsearch/install-configure/self-hosted/otel-collector-contrib)
- [Elasticsearch metrics reference](/docs/opentelemetry/integrations/elasticsearch/metrics-reference)
- [Elasticsearch troubleshooting](/docs/opentelemetry/integrations/elasticsearch/troubleshooting)
