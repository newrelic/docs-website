---
title: 'Monitor Linkerd on Kubernetes with OpenTelemetry'
metaDescription: 'Step-by-step guide to send Linkerd service mesh metrics, traces, and logs to New Relic using the OpenTelemetry Collector.'
redirects:
  - /docs/opentelemetry/integrations/linkerd/linkerd-otel-kubernetes
freshnessValidatedDate: never
---

Get complete visibility into your Linkerd service mesh with OpenTelemetry, all sent to New Relic.

You can choose between two collector options:
* **NRDOT:** New Relic's supported distribution of the OpenTelemetry Collector, installed via Helm.
* **OTel Collector Contrib:** Standard OpenTelemetry Collector with community-contributed components.

This integration scrapes Linkerd proxy and control plane metrics via the OpenTelemetry Collector's Prometheus receiver, correlating request rate, latency, error rate, and mTLS status for every meshed service without instrumenting the services themselves. It can also collect `linkerd-proxy` sidecar logs, giving you a full picture of mesh traffic.

<Tabs>
  <TabsBar>
    <TabsBarItem id="nrdot-k8s-prereqs">
      NRDOT collector
    </TabsBarItem>
    <TabsBarItem id="otelcol-k8s-prereqs">
      OpenTelemetry Collector Contrib
    </TabsBarItem>
  </TabsBar>

  <TabsPages>
    <TabsPageItem id="nrdot-k8s-prereqs">

## Before you begin [#prerequisites-nrdot]

Ensure you have:

- A running Kubernetes cluster (EKS, GKE, AKS, self-managed) with `kubectl` configured against it
- Linkerd installed and healthy in your cluster (`linkerd check` passes). If you plan to enable distributed tracing below, use Linkerd edge-26.7.0 or later. See the [Linkerd getting started guide](https://linkerd.io/2/getting-started/) if needed
- Injection enabled on the namespaces you want to observe. If not, annotate and restart them:

    ```bash
    kubectl annotate namespace <YOUR_NAMESPACE> linkerd.io/inject=enabled
    kubectl rollout restart deployment -n <YOUR_NAMESPACE>
    ```

- New Relic [ingest license key](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)
- For manifest install: complete the base [Kubernetes OpenTelemetry manifest installation](/docs/kubernetes-pixie/k8s-otel/install/#manifest-install)
- For helm install: complete the base [Kubernetes OpenTelemetry helm installation](/docs/kubernetes-pixie/k8s-otel/install/#helm-install)

## Configure the NRDOT collector [#install-nrdot-collector]

The chart bundles `kube-state-metrics` and enables it by default, so Kubernetes workload entities (deployments, pods) are already correlated with Linkerd metrics with no extra setup.

<CollapserGroup>

  <Collapser id="nrdot-k8s-helm-install" title="Helm install">

Add the following sections under [`deployment.configMap.extraConfig`](https://github.com/newrelic/helm-charts/blob/c6dbb791072d4f11cbd712e84d13cf997d78eddc/charts/nr-k8s-otel-collector/values.yaml#L259) in your [`values.yaml`](https://github.com/newrelic/helm-charts/blob/master/charts/nr-k8s-otel-collector/values.yaml):

<Callout variant="important">
  `pipelines:` must be defined at the root of `extraConfig` - not nested under `service:`. The Helm chart template maps `extraConfig.pipelines` into `service.pipelines`.
</Callout>

Set your cluster name:

```yaml
deployment:
  envs:
    - name: OTEL_RESOURCE_ATTRIBUTES
      value: "k8s.cluster.name=<YOUR_CLUSTER_NAME>"
```

Receivers:

```yaml
receivers:
  prometheus:
    config:
      scrape_configs:
        - job_name: 'linkerd-controller'
          kubernetes_sd_configs:
            - role: pod
              namespaces: { names: ['linkerd', 'linkerd-viz'] }
          relabel_configs:
            - { source_labels: [__meta_kubernetes_pod_container_port_name], action: keep, regex: admin-http }
            - { source_labels: [__meta_kubernetes_pod_container_name], target_label: component }
            - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
            - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
        - job_name: 'linkerd-proxy'
          kubernetes_sd_configs: [{ role: pod }]
          relabel_configs:
            - { source_labels: [__meta_kubernetes_pod_container_name, __meta_kubernetes_pod_container_port_name, __meta_kubernetes_pod_label_linkerd_io_control_plane_ns], action: keep, regex: ^linkerd-proxy;linkerd-admin;linkerd$$ }
            - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
            - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
            - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_ns], target_label: linkerd_control_plane_ns }
            - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_component], target_label: linkerd_control_plane_component }
```

Processors:

```yaml
processors:
  resource/strip_service:
    attributes:
      - { key: service.name, action: delete }
      - { key: service.instance.id, action: delete }
  transform/k8s:
    metric_statements:
      - context: datapoint
        statements:
          - set(attributes["k8s.namespace.name"], attributes["namespace"]) where attributes["namespace"] != nil
          - set(attributes["k8s.pod.name"], attributes["pod"]) where attributes["pod"] != nil
          - delete_key(attributes, "instance")
  transform/deployment:
    metric_statements:
      - context: datapoint
        statements:
          - set(attributes["k8s.deployment.name"], attributes["k8s.pod.name"]) where attributes["k8s.deployment.name"] == nil and attributes["k8s.pod.name"] != nil
          - replace_pattern(attributes["k8s.deployment.name"], "-[a-z0-9]+-[a-z0-9]+$", "") where attributes["k8s.deployment.name"] == attributes["k8s.pod.name"]
  transform/metadata_nullify:
    metric_statements:
      - context: metric
        statements:
          - set(description, "")
          - set(unit, "")
  filter/drop_unused:
    error_mode: ignore
    metrics:
      metric:
        - 'name == "rustls_info" or name == "proxy_build_info"'
        - 'name == "scrape_series_added"'
        - 'IsMatch(name, "stack_(poll|create|drop)_total") or name == "stack_poll_total_ms"'
        - 'IsMatch(name, "tokio_rt_.*")'
        - 'IsMatch(name, "(inbound|outbound)_http_.*_frame_size_bytes")'
        - 'IsMatch(name, "(inbound|outbound)_tcp_detect_http_duration_seconds")'
        - 'IsMatch(name, "outbound_tcp_balancer_queue_.*")'
        - 'name == "scrape_duration_seconds" or name == "scrape_samples_scraped" or name == "scrape_samples_post_metric_relabeling"'
```

Pipelines:

```yaml
pipelines:
  metrics/linkerd:
    receivers: [prometheus]
    processors: [memory_limiter, resource/strip_service, transform/k8s, transform/deployment, transform/metadata_nullify, filter/drop_unused, resource/newrelic, batch]
    exporters: [otlp_http/newrelic]
```

#### Configuration parameters

The following table describes the key configuration parameters:

<table>
<thead>
  <tr>
    <th>Parameter</th>
    <th>Description</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>`OTEL_RESOURCE_ATTRIBUTES`</td>
    <td>Set `k8s.cluster.name` to your Kubernetes cluster name for identification in New Relic</td>
  </tr>
  <tr>
    <td>`filter/drop_unused`</td>
    <td>Drops low-value Linkerd proxy metrics by default. Remove entries from this list to re-enable them</td>
  </tr>
</tbody>
</table>

Apply the updated values to your running Helm release:

```bash
helm upgrade nr-k8s-otel-collector newrelic/nr-k8s-otel-collector \
  --namespace newrelic --reuse-values -f values.yaml
```

  </Collapser>

  <Collapser id="nrdot-k8s-manifest-install" title="Manifest install">

Complete the base [Kubernetes OpenTelemetry manifest installation](/docs/kubernetes-pixie/k8s-otel/install/#manifest-install), then add the Linkerd scrape config to the `receivers.prometheus.config.scrape_configs` list in your local [`deployment-configmap.yaml`](https://github.com/newrelic/helm-charts/blob/master/charts/nr-k8s-otel-collector/examples/k8s/rendered/deployment-configmap.yaml):

```yaml
            - job_name: 'linkerd-controller'
              kubernetes_sd_configs:
                - role: pod
                  namespaces: { names: ['linkerd', 'linkerd-viz'] }
              relabel_configs:
                - { source_labels: [__meta_kubernetes_pod_container_port_name], action: keep, regex: admin-http }
                - { source_labels: [__meta_kubernetes_pod_container_name], target_label: component }
                - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
                - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
            - job_name: 'linkerd-proxy'
              kubernetes_sd_configs: [{ role: pod }]
              relabel_configs:
                - { source_labels: [__meta_kubernetes_pod_container_name, __meta_kubernetes_pod_container_port_name, __meta_kubernetes_pod_label_linkerd_io_control_plane_ns], action: keep, regex: ^linkerd-proxy;linkerd-admin;linkerd$ }
                - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
                - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
                - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_ns], target_label: linkerd_control_plane_ns }
                - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_component], target_label: linkerd_control_plane_component }
```

Then add the following to the same [`deployment-configmap.yaml`](https://github.com/newrelic/helm-charts/blob/master/charts/nr-k8s-otel-collector/examples/k8s/rendered/deployment-configmap.yaml):

Processors to add:

```yaml
processors:
  resource/strip_service:
    attributes:
      - { key: service.name, action: delete }
      - { key: service.instance.id, action: delete }
  transform/k8s:
    metric_statements:
      - context: datapoint
        statements:
          - set(attributes["k8s.namespace.name"], attributes["namespace"]) where attributes["namespace"] != nil
          - set(attributes["k8s.pod.name"], attributes["pod"]) where attributes["pod"] != nil
          - delete_key(attributes, "instance")
  transform/deployment:
    metric_statements:
      - context: datapoint
        statements:
          - set(attributes["k8s.deployment.name"], attributes["k8s.pod.name"]) where attributes["k8s.deployment.name"] == nil and attributes["k8s.pod.name"] != nil
          - replace_pattern(attributes["k8s.deployment.name"], "-[a-z0-9]+-[a-z0-9]+$", "") where attributes["k8s.deployment.name"] == attributes["k8s.pod.name"]
  transform/metadata_nullify:
    metric_statements:
      - context: metric
        statements:
          - set(description, "")
          - set(unit, "")
  filter/drop_unused:
    error_mode: ignore
    metrics:
      metric:
        - 'name == "rustls_info" or name == "proxy_build_info"'
        - 'name == "scrape_series_added"'
        - 'IsMatch(name, "stack_(poll|create|drop)_total") or name == "stack_poll_total_ms"'
        - 'IsMatch(name, "tokio_rt_.*")'
        - 'IsMatch(name, "(inbound|outbound)_http_.*_frame_size_bytes")'
        - 'IsMatch(name, "(inbound|outbound)_tcp_detect_http_duration_seconds")'
        - 'IsMatch(name, "outbound_tcp_balancer_queue_.*")'
        - 'name == "scrape_duration_seconds" or name == "scrape_samples_scraped" or name == "scrape_samples_post_metric_relabeling"'
```

Pipelines to add:

```yaml
pipelines:
  metrics/linkerd:
    receivers: [prometheus]
    processors: [memory_limiter, resource/strip_service, transform/k8s, transform/deployment, transform/metadata_nullify, filter/drop_unused, resource/newrelic, batch]
    exporters: [otlp_http/newrelic]
```

Re-apply:

```bash
kubectl apply -f rendered/deployment-configmap.yaml -n newrelic
kubectl rollout restart deployment -n newrelic
```

  </Collapser>

</CollapserGroup>

    </TabsPageItem>

    <TabsPageItem id="otelcol-k8s-prereqs">

## Before you begin [#prerequisites-otel]

Ensure you have:

- A running Kubernetes cluster (EKS, GKE, AKS, self-managed) with `kubectl` configured against it
- Linkerd installed and healthy in your cluster (`linkerd check` passes). If you plan to enable distributed tracing below, use Linkerd edge-26.7.0 or later. See the [Linkerd getting started guide](https://linkerd.io/2/getting-started/) if needed
- Injection enabled on the namespaces you want to observe. If not, annotate and restart them:

    ```bash
    kubectl annotate namespace <YOUR_NAMESPACE> linkerd.io/inject=enabled
    kubectl rollout restart deployment -n <YOUR_NAMESPACE>
    ```

- (Optional) [kube-state-metrics](https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-state-metrics) running in your cluster, if you want Kubernetes workload entities correlated with Linkerd metrics. If you already have the [New Relic Kubernetes integration](/docs/kubernetes-pixie/kubernetes-integration/get-started/introduction-kubernetes-integration/) installed, this is already handled
- New Relic [ingest license key](/docs/apis/intro-apis/new-relic-api-keys/#ingest-license-key)
- [Helm](https://helm.sh/docs/intro/install/) installed and configured against your cluster

## Configure the OpenTelemetry Collector [#install-otelcol-collector]

<CollapserGroup>

  <Collapser id="otelcol-k8s-helm-install" title="Helm install">

### Add the OpenTelemetry Helm repo

```bash
helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
helm repo update
```

### Create `values.yaml` and install the chart

Download or create a custom `values.yaml` file based on the [OpenTelemetry Collector values.yaml](https://github.com/open-telemetry/opentelemetry-helm-charts/blob/main/charts/opentelemetry-collector/values.yaml).

Update the following sections in your `values.yaml` file:

1. Set mode to deployment:

```yaml
mode: deployment
```

2. Replace the image repository:

```yaml
image:
  repository: otel/opentelemetry-collector-contrib
```

3. Configure the cluster role:

```yaml
clusterRole:
  create: true
  rules:
    - apiGroups: [""]
      resources: [pods, nodes, nodes/proxy, endpoints, services, namespaces]
      verbs: [get, list, watch]
    - apiGroups: ["apps"]
      resources: [replicasets, deployments, daemonsets, statefulsets]
      verbs: [get, list, watch]
```

4. Set your cluster name and New Relic ingest license key:

```yaml
extraEnvs:
  - name: K8S_CLUSTER_NAME
    value: "<YOUR_CLUSTER_NAME>"
  - name: NEWRELIC_LICENSE_KEY
    value: "<YOUR_NR_INGEST_LICENSE_KEY>"
```

5. Replace the entire `config` section with the Linkerd monitoring configuration:

```yaml
config:
  receivers:
    prometheus:
      config:
        scrape_configs:
          - job_name: 'linkerd-controller'
            kubernetes_sd_configs:
              - role: pod
                namespaces: { names: ['linkerd', 'linkerd-viz'] }
            relabel_configs:
              - { source_labels: [__meta_kubernetes_pod_container_port_name], action: keep, regex: admin-http }
              - { source_labels: [__meta_kubernetes_pod_container_name], target_label: component }
              - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
              - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
          - job_name: 'linkerd-proxy'
            kubernetes_sd_configs: [{ role: pod }]
            relabel_configs:
              - { source_labels: [__meta_kubernetes_pod_container_name, __meta_kubernetes_pod_container_port_name, __meta_kubernetes_pod_label_linkerd_io_control_plane_ns], action: keep, regex: ^linkerd-proxy;linkerd-admin;linkerd$ }
              - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
              - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
              - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_ns], target_label: linkerd_control_plane_ns }
              - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_component], target_label: linkerd_control_plane_component }

  processors:
    resource/strip_service:
      attributes:
        - { key: service.name, action: delete }
        - { key: service.instance.id, action: delete }
    resource/cluster:
      attributes:
        - key: k8s.cluster.name
          value: "${env:K8S_CLUSTER_NAME}"
          action: insert
    transform/k8s:
      metric_statements:
        - context: datapoint
          statements:
            - set(attributes["k8s.namespace.name"], attributes["namespace"]) where attributes["namespace"] != nil
            - set(attributes["k8s.pod.name"], attributes["pod"]) where attributes["pod"] != nil
            - delete_key(attributes, "instance")
    transform/deployment:
      metric_statements:
        - context: datapoint
          statements:
            - set(attributes["k8s.deployment.name"], attributes["k8s.pod.name"]) where attributes["k8s.deployment.name"] == nil and attributes["k8s.pod.name"] != nil
            - replace_pattern(attributes["k8s.deployment.name"], "-[a-z0-9]+-[a-z0-9]+$", "") where attributes["k8s.deployment.name"] == attributes["k8s.pod.name"]
    transform/metadata_nullify:
      metric_statements:
        - context: metric
          statements:
            - set(description, "")
            - set(unit, "")

  exporters:
    otlp_http/newrelic:
      endpoint: "https://otlp.nr-data.net:4318"
      headers:
        api-key: "${env:NEWRELIC_LICENSE_KEY}"

  service:
    pipelines:
      metrics:
        receivers: [prometheus]
        processors: [memory_limiter, resource/strip_service, resource/cluster, transform/k8s, transform/deployment, transform/metadata_nullify, batch]
        exporters: [otlp_http/newrelic]
```

#### Configuration parameters

The following table describes the key configuration parameters:

<table>
<thead>
  <tr>
    <th>Parameter</th>
    <th>Description</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>`<YOUR_CLUSTER_NAME>`</td>
    <td>Replace with your Kubernetes cluster name for identification in New Relic</td>
  </tr>
  <tr>
    <td>`K8S_CLUSTER_NAME` / `NEWRELIC_LICENSE_KEY`</td>
    <td>Set via `extraEnvs` - your cluster name and New Relic ingest license key</td>
  </tr>
  <tr>
    <td>`filter/drop_unused`</td>
    <td>Not included by default in this chart's config - see the NRDOT tab if you want the same low-value-metric filtering</td>
  </tr>
</tbody>
</table>

```bash
helm upgrade my-opentelemetry-collector open-telemetry/opentelemetry-collector \
  -f values.yaml -n newrelic --create-namespace --install
```

<Callout variant="tip">
  Want Linkerd proxy logs? That's an optional add-on - see [Collect Linkerd proxy logs](#collect-logs) below. For distributed tracing, see [Linkerd distributed tracing with OpenTelemetry](/docs/opentelemetry/integrations/linkerd/linkerd-otel-distributed-tracing/).
</Callout>

  </Collapser>

  <Collapser id="otelcol-k8s-manifest-install" title="Manifest install">

Copy the manifest below into a file named `otel-collector.yaml`. Set the placeholders below, then apply:

**Customise before applying:**

| Field | Location | What to set |
|---|---|---|
| `resource/cluster` | processor | Kubernetes cluster name |
| `otlp_http/newrelic` | exporter | New Relic ingest license key |
| `memory_limiter` | processor | Memory limit/spike percentages, e.g. `80`/`25` |
| `resources.limits` | Deployment container | CPU/memory limits for the collector pod |

```yaml
apiVersion: v1
kind: ServiceAccount
metadata: { name: my-opentelemetry-collector, namespace: newrelic }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata: { name: my-opentelemetry-collector }
rules:
  - apiGroups: [""]
    resources: [pods, nodes, nodes/proxy, endpoints, services, namespaces]
    verbs: [get, list, watch]
  - apiGroups: ["apps"]
    resources: [replicasets, deployments, daemonsets, statefulsets]
    verbs: [get, list, watch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata: { name: my-opentelemetry-collector }
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: ClusterRole, name: my-opentelemetry-collector }
subjects:
  - { kind: ServiceAccount, name: my-opentelemetry-collector, namespace: newrelic }
---
apiVersion: v1
kind: ConfigMap
metadata: { name: my-opentelemetry-collector-config, namespace: newrelic }
data:
  config: |
    receivers:
      prometheus:
        config:
          scrape_configs:
            - job_name: 'linkerd-controller'
              kubernetes_sd_configs:
                - role: pod
                  namespaces: { names: ['linkerd', 'linkerd-viz'] }
              relabel_configs:
                - { source_labels: [__meta_kubernetes_pod_container_port_name], action: keep, regex: admin-http }
                - { source_labels: [__meta_kubernetes_pod_container_name], target_label: component }
                - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
                - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
            - job_name: 'linkerd-proxy'
              kubernetes_sd_configs: [{ role: pod }]
              relabel_configs:
                - { source_labels: [__meta_kubernetes_pod_container_name, __meta_kubernetes_pod_container_port_name, __meta_kubernetes_pod_label_linkerd_io_control_plane_ns], action: keep, regex: ^linkerd-proxy;linkerd-admin;linkerd$ }
                - { source_labels: [__meta_kubernetes_namespace], target_label: namespace }
                - { source_labels: [__meta_kubernetes_pod_name], target_label: pod }
                - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_ns], target_label: linkerd_control_plane_ns }
                - { source_labels: [__meta_kubernetes_pod_label_linkerd_io_control_plane_component], target_label: linkerd_control_plane_component }

    processors:
      memory_limiter:
        check_interval: 1s
        limit_percentage: <MEMORY_LIMIT_PERCENTAGE>
        spike_limit_percentage: <MEMORY_SPIKE_LIMIT_PERCENTAGE>
      resource/strip_service:
        attributes:
          - { key: service.name, action: delete }
          - { key: service.instance.id, action: delete }
      resource/cluster:
        attributes:
          - key: k8s.cluster.name
            value: "<YOUR_CLUSTER_NAME>"
            action: insert
      transform/k8s:
        metric_statements:
          - context: datapoint
            statements:
              - set(attributes["k8s.namespace.name"], attributes["namespace"]) where attributes["namespace"] != nil
              - set(attributes["k8s.pod.name"], attributes["pod"]) where attributes["pod"] != nil
              - delete_key(attributes, "instance")
      transform/deployment:
        metric_statements:
          - context: datapoint
            statements:
              - set(attributes["k8s.deployment.name"], attributes["k8s.pod.name"]) where attributes["k8s.deployment.name"] == nil and attributes["k8s.pod.name"] != nil
              - replace_pattern(attributes["k8s.deployment.name"], "-[a-z0-9]+-[a-z0-9]+$", "") where attributes["k8s.deployment.name"] == attributes["k8s.pod.name"]
      transform/metadata_nullify:
        metric_statements:
          - context: metric
            statements:
              - set(description, "")
              - set(unit, "")
      batch: {}

    exporters:
      otlp_http/newrelic:
        endpoint: "https://otlp.nr-data.net:4318"
        headers:
          api-key: "<YOUR_NR_INGEST_LICENSE_KEY>"

    service:
      pipelines:
        metrics:
          receivers: [prometheus]
          processors: [memory_limiter, resource/strip_service, resource/cluster, transform/k8s, transform/deployment, transform/metadata_nullify, batch]
          exporters: [otlp_http/newrelic]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: my-opentelemetry-collector, namespace: newrelic }
spec:
  replicas: 1
  selector: { matchLabels: { app: my-opentelemetry-collector } }
  template:
    metadata:
      labels: { app: my-opentelemetry-collector }
    spec:
      serviceAccountName: my-opentelemetry-collector
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: opentelemetry-collector
          image: otel/opentelemetry-collector-contrib:0.160.0
          args: ["--config=/conf/config.yaml"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          resources:
            limits: { cpu: <CPU_LIMIT>, memory: <MEMORY_LIMIT> }
          volumeMounts:
            - { name: config, mountPath: /conf }
      volumes:
        - name: config
          configMap:
            name: my-opentelemetry-collector-config
            items:
              - { key: config, path: config.yaml }
---
apiVersion: v1
kind: Service
metadata: { name: my-opentelemetry-collector, namespace: newrelic }
spec:
  selector: { app: my-opentelemetry-collector }
  ports:
    - { name: otlp, port: 4317, targetPort: 4317, protocol: TCP, appProtocol: grpc }
    - { name: otlp-http, port: 4318, targetPort: 4318, protocol: TCP }
```

```bash
kubectl apply -f otel-collector.yaml
kubectl rollout status deployment/my-opentelemetry-collector -n newrelic
```

For distributed tracing, see [Linkerd distributed tracing with OpenTelemetry](/docs/opentelemetry/integrations/linkerd/linkerd-otel-distributed-tracing/).

  </Collapser>

</CollapserGroup>

    </TabsPageItem>
  </TabsPages>
</Tabs>

## View your data in New Relic [#find-data]

Once your setup is complete and data is flowing (usually within a few minutes), you can access your Linkerd metrics, traces, and logs in New Relic dashboards, run NRQL queries, and create alerts.

For complete instructions on accessing the built-in dashboard, querying data with NRQL, and troubleshooting data visibility, see [Find and query your Linkerd data](/docs/opentelemetry/integrations/linkerd/find-and-query-your-data/).

## Optional: Collect Linkerd proxy logs [#collect-logs]

This adds the `linkerd-proxy` sidecar container's own logs to New Relic - separate from the metrics and traces collected above. It's scoped to just the proxy container, not your application's logs.

<CollapserGroup>

  <Collapser id="collect-logs-nrdot" title="NRDOT collector">

The NRDOT chart ships a `filelog` receiver enabled by default (`receivers.filelog.enabled: true`) that already collects every container's logs, including `linkerd-proxy`, from `/var/log/pods` on the node. Nothing to add to `values.yaml`.

  </Collapser>

  <Collapser id="collect-logs-otelcol" title="OpenTelemetry Collector Contrib">

Install a **second** Helm release in `mode: daemonset`. Save the following as `filelog-values.yaml`. Set `<YOUR_CLUSTER_NAME>` and `<YOUR_NR_INGEST_LICENSE_KEY>`, then install:

**Customise before applying:**

| Field | Location | What to set |
|---|---|---|
| `extraEnvs` | `K8S_CLUSTER_NAME` | Your Kubernetes cluster name |
| `extraEnvs` | `NEWRELIC_LICENSE_KEY` | Your New Relic ingest license key |
| `filter/onlyproxy` | processor | Drops every log record except the `linkerd-proxy` container's |
| `resource/cluster` | processor | Attaches `k8s.cluster.name` to every log record |

```yaml
mode: daemonset
image:
  repository: otel/opentelemetry-collector-contrib

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: [ALL]

extraVolumes:
  - name: varlogpods
    hostPath:
      path: /var/log/pods
extraVolumeMounts:
  - name: varlogpods
    mountPath: /var/log/pods
    readOnly: true

extraEnvs:
  - name: K8S_CLUSTER_NAME
    value: "<YOUR_CLUSTER_NAME>"
  - name: NEWRELIC_LICENSE_KEY
    value: "<YOUR_NR_INGEST_LICENSE_KEY>"

config:
  receivers:
    filelog:
      include: [/var/log/pods/*/*/*.log]
      include_file_path: true
      start_at: end
      operators:
        - type: container
          add_metadata_from_filepath: true

  processors:
    filter/onlyproxy:
      error_mode: ignore
      logs:
        log_record:
          - 'resource.attributes["k8s.container.name"] != "linkerd-proxy"'
    resource/cluster:
      attributes:
        - key: k8s.cluster.name
          value: "${env:K8S_CLUSTER_NAME}"
          action: insert
    batch: {}

  exporters:
    otlp_http/newrelic:
      endpoint: "https://otlp.nr-data.net:4318"
      headers:
        api-key: "${env:NEWRELIC_LICENSE_KEY}"

  service:
    pipelines:
      logs:
        receivers: [filelog]
        processors: [filter/onlyproxy, resource/cluster, batch]
        exporters: [otlp_http/newrelic]
```

```bash
helm upgrade my-opentelemetry-collector-logs open-telemetry/opentelemetry-collector \
  -f filelog-values.yaml -n newrelic --create-namespace --install
```

  </Collapser>

</CollapserGroup>

## Next steps [#next-steps]

- [Linkerd distributed tracing with OpenTelemetry](/docs/opentelemetry/integrations/linkerd/linkerd-otel-distributed-tracing/) - Enable proxy trace export and instrument your application pods
- [Find and query your Linkerd data](/docs/opentelemetry/integrations/linkerd/find-and-query-your-data/) - Dashboards, NRQL queries, and troubleshooting data visibility
- [Linkerd metrics reference](/docs/opentelemetry/integrations/linkerd/linkerd-otel-metrics-reference/) - Complete metrics and attributes reference
- [Linkerd OpenTelemetry monitoring overview](/docs/opentelemetry/integrations/linkerd/linkerd-otel-overview/) - Back to overview
