---
title: Advanced configuration for Docker monitoring
tags:
  - Integrations
  - OpenTelemetry
  - Docker
  - Containers
metaDescription: "Advanced configuration options for Docker container monitoring with OpenTelemetry, including log collection for the NRDOT Collector and OpenTelemetry Collector Contrib."
freshnessValidatedDate: never
---

This page covers optional configuration for Docker container monitoring beyond the minimum setup — currently, log collection. Pick the tab for the collector you installed: [NRDOT Collector](/docs/opentelemetry/integrations/docker/install-nrdot) or [OpenTelemetry Collector Contrib](/docs/opentelemetry/integrations/docker/install-otelcol-contrib).

## Configure log collection [#logs]

You can collect logs from your Docker containers using the OpenTelemetry [receiver creator](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/receiver/receivercreator) with the [Docker observer](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/extension/observer/dockerobserver) extension.

<Callout variant="important">
  **Requirements for log collection:**
  - The collector process must have read permissions to the `/var/lib/docker/containers/` directory. This typically requires running the collector as root or adding the collector user to the docker group.
  - Container ports must be exposed for the receiver creator to discover and scrape logs from containers.
</Callout>

<CollapserGroup>
<Collapser
  id="logs-nrdot"
  title="NRDOT Collector"
>

Add the log collection configuration to your existing `docker-stats-config.yaml`.

```yaml
extensions:
  # Docker observer - discovers running containers
  docker_observer:
    endpoint: unix:///var/run/docker.sock
    use_hostname_if_present: true

receivers:
  # Your existing dockerstats receiver...

  # Receiver creator - dynamically creates filelog receivers for discovered containers
  receiver_creator:
    watch_observers: [docker_observer]
    receivers:
      filelog:
        rule: type == "container"
        config:
          include:
            - /var/lib/docker/containers/`container_id`/`container_id`-json.log
          poll_interval: 200ms
          start_at: end
          include_file_name: false
          include_file_path: false
          operators:
            - id: container-parser
              type: container
              format: docker
              add_metadata_from_filepath: false

processors:
  # Your existing processors...

  # Add entity type attribute for logs (required for New Relic entity correlation)
  attributes/logs:
    actions:
      - key: nr.entity_type
        value: CONTAINER
        action: upsert

service:
  extensions: [docker_observer]
  pipelines:
  # Your existing metrics pipeline...

    logs:
      receivers: [receiver_creator]
      processors: [attributes/logs, batch]
      exporters: [otlp_http]
```

After updating the configuration, [restart the collector](/docs/opentelemetry/integrations/docker/install-nrdot#start) to apply the changes.

To check the collector's own logs for errors, use `journalctl`:

```bash
journalctl -u nrdot-collector -f
```

</Collapser>
<Collapser
  id="logs-otelcol-contrib"
  title="OpenTelemetry Collector Contrib"
>

Merge the following configuration with your existing config file at `/etc/otelcol-contrib/config.yaml`.

```yaml
extensions:
  # Docker observer - discovers running containers
  docker_observer:
    endpoint: unix:///var/run/docker.sock
    use_hostname_if_present: true

receivers:
  # Your existing dockerstats receiver...

  # Receiver creator - dynamically creates filelog receivers for discovered containers
  receiver_creator:
    watch_observers: [docker_observer]
    receivers:
      filelog:
        rule: type == "container"
        config:
          include:
            - /var/lib/docker/containers/`container_id`/`container_id`-json.log
          poll_interval: 200ms
          start_at: end
          include_file_name: false
          include_file_path: false
          operators:
            - id: container-parser
              type: container
              format: docker
              add_metadata_from_filepath: false

processors:
  # Your existing processors...

  # Add entity type attribute for logs (required for New Relic entity correlation)
  attributes/logs:
    actions:
      - key: nr.entity_type
        value: CONTAINER
        action: upsert

service:
  extensions: [docker_observer]
  pipelines:
  # Your existing metrics pipeline...

    logs:
      receivers: [receiver_creator]
      processors: [attributes/logs]
      exporters: [otlp_http/newrelic]
```

After updating the configuration, [restart the collector](/docs/opentelemetry/integrations/docker/install-otelcol-contrib#start) to apply the changes.

To check the collector's own logs for errors, use `journalctl`:

```bash
journalctl -u otelcol-contrib -f
```

</Collapser>
</CollapserGroup>

### Configuration parameters [#logs-configuration-parameters]

The following table describes the key configuration parameters for log collection:

| Parameter | Description |
|-----------|-------------|
| `endpoint` (`docker_observer`) | Enter the path to the Docker socket the observer connects to. Default: `unix:///var/run/docker.sock` |
| `use_hostname_if_present` | Set whether to use the container's hostname when available. Default: `true` |
| `rule` (`filelog`) | Enter the matching rule the receiver creator uses to decide which discovered endpoints get a `filelog` receiver. Default: `type == "container"` |
| `poll_interval` | Set how often the receiver checks log files for new lines. Default: `200ms` |
| `start_at` | Set where the receiver starts reading each log file. Use `end` to skip existing log lines and collect only new ones. |

## Related articles [#related-articles]

<DocTiles>
  <DocTile title="Install and configure Docker with NRDOT Collector" path="/docs/opentelemetry/integrations/docker/install-nrdot">
    Minimum install, configuration, and verification steps
  </DocTile>
  <DocTile title="Install and configure Docker with OpenTelemetry Collector Contrib" path="/docs/opentelemetry/integrations/docker/install-otelcol-contrib">
    Minimum install, configuration, and verification steps
  </DocTile>
  <DocTile title="Docker metrics reference" path="/docs/opentelemetry/integrations/docker/metrics-reference">
    Complete metric and attribute reference, with alerting recommendations
  </DocTile>
</DocTiles>
