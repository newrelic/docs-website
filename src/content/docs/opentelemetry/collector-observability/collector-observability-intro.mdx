---
title: OpenTelemetry Collector monitoring
metaDescription: Monitor your collector with all the native telemetry it provides using a dedicated APM UI.
freshnessValidatedDate: 2025-05-27
tags:
  - OpenTelemetry
  - Collector
  - Monitoring
  - Observability
  - APM
---

<Callout variant="caution" title="Preview">
We're still working on this feature, but we'd love for you to try it out!

This feature is currently provided as part of a preview pursuant to our [pre-release policies](/docs/licenses/license-information/referenced-policies/new-relic-pre-release-policy/).
</Callout>

The [OpenTelemetry collector](https://opentelemetry.io/docs/collector/) is a critical component of the OpenTelemetry ecosystem, and it is integral to any adoption of OTel if your setup has reached a certain complexity. However, when it fails or misbehaves, it can cause a blackout of observability data, permanent data loss, or distorted insights. Thankfully, the collector is also capable of [reporting telemetry about itself](https://opentelemetry.io/docs/collector/internal-telemetry/) but the standard New Relic APM UI is not tailored towards the streaming-based work that the collector performs. In order to address this shortcoming, we built an APM experience that exposes the internal telemetry emitted by the collector in a way that allows you to see with one glance how the various components of your collector are performing.

## Get started [#get-started]

To start monitoring your collectors:

1. [Set up collector monitoring](/docs/opentelemetry/collector-observability/collector-observability-setup) - Enable internal telemetry and configure your collector
2. [View your collector in the UI](/docs/opentelemetry/collector-observability/collector-observability-ui) - Navigate to the APM UI and explore collector metrics
3. [Review examples](/docs/opentelemetry/collector-observability/collector-observability-examples) - See real-world configuration examples

## Understand costs [#understand-costs]

Collector observability generates additional telemetry data. Learn about [billing and data overhead](/docs/opentelemetry/collector-observability/collector-observability-overhead) before enabling this feature.

## Limitations [#limitations]

- **Certain common APM features** do not seamlessly translate to the Collector and its stream-processing nature and have been hidden from the Summary page and side navigation. We'll be taking a look at each of them for General Availability to determine if/how we can best integrate them. Examples are:
  - Apdex score
  - Meaningful `apm.%` metrics
  - Error Rate
  - Transactions

- **Collector telemetry is not stable yet**:
  - The latest supported version of telemetry emitted by collector components is the component version in our NRDOT distributions (see [manifest](https://github.com/newrelic/nrdot-collector-releases/blob/main/distributions/nrdot-collector/manifest.yaml) for reference).
  - If the emitted telemetry changes during Public Preview, we reserve the right to only support the most recent version, e.g. support `http.client.request.duration` (collector version &gt;=0.128.0, NRDOT version &gt;=1.2.0, nr-k8s-otel-collector &gt;=0.8.37) but not `http.request.duration`.
  - With General Availability we will start providing backwards compatibility if metric names continue to change.

- **Export format requirements**: Collector UI expects telemetry in the shape as exported by the `otlpexporter`. In particular, it does not support metrics exported via prometheus, e.g. `http.client.request.duration` is supported, `http_client_request_duration` is not supported.

- **Custom component metrics**: Internal telemetry that is not listed in the [internal telemetry documentation](https://opentelemetry.io/docs/collector/internal-telemetry/) is not supported yet. Custom or [contrib components](https://github.com/open-telemetry/opentelemetry-collector-contrib) emit those standard metrics but they can also define metrics separate from that and we're still working on a way that would allow you to get insights from those as well without having to write custom dashboards.

- **Golden metrics for OTel containers**: Not fully supported yet which means some columns in the infrastructure panel might not be populated for containers.

## What's next [#whats-next]

Ready to get started? [Set up collector monitoring](/docs/opentelemetry/collector-observability/collector-observability-setup) to enable internal telemetry and configure your collector.
