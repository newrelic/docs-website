---
title: "AWS S3 actions"
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS S3 actions
metaDescription: "A list of available aws s3 actions in the actions catalog for workflow definitions"
redirects:
  - /docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-s3
freshnessValidatedDate: 2026-09-16
---

This page provides a reference for AWS S3 actions available in the workflow automation actions catalog. Use these actions to upload, download, list, and delete objects in Amazon S3 buckets.

## Prerequisites

Before using AWS actions in workflow automation, ensure you have:

  * An AWS account with appropriate permissions.
  * AWS credentials configured (IAM user credentials, IAM role ARN, or session credentials).
  * The necessary IAM permissions for the specific AWS services you plan to use.

See [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) for information on how to create IAM users and IAM roles, and set up static and session AWS credentials for integration with workflow automation AWS actions.

## List objects in an S3 bucket

The action identifier is `aws.s3.listObjectsV2`.

The `listObjectsV2` method returns some or all (up to 1,000) of the objects in a bucket. It is a more modern and recommended version of `list_objects`.

<Tabs>
  <TabsBar>
    <TabsBarItem id="s3-listobjectsv2-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="s3-listobjectsv2-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="s3-listobjectsv2-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="s3-listobjectsv2-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>Optional. IAM role ARN to assume for this request.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>Optional. AWS access key ID for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>Optional. AWS secret access key for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>Optional. AWS session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>Required. AWS region where the bucket resides.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**bucket**</td>
      <td>String</td>
      <td>Required. Name of the S3 bucket to list objects from.</td>
      <td>`"examplebucket"`</td>
    </tr>
    <tr>
      <td>**prefix**</td>
      <td>String</td>
      <td>Optional. Limits the response to keys that begin with the specified prefix.</td>
      <td>`"path/to/folder/"`</td>
    </tr>
    <tr>
      <td>**maxKeys**</td>
      <td>Integer</td>
      <td>Optional. Maximum number of keys to return (up to 1,000).</td>
      <td>`100`</td>
    </tr>
    <tr>
      <td>**continuationToken**</td>
      <td>String</td>
      <td>Optional. Token to continue a previous paginated listing.</td>
      <td>`some-token`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>Optional. Additional boto3 parameters passed directly to the API call.</td>
      <td>
```yaml
{
  "Delimiter": "/"
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>Optional. JMESPath selectors to extract specific fields from the response.</td>
      <td>`[{"name": "response", "expression": ".response"}, {"name": "success", "expression": ".success"}, {"name": "errorMessage", "expression": ".errorMessage"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-listobjectsv2-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>
        The raw response from the S3 API. Key fields include:

```yaml
{
    'IsTruncated': True|False,
    'Contents': [
        {
            'Key': 'string',
            'LastModified': datetime(2015, 1, 1),
            'ETag': 'string',
            'Size': 123,
            'StorageClass': 'STANDARD'|'...',
        },
    ],
    'Name': 'string',
    'Prefix': 'string',
    'MaxKeys': 123,
    'KeyCount': 123,
    'NextContinuationToken': 'string',
}
```

        See [boto3 list_objects_v2 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/list_objects_v2.html) for the full response schema.
      </td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>true on success, false on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error message on failure. null on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-listobjectsv2-example">
The following example lists objects in an S3 bucket using a role ARN for authentication.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: aws-s3-list-objects-v2
description: 'List Objects in an AWS S3 Bucket'

steps:
  - name: aws_s3_listObjectsV2_1
    type: action
    action: aws.s3.listObjectsV2
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      prefix: "path/to/folder/"
      maxKeys: 100
      continuationToken: "some-token"
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Delete an S3 object

The action identifier is `aws.s3.deleteObject`.

The `deleteObject` method permanently removes a single object from a bucket. For versioned buckets, this operation inserts a **delete marker**, which hides the object without permanently deleting it unless a `VersionId` is specified.

<Tabs>
  <TabsBar>
    <TabsBarItem id="s3-deleteobject-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="s3-deleteobject-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="s3-deleteobject-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="s3-deleteobject-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>Optional. IAM role ARN to assume for this request.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>Optional. AWS access key ID for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>Optional. AWS secret access key for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>Optional. AWS session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>Required. AWS region where the bucket resides.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**bucket**</td>
      <td>String</td>
      <td>Required. Name of the S3 bucket containing the object to delete.</td>
      <td>`"examplebucket"`</td>
    </tr>
    <tr>
      <td>**key**</td>
      <td>String</td>
      <td>Required. Key (path) of the object to delete.</td>
      <td>`"path/to/object.txt"`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>Optional. Additional boto3 parameters passed directly to the API call.</td>
      <td>
```yaml
{
  "RequestPayer": "test",
  "BypassGovernanceRetention": false,
  "VersionId": "testVersion",
  "MFA": "Test"
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>Optional. JMESPath selectors to extract specific fields from the response.</td>
      <td>`[{"name": "response", "expression": ".response"}, {"name": "success", "expression": ".success"}, {"name": "errorMessage", "expression": ".errorMessage"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-deleteobject-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>
        The raw response from the S3 API. Key fields include:

```yaml
{
    'DeleteMarker': True|False,
    'VersionId': 'string',
    'RequestCharged': 'requester'
}
```

        See [boto3 delete_object documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/delete_object.html) for the full response schema.
      </td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>true on success, false on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error message on failure. null on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-deleteobject-example">
The following example deletes an object from an S3 bucket using a role ARN for authentication.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: aws-s3-delete-object
description: 'Delete an AWS S3 Object'

steps:
  - name: aws_s3_deleteObject_1
    type: action
    action: aws.s3.deleteObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-west-2"
      bucket: "my-bucket"
      key: "path/to/object.txt"
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Upload an S3 object

The action identifier is `aws.s3.putObject`.

Adds an object to a bucket. Amazon S3 is a distributed system. If it receives multiple write requests for the same object simultaneously, it overwrites all but the last object written.

<Tabs>
  <TabsBar>
    <TabsBarItem id="s3-putobject-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="s3-putobject-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="s3-putobject-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="s3-putobject-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>Optional. IAM role ARN to assume for this request.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>Optional. AWS access key ID for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>Optional. AWS secret access key for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>Optional. AWS session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>Required. AWS region where the bucket resides.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**bucket**</td>
      <td>String</td>
      <td>Required. Name of the S3 bucket to upload the object to.</td>
      <td>`"examplebucket"`</td>
    </tr>
    <tr>
      <td>**key**</td>
      <td>String</td>
      <td>Required. Key (path) under which to store the object.</td>
      <td>`"path/to/object.txt"`</td>
    </tr>
    <tr>
      <td>**body**</td>
      <td>String</td>
      <td>Required. Content to upload as the object body.</td>
      <td>`file content`</td>
    </tr>
    <tr>
      <td>**contentType**</td>
      <td>String</td>
      <td>Required. MIME type of the object being uploaded.</td>
      <td>`"plain/text"`</td>
    </tr>
    <tr>
      <td>**tagging**</td>
      <td>String</td>
      <td>Optional. URL-encoded tag set to apply to the object.</td>
      <td>`"key1=value1"`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>Optional. Additional boto3 parameters passed directly to the API call.</td>
      <td>
```yaml
{
  "ServerSideEncryption": "AES256",
  "Metadata": {
    "metadata1": "value1",
    "metadata2": "value2"
  }
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>Optional. JMESPath selectors to extract specific fields from the response.</td>
      <td>`[{"name": "response", "expression": ".response"}, {"name": "success", "expression": ".success"}, {"name": "errorMessage", "expression": ".errorMessage"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-putobject-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>
        The raw response from the S3 API. Key fields include:

```yaml
{
    'ETag': 'string',
    'VersionId': 'string',
    'ServerSideEncryption': 'AES256'|'aws:kms'|'...',
    'BucketKeyEnabled': True|False,
    'RequestCharged': 'requester'
}
```

        See [boto3 put_object documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/put_object.html) for the full response schema.
      </td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>true on success, false on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error message on failure. null on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-putobject-example">
The following example uploads a text object to an S3 bucket using a role ARN for authentication.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: s3-put-object
description: 'Put an AWS S3 Object'

steps:
  - name: aws_s3_putObject_1
    type: action
    action: aws.s3.putObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
      body: "Hello world"
      contentType: "plain/text"
      tagging: "key1=value1"
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Download an S3 object

The action identifier is `aws.s3.getObject`.

In the `GetObject` request, specify the full key name for the object.

<Tabs>
  <TabsBar>
    <TabsBarItem id="s3-getobject-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="s3-getobject-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="s3-getobject-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="s3-getobject-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>Optional. IAM role ARN to assume for this request.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>Optional. AWS access key ID for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>Optional. AWS secret access key for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>Optional. AWS session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>Required. AWS region where the bucket resides.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**bucket**</td>
      <td>String</td>
      <td>Required. Name of the S3 bucket containing the object.</td>
      <td>`"examplebucket"`</td>
    </tr>
    <tr>
      <td>**key**</td>
      <td>String</td>
      <td>Required. Full key name of the object to retrieve.</td>
      <td>`"path/to/object.txt"`</td>
    </tr>
    <tr>
      <td>**versionId**</td>
      <td>String</td>
      <td>Optional. Version ID of the object to retrieve from a versioned bucket.</td>
      <td>`"some-version-id"`</td>
    </tr>
    <tr>
      <td>**range**</td>
      <td>String</td>
      <td>Optional. HTTP range header to retrieve only a portion of the object.</td>
      <td>`bytes=0-99`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>Optional. Additional boto3 parameters passed directly to the API call.</td>
      <td>
```yaml
{
  "ChecksumMode": "ENABLED",
  "ExpectedBucketOwner": "test-user"
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>Optional. JMESPath selectors to extract specific fields from the response.</td>
      <td>`[{"name": "response", "expression": ".response"}, {"name": "success", "expression": ".success"}, {"name": "errorMessage", "expression": ".errorMessage"}]`</td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
  The action will fail if the object is more than 100kb.
</Callout>
    </TabsPageItem>
    <TabsPageItem id="s3-getobject-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>
        The raw response from the S3 API. Key fields include:

```yaml
{
    'Body': StreamingBody(),
    'ContentLength': 123,
    'ContentType': 'string',
    'ETag': 'string',
    'VersionId': 'string',
    'LastModified': datetime(2015, 1, 1),
    'Metadata': {
        'string': 'string'
    },
    'StorageClass': 'STANDARD'|'...',
}
```

        See [boto3 get_object documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/s3/client/get_object.html) for the full response schema.
      </td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>true on success, false on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error message on failure. null on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="s3-getobject-example">
The following example downloads an object from an S3 bucket using a role ARN for authentication.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: s3-get-object
description: 'Get an AWS S3 Object'

steps:
  - name: aws_s3_getObject_1
    type: action
    action: aws.s3.getObject
    version: '1'
    inputs:
      awsRoleArn: "arn:aws:iam::123456789012:role/my-workflow-role"
      region: "us-east-2"
      bucket: "examplebucket"
      key: "path/to/object.txt"
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## What's next

- [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) — Configure IAM users, IAM roles, and static or session credentials for use with workflow automation AWS actions.
- [AWS Execute API actions](/docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-execute-api/) — Make authenticated calls to any AWS service endpoint using the Execute API action.
- [Workflow examples](/docs/workflow-automation/getting-started/workflow-examples/) — Browse end-to-end workflow definitions that demonstrate common automation patterns.
