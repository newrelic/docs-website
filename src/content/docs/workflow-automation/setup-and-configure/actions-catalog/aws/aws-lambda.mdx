---
title: "AWS Lambda actions"
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS Lambda actions
metaDescription: "A reference for AWS Lambda actions available in the workflow automation actions catalog."
redirects:
  - /docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-lambda
freshnessValidatedDate: 2026-09-16
---

This page provides a reference for AWS Lambda and Bedrock actions available in the workflow automation actions catalog. Use these actions to invoke Lambda functions, update configurations, and run Bedrock agents.

## Prerequisites

Before using AWS Lambda actions in workflow automation, ensure you have:

* An AWS account with appropriate permissions.
* AWS credentials configured (IAM user credentials, IAM role ARN, or session credentials). At least one credential option must be provided — IAM role takes precedence over static access keys.
* The necessary IAM permissions for the specific Lambda operations you plan to use.

See [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) for information on how to create IAM users and IAM roles, and set up static and session AWS credentials for integration with workflow automation AWS actions.

## Invoke a Lambda function

The action identifier is `aws.lambda.invoke`.

Invokes a Lambda function synchronously or asynchronously with an optional payload.

<Tabs>
  <TabsBar>
    <TabsBarItem id="lambda-invoke-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="lambda-invoke-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="lambda-invoke-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="lambda-invoke-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when calling the Lambda API. Takes precedence over static credentials when provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for an IAM user. Use with `awsSecretAccessKey` for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key for an IAM user. Use with `awsAccessKeyId` for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** Session token for temporary credentials. Provide together with `awsAccessKeyId` and `awsSecretAccessKey` when using session credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** AWS region where the Lambda function is deployed.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**functionName**</td>
      <td>String</td>
      <td>**Required.** Name or ARN of the Lambda function to invoke.</td>
      <td>`functionName: "hello-you"`</td>
    </tr>
    <tr>
      <td>**payload**</td>
      <td>Map</td>
      <td>**Optional.** JSON payload to pass to the Lambda function as the event input.</td>
      <td>
`payload:`
`  user: R2-D2`
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "payload", "expression": ".payload"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="lambda-invoke-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**payload**</td>
      <td>Map</td>
      <td>The response body returned by the Lambda function. For example: `{statusCode=200.0, body="Hello R2-D2!"}`</td>
    </tr>
    <tr>
      <td>**statusCode**</td>
      <td>Int</td>
      <td>HTTP status code of the Lambda invocation response. For example: `200`</td>
    </tr>
    <tr>
      <td>**executedVersion**</td>
      <td>String</td>
      <td>The version of the Lambda function that was executed. For example: `"$LATEST"`</td>
    </tr>
    <tr>
      <td>**functionError**</td>
      <td>String</td>
      <td>Error type string returned when the Lambda function throws an unhandled exception. For example: `"Unhandled::Error occurred"`</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` if the action completed successfully; `false` otherwise.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Human-readable failure reason when `success` is `false`.</td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
  At least one set of AWS credentials must be provided: `awsRoleArn`, static credentials (`awsAccessKeyId` + `awsSecretAccessKey`), or session credentials (`awsAccessKeyId` + `awsSecretAccessKey` + `awsSessionToken`). `awsRoleArn` takes precedence over the others. The credential owner must have the `lambda:InvokeFunction` IAM permission. Use `selectors` to return only the specified output fields.
</Callout>
    </TabsPageItem>
    <TabsPageItem id="lambda-invoke-example">
The following example invokes a Lambda function and logs the response body.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: LambdaTest

workflowInputs:
  region:
    type: String
  functionName:
    type: String
  payload:
    type: Map
    defaultValue: {}

steps:
  - name: awsLambda
    type: action
    action: aws.lambda.invoke
    version: 1
    inputs:
      region: ${{ .workflowInputs.region }}
      functionName: ${{ .workflowInputs.functionName }}
      awsAccessKeyId: ${{ :secrets:awsAccessKeyId }}
      awsSecretAccessKey: ${{ :secrets:awsSecretAccessKey }}
      awsSessionToken: ${{ :secrets:awsSessionToken }}
      payload: ${{ .workflowInputs.payload }}
      selectors: ${{ .workflowInputs.selectors }}

  - name: logOutput
    type: action
    action: newrelic.instrumentation.log
    version: 1
    inputs:
      message: 'The lambda function message output is:${{ .steps.awsLambda.outputs.payload.body }}'
      licenseKey: '${{ :secrets:STAGING_NEW_RELIC_LICENSE_KEY }}'
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Update Lambda function configuration

The action identifier is `aws.lambda.updateFunctionConfiguration`.

Modifies the configuration of a specific AWS Lambda function. Provide only the parameters you want to change.

<Tabs>
  <TabsBar>
    <TabsBarItem id="lambda-updateconfig-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="lambda-updateconfig-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="lambda-updateconfig-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="lambda-updateconfig-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when calling the Lambda API. Takes precedence over static credentials when provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for an IAM user.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key for an IAM user.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** Session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** AWS region where the Lambda function is deployed.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**functionName**</td>
      <td>String</td>
      <td>**Required.** Name or ARN of the Lambda function to update.</td>
      <td>`"my-lambda-function-to-update"`</td>
    </tr>
    <tr>
      <td>**role**</td>
      <td>String</td>
      <td>**Optional.** ARN of the IAM role to assign to the Lambda function.</td>
      <td>`arn:aws:iam::123456789012:role/new-lambda-role`</td>
    </tr>
    <tr>
      <td>**handler**</td>
      <td>String</td>
      <td>**Optional.** Function handler entrypoint to set on the Lambda function.</td>
      <td>`"main.new_handler"`</td>
    </tr>
    <tr>
      <td>**description**</td>
      <td>String</td>
      <td>**Optional.** Human-readable description to set on the Lambda function.</td>
      <td>`Updated function description`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>**Optional.** Additional configuration fields to update. Accepts any optional argument supported by the AWS `UpdateFunctionConfiguration` API.</td>
      <td>
```yaml
{
  "Timeout": "60",
  "MemorySize": 123,
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "response", "expression": ".response"}, {"name": "success", "expression": ".success"}, {"name": "errorMessage", "expression": ".errorMessage"}]`</td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
  To support a wide range of inputs, the `parameters` map accepts any optional argument available. This allows you to dynamically construct requests by adding multiple fields.
</Callout>
    </TabsPageItem>
    <TabsPageItem id="lambda-updateconfig-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object. Example snippet: `{"FunctionName":"hello-you","FunctionArn":"arn:aws:lambda:us-east-2:123456789012:function:hello-you","Runtime":"nodejs20.x","Timeout":30,"MemorySize":128,...}`. For the complete response schema, see [update_function_configuration — Boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/lambda/client/update_function_configuration.html).</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` if the update completed successfully; `false` otherwise.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Human-readable failure reason when `success` is `false`. For example: `"An error occurred (ResourceNotFoundException) when calling the UpdateFunctionConfiguration operation: Function not found: arn:aws:lambda:us-east-2:123456789012:function:my-lambda-function-to-update"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="lambda-updateconfig-example">
The following example updates the timeout and memory of a Lambda function using an assumed IAM role.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: lambda-update-config-test
description: 'Updates the timeout and memory of a Lambda function'
workflowInputs:
  arnRole:
    type: String
steps:
  - name: aws_lambda_update_function_configuration_1
    type: action
    action: aws.lambda.updateFunctionConfiguration
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-west-2
      functionName: my-lambda-function-to-update
      parameters:
      Timeout:60
      MemorySize:123

    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Get Lambda function details

The action identifier is `aws.lambda.getFunction`.

Retrieves the configuration details, code location, and other metadata for a specific Lambda function.

<Tabs>
  <TabsBar>
    <TabsBarItem id="lambda-getfunction-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="lambda-getfunction-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="lambda-getfunction-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="lambda-getfunction-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when calling the Lambda API. Takes precedence over static credentials when provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for an IAM user.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key for an IAM user.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** Session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** AWS region where the Lambda function is deployed.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**functionName**</td>
      <td>String</td>
      <td>**Required.** Name or ARN of the Lambda function to retrieve.</td>
      <td>`"my-lambda-function"`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>**Optional.** Additional API parameters. Accepts any optional argument supported by the AWS `GetFunction` API.</td>
      <td>
```yaml
{
  "Qualifier": "1"
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "response", "expression": ".response"}]`</td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
  To support a wide range of inputs, the `parameters` map accepts any optional argument available. This allows you to dynamically construct requests by adding multiple fields.
</Callout>
    </TabsPageItem>
    <TabsPageItem id="lambda-getfunction-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object containing function configuration, code location, and metadata. For the complete response schema, see [get_function — Boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/lambda/client/get_function.html).</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` if the request completed successfully; `false` otherwise.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Human-readable failure reason when `success` is `false`. For example: `"ResourceNotFoundException: Function not found"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="lambda-getfunction-example">
The following example retrieves the configuration of a specific Lambda function.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: lambda-get-function-test
description: 'Retrieves the configuration of a specific Lambda function'
workflowInputs:
  arnRole:
    type: String
steps:
  - name: aws_lambda_getFunction_1
    type: action
    action: aws.lambda.getFunction
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      FunctionName: hello-you
      parameters:
        Qualifier: 1
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## List Lambda function aliases

The action identifier is `aws.lambda.listAliases`.

Returns a list of aliases for a specific Lambda function. Aliases are pointers to function versions.

<Tabs>
  <TabsBar>
    <TabsBarItem id="lambda-listaliases-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="lambda-listaliases-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="lambda-listaliases-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="lambda-listaliases-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when calling the Lambda API. Takes precedence over static credentials when provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for an IAM user.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key for an IAM user.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** Session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** AWS region where the Lambda function is deployed.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**functionName**</td>
      <td>String</td>
      <td>**Required.** Name or ARN of the Lambda function whose aliases to list.</td>
      <td>`"my-lambda-function"`</td>
    </tr>
    <tr>
      <td>**marker**</td>
      <td>String</td>
      <td>**Optional.** Pagination token. Pass the `NextMarker` value from a previous response to retrieve the next page of results.</td>
      <td>`"abcd....."`</td>
    </tr>
    <tr>
      <td>**maxItems**</td>
      <td>Int</td>
      <td>**Optional.** Maximum number of aliases to return in a single response.</td>
      <td>`123`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>**Optional.** Additional optional API parameters. Accepts any optional argument supported by the AWS `ListAliases` API.</td>
      <td>`{"AnotherOptionalParam": "value"}`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "response", "expression": ".response"}]`</td>
    </tr>
  </tbody>
</table>

<Callout variant="important">
  Use `marker` and `maxItems` to paginate through a large number of aliases. To support a wide range of inputs, the `parameters` map accepts any optional argument available, allowing you to dynamically construct requests by adding multiple fields.
</Callout>
    </TabsPageItem>
    <TabsPageItem id="lambda-listaliases-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response containing the list of aliases and an optional `NextMarker` for pagination. For the complete response schema, see [list_aliases — Boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/lambda/client/list_aliases.html).</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` if the request completed successfully; `false` otherwise.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Human-readable failure reason when `success` is `false`. For example: `"ResourceNotFoundException: Function not found"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="lambda-listaliases-example">
The following example lists all aliases for a Lambda function.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: lambda-list-aliases-test
description: 'Lists all aliases for a Lambda function'
workflowInputs:
  arnRole:
    type: String
steps:
  - name: aws_lambda_list_aliases_1
    type: action
    action: aws.lambda.listAliases
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      functionName: hello-you
      parameters:
        FunctionVersion: 1
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Invoke a Bedrock agent

The action identifier is `aws.bedrock.invokeAgent`.

Invokes a Bedrock agent with a text prompt and returns the agent's completion response.

<Tabs>
  <TabsBar>
    <TabsBarItem id="bedrock-invokeagent-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="bedrock-invokeagent-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="bedrock-invokeagent-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="bedrock-invokeagent-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when calling the Bedrock API. Takes precedence over static credentials when provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for an IAM user.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key for an IAM user.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** Session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** AWS region where the Bedrock agent is deployed.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**agentId**</td>
      <td>String</td>
      <td>**Required.** Unique identifier of the Bedrock agent to invoke.</td>
      <td>`Q123456789`</td>
    </tr>
    <tr>
      <td>**agentAliasId**</td>
      <td>String</td>
      <td>**Required.** Alias ID of the Bedrock agent to invoke.</td>
      <td>`AL12345678`</td>
    </tr>
    <tr>
      <td>**sessionId**</td>
      <td>String</td>
      <td>**Optional.** Session ID for multi-turn conversations. Omit to start a new session.</td>
      <td>`e1572683-d2dd-4904-a32a-36f398b52dd5`</td>
    </tr>
    <tr>
      <td>**inputText**</td>
      <td>String</td>
      <td>**Required.** The prompt or question to send to the Bedrock agent.</td>
      <td>`"your prompt to agent"`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>**Optional.** Additional optional API parameters accepted by the Bedrock `InvokeAgent` API.</td>
      <td>
```yaml
{
  invocationId: "your-invocation-id",
  endSession: true
}
```
      </td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "response", "expression": ".response"}, {"name": "success", "expression": ".success"}, {"name": "errorMessage", "expression": ".errorMessage"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="bedrock-invokeagent-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The agent's response object, including `contentType`, `sessionId`, and `completion` text. For example: `{"contentType": "application/json", "sessionId": "046d9e16-7b2b-4404-88c0-1fa8efb655a9", "completion": "Response from bedrock agent"}`</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` if the invocation completed successfully; `false` otherwise.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Human-readable failure reason when `success` is `false`. For example: `"An error occurred (ValidationException) when calling the InvokeAgent operation"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="bedrock-invokeagent-example">
The following example invokes a Bedrock agent with a text prompt.
<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
```yaml
name: test-bedrock-agent
description: 'This invokes an aws bedrock agent'

steps:
  - name: aws_bedrock_invokeAgent_1
    type: action
    action: aws.bedrock.invokeAgent
    version: '1'
    inputs:
      region: us-east-1
      agentId: Agent123456
      agentAliasId: Alias12345
      sessionId: 5aa61ad7-8e57-4369-a178-66ff5dae71f2
      inputText: What are the top 3 developments in AI?
      awsRoleArn: ${{ :secrets:arnRole }}
      parameters:
        endSession: false
    next: end
```
      </td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

The IAM role used to invoke a Bedrock agent must include the following permissions and trust relationships.

**AWS IAM policy example**

```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "AllowAgentExecution",
            "Effect": "Allow",
            "Action": [
                "bedrock:InvokeAgent"
            ],
            "Resource": [
                "arn:aws:bedrock:us-east-2:1234567890:agent/H7XK3RZSYI",
                "arn:aws:bedrock:us-east-2:1234567890:agent-alias/H7XK3RZSYI/NUAYR0EK1X"
            ]
        },
        {
            "Sid": "AllowModelInvocation",
            "Effect": "Allow",
            "Action": [
                "bedrock:InvokeModel",
                "bedrock:InvokeModelWithResponseStream"
            ],
            "Resource": "*"
        },
        {
            "Sid": "AllowCloudWatchLogging",
            "Effect": "Allow",
            "Action": [
                "logs:CreateLogGroup",
                "logs:CreateLogStream",
                "logs:PutLogEvents"
            ],
            "Resource": [
                "arn:aws:bedrock:us-east-2:1234567890:agent/H7XK3RZSYI",
                "arn:aws:bedrock:us-east-2:1234567890:agent-alias/H7XK3RZSYI/NUAYR0EK1X"
            ]
        }
    ]
}
```

**Trust policy for production**

```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Principal": {
                "AWS": [
                    "arn:aws:iam::253490767857:root"
                ]
            },
            "Action": "sts:AssumeRole",
            "Condition": {
                "StringEquals": {
                    "sts:ExternalId": "12345678"
                }
            }
        }
    ]
}
```

**Trust policy for staging or development**

```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Principal": {
                "AWS": [
                    "arn:aws:iam::430118836486:root",
                    "arn:aws:iam::123456789012:root"
                ]
            },
            "Action": "sts:AssumeRole",
            "Condition": {
                "StringEquals": {
                    "sts:ExternalId": "12345678"
                }
            }
        }
    ]
}
```

## What's next

* [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) — Learn how to configure IAM users, IAM roles, and session credentials for use with workflow automation AWS actions.
* [AWS Execute API actions](/docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-execute-api/) — Use the Execute API action to call any AWS service endpoint not covered by a dedicated action.
* [Workflow examples](/docs/workflow-automation/get-started/workflow-examples/) — Browse end-to-end workflow definition examples that use AWS actions.
