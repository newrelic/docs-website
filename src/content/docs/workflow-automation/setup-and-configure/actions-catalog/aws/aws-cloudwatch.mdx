---
title: "AWS CloudWatch actions"
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS CloudWatch actions
metaDescription: "A reference for AWS CloudWatch actions available in the workflow automation actions catalog."
redirects:
  - /docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-cloudwatch
freshnessValidatedDate: 2026-09-16
---

This page provides a reference for AWS CloudWatch actions available in the workflow automation actions catalog. Use these actions to retrieve and send CloudWatch Logs events.

## Prerequisites

Before using AWS actions in workflow automation, ensure you have:

* An AWS account with appropriate permissions.
* AWS credentials configured (IAM user credentials, IAM role ARN, or session credentials).
* The necessary IAM permissions for the specific AWS services you plan to use.

See [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) for information on how to create IAM users and IAM roles, and set up static and session AWS credentials for integration with workflow automation AWS actions.

## Get log events

The action identifier is `aws.cloudWatch.getLogEvents`.

Retrieves a batch of log events from a specified log stream in AWS CloudWatch Logs. It's essential for monitoring, auditing, and troubleshooting applications by programmatically fetching log data.

<Tabs>
  <TabsBar>
    <TabsBarItem id="getlogevents-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="getlogevents-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="getlogevents-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="getlogevents-inputs">
The following table describes all available input fields for this action.
<table>
  <thead><tr><th>Input</th><th>Type</th><th>Description</th><th>Example</th></tr></thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this action.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the log group resides.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**logStreamName**</td>
      <td>String</td>
      <td>**Required.** The name of the log stream to retrieve events from.</td>
      <td>`"2023/10/27/[$LATEST]abcdef123456"`</td>
    </tr>
    <tr>
      <td>**logGroupName**</td>
      <td>String</td>
      <td>**Optional.** The name of the log group containing the log stream.</td>
      <td>`"/aws/lambda/my-function"`</td>
    </tr>
    <tr>
      <td>**logGroupIdentifier**</td>
      <td>String</td>
      <td>**Optional.** The ARN or name of the log group. Use instead of `logGroupName` when referencing by ARN.</td>
      <td>`"arn:partition:service:region:account-id:resource"`</td>
    </tr>
    <tr>
      <td>**startTime**</td>
      <td>Int</td>
      <td>**Optional.** The start of the time range in milliseconds UTC.</td>
      <td>`1759296000000`</td>
    </tr>
    <tr>
      <td>**endTime**</td>
      <td>Int</td>
      <td>**Optional.** The end of the time range in milliseconds UTC.</td>
      <td>`1759296000000`</td>
    </tr>
    <tr>
      <td>**limit**</td>
      <td>Int</td>
      <td>**Optional.** The maximum number of log events to return.</td>
      <td>`50`</td>
    </tr>
    <tr>
      <td>**startFromHead**</td>
      <td>Boolean</td>
      <td>**Optional.** If `true`, returns events from the oldest to the newest. Defaults to `false`.</td>
      <td>`true`</td>
    </tr>
    <tr>
      <td>**unmask**</td>
      <td>Boolean</td>
      <td>**Optional.** If `true`, displays the unmasked log event. Defaults to `false`.</td>
      <td>`false`</td>
    </tr>
    <tr>
      <td>**nextToken**</td>
      <td>String</td>
      <td>**Optional.** The token for the next set of items to return, obtained from a previous call.</td>
      <td>`"f/39218833627378687642013305455131706539523449361490509828/s"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="getlogevents-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead><tr><th>Output</th><th>Type</th><th>Description</th></tr></thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The raw JSON response from CloudWatch Logs. Example: `{'events': [{'timestamp': 123, 'message': 'string', 'ingestionTime': 123}], 'nextForwardToken': 'string', 'nextBackwardToken': 'string'}`</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error message on failure. `null` on success. Example: `"An error occurred (ExpiredTokenException) when calling the GetLogEvents operation: The security token included in the request is expired"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="getlogevents-example">
The following example retrieves log events from an AWS Lambda function log stream.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody><tr><td>

```yaml
name: get-lambda-logs
description: 'Retrieve log events from an AWS Lambda function'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: /aws/lambda/my-function
  logStreamName:
    type: String
    defaultValue: 2023/10/27/[$LATEST]abcdef123456
steps:
  name: get_events_step
  type: action
  action: aws.cloudWatch.getLogEvents
  version: '1'
  inputs:
    region: ${{ .workflowInputs.region }}
    logGroupName: ${{ .workflowInputs.logGroupName }}
    logStreamName: ${{ .workflowInputs.logStreamName }}
    limit: 100
```

  </td></tr></tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Upload log events

The action identifier is `aws.cloudwatch.putLogEvents`.

Uploads a batch of log events to the specified log stream in AWS CloudWatch Logs. Use this action to programmatically send log data from applications or services to CloudWatch for monitoring and analysis.

<Tabs>
  <TabsBar>
    <TabsBarItem id="putlogevents-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="putlogevents-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="putlogevents-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="putlogevents-inputs">
The following table describes all available input fields for this action.
<table>
  <thead><tr><th>Input</th><th>Type</th><th>Description</th><th>Example</th></tr></thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this action.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credentials.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credentials.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credentials.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the log group resides.</td>
      <td>`"us-east-2"`</td>
    </tr>
    <tr>
      <td>**logGroupName**</td>
      <td>String</td>
      <td>**Required.** The name of the log group to upload events to.</td>
      <td>`"/aws/lambda/hello-you"`</td>
    </tr>
    <tr>
      <td>**logStreamName**</td>
      <td>String</td>
      <td>**Required.** The name of the log stream to upload events to.</td>
      <td>`"2025/09/24/[$LATEST]09f7ca9e9ab044f389419ce60305f594"`</td>
    </tr>
    <tr>
      <td>**logEvents**</td>
      <td>List</td>
      <td>**Required.** A list of log event objects, each with a `timestamp` (milliseconds UTC) and `message`.</td>
      <td>`[{"timestamp": 1698384000000, "message": "Workflow task started."}, {"timestamp": 1698384000015, "message": "Data validated successfully."}]`</td>
    </tr>
    <tr>
      <td>**entity**</td>
      <td>Dict</td>
      <td>**Optional.** An entity object with `keyAttributes` and `attributes` to associate the log events with a specific monitored entity.</td>
      <td>`{"entity": {"keyAttributes": {"ResourceType": "AWS::ElasticLoadBalancingV2::LoadBalancer", "Identifier": "app/my-web-lb/12345", "Environment": "Production"}, "attributes": {"MonitoringTier": "Gold", "OwnerTeam": "Networking", "MaintenanceWindow": "Sat: 0200-0400"}}}`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="putlogevents-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead><tr><th>Output</th><th>Type</th><th>Description</th></tr></thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The raw JSON response, which includes the `nextSequenceToken`. Example: `{'nextSequenceToken': 'string', 'rejectedLogEventsInfo': {'tooNewLogEventStartIndex': 123, 'tooOldLogEventEndIndex': 123, 'expiredLogEventEndIndex': 123}, 'rejectedEntityInfo': {'errorType': 'InvalidEntity|InvalidTypeValue|InvalidKeyAttributes|InvalidAttributes|EntitySizeTooLarge|UnsupportedLogGroupType|MissingRequiredFields'}}`</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>`true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error message on failure. `null` on success. Example: `"An error occurred (ExpiredTokenException) when calling the PutLogEvents operation: The security token included in the request is expired"`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="putlogevents-example">
The following example uploads a batch of custom log events to a CloudWatch log stream.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody><tr><td>

```yaml
name: put-custom-logs
description: 'Upload a batch of custom log events to CloudWatch'
workflowInputs:
  region:
    type: String
    defaultValue: us-east-2
  logGroupName:
    type: String
    defaultValue: my-custom-app-logs
  logStreamName:
    type: String
    defaultValue: instance-01-thread-01

steps:
  - name: upload_logs
    type: action
    action: aws.cloudwatch.putLogEvents
    version: '1'
    inputs:
      region: ${{ .workflowInputs.region }}
      logGroupName: ${{ .workflowInputs.logGroupName }}
      logStreamName: ${{ .workflowInputs.logStreamName }}
      logEvents:
        - timestamp: 1698384000000
          message: "Workflow started successfully"
        - timestamp: 1698384005000
          message: "Step 1 completed"
    next: end
```

  </td></tr></tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## What's next

* [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) — Configure IAM users, IAM roles, and static or session credentials for use with workflow automation AWS actions.
* [AWS Execute API actions](/docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-execute-api/) — Use the AWS Execute API action to invoke any AWS API Gateway endpoint from a workflow step.
* [Workflow examples](/docs/workflow-automation/workflow-examples/) — Browse end-to-end workflow definitions that demonstrate how to combine actions into multi-step automations.
