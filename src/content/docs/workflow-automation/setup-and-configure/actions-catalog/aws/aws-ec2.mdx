---
title: "AWS EC2 actions"
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS EC2 actions
metaDescription: "A reference for AWS EC2 actions available in the workflow automation actions catalog."
redirects:
  - /docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-ec2
freshnessValidatedDate: 2026-09-16
---

This page provides a reference for AWS EC2 actions available in the workflow automation actions catalog. Use these actions to manage EC2 instances and EBS snapshots.

## Prerequisites

Before using AWS actions in workflow automation, ensure you have:

* An AWS account with appropriate permissions.
* AWS credentials configured (IAM user credentials, IAM role ARN, or session credentials).
* The necessary IAM permissions for the specific AWS services you plan to use.

See [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) for information on how to create IAM users and IAM roles, and set up static and session AWS credentials for integration with workflow automation AWS actions.

## Launch EC2 instances

The action identifier is `aws.ec2.runInstances`.

Launches the specified number of instances using an AMI for which you have permissions.

You can specify a number of options, or leave the default options. The following rules apply:
- If you don't specify a subnet ID, we choose a default subnet from your default VPC for you. If you don't have a default VPC, you must specify a subnet ID in the request.
- If any of the AMIs have a product code attached for which the user has not subscribed, the request fails.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-runinstances-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-runinstances-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-runinstances-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-runinstances-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region in which to launch the instances.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**imageId**</td>
      <td>String</td>
      <td>**Required.** The ID of the AMI to use for the instances.</td>
      <td>`"ami-0ca4d5db4872d0c28"`</td>
    </tr>
    <tr>
      <td>**instanceType**</td>
      <td>String</td>
      <td>**Required.** The instance type to use for the instances.</td>
      <td>`"t2.micro"`</td>
    </tr>
    <tr>
      <td>**minCount**</td>
      <td>Int</td>
      <td>**Required.** The minimum number of instances to launch.</td>
      <td>`1`</td>
    </tr>
    <tr>
      <td>**maxCount**</td>
      <td>Int</td>
      <td>**Required.** The maximum number of instances to launch.</td>
      <td>`10`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>**Optional.** A map of additional optional arguments to pass to the API request. To support a wide range of inputs, this map accepts any optional argument available, allowing you to dynamically construct requests by adding multiple fields.</td>
      <td>`{"EbsOptimized": false, "TagSpecifications": [{"ResourceType": "instance", "Tags": [{"Key": "Name", "Value": "My-Web-Server"}]}]}`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-runinstances-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/run_instances.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-runinstances-example">
The following example launches a single EC2 instance using a role ARN and tags it with a name.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ec2_run_instance
workflowInputs:
  arnRole:
    type: String
    required: true
steps:
  - name: RunInstance
    type: action
    action: aws.ec2.runInstances
    version: '1'
    inputs:
      awsRoleArn: ${{.workflowInputs.arnRole}}
      region: us-east-2
      imageId: ami-0ca4d5db4872d0c28
      instanceType: t2.micro
      minCount: 1
      maxCount: 1
      parameters:
        EbsOptimized: false
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: My-Test-Instance
      selectors:
        - name: instanceId
          expression: .response.Instances[0].InstanceId
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Describe EC2 instances

The action identifier is `aws.ec2.describeInstances`.

Describes the specified instances or all instances.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-describeinstances-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-describeinstances-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-describeinstances-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-describeinstances-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region to query for instances.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**instanceIds**</td>
      <td>List</td>
      <td>**Optional.** A list of instance IDs to describe. Cannot be used together with `maxResults`.</td>
      <td>`"[\"i-0123456789abcdef0\", \"i-0fedcba9876543210\"]"`</td>
    </tr>
    <tr>
      <td>**filters**</td>
      <td>List</td>
      <td>**Optional.** A list of filter objects to narrow the results to instances that match the specified criteria.</td>
      <td>`"[{\"Name\": \"instance-state-name\", \"Values\": [\"running\"]}]"`</td>
    </tr>
    <tr>
      <td>**nextToken**</td>
      <td>String</td>
      <td>**Optional.** The token for the next page of results, used for paginating large result sets.</td>
      <td>`"abcdefgh"`</td>
    </tr>
    <tr>
      <td>**maxResults**</td>
      <td>Int</td>
      <td>**Optional.** The maximum number of results to return. Cannot be used together with `instanceIds`.</td>
      <td>100</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-describeinstances-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object. Output includes information for only the specified instances if `instanceIds` is set; for only those instances meeting filter criteria if `filters` is set; or for all instances if neither is set. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/describe_instances.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed, for example `"The parameter instancesSet cannot be used with the parameter maxResults"`. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-describeinstances-example">
The following example describes a specific EC2 instance by ID using a role ARN.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ab-ec2-describe-test
description: ''
workflowInputs:
  arnRole:
    type: String
steps:
  - name: aws_ec2_describeInstances_1
    type: action
    action: aws.ec2.describeInstances
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-2
      instanceIds:
        - i-123456789abcdef0
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Start EC2 instances

The action identifier is `aws.ec2.startInstances`.

Starts an Amazon EBS-backed instance that you previously stopped.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-startinstances-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-startinstances-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-startinstances-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-startinstances-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the instances reside.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**instanceIds**</td>
      <td>List</td>
      <td>**Required.** A list of instance IDs to start.</td>
      <td>`"[\"i-0123456789abcdef0\", \"i-0fedcba9876543210\"]"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-startinstances-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object containing the `StartingInstances` array with each instance's `InstanceId`, `CurrentState`, and `PreviousState`. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/start_instances.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-startinstances-example">
The following example starts a list of EC2 instances in a specified region using a role ARN.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ab-ec2-startInstance-test
description: ''
workflowInputs:
  arnRole:
    type: String
  region:
    type: String
    defaultValue: us-west-2
  instanceIds:
    type: list
    defaultValue: ["i-123456789abcdef0"]
steps:
  - name: aws_ec2_startInstances_1
    type: action
    action: aws.ec2.startInstances
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: ${{ .workflowInputs.region }}
      instanceIds: ${{ .workflowInputs.instanceIds }}
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Stop EC2 instances

The action identifier is `aws.ec2.stopInstances`.

Stops an Amazon EBS-backed instance.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-stopinstances-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-stopinstances-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-stopinstances-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-stopinstances-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the instances reside.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**instanceIds**</td>
      <td>List</td>
      <td>**Required.** A list of instance IDs to stop.</td>
      <td>`"[\"i-0123456789abcdef0\", \"i-0fedcba9876543210\"]"`</td>
    </tr>
    <tr>
      <td>**hibernate**</td>
      <td>Boolean</td>
      <td>**Optional.** When `true`, hibernates the instance if hibernation is enabled. Default: `false`.</td>
      <td>true or false (Default: `false`)</td>
    </tr>
    <tr>
      <td>**force**</td>
      <td>Boolean</td>
      <td>**Optional.** When `true`, forces the instance to stop. Default: `false`.</td>
      <td>true or false (Default: `false`)</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-stopinstances-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object containing the `StoppingInstances` array with each instance's `InstanceId`, `CurrentState`, and `PreviousState`. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/stop_instances.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed, for example `"An error occurred (InvalidInstanceID.Malformed) when calling the StopInstances operation: The instance ID 'i-123456789' is malformed"`. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-stopinstances-example">
The following example stops a list of EC2 instances in a specified region using a role ARN.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ec2_stop_instance
description: ''
workflowInputs:
  arnRole:
    type: String
  region:
    type: String
    defaultValue: us-west-2
  instanceIds:
    type: list
    defaultValue: ["i-123456789abcdef0"]
steps:
  - name: aws_stop_instances_1
    type: action
    action: aws.ec2.stopInstances
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: ${{ .workflowInputs.region }}
      instanceIds: ${{ .workflowInputs.instanceIds }}
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Reboot EC2 instances

The action identifier is `aws.ec2.rebootInstances`.

Requests a reboot of the specified instances. This operation is asynchronous; it only queues a request to reboot the specified instances. The operation succeeds if the instances are valid and belong to you. Requests to reboot terminated instances are ignored.

If an instance does not cleanly shut down within a few minutes, Amazon EC2 performs a hard reboot.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-rebootinstances-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-rebootinstances-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-rebootinstances-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-rebootinstances-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the instances reside.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**instanceIds**</td>
      <td>List</td>
      <td>**Required.** A list of instance IDs to reboot.</td>
      <td>`"[\"i-0123456789abcdef0\", \"i-0fedcba9876543210\"]"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-rebootinstances-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/reboot_instances.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-rebootinstances-example">
The following example reboots a list of EC2 instances in a specified region using a role ARN.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: reboot-ec2-test
description: ''
workflowInputs:
  arnRole:
    type: String
  region:
    type: String
    defaultValue: us-west-2
  instanceIds:
    type: list
    defaultValue: ["i-123456789abcdef0"]
steps:
  - name: aws_ec2_rebootInstances_1
    type: action
    action: aws.ec2.rebootInstances
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: ${{ .workflowInputs.region }}
      instanceIds: ${{ .workflowInputs.instanceIds }}
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Terminate EC2 instances

The action identifier is `aws.ec2.terminateInstances`.

Shuts down the specified instances. This operation is [idempotent](https://docs.aws.amazon.com/ec2/latest/devguide/ec2-api-idempotency.html); if you terminate an instance more than once, each call succeeds.

If you specify multiple instances and the request fails (for example, because of a single incorrect instance ID), none of the instances are terminated.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-terminateinstances-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-terminateinstances-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-terminateinstances-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-terminateinstances-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the instances reside.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**instanceIds**</td>
      <td>List</td>
      <td>**Required.** A list of instance IDs to terminate.</td>
      <td>`"[\"i-0123456789abcdef0\", \"i-0fedcba9876543210\"]"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-terminateinstances-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object containing the `TerminatingInstances` array with each instance's `InstanceId`, `CurrentState`, and `PreviousState`. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/terminate_instances.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed, for example `"An error occurred (InvalidInstanceID.Malformed) when calling the TerminateInstances operation: The instance ID 'i-012345678' is malformed"`. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-terminateinstances-example">
The following example terminates a list of EC2 instances in a specified region using a role ARN.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ec2-terminate-test
description: ''
workflowInputs:
  arnRole:
    type: String
  region:
    type: String
    defaultValue: us-west-2
  instanceIds:
    type: list
    defaultValue: ["i-123456789abcdef0"]
steps:
  - name: aws_ec2_terminateInstances_1
    type: action
    action: aws.ec2.terminateInstances
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: ${{ .workflowInputs.region }}
      instanceIds: ${{ .workflowInputs.instanceIds }}
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Create an EBS snapshot

The action identifier is `aws.ec2.createSnapshot`.

Creates a snapshot of an EBS volume and stores it in Amazon S3. You can use snapshots for backups, to make copies of EBS volumes, and save data before shutting down an instance.

The location of the source EBS volume determines where you can create the snapshot.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-createsnapshot-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-createsnapshot-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-createsnapshot-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-createsnapshot-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the EBS volume resides.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**volumeId**</td>
      <td>String</td>
      <td>**Required.** The ID of the EBS volume to snapshot.</td>
      <td>`"vol-0123456789abcdef0"`</td>
    </tr>
    <tr>
      <td>**description**</td>
      <td>String</td>
      <td>**Optional.** A human-readable description for the snapshot.</td>
      <td>`"This is a test snapshot"`</td>
    </tr>
    <tr>
      <td>**outpostArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the Outpost on which to create the snapshot. Required only when creating a snapshot on an Outpost.</td>
      <td>`"arn:aws:ec2:us-east-1:123456789012:outpost/op-1a2b3c4d5e6f7g8h9"`</td>
    </tr>
    <tr>
      <td>**tagSpecifications**</td>
      <td>List</td>
      <td>**Optional.** A list of tag specification objects to apply to the snapshot at creation time.</td>
      <td>`[{"ResourceType":"snapshot","Tags":[{"Key":"testKey","Value":"testValue"}]}]`</td>
    </tr>
    <tr>
      <td>**location**</td>
      <td>String</td>
      <td>**Optional.** The snapshot copy location. Use `"regional"` to store the snapshot in the same region as the volume.</td>
      <td>`"regional"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-createsnapshot-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>The full AWS API response object containing snapshot metadata such as `SnapshotId`, `VolumeId`, `State`, `Progress`, `StartTime`, `OwnerId`, `Description`, `VolumeSize`, `Encrypted`, and other fields. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/create_snapshot.html) for the complete response syntax.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-createsnapshot-example">
The following example creates a snapshot of an EBS volume and applies a tag to identify it.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ec2-createSnapshot
description: ''
workflowInputs:
  arnRole:
    type: String
  region:
    type: String
    defaultValue: us-east-2
  volumeId:
    type: String
    defaultValue: vol-0123456789abcdef0
steps:
  - name: aws_ec2_createSnapshot_1
    type: action
    action: aws.ec2.createSnapshot
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: ${{ .workflowInputs.region }}
      volumeId: ${{ .workflowInputs.volumeId }}
      description: Automated backup snapshot
      tagSpecifications:
        - ResourceType: snapshot
          Tags:
            - Key: Name
              Value: backup-snapshot
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## Delete an EBS snapshot

The action identifier is `aws.ec2.deleteSnapshot`.

Deletes the specified snapshot.

You cannot delete a snapshot of the root device of an EBS volume used by a registered AMI. You must first deregister the AMI before you can delete the snapshot.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ec2-deletesnapshot-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ec2-deletesnapshot-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ec2-deletesnapshot-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ec2-deletesnapshot-inputs">
The following table describes all available input fields for this action.
<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** The ARN of the IAM role to assume for this request. Takes precedence over static credentials if provided. At least one AWS credential (short-term, long-term, or role) must be provided.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** The AWS access key ID for static credential authentication.</td>
      <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** The AWS secret access key for static credential authentication.</td>
      <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** The AWS session token for temporary credential authentication.</td>
      <td>`${{ :secrets:<awsSessionToken> }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the snapshot resides.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**snapshotId**</td>
      <td>String</td>
      <td>**Required.** The ID of the snapshot to delete.</td>
      <td>`"snapshot-id-1"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** A list of JMESPath selector expressions to extract specific fields from the action output.</td>
      <td>`[{\"name\": \"response\", \"expression\": \".response\"}, {\"name\": \"success\", \"expression\": \".success\"}, {\"name\": \"errorMessage\", \"expression\": \".errorMessage\"}]`</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-deletesnapshot-outputs">
The following table describes all output fields returned by this action.
<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**response**</td>
      <td>Object</td>
      <td>No response body is returned when `success` is `true`. See [boto3 documentation](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/ec2/client/delete_snapshot.html) for details.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully. Returns `true` on success, `false` on failure.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Contains the error message if the action failed, for example `"Failed to delete snapshot"`. Empty string on success.</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
    <TabsPageItem id="ec2-deletesnapshot-example">
The following example deletes an EBS snapshot by ID using a role ARN.
<table>
  <thead><tr><th>Workflow example</th></tr></thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ec2-deleteSnapshot-test
description: ''
workflowInputs:
  arnRole:
    type: String
  region:
    type: String
    defaultValue: us-west-2
  snapshotId:
    type: String
    defaultValue: snapshot-id-1
steps:
  - name: aws_ec2_deleteSnapshot_1
    type: action
    action: aws.ec2.deleteSnapshot
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: ${{ .workflowInputs.region }}
      snapshotId: ${{ .workflowInputs.snapshotId }}
    next: end
```

</td>
    </tr>
  </tbody>
</table>
    </TabsPageItem>
  </TabsPages>
</Tabs>

## What's next

- [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) — Configure IAM users, IAM roles, and static or session credentials for use with workflow automation AWS actions.
- [AWS Execute API action](/docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-execute-api/) — Use the generic AWS Execute API action to call any AWS service endpoint not covered by a dedicated action.
- [Workflow examples](/docs/workflow-automation/workflow-examples/) — Browse end-to-end workflow examples that combine AWS actions with other automation steps.
