---
title: "AWS Step Functions actions"
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS Step Functions actions
metaDescription: "A reference for AWS Step Functions actions available in the workflow automation actions catalog. Use these actions to start, stop, redrive, and inspect Step Functions executions and state machines."
redirects:
  - /docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-stepfunctions
freshnessValidatedDate: 2026-09-16
---

This page provides a reference for AWS Step Functions actions available in the workflow automation actions catalog. Use these actions to start, stop, redrive, and inspect Step Functions executions and state machines from within your workflow definitions.

## Prerequisites

Before using AWS Step Functions actions in workflow automation, ensure you have:

  * An AWS account with appropriate permissions.
  * AWS credentials configured (IAM user credentials, IAM role ARN, or session credentials).
  * The necessary IAM permissions for Step Functions operations.

See [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) for information on how to create IAM users and IAM roles, and set up static and session AWS credentials for integration with workflow automation AWS actions.

## Required IAM permissions

The permissions you need depend on which Step Functions actions your workflow calls. Use the example below as a template for a least-privilege policy covering all seven actions.

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "states:StartExecution",
        "states:StopExecution",
        "states:RedriveExecution",
        "states:DescribeExecution",
        "states:ListExecutions",
        "states:DescribeStateMachine",
        "states:ListStateMachines"
      ],
      "Resource": "arn:aws:states:<region>:<account-id>:*"
    }
  ]
}
```

<Callout variant="important">
  Replace `<region>` and `<account-id>` with your actual values, and restrict the `Resource` to specific state machine or execution ARNs where possible.
</Callout>

## Step Functions actions

<CollapserGroup>
  <Collapser
    id="aws.stepFunctions.startExecution"
    title="Start a Step Functions execution"
  >
  Starts an execution of an AWS Step Functions state machine.

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-startexecution-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-startexecution-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-startexecution-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-startexecution-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**stateMachineArn**</td>
              <td>Required</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService`</td>
            </tr>
            <tr>
              <td>**name**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`incident-42` — must be unique per state machine; auto-generated (UUID) if omitted</td>
            </tr>
            <tr>
              <td>**input**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`'{"instanceId": "i-0123456789abcdef0"}'` — JSON string</td>
            </tr>
            <tr>
              <td>**traceHeader**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`Root=1-5e1b2f1a-3c1a1a1a1a1a1a1a1a1a1a1a` — X-Ray trace header</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "executionArn", "expression": ".response.executionArn"}]`</td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>

      <TabsPageItem id="sfn-startexecution-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "executionArn": "arn:aws:states:us-east-1:123456789012:execution:RestartUnhealthyService:incident-42",
  "startDate": "2026-08-24T09:00:00.000Z"
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - On `STANDARD` state machines, this action is idempotent by `name` + `input` for up to 90 days — reusing a running execution's name with different input returns `ExecutionAlreadyExists`. On `EXPRESS` state machines, it is not idempotent and names can be reused immediately.
          - Use `selectors` to extract specific fields from the response as named outputs.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-startexecution-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: start-sfn-remediation
description: 'Starts a Step Functions remediation execution'

workflowInputs:
  arnRole:
    type: String
  stateMachineArn:
    type: String
  incidentId:
    type: String

steps:
  - name: startRemediation
    type: action
    action: aws.stepFunctions.startExecution
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      stateMachineArn: ${{ .workflowInputs.stateMachineArn }}
      name: incident-${{ .workflowInputs.incidentId }}
      input: '{"incidentId": "${{ .workflowInputs.incidentId }}"}'
      selectors:
        - name: executionArn
          expression: '.response.executionArn'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>

  <Collapser
    id="aws.stepFunctions.stopExecution"
    title="Stop a Step Functions execution"
  >
  Stops a running AWS Step Functions execution.

  <Callout variant="important">
    This action is only supported on `STANDARD` state machines. It cannot stop `EXPRESS` executions.
  </Callout>

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-stopexecution-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-stopexecution-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-stopexecution-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-stopexecution-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**executionArn**</td>
              <td>Required</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:execution:RestartUnhealthyService:incident-42`</td>
            </tr>
            <tr>
              <td>**error**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`TimeoutFromWorkflowAutomation` — short error code</td>
            </tr>
            <tr>
              <td>**cause**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`Remediation exceeded SLA, aborted by workflow` — human-readable detail</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "stopDate", "expression": ".response.stopDate"}]`</td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>

      <TabsPageItem id="sfn-stopexecution-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "stopDate": "2026-08-24T09:15:30.000Z"
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - This action is **not supported** on `EXPRESS` state machines.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-stopexecution-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: abort-stuck-sfn-run
description: 'Aborts a Step Functions execution that exceeded its SLA'

workflowInputs:
  arnRole:
    type: String
  executionArn:
    type: String

steps:
  - name: abortStuckRun
    type: action
    action: aws.stepFunctions.stopExecution
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      executionArn: ${{ .workflowInputs.executionArn }}
      error: 'TimeoutFromWorkflowAutomation'
      cause: 'Remediation exceeded 10 minute SLA, aborted by workflow'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>

  <Collapser
    id="aws.stepFunctions.redriveExecution"
    title="Redrive a Step Functions execution"
  >
  Redrives a failed, timed-out, or aborted Step Functions execution, resuming it from the last unsuccessful state.

  <Callout variant="important">
    This action is only supported on `STANDARD` state machines. It cannot redrive `EXPRESS` executions.
  </Callout>

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-redriveexecution-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-redriveexecution-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-redriveexecution-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-redriveexecution-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**executionArn**</td>
              <td>Required</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:execution:RestartUnhealthyService:incident-42`</td>
            </tr>
            <tr>
              <td>**clientToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`a1b2c3d4-e5f6-7890-abcd-ef1234567890` — idempotency token; auto-generated by the SDK if omitted</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "redriveDate", "expression": ".response.redriveDate"}]`</td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>

      <TabsPageItem id="sfn-redriveexecution-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "redriveDate": "2026-08-24T09:20:00.000Z"
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - This action is **not supported** on `EXPRESS` state machines.
          - An execution can only be redriven if it has not `SUCCEEDED`, is within 14 days of completion, and has fewer than 24,999 history events.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-redriveexecution-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: retry-failed-sfn-execution
description: 'Redrives a failed Step Functions execution from its last unsuccessful state'

workflowInputs:
  arnRole:
    type: String
  executionArn:
    type: String

steps:
  - name: retryFailedRemediation
    type: action
    action: aws.stepFunctions.redriveExecution
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      executionArn: ${{ .workflowInputs.executionArn }}
      selectors:
        - name: redriveDate
          expression: '.response.redriveDate'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>

  <Collapser
    id="aws.stepFunctions.describeExecution"
    title="Describe a Step Functions execution"
  >
  Returns details about a Step Functions execution, including its status, input, output, and timestamps.

  <Callout variant="important">
    This action supports `STANDARD` state machine executions. `EXPRESS` executions are not directly describable unless they were dispatched by a Map Run.
  </Callout>

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-describeexecution-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-describeexecution-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-describeexecution-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-describeexecution-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**executionArn**</td>
              <td>Required</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:execution:RestartUnhealthyService:incident-42`</td>
            </tr>
            <tr>
              <td>**includedData**</td>
              <td>Optional</td>
              <td>Enum</td>
              <td>`ALL_DATA` or `METADATA_ONLY`</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "status", "expression": ".response.status"}]`</td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>

      <TabsPageItem id="sfn-describeexecution-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "executionArn": "arn:aws:states:us-east-1:123456789012:execution:RestartUnhealthyService:incident-42",
  "stateMachineArn": "arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService",
  "name": "incident-42",
  "status": "SUCCEEDED",
  "startDate": "2026-08-24T09:00:00.000Z",
  "stopDate": "2026-08-24T09:02:15.000Z",
  "input": "{\"incidentId\": \"42\"}",
  "output": "{\"result\": \"resolved\"}",
  "redriveCount": 0,
  "redriveStatus": "NOT_REDRIVABLE"
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - On `FAILED`, `TIMED_OUT`, or `ABORTED` executions, `output` is absent and `error` / `cause` are populated instead.
          - `redriveStatus` explains whether the execution can be redriven. When it cannot, `redriveStatusReason` provides the explanation.
          - Use `METADATA_ONLY` to avoid needing `kms:Decrypt` when you only need execution metadata and the definition is KMS-encrypted.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-describeexecution-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: check-sfn-execution-status
description: 'Polls the status of a Step Functions execution'

workflowInputs:
  arnRole:
    type: String
  executionArn:
    type: String

steps:
  - name: checkStatus
    type: action
    action: aws.stepFunctions.describeExecution
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      executionArn: ${{ .workflowInputs.executionArn }}
      selectors:
        - name: status
          expression: '.response.status'
        - name: output
          expression: '.response.output'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>

  <Collapser
    id="aws.stepFunctions.listExecutions"
    title="List Step Functions executions"
  >
  Lists executions of an AWS Step Functions state machine or map run, optionally filtered by status.

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-listexecutions-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-listexecutions-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-listexecutions-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-listexecutions-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**stateMachineArn**</td>
              <td>Optional*</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService` — provide this or `mapRunArn`, not both</td>
            </tr>
            <tr>
              <td>**mapRunArn**</td>
              <td>Optional*</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:mapRun:MyStateMachine/abcd1234` — provide this or `stateMachineArn`, not both</td>
            </tr>
            <tr>
              <td>**statusFilter**</td>
              <td>Optional</td>
              <td>Enum</td>
              <td>`RUNNING` | `SUCCEEDED` | `FAILED` | `TIMED_OUT` | `ABORTED` | `PENDING_REDRIVE`</td>
            </tr>
            <tr>
              <td>**maxResults**</td>
              <td>Optional</td>
              <td>Int</td>
              <td>`20` — default 100, max 1000</td>
            </tr>
            <tr>
              <td>**nextToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>Pagination token from a previous call's `response.nextToken`; expires after 24 hours</td>
            </tr>
            <tr>
              <td>**redriveFilter**</td>
              <td>Optional</td>
              <td>Enum</td>
              <td>`REDRIVEN` or `NOT_REDRIVEN`</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "executions", "expression": ".response.executions"}]`</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          Provide `stateMachineArn` **or** `mapRunArn` — not both. `statusFilter: PENDING_REDRIVE` requires `mapRunArn`; combining it with `stateMachineArn` returns a validation error from AWS.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-listexecutions-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "executions": [
    {
      "executionArn": "arn:aws:states:us-east-1:123456789012:execution:RestartUnhealthyService:incident-42",
      "stateMachineArn": "arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService",
      "name": "incident-42",
      "status": "RUNNING",
      "startDate": "2026-08-24T09:00:00.000Z"
    }
  ],
  "nextToken": null
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - `EXPRESS` state machines are not directly listable. To list `EXPRESS` child executions of a Distributed Map, specify `mapRunArn` instead of `stateMachineArn`.
          - Results are sorted most-recent first. `nextToken` is present only when more pages are available.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-listexecutions-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: find-running-sfn-executions
description: 'Lists running executions for a state machine'

workflowInputs:
  arnRole:
    type: String
  stateMachineArn:
    type: String

steps:
  - name: findRunningExecutions
    type: action
    action: aws.stepFunctions.listExecutions
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      stateMachineArn: ${{ .workflowInputs.stateMachineArn }}
      statusFilter: RUNNING
      maxResults: 20
      selectors:
        - name: executions
          expression: '.response.executions'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>

  <Collapser
    id="aws.stepFunctions.describeStateMachine"
    title="Describe a state machine"
  >
  Returns the definition, IAM role, logging configuration, and type of an AWS Step Functions state machine.

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-describestatemachine-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-describestatemachine-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-describestatemachine-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-describestatemachine-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**stateMachineArn**</td>
              <td>Required</td>
              <td>String</td>
              <td>`arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService` — also accepts a version ARN</td>
            </tr>
            <tr>
              <td>**includedData**</td>
              <td>Optional</td>
              <td>Enum</td>
              <td>`ALL_DATA` or `METADATA_ONLY`</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "type", "expression": ".response.type"}]`</td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>

      <TabsPageItem id="sfn-describestatemachine-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "stateMachineArn": "arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService",
  "name": "RestartUnhealthyService",
  "status": "ACTIVE",
  "definition": "{\"StartAt\": \"RestartTask\", \"States\": {...}}",
  "roleArn": "arn:aws:iam::123456789012:role/StepFunctionsExecutionRole",
  "type": "STANDARD",
  "creationDate": "2026-01-15T00:00:00.000Z"
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - Use `METADATA_ONLY` to skip returning the state machine definition and avoid needing `kms:Decrypt` when the definition is KMS-encrypted. Note: this does not apply when calling with a labeled/versioned ARN for an encrypted state machine that contains a Distributed Map state.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-describestatemachine-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: get-sfn-state-machine-type
description: 'Retrieves the type and definition of a state machine'

workflowInputs:
  arnRole:
    type: String
  stateMachineArn:
    type: String

steps:
  - name: getDefinition
    type: action
    action: aws.stepFunctions.describeStateMachine
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      stateMachineArn: ${{ .workflowInputs.stateMachineArn }}
      selectors:
        - name: type
          expression: '.response.type'
        - name: definition
          expression: '.response.definition'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>

  <Collapser
    id="aws.stepFunctions.listStateMachines"
    title="List state machines"
  >
  Lists AWS Step Functions state machines in an account and region.

  <Tabs>
    <TabsBar>
      <TabsBarItem id="sfn-liststatemachines-inputs">
        Inputs
      </TabsBarItem>
      <TabsBarItem id="sfn-liststatemachines-outputs">
        Outputs
      </TabsBarItem>
      <TabsBarItem id="sfn-liststatemachines-example">
        Example
      </TabsBarItem>
    </TabsBar>

    <TabsPages>
      <TabsPageItem id="sfn-liststatemachines-inputs">
        <table>
          <thead>
            <tr>
              <th>Input field</th>
              <th>Optionality</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**awsRoleArn**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
            </tr>
            <tr>
              <td>**awsAccessKeyId**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsAccessKeyId> }}`</td>
            </tr>
            <tr>
              <td>**awsSecretAccessKey**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSecretAccessKey> }}`</td>
            </tr>
            <tr>
              <td>**awsSessionToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>`${{ :secrets:<awsSessionToken> }}`</td>
            </tr>
            <tr>
              <td>**region**</td>
              <td>Required</td>
              <td>String</td>
              <td>`us-east-1`</td>
            </tr>
            <tr>
              <td>**maxResults**</td>
              <td>Optional</td>
              <td>Int</td>
              <td>`50` — default 100, max 1000</td>
            </tr>
            <tr>
              <td>**nextToken**</td>
              <td>Optional</td>
              <td>String</td>
              <td>Pagination token from a previous call's `response.nextToken`; expires after 24 hours</td>
            </tr>
            <tr>
              <td>**selectors**</td>
              <td>Optional</td>
              <td>List</td>
              <td>`[{"name": "stateMachines", "expression": ".response.stateMachines"}]`</td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>

      <TabsPageItem id="sfn-liststatemachines-outputs">
        <table>
          <thead>
            <tr>
              <th>Output field</th>
              <th>Type</th>
              <th>Example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>**response**</td>
              <td>Object</td>
              <td>
```json
{
  "stateMachines": [
    {
      "stateMachineArn": "arn:aws:states:us-east-1:123456789012:stateMachine:RestartUnhealthyService",
      "name": "RestartUnhealthyService",
      "type": "STANDARD",
      "creationDate": "2026-01-15T00:00:00.000Z"
    }
  ],
  "nextToken": null
}
```
              </td>
            </tr>
            <tr>
              <td>**success**</td>
              <td>Boolean</td>
              <td>`true | false`</td>
            </tr>
            <tr>
              <td>**errorMessage**</td>
              <td>String</td>
              <td>Error message on failure.</td>
            </tr>
          </tbody>
        </table>

        <Callout variant="important">
          - At least one of the AWS credentials (`awsRoleArn`, or `awsAccessKeyId` + `awsSecretAccessKey`) must be provided. `awsRoleArn` takes precedence.
          - `nextToken` is present only when more pages are available. Pass it as `nextToken` on a subsequent call to retrieve the next page.
        </Callout>
      </TabsPageItem>

      <TabsPageItem id="sfn-liststatemachines-example">
        <table>
          <thead>
            <tr>
              <th>Workflow example</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
```yaml
name: discover-sfn-state-machines
description: 'Lists state machines in the account'

workflowInputs:
  arnRole:
    type: String

steps:
  - name: discoverStateMachines
    type: action
    action: aws.stepFunctions.listStateMachines
    version: '1'
    inputs:
      awsRoleArn: ${{ .workflowInputs.arnRole }}
      region: us-east-1
      maxResults: 50
      selectors:
        - name: stateMachines
          expression: '.response.stateMachines'
```
              </td>
            </tr>
          </tbody>
        </table>
      </TabsPageItem>
    </TabsPages>
  </Tabs>
  </Collapser>
</CollapserGroup>

## What's next

- **[AWS credentials setup](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/)**: Configure IAM roles and access keys for use with AWS actions.
- **[AWS Execute API](/docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-execute-api)**: Call any AWS API directly when a dedicated action is not available.
- **[Workflow examples](/docs/workflow-automation/workflow-examples/workflow-examples)**: See real-world workflow examples using AWS actions.
