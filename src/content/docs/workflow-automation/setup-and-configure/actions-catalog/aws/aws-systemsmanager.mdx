---
title: "AWS Systems Manager actions"
tags:
  - workflow automation
  - workflow
  - workflow automation actions
  - AWS actions
  - AWS Systems Manager actions
metaDescription: "A reference for AWS Systems Manager actions available in the workflow automation actions catalog."
redirects:
  - /docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-systemsmanager
freshnessValidatedDate: 2026-09-16
---

This page provides a reference for AWS Systems Manager actions available in the workflow automation actions catalog. Use these actions to create, run, and monitor Systems Manager automation documents.

## Prerequisites

Before using AWS actions in workflow automation, ensure you have:

* An AWS account with appropriate permissions.
* AWS credentials configured (IAM user credentials, IAM role ARN, or session credentials).
* The necessary IAM permissions for the specific AWS services you plan to use.

See [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) for information on how to create IAM users and IAM roles, and set up static and session AWS credentials for integration with workflow automation AWS actions.

## Create or update a Systems Manager document

The action identifier is `aws.systemsManager.writeDocument`.

Write an SSM document to the AWS account based on the AWS credentials passed in the action input. See [AWS Systems Manager Documentation](https://docs.aws.amazon.com/systems-manager/?id=docs_gateway).

<Tabs>
  <TabsBar>
    <TabsBarItem id="ssm-writedocument-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ssm-writedocument-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ssm-writedocument-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ssm-writedocument-inputs">

The following table describes all available input fields for this action.

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when authenticating with AWS. Use as an alternative to static key credentials.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID. Provide together with `awsSecretAccessKey` for static IAM user credentials. If session credentials are required, also provide `awsSessionToken`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsAccessKeyId }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key. Provide together with `awsAccessKeyId`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsSecretAccessKey }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** AWS session token for temporary session credentials. Required when using short-lived credentials alongside `awsAccessKeyId` and `awsSecretAccessKey`.</td>
      <td>`${{ :secrets:awsSessionToken }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the SSM document will be created.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**documentName**</td>
      <td>String</td>
      <td>**Required.** The name to assign to the SSM document.</td>
      <td>`documentName: "my-ssm-document"`</td>
    </tr>
    <tr>
      <td>**documentType**</td>
      <td>String</td>
      <td>**Optional.** The type of SSM document. See valid values in the [AWS API reference](https://docs.aws.amazon.com/systems-manager/latest/APIReference/API_DocumentDescription.html#systemsmanager-Type-DocumentDescription-Status:text=Required%3A%20No-,Status,-The%20status%20of).</td>
      <td>`documentType: "Command"`</td>
    </tr>
    <tr>
      <td>**documentFormat**</td>
      <td>String</td>
      <td>**Optional.** The format of the document content. See valid values in the [AWS API reference](https://docs.aws.amazon.com/systems-manager/latest/APIReference/API_DocumentDescription.html#systemsmanager-Type-DocumentDescription-Status:text=Required%3A%20No-,Status,-The%20status%20of).</td>
      <td>`documentFormat: "YAML"`</td>
    </tr>
    <tr>
      <td>**documentContent**</td>
      <td>String</td>
      <td>**Required.** The content of the SSM document. See the Example tab for a complete document definition.</td>
      <td>See Example tab.</td>
    </tr>
    <tr>
      <td>**override**</td>
      <td>Boolean</td>
      <td>**Optional.** When `true` (default), always writes the document with the provided name even if one already exists. When `false`, if a document with the provided `documentName` already exists, the action returns `success: false` and `errorMessage: Document already exists. Please enable override if you want to update the existing document.`</td>
      <td>`override: true`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "documentName", "expression": ".documentName"}, {"name": "documentType", "expression": ".documentType"}, {"name": "documentStatus", "expression": ".documentStatus"}, {"name": "success", "expression": ".success"}]`</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-writedocument-outputs">

The following table describes all output fields returned by this action.

<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**documentName**</td>
      <td>String</td>
      <td>The name of the created or updated SSM document.</td>
    </tr>
    <tr>
      <td>**documentVersion**</td>
      <td>String</td>
      <td>The version number of the document after the write operation.</td>
    </tr>
    <tr>
      <td>**documentType**</td>
      <td>String</td>
      <td>The type of the SSM document, for example `Command` or `Automation`.</td>
    </tr>
    <tr>
      <td>**documentStatus**</td>
      <td>String</td>
      <td>The current status of the document. See valid status values in the [AWS API reference](https://docs.aws.amazon.com/systems-manager/latest/APIReference/API_DocumentDescription.html#systemsmanager-Type-DocumentDescription-Status:text=Required%3A%20No-,Status,-The%20status%20of).</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error details if the action did not complete successfully.</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-writedocument-example">

The following example creates an SSM document that lists all Lambda function names, then runs it as a complete automation workflow.

<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>

SSM document content (Lambda list functions):

```yaml
schemaVersion: '0.3'
description: List all Lambda function names.
mainSteps:
  - name: ExecuteAwsApi
    action: aws:executeAwsApi
    isEnd: true
    inputs:
      Service: lambda
      Api: ListFunctions
    outputs:
      - Name: resultFunctionName
        Selector: $..FunctionName
        Type: StringList
outputs:
  - ExecuteAwsApi.resultFunctionName
```

Complete workflow definition:

```yaml
name: aws-api
workflowInputs:
  key:
    type: String
    defaultValue: "${{ :secrets:awsAccessKeyId }}"
  access:
    type: String
    defaultValue: "${{ :secrets:awsSecretAccessKey }}"
  token:
    type: String
    defaultValue: "${{ :secrets:awsSessionToken }}"
  region:
    type: String
    defaultValue: us-east-2
steps:
  - name: createSsmDocument
    type: action
    action: aws.systemsManager.writeDocument
    version: 1
    inputs:
      awsAccessKeyId: ${{ .workflowInputs.key }}
      awsSecretAccessKey: ${{ .workflowInputs.access }}
      awsSessionToken: ${{ .workflowInputs.token }}
      region: ${{ .workflowInputs.region }}
      documentName: "LambdaListFunctionNames"
      documentContent: |
        schemaVersion: '0.3'
        description: List all Lambda function names.
        mainSteps:
          - name: ExecuteAwsApi
            action: aws:executeAwsApi
            isEnd: true
            inputs:
              Service: lambda
              Api: ListFunctions
            outputs:
              - Name: resultFunctionName
                Selector: $..FunctionName
                Type: StringList
        outputs:
          - ExecuteAwsApi.resultFunctionName

  - name: generateIdempotencyToken
    type: action
    action: utils.uuid.generate
    version: 1

  - name: start1
    type: action
    action: aws.systemsManager.startAutomation
    version: 1
    inputs:
      awsAccessKeyId: ${{ .workflowInputs.key }}
      awsSecretAccessKey: ${{ .workflowInputs.access }}
      awsSessionToken: ${{ .workflowInputs.token }}
      region: ${{ .workflowInputs.region }}
      documentName: "${{ .steps.createSsmDocument.outputs.documentName }}"
      idempotencyToken: ${{ .steps.generateIdempotencyToken.outputs.uuid }}

  - name: waitForCompletion
    type: action
    action: aws.systemsManager.waitForAutomationStatus
    version: 1
    inputs:
      awsAccessKeyId: ${{ .workflowInputs.key }}
      awsSecretAccessKey: ${{ .workflowInputs.access }}
      awsSessionToken: ${{ .workflowInputs.token }}
      region: ${{ .workflowInputs.region }}
      automationExecutionId: ${{ .steps.start1.outputs.automationExecutionId }}
      # Optional, default is Success and Failed
      automationExecutionStatuses:
        - "Success"
        - "Failed"
      timeout: 60

  - name: hasCompleted
    type: switch
    switch:
      - condition: ${{ .steps.waitForCompletion.outputs.automationExecutionStatus == "Failed" }}
        next: displayError
      - condition: ${{ .steps.waitForCompletion.outputs.automationExecutionStatus == "Success" }}
        next: displaySuccess
    next: displayUnexpected

  - name: displayUnexpected
    type: action
    action: newrelic.instrumentation.log
    version: 1
    inputs:
      message: "Unexpected status ${{ .steps.waitForCompletion.outputs.automationExecutionStatus | tojson }}"
    next: cleanupSsmDocument

  - name: displaySuccess
    type: action
    action: newrelic.instrumentation.log
    version: 1
    inputs:
      message: "This is all the lambda function names on the region ${{ .workflowInputs.region }}:${{ .steps.waitForCompletion.outputs.automationExecutionOutputs.ExecuteAwsApi.resultFunctionName | join(\",\") }}"
    next: cleanupSsmDocument

  - name: displayError
    type: action
    action: newrelic.instrumentation.log
    version: 1
    inputs:
      message: "Error while executing document ${{ .steps.createSsmDocument.outputs.documentName }}, detail: ${{ .steps.waitForCompletion.outputs.errorMessage }}"
    next: cleanupSsmDocument

  - name: cleanupSsmDocument
    type: action
    action: aws.systemsManager.deleteDocument
    version: 1
    inputs:
      awsAccessKeyId: ${{ .workflowInputs.key }}
      awsSecretAccessKey: ${{ .workflowInputs.access }}
      awsSessionToken: ${{ .workflowInputs.token }}
      region: ${{ .workflowInputs.region }}
      documentName: ${{ .steps.createSsmDocument.outputs.documentName }}
```

      </td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
  </TabsPages>
</Tabs>

## Delete a Systems Manager document

The action identifier is `aws.systemsManager.deleteDocument`.

Delete an AWS SSM document in the AWS account based on the credentials passed in the action input. See [AWS Systems Manager Documentation](https://docs.aws.amazon.com/systems-manager/latest/userguide/documents.html).

<Tabs>
  <TabsBar>
    <TabsBarItem id="ssm-deletedocument-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ssm-deletedocument-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ssm-deletedocument-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ssm-deletedocument-inputs">

The following table describes all available input fields for this action.

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when authenticating with AWS. Use as an alternative to static key credentials.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Required.** AWS access key ID for an IAM user. Provide together with `awsSecretAccessKey`. If session credentials are required, also provide `awsSessionToken`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsAccessKeyId }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Required.** AWS secret access key. Provide together with `awsAccessKeyId`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsSecretAccessKey }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** AWS session token for temporary session credentials. Required when using short-lived credentials alongside `awsAccessKeyId` and `awsSecretAccessKey`.</td>
      <td>`${{ :secrets:awsSessionToken }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the SSM document exists.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**documentName**</td>
      <td>String</td>
      <td>**Required.** The name of the SSM document to delete.</td>
      <td>`documentName: "my-ssm-document"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "success", "expression": ".success"}, {"name": "documentName", "expression": ".documentName"}]`</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-deletedocument-outputs">

The following table describes all output fields returned by this action.

<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**documentName**</td>
      <td>String</td>
      <td>The name of the deleted SSM document.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the delete operation completed successfully.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error details if the action did not complete successfully.</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-deletedocument-example">

The following example creates a simple SSM document and then deletes it in a cleanup step.

<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>

```yaml
name: ssm-create-and-delete

workflowInputs:
  key:
    type: String
    defaultValue: "${{ :secrets:awsAccessKeyId }}"
  access:
    type: String
    defaultValue: "${{ :secrets:awsSecretAccessKey }}"
  token:
    type: String
    defaultValue: "${{ :secrets:awsSessionToken }}"
  region:
    type: String
    defaultValue: us-east-2

steps:
  - name: createSsmDocument
    type: action
    action: aws.systemsManager.writeDocument
    version: 1
    inputs:
      awsAccessKeyId: ${{ .workflowInputs.key }}
      awsSecretAccessKey: ${{ .workflowInputs.access }}
      awsSessionToken: ${{ .workflowInputs.token }}
      region: ${{ .workflowInputs.region }}
      documentName: "MyTemporaryDocument"
      documentContent: |
        schemaVersion: '0.3'
        description: A temporary document for demonstration.
        mainSteps:
          - name: ExecuteAwsApi
            action: aws:executeAwsApi
            isEnd: true
            inputs:
              Service: lambda
              Api: ListFunctions
            outputs:
              - Name: resultFunctionName
                Selector: $..FunctionName
                Type: StringList
        outputs:
          - ExecuteAwsApi.resultFunctionName

  - name: deleteSsmDocument
    type: action
    action: aws.systemsManager.deleteDocument
    version: 1
    inputs:
      awsAccessKeyId: ${{ .workflowInputs.key }}
      awsSecretAccessKey: ${{ .workflowInputs.access }}
      awsSessionToken: ${{ .workflowInputs.token }}
      region: ${{ .workflowInputs.region }}
      documentName: ${{ .steps.createSsmDocument.outputs.documentName }}
```

      </td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
  </TabsPages>
</Tabs>

## Start a Systems Manager automation

The action identifier is `aws.systemsManager.startAutomation`.

Start an automation using an AWS SSM document. See [AWS Systems Manager Documentation](https://docs.aws.amazon.com/systems-manager/latest/APIReference/API_StartAutomationExecution.html).

<Tabs>
  <TabsBar>
    <TabsBarItem id="ssm-startautomation-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ssm-startautomation-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ssm-startautomation-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ssm-startautomation-inputs">

The following table describes all available input fields for this action.

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when authenticating with AWS. Use as an alternative to static key credentials.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for static IAM user credentials. Provide together with `awsSecretAccessKey`. If session credentials are required, also provide `awsSessionToken`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsAccessKeyId }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key. Provide together with `awsAccessKeyId`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsSecretAccessKey }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** AWS session token for temporary session credentials. Required when using short-lived credentials alongside `awsAccessKeyId` and `awsSecretAccessKey`.</td>
      <td>`${{ :secrets:awsSessionToken }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the automation will run.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**documentName**</td>
      <td>String</td>
      <td>**Required.** The name of the SSM document to use for the automation.</td>
      <td>`documentName: "my-ssm-document"`</td>
    </tr>
    <tr>
      <td>**parameters**</td>
      <td>Map</td>
      <td>**Optional.** Key-value pairs to pass as parameters to the SSM document.</td>
      <td>

```yaml
parameters:
  myKey: myValue
```

      </td>
    </tr>
    <tr>
      <td>**idempotencyToken**</td>
      <td>UUID</td>
      <td>**Optional.** A client token passed to the AWS SSM start automation call to ensure idempotency. Use `utils.uuid.generate` to produce a unique value per run.</td>
      <td>`idempotencyToken: "any token"`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "automationExecutionId", "expression": ".automationExecutionId"}, {"name": "success", "expression": ".success"}]`</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-startautomation-outputs">

The following table describes all output fields returned by this action.

<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**automationExecutionId**</td>
      <td>String</td>
      <td>The unique identifier for the automation execution. Pass this to `aws.systemsManager.waitForAutomationStatus` to poll for completion.</td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the automation was started successfully.</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error details if the action did not complete successfully.</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-startautomation-example">

The following example shows an SSM document that lists Lambda functions, which can be passed as `documentContent` to `aws.systemsManager.writeDocument` before starting the automation.

<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>

```yaml
schemaVersion: '0.3'
description: List all Lambda function names.
mainSteps:
  - name: ExecuteAwsApi
    action: aws:executeAwsApi
    isEnd: true
    inputs:
      Service: lambda
      Api: ListFunctions
    outputs:
      - Name: resultFunctionName
        Selector: $..FunctionName
        Type: StringList
outputs:
  - ExecuteAwsApi.resultFunctionName
```

      </td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
  </TabsPages>
</Tabs>

## Wait for automation status

The action identifier is `aws.systemsManager.waitForAutomationStatus`.

Wait for an AWS SSM automation execution to reach one of the specified statuses. See [AWS Systems Manager Documentation](https://docs.aws.amazon.com/systems-manager/?id=docs_gateway) for more information.

<Tabs>
  <TabsBar>
    <TabsBarItem id="ssm-waitstatus-inputs">Inputs</TabsBarItem>
    <TabsBarItem id="ssm-waitstatus-outputs">Outputs</TabsBarItem>
    <TabsBarItem id="ssm-waitstatus-example">Example</TabsBarItem>
  </TabsBar>
  <TabsPages>
    <TabsPageItem id="ssm-waitstatus-inputs">

The following table describes all available input fields for this action.

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**awsRoleArn**</td>
      <td>String</td>
      <td>**Optional.** IAM role ARN to assume when authenticating with AWS. Use as an alternative to static key credentials.</td>
      <td>`arn:aws:iam::123456789012:role/my-workflow-role`</td>
    </tr>
    <tr>
      <td>**awsAccessKeyId**</td>
      <td>String</td>
      <td>**Optional.** AWS access key ID for static IAM user credentials. Provide together with `awsSecretAccessKey`. If session credentials are required, also provide `awsSessionToken`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsAccessKeyId }}`</td>
    </tr>
    <tr>
      <td>**awsSecretAccessKey**</td>
      <td>String</td>
      <td>**Optional.** AWS secret access key. Provide together with `awsAccessKeyId`. See [AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials) for instructions.</td>
      <td>`${{ :secrets:awsSecretAccessKey }}`</td>
    </tr>
    <tr>
      <td>**awsSessionToken**</td>
      <td>String</td>
      <td>**Optional.** AWS session token for temporary session credentials. Required when using short-lived credentials alongside `awsAccessKeyId` and `awsSecretAccessKey`.</td>
      <td>`${{ :secrets:awsSessionToken }}`</td>
    </tr>
    <tr>
      <td>**region**</td>
      <td>String</td>
      <td>**Required.** The AWS region where the automation is running.</td>
      <td>`region: "us-east-2"`</td>
    </tr>
    <tr>
      <td>**automationExecutionId**</td>
      <td>String</td>
      <td>**Required.** The execution ID of the automation to poll. Obtain this from the `automationExecutionId` output of `aws.systemsManager.startAutomation`.</td>
      <td>`automationExecutionId: "3143a28d-241c-4abd-a3ca-9c0ff3890241"`</td>
    </tr>
    <tr>
      <td>**automationExecutionStatuses**</td>
      <td>List</td>
      <td>**Optional.** List of automation execution statuses that stop the wait. See valid values in the [AWS AutomationExecution API reference](https://docs.aws.amazon.com/systems-manager/latest/APIReference/API_AutomationExecution.html#AutomationExecutionStatus). Default: `["Success", "Failed"]`.</td>
      <td>`automationExecutionStatuses: ["Success", "Failed"]`</td>
    </tr>
    <tr>
      <td>**timeout**</td>
      <td>Integer</td>
      <td>**Optional.** The maximum number of seconds to wait for the automation to reach one of the expected statuses. Default: `180`. Maximum: `600`. If the timeout is reached, the output contains `success: false` and `errorMessage: Timeout waiting for automation status`.</td>
      <td>`timeout: 180`</td>
    </tr>
    <tr>
      <td>**selectors**</td>
      <td>List</td>
      <td>**Optional.** JMESPath selectors to extract specific fields from the action output.</td>
      <td>`[{"name": "automationExecutionStatus", "expression": ".automationExecutionStatus"}, {"name": "scriptOutput", "expression": ".automationExecutionOutputs.pythonStep.scriptOutput | tojson"}]`</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-waitstatus-outputs">

The following table describes all output fields returned by this action.

<table>
  <thead>
    <tr>
      <th>Output</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**automationExecutionId**</td>
      <td>String</td>
      <td>The execution ID of the automation that was polled.</td>
    </tr>
    <tr>
      <td>**automationExecutionStatus**</td>
      <td>String</td>
      <td>The final status of the automation. On success, this is one of the values passed in `automationExecutionStatuses`. Otherwise, this is `null`.</td>
    </tr>
    <tr>
      <td>**automationExecutionOutputs**</td>
      <td>Map</td>
      <td>

A map of output values from the SSM document steps. Use this to access document outputs in subsequent workflow steps. For example:

```yaml
"automationExecutionOutputs": {
  "ExecuteGetApiResources": {
    "resultResourceId": [
      "pky3cb"
    ]
  },
  "ExecuteListVersionsByFunction": {
    "resultLambdaVersionArn": [
      "arn:aws:lambda:us-east-2:123456789012:function:ApiGwTestFn:1"
    ]
  }
}
```

      </td>
    </tr>
    <tr>
      <td>**success**</td>
      <td>Boolean</td>
      <td>Indicates whether the action completed successfully (that is, the automation reached one of the expected statuses before the timeout).</td>
    </tr>
    <tr>
      <td>**errorMessage**</td>
      <td>String</td>
      <td>Error details if the action did not complete successfully, including timeout messages.</td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
    <TabsPageItem id="ssm-waitstatus-example">

The following example shows an SSM document that can be used with `startAutomation` and then polled using this action.

<table>
  <thead>
    <tr>
      <th>Workflow example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>

```yaml
schemaVersion: '0.3'
description: List all Lambda function names.
mainSteps:
  - name: ExecuteAwsApi
    action: aws:executeAwsApi
    isEnd: true
    inputs:
      Service: lambda
      Api: ListFunctions
    outputs:
      - Name: resultFunctionName
        Selector: $..FunctionName
        Type: StringList
outputs:
  - ExecuteAwsApi.resultFunctionName
```

      </td>
    </tr>
  </tbody>
</table>

    </TabsPageItem>
  </TabsPages>
</Tabs>

## What's next

* [Set up AWS credentials](/docs/workflow-automation/setup-and-configure/set-up-aws-credentials/) — learn how to configure IAM users, IAM roles, and session credentials for use with workflow automation AWS actions.
* [AWS Execute API actions](/docs/workflow-automation/setup-and-configure/actions-catalog/aws/aws-executeapi/) — use the Execute API action to call any AWS service endpoint directly from a workflow step.
* [Workflow automation examples](/docs/workflow-automation/get-started/workflow-automation-examples/) — explore end-to-end workflow examples that combine multiple AWS actions.
