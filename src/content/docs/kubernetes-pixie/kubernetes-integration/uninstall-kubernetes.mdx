---
title: 'Uninstall Kubernetes integration'
tags:
  - Integrations
  - Kubernetes integration
  - Uninstall
metaDescription: "Learn how to uninstall Kubernetes integration."
---

The New Relic Kubernetes integration can be easly uninstalled from your platform. 

Ensure you are using the appropriate context in the machine where you are running Helm and `kubectl`. You can check the available contexts with this command:

```shell
kubectl config get-contexts
```

And switch to the desired context using:

```shell
kubectl config use-context <var>CONTEXT_NAME</var>
```

<CollapserGroup>
  <Collapser
    className="freq-link"
    id="using-helm"
    title="Uninstall Kubernetes using Helm"
  >
  If you used Helm to install the Kubernetes integration, run:

  ```shell
  helm uninstall newrelic-bundle -n newrelic
  ```

  <Callout variant="important">
    The guided installer includes `newrelic-bundle` as the default release name.
  </Callout>
  </Collapser>

  <Collapser
    className="freq-link"
    id="using-manifest"
    title="Uninstall Kubernetes using manifest"
  >
    If you installed Kubernetes integration using a manifest, use the same manifest to uninstall it:

    ```shell
    kubectl delete -f newrelic-infrastructure.yaml
    ```
  </Collapser>

<Collapser
    className="freq-link"
    id="remove-pods"
    title="Remove all the pods"
  >
    Run this command if you want to remove all of the pods and everything under the New Relic namespace:

    ```shell
    kubectl delete ns newrelic
    ```
  </Collapser>

<Collapser
    className="freq-link"
    id=""
    title="Remove kubectl"
  >
    For kubectl, you can execute this one:

    ```shell
    kubectl delete -f <newrelic-manifest.yaml>
    ```
  </Collapser>
</CollapserGroup>