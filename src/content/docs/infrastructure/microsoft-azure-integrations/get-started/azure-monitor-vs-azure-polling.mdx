---
title: Azure Monitor vs. Azure Polling
tags:
  - Integrations
  - Microsoft Azure integrations
  - Get started
metaDescription: Comparison of Azure Monitor and Azure Polling integration methods for collecting Azure metrics in New Relic.
freshnessValidatedDate: never
---

<Callout variant="warning">
  Azure Monitor is the preferred integration method for collecting Azure metrics at scale. Azure Polling will be deprecated soon.
</Callout>

## Comparison overview

<table>
  <thead>
    <tr>
      <th>Azure Polling</th>
      <th>Azure Monitor</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**Integration setup**</td>
      <td>Requires a separate integration per Azure service</td>
      <td>Single integration covers all supported resources</td>
    </tr>
    <tr>
      <td>**New service support**</td>
      <td>Requires building and maintaining a new integration</td>
      <td>New Azure services are [available](https://learn.microsoft.com/en-us/azure/azure-monitor/reference/metrics-index) in New Relic with minimal changes</td>
    </tr>
    <tr>
      <td>**Service coverage**</td>
      <td>~30 Azure services</td>
      <td>All Azure services supported by Azure Monitor</td>
    </tr>
    <tr>
      <td>**Minimum polling interval**</td>
      <td>5 minutes</td>
      <td>1 minute</td>
    </tr>
  </tbody>
</table>

## Limitations of Azure Polling

Azure Polling has known limitations that impact data reliability and cost. As Azure Polling will be deprecated in a future release, we recommend migrating to Azure Monitor.

<CollapserGroup>
  <Collapser
    id="resource-type-config"
    title="Resource type-specific configuration"
  >
    Each polling configuration is scoped to a single resource type and cannot be generalized.

    <Callout variant="tip">
      Azure Monitor uses the Metrics Batch API to retrieve metrics across multiple resources in a single request, eliminating the need for resource-specific configurations.
    </Callout>
  </Collapser>

  <Collapser
    id="hardcoded-metrics"
    title="Hardcoded metrics"
  >
    Metrics are statically defined at configuration time. Changes to available metrics on the Azure side are not reflected automatically.

    <Callout variant="tip">
      Azure Monitor retrieves all available metrics dynamically, without requiring manual updates.
    </Callout>
  </Collapser>

  <Collapser
    id="limited-coverage"
    title="Limited service coverage"
  >
    Azure Polling supports approximately 30 resource types with no planned expansion.

    <Callout variant="tip">
      Azure Monitor supports all resource types that Azure itself supports.
    </Callout>
  </Collapser>

  <Collapser
    id="high-api-volume"
    title="High API call volume"
  >
    The polling model issues a large number of individual API calls, which can trigger Azure API throttling and increase costs.

    <Callout variant="tip">
      Azure Monitor's Metrics Batch API consolidates requests across multiple resources, significantly reducing API call volume and the risk of throttling.
    </Callout>
  </Collapser>

  <Collapser
    id="data-loss-risk"
    title="Risk of data loss"
  >
    Azure Polling tracks timestamps at the resource level. When metrics for a given resource type are reported at different time frequencies, data points may be missed or dropped during collection.

    <Callout variant="tip">
      Azure Monitor tracks timestamps at the individual metric level, ensuring accurate alignment regardless of varying reporting frequencies across metrics within the same resource.
    </Callout>
  </Collapser>

  <Collapser
    id="new-relic-support"
    title="New Relic support"
  >
    Azure Polling will be deprecated soon, so there would be minimal support if there are any new requests.

    <Callout variant="tip">
      Azure Monitor will be improved actively so that they provide first class experience to customers.
    </Callout>
  </Collapser>
</CollapserGroup>
