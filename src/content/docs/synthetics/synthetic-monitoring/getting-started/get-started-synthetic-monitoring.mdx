---
title: Get started with synthetic monitoring
tags:
    - Synthetics
    - Synthetic monitoring
    - Getting started
    - Synthetic monitoring tutorial
translate:
    - jp
metaDescription: "New Relic's synthetic monitoring capabilities give you automated, scriptable tools to monitor your websites, critical business transactions, and API endpoints."
redirects:
    - /docs/new-relic-only/new-relic-synthetics/getting-started/new-relic-synthetics
    - /docs/synthetics/new-relic-synthetics/getting-started/new-relic-synthetics
    - /docs/synthetics/new-relic-synthetics/getting-started/introduction-new-relic-synthetics
    - /docs/synthetics/synthetic-monitoring/getting-started/get-started-new-relic-synthetic-monitoring
    - /docs/synthetics
    - /docs/synthetics/synthetic-monitoring/getting-started
---

You can think of our synthetic monitors as crash test dummies for your websites, applications, and API endpoints. When you deploy a synthetic monitor, an API check or Chrome browser runs through your most important workflows, testing your critical endpoints and reporting the results to New Relic. By mimicking the behaviors of customers, your monitors test how well your web apps handle traffic. 

The below tutorial walks you through creating your first monitor. Each step shows the process to create one of our monitors, either a simple browser, step, or scripted browser monitor. 

## Create your first monitor [#create]
<Steps>

<Step>

### Check page load performance with simple browser monitors 

<SideBySide>
    <Side>
        Simple browser monitors are pre-built scripted browser monitors that make a request to your site with Google Chrome. Compared to our ping monitors, scripted browser monitors check for both availability and performance of a single page, including a full page load.
    </Side>
    <Side>
<Video
    type="wistia"
    id="8x0kt4wrs4"
/>

<figcaption>
Go to **[one.newrelic.com](https://one.newrelic.com) > [one.newrelic.com/synthetics-nerdlets](https://one.newrelic.com/synthetics-nerdlets)** > Create a monitor > (choose **Page load performance** for simple browser). 
</figcaption>
</Side>
</SideBySide>

</Step>

<Step>

### Mimic customer behavior with step monitors

<SideBySide>
    <Side>
    Step monitors let you interact with different elements across a web page without needing to write your own scripts. This is a good beginner option if you want your monitor to report on specific customer flows, but don't need your monitor to conditionally respond to your page's behaviors. In the video, you can create a step monitor that uses XPaths to:

    * Go to `http://telco.nrdemo.com/`
    * Click the **Contact** button in the top nav
    * Input text in the **Send us a Message** fields
    * Click **Send message**
    </Side>
    <Side>
<Video
    type="wistia"
    id="8x0kt4wrs4"
/>

<figcaption>
Go to **[one.newrelic.com](https://one.newrelic.com) > [one.newrelic.com/synthetics-nerdlets](https://one.newrelic.com/synthetics-nerdlets)** > Create a monitor > (choose **User step execution** for step monitor). 
</figcaption>

</Side>
</SideBySide>
</Step>

<Step>

## Create a scripted browser monitor [#browser]

Scripted browser monitors let you test the availability, functionality, and performance of critical user flows in your web application. When your script runs, New Relic creates a fully virtualized Selenium-driven Google Chrome browser that navigates your website and follows each step of the script.

<Tabs stacked>
	<TabsBar>
		<TabsBarItem id="URL">Go to a URL</TabsBarItem>
		<TabsBarItem id="element">Interact with a page element</TabsBarItem>
		<TabsBarItem id="wait">Wait for a page element</TabsBarItem>
        <TabsBarItem id="login">Log in to a website</TabsBarItem>

	</TabsBar>

	<TabsPages>
		<TabsPageItem id="URL">
        The beginning of all scripts specifies the URL the monitor should check.

        ```js
        //Visit `http://telco.nrdemo.com/`
        $webDriver.get("http://telco.nrdemo.com/");  
        ```
        </TabsPageItem>
		<TabsPageItem id="element">
        Once your monitor goes to a URL, it scans for then interacts with the page's available elements.
            
        ```js
        $webDriver.get("http://telco.nrdemo.com/").then(function(){
        //Find a link with display text that matches `About`
            return $webDriver.findElement($selenium.By.linkText("About")).click();
        //Click the link that matches `About`
        }).then(function(){
            return $webDriver.findElement($selenium.By.partialLinkText("Home")).click();
        });
        ```
		</TabsPageItem>
		<TabsPageItem id="wait">
        Your script may need to wait an interval before reporting a failure, since images or dynamic elements can take time to generate on a page.

        ```js
        $webDriver.get("http://telco.nrdemo.com/").then(function(){
            return $webDriver.findElement($selenium.By.id("supportDropDown")).click();
        }).then(function(){
        //Wait up to 20000 seconds for the FAQ page to appear
            return $webDriver.waitForAndFindElement($selenium.By.id("supportFAQLink"), 20000).then(function(aboutPage){
                return aboutPage.click();
            })
        });
        ```
		</TabsPageItem>
        <TabsPageItem id="login">
        You can take your stored secure credentials to test certain page elements on your web apps. For scripting purposes, we recommend that you create new credentials unique to your script. Don't use your personal credentials or reuse credentials when running a monitor.

        ```js
        $webDriver.get("http://example.com/login.jsp").then(function(){
            return $webDriver.findElement($selenium.By.name("username")).sendKeys($secure.SECURE_USERNAME);
        }).then(function(){
        //Find the password field by specifying its name, then submits a secured password.
            return $webDriver.findElement($selenium.By.name("password")).sendKeys($secure.SECURE_PASSWORD);
        }).then(function(){
        //Find and click the login button.
            return $webDriver.findElement($selenium.By.xpath("//inp[@value='Login']")).click();
        });
        ```
		</TabsPageItem>
	</TabsPages>
</Tabs>
<figcaption>
Go to **[one.newrelic.com](https://one.newrelic.com) > [one.newrelic.com/synthetics-nerdlets](https://one.newrelic.com/synthetics-nerdlets)** > Create a monitor > (choose **User flow/functionality** for scripted browser). 
</figcaption>

</Step>

</Steps>

## What's next? [#next]

<Callout variant="tip">
  To view other scripted browser examples, check out the [Quickstarts Synthetics library](https://github.com/newrelic/quickstarts-synthetics-library) in New Relic’s Github repository. You may also view tips from New Relic support engineers in the [Level Up Relic Solutions](https://discuss.newrelic.com/search?q=Synthetics%20category%3A463) section of the New Relic Online Technical Community.
</Callout>

API TESTS, Alerts + synthetic monitoring, scripted browser (couple points that convert to sign ups)

banner at the top/bottom 