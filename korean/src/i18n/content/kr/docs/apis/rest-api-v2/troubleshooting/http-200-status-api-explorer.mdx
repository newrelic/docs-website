---
title: API 탐색기가 있는 200 상태
type: troubleshooting
tags:
  - APIs
  - REST API v2
  - Troubleshooting
metaDescription: 'If you received an HTTP 200 status code, but the message body is empty, make sure you have specified an Accept header in your GET request.'
translationType: machine
---

## 문제

`HTTP 200` 상태 코드를 받았지만 메시지 본문이 비어 있습니다.

## 해결책

이 문제를 수정하려면 `application/json` 또는 `application/xml` 가 포함된 `Accept` 헤더를 `GET` 요청에 추가하세요. 예를 들어:

```
curl -X GET 'https://api.newrelic.com/v2/applications/YOUR_APP_ID/metrics/data.json'
     -H 'X-Api-Key:YOUR_API_KEY' -i
     -H 'Accept: application/json'
```

## 원인

이것은 허용되는 콘텐츠 유형을 지정하지 않는 기본 `GET` 헤더를 사용할 때 발생할 수 있습니다. HTTP 헤더에 대한 자세한 내용은 HTTP 헤더를 [사용한 콘텐츠 협상](https://restfulapi.net/content-negotiation/) 에 대한 이 자습서를 참조하세요.