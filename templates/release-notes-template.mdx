---
# Release notes file name: <subject>-<version>.md You must use the subject and version exactly as reported by the agent or product. The file name is used to generate the release notes page URL, so it must be unique for each release. For example, PHP agent v10.3.0.315 would be php-agent-10-3-0-315.md or php-agent-1003015.md.
# ─── Required — every release note must have these ───
subject: <Agent or product name> For example, PHP agent. 
releaseDate: 'YYYY-MM-DD'
version: <version string, exactly as reported by the agent or product>
features: []  # New features in this release — leave as [] if none; if you have entries, replace [] with:
  # - feature 1
  # - feature 2
enhancements: []  # Improvements/enhancements. Leave as [] if none, otherwise replace with:
  # - enhancement 1
  # - enhancement 2
bugs: []  # Bug fixes in this release — leave as [] if none, otherwise replace with:
  # - bug fixed 1
  # - bug fixed 2
security: []  # Security fixes in this release — leave as [] if none, otherwise replace with:
  # - security fix 1
  # - security fix 2

# ─── Optional ───
downloadLink: 'https://...'   # If you have agent download link, add it here. If you don't have a link, delete this line.
category: # Only needed when this subject has multiple independent release tracks — e.g. NRDOT (Host / Collector) or Streaming for Browser (Shaka / HTML / DASH / Bitmovin). Delete this line if this subject only ever has one release track. Lets consumers FILTER by category — the API does not return category on an individual release, only accepts it as a filter, so don't rely on it round-tripping back.
---

{/* Fill in every field above first. Each bullet below should also appear, in short form, in the matching array above. Be specific — "various bug fixes" or "general improvements" tells a reader nothing. */}
{/* Follow the technical documentation rules for writing release notes. */}

### New features

{/* One bullet per `features` entry. Delete this section if empty. */}

- Item 1
- Item 2
- Item 3

### Improvements and enhancements

{/* One bullet per `enhancements` entry. Delete this section if empty. */}

- Item 1
- Item 2
- Item 3

### Bug fixes

{/* One bullet per `bugs` entry — state what was broken. Delete this section if empty. */}

- Item 1
- Item 2
- Item 3

### Security updates

{/* One bullet per `security` entry. Delete this section if empty — must match whether `security` is empty. */}

- Item 1
- Item 2
- Item 3

{/* This is a template, remove all the comments and placeholders before publishing. */}